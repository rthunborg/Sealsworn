---
project_name: 'Sealsworn'
user_name: 'Rasmus'
date: '2026-07-19'
sections_completed:
  - technology_stack
  - engine_rules
  - performance_rules
  - organization_rules
  - testing_rules
  - platform_rules
  - anti_patterns
  - save_serialization_rules
  - presentation_view_model_rules
  - settings_rules
  - generation_rules
  - run_progression_rules
  - class_kit_rules
  - rules_kernel_rules
  - content_loot_reward_rules
  - inventory_equipment_rules
  - consume_destroy_rules
  - risk_economy_rules
  - affinity_rules
  - run_end_meta_profile_rules
  - larval_avatar_finale_rules
  - run_flow_scene_hud_rules
  - interactive_tactical_combat_rules
  - live_board_render_reward_hud_rules
  - readiness_perf_seed_fairness_rules
  - playable_presentable_rules
status: 'complete'
rule_count: 367
optimized_for_llm: true
architecture: '_bmad-output/game-architecture.md'
refreshed_after: 'epic-14 "Playable & Presentable" (the SECOND pre-ship backlog epic, after Epic 13; 11 stories in two bands — Band 1 14-1..14-7 makes the loop FINISHABLE+READABLE [corpse-clear+wait, attack preview+reject cues, combat log+hit feedback, per-run seed variety, run-end beat+summary, live route map, full-backpack escape hatch], Band 2 14-8..14-11 makes it PRESENTABLE [hero-select rebuild, outpost cleanup, player HUD+range highlights, shared UI theme+semantic layout]). Two domain touches, both the 4.3 idiom, additive: 14-1 WaitCommand -> hero_waited (the F1 turn-advance backstop) + corpse-clearing (blocks_movement released on death), and 14-7 DeclineRewardCommand -> reward_declined (the full-backpack escape hatch closing the 13-2 soft-lock). The DomainEvent.Type enum grew by exactly those two append-only tail members -> Type.size() == 44 (tail reward_declined, index 43). Per-run seed variation (14-4 RunSeedSource) makes live runs vary from OS entropy with NO save change (root_seed is already field #5; the 23-key gate stays 23, SCHEMA_VERSION 1); the other nine stories are pure presentation/flow over scripts/ui/. INVARIANTS HELD (the ONE justified exception is 14-1 re-pinning the combat-replay seed 512->24680 under corpse-clear): the 23-key RunSnapshot gate, 7 named RNG streams, 16-key board VM, SCHEMA_VERSION 1, empty conditions/ + one-file operations/, no new autoload/content-family, and every generator/route/finale fingerprint byte-identical. Suite 205 PASS / 0 ^FAIL. MVP verdict stays READY_WITH_GATES; the Band-1/2 on-device human playtest is the standing open obligation'
---

# Project Context for AI Agents

This file contains critical rules and patterns that AI agents must follow when implementing game code in this project. Keep it lean, specific, and focused on mistakes agents might otherwise make.

Canonical location: root `project-context.md`. Do not create duplicate project context files under `_bmad-output/`; duplicate context risks stale or conflicting agent instructions.

---

## Core Direction

Sealsworn is a mobile-first, desktop-playable, turn-based dark fantasy roguelite RPG. The player controls one hero through seeded, forward-only procedural levels with fog of war, tactical positioning, weapon-shaped basic attacks, risk/reward routing, loot, passive rule-benders, and meta progression.

Story spine:

> The Labyrinth was not built to keep heroes out. It was built to keep something in.

Design guardrails:

- Game loop first: the game must be fun for players who ignore all lore.
- Story is optional discovery, not required reading.
- Cutscenes and control-loss narrative moments should be skippable.
- Tone is dark medieval fantasy with a cosmic horror undercurrent.
- Ancient containment technology should read as relic-magic or Wardenwork, not science fiction.
- Meta progression expands variety and knowledge more than raw power.
- Passives are awakened memories, oaths, scars, relic echoes, forbidden instincts, or broken protocols.
- Affinities are failed containment protocols and safety measures.
- MVP scope must prove the complete rough roguelite loop before content sprawl.

## Technology Stack & Versions

- Production engine: Godot 4.6.3 stable standard build.
- Primary language: typed GDScript.
- Production project root: `C:/Sealsworn/godot/`.
- Target platforms: iOS/Android mobile and tablet first; Windows desktop/laptop parity.
- MVP mode: offline-first single-player. No accounts, multiplayer, cloud saves, leaderboards, or live-service dependency.
- Prototype: `prototype/` is React/Vite/TypeScript validation evidence only. Production Godot code must not depend on prototype source.
- AI tooling: GoPeak Godot MCP and Context7 are selected once the Godot project exists; Rasmus also has premium access to Codex, Claude, Claude Design, Google Stitch, Google Gemini, and related tools.
- Do not use Godot .NET/C# for MVP. Use the standard GDScript build unless architecture is explicitly revised.
- Project shape (post-Epic 2): main scene is `res://scenes/app/boot.tscn`; viewport is 1080x1920 portrait; renderer is Mobile; `stretch/mode=canvas_items`, `aspect=expand`. Epic 11 added the FIRST real presentation/scene layer that DRIVES a hands-off run on screen: the flow scenes under `godot/scenes/` (`scenes/app/main.tscn` boots into `scenes/ui/hero_select.tscn`; `scenes/ui/route_map.tscn`; the gameplay SHELL `scenes/game/gameplay_shell.tscn` = board + in-run HUD; `scenes/ui/run_end.tscn` = the fail-loud non-terminal dead-end landing; `scenes/ui/outpost.tscn` = the polished meta dashboard the run-end return lands on). The presenters (`scripts/ui/presenters/`, `Control` nodes) OBSERVE domain outcomes; the flow SEAMS (`scripts/ui/flow/`: `RunFlowController`, `RunFlowRouter`, `RunEndProfileBridge`, `OutpostSpendBridge` — all thin `RefCounted`) own NO gameplay decision a command/orchestrator does not. Presentation stays scene-observes-domain: no scene node is authoritative for tactical/run truth. Epic 12 (inserted between Stories 10.3 and 10.4 by the 2026-07-07 sprint change) added the INTERACTIVE tactical-combat surface on top of that flow layer: the scene-free step-driven `InteractiveCombatSession` (`scripts/run/`, one action per tap through `TacticalCommandBridge`), the additive `RunOrchestrator.begin_interactive_combat_node`/`finish_interactive_combat_node` seams, the class-kit → live-combat `CombatLoadout` source (`scripts/run/`), and the strengthened LoS-aware `ReferenceCombatDriver` winnability PROOF harness (`scripts/run/`). Still NO new autoload; the tap-loop is the on-screen path, the reference driver is a headless proof harness. Epic 13 (post-MVP-close, inserted after the first human desktop playtest) MATURES that on-screen path into a real rendered, tappable, reward-resolving surface: 13-1 replaces the text-only board region with a LIVE TILE-GRID render + tap hit-test wired into the 12-1 interactive seams (the pure `RefCounted` `scripts/ui/view_models/tactical_board_grid_fit.gd` grid-fit + `tactical_board_tap_router.gd` tap-routing DECISION seams + the thin `scripts/ui/presenters/tactical_board_grid.gd` draw `Control`; the draw surface + hit-test share ONE `TacticalBoardZoomState`; the approved board art is now imported + its `*.png.import` sidecars committed, loaded via a guarded `load()`), and 13-2 wires the post-victory reward + passive Consume/Destroy HUD (the exact-key `scripts/ui/view_models/reward_hud_view_model.gd` render projection + the execute-capable `scripts/ui/flow/reward_resolution_bridge.gd`) over the pinned Epic-6/7 contracts, firing the reward GENERATE at the INTERACTIVE-shell victory boundary ONLY. Both are pure presentation + flow-wiring — NO new command/event/RNG-stream/autoload/save-key/board-VM-key, every seed-regression fingerprint byte-identical; 13-1 also settled the durable version-control convention that `*.gd.uid` script-UID sidecars are COMMITTED (never gitignored). See the dedicated Live-Board-Render/Reward-HUD section for the durable rules. Epic 10's first tranche (10-1/10-2/10-3/10-8) added the LOCAL/OFFLINE readiness diagnostics + consolidated regression/fairness harnesses (`PerformanceBudgetReport` in `scripts/diagnostics/`; the consolidated `test_seed_regression_suite.gd` + `test_generator_fairness_batch.gd` under `tests/integration/`) — read-only, dev-build-gated observers that touch no gameplay/RNG/save state. Epic 10 is now COMPLETE: the second tranche (10-4 comprehension checklist, 10-5 accessibility audit, 10-6 the final MVP-readiness GATE, 10-7 the asset/audio/placeholder & UX GATE) is READINESS/AUDIT/GATE work that authored planning-artifact docs + AT MOST one additive read-only test (10-5's run-flow cue-coverage test) and changed NO production `godot/` gameplay/RNG/save/content/generator/view-model/presenter/scene/`export_presets.cfg` path — the MVP verdict is `READY_WITH_GATES` (green suite + complete winnable live loop + held pre-export invariants + regression-enforced difficulty non-goal; every open gap is a documented physical-device / human-eyes / produced-asset limitation with a named owner, none a hard blocker). See the dedicated Readiness/Perf/Seed/Fairness section for the durable rules.
- Registered autoloads (unchanged through Epic 9): `GameSession`, `SceneManager`, `SaveManager`, `AudioManager`, `SettingsManager`, `Diagnostics`. Keep them thin; they delegate gameplay decisions to domain services. Generation, run-progression, the Epic-5 class/kit/passive layer, the Epic-6 loot/reward/inventory/consume-destroy layer, the Epic-7 risk-economy/curse/event/affinity layer, the Epic-8 outpost/meta-progression/run-summary layer, AND the Epic-9 Larval Avatar boss finale all added NO new autoload — `LevelGenerator`/`ManualSeedLoader`/`RouteGenerator`, every run-domain command (now incl. the Epic-8 run-end quartet `CompleteRunCommand`/`AwardMetaProgressCommand`/`MergeRunDiscoveriesCommand`/`RecordFirstDeathCommand` PLUS the Epic-9 `BossNodeEnterCommand`/`RecordFirstVictoryCommand`, alongside `PickupItemCommand`/`ResolveRewardCommand`/`ConsumePassiveCommand`/`DestroyPassiveCommand`/`UseConsumableCommand`/`ChooseEventOptionCommand`/`AffinityHazardDamageCommand`), `RunOrchestrator`, the FIFTEEN content repositories (Epic 9 added `BossRepository`), `RewardOfferBuilder`, `AffinityEffectResolver`, `DarknessVisibilityLayer`, `DarknessFairnessQuery`, the Epic-9 boss chain (`BossArenaBuilder`/`BossEncounterRequest`, `BossPhaseResolver`, `BossTurnResolver`/`BossCommandAdapter`, `BossAi`, the `BossAttemptDiagnostics` dev recorder, the `FirstVictoryRevealBeat` read DTO), the Epic-8 `ProfileRepository` (the cross-run `user://profile.json`), the pure `MetaAwardRules`/`UnlockProgressRules` calculators, the `RunSummary`/`RunEndOutcome`/`FirstDeathNarrativeBeat`/`OutpostViewModel` read surfaces, and the `RulesResolver` are pure `RefCounted` services/DTOs called directly (the rules resolver + inventory + pending reward/event offer + risk-economy state are seated ON the `RunState`, never on a global; the cross-run `ProfileSnapshot` lives OUTSIDE the run, on its own repository). `SaveManager` gained thin route-position delegators (`autosave_route_position`, `resume_route_position` -> `RunResumeService`) but owns no run logic; Epics 8-9 added NO `SaveManager` profile delegator (the caller drives `ProfileRepository` directly — no live boot-flow wiring exists yet). The Epic-9 boss VICTORY resolution is likewise CALLER-DRIVEN behind the run-end seam via the additive thin `RunOrchestrator.resolve_boss_victory()` (clears the boss route node, then `resolve_run_end(victory)`) — still NOT auto-wired into `run_to_completion`; the live boss fight is driven by explicit turns (an integration test + thin continuation, not a shipped auto-play loop).
- Static-content storage (through Epic 9): `godot/data/source/` and `godot/data/resources/` carry per-family SUBDIRECTORIES (`affinities`, `classes`, `enemies`, `items`, `level_recipes`, `passives`, `reward_tables`, `support_items`, `weapons`) but ZERO content files — they are EMPTY SCAFFOLDING. Every definition (enemies, weapons, supports, level recipes, the Epic-5 classes + starting passives, the full Epic-6 loot roster — armor, jewelry, consumables, pickups, gold rewards, reward tables, the Destroy-outcome table — the full Epic-7 roster: the 4 MVP affinities + neutral `none` (`AffinityDefinition`), the 3 risk/reward events (`EventDefinition`), the curse content (`CurseDefinition`) — AND the Epic-9 Larval Avatar boss content family: `BossDefinition` + its ordered `BossPhaseDefinition`/`BossActionDefinition` sub-resources, via `BossRepository`) is STILL a code constant authored as a baseline `_baseline_definitions()`/`create_baseline_*` on its repository/definition, registered through the `ContentRepository` boundary — there is STILL NO `.tres`/JSON content pipeline. Epic 9 IS the first epic since Epic 6/7 to add NEW `*Definition` families + a repository (`BossRepository` — the FIFTEENTH content repository, mirroring `EnemyRepository` verbatim); the boss is a SINGLE code-constant baseline (`boss_id == "larval_avatar"`). Epic 8 added NO content family (its Echo/Seal-Fragment/class-mastery/unlock content is tracked BY ID off `content_discovered` events with NO roster/repository; the single first-death narrative line is one const on `FirstDeathNarrativeBeat`); Epic 9 likewise authored NO narrative CONTENT roster for its single first-VICTORY line (one const on `FirstVictoryRevealBeat` — no `NarrativeRepository`/codex/JSON pipeline). The JSON-source -> typed-Resource mirror remains a deliberately-deferred later decision (do not introduce it early; when content moves to a data pipeline the fail-loud duplicate-id guard becomes load-bearing — see below). The `data/` roots are still asserted as required structure by `test_project_structure.gd`.
- `godot` is NOT on the Bash/`where` PATH on this machine; it resolves only as `C:\Users\Rasmus\bin\godot.cmd` via PowerShell. Run the headless test command through PowerShell (`powershell.exe -NoProfile -Command ...`), not the Bash tool's PATH lookup.

## Critical Implementation Rules

### Engine-Specific Rules

- Use typed GDScript for gameplay, tooling, UI presenters, and tests.
- Godot scenes, `Control` nodes, audio, VFX, and animation are presentation. They must not own authoritative tactical state.
- The scene-independent domain model owns tactical truth: board state, entities, turns, RNG, rules, saves, and run progression.
- Presentation observes domain state/events and submits commands through a command bridge.
- Use Godot signals for presentation/UI feedback, not for hidden domain control flow.
- Keep autoloads thin. Acceptable autoloads: `GameSession`, `SceneManager`, `SaveManager`, `AudioManager`, `SettingsManager`, `Diagnostics`. They delegate gameplay decisions to domain services (e.g. `SaveManager.resume_run()` delegates to `RunResumeService`; `SettingsManager` delegates to `SettingsRepository`/`SettingsApplyService`).
- Do not serialize scene nodes as save-game truth. Save versioned domain snapshots only.
- Use custom Godot `Resource` assets for typed editor/runtime definitions, mirrored from JSON/CSV source data when useful.
- Use the Mobile renderer first; keep Compatibility as fallback if low-end device testing demands it.

### Determinism & Simulation Rules

- Gameplay actions are commands. Commands validate before mutation and return `ActionResult`.
- Successful commands emit deterministic past-tense `DomainEvent` records.
- Domain events drive state changes, presentation, replay, logs, saves, tests, and future analytics.
- Use named RNG streams derived from the root seed: `map`, `level`, `combat`, `loot`, `rewards`, `events`, and `cosmetic`.
- Gameplay-affecting randomness must use its assigned stream. Cosmetic-only randomness cannot affect outcomes, rewards, unlocks, or progression.
- Headless simulation is a first-class target. It must run without rendering, audio, UI scenes, presentation nodes, or scene-tree-only state.
- Manual seed/debug runs may support replay, practice, and debugging, but they must not grant meta progression unless explicitly allowed.

### Rules, AI, and Generation Rules

- Use the Sealsworn rules kernel for passives, items, affinities, curses, classes, bosses, Consume/Destroy choices, and tactical rule-benders.
- Rules use explicit trigger windows, conditions, targets, operations, durations, stacking, conflict handling, and stable resolver ordering.
- Rule-driven outcomes must be explainable in player/debug language.
- Procedural generation runs in phases: route, recipe, layout, pathing, blockers, hazards, enemies, rewards, affinity rules, validation, final snapshot. The phase vocabulary is fixed in `GenerationResult.PHASE_*`; a failure reports the failing phase.
- Generator validation failures must report seed, phase, reason, and compact diagnostics (counts/coords/ratios) — NEVER a full terrain-grid dump. See the dedicated Procedural Generation section below for the full Epic-3 contract.
- Enemy AI uses state/phase-constrained utility scoring over valid tactical actions.
- Every AI decision must be explainable with top score, chosen action, and major reasons.
- Shared tactical query services handle pathfinding, line of sight, threat maps, valid moves, attack previews, and tile scoring.

### Save, Snapshot & Serialization Rules (hardened in Epic 2)

- Snapshots are pure reads. Composing or restoring a snapshot (`RunSnapshot.from_between_level`, `RunResumeService.resume`, `*.to_snapshot`, `try_restore`) must consume NO RNG draws, execute NO commands, advance NO turns, and mutate neither the source domain state nor the save file.
- JSON numbers are IEEE-754 doubles (52-bit mantissa). Any save field that can exceed 2^53 — notably the RNG `root_seed` and each per-stream `state` — MUST be string-encoded (decimal string) in `to_snapshot()`, or `JSON.stringify`/`parse_string` silently truncates it and breaks resume determinism. Restore must read-tolerate the legacy numeric form (int or integral float) AND the string form. Small bounded fields (per-stream `seed` <= 2^31, `draw_index`) may stay numeric.
- ALWAYS JSON-round-trip snapshots in tests (`JSON.stringify` then `parse_string`), never assert only on native dictionaries. A latent int64 precision/resume bug survived Epic 1 precisely because tests round-tripped native dicts instead of real JSON.
- Restore exposes NO partial corrupt state. Each restore step propagates the FIRST validator's structured error verbatim and returns ZERO restored domain objects on failure (the "no partial corrupt state becomes active" guarantee). Restore order for a run: read repository -> strict embedded `TacticalSnapshot.parse` -> `BoardState.try_from_snapshot` -> `RngStreamSet.try_restore`.
- Route an embedded tactical payload through the STRICT `TacticalSnapshot.parse`, never the lenient run-level `RunSnapshot.parse`. Run-level parse is intentionally forward-compatible for run fields; trusting it for the tactical payload could "restore" a corrupt board into a broken shape.
- Compose, do not fork. The between-level save embeds the Epic 1 `TacticalSnapshot` under `level_state["tactical_snapshot"]` (`RunSnapshot.TACTICAL_SNAPSHOT_KEY`). Do NOT flatten tactical board/turn/telegraph/event fields onto the run save or invent a parallel scene-owned tactical save format.
- RNG authority on resume is the run-level `RunSnapshot.rng_streams`. At a between-level boundary it equals the embedded tactical `rng_streams` by construction (both written from one `streams.to_snapshot()` read); a test asserts that equality. Restore the run-level streams as the live gameplay streams.
- Repositories own atomic writes: write to `<path>.tmp`, then backup/replace via `DirAccess.rename_absolute` with `<path>.bak` rollback. Run autosave is `user://run_autosave.json` (`SaveRepository`); settings are `user://settings.json` (`SettingsRepository`). The two files and their repositories are strictly independent — neither reads, writes, truncates, or renames the other.
- Read errors are structured `ActionResult` codes (`save_not_found`, `save_open_failed`, `save_parse_failed`, `unsupported_save_schema`, `invalid_tactical_snapshot`, `invalid_rng_snapshot`), not exceptions. Schema-version changes need migration tests.
- Exercising the real parse-failure path emits one expected `ERROR: Parse JSON failed` line to stderr (Godot's `JSON.parse_string` on deliberate non-JSON bytes). The test still passes and the runner exits 0 — do NOT read that stderr diagnostic as a suite failure.

### Procedural Level Generation Rules (Epic 3)

The level generator (`scripts/generation/level/`) is a scene-free, deterministic, repository-fed pipeline. v0 ships Small (fixed 8x8, `small_combat_basic`) and Medium (fixed 14x12, `medium_combat_basic`) recipes only; Large/Huge are deferred (FR39).

- LEVEL = PURE FUNCTION OF `(root_seed, recipe)`. A generated candidate is fully determined by the seed, the resolved recipe, and the `level`-stream start state. Same `(seed, recipe)` -> byte-identical layout AND identical final `GenerationResult` (same attempt count, same payload or same error). This invariant is non-negotiable and is the foundation of seed regression, manual-seed replay, and bounded retry.
- ALL layout-affecting randomness draws through `GenerationRequest.draw_layout_int` / `draw_layout_float`, which route EXCLUSIVELY through `RngStreamSet.STREAM_LEVEL`. Generators and placers NEVER call `randi()`/`randf()`, NEVER construct a `RandomNumberGenerator`, and NEVER touch another stream. The `rewards`/`loot` streams are RESERVED for runtime reward/loot resolution (Epic 6), NOT generation placement.
- The seed is `GenerationRequest.root_seed`, surfaced via `request.level_seed()` (v0: identity). There is no bare `request.seed`. On a SUCCESS `GenerationResult` the seed lives in `payload.level_seed` (a String) and `result.seed` is `""`; `result.seed` is populated only on the ERROR path. Read `payload.level_seed` on success — this split is a known wart (deferred) that has cost real test bugs.
- FIXED DRAW ORDER (both generators are siblings; reordering or inserting a draw silently changes every pinned fixture): (1) blocker count, (2) blocker positions, (3) wrinkle kinds, (4) wrinkle positions, (5) enemy count, (6/7) enemy position+kind interleaved per enemy, (8) reward count, (9) reward positions. Every count draw fires even when its band collapses to one value, so the stream advances identically across recipes. Positions use a rejection-free SHRINKING CANDIDATE POOL (row-major order, picked cell removed) shared across blockers/wrinkles/enemies/rewards, so nothing ever collides and the reserved central corridor + entrance + exit + WALL cells are pre-excluded.
- Entrance/exit are deterministic (NOT seed-randomized) cells on a reserved blocker-free central corridor row; seed-to-seed divergence comes from interior blockers/wrinkles/placement. This fairness-by-construction is coupled to the fixed even-height footprint; a future jittered size must re-establish the reserved-corridor invariant and re-pin fingerprints.
- BOUNDED DETERMINISTIC RETRY (`LevelGenerator`): a rejected candidate is re-attempted up to `MAX_GENERATION_ATTEMPTS` (8) with a per-attempt seed mix. ATTEMPT 0 USES THE UNPERTURBED `level_seed()` EXACTLY (`_attempt_seed(seed, 0) == seed`) — this preserves the pinned terrain fingerprints and all 3.2-3.5 layout/placement tests with NO re-pin. This is the hard invariant. The cap keeps the worst case inside the NFR4 < 3s budget. Unrecoverable errors (missing/empty enemy repo, structural input/recipe error) short-circuit to attempts=1 instead of burning all 8.
- `LevelValidator` is the COMPREHENSIVE, SIZE-AGNOSTIC, PURE-QUERY validator run on every built candidate. It draws NO RNG, runs NO commands, mutates nothing (same purity contract as snapshots) — wiring it in must keep the `level`-stream draw-count assertions green. It reuses the Medium generator's `validate_readability` (one canonical readability bound) and the placer's reward check, strengthening reward reachability to be ENTITY-AWARE for mandatory rewards. Checks run in the FIXED `check_order()`; the first failure short-circuits with a stable lower-snake code mapped to a phase via `LevelValidator.phase_for_code()`. Diagnostics are compact counts/coords, never a grid dump.
- SAFE-FIRST-REVEAL v0 SEMANTIC (ratified, NOT a silent weakening): the check rejects only the entrance cell being HAZARD or occupied by an entity. A threat merely ADJACENT to the entrance is PERMITTED (seen on spawn within LoS radius 4, FR5; engaged by choice). A `Chebyshev<=1` enemy guard was deliberately REJECTED — it would fail fair baseline candidates and force attempt-0 re-rolls that drift the pinned fingerprints.
- ENTITY OCCUPANCY on built board snapshots (CORRECTED contract — the opposite of an earlier note): `build_board_snapshot` MUST set the matching `occupant_id` on each blocking entity's cell, mirroring `BoardState.to_snapshot()`. `try_from_snapshot` records the cell occupant, strips the cell to `""`, then cross-checks it against blocking entities and REJECTS a blocking entity whose cell carried `""` (`invalid_cell_occupant`). Entity-aware reachability reads occupancy from the ENTITY list (`board.entities()`), never the stripped cell field.
- Built board snapshots are validated through the STRICT `BoardState.try_from_snapshot` (validate-then-reject, never coerce; the Story 1.3 precedent). A malformed cell is a generator bug — surface the validator error, don't fix the snapshot. The emitted payload is PURE serializable data (board snapshot dict + entrance/exit/blockers + `rewards` markers + size_class/recipe_id/level_seed) that survives a JSON round-trip; never put the live `BoardState`/`RefCounted` in the payload.
- `recipe.wall_density` is INTENTIONALLY INERT in v0 for BOTH generators — the `blocker_budget_min..max` band is the authoritative count bound (honoring density would clamp to a constant count and kill AC1 divergence). The Medium AC2 `excessive_blockage` ratio (`MAX_INTERIOR_WALL_RATIO = 0.35`) is the independent readability backstop, not derived from `wall_density`. Touching this field's effect requires widening the band AND re-pinning Small + Medium fingerprints deliberately.
- Tactical wrinkles (`TacticalWrinklePlacer`, shared) draw kinds from the recipe allowlist filtered to the v0-realizable subset (`choke_point`/`blocker_cluster`/`flank_route`/`hazard`) realized as WALL/HAZARD terrain. HAZARD is board-valid, WALKABLE, and sight-TRANSPARENT (only WALL blocks occupancy/LOS) — never make HAZARD block movement; its danger is the rules kernel's job. `door`/`affinity_placeholder`/`enemy_formation`/`reward_behind_danger`/`risky_side_branch` are NOT realized as terrain in v0.
- Enemies + rewards (`EntityRewardPlacer`, shared) are placed deterministically from the residual pool. ENEMIES become board entities (`TacticalEntityState`, `entity_type = ENEMY`) resolved THROUGH `EnemyRepository` (a null/empty repo with a positive enemy budget is a structured error, never a silent no-placement); the enemy kind set is the canonical `PLACEMENT_ENEMY_ORDER`, independent of registration order. REWARDS are abstract payload markers (`{x, y, optional}`), NOT board entities — there is no reward entity type and the generation layer adds none. The GENERATION layer is NOT reward-table-driven: reward-placement rules live on `LevelRecipeDefinition`, and the placer emits abstract markers — it does NOT read `RewardTableDefinition` (that concrete loot-table layer shipped in Epic 6 as a RUNTIME reward-offer concern via `RewardOfferBuilder`/`RunOrchestrator`, NOT a generation-placement input). Do not wire reward tables into the placer.
- `Array[Dictionary]` does NOT survive `ActionResult` metadata deep-copy — it returns as a plain `Array` and crashes a strict re-typed receiver. Receive metadata-carried dictionary lists (enemies, rewards, wrinkles) as untyped `Array`.
- `ManualSeedLoader` (Story 3.7) is a THIN pure-domain parse+orchestration service over the COMPLETE `LevelGenerator.generate(...)` pipeline — it re-authors NO generation/validation/retry and persists nothing. `parse_seed` accepts a plain int or a decimal String/StringName, normalizes a negative-decoding signed-int64 seed to non-negative via `NON_NEGATIVE_MASK` (`& 0x7fffffffffffffff`) at the loader boundary (do NOT relax `GenerationRequest.validate()`'s `root_seed >= 0` rule), and REJECTS empty / non-decimal / out-of-int64-range / float seeds with stable codes (`empty_seed`/`non_integer_seed`/`unsupported_seed_type`). A float is rejected even if integral. Manual-seed results ALWAYS carry `is_manual_seed: true` + `meta_progression_eligible: false` (the existing `RunSnapshot` vocabulary); never re-introduce the dropped `manual_seed_eligible_for_progression` key (test-pinned absent). The actual meta gate is Epic 8.
- SEED REGRESSION is the tripwire. `*_seed_regression.gd` tests pin a compact TERRAIN fingerprint (dimensions + row-major terrain + entrance + exit) per approved seed; `test_seed_batch_regression.gd` is the `generate`-level harness driving the approved-seed catalog (inline in that test — bland/unfair seeds are KEPT + annotated, not deleted) and cross-checks that the full-`generate` terrain agrees with the `generate_layout` fingerprint (the two pinning paths can never silently diverge). Fingerprints change ONLY via an intentional generator/recipe change re-pinned in the SAME PR — regenerate with `tools/dump_small_layout_fingerprints.gd` / `dump_medium_layout_fingerprints.gd` / `dump_seed_batch_report.gd`, never edit a value to make a drifting test pass.

### Run Progression & Route Rules (Epic 4)

Epic 4 added the scene-free run-progression layer that turns a seed into one playable start-to-end run over a generated route. It REUSES Epic 1-3 contracts (events, snapshots, the level pipeline, the save bridge) — do not fork parallel formats.

- DOMAIN vs GENERATION vs COMMANDS placement (load-bearing): the run-progression MODEL — `RunState` (phase machine + transition table), `RouteState` (forward-only node graph), `RouteNode` (stable hyphenated ids) — plus the `RunOrchestrator` driver live in `scripts/run/`. Route GENERATION (`RouteGenerator`, `RouteValidator`) lives separately under `scripts/generation/route/`. Run-domain COMMANDS (`RouteAdvanceCommand`, `NodeEnterCommand`, `NodeExitCommand`, `NodeResolvePlaceholderCommand`, `RunStartCommand`) live with the tactical commands under `scripts/core/commands/`, extending `game_command.gd` — NOT under `scripts/run/`. Keep these three homes distinct.
- RUN-COMMAND IDIOM (4.3 ratified, followed by 4.4/4.5; honor it for any new run command): a run command takes the live `RunState` DIRECTLY as its `validate(state)`/`execute(state)` arg — no `RunActionContext` wrapper for a single-field context. The run domain has NO event sequencer (BoardState owns the tactical one); the CALLER supplies the run-level `sequence_id` via the constructor (default 1), and `validate()` rejects `sequence_id <= 0` FIRST (`invalid_event_sequence_id`) so a success path can never emit an event its own validator would reject. Validate-then-mutate: on any rejection return a structured `ActionResult.error` with ZERO events and a byte-identical no-mutation `RunState`. Build the success event only AFTER every (infallible) transition succeeds.
- RUN-COMMAND ERROR MODEL: ONE stable top-level error code per failure class; the precise machine-readable reason goes in `metadata` (e.g. `ineligible_route_choice` + `reason: unknown_node|not_linked|hidden_node|cleared_node|is_current_node`). Route node ids are HYPHENATED (`node-1-0`) — NEVER embed a hyphenated id in an error code. (Note: `GenerationRequest.node_id` validates lower_snake and rejects hyphens, so node entry derives a lower_snake request id from the hyphenated node id — don't pass the raw id through.)
- `RunStartCommand` is the ONE exception to the take-a-`RunState` idiom (Option A): the run does not exist yet, so it takes `(root_seed, is_manual_seed, sequence_id)` in its CONSTRUCTOR, its `state` arg is UNUSED (accepts null), and `execute` BUILDS and returns the live `RunState` in `result.metadata["run"]`. It is the FIRST/ONLY `run_started` emitter. An event-emitting action MUST be a `GameCommand` returning `ActionResult` (a plain service was rejected for the emitting path) so `run_started` rides the same validate/execute/no-mutation contract.
- `RouteGenerator` (`scripts/generation/route/`) is a seeded, deterministic, scene-free generator that draws the `map` stream EXCLUSIVELY to build a forward-only layered DAG of 8-12 NON-boss nodes plus exactly ONE terminal boss inside a `RouteState`. ROUTE = pure function of `root_seed` (same seed -> byte-identical route). It returns a `GenerationResult` (`PHASE_ROUTE`) whose serializable `payload.route_state` rehydrates the live `RouteState` via `RouteGenerator.route_from_result(...)` — the payload carries the snapshot dict, NOT a live `RefCounted` (mirror the level pipeline; consumers rebuild the `RouteState`). `INTERIOR_COLUMN_COUNT = 6` (not 7) is LOAD-BEARING for the "≥1 branch point per seed" guarantee. Route DEPTH is CONSTANT at 8 tiers for every seed (boss always at depth 7); the [8,12] variation lives entirely in column WIDTH, never route length — seed-varied depth is an Epic-10 pacing follow-up, not a v0 bug.
- ROUTE SEED REGRESSION is the tripwire (same discipline as level fingerprints): `tools/dump_route_fingerprints.gd` pins per-seed route fingerprints (count|id:type@depth|edges|boss). Any column-count/band/draw-order change MUST re-verify the branch-point + forward-only + full-reachability invariants and re-pin in the SAME change — never edit a value to make a drifting test pass. The fingerprint currently excludes `clues`/`reveal_state` (an Epic-10 clue-tuning follow-up may extend it).
- REVEAL-ON-ARRIVAL is the soft-lock-prevention invariant: the generator marks only depths 0-1 `REVEAL_REVEALED` (everything deeper HIDDEN). `RouteState.eligible_choice_ids()`/`is_eligible_choice()` is the reveal-gated, cleared-excluded forward filter (added in 4.3; 4.1's raw `available_choice_ids()` does NOT gate reveal). `RouteAdvanceCommand` MUST reveal the arrived node's direct forward neighbors (HIDDEN -> REVEALED, monotonic) on every commit, or the run soft-locks after the first choice. Any advance/entry path keeps reveal in lockstep with commitment.
- CLEARED-SET LOCKSTEP (a 4.4 cross-command gotcha): `RouteAdvanceCommand` appends the LEFT node to `cleared_node_ids` on advance; `NodeExitCommand` clears the CURRENT node on exit; `NodeResolvePlaceholderCommand` clears the boss on resolve. `RouteState.validate()` rejects `duplicate_cleared_node`, so the advance's left-node clear is IDEMPOTENT (guarded on existing membership) to survive the enter->exit->advance sequence. Do NOT shuffle clear/reveal responsibilities between commands or double-clear a node.
- NODE-TYPE RESOLUTION (4.5): `combat`/`elite_combat` are real level entry/exit (`NodeEnterCommand` builds + returns a validated `GenerationRequest`, draws ZERO RNG, does NOT run `LevelGenerator`; the orchestrator runs generation). `shop`/`reforge`/`gambling`/`event`/`secret` resolve as NO-OP placeholders via `NodeResolvePlaceholderCommand` (emit `node_placeholder_resolved`, `resolution = placeholder_completed`); `boss` resolves as a placeholder run-END (emit `node_placeholder_resolved` + `run_completed`, `outcome = boss_placeholder`). These are TRACKED MVP placeholders to be REPLACED at the SAME node boundary by their owning epic (shop/reforge Epic 6/7, gambling/event/secret Epic 7, real boss level Epic 9) — the replacing epic must NOT change the route model, node types, or the `run_completed` event, only the pre-completion behavior. (Epic 6 shipped the loot/reward CONTENT + the reward-offer flow + Consume/Destroy, but DID NOT replace these node placeholders — the shop/reforge/gambling node behaviors remain NO-OP placeholders; a later story wires the reward flow into them.) There is still NO reward CURRENCY/wallet entity in v0 (the gold wallet is Epic 7's risk-economy state); a `RewardTableDefinition` now EXISTS (Epic 6) but only as a RUNTIME reward-offer input, never in the route/generation layer.
- `RunOrchestrator` (`scripts/run/`) is the THIN scene-free type-dispatch start-to-end driver (NOT a Node/autoload/scene). It threads ONE `RunState` + one run-level `RngStreamSet` + a monotonic run-level `sequence_id` through the loop, SEQUENCING the existing 4.3/4.4/4.5 commands UNCHANGED (it owns no gameplay decision a command does not). It owns the run-level sequence counter (advancing past every emitted event so ids stay unique) and is the ONLY 4.x site that runs `LevelGenerator.generate(...)` — reading `payload.level_seed` on success, NEVER `result.seed`. v0 combat is AUTO-RESOLVED (level generated successfully -> node cleared); there is no real tactical play loop wired into the headless orchestrator yet (a later HUD concern). The boss path STOPS (no exit/advance) — the run ENDS.
- RUN-LEVEL `RngStreamSet` IS NOW LIVE (the 4.6 inert-stream residual is CLOSED across Epic 6/7): real run-affecting RNG routes through the orchestrator's `streams` — reward/loot draws on `rewards`/`loot` via `RewardOfferBuilder` (Epic 6), the affinity ASSIGNMENT on `map` and the event OFFER on `events` (Epic 7) — so a route-position save composed AFTER such a draw round-trips the ADVANCED stream (interrupted == uninterrupted determinism holds once RNG advances mid-run). The `LevelGenerator.generate`-INJECTION half was deliberately NOT done and is now declared CLOSED for the FR54-FR58 arc: every Epic-6/7 run draw routes through the orchestrator's run-level set, while `LevelGenerator.generate` keeps minting its OWN attempt-0-unperturbed `level` stream from `request.level_seed()` == `root_seed` (every generator/route fingerprint stays byte-identical). The affinity EFFECT (Scorched hazard stamp) is a POST-generation board operation, NOT a generation-time draw — it does NOT bake hazards into generated terrain (that WOULD perturb fingerprints). Only a FUTURE out-of-arc feature that needs an in-`generate` affinity-driven layout draw re-opens the injection, on its own terms, re-pinning fingerprints in the same change.
- RUN-PHASE PERSISTENCE: the run phase is serialized as `run_phase` NESTED inside the existing `route_state` payload (`RunState.RUN_PHASE_KEY`), DELIBERATELY not a new top-level `RunSnapshot` key — this keeps the pinned 23-key no-surprise gate green. NEVER flatten new top-level run keys onto `RunSnapshot`; nest run-progression fields under `route_state`. A phaseless `route_state` payload resumes as `PHASE_NEW_RUN` (ratified default). `RunState.try_from_run_snapshot_fields` prefers/cross-checks the canonical top-level `current_route_node_id` and fail-loud-rejects a conflicting nested pointer (`route_node_pointer_conflict`).
- BOARD-FREE ROUTE-POSITION SAVE/RESUME (4.6) — COMPOSE, DO NOT FORK: a between-NODE route choice has NO live `BoardState`, so the board-centric `RunSnapshot.from_between_level` (which requires a board + embeds a strict `TacticalSnapshot`) does not fit. `RunSnapshot.from_route_position(run, streams)` REUSES the existing 4.1 bridge (`run.to_run_snapshot_fields()` for the run/route fields with nested `run_phase`; `streams.to_snapshot()` for `rng_streams`), leaves `level_state` EMPTY, and adds NO new top-level key (the 23-key gate stays green). Restore is `RunResumeService.resume_route_position` reading those fields back through `RunState.try_from_run_snapshot_fields` + `RngStreamSet.try_restore`. The compose side AND the resume side BOTH cross-check that the run's `root_seed` equals `rng_streams.root_seed` (`route_position_seed_mismatch`) so a mis-wired/hand-edited save with divergent seeds fails loud, not silently. The orchestrator composes the route-position snapshot AFTER the advance (parked on a FRESH unresolved node), and `run_to_completion` hands it to an optional save callback (commands stay save-free); a no-op guard skips re-resolving an already-cleared parked node on resume.
- RUN EVENT LIFECYCLE: `run_started` (4.1-wired, FIRST emitted by `RunStartCommand` in 4.6) and `run_completed` (4.5, emitted at the boss boundary) bracket a run. New run-domain events are SYSTEM events (no actor), appended to the END of the event enum (never renumbered) so the snapshot/event schema stays back-compatible, and wired end-to-end (factory + payload validator + stable-id map + round-trip + malformed tests). `run_started.node_count` is the route's bounded [8,12] NON-boss count carried as a RAW JSON integer (a bounded count, NOT a seed — do NOT decimal-string encode it). `run_started.root_seed` IS decimal-string encoded (int64-safe).
- SEED-STRING VALIDATOR IDIOM (3.7, ratified epic-wide): ANY validator for an int64 seed string carried in a payload MUST adopt the lossless/canonicalize check, NOT bare `is_valid_int()` (which silently accepts out-of-int64 strings that `to_int()` saturates/wraps). `DomainEvent._has_decimal_string_payload` now ports the 3.7 idiom as PRIVATE STATIC helpers local to `domain_event.gd` (`_decimal_string_round_trips_losslessly` / `_canonical_decimal_string`, NOT a cross-call to `ManualSeedLoader`): after `is_valid_int()` it requires `_canonical_decimal_string(text) == str(text.to_int())`, rejecting an out-of-range decimal string with `invalid_event_payload`. Reuse this pattern for any new seed-string payload field.

### Classes, Starting Kits & Rules Kernel (Epic 5)

Epic 5 added the hero-class content layer, the per-class starting kit, and the FIRST (minimal, explanation-only) rules kernel that seats class passives on the run. It REUSES every prior contract — the `ContentRepository` boundary, the typed-`Resource` definition + `validate()` discipline, the run-command idiom, the 23-key `RunSnapshot` gate, the named-RNG rule — and forks NO parallel format. v0 starting passives are EXPLANATION-ONLY; the felt-in-combat effect engine is Epic 6.

- CONTENT-ACCESSOR NAMING — `get_class` IS A RESERVED `Object` METHOD: defining `ClassRepository.get_class(id)` is a HARD GDScript PARSE ERROR (collides with the native `Object.get_class()`). The canonical class accessor is `ClassRepository.get_class_definition(class_id)` — ALWAYS call that, NEVER `get_class`. Any future `get_<thing>` content accessor MUST avoid reserved `Object` method names (`get_class`, `get_script`, `get_meta`, etc.); the sibling repos use collision-free names (`get_passive`, `get_weapon`, `get_support`, `get_enemy`). A false-PASS guard earned its keep here: a reserved-name collision compile-failed test files yet one still printed `PASS` — caught only by grepping raw run output for `SCRIPT ERROR|Parse Error`, never the summary line. Keep that grep as a standing gate for every new content/test file.
- CLASS CONTENT (`scripts/content/`): `ClassDefinition` (typed `Resource`) carries `class_id` (lower_snake), `display_name`, `lock_state` (`selectable`/`locked`, FR42/FR43), `unlock_hint`, the starting kit fields (`starting_weapon_id`, `starting_support_id`, `baseline_hp`), and the two passive-id forward references (`class_passive_id`, `equipment_synergy_passive_id`). `validate()` requires the kit + passive fields ONLY for a SELECTABLE class (a LOCKED class needs only a non-empty `unlock_hint`) — the spec-flagged asymmetry that a LOCKED class may carry a malformed/absent kit is HARMLESS in v0 (locked classes never start a run). v0 baselines: Warrior/Pyromancer/Ranger SELECTABLE, Necromancer/Shadeblade LOCKED. Equipment + passive ids on the definition are SHAPE-validated (lower_snake) only — `ClassDefinition.validate()` resolves them against NO repository.
- STARTING KIT (`scripts/run/starting_kit.gd`): `StartingKit` is a scene-free `RefCounted` BY-ID value object (`class_id`, resolved `weapon_id`/`support_id`, `baseline_hp`, the two passive ids) recorded on `RunState.starting_kit`. It owns no truth beyond the recorded kit, submits no commands, draws no RNG, and instantiates NO tactical board player (the live tactical loop is a later concern). It mirrors the exact-key `to_dictionary()` discipline (the `TacticalBoardViewModel`/`HeroSelectViewModel` precedent — its `DICTIONARY_KEYS` set is pinned by `test_starting_kit.gd`; a key never silently appears/vanishes). `support_id == &"none"` is the REAL baseline `SUPPORT_NONE` (Ranger's no-support kit) — it RESOLVES and is a VALID support; NEVER treat `none` as a missing item (doing so falsely fails the Ranger run-start). `baseline_hp` is a small bounded int (NOT a seed — no int64/decimal-string encoding).
- RUN-START IS THREE SEQUENTIAL FAIL-CLOSED CONTENT GATES: `RunStartCommand` resolves a NON-empty selected class through, in order, (1) the CLASS gate (`get_class_definition` -> reject `unknown_class` / `class_not_selectable`), (2) the KIT gate (`WeaponRepository.get_weapon` + `SupportRepository.get_support` -> reject `unknown_starting_weapon` / `unknown_starting_support`), (3) the PASSIVE gate (`PassiveRepository.get_passive` for BOTH passive ids -> reject `unknown_class_passive` / `unknown_equipment_synergy_passive`). Each gate resolves in `validate()` (a pure read — no mutation, no event, no RNG) and the offending id rides `metadata`, never the error code. `execute()` records in order: `selected_class_id` (after `new_run` + the `NEW_RUN -> ACTIVE_ROUTE` transition) -> `starting_kit` -> seat the `RulesResolver` — validate-then-mutate with ZERO partial state on any reject. An EMPTY class id SKIPS the whole block (the back-compat "no class chosen" run stays byte-identical). A new starting-content type follows this same resolve-in-validate / record-in-execute shape. The new repo params are LAST on `RunStartCommand.new(...)` / injected on `RunOrchestrator` (`start(root_seed, is_manual_seed, class_id)`); every existing call site is preserved.
- MINIMAL RULES KERNEL (`scripts/rules/`): `RuleTrigger` (`triggers/`) is the SINGLE SOURCE OF TRUTH for the FIXED ten-window vocabulary (`run_started`, `level_entered`, `turn_started`, `before_move`, `after_move`, `before_attack`, `damage_calculated`, `enemy_killed`, `reward_offered`, `level_completed`) + `is_valid_window(...)`; a passive can never declare a window outside it. Do NOT add, rename, or renumber a window (a later epic wires combat HOOK sites against these exact ids). `RulesResolver` (`resolver/`) is a PURE-READ service (like a snapshot / `LevelValidator`): `register_passive` (registration order == stable resolution order), `resolve(window)`, `explain(window)` — it draws NO RNG, runs NO commands, mutates no tactical state. `scripts/rules/{conditions,operations}` are INTENTIONALLY EMPTY scaffolding — the `RuleCondition`/`RuleTarget`/`RuleOperation` evaluation model, stacking/conflict/duration, and the combat hook sites that FIRE these windows are Epic 6. Do NOT author them early.
- PASSIVES ARE EXPLANATION-ONLY IN v0 (FR44/FR45): `PassiveDefinition` (typed `Resource`) carries `passive_id`, `display_name`, `passive_kind` (`class`/`equipment_synergy`), `trigger_windows` (each validated against the fixed `RuleTrigger` vocabulary, at least one required), and a player/debug-readable `explanation`. It DELIBERATELY has NO active-skill field, NO level/cooldown/activation, NO RNG, and NO effect/operation/amount model (AC3 forbids any active-skill concept; the per-effect operation is Epic 6). A registered passive surfaces with its `explanation` when its window resolves — it does NOT yet mutate any HP/movement/damage number. The six v0 baseline passives (`PassiveRepository._baseline_definitions()`) MUST EXACTLY MATCH the class baselines' passive ids or a class start fails closed at the passive gate; the broader 20-30 MVP passive POOL (FR46) is Epic 6 — do NOT author it here. `RunStartCommand` registers the class passive FIRST, then the equipment-synergy passive (the documented stable order).
- ROUTE-POSITION SAVE PERSISTS CLASS-ID ONLY; RE-DERIVE KIT + RESOLVER ON RESTORE: `RunState.to_run_snapshot_fields()` NESTS `selected_class_id` inside the `route_state` payload (`RunState.SELECTED_CLASS_ID_KEY`, the SAME mechanism as `run_phase`) — adding NO new top-level `RunSnapshot` key (the pinned 23-key gate stays green). `starting_kit` AND `rules_resolver` are LIVE `RefCounted` services (the 4.6 inert-`RngStreamSet` precedent) and are DELIBERATELY NOT serialized — absent from `to_run_snapshot_fields()`; a route-position save carries the class id ONLY. On restore `try_from_run_snapshot_fields` reads the class id back lenient (default `&""` for a pre-5.3 save) and reconstructs a `RunState` with `starting_kit == null` AND `rules_resolver == null` BY DESIGN. ANY resumer (an Epic-6+ live tactical resume) that needs the kit or resolver AFTER a route-position resume MUST RE-DERIVE BOTH from the restored `selected_class_id` via the canonical deterministic pure helpers `ClassStartSummaryViewModel.re_derive_kit(class_id)` / `re_derive_resolver(class_id)` (re-derive the kit -> resolve the two passive ids through the baseline `PassiveRepository` -> rebuild the resolver registering the class passive FIRST then the equipment-synergy passive — byte-equal to a fresh `RunStartCommand` start). `selected_class_id`/`starting_kit` DO ride the FULL `to_dictionary()`/`try_from_dictionary` (lenient-read) so copied/round-tripped runs preserve them; `rules_resolver` rides only as a `copy()` reference (immutable content), never serialized.
- CLASS-START SURFACE IS A SCENE-FREE PROJECTION (`scripts/ui/view_models/`): `HeroSelectViewModel` (5.2) and `ClassStartSummaryViewModel` (5.5) are `RefCounted` view-model projections with EXACT pinned key contracts (`SUMMARY_KEYS` / `ENTRY_KEYS`) — NOT `Control`s, Nodes, or `.tscn` scenes (the FR47/FR68 hero-select/HUD scenes are a later HUD story; UI-scene-last holds). They read `display_name` through `get_class_definition` (never the reserved `get_class`), surface passive EXPLANATIONS (the class passive then the equipment-synergy passive, the resolver's stable order) via the run's seated `rules_resolver` as the single source of truth, and introduce NO active-skill key (FR45). They mutate no combat number (v0 passives are explanation-only) and draw no RNG.
- DUPLICATE-ID FAIL-LOUD IS NOW ENFORCED CENTRALLY (Story 6.1 RESOLVED the carried Epic-5 `[Review][Defer]`): `ContentRepository.register_definition` was changed from `void`/last-write-wins to RETURN an `ActionResult` and REJECT a second registration under an already-present `(type, id)` with a stable `duplicate_definition` error (carrying the offending `{type, id}`). A rejected duplicate is NEITHER stored (`get_definition` still returns the FIRST) NOR appended to the per-repo id order — no partial registration survives. The guard is applied ONCE on the shared boundary so ALL content repositories (the original six PLUS the seven Epic-6 loot/reward repos) inherit fail-loud duplicate rejection uniformly (no single repo forked); each `register_<x>` surfaces it as a per-type `duplicate_<x>` error in the caller's own vocabulary (`duplicate_class`/`duplicate_weapon`/`duplicate_armor`/`duplicate_consumable`/`duplicate_reward_table`/...). Every `register_*` call site now treats the returned `ActionResult` as fallible (batch builders fail the batch closed on a duplicate). Do NOT regress this to last-write-wins, and do NOT add a new content repository that bypasses `ContentRepository.register_definition` (it would lose the guard).

### Loot, Rewards, Inventory & Consume/Destroy (Epic 6)

Epic 6 added the loot/reward CONTENT layer, the small run-domain inventory/equipment model, the deterministic reward-offer flow, the passive-reward modal data contract, and the REAL Consume/Destroy passive resolution. It REUSES every prior contract — the `ContentRepository` boundary, the typed-`Resource` `validate()` discipline, the run-command idiom (take `RunState` directly, reject `sequence_id <= 0` first, validate-then-mutate, ZERO partial state on reject), the named-RNG rule, the append-only SYSTEM-event enum, the 23-key `RunSnapshot` gate — and forks NO parallel format. v0 is OUTCOME-RECORD-ONLY + EXPLANATION-ONLY: rewards/Destroy/consumable-use RECORD their effect, passives RESOLVE + EXPLAIN, but NOTHING yet mutates an HP/damage/wallet number (no live current-HP / gold / curse domain field exists in v0 — the run carries only `starting_kit.baseline_hp`, a FIXED value). `scripts/rules/{conditions,operations}` remain EMPTY — the per-effect operation engine + combat hook sites are the later Epic-6 operations story; do NOT author them.

- CONTENT ROSTER + REPOSITORIES (`scripts/content/`): Epic 6 added seven content families through the `ContentRepository` boundary — `ArmorDefinition`/`armor_repository`, `JewelryDefinition`/`jewelry_repository`, `ConsumableDefinition`/`consumable_repository`, `PickupDefinition`/`pickup_repository`, `GoldRewardDefinition`/`gold_reward_repository`, `RewardTableDefinition`/`reward_table_repository`, and the standalone `DestroyOutcomeTableDefinition` (created via `create_baseline_table()`, NO repository — a SINGLE defaulted baseline, the deliberate scope-minimal divergence from the table-with-repository pattern). Plus `ItemRollModel` (the instance-variation value type embedded on equippables) and the pure `RewardOfferBuilder`. The EIGHT MVP loot CATEGORIES (`RewardTableDefinition.REWARD_CATEGORIES`) are `weapon`/`armor`/`jewelry`/`support`/`consumable`/`pickup`/`passive`/`gold` — each string matches that family's `DEFINITION_TYPE` (plus `gold`). Every definition references other content BY ID + CATEGORY only; `validate()` shape-checks (lower_snake id, allowlisted category, positive weight) and REJECTS fail-loud (never coerces), but does NOT resolve the referenced id against a repository (resolution is the offer flow's / command's job — the by-id-defer precedent). All content is still code-constant baselines (no `.tres`/JSON).
- ITEM VARIATION IS RANGE-LIVE, AFFIXES/ENHANCEMENTS/AFFINITIES SHAPE-ONLY-DEFERRED (`ItemRollModel`, FR76): the single inclusive int roll RANGE (`roll_min`/`roll_max`) ships LIVE; the `affix_ids`/`enhancement_ids`/`affinity_tags` lists EXIST + validate INERT (lower_snake shape only) but each carries a per-family `*_mvp_deferred` marker and MUST stay EMPTY while deferred (the content is Epic 7+/post-MVP — "hundreds of loot affixes" is explicitly non-MVP). There is NO `item_level` field — variation is ranges/affixes/affinities, never a fixed level. To light a family up later: flip the marker false + populate the list + add the resolution path — NO schema fork. Consumables/pickups/gold carry NO roll model and NO equip gate (only the equippable categories do).
- REWARD-TABLE `choice_count` + THE VISIBLE EXCEPTION-MARKER PATTERN (FR52, ratified epic-wide): a `RewardTableDefinition` declares `choice_count` (default 1; 3 for a passive "3-choice moment"). `validate()` REJECTS a table whose `choice_count` exceeds its DISTINCT-content-id count UNLESS the explicit `mvp_choice_count_exception` marker is set WITH a non-empty `choice_count_exception_reason`. This is the canonical VISIBLE-TUNING-EXCEPTION pattern reused across Epic 6 (the 6.1 `*_mvp_deferred` posture): an off-target table is VALID only with a reasoned, surfaced marker — NEVER a silent reduction. `DestroyOutcomeTableDefinition` uses the SAME shape for its 70/20/10 check (`mvp_distribution_exception` + `distribution_exception_reason`). Reuse this exact pattern (marker + required reason + `validate()` enforcement) for any future tunable-distribution table; do not add a silent fallback.
- REWARD DRAWS ALWAYS ROUTE THROUGH A VALIDATED TABLE + THE BUILDER, ON THE `rewards`/`loot` STREAMS ONLY: `RewardOfferBuilder.build_offer(streams, stream_name, table)` is the ONE reward/loot draw path — it weighted-picks an entry by drawing EXCLUSIVELY through the INJECTED `RngStreamSet.rand_int` on `stream_name` (must be `STREAM_REWARDS` or `STREAM_LOOT`, the `ALLOWED_STREAMS` gate), NEVER mints a `RandomNumberGenerator`, NEVER calls `randi()`/`randf()`, NEVER touches another stream. It re-VALIDATES the table before drawing (validate-before-draw) and fail-closes on a null/invalid table or zero total weight — never a hand-rolled draw, never a fabricated default. The builder ACCEPTS the stream-set rather than minting one: this is the seam that closed the 4.6 inert-run-level-`RngStreamSet` half — `RunOrchestrator.generate_reward_offer`/`generate_passive_reward_offer` hand it `RunOrchestrator.streams` (the run-level set), so a route-position save composed AFTER a reward roll round-trips the ADVANCED stream (interrupted == uninterrupted determinism holds once RNG advances mid-run). Do NOT fork a parallel offer/draw path; extend this one. (The `LevelGenerator.generate`-injected-`RngStreamSet` half remains the only residual of that item — no AC requires it.)
- REWARD GENERATE IS AN EXPLICIT, CALLER-DRIVEN ORCHESTRATOR METHOD — NOT AUTO-WIRED: `RunOrchestrator.generate_reward_offer(table_id, stream_name=STREAM_REWARDS)` (single-pick) and `generate_passive_reward_offer(...)` (AC4 3-choice draw-WITHOUT-replacement) are invoked by a caller at the node-completion boundary; they are DELIBERATELY NOT auto-wired into `run_to_completion`/`_resolve_combat` (auto-generating every combat node would advance the run-level streams mid-run with no auto-resolve policy and trip the `reward_offer_pending` guard + perturb the interrupted==uninterrupted determinism tests). Each resolves the table through `_reward_table_repository` (validated tables only), draws via the builder, STORES the offer on `RunState.pending_reward_offer` as `pending`, emits a `reward_offered` event, and advances the run-level sequence; it rejects a second generate while an offer is still pending (`reward_offer_pending`) and fail-closes `unknown_reward_table` on a repo miss. A later HUD/orchestrator story owns the auto-wire + the board-reward-marker -> offer link once it owns a resolution policy. The reward/loot commands (resolve/consume/destroy/use) are ALL pure caller-driven commands — NO orchestrator convenience method was added for any of them.
- INVENTORY/EQUIPMENT MODEL (`scripts/run/inventory_state.gd`, FR51): `InventoryState` is a scene-free `RefCounted` BY-ID value object recorded on `RunState.inventory` — a fixed-capacity ordered BACKPACK (default 6, `DEFAULT_BACKPACK_CAPACITY`) of single-item slots `{item_id, category, quantity}` PLUS a fixed set of NAMED EQUIPMENT slots (`weapon`/`armor`/`jewelry`/`support`). NO STACKING in v0: each occupied slot is EXACTLY ONE item — a second pickup of the same id is a SECOND slot, never `quantity++`; the `quantity` field defaults to 1 (validated >= 1) so a later stackable opt-in is a marker flip, not a schema fork, but v0 NEVER sets quantity > 1. It mirrors the exact-key `to_dictionary()` discipline (`DICTIONARY_KEYS`/`SLOT_KEYS` pinned by `test_inventory_state.gd` — a key never silently appears/vanishes), the lenient `try_from_dictionary` (value object, no reject path), and the deep `copy()`. `capacity`/`quantity` are small bounded ints (NOT seeds — no decimal-string encoding). EQUIP/UNEQUIP commands + the character-level equip-gate CHECK are DEFERRED (no hero character-level system exists — equipment STRUCTURE only this epic).
- INVENTORY IS NOT IN THE ROUTE-POSITION SAVE (compose, don't fork): `InventoryState` AND `RewardOffer` ride ONLY the FULL `RunState.to_dictionary()`/`try_from_dictionary` — they are DELIBERATELY NOT serialized into `to_run_snapshot_fields()` / the route-position `RunSnapshot` (the placeholder `RunSnapshot.inventory`/`equipment` fields stay EMPTY; the 23-key gate stays green, NO new top-level key). The live in-node inventory + the pending reward offer + a rolled Destroy outcome + a consumed passive are a LATER in-node-save / live-resume story's concern; do NOT expand the route-position save to persist them. A resumer that needs the live consumed-passive resolver state must RE-DERIVE the starting resolver from `selected_class_id` (the 5.5 `re_derive_resolver`) then RE-REGISTER consumed passives.
- PICKUP / RESOLVE / CONSUME / DESTROY / USE — FIVE RUN-DOMAIN COMMANDS, ALL THE 4.3 IDIOM VERBATIM (`scripts/core/commands/`): every one extends `game_command.gd`, takes the live `RunState` DIRECTLY (no wrapper), rejects `sequence_id <= 0` FIRST, is validate-then-mutate with ZERO events + byte-identical no-mutation `RunState` on any reject, and builds its event ONLY AFTER the mutation. `PickupItemCommand` (6.2) appends ONE backpack slot + emits `item_gained`, fail-closes `inventory_full` (the AC3 "no silent delete" — a full backpack rejects, never overwrites/drops a slot; replacement-choice UX deferred); ZERO RNG. `ResolveRewardCommand` (6.3) applies the SELECTED entry from the pending offer + flips it to `resolved` + emits `reward_resolved`, ZERO NEW RNG (the offer was already rolled at GENERATE); a BACKPACK-category reward is applied by COMPOSING `PickupItemCommand` (distinct `sequence_id + 1`), and a FULL backpack surfaces the pickup's `inventory_full` HONESTLY and leaves the offer `pending` (no flip); gold/passive are OUTCOME-RECORD-ONLY (offer flip + event, no domain mutation). `ConsumePassiveCommand` (6.5) ADOPTS the resolved `PassiveDefinition` into the run's `RulesResolver` via `register_passive` (creating + seating a fresh resolver when null), flips the offer, emits `passive_consumed`; ZERO RNG. `DestroyPassiveCommand` (6.6) is the OPPOSITE of adoption (does NOT register; `run.rules_resolver` UNTOUCHED) — it ROLLS the 70/20/10 outcome via `rand_int(STREAM_REWARDS, ...)` (EXACTLY ONE draw, the SAME stream the reward GENERATE path advances), flips the offer, emits `passive_destroyed` carrying the draw provenance (`roll`/`draw_index`). `UseConsumableCommand` (6.7) USES a backpack consumable: resolves it through `ConsumableRepository`, REMOVES its backpack slot (`remove_first_slot_with_id` — the inverse of `append_slot`; whole-slot removal, no `quantity--` since no stacking), emits a DETERMINISTIC `item_consumed` (NO draw provenance, unlike Destroy); ZERO RNG.
- A PENDING PASSIVE OFFER IS RESOLVED BY EXACTLY ONE COMMAND — one of `ResolveRewardCommand` (the 6.3 generic outcome-only fallback), `ConsumePassiveCommand` (Consume, adopt), or `DestroyPassiveCommand` (Destroy, roll 70/20/10). Each OWNS the offer's resolution end-to-end (validates the pending offer, flips it to `resolved` ITSELF, emits ONLY its own event) — none composes another resolution command (that would double-record). A HUD wiring story decides which command a passive offer routes to. The `RunState.pending_reward_offer` (`RewardOffer`, `scripts/run/reward_offer.gd`) stores by-id only (table id, offered `{category, content_id}` entries, the selected entry, the draw provenance, a `pending`/`resolved` status) — NEVER a live `RewardTableDefinition`/`RngStreamSet`; `state_after` is a full int64 (decimal-string encoded in `to_dictionary()`). Offered-entry lists are plain `Array` of plain `Dictionary` (the `Array[Dictionary]`-does-not-survive-`ActionResult`-deep-copy rule — receive metadata-carried lists untyped).
- DESTROY OUTCOME DISTRIBUTION IS EXACT-INTEGER 70/20/10 (`DestroyOutcomeTableDefinition`, FR50): the three fixed outcome categories are `small_immediate_benefit`/`progress_unlock_hidden_flag`/`no_obvious_reward_avoids_danger` (lower_snake; do NOT add/rename/renumber — pinned to match `DomainEvent.DESTROY_OUTCOME_CATEGORIES` by test). `validate()` enforces the distribution via EXACT integer arithmetic (no float drift: `small*10 == total*7 AND progress*10 == total*2 AND no_reward*10 == total*1`); `create_baseline_table()` ships AT EXACTLY 70/20/10 (weights 7/2/1) with NO exception marker. An off-target table is valid only with the visible `mvp_distribution_exception` + reason (the shared exception-marker pattern). Each entry carries a non-empty `effect` (the OUTCOME-RECORD marker) + `explanation` (the Readability-Rule known result; an honest-unknown outcome is labeled honestly — the 6.4 contract).
- OUTCOME-RECORD-ONLY IS THE EPIC-6 v0 POSTURE (the load-bearing forward-defer): gold rewards, Destroy outcomes, AND consumable-use effects RECORD a `category`/`outcome_category`/`outcome_id`/`outcome_effect` marker via their event — but mutate NO domain field because none exists in v0. The live wallet/gold, heal/cleanse/curse-removal, and ward/burst MUTATION is Epic 7's risk-economy state; Oath-Shards/mastery/unlock/Echoes is Epic 8 (FR59/FR60); the future-reward reroll is a later reward-flow story. When those land they wire the actual mutation OFF the recorded event (the event's `item_id`/`outcome_category`/`outcome_id` is the single hook). Do NOT pull Epic-7 live-HP/wallet state forward into an Epic-6 command.
- EPIC-6 EVENTS ARE APPEND-ONLY SYSTEM EVENTS: `ITEM_GAINED`, `REWARD_OFFERED`, `REWARD_RESOLVED`, `PASSIVE_CONSUMED`, `PASSIVE_DESTROYED`, `ITEM_CONSUMED` were each appended at the `DomainEvent.Type` enum END (after `RUN_COMPLETED`), NEVER renumbered, and wired end-to-end (factory + per-event payload validator + both id maps + JSON round-trip + per-field malformed-negative tests + the exhaustive `expected_ids` pin). They are SYSTEM events (no actor). `passive_destroyed` carries draw provenance (`roll`/`draw_index`) because Destroy DRAWS RNG; `item_gained`/`reward_resolved`/`passive_consumed`/`item_consumed` are DETERMINISTIC (no provenance). Continue this discipline for any new event: append at the end, wire all touch-points, add to `expected_ids`. (`warding_salve` is the one uncommon baseline consumable weighted into NO reward table — obtainable only via direct `PickupItemCommand` in v0; the Epic-10 consumable-frequency pass weights it in or records the omission.)
- PASSIVE-REWARD MODAL IS A SCENE-FREE DATA CONTRACT (6.4, FR47/FR77): `PassiveDefinition` was ADDITIVELY extended with the modal fields (`icon`/`flavor`/`exact_mechanical_effects`/`consume_text`/`destroy_text` + the honest-unknown `has_unknown_consequences`/`consequences_text` + the FR77 `served_pillars`), with `validate()` tightened to REJECT any blank required field + an empty/out-of-allowlist pillar set — and all SIX baseline passives extended in the SAME change (the tightening-validate-breaks-baselines lesson: when you tighten a `validate()` to require new fields, extend every existing baseline in the same change or `create_baseline_repository()` returns null and class-start/reward-table suites break). `PassiveRewardModalViewModel` (exact `MODAL_KEYS`, fail-closed, leaks no live handle) + `PassiveRewardCommitFlow` (the Consume-vs-Destroy commit-INTENT, mutates nothing on cancel/dismiss — mirrors `TacticalAttackCommitFlow`) are scene-free `RefCounted` projections; the real modal `.tscn` + icon ART are a later HUD/asset story (UI-scene-last; the `icon` is a placeholder id sentinel). The same ADDITIVE-extend-+-extend-all-baselines discipline applied to `ConsumableDefinition` in 6.7 (`outcome_effect`/`explanation`, new params LAST, three baselines extended).

### Risk Economy, Curses, Events & Affinities (Epic 7)

Epic 7 added the risk/reward LAYER over the Epic-4 run: the live risk-economy state (gold wallet + corruption + `risk_flags`), curse resolution through the rules kernel, the risk/reward `event` decision, and the full affinity domain (definitions + deterministic assignment + LIVE tactical effects). It REUSES every prior contract — the `ContentRepository` boundary, the typed-`Resource` `validate()` discipline, the run-command idiom (take `RunState` directly, reject `sequence_id <= 0` first, validate-then-mutate, ZERO partial state on reject), the named-RNG rule, the append-only SYSTEM-event enum, the 23-key `RunSnapshot` gate, the nested-`route_state` save discipline — and forks NO parallel format. **Epic 7's affinity arc is COMPLETE: all 4 MVP affinities now have effects.** The 23-key save gate, the 7 named RNG streams (`required_streams()` returns exactly `map`/`level`/`combat`/`loot`/`rewards`/`events`/`cosmetic`), and every generator/route seed-regression fingerprint are INVARIANT across the whole epic (generators untouched all epic — fingerprints byte-identical).

- RISK-ECONOMY STATE PERSISTS NESTED UNDER `route_state` (7.1, the ratified Epic-7 economy-persistence pattern): the live wallet/corruption/risk-flags state (`RiskEconomyState`, `scripts/run/`) is recorded on `RunState` and serialized NESTED inside the existing `route_state` payload (reusing the `gold`/`corruption` placeholders) — adding NO new top-level `RunSnapshot` key (the 23-key gate COUNT stays 23), with NO migration (a pre-7.1 save parses with a default economy derived from `is_manual_seed`; reads are lenient/back-compatible). This is the canonical pattern for Epic-7 run state that must SURVIVE a route-position resume (vs Epic-6 off-save inventory). The gold reward is a `gold_min..gold_max` BAND on `GoldRewardDefinition` rolled at GENERATE time through the run-level `rewards` stream (carried on the offer + `reward_resolved` payload) so RESOLVE keeps the Epic-6 zero-new-RNG-on-resolve invariant — never "read the amount" at resolve. A GENERATE-time gold roll is a SECOND `rewards`-stream draw for a gold offer; any later GENERATE-time roll must account for `rewards`-stream draw-index shifts in existing reward tests.
- v0 RISK EFFECTS ARE EXPLANATION / RECORD-ONLY — NO GENERIC OPERATION ENGINE: a curse/corruption increment, a resource cost, and a raised `risk_flag` are RECORDED + EXPLAINED; they mutate NO live combat HP/damage number (the "lose max HP" GDD example is the economy side + a `max_hp_loss` flag — v0 has no live max-HP). The `risk_flag` is a recorded future-danger MARKER (`add_risk_flag`/`has_risk_flag`), NOT a difficulty knob / live multiplier — the system that READS a flag and ALTERS generation (reward odds / elite rates / spawn) is a SEPARATE later generation-modifier story. `scripts/rules/conditions/` STAYS EMPTY by ratified design (0 `.gd`, verified) — v0 is explanation-only; the generic `RuleCondition`/`RuleTarget`/`RuleOperation` evaluation model, stacking/conflict/duration, and the combat HOOK sites that fire every trigger window are the later/broader operations story. Do NOT author them.
- CURSE RESOLUTION SEATS A SEPARATE CURSE REGISTRY ON THE KERNEL (7.2): a curse is a `CurseDefinition` resolved through `RulesResolver` via a SEPARATE `Array[CurseDefinition]` registry (`register_curse`/`resolve_curses`/`registered_curse_ids`/`registered_curse_count`), NOT by broadening the `Array[PassiveDefinition]` typing (the leaner fork that avoids re-pinning the 5.4 typed-passive tests). `explain()` is the unified passive+curse surface; `resolve()` keeps its strict passive-only return. The curse rule source is LIVE re-derivable (NOT serialized) — only the `curse_count`/`corruption` COUNT survives the route-position save (the `RunSnapshot.curses` per-curse id list stays `[]` in v0). The `CURSE_APPLIED` SYSTEM event was appended at the enum end.
- THE `event` NODE IS A CALLER-DRIVEN OFFER/CHOOSE PAIR, NOT AUTO-WIRED (7.3, the 6.3 `generate_reward_offer` posture): `RunOrchestrator.generate_event_offer` is the deterministic `events`-stream OFFER roll (`rand_int(STREAM_EVENTS, ...)` — 7.3 is the FIRST consumer of the previously-declared-but-undrawn `events` stream; tests assert ONLY `events` advanced), invoked by a caller at a node boundary; it is DELIBERATELY NOT wired into `run_to_completion`/`_resolve_combat`/`_resolve_non_combat_placeholder`, and the pending `EventOffer` rides ONLY the full `RunState.to_dictionary()`, NOT the route-position save (else it perturbs interrupted==uninterrupted determinism). The `event` node STAYS in `NodeResolvePlaceholderCommand.PLACEHOLDER_NODE_TYPES` (the no-soft-lock partition). `ChooseEventOptionCommand` (the 4.3 idiom, ZERO RNG) applies BOTH the reward AND the risk through the `RiskEconomyState` API, is the FIRST/ONLY `add_risk_flag` caller (the `risk_flags` PRODUCER), and emits `event_resolved` + `economy_changed` + (on a curse increment) `curse_applied`. `EVENT_OFFERED`/`EVENT_RESOLVED` were appended at the enum end.
- AFFINITIES ARE PRODUCER (definitions + assignment, 7.4) → EFFECT (tactical pressure, 7.5/7.6), 4 MVP + neutral `none`: `AffinityDefinition` (`scripts/content/`) carries `affinity_id`/`display_name`/`tactical_rules`/`visual_tags`/`explanation`, where `tactical_rules` is a RECORD/EXPLANATION-ONLY plain dict-list of `{rule_id, description}` markers (NOT an operation engine); `is_neutral()` + a `neutral()` factory; the fail-closed `AffinityRepository` (the `CursedRewardRepository` template) exposes a PURE-READ `tactical_rules_for(id)` returning EMPTY for `none`/unknown. Assignment is the deterministic CALLER-DRIVEN `RunOrchestrator.assign_affinity(node)` — ONE `streams.rand_int(STREAM_MAP, ...)` draw in the ORCHESTRATOR (`LevelGenerator.generate` UNTOUCHED — generator/route fingerprints stay byte-identical), recorded on the additive `RunState.assigned_affinities` and MIRRORED into the EXISTING top-level `RunSnapshot.affinities` placeholder (read-only mirror; no new key, COUNT stays 23, no migration). The assigned affinity is RE-DERIVABLE from the seed after a resume (a pure function of `(root_seed, route position)` on the `map` stream) — the route-position resume reconstructs with an EMPTY `assigned_affinities` and re-derives. `assign_affinity` has NO once-per-node idempotency guard yet (caller-driven, no live per-node call site — a double-assign re-rolls; the later run-flow story that wires the per-node call site decides on an assign-if-absent guard).
- AFFINITY EFFECTS ARE A NARROW, BOARD-SCOPED, CALLER-DRIVEN LAYER — `operations/` OPENED TO EXACTLY ONE FILE (7.5, FR57): `scripts/rules/operations/affinity_effect_resolver.gd` is the FIRST + ONLY occupant of `operations/` — a pure-domain, ZERO-RNG resolver that reads a level's assigned affinity's `tactical_rules` (7.4 data) and turns them into REAL tactical pressure on a built Epic-1 `BoardState`. It is NOT a generic `RuleCondition`/`RuleTarget`/`RuleOperation` engine and NOT live-run-loop wiring (the orchestrator AUTO-RESOLVES combat; the "enter node -> instantiate board -> apply effects -> play turns" call site + the affinity HUD/VFX are a later HUD/run-flow story). Per affinity: **Scorched** STAMPS `BoardCell.Terrain.HAZARD` cells (the 3.4 contract — HAZARD stays board-valid, WALKABLE, sight-TRANSPARENT; only WALL blocks) + resolves a DoT via the EXISTING `DAMAGE_APPLIED` event through `AffinityHazardDamageCommand` (fixed v0 amount, ZERO RNG, validate-then-mutate; NO new `DomainEvent.Type` member — the enum is unchanged for affinity effects); hazard cells are chosen DETERMINISTICALLY over eligible FLOOR+UNOCCUPIED cells (never a spawn cell — fair by construction). **Flooded/Conductive** DETERMINISTICALLY MARKS conductive-danger + pathing-pressure cells as preview DATA (NOT terrain). **Cursed** builds a curse-like `CurseDefinition` the caller seats on the run's `RulesResolver` (NO board mutation). `AffinityPreviewQuery` (`scripts/tactical/targeting/`) is a PURE sibling reading the SAME resolver plan so preview can never disagree with the applied effect. Neutral/`none`/unknown → NO effects; **Darkness is a deliberate NO-OP here** (its effect is 7.6, a different domain).
- DARKNESS IS A SEPARATE VISIBILITY/FOG/MEMORY LAYER, NOT A HAZARD EFFECT (7.6, FR58) — the ONE MVP affinity whose effect does NOT live in the board-cell hazard model: `scripts/tactical/fog/darkness_visibility_layer.gd` (homed in `fog/` alongside `TacticalVisibilityQuery`, NOT `rules/operations/`) is a pure-domain, ZERO-RNG layer — a reduced LoS radius (`reduced_visibility` → baseline 4 → `DARKNESS_REDUCED_LINE_OF_SIGHT_RADIUS` 2, floor 1, NEVER 0) applied THROUGH the existing `TacticalVisibilityQuery.calculate_visible_cells` (no parallel LoS algorithm — a smaller `visible_cells` via the existing `visibility_updated`, so NO new event), plus a READ-LAYER memory-uncertainty annotation (`fog_memory_pressure`) that marks a `memory`-state cell stale/uncertain WITHOUT ever touching the authoritative `visible` branch or mutating stored `BoardCell`/snapshot state. The 7.5 hazard resolver's Darkness branch STAYS a NO-OP (two existing `darkness → no board effect` tests stay green BY CONSTRUCTION — do NOT extend the hazard `AffinityEffectResolver`/`AffinityPreviewQuery` for Darkness). The reduced radius + memory flag are LIVE re-derivable from `(assigned affinity, board)` — NO new save key, NO migration (23-key gate stays 23).
- FR58 DARKNESS FAIRNESS IS A BOARD-SCOPED QUERY, NOT A `LevelValidator` WIRING CHANGE (7.6): `scripts/generation/level/darkness_fairness_query.gd` extends the Epic-3 `LevelValidator` safe-first-reveal VOCABULARY to be affinity-aware at the Darkness-reduced radius (a reachable HAZARD cell unseen from the entrance at the reduced radius FAILS LOUD with a stable `fairness_reason` + seed + phase — "no unavoidable damage from unseen space"). It is a SEPARATE additive file, NOT wired into the generation success path (`LevelValidator`/the generators are untouched → seed-regression fingerprints byte-identical). KNOWN v0 SCOPE: all GENERATED boards are still all-FLOOR (`LevelRecipeDefinition.WRINKLE_AFFINITY_PLACEHOLDER` stays INERT), so the FAIL branch is exercised only via hand-built boards; the predicate goes LIVE on real generated boards when a later story bakes hazards into generated terrain. The v0 fairness predicate scopes the unseen-damage source to `Terrain.HAZARD` cells (the ratified contract; broadening it for an "empower enemies" / live-combat effect is tied to the deferred live-tactical-loop work).
- AFFINITY/RISK ACCESSIBILITY CUES LIVE IN `TacticalAccessibilityModel._CUE_CATALOG` (the canonical color-independence driver `test_tactical_accessibility_cues.gd` iterates), each with a non-color channel — NOT in `AttackPreviewContractMatrix` (which is the attack-preview CASE corpus, not a cue registry). The Scorched-hazard (`affinity_scorched_hazard`), Flooded pathing-pressure (`affinity_pathing_pressure`), and the two Darkness cues (`affinity_darkness_reduced_visibility`/`affinity_darkness_memory_uncertain`) are FINAL ids. The Flooded electric interaction ships DISTINCT-from-final `_placeholder` ids (`affinity_conductive_danger_placeholder`/`..._vfx`) — a TRACKED Epic-10 readiness obligation (replace / de-scope / block readiness); the conductive danger MARK is real, the live water/electric chain reaction is the placeholder.

### Run-End, Meta Profile & Outpost Rules (Epic 8)

Epic 8 added the RUN-END + CROSS-RUN META layer: the run-completion/return-to-outpost resolution, the pure-read run summary, the FIRST persistent cross-run profile (Oath-Shard currency + Echoes/Seal-Fragments/class-mastery/unlock progress + the first-death latch), the deterministic capped meta-award + unlock-threshold calculators, the optional first-death narrative beat, and the outpost view-model + start-another-descent seam. It REUSES every prior contract — the 4.3 run-command idiom (take `RunState` directly, reject `sequence_id <= 0` first, validate-then-mutate, ZERO partial state on reject), the append-only SYSTEM-event enum, the versioned-snapshot template (`SCHEMA_VERSION` + exact-key `to_dictionary()` + strict `parse` + lenient decode), the atomic-write repository, the exact-key/fail-closed/no-live-handle view-model discipline, the named-RNG rule — and forks NO parallel format. v0 is STILL EXPLANATION/RECORD-ONLY at the run level (no live combat HP/wallet mutation) and CALLER-DRIVEN at the run-end seam (no live combat-death source; nothing is auto-wired into `RunOrchestrator.run_to_completion`). **The 23-key `RunSnapshot` gate, the 7 named RNG streams, and every generator/route seed-regression fingerprint are INVARIANT across the whole epic (generators untouched — fingerprints byte-identical; Epic 8 draws ZERO RNG anywhere — every run-end command/calculator/summary is deterministic).**

- THE PROFILE IS THE FIRST PERSISTENT CROSS-RUN STATE — ITS OWN SNAPSHOT + REPOSITORY + SAVE FILE, NOT nested under `route_state`/`RunSnapshot` (8.3, the ratified Epic-7 §7 Action-T2 heads-up): `ProfileSnapshot` (`scripts/save/snapshots/`) OUTLIVES a run and ACCUMULATES across descents; it lives in `user://profile.json` via `ProfileRepository` (`scripts/save/`), STRICTLY INDEPENDENT of the run autosave `user://run_autosave.json`/`SaveRepository` (neither reads/writes/renames the other — the settings-vs-run independence precedent). It MIRRORS `RunSnapshot` VERBATIM: `const SCHEMA_VERSION = 1`, an exact-key `to_dictionary()` (pinned by `test_profile_snapshot.gd`, deep-copied sub-dicts/lists), a static `parse(data) -> ActionResult` that REJECTS `schema_version != SCHEMA_VERSION` with the stable `unsupported_profile_schema` code (+ `{expected,actual}` — the v0 migration reject; a future bump adds a real migrate step, Story 8.7 owns the matrix), lenient field decode, `fresh(id)` (the recovery default), and `copy()`. `ProfileRepository` mirrors `SaveRepository`'s atomic temp->backup->replace write + structured read, but with PROFILE-SCOPED codes (`profile_save_open_failed`/`profile_save_backup_remove_failed`/`profile_save_backup_failed`/`profile_save_replace_failed`; `profile_not_found`/`profile_open_failed`/`profile_parse_failed`) so a profile failure is diagnosable distinctly from a run-autosave failure. `profile_not_found` -> the caller starts `ProfileSnapshot.fresh()` (AC5 recovery). Do NOT nest profile state under `route_state`/`RunSnapshot`, do NOT share the run-autosave file/codes, and do NOT bump `SCHEMA_VERSION` without adding a migrate step + a migration test.
- PROFILE FIELD SET (the versioned-snapshot header + 6 content fields): `schema_version`/`content_version`/`profile_id` (header); `oath_shards` (the CROSS-RUN AWARDED currency total — a small bounded NON-NEGATIVE int, so PLAIN NUMERIC, NOT decimal-string-encoded, unlike a seed); `last_awarded_run_seed` (the award idempotency marker — the LAST awarded run's `root_seed`, a full int64 -> DECIMAL-STRING encoded, `""` = never awarded); `class_mastery` (Dictionary, per-class ACCUMULATING count), `echoes` (Array[String], unique-id SET), `unlock_progress` (Dictionary — the Seal-Fragment set + threshold state flags + the merge marker); `first_death_recorded` (bool latch). The 8.3 snapshot RESERVED empty/0 homes for the 8.4 content (`class_mastery`/`echoes`/`unlock_progress`) + the 8.5 flag (`first_death_recorded`) so BOTH 8.4 AND 8.5 MERGE INTO THE EXISTING SHAPE WITHOUT A MIGRATION (`SCHEMA_VERSION` stays 1). Continue that discipline: reserve a home in the current schema rather than bumping the version for additive run-end state.
- THREE RUN-END PROFILE-MUTATING COMMANDS, ALL THE 4.3 IDIOM VERBATIM, ALL CALLER-DRIVEN BEHIND THE RUN-END SEAM (`scripts/core/commands/`): `AwardMetaProgressCommand` (8.3 — Oath-Shard currency), `MergeRunDiscoveriesCommand` (8.4 — Echoes/Seal-Fragments/class-mastery/unlock flags + threshold crossings), `RecordFirstDeathCommand` (8.5 — the first-death latch). Each takes the live TERMINAL `RunState` DIRECTLY as `state`; the PROFILE (+ per-command inputs: the 8.3 summary, the 8.4 discovery-event list) + the run-level `sequence_id` ride the CONSTRUCTOR; `validate()` rejects `sequence_id <= 0` FIRST; validate-then-mutate with ZERO events + a byte-identical no-mutation run AND profile on ANY reject; the event is built ONLY AFTER the mutation; ZERO RNG. NONE persists the profile itself — the CALLER calls `ProfileRepository.write_profile` with the mutated profile. NONE is auto-wired into `run_to_completion` (no live death/discovery source in v0). The shared gate ORDER is sequence-id -> context (`invalid_context` for null-profile/not-a-`RunState`/null-route/structurally-invalid-run, surfacing the inner error) -> `run_not_terminal` -> [eligibility] -> [idempotency].
- THREE INDEPENDENT RUN-END IDEMPOTENCY MECHANISMS — SAFE IN ANY CALLER ORDER (the load-bearing 8.4 two-marker + 8.5 latch invariant): (1) the AWARD keys on `profile.last_awarded_run_seed` (reject `run_already_awarded`); (2) the MERGE keys on a DEDICATED `unlock_progress["_last_merged_run_seed"]` marker (reject `run_already_merged`) stored INSIDE the existing `unlock_progress` dict (NOT a new top-level key — merges without a migration; the leading `_` can never collide with a lower_snake unlock-track key and the threshold rule/summary derive ignore it); (3) the FIRST-DEATH latch IS `profile.first_death_recorded` itself (reject `first_death_already_recorded`). The award + merge use TWO INDEPENDENT PER-RUN markers DELIBERATELY (a SHARED marker would make whichever run-end command runs FIRST block the SECOND in either order) — 8.6/8.7 MUST treat them as independent. The first-death latch is PER-PROFILE-LIFETIME (fires on the FIRST death across ALL runs), NOT per-run. Do NOT collapse these into one marker.
- ELIGIBILITY IS SPLIT — META vs NARRATIVE (the ratified FR28 boundary precedent, 8.5): the AWARD (8.3) + the MERGE (8.4) DENY a manual-seed run at a `run_not_meta_eligible` gate (identical code + shape — a manual-seed run grants ZERO Oath Shards, ZERO Echoes/Seal-Fragments/mastery/unlock progress, ZERO event, profile byte-identical). But the FIRST-DEATH latch (8.5) is DELIBERATELY NOT eligibility-gated (Option A, human-ratified): a manual-seed practice death STILL records the latch + shows the line, because a boolean NARRATIVE latch grants NO progression currency/unlocks (it is provably progression-free — FR64 "story discovery optional" cuts toward availability). This IS the FR28 narrative-vs-meta boundary precedent — future narrative-vs-meta state builds on it. Eligibility is READ from `run.meta_progression_eligible` (already lockstep with `not is_manual_seed`, asserted in `RunState.validate()` + `RiskEconomyState.oath_shard_eligible`); the commands READ it, never re-derive it.
- `CompleteRunCommand` (8.1) is the RUN-END RESOLUTION command — the FIRST to DRIVE `PHASE_FAILED` (reachable in the transition table since Epic 4 but never triggered — combat auto-resolves to success). ONE OUTCOME-PARAMETERIZED command: the caller supplies an `outcome` that is EITHER a death CAUSE (in `DomainEvent.RUN_FAILED_CAUSES` = `hero_death`/`level_defeat`/`boss_defeat`/`abandoned`) -> transition to `PHASE_FAILED` + emit `run_failed` (with the cause), OR the completion marker (`DomainEvent.RUN_COMPLETED_OUTCOME_COMPLETED` = `completed`) -> transition to `PHASE_COMPLETED` + emit `run_completed` (broadened `completed` outcome), else reject `unknown_run_end_outcome`. Both carry `next_destination = outpost` (`RUN_END_DESTINATION_OUTPOST`, FR32). It DRIVES the already-existing `_legal_next_phases` edges (adds NONE): the completion path from `ACTIVE_ROUTE` runs the boss TWO-STEP (`ACTIVE_ROUTE -> NODE_RESOLUTION -> COMPLETED`) so NO new edge is needed. It BROADENS the existing 4.5 `run_completed` (does NOT create a parallel completed event) and does NOT change the boss boundary (Epic 9 reuses `NodeResolvePlaceholderCommand`'s boss `run_completed` unchanged). AC3 idempotency: re-resolving an ALREADY-terminal run rejects `run_already_terminal` — this is the SEAM the 8.3 award/8.4 merge/8.5 latch sit BEHIND.
- BREAKING (8.1, documented): `run_completed.boss_node_id` is now BOSS-PLACEHOLDER-ONLY — a generic `completed` completion OMITS it (the broadened validator requires it only for the `boss_placeholder` outcome). Epic 9 boss consumers MUST guard on its presence. Derive boss progress from the ROUTE (a cleared `TYPE_BOSS` node), NOT the `run_completed` payload.
- `RunSummary` (8.2) is a PURE-READ AGGREGATOR — builds NO domain state, NO event, NO save key, NO command, NO mutation, NO RNG; NOT a save snapshot (DERIVED on demand; adds no `RunSnapshot` key — the 23-key gate stays 23). `RunSummary.build(run, events)` AGGREGATES the FR60 field set from the terminal `RunState` + an EXPLICIT ordered `Array` of `DomainEvent` the CALLER supplies (v0 has NO run-level event STORE — commands RETURN events; the orchestrator threads sequence ids but does not accumulate a log). It reads DOMAIN STATE + EVENT RECORDS as source truth (AC2 forbids reading `CombatExplanationLog`/any presentation log). `to_dictionary()` projects an EXACT pinned key set (`test_run_summary.gd`) with THREE state-boundary sub-dicts keeping the AC3 state kinds SEPARABLE — `run_scoped` (this run's RunState+events: nodes_cleared/boss_cleared/elite_nodes_cleared/passives_consumed/passives_destroyed/notable_loot/gold/curse_count/corruption), `profile_meta` (`oath_shards_earned` — 0, read from NO profile; still `not_yet_supported` — wiring the EARNED count off the profile is 8.6/8.7), `content_unlock` (`echoes_discovered`/`unlock_progress` — 8.4 SOURCED these from `content_discovered` events, no longer placeholders). A null/non-terminal run projects the fail-closed empty fact (`has_summary == false`). `seed` is decimal-string encoded; the backing member is named `root_seed` (NOT `seed`) to avoid shadowing GDScript's built-in `seed()` under the ZERO-`randi`/`randf` discipline.
- `RunSummary.notable_loot` IS SINGLE-SOURCED FROM `item_gained` EVENTS ONLY (the ratified 8.2 review decision) — ALL `reward_resolved` events are EXCLUDED (gold is an economy readout; a passive is tracked in passives_consumed/destroyed; a reward->backpack pickup ALREADY emits a PAIRED `item_gained` via `ResolveRewardCommand` composing `PickupItemCommand`). So each gained item is counted EXACTLY ONCE and the 8.6 outpost renders `notable_loot` DIRECTLY with NO further dedup. Do NOT scan `reward_resolved` into `notable_loot` (it would double-count).
- `FirstDeathNarrativeBeat` (8.5) is a SEPARATE PURE-READ DTO, OFF THE CRITICAL PATH — the LINE DELIVERY is DECOUPLED from the flag MUTATION (`RecordFirstDeathCommand` owns the latch), so a skip/dismiss is STRUCTURALLY a pure no-op (the DTO owns no truth to change; there is NO "skip command"). LINE-AS-ID: keyed by a stable `line_id` (== `DomainEvent.FIRST_DEATH_LINE_ID` = `first_death`); the single display prose ("Good. You remembered how to die.") is a const on the DTO resolved via a tiny `LINE_BY_ID` map — v0 has EXACTLY ONE line (no `NarrativeRepository`/codex/JSON pipeline; the epic-wide by-id + no-content-pipeline defer). It is COMPLETE WITHOUT the summary — 8.5 adds NO narrative field to `RunSummary` and makes neither the summary nor the start-another-descent path depend on the beat (AC3 — ignoring lore never blocks understanding the summary/outpost/starting a descent). The GDD first-VICTORY reveal ("It did not die. It learned the way back." — FR62) is Epic 9 (Story 9.4), NOT here.
- `MetaAwardRules`/`UnlockProgressRules` (`scripts/save/`) are PURE, DETERMINISTIC, ZERO-RNG, CAPPED calculators (the snapshot/`LevelValidator` pure-read posture; DECLARED const config, test-pinned). `MetaAwardRules.oath_shard_award_for(run)`: a COMPLETED run yields `min(BASE_AWARD + PER_NODE_AWARD * nodes_cleared, MAX_AWARD)` (v0 = `min(1 + 1*nodes, 5)`), a FAILED (death) run yields 0, a non-terminal/null run yields 0 — SPARSE + CAPPED (AC3 "raw-stat ladders REJECTED"; Oath Shards are a CURRENCY toward variety/options, NOT a combat stat). `nodes_cleared` is derived DIRECTLY off `run.route.cleared_node_ids.size()` (IDENTICAL to `RunSummary`; the amount is a pure function of the VALIDATED run ALONE — the 8-3 review hardening removed a foreign-summary coupling; do NOT feed the amount from a caller-supplied summary). `UnlockProgressRules.evaluate(unlock_progress)`: returns `{thresholds_crossed, state}` (a fresh deep-copied state — does NOT mutate its input) — v0 declares two seal-fragment-COUNT thresholds (`SEAL_FRAGMENT_THRESHOLDS`: count 1 -> `seal_gate_1_unlocked`, count 3 -> `seal_gate_2_unlocked`) mapping a required Seal-Fragment count to a bool STATE flag flipped ONCE (idempotent; a finite capped ladder of VARIETY gates, never a raw stat). NEITHER scales by DIFFICULTY (a hard non-goal). NEITHER produces a raw-stat unlock key (`is_raw_stat_unlock_key` classifies the AC3-rejected `damage`/`max_hp`/`armor`/`crit`/`dodge` tokens so a test asserts none is ever produced). Both RECORD the flip; the EFFECT-APPLICATION (spending Oath Shards / applying an unlock's class/passive/starting-option) is a LATER meta-spend story — do NOT build the unlock-spend tree.
- SEAL FRAGMENTS ARE UNLOCK PROGRESS, NOT A TOP-LEVEL FIELD (the 8.4 home decision): despite the story TITLE, `ProfileSnapshot` has NO `seal_fragments` field — Seal Fragments live as a unique-id SET under `unlock_progress[UnlockProgressRules.SEAL_FRAGMENTS_KEY == "seal_fragments"]` so 8.4 merges WITHOUT a migration (`SCHEMA_VERSION` stays 1). A dedicated top-level field would be a deliberate schema-v2 + migration decision (8.7 territory), not a silent add.
- DISCOVERIES ARE DERIVED FROM `content_discovered` EVENTS (the 8.4 discovery-source decision — v0 has NO persisted in-run discovery home, mirroring 8.2's derive-from-events for passives/loot). `MergeRunDiscoveriesCommand` scans the caller-supplied event list once, bucketing by `content_kind` (∈ `DomainEvent.DISCOVERED_CONTENT_KINDS` = `echo`/`seal_fragment`/`class_mastery`/`unlock_flag`), and MERGES: `echoes`/`seal_fragments`/`unlock_flags` use SET semantics (a duplicate id grants each unique unlock EXACTLY once — AC1 "duplicate discoveries" is about the CONTENT, not just re-invocation), while `class_mastery` ACCUMULATES per discovery (per-RUN idempotency comes from the per-invocation marker, not set semantics). The threshold rule reads ONLY `seal_fragments`, so the merge marker never perturbs a crossing.
- `OutpostViewModel` (8.6) is a PURE-READ ASSEMBLY that AGGREGATES the five prior Epic-8 read surfaces (`ProfileSnapshot` for the cross-run meta SOURCE OF TRUTH; `RunSummary` for the just-ended run; `HeroSelectViewModel` DELEGATED for the class roster; `FirstDeathNarrativeBeat`; the `RunEndOutcome` outpost flow signal) into ONE exact-key/fail-closed/no-live-handle surface (`test_outpost_view_model.gd`) — NOT a scene/`.tscn` (UI-scene-last; the outpost screen + button wiring + `next_destination` navigation are a later HUD/boot-flow story). The Oath-Shard display reads `profile.oath_shards` (the AWARDED total), NOT `RunSummary.profile_meta.oath_shards_earned` (which stays 0). It re-projects NOTHING (delegates the roster, renders `notable_loot` directly with no re-dedup, does not re-evaluate unlock thresholds). START-ANOTHER-DESCENT is a REQUEST SEAM (`start_run_request(...)` returns a `{root_seed, is_manual_seed, class_id, is_startable}` value; the CALLER hands it to a FRESH `RunOrchestrator.start(...)`/`RunStartCommand`) — the view model does NOT call `start(...)` (do not let UI mutate domain state); the prior run is NOT reused BY CONSTRUCTION (`RunState.new_run` resets the route). The class is fail-closed pre-gated via `HeroSelectViewModel.is_class_selectable` (an EMPTY class id is the legacy no-class start, startable); the AUTHORITATIVE gate is still `RunStartCommand`.
- NAMED OUTPOST SPACES ARE STABLE-ID DATA-ONLY PLACEHOLDERS (8.6, AC2): `OutpostViewModel.NAMED_SPACES` is a fixed const registry of the four GDD spaces (`memory_archive`/`hall_of_oaths`/`seal_table`/`descent_stair`) each with a stable lower_snake id + display name + a `deferred` status marker + a `maps_to` note (the visible-exception-marker discipline — an unfinished surface is EXPLICITLY marked deferred, never silently omitted). It is DATA only (drives no domain state, gates no run, holds no truth — AC2 "without making UI state authoritative"); only `descent_stair` has a live affordance (the start-another-descent action). Do NOT invent additional spaces, and do NOT make named-space data authoritative. NOTE the FR-numbering trap: canonical `epics.md` FR63 is the Larval Avatar boss (Epic 9) — the named-space obligation traces via the GDD + the 2026-06-04 readiness patch, NOT canonical FR63.
- `OutpostViewModel.for_recovery(...)` HAS TWO MODES (the ratified 8.6 review distinction — a future boot-flow story MUST wire the right one): a PROFILE-LOAD failure (`profile_not_found`/`unsupported_profile_schema`) passes NO `loaded_profile` -> the surface falls back to `ProfileSnapshot.fresh()` (a fresh 0-shard surface, `has_profile == false` — honest, no real totals exist); a PROFILE-WRITE failure (`profile_save_*` codes) passes the INTACT `loaded_profile` -> the surface shows the player's REAL totals behind the retry banner (`has_profile == true`), NOT a misleading 0-shard surface over intact-but-unsaved progress. Both populate `recovery_state {has_recovery, code, is_recoverable}` with no crash and no invalid meta state. AC5's "does not silently lose current run summary data" is STRUCTURAL: `RunSummary` is a DERIVED read that does NOT read the profile file, so a failed profile write leaves the summary fully readable.
- EPIC-8 EVENTS ARE APPEND-ONLY SYSTEM EVENTS (no actor), wired end-to-end (factory + per-event payload validator + both id maps + JSON round-trip + malformed-negative tests + the exhaustive `expected_ids` pin), NEVER renumbered: `RUN_FAILED` (8.1), `OATH_SHARDS_AWARDED` (8.3), `CONTENT_DISCOVERED` (8.4), `PROFILE_PROGRESS_MERGED` (8.4), `FIRST_DEATH_RECORDED` (8.5) — appended at the `DomainEvent.Type` enum END. All are DETERMINISTIC (Epic 8 draws ZERO RNG — no draw provenance). Their allowlist consts are LOCAL statics in `domain_event.gd`, each test-pinned: `RUN_FAILED_CAUSES`, `RUN_COMPLETED_OUTCOME_COMPLETED`/`RUN_COMPLETED_OUTCOME_BOSS_PLACEHOLDER`, `RUN_END_DESTINATION_OUTPOST`, `OATH_SHARDS_AWARDED_REASONS` (= `run_completed_eligible`; a manual-seed run emits NO award event at all — no ineligible reason), `DISCOVERED_CONTENT_KINDS`, `FIRST_DEATH_LINE_ID`. Continue this discipline for any new event.
- SAVE/LOAD JSON INT-COERCION FOOTGUN (the 8.7 test lesson): a whole-dict `to_dictionary()` byte-identity comparison across a JSON boundary FALSE-FAILS on nested int values — JSON decodes ints as floats and Godot's `Dictionary ==` is type-strict on values (`{"warrior": 3} != {"warrior": 3.0}`). Use int-coercion-aware normalization before comparing any JSON-reloaded snapshot that carries a nested int-valued dict (e.g. `class_mastery`). This is why the profile decode helpers (`ProfileSnapshot._nonnegative_int_or_zero`) are int-coercion-tolerant.
- 8.7 AC-WORDING vs AS-BUILT (documented divergence): AC1's "class unlock states restore correctly" does NOT mean a profile->class-selectability wiring exists — v0 has NONE (`ClassDefinition.lock_state` is STATIC; `HeroSelectViewModel` reads no profile). The AC is satisfied by the profile `unlock_progress`/`class_mastery` STATE round-tripping (carried from the 8.3/8.4 divergence). Do NOT build a deferred meta-spend/apply system to satisfy this AC.

### Larval Avatar Finale Rules (Epic 9)

Epic 9 added the MVP FINALE: the boss node's real pre-completion behavior (setup + fight), the Larval Avatar boss content + phases + telegraphed AI, the boss VICTORY -> run-victory + first-victory reveal, and the finale regression + tuning-diagnostics closeout (FR31/FR62/FR63). It REUSES every prior contract — the 4.3 run-command idiom (take `RunState`/`TacticalActionContext` directly, reject `sequence_id <= 0` first, validate-then-mutate, ZERO partial state on reject), the append-only SYSTEM-event enum, the typed-`Resource` `validate()` + `ContentRepository`-boundary discipline, the versioned-`ProfileSnapshot` template, the simulate-then-apply tactical-turn discipline, the named-RNG rule — and forks NO parallel format. It swaps ONLY the boss node's pre-completion behavior at the SAME `boss` node boundary + the SAME `run_completed` boundary Epic 4 shipped (NOT the route model, the node types, or the `run_completed` event). **INVARIANTS HELD ACROSS THE WHOLE EPIC: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams (`map`/`level`/`combat`/`loot`/`rewards`/`events`/`cosmetic`) are unchanged (Epic 9 adds ZERO new RNG stream — the boss/victory/defeat records are DETERMINISTIC; the boss AI is ZERO-RNG), `ProfileSnapshot.SCHEMA_VERSION` stays 1 (the first-victory latch is an ADDITIVE field, NO migration), every generator/route seed-regression fingerprint is byte-identical (the generators are untouched all epic), `scripts/rules/conditions/` STAYS EMPTY (0 `.gd`), and `scripts/rules/operations/` STAYS exactly one file (`affinity_effect_resolver.gd`).** v0 stays CALLER-DRIVEN at the run-end seam: the boss VICTORY resolution is caller-/test-driven (an integration test + the thin `resolve_boss_victory` continuation), NOT auto-wired into `run_to_completion`; the live combat-DEATH SOURCE (hero-death -> `PHASE_FAILED`) is still DEFERRED (9.5 drives the death PATH with a DRIVEN death, not a live source).

- THE BOSS IS A NEW DEFINITION FAMILY, NOT AN `EnemyDefinition` (9.2, `scripts/content/`): the Larval Avatar is `BossDefinition` (+ ordered `BossPhaseDefinition`/`BossActionDefinition` sub-resources) — the flat two-behavior `EnemyDefinition` has no phase/telegraph/multi-action model, so a NEW `*Definition` family was added, mirroring `EventDefinition`/`EventChoiceDefinition` VERBATIM in shape (a `DEFINITION_TYPE` const, `@export` fields copied into a typed sub-resource `Array` in `_init`, a `validate() -> ActionResult` returning `invalid_boss_definition` + `{reason, field}`/`{reason, phase_index, field}`, the shared `_is_lower_snake_id` helper, `get_<x>`/`<x>_ids` accessors). `validate()` is fail-loud (never coerce): `boss_id` lower_snake + `== BOSS_ID` (`larval_avatar`, the 9.1 slot id — a test cross-checks it equals `BossEncounterRequest.BOSS_ENTITY_ID`), `max_hp > 0` (the `TacticalEntityState` rule), `>= 2` phases (a one-phase boss has no phase model), phase-0 threshold `== 100` (the entry phase, always active on the fresh boss), STRICTLY DECREASING thresholds thereafter (forward-only monotonic escalation — the `RouteState` reveal-monotonicity precedent; equal == duplicate, greater == out-of-order), unique phase ids, each phase valid (delegated, surfaced with the phase INDEX), non-blank explanation. Draws ZERO RNG. The boss is AUTHORED escalation content, NOT a difficulty tier (a hard non-goal). Resolved through `BossRepository` (the `EnemyRepository` template) via the `ContentRepository` fail-loud duplicate-id boundary (`duplicate_boss`).
- BOSS FINALE SETUP IS THE `boss` NODE'S REAL PRE-COMPLETION BEHAVIOR, THE SETUP HALF ONLY (9.1, `BossNodeEnterCommand` + `scripts/generation/boss/`): when the run is parked on the terminal `TYPE_BOSS` node in `PHASE_ACTIVE_ROUTE`, `BossNodeEnterCommand` (the boss sibling of `NodeEnterCommand`, the 4.3 idiom) builds + validates a deterministic `BossEncounterRequest` from `(run root_seed, boss node)`, builds + strict-validates the deterministic boss ARENA level snapshot via `BossArenaBuilder` (a FIXED hand-authored 12x12 walled layout through the STRICT `BoardState.try_from_snapshot` — the 1.3 validate-then-reject precedent; the arena carries entrance + boss-entity SLOT MARKER + player start), transitions `ACTIVE_ROUTE -> NODE_RESOLUTION` (the LAST infallible step — atomic; ANY setup failure returns a structured `ActionResult.error` carrying the inner `GenerationResult` seed/phase/reason/diagnostics with ZERO events + a byte-identical no-mutation `RunState`), and emits ONE `boss_encounter_started` event. It does NOT complete the run (the boss NO LONGER auto-completes on arrival — the run awaits the real fight + victory), does NOT author the boss stats/phases (9.2 fills the reserved slot), does NOT clear the boss node (9.4's victory), draws ZERO RNG. HYPHEN TRAP: the route boss id is hyphenated (`node-7-0`) but `BossEncounterRequest.node_id` is lower_snake — DERIVE the request id (`node_7_0`) and carry the ORIGINAL hyphenated id separately in the payload/metadata (never in a code — the 4.4 rule).
- BOSS TURNS RUN THROUGH THE SIMULATE-THEN-APPLY TACTICAL DISCIPLINE, NEVER MUTATING THE BOARD DIRECTLY (9.3, `scripts/tactical/turns/` + `scripts/ai/`): `BossTurnResolver` is the boss analogue of `EnemyTurnResolver` — on the boss's turn it runs `BossAi` -> `BossCommandAdapter` on a SIMULATION COPY of the context, validates events against `PendingTelegraphState`, applies to the real board via `board.apply_events`, then to the real pending telegraphs, then syncs the turn state (the boss NEVER mutates board/entity/turn state directly — AC2). `BossAi` (the `PrototypeEnemyAi` analogue) SCORES only the ACTIVE phase's `legal_action_ids` (recomputed from the boss's current HP) with FIXED integer scores (`resolve` a due telegraph 120 > `telegraph` 80 > `move` skitter 50 > `wait` 0; distinct scores so ties never occur, broken by higher damage then declaration order) and returns an `AiDecision`-shaped DTO with a `score` + `reasons` + the chosen ability's telegraph/damage/explanation in metadata (so the explanation log names the ability — AC4). It is a PURE READ: ZERO RNG (reproducibility is pure determinism, NOT a seeded roll), NO commands, mutates nothing. Boss AI/adapter live in `scripts/ai/`/`scripts/tactical/turns/` — NOT `scripts/rules/operations/` (which stays one file); the boss did NOT open `scripts/rules/conditions/` (still empty). Telegraphs REUSE the existing `tile_marked` event + the Ash Seer's one-turn `due_turn == created_turn + 1` response window (NFR10 forbids a real-time timer).
- THE LIVE PHASE SEAM IS HP-RECOMPUTED, NO STORED PHASE STATE (9.2/9.3): `BossTurnResolver.resolve_phase_transitions(...)` calls `BossPhaseResolver.resolve` LIVE after the boss's HP changes and emits ONE `boss_phase_changed` SYSTEM event per returned `BossPhaseTransition.to_payload()` (appended to the event stream/log, NOT applied to the board — a system event has no actor + `board.apply_events` would reject it). The boss's `current_phase_index` is RECOMPUTED from HP (NO stored phase state — the 23-key gate stays 23; repeated triggers do not duplicate a phase change). `BossPhaseResolver.resolve()` clamps a negative `current_phase_index` to `maxi(0, ...)` so it can NEVER emit an event-invalid `from_phase < 0` (the 9.2 review Low, closed 9.3). Content-family homes: `BossPhaseResolver`/`BossPhaseTransition` live under `scripts/content/boss/`; the definitions under `scripts/content/definitions/`; the repository under `scripts/content/repositories/`.
- THE RUN-VICTORY IS `run_completed` + `outcome == victory`, NOT A PARALLEL EVENT (9.4, the 8.1 "Epic 9 reuses THIS boundary" contract): `RUN_COMPLETED_OUTCOME_VICTORY := &"victory"` was UNBLOCKED in the `run_completed` outcome allowlist + validator + `CompleteRunCommand`'s completion classifier (`victory` joins `completed` as a SECOND completion marker — both run `_resolve_completed`), so the boss defeat drives `run_completed` + `outcome == victory` + `next_destination == outpost` via the EXISTING caller-driven `RunOrchestrator.resolve_run_end` (NO orchestrator change — it already dispatches any outcome; NO parallel `run_victory` event; NO new transition edge — the completion from `ACTIVE_ROUTE` runs the boss two-step). The tactical defeat fact is a NEW `boss_defeated` SYSTEM event emitted by the NEW `BossTurnResolver.detect_boss_defeat(context, sequence_id_base)` seam (the ONLY 9.3-code change in 9.4 — detect `boss.is_dead()` after damage, emit `boss_defeated {boss_entity_id, phase_id, final_hp}`); the caller then drives the run-victory resolution. `boss_defeated` chose `phase_id` (human-readable, survives a phase-model change) over a phase INDEX.
- THE FIRST-VICTORY LATCH IS AN ADDITIVE `ProfileSnapshot` FIELD + THE FOURTH INDEPENDENT RUN-END IDEMPOTENCY MARKER (9.4, FR62 — the OPPOSITE-terminal-phase TWIN of 8.5's first-death): `ProfileSnapshot.first_victory_recorded` (bool) was added at `SCHEMA_VERSION == 1` as a NEW ADDITIVE field (NO home was pre-reserved, unlike 8.5's first-death which 8.3 pre-reserved) — lenient-parsed (a pre-9.4 dict -> false), `DICTIONARY_KEYS` pin UPDATED, NO version bump (reconciled with 8.7's migration matrix — a lenient additive field at v1 keeps its `SCHEMA_VERSION == 1` + `schema_version:2 -> unsupported_profile_schema` pins green). `RecordFirstVictoryCommand` (the `RecordFirstDeathCommand` twin, the 4.3 idiom, ZERO RNG, CALLER-DRIVEN behind the run-end seam) mirrors 8.5 VERBATIM at the OPPOSITE phase: the gate is VICTORY-only (`run.phase == PHASE_COMPLETED`, reject a FAILED run `run_not_completed` — the discriminator vs 8.5's `run_not_failed`), the latch IS its own once-only marker (a MONOTONIC PER-PROFILE-LIFETIME latch — set once on the FIRST victory across ALL runs; reject `first_victory_already_recorded` thereafter with ZERO mutation + ZERO event), and it emits a NEW `first_victory_recorded` SYSTEM event carrying the line BY id. This is the FOURTH INDEPENDENT run-end idempotency mechanism alongside the award's `last_awarded_run_seed`, the merge's `unlock_progress["_last_merged_run_seed"]`, and 8.5's `first_death_recorded` — 9.4 reads/writes NONE of the other three; all four are independent + safe in ANY caller order.
- FIRST-VICTORY ELIGIBILITY IS OPTION A (eligibility-INDEPENDENT, mirroring the ratified 8.5 first-death Option A): the first-victory latch is DELIBERATELY NOT gated on `meta_progression_eligible` — a manual-seed first victory STILL records the flag + shows the line, because the line is PURE NARRATIVE FLAVOR (FR61/FR64) and setting a boolean narrative latch grants ZERO Oath Shards/unlocks/mastery (provably progression-free). This DIVERGES from the award/merge (which DENY a manual-seed run) but MATCHES 8.5 — the FR28 narrative-vs-meta boundary precedent. `FirstVictoryRevealBeat` (`scripts/run/`) is a SEPARATE pure-read DTO carrying the FR62 line "It did not die. It learned the way back." (the `FirstDeathNarrativeBeat` twin — line-as-id keyed by `line_id == first_victory`, ONE const, no narrative pipeline): building/reading/"dismissing" it draws ZERO RNG, runs NO command, emits NO event, mutates NOTHING (the latch mutation is `RecordFirstVictoryCommand`, DECOUPLED from the line delivery — a skip/dismiss is STRUCTURALLY a pure no-op, there is NO "skip command"). It is OFF the critical path (AC3): `RunSummary` adds NO narrative field + neither the summary nor the outpost-return/rewards/progression depend on the beat. AC3 render is DEFERRED — the reveal ships as ONLY the DTO + flag + event + tests, NOT wired into `OutpostViewModel` (a later UI story wires it alongside the first-death beat).
- THE SEQUENCE-ID SEAM IS A SHARED MONOTONIC COUNTER ACROSS THE INTERLEAVED BOSS STREAMS (9.3 contract, come-due in 9.4/9.5): a stream-merging caller threads the run-level counter as the base to `resolve_phase_transitions(..., sequence_id_base)` FIRST, threads the returned `next_sequence_id_after` cursor into `detect_boss_defeat(context, sequence_id_base)`, and threads THAT cursor into whatever it appends next (the `run_completed` victory event) — NEVER the board-baseline `next_sequence_id()` fallback (which reads the id without advancing it and would duplicate ids when the boss-action + phase-change + boss_defeat + run_completed streams merge into ONE append-only run log). 9.5 is the FIRST consumer to interleave all four streams end-to-end through the shell and asserts every merged sequence id is unique. Duplicate sequence ids across the merged stream are the specific thing to check in any future boss-loop wiring.
- THE `boss_cleared` RECONCILIATION IS ON VICTORY, VIA `resolve_boss_victory` (9.5, [Decision] A): the live 9.4 victory chain drives `PHASE_COMPLETED` WITHOUT clearing the boss route node, but `RunSummary.boss_cleared` derives from a cleared `TYPE_BOSS` node — so the additive `RunOrchestrator.resolve_boss_victory()` reconciles it: mark the boss node `REVEAL_CLEARED` + idempotently append it to `cleared_node_ids` (the `NodeResolvePlaceholderCommand._resolve_boss` discipline, mirrored — guarded on existing membership so `RouteState.validate()`'s `duplicate_cleared_node` stays green even if called twice) BEFORE the infallible `resolve_run_end(victory)`. `CompleteRunCommand` is UNCHANGED. A documented idempotent-safe mutate-before-reject ordering wart exists (the boss-clear is applied before `resolve_run_end` could reject a wrong-phase run) but `RouteState.validate()` has NO forward-only cleared-set constraint so the boss-clear can never corrupt state, and the clear is idempotent so a retry is safe.
- THE FINALE REGRESSION SUITE IS A COMPOSITE DETERMINISM PIN, NOT A LAYOUT FINGERPRINT (9.5, AC1, `tests/integration/finale/`): `test_finale_seed_regression.gd` is the boss-chain analogue of `test_seed_batch_regression.gd` (the 3.7 approved-annotated-preserved-catalog + failure-report model) — an INLINE `APPROVED_BOSS_SEED_CATALOG` (5-seed, NOT a `tools/dump_*` pin tool: the arena is FIXED + the AI is ZERO-RNG, so there is no layout fingerprint to dump; the "fingerprint" is a COMPOSITE of the deterministic setup/phases/telegraphs/victory/defeat behaviors, each run TWICE and asserted byte-identical). Each approved seed pins: (a) SETUP `BossEncounterRequest -> BossArenaBuilder.build()` -> a reproducible strict-validated arena; (b) PHASES the same HP drop -> the same `boss_phase_changed` chain; (c) TELEGRAPHS the same board+turn -> the same `BossAi` decision + two-turn telegraph->resolve; (d) VICTORY boss -> 0 HP -> the same `boss_defeated` -> the same victory `run_completed`; (e) DEFEAT a seeded-fixture hero death -> the same `run_failed` + cause. The FAILURE REPORT carries seed + phase (never a grid dump); a forced-failure shape test proves the harness never silently passes. `test_finale_full_run.gd` drives the COMPLETE chain end-to-end THROUGH the run shell (start -> `run_to_completion` to the boss-setup terminus -> the live boss fight via explicit turns -> the 9.4 victory/driven-death resolution -> `RunSummary` + `RunEndOutcome`).
- THE BOSS-ATTEMPT DIAGNOSTICS RECORDER IS LOCAL/OFFLINE + DEV-BUILD-GATED (9.5, AC3, `scripts/diagnostics/boss_attempt_diagnostics.gd`): the boss analogue of `LocalTimingRecorder` — a build-profile-gated (`enabled = new_enabled and OS.is_debug_build()`, INERT in release), in-memory recorder that captures a boss attempt's tuning facts (`turn_count`/`damage_taken`/`major_telegraphs`/`outcome`, exact-key `RECORD_KEYS`) so a human can READ them to tune the finale's run arc (the FR31 fair-culmination pass). It is a PURE OBSERVER: ZERO telemetry/network/cloud/account/file-persist dependency (the NFR11 no-live-service rule + the `TelemetrySink`-stays-local platform rule), ZERO RNG, mutates NOTHING (not the run/board/save), emits NO `DomainEvent` (the enum tail is UNCHANGED — a dev record is NOT an append-only event), adds NO save key (ephemeral in-memory). It DERIVES `damage_taken` (sum of `damage_applied` events targeting the hero) + `major_telegraphs` (count of `tile_marked` events) from the attempt's ordered `DomainEvent` stream (the `RunSummary` derive-from-events posture). It is NOT a difficulty knob (it OBSERVES; it does not TUNE — difficulty is a hard non-goal). A dedicated `RefCounted` under `scripts/diagnostics/`, NOT a Node/autoload (9.5 adds NO autoload).
- EPIC-9 EVENTS ARE APPEND-ONLY DETERMINISTIC SYSTEM EVENTS (no actor), wired end-to-end (factory + per-event payload validator + both id maps + JSON round-trip + malformed-negative tests + the exhaustive `expected_ids` pin), NEVER renumbered: `BOSS_ENCOUNTER_STARTED` (9.1), `BOSS_PHASE_CHANGED` (9.2), `FIRST_VICTORY_RECORDED` (9.4, enum index 46), `BOSS_DEFEATED` (9.4, enum index 47 — the current enum TAIL) — appended at the `DomainEvent.Type` enum END after Epic 8's events. All are DETERMINISTIC (Epic 9 draws ZERO RNG — no draw provenance). The `first_victory_recorded.line_id` const is `first_victory` (`DomainEvent.FIRST_VICTORY_LINE_ID`, the LINE-AS-ID). The boss VICTORY reuses the existing `run_completed` (broadened with the `victory` outcome — NO new event); telegraphs reuse `tile_marked`; the DoT/damage reuses `damage_applied`. Continue this discipline for any new event.

### Run Flow, Scene Navigation & In-Run HUD (Epic 11)

Epic 11 is the LIVE-ON-SCREEN epic: it wires the existing Epic-1..9 domain (the live combat loop, the run orchestrator, affinity effects, the run-end→profile bridge, the outpost view model, meta-spend/unlock) into a hands-off start→hero-select→route→live-fight→boss→run-end→outpost→another-descent loop rendered on real scenes, PLUS the FIRST meta-SPEND command that flips a class locked→selectable. It REUSES every prior contract — the 4.3 run-command idiom, the append-only SYSTEM-event enum, the exact-key/fail-closed/no-live-handle view-model discipline, the pure-`RefCounted`-services rule, the named-RNG rule — and forks NO parallel format. **INVARIANTS HELD ACROSS THE WHOLE EPIC: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams (`map`/`level`/`combat`/`loot`/`rewards`/`events`/`cosmetic`) are unchanged (`required_streams()` == 7), `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, every generator/route/finale seed-regression fingerprint is byte-identical (generators untouched all epic — the DEFAULT `run_to_completion` stays byte-identical), `scripts/rules/conditions/` STAYS EMPTY, `scripts/rules/operations/` STAYS exactly one file (`affinity_effect_resolver.gd`), and NO new autoload was added (the flow layer is thin `RefCounted` seams + presenter `Control`s, driven by an unchanged autoload set).** The ONE event added all epic is `oath_shards_spent` (11.6); everything else on screen reuses existing events.

- THE HEADLESS HARNESS IS SCENE-FREE — `.tscn`/`Control` SURFACES ARE VERIFIED BY CONSTRUCTION, NOT SceneTree TESTS (the load-bearing Epic-11 testing constraint): `tests/headless/test_runner.gd` runs each test via `script.new().run()` with NO `SceneTree` (no `add_child`, no `get_tree()` inside a test) — so a presenter `Control` / a `.tscn` cannot be unit-tested by instantiating it in the tree. Epic 11 steers ALL testable logic into `RefCounted` seams: the HUD/route/affinity/outpost PROJECTIONS (`RunHudViewModel`/`RouteMapViewModel`/`LiveAffinityReadModel`/`OutpostRenderView`, each exact-pinned-key), the `RunFlowRouter` route resolver (pure static stage→scene table), and the `RunFlowController` orchestration seam. Scene WIRING is verified by (a) construction/code-audit and (b) a scene-load COMPILE guardrail (`test_run_flow_scenes_load.gd` loads every flow `.tscn` so a broken scene/script fails the suite). Do NOT expect (or write) a SceneTree test for a presenter; put the assertable logic in a `RefCounted` projection with a pinned key set and test THAT. Do NOT trust a prose "it's wired" claim about a guarded accessor without grepping the probed method name against source — a dead `has_method` probe silently no-ops and reads as wired (the 11.3 M2 lesson: a probe for a non-existent `current_text_scale` left HUD text scale hardcoded 1.0). The compile-correct text-scale read is `TacticalTextScale.from_value(snapshot.text_scale).to_dictionary().get("scale", 1.0)` — the `.scale` accessor the reviewer first suggested does NOT exist.

- `RunFlowController` IS THE THIN SCENE-FREE HANDS-OFF DRIVER (`scripts/ui/flow/`, `RefCounted`, NOT a Node/autoload): it composes the existing `RunOrchestrator` (the domain driver it owns no decision beyond) + `LiveCombatResolver` + `RunFlowRouter` into the on-screen loop. `start(root_seed, is_manual_seed, class_id, profile)` starts the run (threading the OPTIONAL profile into the profile-aware class gate — 11.6); `play_hands_off_to_run_end_with_hp(loadout_hp)` drives the live pre-boss walk; `finalize_run_end(bridge)` runs the run-end→profile→outpost bridge and returns the `OutpostViewModel`. It DELEGATES the SceneManager route resolution to `RunFlowRouter` and re-uses the domain's sequencing — it does NOT re-author generation/combat/run-end logic. `current_node_needs_board()` gates the board render to `LIVE_BOARD_NODE_TYPES` (`combat`/`elite_combat`) — a non-combat node NEVER renders the live board.
- RESOLVE-THEN-ADVANCE IS THE SHARED LIVE SEQUENCING SEAM — DEFINED ONCE, TESTED ON THE SEAM (the 11.3 H1 lesson): a live combat node must be RESOLVED before the route ADVANCES. `RouteGenerator` GUARANTEES depth-0 is the opening combat node; an on-screen ADVANCE-then-resolve ordering silently SEALS the unplayed depth-0 node (picking a depth-1 successor makes `RouteAdvanceCommand` clear depth-0 unplayed), diverging from the tested resolve-then-advance live driver and SKIPPING the guaranteed opener. Keep the resolve-then-advance ordering in ONE place (the controller/`LiveCombatResolver`), and when a presenter re-implements a sequencing the domain already encodes, TEST THE PRESENTER'S SHARED SEQUENCING SEAM, not just the domain driver.
- HERO HP IS TWO DISTINCT CONCERNS — THE LIVE-COMBAT LOADOUT HP vs THE DISPLAYED BASELINE (the load-bearing 11.3 distinction, REVISED by 12.2): `StartingKit.baseline_hp` (warrior 18) is a BALANCE number AND — since Story 12.2 (AC1) — the LIVE-COMBAT LOADOUT HP. **12.2 CLOSED the 11.2 "class-kit → combat-loadout is a later story" boundary:** the live combat loadout (HP / weapon / support) now DERIVES from `run.starting_kit` via the pure `CombatLoadout` source (`RunFlowController.hero_hp()`/`hero_weapon_id()`/`hero_support()`), threaded into the 12-1 interactive seam (`begin_interactive_combat_node`/`InteractiveCombatSession`). A run with NO kit (a seed-only / empty-class / legacy run) FALLS OPEN to the driver default (`LiveCombatResolver.DEFAULT_HERO_HP` 60 / sword), so the run still resolves. The `RunHudViewModel` STILL displays the class `baseline_hp` between levels and the LIVE BOARD ENTITY's `current_hp` DURING a fight (there is STILL NO run-level HP field — the two DISPLAY reads are unchanged). **WHAT IS NOT REVISED (keep intact):** no persisted run-level HP field, the in-node fight stays EPHEMERAL, the 23-key `RunSnapshot` gate stays 23. **WHY 18 HP IS WINNABLE (the 11.3 death is superseded by a stronger driver):** the OLD pure focus-fire `LiveCombatResolver` scripted hero still DIES at 18 HP on a full live walk (it closes to melee + eats ash-seer detonations) — that caveat is scoped to the OLD default driver. Story 12.2's STRENGTHENED LoS-aware reference driver (`ReferenceCombatDriver`, retro T2 — a headless PROOF harness, NOT a shipped on-screen loop) makes the 18-HP class loadout WINNABLE on the approved live-combat seed batch by dodging seer detonations + kiting/committing per weapon; on screen the HUMAN drives the 12-1 tap loop (presumed to play at least as well). The auto-resolve `LiveCombatResolver.resolve(...)` DEFAULT (60/sword/no-support) stays the byte-identical seed-batch/hands-off proof driver; an unwinnable seed still fails loud (`live_combat_did_not_resolve` — seed + reason) for triage. The neutral no-support path draws ZERO `combat` RNG; a warrior `shield` support engages the seeded `shield_block` roll on the `combat` stream (an INTENTIONAL, seeded class-path change — re-pin any live-combat fixture it moves).
- `RunFlowRouter` IS THE PURE STAGE→SCENE ROUTE TABLE (`scripts/ui/flow/`, `RefCounted`, static resolvers): the six flow `STAGES` are `launch`→`hero_select`→`route_map`→`tactical_board`→`run_end`→`outpost` (`tactical_board` is the gameplay SHELL = board + in-run HUD; `launch` = `scenes/app/main.tscn`). `scene_path_for_stage(stage)` returns the pinned `.tscn` or `""` fail-closed; `_DESTINATION_STAGES` maps the `RunEndOutcome.next_destination` marker (`outpost`) to its stage. 11.5 RE-POINTED the outpost destination to the real `outpost` scene (the `OutpostViewModel`-bound dashboard) — `run_end` is RETIRED as the outpost nav target and survives ONLY as the gameplay shell's fail-loud NON-terminal dead-end landing (there are NOT two competing outpost surfaces). The route table is test-pinned; a new stage/scene is an intentional table + pin update.
- THE RUN-END→PROFILE BRIDGE IS THE CALLER-DRIVEN SEAM THE LIVE FLOW NEVER HAD (11.5, `RunEndProfileBridge`, `scripts/ui/flow/`): at the live run-END it LOADS the profile (`ProfileRepository.read_profile` → `ProfileSnapshot.fresh()` on `profile_not_found`), RECORDS the first-death/victory LATCH off the REAL terminal `RunState` (`RecordFirstDeath/VictoryCommand`, threaded with a unique `sequence_id > 0` from `RunOrchestrator.next_sequence_id()`), PERSISTS (`write_profile`, with the AC3 write-failure recovery fallback), and BUILDS the `OutpostViewModel` + `RunSummary`. It is the bridge closing the FR1 loop (a working "start another descent") — 11.2/11.3 drove the run terminal but never load/record/persist the profile. It builds `RunSummary.build(run, [])` with an EMPTY events list (the ratified Option (a)): the route/economy facts populate from the terminal `RunState`, but passives/loot/discovery come out EMPTY and `RunSummary.outcome_or_cause` is BLANK — so a summary render MUST key victory/death off `phase` (`PHASE_COMPLETED`/`PHASE_FAILED`) + the reveal beats, NOT `outcome_or_cause`. The run-level event STORE that would populate those lists (+ `outcome_or_cause`) is a later save-shape story; the 11.3 live flow DISCARDS intermediate `ActionResult.events` (do NOT read a presentation/combat log as source truth — the 8.2 AC2 rule).
- `OutpostRenderView` IS THE FAIL-CLOSED RENDER-DECISION SEAM (11.5, `scripts/ui/view_models/`, `RefCounted`): the presenter's render DECISIONS (recovery-mode branch, manual-seed warning, reveal-beat presence, the four deferred named-space markers, the G3 Oath-Shard EARNED-count coupling, and — 11.6 — the Seal-Table spend tiles: `class_unlock_options`/`can_spend_unlock`/`has_affordable_unlock`, all non-color text+icon state) live on this pinned-key `RefCounted`, tested WITHOUT a SceneTree. The G3 coupling reads the AWARDED total from `profile.oath_shards` (`awarded_oath_shards()`); `RunSummary.profile_meta.oath_shards_earned` STAYS `0`/`not_yet_supported` (the 8.2/8.4 pinned contract is intact — no summary→profile coupling). The outpost run-summary panel still surfaces an honest "not yet tallied" note (no explicit victory/death label yet — folded into the later summary-render polish that keys the label off `phase`).
- LIVE AFFINITY ON SCREEN IS AN ADDITIVE OPT-IN ON THE RESOLVER + A FAIL-CLOSED READ AGGREGATION (11.4): `LiveCombatResolver.resolve(...)` gained OPTIONAL `affinity_id` + `affinity_repository` params defaulting to neutral `none`/null — the EXISTING 11.2/11.3 callers stay BYTE-IDENTICAL (verified). `RunOrchestrator.resolve_combat_node_live` ASSIGNS the node's affinity ONCE via the assign-if-absent guard (`assigned_affinity_for(node.id)` reads neutral/absent → assign; a re-drive reads the recorded id back and NEVER re-rolls the `map` stream — this IS the 7.4 once-per-node idempotency call site), runs `DarknessFairnessQuery.check_board` on the LIVE board (a fairness violation STOPS with no partial progression, mirroring `live_combat_failed`), SEATS the cursed-affinity rule source on `run.rules_resolver` (RESOLVE+EXPLAIN, idempotent), and surfaces `board`+`affinity_id`+`darkness_fairness` in the victory/defeat metadata. `LiveAffinityReadModel` (`scripts/ui/view_models/`, exact-key) COMPOSES `AffinityViewModel`+`DarknessReadView`+`AffinityPreviewQuery` and REFLECTS the `DarknessFairnessQuery` verdict (never re-derives one). The affinity is applied POST-generation on a BUILT board (the generator stays affinity-blind, `WRINKLE_AFFINITY_PLACEHOLDER` INERT — fingerprints byte-identical); the boss arena carries NO affinity by decision (a fixed finale board, not an affinity-assigned generated level). Scorched DoT makes live fights resolve slightly FASTER (enemies burn too — changes live round counts on affinity levels only; the neutral/DEFAULT path is byte-identical). The seated Cursed rule source is LIVE re-derivable (NOT serialized) — a later in-node/route-position live-Cursed resume must RE-DERIVE it from the seed-re-derivable affinity id.
- META-SPEND FLIPS SELECTABILITY VIA A PROFILE-AWARE OVERLAY — THE STATIC `ClassDefinition.lock_state` IS NEVER MUTATED (11.6, the crux the whole meta layer deferred since Epic 8): `SpendOathShardsCommand` (`scripts/core/commands/`, the `AwardMetaProgressCommand` idiom at the OPPOSITE sign — `_init(profile, unlock_id, sequence_id)`; reject `sequence_id <= 0` FIRST; `state` arg UNUSED/null, the outpost spend has NO live run — the `RunStartCommand` precedent; fail-close `unknown_unlock`/`unlock_already_applied` (idempotent, retry-safe)/`insufficient_oath_shards` with the shortfall in metadata; validate-then-mutate; ZERO events + byte-identical no-mutation profile on reject; ZERO RNG; the CALLER persists) is the FIRST command that SPENDS `profile.oath_shards`. `MetaSpendRules` (`scripts/save/`, the pure const-config calculator — the `MetaAwardRules`/`UnlockProgressRules` template): `CLASS_UNLOCKS` (necromancer cost 3 / shadeblade cost 5 — the two locked baselines, FR43) + the SINGLE AC2 seam `unlocked_class_ids_for(unlock_progress)` that BOTH the profile-aware `HeroSelectViewModel` AND the authoritative `RunStartCommand` class gate read (both take an OPTIONAL trailing `ProfileSnapshot`, threaded via `RunOrchestrator.start`/`RunFlowController.start`) — so the VM affordance + the gate AGREE. A null profile ⇒ byte-identical static Story-5.2 behavior; a locked class becomes selectable iff (static `LOCK_STATE_SELECTABLE`) OR (its `<class>_unlocked` applied-unlock flag). The spend state (`<class>_unlocked` flags + the `_oath_shards_spent` ledger) lives INSIDE the existing `unlock_progress` Dictionary home (NO new top-level key, NO migration — the seal-fragments/merge-marker precedent). The `oath_shards_spent` SYSTEM event (enum tail after `boss_defeated`; the honest-record `before - amount == after` validator, wired end-to-end incl. `expected_ids`) is the ONLY new event all epic. `OutpostSpendBridge` (`scripts/ui/flow/`) is the caller-driven load→spend→persist→rebuild seam (mirroring `RunEndProfileBridge`; its own monotonic `sequence_id`). FR28 is STRUCTURAL, not a new gate: manual-seed runs never award shards, so there is nothing manual-seed-earned to spend — do NOT add a redundant manual-seed eligibility gate to the spend command.
- STILL DEFERRED AFTER EPIC 11 (RE-RECORDED — do NOT silently close or pre-empt): (a) the Necromancer/Shadeblade CLASS-KIT CONTENT — 11.6 flips their SELECTABILITY but the baseline `necromancer`/`shadeblade` `ClassDefinition`s carry NO kit (no `starting_weapon_id`/`starting_support_id`/passive ids), so starting an unlocked locked-baseline class would fail `RunStartCommand`'s kit/passive resolution (AC2 is proven with a FIXTURE repo whose locked class carries a valid kit); a later CONTENT story authors the real kit — and it MUST then thread the loaded profile into `hero_select_presenter.gd`'s STANDALONE scene (both `HeroSelectViewModel.new()` and `controller.start(...)`) AND make `ClassStartSummaryViewModel.re_derive_kit` profile-aware (it currently gates on the STATIC `def.is_selectable()`, so it re-derives a `null` kit for a profile-unlocked class — ZERO observable effect in v0 since no unlocked run can start, but a live hazard once the kit content lands). (b) the LIVE discovery/echo/Seal-Fragment SOURCE — v0 has NO live path emitting `content_discovered` during a hands-off run (grep-verified: produced by nothing in the live flow), so a live run never earns `unlock_progress` on its own; 11.6 proves AC2 by SEEDING the state a merge would have produced. (c) the LIVE in-node board / pending-fight SAVE (the in-node fight stays EPHEMERAL; the 23-key gate stays 23). (d) the run-level event STORE for a full `RunSummary`. (e) G4 — the settings VIEW MODEL (11.1 appendix §16 G4): Epic 11 built NO settings scene (the outpost/meta menu has NO difficulty selector — the ratified HARD non-goal, appendix §12.3), so G4 stays PARKED. (f) the Flooded `_placeholder` electric interaction remains an EPIC-10 readiness item (replace/de-scope/block) — 11.4 surfaces the deterministic conductive-danger + pathing MARKS live (the ratified MVP posture) but does NOT realize the live water/electric chain; do NOT let "Flooded per its ratified MVP placeholder posture" AC wording silently close it. (g) the affinity-driven GENERATION modifier (reward-odds/elite-rate/spawn from a cursed node) is a SEPARATE later generation-modifier story — 11.4 applies TACTICAL pressure on a built board only.

### Interactive Tactical Combat (Epic 12)

Epic 12 (INSERTED between Stories 10.3 and 10.4 by the 2026-07-07 sprint change, closing the Epic-11 retro T1 [an interactive tap-loop] + T2 [a winnable hands-on hero path]) turns the hands-off auto-resolve run into an INTERACTIVE, human-drivable, class-armed tactical fight on screen. It REUSES every prior contract — the 4.3 run-command idiom, the Epic-1 tactical board/commands/events, the 11.x flow seams, the exact-key/fail-closed/no-live-handle view-model discipline, the pure-`RefCounted`-services rule, the named-RNG rule — and forks NO parallel format. **INVARIANTS HELD ACROSS THE WHOLE EPIC: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams (`map`/`level`/`combat`/`loot`/`rewards`/`events`/`cosmetic`) are unchanged (`required_streams()` == 7 — the class-support shield-block/tome draw rides the EXISTING `combat` stream; NO new stream, NO new draw site, NO `randi`/`randf`/fresh `RandomNumberGenerator`), the `DomainEvent.Type` enum is UNCHANGED (Epic 12 adds ZERO events — tail `boss_defeated` @ 47), `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, every generator/route/finale seed-regression fingerprint is byte-identical (the neutral DEFAULT `LiveCombatResolver.resolve(...)` + `run_to_completion` + the boss auto-play stay byte-identical), `scripts/rules/conditions/` STAYS EMPTY + `scripts/rules/operations/` STAYS exactly one file, the in-node fight stays EPHEMERAL (NO in-node fight save key), and NO new autoload was added.** The tap-loop is the ON-SCREEN path; the reference driver is a HEADLESS PROOF harness — do not confuse the two.

- THE INTERACTIVE SESSION IS THE SCENE-FREE STEP-DRIVEN LIVE FIGHT — ONE ACTION PER TAP (12-1, `scripts/run/interactive_combat_session.gd`, `RefCounted`): `InteractiveCombatSession` is the STEP-DRIVEN counterpart of the one-shot `LiveCombatResolver` — it restores the board, applies affinity (the same building blocks the resolver uses), places the hero, and then advances ONE action per call through `TacticalCommandBridge` + `TacticalAttackCommitFlow`: `begin(board_snapshot, entrance, streams, hero_hp, hero_weapon_id, affinity_id, affinity_repository, hero_support)` (the `hero_support` is the TRAILING 8th param — added by 12-2), `submit_move(target_cell)` / `tap_attack(target_cell, attacker_support, defender_support)` / `inspect(target_cell)`, and it runs the per-action ENEMY phase between hero actions. The assertable tap-loop logic lives HERE (unit-tested `RefCounted`), not in a presenter. It draws gameplay RNG ONLY through the run-level `streams` on the `combat` stream (via the same `AttackCommand`/enemy-phase draws). Do NOT add a board-VM key or a parallel combat path.
- THE ORCHESTRATOR SEAMS ARE ADDITIVE — `resolve_combat_node_live` STAYS BYTE-IDENTICAL (12-1/12-2, `RunOrchestrator.begin_interactive_combat_node`/`finish_interactive_combat_node`): `begin_interactive_combat_node(node, hero_hp, hero_weapon_id, hero_support)` runs the SAME pre-fight steps as the atomic auto-resolve (enter → generate → assign-affinity-once → `DarknessFairnessQuery` → seat-Cursed) then `InteractiveCombatSession.begin(...)`; `finish_interactive_combat_node(node, session)` applies the SAME post-fight resolution (VICTORY → `NodeExitCommand`; DEFEAT → `resolve_run_end(hero_death)`). These are ADDITIVE seams — the atomic `resolve_combat_node_live` + `run_to_completion` + `auto_play_boss_fight` are UNTOUCHED (byte-identical proof paths). `hero_hp()` is now read ONLY on the interactive branch; the boss/placeholder shell branches are DECOUPLED to the tuned `LiveCombatResolver.DEFAULT_HERO_HP` default (threading 18 HP into the naive focus-fire auto-play would reproduce the 11.3 death).
- THE LIVE LOADOUT DERIVES FROM THE CLASS KIT VIA `CombatLoadout` — THE 11.2 BOUNDARY IS FORMALLY CLOSED (12-2 AC1, `scripts/run/combat_loadout.gd`, `RefCounted`): `CombatLoadout.for_run(run, ...)` reads `run.starting_kit` and yields `{ hp, weapon_id, support }` (+ the two RECORDED passive ids + a `derived_from_kit` flag), resolving the support via `SupportRepository` (validated supports only, exactly as `RunStartCommand` did at record time). All three MVP class kits carry `baseline_hp` 18 (warrior sword/shield, pyromancer staff/tome, ranger bow/none). `RunFlowController.hero_hp()`/`hero_weapon_id()`/`hero_support()` are now kit-derived reads of it. **FAIL-OPEN FALLBACK (AC1):** a null starting_kit (a seed-only / empty-class / pre-5.3 / hypothetical kit-less locked class — the T3 guard) yields the DRIVER DEFAULT (`LiveCombatResolver.DEFAULT_HERO_HP` 60 / `DEFAULT_HERO_WEAPON` sword / no support) so the run STILL resolves — it never crashes on a kit-less start. The loadout DECISION lives in the domain/flow layer (`CombatLoadout` + `RunFlowController`); the shell is a thin observer that passes it into the unchanged orchestrator seam. NO passive-combat-effect engine — the kit's passive ids are RECORDED (the derivation), distinctness (AC3) is carried by equipment + support + preview.
- THE STRENGTHENED REFERENCE DRIVER PROVES 18-HP WINNABILITY — A HEADLESS PROOF HARNESS, NOT A SHIPPED LOOP (12-2 AC2, retro T2, `scripts/run/reference_combat_driver.gd`, `RefCounted`): the naive focus-fire `LiveCombatResolver` scripted hero DIES at 18 HP on a full live walk (the 11.3 lesson — it closes to melee + eats ash-seer detonations), so `ReferenceCombatDriver` is a SIBLING `RefCounted` that REUSES the resolver's building blocks (board restore / affinity apply / per-action enemy phase / Scorched DoT / `CombatOutcomeEvaluator` — NOT a forked combat rule) with a LoS-aware hero policy: DODGE ash-seer detonations (a hard mark-end-cell constraint — the dominant 18-HP death is a STATIONARY hero eating repeated marks; `marked_tile_detonated` only hits if the hero ENDS a turn on the marked cell), RANGED classes shoot + kite (>= 2 from melee), MELEE commits to the nearest body then mops up seers. Its `resolve(...)` shape is a clean SUPERSET of `LiveCombatResolver.resolve` + a TRAILING `hero_support` (aligned with `InteractiveCombatSession.begin`). It clears the INLINE annotated approved catalog `[4242, 8080, 6006, 2048, 512]` (a code const in the test — the finale `APPROVED_BOSS_SEED_CATALOG` discipline, NOT a `tools/dump_*` artifact) for all three playable classes at 18 HP; an unwinnable seed FAILS LOUD `live_combat_did_not_resolve` (seed + class + reason — a hard combat error, no partial progression, exactly like a generation failure; proven on a sealed mutually-unreachable fixture). `LiveCombatResolver.resolve(...)` is UNTOUCHED (byte-identical). **KNOWN COVERAGE GAP (deferred, Med): the catalog proves ONLY `small_combat_basic`/`SIZE_SMALL` NEUTRAL boards — the shipped interactive path also hosts the loadout on `elite_combat`/`SIZE_MEDIUM` + affinity-loaded live boards; the Medium/affinity winnability proof is a fast-follow owned by 10.4/10.6 (deferred-work).**
- THE HERO-DEFENSE SHIELD SEAM — THE SHIELD PROTECTS ITS OWNER ON INCOMING ENEMY ATTACKS (12-2 AC3/AC4, the Round-1 human-call design): a hero carries ONE off-hand. It activates as the hero's ATTACKER support on the hero's OWN attacks (a pyromancer `tome` adds its +1 bonus damage — DETERMINISTIC, no draw) and as the hero's DEFENDER support on INCOMING enemy attacks (a warrior `shield` rolls the seeded `shield_block` — halving the hit — so the shield protects its OWNER). The single seeded `shield_block` roll was EXTRACTED into the reusable static `AttackCommand.roll_shield_block(rng_streams, defender_support, actor_id, target_entity_id)` and is REUSED (NOT forked) by both the hero's attack path AND the enemy-attack-against-a-shielded-hero path: `EnemyCommandAdapter._apply_attack` applies the shielded hero's `armor` + the SAME block roll when an enemy hit lands on the seated hero (its ctor gained `_player_id`/`_player_defender_support`), and `EnemyTurnResolver` gained a TRAILING `player_defender_support` (default null) that constructs the adapter with the player id + defender support and SYNCS the enemy-phase simulation streams back to the run-level `combat` stream — so the block draw is a real, seeded, reproducible `combat` draw. **BYTE-IDENTICAL NO-OP WHEN NO SHIELD IS SEATED:** a null defender support draws NOTHING; the stream sync-back is a true no-op across all 7 streams (`RngStreamSet.try_restore` is a FULL OVERWRITE from a byte-identical `to_snapshot()`, never an advance — it cannot double-advance/skip); the `max(1,...)` floor + armor are gated on `defender_support != null`, so the neutral enemy hit stays byte-for-byte the raw `melee_damage`. The earlier INVERTED threading (the hero's shield in the ENEMY's defender slot on the hero's own attack, which slowed the warrior's kills) was REMOVED — the warrior legitimately got stronger (longest approved clear 8080 dropped ~r33 → r15). Every pre-diff `EnemyTurnResolver.new(...)` caller omits the new trailing arg → null → byte-identical.
- THE ADAPTER'S PLAYER ID IS KEPT IN SYNC AT THE LATE-BIND SITE (12-2 Round-2 hardening): `EnemyTurnResolver` late-binds an empty-default `_player_id` from the active actor at resolve time, but the `EnemyCommandAdapter` is built ONCE in `_init`. To make a stale-id state IMPOSSIBLE (rather than merely unreached), the resolver propagates the late-bound `_player_id` into the already-constructed adapter via the tiny `EnemyCommandAdapter.set_player_id(new_player_id)` setter (the ONLY post-construction mutator of the adapter's `_player_id`), called IMMEDIATELY after the late-bind — i.e. AFTER the `invalid_active_actor` guard has already rejected an empty/mismatched id. So a future `EnemyTurnResolver.new(repo, &"", shield_support)` caller can no longer leave the adapter holding `&""` and SILENTLY drop the hero's shield defense. The concrete-id path (every current production/test caller passes the concrete `HERO_ID`) is byte-identical (the set re-writes the same value, an idempotent no-op).

### Live Board Render, Tap Input & Reward HUD (Epic 13)

Epic 13 is the POST-MVP-CLOSE presentation-maturation epic (added after the first human desktop playtest found the on-screen combat surface was text-only and generated no rewards). It makes the 12-x interactive tap-loop actually RENDER and RESOLVE on screen: 13-1 replaces the text-only board region with a live tile-grid render + tap hit-test, and 13-2 wires the post-victory reward + passive Consume/Destroy HUD over the pinned Epic-6/7 contracts. It is PURE PRESENTATION + flow-wiring — it REUSES every prior contract (the 12-1 `InteractiveCombatSession`/`TacticalCommandBridge` seams, the Story-2.4 `TacticalBoardZoomState.screen_to_cell`/`cell_rect` geometry, the Epic-6 reward-offer generate + resolution commands + `PassiveRewardModalViewModel`, the 11.x flow-bridge idiom, the exact-key/fail-closed view-model discipline) and forks NO parallel format. **INVARIANTS HELD ACROSS THE WHOLE EPIC: NO domain command added, NO new `DomainEvent.Type` (tail stays `oath_shards_spent`), NO new RNG stream (`required_streams()` == 7 — Epic 13 draws NO gameplay RNG of its own; the reward GENERATE draws the EXISTING `rewards`/`loot` streams via the unchanged builder), the 16-key `TacticalBoardViewModel` gate stays 16, the 23-key `RunSnapshot` gate stays 23, `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, every generator/route/finale seed-regression fingerprint is byte-identical, `scripts/rules/conditions/` STAYS EMPTY + `scripts/rules/operations/` STAYS exactly one file, and NO new autoload was added.** The reward GENERATE is wired into the INTERACTIVE-shell victory boundary ONLY (`gameplay_shell_presenter._on_interactive_action_committed` victory branch) — never `run_to_completion`/`_resolve_combat`/`auto_play_boss_fight` — so the hands-off driver stays byte-identical.

- THE BOARD DRAW SURFACE + THE TAP HIT-TEST SHARE ONE `TacticalBoardZoomState` — NO SECOND GEOMETRY (13-1, the load-bearing render constraint): `tactical_board_grid_fit.gd` (`scripts/ui/view_models/`, pure `RefCounted`) builds the shared `TacticalBoardZoomState` (square cells, centered origin) that BOTH the tile-grid draw AND the tap `screen_to_cell` hit-test consume — a single source of truth so a tap can never resolve to a different cell than the one drawn (no wrong-cell class of bug). `tactical_board_tap_router.gd` (`scripts/ui/view_models/`, pure `RefCounted`) is the tap-routing DECISION seam (cell-state → `move`/`attack`/`inspect`/`none`), and `tactical_board_grid.gd` (`scripts/ui/presenters/`) is the thin tile-grid draw `Control` + `cell_tapped` forwarder whose `_gui_input` calls `accept_event()` BEFORE `cell_tapped.emit(...)` (input consumption stays independent of the emitted chain's scene swap). The ASSERTABLE logic lives in the two `RefCounted` seams (unit-tested `test_tactical_board_grid_fit.gd`/`test_tactical_board_tap_router.gd`); the draw `Control` is verified by CONSTRUCTION + the `test_run_flow_scenes_load.gd` compile guardrail (the Epic-11 scene-free-harness rule — do NOT write a SceneTree presenter test). Board render reads HONOR the VM's pre-filtered visibility (fog/occupant) — a fogged/memory cell renders as fog, never leaking hidden occupants (no fairness leak).
- APPROVED ART MUST BE IMPORTED + ITS `*.png.import` SIDECAR COMMITTED, AND LOADED DEFENSIVELY (13-1, the durable art-consuming discipline): approved board/tile/affinity art can sit on disk yet be UNRESOLVABLE by `load`/`preload` if it was never imported (no `*.png.import` sidecar) — the exact 13-1 trap (art existed since the Epic-7 pipeline but had never been imported). Fix: run a headless `--import`, COMMIT the generated `*.png.import` sidecars (25 committed with 13-1), and load textures in the presenter via a GUARDED `load()` (NEVER `preload`) so a missing/unimported texture degrades gracefully and the compile guardrail stays green on a fresh checkout. Any future art-consuming story MUST import + commit the sidecars in the same change; the machine-local `.godot/` import cache stays gitignored.
- `*.gd.uid` SCRIPT-UID SIDECARS ARE COMMITTED, NEVER GITIGNORED (13-1 review Decision, ratified human call): 13-1 was the FIRST story to import the Godot project, so Godot 4.6 generates one `*.gd.uid` script-UID sidecar per `.gd` file (~409 now TRACKED). Per Godot 4.4+ official guidance (UID stability across renames/moves), these are COMMITTED — `*.gd.uid` is NOT in any `.gitignore` — so a re-import / editor-open is a git-clean no-op. Future import-touching stories MUST retain + commit them (deleting them breaks the mandatory "git status clean" invariant on the next import).
- THE REWARD/PASSIVE HUD IS PURE PRESENTATION OVER TWO `RefCounted` SEAMS (13-2): `RewardHudViewModel` (`scripts/ui/view_models/reward_hud_view_model.gd`) is the exact-key render projection (`REWARD_KEYS`/`CHOICE_KEYS`) — it REUSES `PassiveRewardModalViewModel.project_offer` (its pinned `MODAL_KEYS`, identity-absent for a generic entry) so it adds NO new key, and it also owns the v0 node→table policy static. `RewardResolutionBridge` (`scripts/ui/flow/reward_resolution_bridge.gd`) is the caller-driven run-command seam that constructs + executes EXACTLY ONE of `ResolveRewardCommand`/`ConsumePassiveCommand`/`DestroyPassiveCommand` against `RunState` with `orchestrator.next_sequence_id()`, mirroring `RunEndProfileBridge`'s idiom (NOT a `TacticalCommandBridge` intent). A passive offer is resolved by AT MOST one of the three commands (the Epic-6 no-double-record rule); a full-backpack generic reward returns `inventory_full` and leaves the offer `pending` (the shell re-renders honestly and does NOT advance).
- BRIDGES THAT EXECUTE COMMANDS LIVE IN `scripts/ui/flow/`; PURE RENDER PROJECTIONS LIVE IN `scripts/ui/view_models/` (the 13-2 placement convention): `RewardResolutionBridge` sits in `flow/` alongside the other execute-capable run-command bridges (`RunEndProfileBridge`/`OutpostSpendBridge`), NOT in `view_models/` — a seam that EXECUTES a run command belongs in `flow/`; only the pure render projection (`RewardHudViewModel`) belongs in `view_models/`. (A story's literal "put it in `view_models/`" guidance yields to its "mirror `RunEndProfileBridge`" instruction — the more specific direction wins.)
- THE v0 NODE→TABLE REWARD POLICY IS ISOLATED IN THE ONE TESTED SEAM (13-2, `RewardHudViewModel.table_for_node_type(node_type, node_depth)`, human-ratified): the guaranteed DEPTH-0 opener `combat` node earns the passive Consume/Destroy 3-choice moment (`passive_reward_choice`); a DEEPER `combat` node earns the generic single-pick `standard_combat_reward`; an `elite_combat` node earns the richer generic `elite_combat_reward`; every other node type earns no combat-node reward HUD (its own surface owns its offer). The depth-0 opener is DETERMINISTIC (the route generator ALWAYS seats depth 0 as `combat` with no type RNG; the run parks there at start) and ALWAYS reachable, so a normal desktop playtest exercises BOTH the passive UI (opening victory) and a generic reward (every deeper combat/elite victory). All three baseline reward tables are now live (`elite_combat_reward` is no longer intentionally-unused). Capture `node_depth`/`node_type` BEFORE `NodeExitCommand` advances. **Follow the story's recommended node→table mapping unless a deviation is argued in-story — the dev's original elite→`passive_reward_choice` deviation cost a review round before the human ratified the story's own Task-1 direction.**
- INSPECT TAPS SURFACE CELL FACTS (13-2 Task 5, closes the carried 13-1 defer): `interactive_inspect(cell)` now stores the returned `CommandBridgeResult` cell facts + re-renders, so an inspect-routed tap surfaces the tapped cell (coords, visibility, occupant id/type/HP) in the inspect region instead of the static "Inspect: tap a cell". It stays a PURE metadata-only read — no mutation, no turn advance. (`_inspect_facts_from` is a presenter-local transform, untested — a candidate for extraction into a tiny tested `RefCounted` seam per the assertable-logic rule; below the AC bar.)
- THE FULL-BACKPACK REWARD SOFT-LOCK IS RESOLVED (13-2 gap -> 14-7 + 14-11), BUT THE FAIL-CLOSED DOMAIN GUARD STAYS UNWEAKENED: a generic backpack-category reward resolved against a FULL backpack returns `inventory_full` (the documented Epic-6 fail-closed domain decision, `resolve_reward_command.gd:25`) and CORRECTLY leaves the offer `pending`. Story 14-7 broke the resulting no-escape soft-lock with the minimal caller-driven `DeclineRewardCommand` skip disposition (see the Epic-14 section) — NOT by weakening the domain guard (`resolve_reward_command.gd`/`pickup_item_command.gd` are byte-identical; the decline never touches the backpack, so it can never hit `inventory_full`). Story 14-11 replaced the hardcoded overlay geometry (previously a full-rect `Panel` with fixed insets, no `ScrollContainer`) with a `TacticalLayoutProfile` content-area modal + `ScrollContainer` + the `modal_frame` Theme StyleBox. Do NOT regress either: keep `inventory_full` fail-closed and route the player escape through `DeclineRewardCommand`, never a guard relaxation.

### Playable & Presentable: Corpse-Clear, Wait, Seed Variation, Escape Hatch & UI Theme (Epic 14)

Epic 14 ("Playable & Presentable") is the SECOND pre-ship backlog epic (added 2026-07-16, executes after Epic 13), opened after an agent-driven desktop playtest proved the MVP loop was NOT honestly finishable/readable (corpse-block soft-lock, no wait, invisible attack preview, silent rejections, class-less Descend Again, hard-cut death). Two bands: **Band 1 (14-1..14-7)** makes the loop FINISHABLE + READABLE (corpse-clear + wait, attack preview + reject cues, combat log + hit feedback, per-run seed variety, run-end beat + summary, live route map, full-backpack escape hatch); **Band 2 (14-8..14-11)** makes it PRESENTABLE (hero-select rebuild, outpost cleanup, player HUD + range highlights, shared UI theme + semantic layout). It REUSES every prior contract — the 4.3 run-command idiom, the Epic-1 tactical command/event model, the exact-key/fail-closed/no-live-handle view-model discipline, the scene-free-harness rule (`script.new().run()`, no SceneTree), the append-only SYSTEM-event enum, the named-RNG rule — and forks NO parallel format. **INVARIANTS HELD ACROSS THE WHOLE EPIC (the ONE justified exception is the 14-1 combat-replay re-pin below): the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams unchanged (`required_streams()` == 7 — Epic 14 adds NO stream and draws NO new gameplay RNG: `WaitCommand`/`DeclineRewardCommand` are ZERO-RNG and the per-run seed is an INJECTED OS-entropy read, never a named stream/`randi()`), the 16-key `TacticalBoardViewModel` gate stays 16, `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, `scripts/rules/conditions/` stays EMPTY + `scripts/rules/operations/` stays exactly one file, NO new autoload, NO new content family/repository, and every generator/route/finale seed-regression fingerprint is byte-identical.** The `DomainEvent.Type` enum grew by exactly TWO append-only tail members — `HERO_WAITED` (14-1) then `REWARD_DECLINED` (14-7) — so `Type.size() == 44` (tail `reward_declined`, index 43). Only the two domain touches (14-1 corpse/wait, 14-7 reward decline) touch `godot/scripts` outside `scripts/ui/`; the other nine stories are pure presentation/flow over `scripts/ui/`. Suite baseline at the epic close: **205 PASS / 0 `^FAIL` / exit 0** (the same 6 documented stderr negatives).

- CORPSE-CLEARING RELEASES OCCUPANCY ON DEATH — THE F1 SOFT-LOCK FIX (14-1, `board_state.gd`): when an entity dies, `board.apply_events` flips `entity.blocks_movement = false` AND releases its cell (`death_cell.occupant_id = &""`), so a corpse is WALKABLE + non-targetable for free (targeting reads the cell `occupant_id`). The corpse STAYS in `_entities` (hp 0, at its position) so the `level_victory_reached.defeated_enemy_ids` payload + the corpse-decal read still see it. Clearing `blocks_movement` keeps the `_cells`/`_entities` occupancy invariant consistent (a non-blocking corpse claims no cell), so BOTH the snapshot round-trip AND the enemy-phase simulation copy stay valid. Setup (`_validate_entity_for_setup`) tolerates ONLY the `cell_occupied` rejection for a NON-blocking entity (still rejects out-of-bounds / WALL terrain), so a hero may legally stand on a corpse cell independent of entity-id storage order. Any future domain-death change MUST weigh this `_cells`/`_entities` occupancy invariant — it is a snapshot-occupancy-invariant consequence the story task did not spell out.
- THE WAIT COMMAND IS THE TURN-ADVANCE BACKSTOP — A TACTICAL COMMAND, ZERO RNG (14-1, `WaitCommand`, `scripts/core/commands/`): `WaitCommand` is the F1 pass-turn guarantee (a turn can ALWAYS advance even when the hero is boxed in with no legal move/attack, so a fight can never permanently soft-lock). It takes a `TacticalActionContext` (like `MoveCommand`, NOT a run command that takes `RunState`), validates active-actor + `PLAYER_PLANNING` phase, emits ONE append-only `hero_waited` event (reasons `voluntary`/`no_legal_action`, mirroring the `enemy_waited` reason vocabulary), BOARD-APPLIES it (so `_next_sequence_id` advances past it — a non-applied wait event would collide sequence ids with the first enemy-phase event), and reports `advances_turn: true` so the enemy phase runs. It draws ZERO RNG (no stream, no draw site).
- THE 14-1 COMBAT-REPLAY RE-PIN IS THE ONLY FINGERPRINT THAT MOVED ALL EPIC — JUSTIFIED (14-1): corpse-clear gives ENEMY pursuers mobility too (dead bodies vacate cells, so melee pursuers path THROUGH corpse cells), so "more mobility can only help" did NOT hold — the `ReferenceCombatDriver` ranger-kite heuristic stalemated the old Medium seed 512 (MAX_ROUNDS, not a loss). The combat-replay seed was deliberately re-pinned 512 -> 24680, and Medium seed 1337 (14x12; 1 iron_cultist + 2 gate_brute + 1 ash_seer) was ADDED to `APPROVED_MEDIUM_LIVE_COMBAT_SEED_CATALOG` to restore ash_seer winnability coverage under corpse-clear (winnable by all three class heuristics). This is the ONE justified combat re-pin all epic; every generator/route/finale fingerprint stays byte-identical. NOTE the ranger kite heuristic is genuinely FRAGILE with `ash_seer` under corpse-clear (only ~24% of driven Medium seer+melee seeds converge for all three classes) — future Medium seer-catalog seeds MUST be found by SEARCH, never picked arbitrarily.
- PER-RUN SEED VARIATION IS A PURE INJECTED-ENTROPY SEAM — NOT A SAVE CHANGE (14-4, `RunSeedSource`, `scripts/ui/flow/`): `RunSeedSource.resolve(configured_seed, entropy)` is the pure static seam deciding the `{root_seed, is_manual_seed}` pair the live new-run caller hands to the authoritative `RunFlowController.start` -> `RunOrchestrator.start` -> `RunStartCommand`. It REPLACED the two hardcoded seed-4242 start sites. MANUAL branch (`configured_seed != 0`): the explicit int64 seed VERBATIM (no mask/truncation) + `is_manual_seed == true` (earns NO meta progression — FR28). ENTROPY branch (`== 0`, the normal run): normalize the injected entropy to a NON-NEGATIVE, NON-ZERO 31-bit seed (`& 0x7fffffff`, 0 -> 1) + `is_manual_seed == false`. 31 bits because `RngStreamSet._derive_seed` masks the base seed to 31 bits anyway (a wider domain wastes entropy); 0 is avoided because it is the `GameSession` "unconfigured" sentinel the manual branch keys off. The ONE impure line — the OS-entropy read — stays in the PRESENTER (a local `RandomNumberGenerator.new().randomize().randi()`, NOT a named gameplay stream, NOT global `randi()`, rejected over the coarser 1-second `Time.get_unix_time_from_system()`); the seam only CONSUMES the injected value, which is what keeps it pure/testable. Determinism is preserved because ALL regression/winnability tests pass EXPLICIT seeds -> byte-identical; only the LIVE run seed varies. 14-4 touches NO `RunStartCommand` / generation pipeline / named stream / save key — a run stays a pure function of its given `root_seed`. `root_seed` is already field #5 of the 23-key `RunSnapshot` (gate stays 23, SCHEMA_VERSION 1, NO migration). Live arbitrary seeds rely on the generator's per-seed fairness/no-soft-lock validation, NOT the driver-winnability batch `[4242,8080,6006,2048,512]` which stays on verified seeds.
- 14-5 SUPERSEDED THE OUTPOST "DESCEND AGAIN" SEED WIRING — ONE LIVE SEED SOURCE AT HERO-SELECT (14-5): the Descend-Again reroute makes the outpost re-descent go through `go_to_stage("hero_select")` (a valid registered stage in `run_flow_router._STAGE_SCENES`), so the outpost's own `RunSeedSource`/`_new_run_entropy`/inline start became dead code and was REMOVED — an intentional convergence to ONE live seed source (hero-select's `_on_confirm_pressed`), NOT a 14-4 regression. `hero_select_presenter._on_confirm_pressed` builds its OWN fresh `RunFlowController` (does not read the outpost's run-flow handle), so the outpost `clear_run_flow()` is safe.
- RUN-END BEAT + SUMMARY KEY OFF `phase`, NOT `outcome_or_cause` (14-5, `OutpostRenderView`/`outpost_presenter`): the run-end outcome label keys off the summary `phase` (`PHASE_COMPLETED`/`PHASE_FAILED`), NOT the live-blank `outcome_or_cause` (the bridge builds `RunSummary.build(run, [])` with EMPTY events — the 11.5 contract; the blank is real). The fail-closed EMPTY `RunSummary` carries `seed == "0"` (NOT `""`), so `summary_seed()` gates on `has_summary` to return `""` when absent — the pattern disambiguates "no just-ended run" from a genuine seed-0 run; future `RunSummary` render slots should copy the gate. The honest oath-shards-EARNED-this-run count reads through `OutpostRenderView.run_oath_shards_earned()` = structurally `MetaAwardRules.oath_shard_award_for(run)` for an eligible completion PLUS the `meta_progression_eligible` gate the rule omits (a manual-seed completion earns 0 — FR28); `RunSummary.profile_meta.oath_shards_earned` STAYS `0`/`not_yet_supported` (the 8.2/8.4 pinned contract is intact — no summary->profile coupling). 14-9 INHERITS this earned count on the same outpost summary surface (it does NOT re-implement it). The FR64 first-death/victory beat stays a pure off-critical-path DTO — Dismiss is a `card.queue_free` no-op; summary/Descend never depend on the beat.
- THE FULL-BACKPACK ESCAPE HATCH IS A RUN-DOMAIN DECLINE COMMAND, NOT A GUARD-WEAKENING (14-7, `DeclineRewardCommand`, `scripts/core/commands/`): `DeclineRewardCommand` (the 4.3 idiom VERBATIM — takes the live `RunState` directly, rejects `sequence_id <= 0` FIRST, validate-then-mutate, ZERO events + byte-identical no-mutation `RunState` on reject, event built AFTER the mutation) CLEARS the run's PENDING reward offer WITHOUT applying it (flip `status` -> `resolved`, `selected_entry` -> `{}`) and emits the `reward_declined` tail event (`reason == player_declined`, distinct from the event id). It RESOLVED the carried 13-2 full-backpack soft-lock. The decline NEVER touches the backpack/inventory/economy, so it can NEVER hit `inventory_full` and does NOT weaken the fail-closed guard (`ResolveRewardCommand`/`PickupItemCommand` byte-identical). It SELECTS no entry (contributes NO `notable_loot` — `RunSummary` single-sources loot from `item_gained`; `RunSummary`'s `match` default-arm ignores `reward_declined`), is offer-type-agnostic but only the GENERIC overlay routes to it (a passive offer always resolves via Consume/Destroy), so each offer still resolves via EXACTLY ONE command (a second decline hits `reward_offer_already_resolved`, no double-record). NO SAVE-SCHEMA CHANGE: it rides the existing `pending_reward_offer` key + flips only already-valid `RewardOffer` fields (`RewardOffer.DICTIONARY_KEYS` stays 9, the 23-key gate stays 23, SCHEMA_VERSION 1). ZERO RNG (structural — no stream param, no draw in the body). The always-present single-tap decline is IRREVERSIBLE (no confirm/undo) — a mis-tap forfeits a takeable reward; the richer decline-confirm / show-only-when-un-takeable disposition is the deferred reward-UX owner (AC3). Do NOT "fix" the residual by weakening the domain guard.
- THE TWO APPEND-ONLY EPIC-14 EVENTS FOLLOW THE FULL WIRING DISCIPLINE (14-1 `hero_waited`, 14-7 `reward_declined`): each was appended at the `DomainEvent.Type` enum END (never renumbered), wired end-to-end (id const + factory + strict per-event payload validator [`hero_waited` `reason` lower_snake; `reward_declined` `table_id`+`reason` lower_snake] + both `id_for_type`/`type_for_id` maps + `_event_requires_actor` membership [`hero_waited` HAS an actor; `reward_declined` is a SYSTEM event, correctly absent] + JSON round-trip + malformed-negative tests + BOTH `Type.size()` pins + the exhaustive `expected_ids` pin). `Type.size()` is now 44. GREP FOR ALL `DomainEvent.Type.size()` COUNT PINS when appending — 14-7 nearly missed a second hard-coded `size()==43` pin in `test_interactive_combat_flow.gd` (caught by the suite, not the checklist). Malformed-negative assertions add NO stderr line (caught `ActionResult.error`).
- BAND-2 PRESENTATION SEAMS ARE PURE `RefCounted` VIEW-MODELS — THE SCENE-FREE-HARNESS RULE HOLDS (14-2/14-3/14-6/14-8/14-10): every new on-screen surface puts its ASSERTABLE logic in an exact-pinned-key `RefCounted` under `scripts/ui/view_models/` — `TacticalAttackPreviewPanel`/`TacticalRejectionFeedback` (14-2 attack preview + reject cues), `TacticalCombatFeedback`/`TacticalCombatLogView` (14-3 hit feedback + combat log), the `RouteMapViewModel` computed accessors (14-6), `HeroSelectRenderView` (14-8), `TacticalHudView`/`TacticalRangeHighlightView` (14-10) — unit-tested WITHOUT a SceneTree; the `Control` presenters (`tactical_board_presenter`/`route_map_presenter`/`outpost_presenter`/`hero_select_presenter`) are verified by CONSTRUCTION + the `test_run_flow_scenes_load.gd` compile guardrail. Do NOT write a SceneTree presenter test. The 14-2 F2 root fix: the VM `preview`/`action_availability` slots must be sourced from the LIVE bound session commit flow (the recurring Epic-14 "live VM slot never sourced from the session" defect class — 14.1/14.2/14.3 each hit it), never the presenter's empty local state. The recurring Band-2 tension — an SM-pinned seam key set carrying fields the presenter does not consume (`turn_is_player`/`has_hud` in 14-10; `KIT_KEYS` in 14-8) — is the "seams expose only what the presenter consumes" rule pulling against the exact-key-set spec; 14-11 CONSUMED the 14-10 spares. The route map (14-6) and combat surfaces (13-x) are VERIFY/HARDEN + render-enrichment, NOT build-from-scratch (both shipped since 11.3/13-x).
- THE FIRST EPIC-14 SCENE ANIMATION IS BOUNDED / SELF-TERMINATING — NEVER `_process` (14-3): the F8 move/hit/death/telegraph feedback is Epic-14's first multi-frame scene behavior. It reconciles with the perf rule "one draw pass per render, never per-frame in `_process`" via bounded self-terminating tweens / transient overlay nodes with detach-safety guards. The moving unit's own draw interpolates origin->destination via a GRID-BOUND tween that re-issues the grid's op list mid-flight (detach-safety holds because a fight-ending grid `free` kills the tween before any `set_ops` reaches a freed node); the hit/death cues are self-freeing overlays; `_active_slides` is cosmetic-only presenter state that never feeds the VM/domain (worst case is a cosmetic glitch, never a board/turn desync). Zero RNG (`lerp`/`clampf`/fixed durations), animate-once via a `_last_animated_sequence_id` high-water. It is verified by compile-guardrail + construction only (no SceneTree runtime coverage) — tween lifecycle / mid-tween detach-safety rest on review + the pending human playtest. The `damage_numbers` seam field was PRUNED as dead output (the emerging Epic-14 convention: seams expose ONLY what the presenter consumes; re-add when a real consumer lands).
- THE SHARED UI THEME IS A GENERATED `.tres` FROM A COMMITTED ONE-SHOT TOOL, APPLIED PER-SCENE-ROOT (14-11): `godot/assets/ui/sealsworn_theme.tres` is the repo's FIRST Godot `Theme` `.tres` — GENERATED by the committed one-shot dev tool `godot/tools/build_sealsworn_theme.gd` (an `extends SceneTree` script under `tools/`, NOT a test, run via `--script`) for guaranteed validity: hand-authoring a `.tres` risks the compile guardrail, so this generate-via-committed-tool pattern is REUSABLE. It assembles nine-patch `StyleBoxTexture`s (BORDER frames, transparent center) from the imported Recraft frame kit (`button_plate`/`panel_frame`/`modal_frame` SVGs — IMPORT + COMMIT the `*.svg.import` sidecars, the 13-1 art-sidecar discipline). The theme is applied via a per-scene-root `theme` assignment on all SIX screen roots (each is a `Control`), NOT `project.godot` `gui/theme/custom` (project settings are FENCED out of scope — the non-obvious constraint shaping the whole story). Godot nine-patch gotcha: `StyleBoxTexture` corners draw at `texture_margin` px 1:1 (NO auto-downscale), so 2048² plates need SMALL margins (20/24/96 used) or corners overflow small controls; an unresolved kit texture yields a null-texture StyleBox (draws nothing, no crash) so the theme still saves/loads green. 14-11 FOLDED the 14-10/13-2 fixed-pixel HUD/reward cosmetics (fonts/spacing/bar-height) INTO the theme.
- A MID-FIGHT RESIZE RE-RENDERS EVENT-DRIVEN, NEVER `_process` (14-11, `tactical_board_presenter`): `tactical_board_presenter.gd` gained the resize handler it lacked — it connects `viewport.size_changed` and COALESCES a burst (a live window drag) into ONE deferred re-layout + re-render so the board re-fits (F15). It is event-driven (the 14-3 one-draw-per-render rule); a no-op `_process` poll is NEVER installed. 14-11 also reworked the reward overlay to `TacticalLayoutProfile` content-area geometry + a `ScrollContainer` + the distinct `modal_frame` Theme StyleBox (RESOLVING the 13-2 hardcoded-overlay-geometry defer so the tall passive 3-choice modal SCROLLS rather than pushing ≥44px targets off), and threaded a new PURE `RewardHudViewModel.display_name_for(projection, content_id)` resolver (fail-closed to the raw id) into the passive-confirm prompt so it reads the evocative name (RESOLVING the 13-2 passive-confirm display_name defer).
- EPIC-14 CROSS-CUTTING VERIFICATION IS DEFERRED TO A HUMAN PLAYTEST — do NOT silently close it: the consecutive Band-1 stories (14-1..14-8) each DEFER their user-facing verification (corpse/wait feel, attack-preview + reject cues, hit/death animation, LIVE seed variety, run-end feel, route-map readability, hero-select) to the STILL-PENDING on-device observed playtest — the automated suite is green but the epic's core promise (honest playability + replay variety + presentability) is unverified by any human pass (presenters carry NO SceneTree test by ratified stance). Human playtests are gated to Band 1. This is a STANDING obligation, not a closed item. Two recurring operational notes: (a) `.gd`-adding presentation-seam stories MUST run `--headless --import` SEPARATELY to emit new `*.gd.uid` (and `*.svg.import`) sidecars — the `--scene` test run does NOT emit them (the 13-1 uid-commit discipline recurs); (b) the hero-select PROFILE-threading stays BUNDLED with the deferred Necromancer/Shadeblade class-kit CONTENT story (14-8 renders necro/shade locked via `_locked` portraits + cost, does NOT thread the profile into the standalone `hero_select_presenter.gd`, and `ClassStartSummaryViewModel.re_derive_kit` stays STATIC-gated — a live hazard only once the kit content lands).

### Readiness, Performance, Seed-Regression & Fairness (Epic 10 — COMPLETE: harnesses 10-1/10-2/10-3/10-8 + readiness/audit/gate tranche 10-4/10-5/10-6/10-7)

Epic 10's first tranche (10-1/10-2/10-3/10-8 — which executes BEFORE Epic 12's 12-1/12-2, then Epic 10 resumes at 10-4 after Epic 12; the sprint-status FILE ORDER drives picking) added the MVP-READINESS measurement + consolidated-regression + fairness layer plus the moving-LoS Darkness-fairness strengthening. **All four stories are READ-ONLY over the domain: they touched NO production generator/gameplay/RNG/save/content source, re-pinned NO seed-regression fingerprint, added NO new RNG stream / event / save key / autoload; every determinism/save invariant (7 RNG streams, 23-key `RunSnapshot`, `SCHEMA_VERSION==1`, all fingerprint sources) is VERIFIED intact.** The Epic-10 readiness CLOSE is the two roll-up GATES authored in the second tranche (10-6 MVP-readiness + 10-7 asset/audio/UX) — the harnesses FEED those gates but do not themselves DECIDE readiness. The four second-tranche stories (10-4/10-5/10-6/10-7) are AUDIT/GATE work: they VERIFY + DECIDE + ROLL UP, produce durable planning-artifact docs under `_bmad-output/planning-artifacts/`, and change NO production `godot/` code (10-5 added the ONE net-new read-only test all tranche — the run-flow cue-coverage test; 10-6/10-7 added none, keeping the suite byte-for-byte green). The execution order is `10-1 → 10-2 → 10-3 → 10-8 → 12-1 → 12-2 → 10-4 → 10-5 → 10-6 → 10-7`; **10-7 is the REAL Epic-10 close** (10-8 is numbered last for list continuity but ran early after 10-3). The retrospective + `epic-10: in-progress → done` transition are orchestrator/Phase-8-owned AFTER 10-7 — no gate story flips epic status.

- PERFORMANCE-BUDGET MEASUREMENT IS A LOCAL/OFFLINE DEV-BUILD-GATED OBSERVER (10-1, NFR4/NFR5/NFR6, `scripts/diagnostics/performance_budget_report.gd`): `PerformanceBudgetReport` is the `LocalTimingRecorder`/`BossAttemptDiagnostics` SIBLING — a build-profile-gated (`enabled = new_enabled and OS.is_debug_build()`, INERT in release) in-memory recorder that captures each measured value, compares it to its project-context budget, and emits an actionable record (exact-key `RECORD_KEYS`: `system`/`subject`/`measured_ms`/`budget_ms`/`delta_ms`/`passed`). The four MVP budgets are CONSTS: `BUDGET_LEVEL_LOAD_MS` 3000 (NFR4), `BUDGET_PREVIEW_RESPONSE_MS`/`BUDGET_SELECTION_RESPONSE_MS` 100 (NFR5), `BUDGET_FRAME_60FPS_MS`/`BUDGET_FRAME_30FPS_MS` (NFR6, for an on-device frame-time compare). It is a PURE OBSERVER — ZERO telemetry/network/cloud/account/file-persist (NFR11), ZERO RNG, mutates NOTHING, emits NO event, adds NO save key; it does NOT tune the game (difficulty is a hard non-goal — it OBSERVES). The headless/desktop harness `tools/dump_performance_budgets.gd` feeds it real `LevelGenerator.generate` + `Epic1MicroCombatScenario` timings; a later on-device pass feeds it frame/latency numbers the same way. (10-1 also noted: `export_presets.cfg` already carries an iOS `preset.2` scaffold; all three presets share the identical `exclude_filter` — iOS packaging is availability gap G7 for the 10.6 gate.)
- THE CONSOLIDATED SEED-REGRESSION SUITE HAS ONE UNIFORM CONTRACT + NO SECOND FINGERPRINT FORMAT (10-2, `tests/integration/test_seed_regression_suite.gd`): the seed-determinism analogue of the 10-1 perf harness — ONE surface reporting a uniform `fingerprint + seed/system/phase/reason` contract across the SIX named systems (tactical, generation, route, reward/passive, affinity, boss) plus the pause/resume proof + cosmetic independence. **THE CRUX (the single most likely review miss): every system ALREADY has a SINGLE canonical fingerprint/determinism source — this suite CALLS those sources and REUSES each per-system fixture's EXACT pinned constant (imported), so there is literally NO second copy that can silently drift.** A re-pin happens in ONE place (the per-system fixture) and this suite follows automatically; an accidental un-re-pinned generator change makes it FAIL loud. A FORCED-failure shape test proves the harness can never silently pass. `tools/dump_seed_regression_report.gd` is the `tools/`-gated print-only report driver. 10-2 also expanded the ROUTE sample 8→20 (the Task-3-sanctioned edit — the canonical fixture + its dump tool; the original 8 pins byte-identical) — a specific sanctioned edit OVERRIDES a blanket "every `tools/dump_*` untouched" clause.
- THE SHARED SMALL/MEDIUM 50-SEED CATALOG + THE MVP-READINESS SAMPLE TARGETS SPAN THE THREE EPIC-10 HARNESSES — EXPAND TOGETHER, NEVER DESYNC (10-3/10-8, the epic-wide convention): the Small/Medium seed catalog is SHARED by the 10-1 perf harness, the 10-2 consolidated regression suite, and the 10-3 fairness batch — expand ALL THREE together (the 10.6 gate decides), never desync or re-pin one harness alone. The suite's `MVP_READINESS_TARGETS` + `CURRENT_SAMPLE` consts (+ the durable `seed-regression-suite-readiness.md`) declare the targets the 10.6 gate consumes: generation Small 50 / Medium 50, tactical 25, route 20, reward 20, boss/finale 10, **affinity 10-per-implemented-affinity** (the flat size is a proxy; the REAL proof is per-affinity). A silently-shrunk sample FAILS LOUD.
- THE GENERATOR FAIRNESS BATCH COMPOSES THE TWO EXISTING VALIDATORS — NO SECOND FLOOD/LoS (10-3, `tests/integration/test_generator_fairness_batch.gd`): a read-only batch harness COMPOSING `LevelValidator` (3.6) + `DarknessFairnessQuery` (7.6) over the shared catalog; `tools/dump_generator_fairness_report.gd` is the print-only report driver; `generator-fairness-batch-readiness.md` is the durable ledger the 10.6 gate consumes. It touched NO production generator/validator source and re-pinned NO fingerprint. The genuine FR58 `darkness_unseen_hazard` finding on Medium seeds 4004+5005 is an honestly-classified 10.6-GATE-OWNED readiness signal (recorded in §4 of that ledger; the resolution — tune generator / strengthen predicate / accept documented limitation — is gate-owned), NOT a defect and NOT a cross-story defer. Epic-retro lesson: test the WHOLE recipe×affinity matrix — the 7.6 `DarknessFairnessQuery` suite only ever exercised Small (all-FLOOR) seeds, so the Medium-recipe baked hazards under Darkness went undetected until the 10-3 batch (the "v0 boards are all-FLOOR" premise is true only for Small).
- DARKNESS FAIRNESS PREDICATE (b) IS NOW MOVING REDUCED-RADIUS LoS "SEEN-BEFORE-CONTACT" (10-8, `scripts/generation/level/darkness_fairness_query.gd`): predicate (b) was STRENGTHENED from static-from-ENTRANCE to a MOVING reduced-radius LoS test — every REACHABLE `Terrain.HAZARD` cell (walkable under stepwise 4-neighbour movement) must be LINE-OF-SIGHT visible at the DARKNESS-REDUCED radius from at least one reachable 4-neighbour "step-from" cell (the hero necessarily SEES the hazard the turn before they could step onto it); a reachable hazard with NO seen-before-contact step FAILS LOUD `darkness_unseen_hazard` (seed + phase, never a grid dump). This RESOLVED the FR58 Medium 4004/5005 finding (Medium flips to legitimate PASS). It stays a SEPARATE additive board-scoped query — NOT wired into the generation success path (`LevelValidator`/the generators untouched → fingerprints byte-identical); the FAIL branch is exercised via hand-built boards until a later story bakes hazards into generated terrain. **CONVENTION (10-8 lesson): when changing a validator/predicate contract, grep-sweep ALL tests/tools/ledgers for baked expectations BEFORE scoping ACs** — four stale static-from-entrance FAIL expectations existed but only two were named in the epics.md ACs (the live-gate violation board + 10.2's honest-sample block were found only by a full-codebase sweep).
- THE CURATED `AFFINITY_SEED_BY_AFFINITY` LIST MUST BE REGENERATED, NEVER HAND-EDITED (10-8, in `test_seed_regression_suite.gd`): the affinity sample landed exactly 10-per-affinity across 40 seeds with NO slack; `AFFINITY_SEED_BY_AFFINITY` documents which seed maps to which affinity, and the suite cross-checks each documented mapping against the LIVE assignment — so ANY generator/RNG change that shifts an assignment FAILS LOUD (by design). REGENERATE the curated list via the affinity-seed search / dump tooling; NEVER hand-edit a value to silence a drift (the same deliberate-update discipline as the level/route/finale fingerprints). The 10-8 sample expansion was COORDINATED across Small/Medium 5→50, tactical 8→25, reward 8→20, boss 5→10, affinity →40 (route untouched at 20).
- THE FINAL MVP-READINESS GATE VERDICT IS `READY_WITH_GATES` — EVERY OPEN GAP IS A DOCUMENTED LIMITATION WITH AN OWNER, NONE A HARD BLOCKER (10-6, `_bmad-output/planning-artifacts/mvp-readiness-gate.md`, FR70 + the FR30 run-length half): the gate VERIFIES the live loop end-to-end (all TEN AC1 steps present + winnable — launch / start run / choose class / generate-or-enter levels / fight / collect rewards / make passive choices / die-or-win / view summary / start another descent; a MISSING step would block readiness, but none is missing), RUNS the fresh full suite (**191 PASS / 0 `^FAIL`** — the post-10.5 baseline), CONFIRMS the pre-export invariants + the regression-enforced difficulty non-goal, ROLLS UP the four sibling ledgers (10.1 device-tiers, 10.2 seed-regression, 10.3 fairness, 10.4 comprehension) + the two audits (10.5 accessibility, 11.1 UX appendix), and DECIDES each open gap. It moves NO pinned invariant. Three loop steps carry a recorded (NON-blocking) limitation: "view summary" is THIN (`RunSummary.build(run, [])` → `outcome_or_cause` BLANK + empty passives/loot/discovery; the outcome is conveyed non-color via `phase` + the reveal beats, NOT `outcome_or_cause` — the run-level event-store/summary-render owner), and "collect rewards" + "make passive choices" are PRESENT + integration-proven (`test_loot_passive_build_smoke_run.gd`) but caller-driven / live-HUD-wiring-deferred. The **physical-device gaps G1–G7** (on-device load/FPS/memory/battery/thermal, real-touch latency, iOS packaging = G7) + the observed-session gaps (OSG-1..4, ≥5 human sessions) + the human-eyes accessibility gaps (ASG-1/ASG-2) are all acceptable documented v0-CANDIDATE limitations owned by a pre-ship physical-device pass — a headless agent legitimately records them against an owner rather than inventing device data. The preserved build is a v0 CANDIDATE, not the final ship.
- THE PRE-EXPORT / RELEASE-GATE INVARIANTS ARE THE HARD PRODUCTION-BUILD CHECKLIST (10-6 AC3, proven by `test_export_setup.gd` + the recorder tests — REUSE these, do not re-implement): (a) the three measurement/diagnostics recorders (`LocalTimingRecorder`, `BossAttemptDiagnostics`, `PerformanceBudgetReport`) are `OS.is_debug_build()`-gated INERT — `enabled = new_enabled and OS.is_debug_build()`, so in a NON-debug (release) export every `record_*` is a no-op and `records()` stays empty; (b) all three export presets (Windows `preset.0`, Android `preset.1`, iOS `preset.2`) carry the IDENTICAL `exclude_filter` that drops `addons/**,data/source/**,scenes/debug/**,tests/**,tools/**,**/*_test.gd,**/test_*.gd` — so every test + every `tools/dump_*` driver + `scenes/debug/**` is PROVABLY excluded from production; (c) `PlatformServices` (`scripts/platform/platform_services.gd`) is a LOCAL NO-OP — `record_telemetry`/`unlock_achievement` are empty `pass`, `sync_save` returns `ActionResult.ok()` with no network call; the `TelemetrySink`/`SaveSyncProvider`/`AchievementProvider`/`CrashReporter` names are the design-time INTERFACE POSTURE, not types that exist in code; (d) save truth = versioned domain snapshots (`RunSnapshot`/`ProfileSnapshot`/`SettingsSnapshot`), never a scene node; (e) offline-first single-player — no accounts/cloud/leaderboards/multiplayer/live-service. The manual pre-export checklist: confirm the `exclude_filter` on every preset, confirm a non-debug release build (recorders inert), confirm `PlatformServices` still the local no-op, confirm no debug/seed/fog/LoS/cheat overlay is registered in the shipped scene tree. iOS `preset.2` is a SCAFFOLD (`runnable=false`, blank signing/icons — a producible iOS binary needs macOS + Xcode) = availability gap G7; the `exclude_filter` still HOLDS on it.
- THERE ARE TWO DIFFERENT "G4"s — KEEP THEM DISTINCT (10-6/10-7 durable disambiguation; the naming collision is a real doc-ambiguity trap that persists into the settings-scene + 10.7 stories): **device-tiers §6 G4** = the on-device / windowed render-frame profiler for sustained 60/30 FPS frame stability (NFR6 — a headless run has no render-frame loop), one of the physical-device G1–G7 set; **UX-appendix §16 G4 (settings)** = the settings VIEW MODEL / settings SCENE gap (PARKED — no settings scene exists, the outpost has no difficulty selector; = accessibility finding F-3, a PAPER audit until the scene is built, owned by the settings-scene owner 11.3/11.5). The bare label "G1–G7" ALWAYS means the device-tiers physical-device set; the settings gap is ALWAYS written "appendix §16 G4 (settings)". Never conflate them.
- THE ASSET/AUDIO/PLACEHOLDER GATE DE-SCOPES AUDIO FOR v0 AND COUNTS THE VISUAL ROSTER HONESTLY (10-7, `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md`, FR46 + FR112/FR113/FR114 + NFR17/NFR18): the SHIPPED `asset_sources/asset-manifest.md` is the roll-up truth. **AUDIO IS DE-SCOPED (non-gating) for v0** — all 14 SFX (`sfx.*`) + 7 ambient loops (`amb.*`) are a `planned`/0-file placeholder track (`godot/assets/audio/{sfx,ambience,music}/**` exist but hold NO files); the v0 candidate ships SILENT and loses NO required information because NOTHING is audio-only (every AC2 feedback meaning has a visual/textual equivalent; only the two preview/commit cues even declare an optional `audio_cue_id`, and each keeps a non-color visual channel — proven by `test_tactical_accessibility_cues.gd` + `test_run_flow_accessibility_coverage.gd`). The 13 required feedback meanings each map to a shipped `sfx.*` cue id AND a past-tense `DomainEvent.Type` (movement→`ENTITY_MOVED`, hits→`DAMAGE_APPLIED`, hazards→`DAMAGE_APPLIED`+`TILE_MARKED`, door-sealing→`ROUTE_SEALED`, death→`RUN_FAILED`+`FIRST_DEATH_RECORDED`, reward→`REWARD_OFFERED`/`REWARD_RESOLVED`, boss victory→`BOSS_DEFEATED`+`RUN_COMPLETED` outcome `victory`+`FIRST_VICTORY_RECORDED`, …) — presentation MIRRORS the event, never drives domain control. **THE COUNTED TRUTH: 28 placeholder passive-icon GLYPHS exist as generated art** (`icon.passive.001`–`028`, in the ratified 20–30 band) — these are ICON placeholders on disk, DISTINCT from the passive DEFINITION roster, which is STILL only the 6 class baselines (the broader 20–30 passive-DEFINITION pool is NOT authored — do not conflate the glyph art with the rules content). The whole visual roster (~92 assets: 5 class ×2 views, 3 enemies, 1 boss, 4 affinities, 9 tiles + props, 11 item icons, the 28 passive glyphs, 4 currency, 9 UI frames, ~9 overlays/banners) is `approved`/`generated`; every required roster class maps to a tracked-or-better id (no "no id, no asset" gap). Readability is gated by the manifest's 3-POINT READABILITY GATE — **grayscale · phone-size · silhouette** (the 22 board-art assets passed it; the `generated`-but-`☐` icons are approval-pending on import, an honest state, not a failure).
- ASSET PROVENANCE IS 10 FIELDS AND THE STATUS VOCABULARY PREVENTS SILENT PROMOTION (10-7 AC3, NFR17/NFR18): every AI-assisted / placeholder asset entry carries all 10 required fields across the manifest + `provenance-log.md` + `prompt-pack.md` — stable id, status, tool/source, prompt (if any), date, source reference, license/provenance notes, EDITABLE-SOURCE path (`asset_sources/...`), RUNTIME-EXPORT path (`godot/assets/...`), approval status. The status ladder `planned → placeholder → generated → approved` (+ `descoped`/`blocking`) is MONOTONE: a `generated`-but-☐ asset is NOT `approved` (the ☐→☑ Approved checkbox IS the 3-point-gate pass), and `_placeholder` in a filename / a `_placeholder` cue id marks a placeholder at both the asset and code level — so an unapproved/exploration asset is STRUCTURALLY distinguishable and cannot be silently shipped as production. No open placeholder is `blocking`: the Flooded conductive `affinity_conductive_danger_placeholder` (+ `..._vfx`) is de-scoped-with-an-approved-limitation (it already carries a non-color shape+label+text channel — the danger reads with color stripped even as a placeholder; the live water/electric art/VFX + final cue is a post-MVP owner), the audio track is de-scoped (above), the `RunSummary` `not_yet_supported` tallies are de-scoped (run-level event-store owner), and the Story-4.5 boss placeholder was REPLACED by Epic 9.
- THE COMPREHENSION + ACCESSIBILITY AUDITS BACK THE FELT-EXPERIENCE GAPS WITH HEADLESS OBJECTIVE PROOFS (10-4 `mvp-playtest-comprehension-checklist.md` + 10-5 `accessibility-and-readability-audit.md`, NFR7/8/9 + FR30): FR30 run-length is the seeded 8–12-node route → boss (Epic 4). Verified HEADLESS (NOT gaps): the run-flow cue-coverage FACT — every affinity/Darkness cue resolves NON-color, proven by the one net-new test `test_run_flow_accessibility_coverage.gd`; the OBJECTIVE per-class distinctness (the 12.2 proof); the Small/Medium-neutral + Scorched winnability inputs (the extended `ReferenceCombatDriver` catalog — 10-4 CLOSED the 12.2 Medium/affinity winnability gap by ADDING coverage, not scoping it away: Medium boards are materially harder [the melee warrior often hits the round cap] and Scorched DoT flips some neutral-winnable seeds to losses, so all-win Medium seeds are relatively rare — the driver handles Scorched hazard cells but does NOT dodge them [mark-dodge covers only seer telegraphs]; winnability holds because the commit/kite policy clears fights fast enough to stay ahead of the burn). The difficulty non-goal is confirmed at the SURFACE level too (the outpost/meta menu presents NO difficulty selector — nothing to audit). The felt dimensions — ≥5 observed human sessions, felt consumable value / class distinctness / pacing (OSG-1..4), real contrast-ratio + phone-hardware legibility (ASG-1/ASG-2) — are the physical-device/human-eyes availability gaps handed to the 10.6 gate; a headless agent records them against an owner, it does not fabricate the human passes.

### Presentation, View-Model & Accessibility Rules (Epic 2)

- Presentation reads domain via semantic view models and submits commands through the command bridge (`scripts/ui/command_bridge/`). View models never own domain truth; they project it.
- `TacticalBoardViewModel.to_dictionary()` has an EXACT sorted-key contract pinned by `test_tactical_board_view_model.gd` (16 keys post-Epic-2, including `layout` and `accessibility`, each defaulting to `{}`). Adding a slot must intentionally bump that assertion — never let a key appear or vanish silently.
- Adaptive layout is a testable SEMANTIC profile first: `TacticalLayoutProfile` plus the sanitized `TacticalBoardViewModel.layout` slot prove the contract scene-free. Build the `Control`/scene presenter (`scripts/ui/presenters/`, `scenes/ui/layouts/<profile>/`) only in a later HUD story. Touch targets stay >= 44px; slots that cannot fit honestly report `reachable: false` rather than overflowing.
- Accessibility is color-INDEPENDENT and audio-absent-equivalent: every committed-action cue must have a visual + textual equivalent that holds with audio off. Preserve the `feedback_preview` (pre-commit, attack-only) vs `feedback_committed` (post-result) distinction; do not collapse them. `AttackPreviewContractMatrix` (`tests/fixtures/tactical/`) is the canonical driver for the color-independence / cue audit — extend it when adding cue vocabulary so no preview ships without an accessibility mapping.
- `TacticalAccessibilityModel` is NOT a settings store; it derives cues from tactical state. Settings holds only the boolean preference (`colorblind_safe`, `high_contrast`) and hands it to this layer at the presenter boundary.

### Settings & Preferences Rules (Epic 2)

- Settings are SAFE preferences only: text scale, master volume / mute, an input-scheme preference, and presentation-hint accessibility toggles. They persist to their own `user://settings.json` via `SettingsRepository` and are NEVER folded into the run autosave or any tactical/RNG/progression state. Future preference work belongs in `scripts/settings/`.
- Loading settings must NEVER hard-block the player at boot. Read policy: missing file (first launch) -> `defaults()` as SUCCESS; unreadable/malformed JSON -> `defaults()` as SUCCESS with a `recovered` diagnostic; schema-version mismatch -> structured `unsupported_settings_schema` error (incompatible build, surfaced not silently overwritten). `SettingsManager._ready()` catches errors and keeps in-memory defaults.
- `SettingsSnapshot.parse` is strict-on-schema, lenient-on-fields: every field is coerced/clamped to named bounds/defaulted; each field's sanitization lives in exactly one helper. Reuse the canonical `TacticalTextScale` clamp rather than writing a second one. Small bounded floats (volume dB) round-trip exactly through JSON; no int64-string encoding needed there.
- Immediate-apply is presentation-only (`SettingsApplyService`): it drives the audio Master bus and surfaces a clamped text-scale presenter hint. Applying ANY preference must leave board/RNG snapshots byte-identical — no command, no RNG draw, no tactical/reward/progression mutation. Guard a missing audio Master bus so headless apply never crashes.
- DIFFICULTY IS A HARD NON-GOAL. Sealsworn has NO player-selectable difficulty tiers. No setting (and nothing in `PREFERENCE_KEYS`) may scale enemy stats, HP, damage, reward rates, RNG, or run length. MVP difficulty comes from run depth, enemy patterns, affinity pressure, elite nodes, risk rewards, attrition, and boss prep; post-MVP challenge is explicit variant/trial/oath/special-run content, never a generic difficulty ladder. This is REGRESSION-ENFORCED at the contract level (10-6 AC4): `SettingsSnapshot.PREFERENCE_KEYS` is exactly the 6 safe keys (`text_scale`, `master_volume_db`, `audio_muted`, `input_scheme`, `colorblind_safe`, `high_contrast`), and `test_settings_snapshot.gd::_difficulty_non_goal_keys_are_absent` asserts a `FORBIDDEN_DIFFICULTY_KEYS` set (`difficulty`/`difficulty_tier`/`easy`/`normal`/`hard`/`challenge_level`/`enemy_scaling`/`damage_multiplier`) is absent from both `PREFERENCE_KEYS` and the serialized dict AND that `parse()` DROPS an injected `difficulty_tier`/`enemy_scaling` — so a contributor cannot add one silently. Do not add a difficulty key or weaken this test.

### Performance Rules

- Generated level load target: under 3 seconds for MVP.
- UI preview and selection response target: under 100ms.
- Target stable 60 FPS where feasible; 30 FPS is acceptable on lower-end mobile if input remains responsive.
- Phone-sized combat readability is a first-order requirement, not polish.
- Avoid per-frame work in nodes that can update through events, signals, or explicit refresh calls.
- Cache node references with `@onready`; do not call `get_node()` in hot loops.
- Use static typing in GDScript hot paths.
- Preload only critical shared assets. Load screens/levels by scene boundary. Add threaded loading for heavier art/audio later.
- Define and measure low/mid/high mobile device tiers before production planning.

### Code Organization Rules

- Production Godot files live under `godot/`. Do not mix production implementation into `prototype/`.
- Use the architecture-defined roots: `scripts/`, `scenes/`, `assets/`, `data/`, `tests/`, `tools/`.
- Organize scripts by domain: `core`, `tactical`, `rules`, `generation`, `ai`, `content`, `save`, `settings`, `run`, `ui`, `platform`, `diagnostics`, `utils`. Autoload scripts live in `scripts/autoloads/`.
- Keep `scripts/tactical/`, `scripts/rules/`, `scripts/generation/`, `scripts/ai/`, `scripts/save/`, `scripts/settings/`, `scripts/run/`, and `scripts/diagnostics/` independent of scene nodes for authoritative/data logic (they are `RefCounted` services + DTOs, not Nodes). The `run` domain holds the run-progression MODEL (`RunState` phase machine, `RouteState` graph, `RouteNode`) AND the thin scene-free `RunOrchestrator` start-to-end driver; route *generation* (`RouteGenerator`, `RouteValidator`) lives separately under `scripts/generation/route/`, and the run-domain COMMANDS live under `scripts/core/commands/` with the tactical commands (Epic 4). The `save` domain holds BOTH the within-run save (`SaveRepository`/`user://run_autosave.json` + `RunSnapshot`/`RunResumeService`) AND — since Epic 8 — the CROSS-RUN meta profile (`ProfileSnapshot`, `ProfileRepository`/`user://profile.json`, and the pure `MetaAwardRules`/`UnlockProgressRules` calculators); the two save files + repositories are strictly independent. The Epic-8 run-END read surfaces (`RunSummary`, `RunEndOutcome`, `FirstDeathNarrativeBeat`) + the Epic-9 `FirstVictoryRevealBeat` live under `scripts/run/`; `OutpostViewModel` lives under `scripts/ui/view_models/`. The Epic-9 boss finale is DISTRIBUTED across the existing domains (NOT a new `boss/` root): boss CONTENT (`BossDefinition`/`BossPhaseDefinition`/`BossActionDefinition`) under `scripts/content/definitions/`, `BossRepository` under `scripts/content/repositories/`, `BossPhaseResolver`/`BossPhaseTransition` under `scripts/content/boss/`; the boss TURN driver + adapter (`BossTurnResolver`/`BossCommandAdapter`) under `scripts/tactical/turns/`; `BossAi` under `scripts/ai/`; boss SETUP (`BossArenaBuilder`/`BossEncounterRequest`) under `scripts/generation/boss/`; the boss COMMANDS (`BossNodeEnterCommand`/`RecordFirstVictoryCommand`) under `scripts/core/commands/`; the dev tuning recorder (`BossAttemptDiagnostics`) under `scripts/diagnostics/`. Finale INTEGRATION tests live under `tests/integration/finale/`.
- UI logic lives in `scripts/ui/view_models/` (semantic view models like `TacticalBoardViewModel`, `TacticalLayoutProfile`, `TacticalAccessibilityModel`, the Epic-13 `TacticalBoardGridFit`/`TacticalBoardTapRouter`/`RewardHudViewModel`, plus the Epic-14 presentation seams `TacticalAttackPreviewPanel`/`TacticalRejectionFeedback` [14-2], `TacticalCombatFeedback`/`TacticalCombatLogView` [14-3], `HeroSelectRenderView` [14-8], `TacticalHudView`/`TacticalRangeHighlightView` [14-10] — all pure `RefCounted` render PROJECTIONS or routing DECISIONS that own no mutation), `scripts/ui/command_bridge/`, `scripts/ui/flow/` (the thin `RefCounted` flow SEAMS + the EXECUTE-capable run-command bridges `RunFlowController`/`RunFlowRouter`/`RunEndProfileBridge`/`OutpostSpendBridge`/`RewardResolutionBridge`, plus the Epic-14 pure `RunSeedSource` seed-source DECISION seam [14-4]), and `scripts/ui/presenters/` (`Control` nodes, incl. the Epic-13 `tactical_board_grid.gd` tile-grid draw surface); layout scenes live in `scenes/ui/layouts/`. PLACEMENT RULE (13-2): a seam that EXECUTES a run/tactical command lives in `flow/` (or `command_bridge/`), NEVER in `view_models/`; `view_models/` holds only pure render projections + routing decisions. The two Epic-14 domain touches live with the tactical/run commands under `scripts/core/commands/` (`WaitCommand` [tactical, 14-1] + `DeclineRewardCommand` [run-domain, 14-7]); the one-shot Theme builder `build_sealsworn_theme.gd` lives under `tools/` (a dev `SceneTree` script, never runtime-loaded).
- COMMIT Godot sidecars (13-1 version-control convention): `*.gd.uid` script-UID sidecars (one per `.gd`, ~409 tracked) AND `*.png.import` asset-import sidecars are COMMITTED, never gitignored (Godot 4.4+ UID-stability guidance) — only the machine-local `.godot/` import cache is gitignored. A re-import / editor-open must be a git-clean no-op; deleting a `*.gd.uid` breaks the mandatory "git status clean" invariant on the next import.
- Player-preference code lives in `scripts/settings/` (`SettingsSnapshot`, `SettingsRepository`, `SettingsApplyService`). NEVER fold preferences into the run snapshot or any tactical/save state.
- Static content source lives in `data/source/`; typed Godot resource mirrors live in `data/resources/`.
- Editable asset source files, prompts, provenance, and reviews live in `asset_sources/`, not `godot/assets/`.
- Runtime-ready art/audio/fonts/shaders live in `godot/assets/`.

### Naming Rules

- Folders and files use `snake_case`.
- GDScript files use `snake_case.gd`: `attack_command.gd`.
- Scene files use `snake_case.tscn`: `gameplay_shell.tscn`.
- Resource files use `snake_case.tres`: `shadow_ambusher_enemy.tres`.
- Test files use `test_*.gd`: `test_attack_command.gd`.
- Classes use `PascalCase`: `AttackCommand`, `EnemyDefinition`.
- Functions and variables use `snake_case`.
- Constants use `UPPER_SNAKE_CASE`.
- Signals use past-tense/event-style `snake_case`: `damage_applied`, `passive_triggered`.
- Commands use imperative names: `MoveCommand`, `AttackCommand`, `ConsumePassiveCommand`.
- Events use past-tense names: `DamageAppliedEvent`, `TileRevealedEvent`.
- Definitions use `*Definition`; snapshots use `*Snapshot`; results use `*Result`.
- Stable content IDs use lower snake case: `shadow_ambusher`, `iron_sword`, `darkness_affinity`.

### Testing Rules

- Every command needs a unit test for valid execution and invalid/no-mutation cases.
- Rules resolver behavior needs tests for trigger timing, ordering, stacking, conflict handling, and explanation output.
- Generator phases need fixtures or seed regression tests.
- Save snapshots need migration tests for every schema change.
- AI behavior families need tests that verify chosen action and explanation reasons.
- Repository layers need tests or checks preventing direct gameplay file access.
- UI command integration tests should verify view models and command bridge behavior without making UI own domain truth.
- Headless simulation tests must run without rendering, audio, UI scenes, or presentation nodes.
- The headless runner (`tests/headless/test_runner.gd`) auto-discovers and sorts `test_*.gd` under `res://tests/unit` and `res://tests/integration` only; it exits with the failure count. Put new tests there (mirroring the domain), not under `tests/headless` or `tests/fixtures`. Run the full suite via PowerShell: `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`.
- Save/snapshot tests MUST exercise a real JSON round-trip (`JSON.stringify` -> `parse_string`), not just native-dict equality; assert that restored RNG streams reproduce the exact next draw. Cover malformed-input rejection paths and assert no partial state is exposed on failure.
- Human playtests remain required for feel, readability, frustration, and excitement.
- RUN THE HEADLESS SUITE FROM A `.ps1` FILE VIA `-File`, NOT INLINE `-Command` (10-6 env gotcha): invoking the PowerShell suite command inline through the Bash tool lets Git Bash expand `$LASTEXITCODE` (and other `$`-vars) BEFORE PowerShell parses it — a silent parse failure. Write a `.ps1` and run `powershell.exe -NoProfile -File <script>.ps1`. Always apply the false-PASS grep guard (`SCRIPT ERROR` / `Parse Error` / `^FAIL`) to the raw output — a green "Headless tests passed." line alone is not proof.
- CURRENT SUITE BASELINE: **205 PASS / 0 `^FAIL` / exit 0** ("Headless tests passed.") on the dev desktop (post-14.11; Epic 14 added ~10 read-only presentation/command-seam test files across 14-1..14-10 [195 -> 205], and 14-11 held 205 by EXTENDING `test_reward_hud_view_model.gd` rather than adding a file; the 195-PASS figure is the historical post-13.2 baseline, 191 the 10.6-gate baseline). Exactly SIX documented stderr negatives are EXPECTED on a green run (int64-overflow ×2 `test_domain_event.gd` + `test_manual_seed_loader.gd`; malformed-JSON ×3 `profile_repository`/`save_repository`+`run_resume_service`/`settings_repository`; `invalid_node_type` ×1 `route_node.gd`) — each a DELIBERATE fail-path assertion (malformed-input rejection / int64-overflow boundary / parse-failure `push_error`), NOT a regression, and they do NOT trip the false-PASS guard (`Parse JSON failed` ≠ `Parse Error`; `ERROR:` ≠ `SCRIPT ERROR:`). Do NOT "fix" the catalog by editing a test. A sane wall-clock guard is < 300 s (5 min) on comparable desktop hardware; a materially-over run is a dev-experience regression to investigate, not a correctness failure. The `ReferenceCombatDriver` PROOF harness (`test_reference_combat_driver.gd`) alone runs ~57 s (its `_relocate_scratch` per-candidate `to_snapshot()`→`try_from_snapshot()` round-trip × the Medium/Scorched catalog) — a KNOWN dev-experience limitation (harness excluded from every export; suite green), NOT blocking; an optional harness-perf pass owns the in-place-relocate fix + folding the Medium/Scorched entries into the byte-determinism loop.

### Platform & Build Rules

- Preserve native mobile packaging path from the start.
- Android exports require Android Studio, Android SDK, and JDK setup.
- iOS exports require macOS and Xcode when iOS packaging begins.
- Commit `project.godot` and `export_presets.cfg` once the Godot project exists.
- Use versioned export presets and local scripted builds early; add CI after vertical slice.
- Use build-profile flags plus pre-export validation and manual release checklist.
- Debug/cheat tools must be disabled or inert in production builds — CONCRETELY (10-6-enforced): the three measurement recorders (`LocalTimingRecorder`/`BossAttemptDiagnostics`/`PerformanceBudgetReport`) are `OS.is_debug_build()`-gated inert in a release export, and all three export presets share the IDENTICAL `exclude_filter` dropping `tools/**`,`tests/**`,`scenes/debug/**`,`**/test_*.gd` (proven by `test_export_setup.gd`). iOS `preset.2` is a scaffold (`runnable=false`, blank signing/icons) = gap G7; the filter still holds on it. See the Readiness section for the full pre-export checklist.
- Test content and experimental assets cannot ship unless explicitly approved for production.
- Platform services stay local/no-op for MVP. `TelemetrySink`/`SaveSyncProvider`/`AchievementProvider`/`CrashReporter` are the design-time INTERFACE POSTURE (the seams a future integration would sit behind), NOT types that exist in code — the actual `PlatformServices` (`scripts/platform/platform_services.gd`) is a local no-op with three bare methods (`record_telemetry`/`unlock_achievement` empty `pass`, `sync_save` returns `ActionResult.ok()`; no `CrashReporter` at all). Do not wire a real telemetry/cloud/achievement sink for MVP.

### Static Content & Asset Rules

- Content may be human-authored or AI-assisted during development, but never dynamically generated by AI at runtime.
- Procedural generation may only select from approved static definitions during play.
- Static content must pass schema validation, semantic validation, applicable simulation/rules smoke tests, and final human approval before production use.
- Human review decides whether an item, passive, enemy, affinity, level recipe, or reward table belongs in Sealsworn.
- Track AI-assisted assets with tool, prompt, date, source references, license/provenance notes, editable source path, runtime export path, and approval status.
- Asset statuses: `exploration`, `placeholder`, `approved_reference`, `production`, `deprecated`. The monotone status ladder + the ☐→☑ Approved checkbox (the 3-point-gate pass) + the `_placeholder` marker make an unapproved/exploration asset STRUCTURALLY distinguishable from production — it can never be silently promoted (10-7 AC3).
- Placeholder assets include `_placeholder` in filenames.
- IMPORT + COMMIT ART SIDECARS BEFORE CONSUMING ART (13-1): approved art on disk is UNRESOLVABLE by `load`/`preload` until Godot imports it (generating a `*.png.import` sidecar) — the exact 13-1 trap (board art existed since Epic 7 but had never been imported). Any art-consuming story MUST run a headless `--import`, COMMIT the generated `*.png.import` sidecars in the SAME change, and load runtime textures via a GUARDED `load()` (never `preload`) so an unimported/missing texture degrades gracefully and the fresh-checkout compile guardrail stays green. The machine-local `.godot/` import cache stays gitignored; the `*.png.import` + `*.gd.uid` sidecars do NOT.
- v0 ASSET STATE (10-7 gate; provenance backbone = `asset_sources/asset-manifest.md` + `provenance-log.md` + `prompt-pack.md`): readability is gated by the 3-POINT gate — **grayscale · phone-size · silhouette**. The ~92-asset visual roster is `approved`/`generated`; **28 placeholder passive-icon glyphs** (`icon.passive.001`–`028`, the 20–30 band) exist as ART, DISTINCT from the passive-DEFINITION roster (still only the 6 class baselines — do not conflate glyph art with rules content). **AUDIO is DE-SCOPED for v0** — 14 SFX + 7 ambient are a 0-file `planned` placeholder track; the candidate ships silent, losing NO required info (nothing is audio-only; every feedback meaning has a visual/textual equivalent). Provenance = the 10 fields (id, status, tool/source, prompt, date, source ref, license, editable-source path `asset_sources/...`, runtime path `godot/assets/...`, approval status) across the three ledgers.

### Critical Don't-Miss Rules

- Do not make Godot scene nodes authoritative for tactical state.
- Do not use one global RNG for gameplay.
- Do not call non-seeded randomness for combat, loot, rewards, map, level, event, unlock, or progression outcomes.
- Do not serialize scene nodes into saves.
- Do not access files directly from gameplay systems; use repositories.
- Do not put gameplay decision logic in autoloads.
- Do not let UI mutate domain state directly.
- Do not use the React prototype as production architecture.
- Do not add cloud services, accounts, multiplayer, or telemetry dependencies to MVP gameplay.
- Do not accept generated/AI-assisted content into production without validation and human approval.
- Do not let debug/manual-seed actions grant progression unless explicitly approved by release policy.
- Do not start implementation with UI-heavy scenes before the domain model, commands/events, RNG streams, and tactical board tests exist.
- Do not persist a full-64-bit save field (RNG `root_seed`/`state`) as a raw JSON number; string-encode it or lose precision beyond 2^53.
- Do not fold player preferences into the run save, and do not let `SettingsRepository` touch the run autosave (or vice versa).
- Do not add any player-selectable difficulty tier or any setting that scales enemy stats/HP/damage/rewards/RNG/run length (hard non-goal).
- Do not let a snapshot compose/restore consume RNG draws or mutate source state; keep it a pure read.
- Do not activate partial restored state on a validation failure; propagate the first error and return nothing.
- Do not change `TacticalBoardViewModel.to_dictionary()`'s key set without intentionally updating its exact sorted-key assertion.
- Do not draw generation randomness from anything but the `level` stream via `GenerationRequest.draw_layout_int/float`, and do not reorder or insert layout draws — the fixed draw order is pinned by seed-regression fingerprints.
- Do not perturb attempt 0 of the bounded retry; it must reproduce the unperturbed `level_seed()` layout (the no-re-pin fingerprint invariant).
- Do not let a generator/validator draw RNG, run commands, or mutate state during validation — `LevelValidator` is a pure read like a snapshot.
- Do not make HAZARD terrain block movement or LOS, and do not model reward/hazard danger in generation (only WALL blocks; danger is the rules kernel's job).
- Do not read `result.seed` on a successful `GenerationResult` (it is `""`); read `payload.level_seed`.
- Do not emit a blocking entity whose board-snapshot cell lacks the matching `occupant_id`, and do not read occupancy from the stripped cell field — use `board.entities()`.
- Do not re-type a metadata-carried list as `Array[Dictionary]` (it survives deep-copy only as a plain `Array`).
- Do not wire a `RewardTableDefinition`, a reward/player entity type, or a JSON-source content pipeline INTO the generation layer (rewards stay abstract placement markers there; the Epic-6 reward tables are a RUNTIME reward-offer concern, never a generation input — and the JSON pipeline is still deferred everywhere); do not let a manual-seed load report meta-progression eligibility or relax the non-negative seed rule.
- Do not silently edit a pinned seed-regression fingerprint to make a test pass; re-pin intentionally via the dump tools in the same change (this includes the `tools/dump_route_fingerprints.gd` route fingerprints).
- Do not add a new top-level `RunSnapshot` key for run-progression state; nest it under `route_state` (the pinned 23-key gate). Do not flatten `run_phase` to top level.
- Do not draw run-affecting RNG from anything but the `map` stream (route generation) or the `level` stream (level generation through the orchestrator); do not bypass the run-level `RngStreamSet` when real run RNG is added in Epic 6/7/9.
- Do not put a run-domain command under `scripts/run/` (commands live in `scripts/core/commands/`), and do not give a run command a `RunActionContext` wrapper — it takes the `RunState` directly. Do not let a run command emit an event with `sequence_id <= 0` (reject it first).
- Do not embed a hyphenated route node id in an error code; carry the precise reason in metadata (one stable top-level code per failure class). Do not pass a hyphenated node id where a lower_snake `GenerationRequest.node_id` is required.
- Do not break reveal-on-arrival: every route advance/entry must reveal the arrived node's forward neighbors, or the run soft-locks. Do not double-clear a node — keep the advance's left-node clear idempotent and the clear/reveal responsibilities split across the existing commands.
- Do not fork a parallel route-position save format; reuse `RunSnapshot.from_route_position` over the 4.1 bridge (board-free, empty `level_state`) and keep the run-seed == `rng_streams.root_seed` cross-check on BOTH compose and resume.
- Do not re-create, rename, or duplicate the 4.5 `run_completed` event; later epics CONSUME it and swap only the placeholder's pre-completion behavior at the same node boundary. Do not renumber the event enum (append new SYSTEM events at the end).
- Do not validate an int64 seed string in any payload with bare `is_valid_int()`; use the lossless canonicalize check (the 3.7 idiom now in `DomainEvent._has_decimal_string_payload`). Do not decimal-string-encode `run_started.node_count` (it is a bounded count, not a seed).
- Do not call `ClassRepository.get_class(id)` — it collides with the reserved native `Object.get_class()` and is a hard parse error; the accessor is `get_class_definition(id)`. Do not name any new content accessor after a reserved `Object` method.
- Do not treat `StartingKit`/class `support_id == &"none"` as a missing item — it is the real baseline `SUPPORT_NONE` (Ranger), resolves, and is a valid support; treating it as missing falsely fails the Ranger run-start.
- Do not skip or reorder the three sequential `RunStartCommand` content gates (class -> kit -> passive); resolve each in `validate()` (pure read), record/seat in `execute()` after the prior gate, leave ZERO partial state on a reject, and carry the offending id in metadata (never in the lower_snake error code). Do not break the empty-class back-compat path (it skips the whole block).
- Do not add, rename, or renumber a `RuleTrigger` window — the fixed ten-window vocabulary is the single source of truth a later epic wires combat hook sites against. Do not author anything in `scripts/rules/{conditions,operations}` (empty Epic-6 scaffolding); do not let the `RulesResolver` draw RNG, run commands, or mutate tactical state (it is a pure read).
- Do not give `PassiveDefinition` an active-skill / cooldown / activation / RNG / effect-amount field (AC3 forbids active skills; v0 passives are explanation-only — the per-effect operation is Epic 6). Do not author the broader 20-30 passive pool yet, and keep the six baseline passive ids EXACTLY matching the class baselines or a class start fails closed.
- Do not serialize `starting_kit` or `rules_resolver` into any save (they are live re-derivable services, null-by-design on restore); a route-position save persists `selected_class_id` ONLY, nested under `route_state` (never a new top-level `RunSnapshot` key). A resumer that needs the kit/resolver MUST re-derive BOTH from the restored class id via `ClassStartSummaryViewModel.re_derive_kit`/`re_derive_resolver` (class passive registered first, then equipment-synergy).
- Do not fail-loud-fork ONE content repository on a duplicate id while leaving the other five last-write-wins — the duplicate-id guard is a single cross-cutting hardening story across ALL SIX repos (parity preserved). Until then, author distinct content ids and do not rely on `*_ids()` and `get_*` agreeing for a duplicate id.
- Do not build a `Control`/`.tscn` hero-select or class-start HUD scene yet — the class-identity surface is a scene-free `RefCounted` view-model projection (`HeroSelectViewModel`/`ClassStartSummaryViewModel`) with an exact pinned key set; the real scenes are a later HUD story (UI-scene-last).
- Do not regress `ContentRepository.register_definition` to `void`/last-write-wins, and do not add a content repository that bypasses it — the fail-loud `duplicate_definition` guard (a rejected duplicate is neither stored nor id-listed) is applied ONCE on the shared boundary so all content repos inherit it (Story 6.1 resolved the carried Epic-5 `[Review][Defer]`); treat every `register_*` result as fallible.
- Do not draw a reward/loot roll outside `RewardOfferBuilder.build_offer` on the `rewards`/`loot` streams (validate-before-draw, fail-closed on a bad/empty table) — never a hand-rolled draw, never `randi()`/`randf()`/a new `RandomNumberGenerator`, never another stream. Hand the builder the run-level `RngStreamSet` (the 4.6 inert-stream fix) so a route-position save round-trips the advanced stream; do not fork a parallel offer/draw path.
- Do not auto-wire reward GENERATE into `run_to_completion`/`_resolve_combat` — `generate_reward_offer`/`generate_passive_reward_offer` are explicit caller-driven orchestrator methods (auto-generating trips the `reward_offer_pending` guard + perturbs interrupted==uninterrupted determinism). Reject a second generate while an offer is pending (`reward_offer_pending`); the reward/loot commands stay pure caller-driven (no orchestrator convenience method).
- Do not stack inventory items in v0 — each occupied backpack slot is exactly one item (a repeat pickup is a new slot, never `quantity++`); `quantity` stays 1, removal drops the whole slot. Do not let a full backpack silently overwrite/drop a slot — `PickupItemCommand` fail-closes `inventory_full` (the "no silent delete" guarantee). Do not author/equip an equip command or a character-level equip-gate yet (equipment STRUCTURE only).
- Do not serialize `InventoryState`/`RewardOffer` into the route-position save — they ride only the FULL `RunState.to_dictionary()`; the `RunSnapshot.inventory`/`equipment` placeholders stay EMPTY and no new top-level key is added (the 23-key gate). The live in-node inventory / pending offer / rolled Destroy outcome / consumed passive are a later in-node-save story.
- Do not give an Epic-6 reward/Destroy/consumable-use command a live HP/wallet/curse mutation — v0 is OUTCOME-RECORD-ONLY (no such domain field exists; the run carries only `starting_kit.baseline_hp`). RECORD the effect on the event; the live mutation is Epic 7/8 wired OFF the recorded event hook. Do not pull Epic-7 live-HP/wallet state into an Epic-6 command.
- Do not let a passive offer be resolved by more than one of {`ResolveRewardCommand`|`ConsumePassiveCommand`|`DestroyPassiveCommand`}, and do not compose one resolution command inside another (double-record). Consume ADOPTS into the `RulesResolver`; Destroy does NOT (it leaves `rules_resolver` untouched and rolls the 70/20/10 outcome through `STREAM_REWARDS` — exactly one draw).
- Do not break the exact-integer 70/20/10 Destroy distribution or the visible-exception-marker pattern — an off-target reward/Destroy table is valid ONLY with the explicit marker (`mvp_choice_count_exception`/`mvp_distribution_exception`) AND a non-empty reason; never a silent reduction/deviation. Do not add/rename/renumber the three Destroy outcome categories (pinned to `DomainEvent.DESTROY_OUTCOME_CATEGORIES`).
- Do not author affix/enhancement/affinity content on `ItemRollModel` — those families are SHAPE-ONLY-DEFERRED (their `*_mvp_deferred` marker is true and their id/tag list MUST stay empty); only the roll RANGE is live. There is no `item_level` field. Light a family up later by flipping the marker + populating the list + adding a resolution path — no schema fork.
- Do not renumber the `DomainEvent.Type` enum — Epic-6 events (`item_gained`/`reward_offered`/`reward_resolved`/`passive_consumed`/`passive_destroyed`/`item_consumed`) were appended at the end as SYSTEM events; wire any new event end-to-end (factory + payload validator + both id maps + round-trip + malformed negatives + the exhaustive `expected_ids` pin). Only `passive_destroyed` carries draw provenance (Destroy draws RNG); the others are deterministic.
- Do not tighten a definition's `validate()` to require new fields without extending EVERY existing baseline in the SAME change (the 6.4 `PassiveDefinition` / 6.7 `ConsumableDefinition` lesson: a tightened validate breaks `create_baseline_repository()` -> null -> downstream suites fail). Add new constructor params LAST (additive extension, never reorder/rename). Do not build the passive-reward modal `.tscn` or icon ART yet (scene-free data contract only; UI-scene-last).
- Do not add a new top-level `RunSnapshot` key for Epic-7 run state — nest risk-economy (gold/corruption/`risk_flags`) under `route_state` (the 7.1 pattern; 23-key gate stays 23, no migration, lenient back-compat read with an `is_manual_seed`-derived default). The pending `EventOffer` rides only the FULL `RunState.to_dictionary()`, never the route-position save. The assigned affinity MIRRORS into the EXISTING `RunSnapshot.affinities` placeholder (no new key) and is re-derivable from the seed on resume; `RunSnapshot.curses` stays `[]` in v0.
- Do not author anything in `scripts/rules/conditions/` — it stays EMPTY by ratified design (v0 is explanation/record-only; verified 0 `.gd`). Do not build the generic `RuleCondition`/`RuleTarget`/`RuleOperation` engine, stacking/conflict/duration, or combat HOOK sites (the later/broader operations story). Curses seat on a SEPARATE `Array[CurseDefinition]` registry on `RulesResolver` (`register_curse`/`resolve_curses`), NOT by broadening the `Array[PassiveDefinition]` typing; `resolve()` stays passive-only, `explain()` is the unified passive+curse surface.
- Do not make a v0 risk effect (curse/corruption increment, resource cost, `risk_flag`) mutate a live combat HP/damage number — it RECORDS + EXPLAINS only (no live max-HP field exists). A `risk_flag` is a recorded marker via `add_risk_flag`/`has_risk_flag`, NOT a difficulty knob; the system that READS a flag/affinity and ALTERS generation (reward odds / elite rates / spawn) is a SEPARATE later generation-modifier story — do not wire an affinity or risk_flag into `RewardOfferBuilder`/reward tables/`EntityRewardPlacer`.
- Do not auto-wire `generate_event_offer` into `run_to_completion`/`_resolve_combat`/`_resolve_non_combat_placeholder` (the 6.3 caller-driven posture — auto-firing perturbs interrupted==uninterrupted determinism); keep the `event` node in `NodeResolvePlaceholderCommand.PLACEHOLDER_NODE_TYPES`. The OFFER draws EXCLUSIVELY through the `events` stream; `ChooseEventOptionCommand` draws ZERO RNG and is the sole `add_risk_flag` producer.
- Do not draw affinity ASSIGNMENT inside `LevelGenerator.generate` — it is ONE `streams.rand_int(STREAM_MAP, ...)` draw in the ORCHESTRATOR (`assign_affinity`), leaving generators/route fingerprints byte-identical. The inert run-level `RngStreamSet` `LevelGenerator.generate`-injection residual is CLOSED for the FR54-FR58 arc; do not re-open it for an affinity effect (effects are POST-generation board ops). Do not bake the Scorched hazard into generated terrain (it would perturb fingerprints) — STAMP it onto a built board; `LevelRecipeDefinition.WRINKLE_AFFINITY_PLACEHOLDER` stays INERT.
- Do not add a `DomainEvent.Type` member for an affinity effect — the Scorched DoT REUSES `DAMAGE_APPLIED` and Darkness's reduced radius rides the existing `visibility_updated` (a smaller `visible_cells`). The only Epic-7 events are `CURSE_APPLIED`/`EVENT_OFFERED`/`EVENT_RESOLVED`, appended at the enum end (never renumbered, wired end-to-end + `expected_ids`). The 7 named RNG streams (`required_streams()`) are INVARIANT — no new stream; any future affinity/Darkness roll draws the EXISTING `combat` stream (never `randi`/`randf`/a fresh `RandomNumberGenerator`).
- Do not extend the 7.5 hazard `AffinityEffectResolver`/`AffinityPreviewQuery` for Darkness — Darkness is a SEPARATE visibility/fog/memory layer (`scripts/tactical/fog/darkness_visibility_layer.gd`), and the hazard resolver's Darkness branch is a deliberate NO-OP pinned by two existing tests. Apply the reduced radius THROUGH `TacticalVisibilityQuery` (no parallel LoS), floor 1 / never 0; keep the memory annotation a READ-layer wrap that never touches the authoritative `visible` branch or stored cell state. `scripts/rules/operations/` holds EXACTLY ONE file (`affinity_effect_resolver.gd`).
- Do not wire FR58 Darkness fairness into the generation success path — `scripts/generation/level/darkness_fairness_query.gd` is a SEPARATE board-scoped query (zero-RNG) extending `LevelValidator`'s safe-first-reveal vocabulary, NOT a `LevelValidator` change (generators/`LevelValidator` untouched → fingerprints byte-identical). Its v0 unseen-damage source is `Terrain.HAZARD` cells only; broadening it is tied to the deferred live-tactical-loop / enemy-empower work.
- Do not register affinity/Darkness accessibility cues anywhere but `TacticalAccessibilityModel._CUE_CATALOG` (the canonical color-independence audit driver), each with a non-color channel. Keep the Flooded electric-interaction `_placeholder` ids (`affinity_conductive_danger_placeholder`/`..._vfx`) DISTINCT-from-final — they are a TRACKED Epic-10 readiness item (replace / de-scope / block); `affinity_scorched_hazard`/`affinity_pathing_pressure`/the two Darkness cues are FINAL ids.
- Do not nest the cross-run profile under `route_state`/`RunSnapshot`, share the run-autosave file/codes, or let `ProfileRepository` touch `user://run_autosave.json` (or vice versa) — the profile is the FIRST persistent CROSS-RUN state with its OWN `ProfileSnapshot` + `ProfileRepository` + `user://profile.json` + `SCHEMA_VERSION` + `unsupported_profile_schema` reject (the run-vs-settings independence precedent). Do not bump `ProfileSnapshot.SCHEMA_VERSION` without adding a migrate step + migration test; reserve an empty/0 home in the current schema for additive run-end state (the 8.4/8.5 merge-without-migration discipline). Do not decimal-string-encode `oath_shards` (a bounded count); DO decimal-string-encode `last_awarded_run_seed` (a full int64 run identity).
- Do not collapse the THREE independent run-end idempotency mechanisms into one marker: the award keys on `profile.last_awarded_run_seed` (`run_already_awarded`), the merge keys on a DEDICATED `unlock_progress["_last_merged_run_seed"]` (`run_already_merged`), and the first-death latch IS `profile.first_death_recorded` (`first_death_already_recorded`). The award + merge markers are DELIBERATELY independent (a shared marker would make whichever command runs first block the second in either order); the first-death latch is per-PROFILE-lifetime, not per-run. Treat all three as independent + safe in any caller order.
- Do not gate the FIRST-DEATH latch on `meta_progression_eligible` — it is eligibility-INDEPENDENT by ratified design (a manual-seed practice death STILL records the latch + shows the line; a boolean narrative latch is provably progression-free — the FR28 narrative-vs-meta boundary precedent). But DO gate the AWARD (8.3) + the MERGE (8.4) on it (`run_not_meta_eligible`, identical code + shape — a manual-seed run grants ZERO Oath Shards/Echoes/Seal-Fragments/mastery/unlock progress/event). Read eligibility from `run.meta_progression_eligible` (already lockstep with `not is_manual_seed`); never re-derive it.
- Do not add a new transition edge for run-END — `CompleteRunCommand` DRIVES the existing `_legal_next_phases` edges (a completion from `ACTIVE_ROUTE` runs the boss two-step `ACTIVE_ROUTE -> NODE_RESOLUTION -> COMPLETED`). Do not create a parallel completed event (BROADEN the 4.5 `run_completed` with the `completed` outcome) or change the boss boundary (Epic 9 reuses the boss `run_completed` unchanged). Re-resolving an already-terminal run rejects `run_already_terminal` (the seam the award/merge/first-death sit behind — AC3 no-double-grant). The valid `outcome`s are a `RUN_FAILED_CAUSES` death cause OR `RUN_COMPLETED_OUTCOME_COMPLETED`; both carry `next_destination = outpost`.
- Do not assume `run_completed` always carries `boss_node_id` — it is BOSS-PLACEHOLDER-ONLY (a generic `completed` completion omits it; the broadened validator requires it only for the `boss_placeholder` outcome). Derive boss progress from a cleared `TYPE_BOSS` route node, not the payload. Epic 9 boss consumers must guard on its presence.
- Do not make `RunSummary` read a presentation log (`CombatExplanationLog`/any UI log) as source truth (AC2) — it AGGREGATES from the terminal `RunState` + an EXPLICIT caller-supplied ordered `DomainEvent` list (v0 has no run-level event store). It is a PURE READ (no state/event/save-key/command/mutation/RNG; adds no `RunSnapshot` key — 23-key gate stays 23). Keep the three state-boundary sub-dicts (`run_scoped`/`profile_meta`/`content_unlock`) SEPARABLE. Do not scan `reward_resolved` into `notable_loot` — it is single-sourced from `item_gained` ONLY (a reward->backpack pickup already emits a paired `item_gained`; scanning both double-counts). Do not rename the summary's backing `root_seed` member to `seed` (it would shadow GDScript's built-in `seed()`).
- Do not make the first-death narrative beat block anything — `FirstDeathNarrativeBeat` is a SEPARATE off-critical-path pure-read DTO; the LATCH mutation (`RecordFirstDeathCommand`) is DECOUPLED from the line DELIVERY so a skip/dismiss is structurally a no-op (there is NO skip command). Do not add a narrative field to `RunSummary` or make the summary/start-descent path depend on the beat. Do not author a `NarrativeRepository`/codex/JSON pipeline — v0 has EXACTLY ONE line (a const on the DTO, referenced by `line_id == first_death`); the first-VICTORY reveal is Epic 9.
- Do not make a meta-award or unlock-threshold calculation scale by difficulty (a hard non-goal), draw RNG, or produce a raw-stat unlock key — `MetaAwardRules`/`UnlockProgressRules` are pure/deterministic/ZERO-RNG/CAPPED with DECLARED const config (award `min(1 + 1*nodes, 5)`; two seal-fragment-count thresholds flipping VARIETY state flags once). Derive `nodes_cleared` off `run.route.cleared_node_ids.size()` (a pure function of the validated run alone — never a caller-supplied summary). Do not build the unlock-SPEND tree / apply an unlock's effect (a later meta-spend story RECORDS the flip; 8.4 does not apply it). `is_raw_stat_unlock_key` (the `damage`/`max_hp`/`armor`/`crit`/`dodge` allowlist) must stay assertable-empty.
- Do not add a top-level `seal_fragments` field to `ProfileSnapshot` — Seal Fragments live under `unlock_progress["seal_fragments"]` (a unique-id set) so 8.4 merges without a migration (a dedicated field is a deliberate schema-v2 decision). Do not persist an in-run discovery home — discoveries are DERIVED from `content_discovered` events (kinds `echo`/`seal_fragment`/`class_mastery`/`unlock_flag`); `echoes`/`seal_fragments`/`unlock_flags` merge with SET semantics (each unique unlock once), `class_mastery` ACCUMULATES.
- Do not build the outpost `.tscn` or perform the `next_destination` navigation yet — `OutpostViewModel` is a scene-free pure-read ASSEMBLY (UI-scene-last). Do not let it call `RunOrchestrator.start(...)` — start-another-descent is a REQUEST SEAM (`start_run_request(...)` -> a `{root_seed, is_manual_seed, class_id, is_startable}` value the caller hands to a FRESH `RunState.new_run` start; the prior run is not reused by construction). Do not re-dedup `notable_loot`, re-project the class roster (delegate to `HeroSelectViewModel`), or re-evaluate unlock thresholds (the merge command owns the flip; the outpost DISPLAYS the recorded progress). Do not make `NAMED_SPACES` authoritative (stable-id data-only `deferred` placeholders; only `descent_stair` has a live affordance). Do not naively cite canonical FR63 for the named-space obligation (that is the Epic-9 boss; the obligation traces via the GDD + the 2026-06-04 readiness patch).
- Do not use `OutpostViewModel.for_recovery(...)` with NO `loaded_profile` for a profile-WRITE failure — that renders a false 0-shard surface over intact-but-unsaved progress. A LOAD failure (`profile_not_found`/`unsupported_profile_schema`) passes no profile (fresh 0-shard, `has_profile == false`); a WRITE failure (`profile_save_*`) passes the intact loaded profile (real totals behind the retry banner, `has_profile == true`). Both populate `recovery_state` with no crash; `RunSummary` never reads the profile file so the summary is never lost on a failed write.
- Do not JSON-round-trip-compare a snapshot carrying a nested int-valued dict via whole-dict `==` — JSON decodes ints as floats and Godot's `Dictionary ==` is value-type-strict (`{"warrior": 3} != {"warrior": 3.0}`); use int-coercion-aware normalization first (the 8.7 lesson; the profile decode helpers are already int-coercion-tolerant). Do not build a profile->class-selectability meta-spend system to satisfy 8.7 AC1 — v0 has none (`ClassDefinition.lock_state` is static); the AC is satisfied by `unlock_progress`/`class_mastery` STATE round-tripping.
- Do not renumber the `DomainEvent.Type` enum — the Epic-8 events (`run_failed`/`oath_shards_awarded`/`content_discovered`/`profile_progress_merged`/`first_death_recorded`) were appended at the END as DETERMINISTIC SYSTEM events (Epic 8 draws ZERO RNG — no draw provenance); wire any new event end-to-end (factory + payload validator + both id maps + round-trip + malformed negatives + the exhaustive `expected_ids` pin). Keep each allowlist const (`RUN_FAILED_CAUSES`/`OATH_SHARDS_AWARDED_REASONS`/`DISCOVERED_CONTENT_KINDS`/...) a local test-pinned static in `domain_event.gd`.
- Do not model the Larval Avatar as an `EnemyDefinition` — the boss is a NEW `BossDefinition` family (+ ordered `BossPhaseDefinition`/`BossActionDefinition` sub-resources, `BossRepository` through the `ContentRepository` fail-loud boundary) mirroring `EventDefinition`/`EventChoiceDefinition`. Keep its `validate()` fail-loud: `boss_id == "larval_avatar"` (== `BossEncounterRequest.BOSS_ENTITY_ID`), `max_hp > 0`, `>= 2` phases, phase-0 threshold `== 100`, STRICTLY DECREASING thresholds thereafter, unique phase ids. The boss is AUTHORED escalation content, NOT a difficulty tier (a hard non-goal); it is a code-constant baseline (no `.tres`/JSON pipeline).
- Do not let the boss mutate the board/entity/turn state directly — `BossTurnResolver` runs `BossAi` -> `BossCommandAdapter` on a SIMULATION COPY, validates against `PendingTelegraphState`, then flows every change through `board.apply_events` (the `EnemyTurnResolver` discipline). `BossAi` is a PURE READ scoring only the ACTIVE phase's `legal_action_ids` with FIXED integer scores (ZERO RNG — reproducibility is pure determinism, not a seeded roll; distinct scores so ties never occur). Boss AI/adapter live in `scripts/ai/`/`scripts/tactical/turns/`; do NOT put them in `scripts/rules/operations/` (still exactly one file) and do NOT open `scripts/rules/conditions/` (still empty). Reuse `tile_marked` for telegraphs (the one-turn `due_turn == created_turn + 1` window; NFR10 forbids a real-time timer) and `damage_applied` for boss damage — NO new event for either.
- Do not store the boss's phase — `current_phase_index` is RECOMPUTED from HP live via `BossPhaseResolver.resolve` (so the 23-key `RunSnapshot` gate stays 23; repeated triggers do not duplicate a phase change). `boss_phase_changed` is a SYSTEM event appended to the log, NOT applied to the board. `BossPhaseResolver.resolve()` clamps a negative index to `maxi(0, ...)` so it can never emit an event-invalid `from_phase < 0`.
- Do not create a parallel `run_victory` event or a new transition edge for the boss victory — the run-victory IS the existing `run_completed` + `outcome == victory` (`RUN_COMPLETED_OUTCOME_VICTORY`, unblocked in the allowlist + validator + `CompleteRunCommand`'s completion classifier alongside `completed`; both run `_resolve_completed`), driven via the existing caller-driven `RunOrchestrator.resolve_run_end` (the boss two-step `ACTIVE_ROUTE -> NODE_RESOLUTION -> COMPLETED` adds no edge). The tactical defeat fact is the NEW `boss_defeated` SYSTEM event from `BossTurnResolver.detect_boss_defeat` (carries `phase_id`, NOT a phase index). `boss_encounter_started` (9.1) is the SETUP event — `BossNodeEnterCommand` no longer auto-completes the boss on arrival (it transitions to `NODE_RESOLUTION` and awaits the real fight; the transition is the LAST infallible step, ZERO RNG, byte-identical no-mutation on any setup failure carrying the inner seed/phase/reason/diagnostics). Derive the request node id by replacing the boss node's hyphens with underscores; never embed the hyphenated id in a code.
- Do not add the first-victory latch as a schema bump — `ProfileSnapshot.first_victory_recorded` is an ADDITIVE bool at `SCHEMA_VERSION == 1` (lenient-parsed to false for a pre-9.4 dict; `DICTIONARY_KEYS` pin updated, NO migration — keeps 8.7's matrix green). `RecordFirstVictoryCommand` (the 8.5 `RecordFirstDeathCommand` twin, the 4.3 idiom, ZERO RNG, CALLER-DRIVEN) gates VICTORY-only (`run.phase == PHASE_COMPLETED`, reject a FAILED run `run_not_completed`) and the latch IS its own once-only per-PROFILE-lifetime marker (reject `first_victory_already_recorded` thereafter). It is the FOURTH INDEPENDENT run-end idempotency marker — do NOT collapse it into the award/merge/first-death markers (9.4 reads/writes none of them; all four are safe in any caller order). Do NOT gate the first-victory latch on `meta_progression_eligible` (Option A — a manual-seed first victory still records the flag + shows the line; a boolean narrative latch is provably progression-free — the FR28 boundary precedent).
- Do not make the first-victory reveal block anything or mutate the flag — `FirstVictoryRevealBeat` (`scripts/run/`, the `FirstDeathNarrativeBeat` twin) is a SEPARATE off-critical-path pure-read DTO; the latch mutation (`RecordFirstVictoryCommand`) is DECOUPLED from the line delivery so a skip/dismiss is STRUCTURALLY a no-op (there is NO skip command). Do NOT add a narrative field to `RunSummary` or make the summary/outpost-return/rewards/progression depend on the beat. Do NOT author a `NarrativeRepository`/codex/JSON pipeline — v0 has EXACTLY ONE first-victory line (a const on the DTO, `line_id == first_victory` = "It did not die. It learned the way back."). The AC3 outpost render of the reveal is DEFERRED (a later UI story; not wired into `OutpostViewModel`).
- Do not derive interleaved boss-stream sequence ids from the board baseline — thread the run-level monotonic counter as the base through `resolve_phase_transitions(..., sequence_id_base)` FIRST, into `detect_boss_defeat(context, sequence_id_base)`, then into the `run_completed` append (never `board.next_sequence_id()`, which reads without advancing and duplicates ids when the boss-action + phase-change + boss_defeat + run_completed streams merge into ONE run log). Any future boss-loop wiring must assert every merged sequence id is unique.
- Do not auto-wire the boss victory into `run_to_completion` — it is CALLER-DRIVEN behind the run-end seam via the additive thin `RunOrchestrator.resolve_boss_victory()` (which reconciles `boss_cleared` by marking the boss node `REVEAL_CLEARED` + idempotently appending it to `cleared_node_ids` — the `NodeResolvePlaceholderCommand._resolve_boss` discipline — BEFORE the infallible `resolve_run_end(victory)`; `CompleteRunCommand` is UNCHANGED). The live boss fight is driven by explicit turns (an integration test + thin continuation, not a shipped auto-play loop); the live combat-DEATH SOURCE (hero-death -> `PHASE_FAILED`) stays DEFERRED (9.5 drives the death PATH with a DRIVEN death via `resolve_run_end(&"hero_death")`, not a live source). `RunSummary.boss_cleared` derives from a cleared `TYPE_BOSS` route node — not a `run_completed` payload field.
- Do not fingerprint the finale like a level layout — the finale regression suite (`tests/integration/finale/test_finale_seed_regression.gd`) is a COMPOSITE determinism pin (an INLINE annotated 5-seed `APPROVED_BOSS_SEED_CATALOG`, NOT a `tools/dump_*` tool: the arena is FIXED + the AI is ZERO-RNG, so there is no layout fingerprint) asserting setup/phases/telegraphs/victory/defeat each run TWICE are byte-identical; the failure report carries seed + phase (never a grid dump). Keep the boss-attempt tuning recorder (`scripts/diagnostics/boss_attempt_diagnostics.gd`) LOCAL/OFFLINE + dev-build-gated (`enabled = new_enabled and OS.is_debug_build()`, INERT in release, the `LocalTimingRecorder` posture): ZERO telemetry/network/file-persist, ZERO RNG, mutates nothing, emits NO event (the enum tail is unchanged), adds NO save key. It OBSERVES (it does not TUNE — difficulty is a hard non-goal).
- Do not renumber the `DomainEvent.Type` enum — the Epic-9 boss events (`boss_encounter_started`/`boss_phase_changed`/`first_victory_recorded`/`boss_defeated`) were appended at the END as DETERMINISTIC SYSTEM events (Epic 9 draws ZERO RNG — no draw provenance; `boss_defeated` was the enum TAIL before Epic 11's `oath_shards_spent`); wire any new event end-to-end (factory + payload validator + both id maps + round-trip + malformed negatives + the exhaustive `expected_ids` pin). The boss VICTORY reuses `run_completed` (broadened with `victory`) — do NOT add a boss-victory event. No new RNG stream (the 7 named streams are invariant).
- Do not write a SceneTree test for an Epic-11 presenter/`.tscn` — the headless harness runs `script.new().run()` with NO `SceneTree` (no `add_child`/`get_tree()` in a test). Put the assertable logic in a `RefCounted` projection with an exact pinned key set (`RunHudViewModel`/`RouteMapViewModel`/`LiveAffinityReadModel`/`OutpostRenderView`) or the pure `RunFlowRouter` resolver and test THAT; scene wiring is verified by construction + the `test_run_flow_scenes_load.gd` compile guardrail. Do not trust a prose "guarded accessor is wired" claim without grepping the probed method name against source — a dead `has_method` probe silently no-ops (the 11.3 M2 lesson). Read the text scale via `TacticalTextScale.from_value(snapshot.text_scale).to_dictionary().get("scale", 1.0)` (the `.scale` accessor does not exist).
- Do not put gameplay/run/combat/run-end decision logic in a `RunFlowController`/`RunFlowRouter`/bridge or a presenter — the Epic-11 flow layer is thin `RefCounted` seams + presenter `Control`s that OBSERVE domain outcomes and DELEGATE to the unchanged `RunOrchestrator`/`LiveCombatResolver`/commands (they own no decision a command/orchestrator does not). No scene node is authoritative for tactical/run truth; Epic 11 added NO autoload. Do not make the run-end return land on two competing outpost surfaces — `outpost.tscn` is the dashboard, `run_end.tscn` is only the gameplay shell's fail-loud non-terminal dead-end landing.
- Do not keep the on-screen live loop ADVANCE-then-resolve — a combat node must be RESOLVED before the route ADVANCES, or a depth-1 pick makes `RouteAdvanceCommand` SEAL the guaranteed depth-0 opening combat node unplayed (the 11.3 H1 divergence). Keep the resolve-then-advance ordering in ONE place; when a presenter re-implements a sequencing the domain already encodes, test the presenter's shared sequencing seam, not just the domain driver.
- The live combat LOADOUT derives from `run.starting_kit` (Story 12.2 CLOSED the 11.2 "class-kit → combat-loadout is a later story" boundary): the interactive (on-screen) live combat hero's HP / weapon / support come from the selected class's `StartingKit` (`baseline_hp` / `weapon_id` / `support_id`) via the pure `CombatLoadout` source (`RunFlowController.hero_hp()`/`hero_weapon_id()`/`hero_support()`), threaded into `begin_interactive_combat_node`/`InteractiveCombatSession`. A NULL-kit (seed-only / legacy) run FALLS OPEN to the driver default (`LiveCombatResolver.DEFAULT_HERO_HP` 60 / sword) so the run still resolves. `RunHudViewModel` STILL displays the class `baseline_hp` between levels and the live board entity's `current_hp` during a fight (two distinct DISPLAY reads — there is STILL NO run-level HP field). Do not invent a persisted run-level HP field or an in-node fight save (the 23-key gate stays 23; the in-node fight stays EPHEMERAL — NOT revised). The OLD pure focus-fire scripted hero still dies at `baseline_hp` 18 on a full live walk (that caveat is scoped to the OLD `LiveCombatResolver` default driver — it is the byte-identical seed-batch/hands-off/auto-resolve proof driver, a mutually-unreachable straggler fail-loud at the round cap, not a shipped hands-off game loop). The retro-T2 STRENGTHENED LoS-aware reference driver (`ReferenceCombatDriver` — a headless PROOF harness) makes the 18-HP class loadout WINNABLE on the approved live-combat seed batch (detonation-dodge + kite/commit); on screen the HUMAN drives the 12-1 tap loop. Threading a class **support** (warrior `shield` / pyromancer `tome`) into the live loadout is an INTENTIONAL, seeded `combat`-stream change on the CLASS path (the shield engages the `shield_block` roll in `AttackCommand`; re-pin any live-combat fixture it moves) — the NEUTRAL default (no-support) / auto-resolve / generator / route / finale paths stay byte-identical.
- Do not break the additive-opt-in affinity signature — `LiveCombatResolver.resolve(...)`'s `affinity_id`/`affinity_repository` default to neutral `none`/null so the pre-11.4 callers stay BYTE-IDENTICAL; the affinity is applied POST-generation on a BUILT board (the generator stays affinity-blind, `WRINKLE_AFFINITY_PLACEHOLDER` INERT — fingerprints byte-identical; the DEFAULT/neutral path is byte-identical). `resolve_combat_node_live` assigns the node's affinity ONCE (the assign-if-absent guard — the 7.4 once-per-node call site; a re-drive never re-rolls the `map` stream), runs `DarknessFairnessQuery.check_board` on the live board (a violation STOPS with no partial progression), and seats the Cursed rule source idempotently. Have `LiveAffinityReadModel` REFLECT the `DarknessFairnessQuery` verdict, never re-derive one. The boss arena carries NO affinity by decision.
- Do not mutate the static `ClassDefinition.lock_state` to apply a meta unlock — an applied unlock is a PROFILE-AWARE OVERLAY: a locked class becomes selectable iff (static `LOCK_STATE_SELECTABLE`) OR (its `<class>_unlocked` flag), derived via the SINGLE `MetaSpendRules.unlocked_class_ids_for(unlock_progress)` source that BOTH the profile-aware `HeroSelectViewModel` AND the authoritative `RunStartCommand` gate read (both take an OPTIONAL trailing `ProfileSnapshot`; a null profile ⇒ byte-identical static behavior). `SpendOathShardsCommand` is the `AwardMetaProgressCommand` idiom at the opposite sign (reject `sequence_id <= 0` first; `state` unused/null; fail-close `unknown_unlock`/`unlock_already_applied`/`insufficient_oath_shards` with the shortfall in metadata; validate-then-mutate; ZERO events/RNG on reject; the CALLER persists). The spend state lives INSIDE `unlock_progress` (NO new top-level key, NO migration). Do not add a redundant manual-seed eligibility gate to the spend command (FR28 is structural — a manual-seed run earns no shards to spend). The `oath_shards_spent` event is append-only at the enum tail (honest `before - amount == after` validator, wired end-to-end + `expected_ids`).
- Do not silently close (nor pre-empt) the Epic-11 deferrals: the Necromancer/Shadeblade CLASS-KIT content (11.6 flips selectability but the baselines carry no runnable kit — the later content story MUST also thread the profile into the STANDALONE `hero_select_presenter.gd` and make `ClassStartSummaryViewModel.re_derive_kit` profile-aware); the LIVE `content_discovered` discovery source (nothing emits it in the live flow — 11.6 SEEDS the state for AC2); the LIVE in-node board/pending-fight SAVE; the run-level event STORE for a full `RunSummary` (the bridge builds `RunSummary.build(run, [])` — passives/loot/discovery + `outcome_or_cause` stay EMPTY, so key victory/death off `phase`, not `outcome_or_cause`); the G4 settings VIEW MODEL (no settings scene / NO difficulty selector — the hard non-goal, so G4 stays PARKED); the Flooded electric-interaction `_placeholder` (still an Epic-10 readiness item — 11.4 surfaces only the deterministic MARKS); and the affinity-driven GENERATION modifier (reward-odds/elite-rate/spawn — a separate later story).
- Do not draw the board tile grid and hit-test taps from two different `TacticalBoardZoomState`s — build ONE via `TacticalBoardGridFit` and share it across the draw + the `screen_to_cell` hit-test, or a tap resolves to the wrong cell. Do not write a SceneTree test for the `tactical_board_grid.gd` draw `Control`; put the assertable render/route logic in the `RefCounted` grid-fit / tap-router seams and verify the draw surface via the `test_run_flow_scenes_load.gd` compile guardrail. Do not let a board render read raw board state — honor the VM's pre-filtered fog/occupant visibility (no fairness leak).
- Do not consume approved art without importing it + committing its `*.png.import` sidecar in the SAME change, and do not `preload` a runtime texture — load it via a guarded `load()` so an unimported/missing texture degrades gracefully (fresh-checkout compile-guardrail safety). Do not gitignore or delete a `*.gd.uid` script-UID sidecar — commit them (Godot 4.4+ UID stability; ~409 tracked); a re-import must stay a git-clean no-op.
- Do not wire the reward/passive GENERATE into the hands-off driver / `run_to_completion` / `_resolve_combat` / `auto_play_boss_fight` — it fires at the INTERACTIVE-shell victory boundary ONLY (auto-generating perturbs interrupted==uninterrupted determinism, trips the `reward_offer_pending` guard, and — since the GENERATE draws the existing `rewards`/`loot` streams — would re-pin seed-regression fingerprints). Route reward clicks through `RewardResolutionBridge` into EXACTLY ONE of `ResolveRewardCommand`/`ConsumePassiveCommand`/`DestroyPassiveCommand` with `orchestrator.next_sequence_id()` (never a second resolution command, never a `TacticalCommandBridge` intent).
- Do not place an execute-capable run-command bridge under `scripts/ui/view_models/` — it belongs in `scripts/ui/flow/` (the `RunEndProfileBridge`/`OutpostSpendBridge` home); keep `view_models/` pure render projections + routing decisions. Do not add a new `RewardHudViewModel`/board-VM/MODAL_KEYS key to render a reward — reuse `PassiveRewardModalViewModel.project_offer`'s pinned keys (the 16-key board VM gate stays 16).
- Do not weaken the fail-closed Epic-6 `inventory_full` reward guard — the domain behavior is CORRECT. The full-backpack soft-lock escape hatch SHIPPED in 14-7 as the caller-driven `DeclineRewardCommand` (it clears the pending offer WITHOUT touching the backpack, so it can never hit `inventory_full`); route the player escape through it, never a guard relaxation. Follow the story's recommended node→table reward mapping unless a deviation is argued in-story (a Round-1 deviation cost a review round).
- Do not model `WaitCommand` as a run command (14-1) — it is a TACTICAL command (takes `TacticalActionContext`, the `MoveCommand` shape), emits the append-only `hero_waited` event (reasons `voluntary`/`no_legal_action`, mirroring `enemy_waited`), BOARD-APPLIES it so `_next_sequence_id` advances past it (a non-applied wait collides sequence ids with the first enemy-phase event), reports `advances_turn: true`, and draws ZERO RNG. It is the F1 turn-advance backstop against a boxed-in soft-lock.
- Do not break the corpse-clear occupancy invariant (14-1) — on death, releasing `entity.blocks_movement` + the cell `occupant_id` is what keeps `_cells`/`_entities` consistent (a non-blocking corpse claims no cell, so it may legally share a cell with a living occupant; `_validate_entity_for_setup` tolerates ONLY the `cell_occupied` rejection for a non-blocking entity, still rejecting out-of-bounds/WALL). The corpse STAYS in `_entities` (hp 0) for the `level_victory_reached.defeated_enemy_ids` payload + the decal read. Any domain-death change must weigh this invariant. The 14-1 combat-replay re-pin (seed 512->24680, plus Medium seed 1337 for ash_seer coverage) is the ONE justified Epic-14 fingerprint move; every generator/route/finale fingerprint is otherwise byte-identical.
- Do not renumber the `DomainEvent.Type` enum — the two Epic-14 events (`hero_waited` 14-1, `reward_declined` 14-7) were appended at the END, so `Type.size()` is now 44 (tail `reward_declined`, index 43). Wire any new event end-to-end (id const + factory + payload validator + both id maps + `_event_requires_actor` membership + round-trip + malformed negatives + BOTH `Type.size()` pins + the exhaustive `expected_ids` pin), and GREP for ALL `Type.size()` count pins (14-7 nearly missed a second `size()==43` pin in `test_interactive_combat_flow.gd`). `hero_waited` HAS an actor; `reward_declined` is a SYSTEM event (no actor). Both are ZERO-RNG (no draw provenance).
- Do not add a new stream/save key for per-run seed variation (14-4) — `RunSeedSource.resolve(configured_seed, entropy)` is a PURE injected-entropy seam feeding the EXISTING `root_seed` (field #5 of the 23-key `RunSnapshot`; gate stays 23, SCHEMA_VERSION 1, no migration). The OS-entropy read stays in the PRESENTER (a local `RandomNumberGenerator.new().randomize().randi()`, never a named gameplay stream / global `randi()`); the seam only consumes the injected value. Manual branch (`configured_seed != 0`) -> verbatim int64 seed + `is_manual_seed` true (no meta — FR28); entropy branch (`== 0`) -> non-negative non-zero 31-bit seed (`& 0x7fffffff`, 0->1) + `is_manual_seed` false. Regression/winnability tests keep EXPLICIT seeds (byte-identical); only the LIVE run seed varies. Live re-descent has ONE seed source at hero-select (14-5 removed the outpost's dead seed wiring; the Descend-Again reroute targets `hero_select`).
- Do not make `DeclineRewardCommand` (14-7) touch the backpack/inventory/economy or record a selection — it flips only the already-valid `RewardOffer.status` (pending->resolved) + `selected_entry` (->{}), adds NO `RewardOffer.DICTIONARY_KEYS`/`RunState`/`RunSnapshot` key (gate stays 23), contributes NO `notable_loot` (it selects nothing), and draws ZERO RNG. A pending offer still resolves via EXACTLY ONE command (resolve/consume/destroy/decline); only the GENERIC overlay routes to decline (a passive offer resolves via Consume/Destroy).
- Do not write a SceneTree presenter test for an Epic-14 surface — put assertable logic in an exact-pinned-key `RefCounted` view-model (the 14-2/14-3/14-6/14-8/14-10 seams) and verify the `Control` presenter by construction + the `test_run_flow_scenes_load.gd` compile guardrail. Source a live VM slot (`preview`/`action_availability`) from the BOUND session, never presenter-local empty state (the recurring 14.1/14.2/14.3 "live VM slot never sourced from the session" defect). New multi-frame scene feedback must be bounded self-terminating tweens / transient overlays with detach-safety (node-bound tween killed on grid free), never `_process`; the mid-fight board resize re-render is event-driven (`viewport.size_changed` coalesced into one deferred pass), never a `_process` poll.
- Do not apply the shared UI theme via `project.godot` `gui/theme/custom` (14-11) — assign `theme` per-scene-root on each of the six `Control` screen roots (project settings are out of scope). Do not hand-author the Theme `.tres` — regenerate `godot/assets/ui/sealsworn_theme.tres` via the committed one-shot `tools/build_sealsworn_theme.gd` (import + commit the Recraft kit's `*.svg.import` sidecars first; `StyleBoxTexture` `texture_margin` is 1:1 with NO auto-downscale, so keep 2048² plate margins small). Run `--headless --import` SEPARATELY to emit new `*.gd.uid`/`*.svg.import` sidecars (the `--scene` test run does not) and COMMIT them.

---

## Usage Guidelines

For AI agents:

- Read this file before implementing any Sealsworn game code.
- Read `_bmad-output/game-architecture.md` before touching architecture-sensitive systems.
- Treat root `project-context.md` as the only canonical project-context file.
- Follow all rules exactly. When in doubt, choose the more restrictive interpretation.
- If a task conflicts with this file, surface the conflict before implementing.
- Update this file only when the project architecture or implementation conventions intentionally change.

For humans:

- Keep this file lean and focused on rules agents might miss.
- Update when the engine, folder structure, patterns, or platform targets change.
- Remove rules that become obvious or obsolete.
- Treat `_bmad-output/game-architecture.md` as the full source of truth and this file as the compact agent handoff.

Last Updated: 2026-07-19 (refreshed after Epic 14 "Playable & Presentable" — the SECOND pre-ship backlog epic [added 2026-07-16, executes after Epic 13], opened after an agent-driven desktop playtest proved the MVP loop was NOT honestly finishable/readable. 11 stories in two bands: BAND 1 (14-1..14-7) makes the loop FINISHABLE + READABLE, BAND 2 (14-8..14-11) makes it PRESENTABLE. TWO DOMAIN TOUCHES, both the 4.3 idiom, additive over pinned contracts: 14-1 shipped corpse-clearing (on death `board.apply_events` releases `entity.blocks_movement` + the cell `occupant_id` so a corpse is walkable/non-targetable while STAYING in `_entities` for the defeated-ids payload/decal) + the `WaitCommand` F1 turn-advance backstop (a TACTICAL command, `hero_waited` event, board-applied, ZERO RNG) — the ONE justified fingerprint move all epic is 14-1's combat-replay re-pin (seed 512->24680 + Medium seed 1337 for ash_seer coverage, because corpse-clear gives ENEMY pursuers mobility too); 14-7 shipped the full-backpack escape hatch `DeclineRewardCommand` (a RUN-domain command, `reward_declined` event, clears the pending offer WITHOUT touching the backpack — closing the 13-2 soft-lock WITHOUT weakening the fail-closed `inventory_full` guard). The `DomainEvent.Type` enum grew by exactly those two append-only tail members -> `Type.size() == 44` (tail `reward_declined`, preceded by `hero_waited`). 14-4 per-run seed variation is a PURE injected-entropy seam (`RunSeedSource`) feeding the EXISTING `root_seed` field #5 — NO save change (23-key gate 23, SCHEMA_VERSION 1); the OS-entropy read stays in the presenter (local `RandomNumberGenerator.randomize().randi()`, never a named stream/`randi()`), and regression/winnability tests keep EXPLICIT seeds so only the LIVE run seed varies; 14-5 converged the live re-descent onto ONE seed source at hero-select (the outpost's dead seed wiring removed). The other nine stories are pure presentation/flow over `scripts/ui/`: 14-2 attack preview + reject cues, 14-3 combat log + the first F8 hit/death animation (bounded self-terminating tweens, never `_process`; `damage_numbers` pruned as dead output), 14-5 run-end beat + honest run-summary (outcome keyed off `phase`, earned-count off `MetaAwardRules` + the meta-eligible gate; EMPTY summary carries `seed=="0"` gated by `has_summary`), 14-6 route-map verify/harden + render-enrichment, 14-8 hero-select rebuild (`HeroSelectRenderView` scene-free seam; profile-threading stays BUNDLED with the deferred class-kit content), 14-9 outpost cleanup + honest loot row, 14-10 player HUD + range highlights, 14-11 the shared UI Theme (the repo's FIRST `.tres`, GENERATED by the committed one-shot `tools/build_sealsworn_theme.gd`, applied per-scene-root on all six `Control` roots — NOT `project.godot`; nine-patch 2048² plates need small margins) + the event-driven mid-fight resize re-render + the reward-overlay `ScrollContainer`/modal-frame rework (resolving the 13-2 geometry + passive-confirm-name defers). Every presentation seam's assertable logic lives in an exact-pinned-key `RefCounted` view-model (verified WITHOUT a SceneTree); the `Control` presenters are verified by construction + the `test_run_flow_scenes_load.gd` compile guardrail. INVARIANTS HELD (the ONE exception is the 14-1 combat re-pin): the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams unchanged (`required_streams()` == 7 — no new stream, no new gameplay RNG), the 16-key `TacticalBoardViewModel` gate stays 16, `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` 1, `scripts/rules/conditions/` EMPTY + `scripts/rules/operations/` exactly one file, NO new autoload/content-family, every generator/route/finale fingerprint byte-identical. Suite baseline: 205 PASS / 0 `^FAIL` / exit 0 (the same 6 documented stderr negatives — int64-overflow ×2 [`test_domain_event.gd:146` + `test_manual_seed_loader.gd:153`] / malformed-JSON ×3 / `invalid_node_type` ×1, ZERO new); Epic 14 added ~10 read-only presentation/command-seam test files. MVP verdict UNCHANGED at READY_WITH_GATES — Epic 14 discharges the honest-playability + presentability qualifiers, but the Band-1/Band-2 on-device HUMAN observed playtest is the STANDING open obligation (every Band-1 story defers its user-facing feel verification to it). Story 14-11 is in `review` [code review passed]; the epic-14 retrospective + the `epic-14: in-progress -> done` transition are orchestrator/Phase-8-owned.)

Prior update: 2026-07-16 (refreshed after Epic 13: live tile-grid board render + tap input [13-1] and the live reward/passive-choice HUD [13-2] — the POST-MVP-CLOSE presentation-maturation epic added after the first human desktop playtest found the on-screen combat surface was text-only and generated no rewards. 13-1 REPLACED the text-only board region with a live TILE-GRID render + tap hit-test wired into the 12-1 interactive seams: the pure `RefCounted` `TacticalBoardGridFit` (grid-fit → the SHARED `TacticalBoardZoomState` the draw + `screen_to_cell` hit-test BOTH consume, so a tap can never resolve to a mis-drawn cell) + `TacticalBoardTapRouter` (cell-state → move/attack/inspect/none) seams + the thin `tactical_board_grid.gd` draw `Control` (`_gui_input` calls `accept_event()` before `cell_tapped.emit`); the approved board art — on disk since Epic 7 but NEVER imported — was imported via a headless `--import`, its 25 `*.png.import` sidecars committed, and textures loaded via a guarded `load()` (never `preload`) so a fresh checkout stays compile-green; 13-1 also SETTLED the durable version-control convention that the ~409 `*.gd.uid` script-UID sidecars are COMMITTED (Godot 4.4+ UID stability), never gitignored. 13-2 WIRED the post-victory reward + passive Consume/Destroy HUD over the pinned Epic-6/7 contracts: `RewardHudViewModel` (`scripts/ui/view_models/`, exact-key `REWARD_KEYS`/`CHOICE_KEYS`, reusing `PassiveRewardModalViewModel`'s `MODAL_KEYS` so it adds NO new key + owning the v0 node→table policy) + `RewardResolutionBridge` (`scripts/ui/flow/`, the caller-driven seam executing EXACTLY ONE of `ResolveReward`/`ConsumePassive`/`DestroyPassive` with `next_sequence_id()`, mirroring `RunEndProfileBridge`). The reward GENERATE fires at the INTERACTIVE-shell victory boundary ONLY (never the hands-off driver / auto-play — so the fingerprints stay byte-identical). The v0 node→table policy (human-ratified): the guaranteed depth-0 opener combat node → the passive 3-choice moment (`passive_reward_choice`), a deeper `combat` node → `standard_combat_reward`, an `elite_combat` node → `elite_combat_reward` (all three baseline tables now live). 13-2 also PICKED UP + CLOSED the carried 13-1 inspect-feedback defer (inspect taps now surface the tapped cell's facts). PLACEMENT CONVENTION settled: a seam that EXECUTES a run command lives in `flow/`, not `view_models/`. KNOWN v0 latent soft-lock (deferred, Low): a full-backpack generic reward returns `inventory_full` + leaves the offer `pending` under an "Accept"-only overlay = no player escape — correct fail-closed DOMAIN behavior, but the missing HUD escape hatch is the later replacement-choice UX owner (unreachable in an early playtest — the backpack starts empty). INVARIANTS HELD (nothing moved): NO domain command, NO new `DomainEvent.Type` (tail `oath_shards_spent`), NO new RNG stream (`required_streams()` == 7 — Epic 13 draws NO gameplay RNG of its own), the 16-key `TacticalBoardViewModel` gate stays 16, the 23-key `RunSnapshot` gate stays 23, `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` 1, every generator/route/finale fingerprint byte-identical, `scripts/rules/conditions/` EMPTY + `scripts/rules/operations/` exactly one file, NO new autoload. Suite baseline: 195 PASS / 0 `^FAIL` / exit 0 (the same 6 documented stderr negatives — int64-overflow ×2 / malformed-JSON ×3 / `invalid_node_type` ×1, no new one); Epic 13 added 4 read-only presentation-seam test files. MVP verdict UNCHANGED at READY_WITH_GATES — Epic 13 discharges the caller-driven-reward / live-HUD-wiring qualifier + UNBLOCKS (does not itself close) the physical-device human-eyes pass for loop steps 6/7. The epic-13 retrospective + the `epic-13: in-progress → done` transition are orchestrator/Phase-8-owned.)

Prior update: 2026-07-12 (refreshed at the EPIC-10 CLOSE — the second tranche 10-4/10-5/10-6/10-7 landed the READINESS/AUDIT/GATE work on top of Epic 12 + the Epic-10 first tranche. These four are NOT feature stories: they VERIFY + DECIDE + ROLL UP, author durable planning-artifact docs under `_bmad-output/planning-artifacts/`, and change NO production `godot/` gameplay/RNG/save/content/generator/view-model/presenter/scene/`export_presets.cfg` path — the ONLY code delta all tranche is 10-5's one additive read-only run-flow cue-coverage test. 10-4 shipped the playtest-comprehension checklist (FR30 run-length; the objective winnability/distinctness proofs; OSG-1..4 observed-session gaps) and CLOSED the 12.2 Medium/affinity winnability gap by ADDING `ReferenceCombatDriver` coverage (the driver handles Scorched hazard cells but does not dodge them — mark-dodge covers only seer telegraphs; winnability holds via the fast commit/kite policy). 10-5 shipped the accessibility-and-readability audit (NFR7/8/9; findings F-1 Flooded `_placeholder`→10.7 / F-2 thin run-summary→event-store owner / F-3 settings paper-audit→settings-scene owner; ASG-1/ASG-2 human-eyes gaps→the physical-device pass) + the cue-coverage test proving every affinity/Darkness cue resolves NON-color. 10-6 shipped the FINAL MVP-readiness GATE (`mvp-readiness-gate.md`, FR70 + the FR30 half): the ten live-loop steps VERIFIED present + winnable, the fresh full suite (191 PASS / 0 `^FAIL`), the pre-export invariants (the `OS.is_debug_build()`-gated recorders + the identical 3-preset `exclude_filter` + the `PlatformServices` local no-op + save-truth snapshots + offline-first, all via `test_export_setup.gd`), the regression-enforced difficulty non-goal (`test_settings_snapshot.gd::_difficulty_non_goal_keys_are_absent`), the four-ledger roll-up, and the gap-disposition ledger (physical-device G1–G7 + iOS-packaging G7 + the settings appendix-§16-G4 paper-audit + the harness-perf/determinism-coverage + the thin summary + the caller-driven reward/passive live-HUD wiring + OSG/ASG — EVERY open gap an acceptable documented v0-CANDIDATE limitation with an owner, NONE a hard blocker). 10-7 shipped the DEDICATED asset/audio/placeholder & UX GATE (`asset-audio-placeholder-ux-readiness-gate.md`, FR46 + FR112/113/114 + NFR17/18): the ~92-asset visual roster (incl. the 28 `icon.passive.001`–`028` placeholder glyphs — art only; the passive-DEFINITION roster is still the 6 baselines), the 3-point readability gate, AUDIO DE-SCOPED for v0 (0-file track, non-gating — nothing audio-only), the 13-meaning event→`sfx.*`→past-tense-`DomainEvent.Type` cue map, the 10-field provenance + no-silent-promotion guarantee, and the placeholder-disposition ledger (none `blocking`). DURABLE CONVENTIONS FOLDED IN: the two DISTINCT "G4"s (device-tiers §6 = FPS profiler vs appendix §16 = settings VM, PARKED); the `.ps1`-not-inline-`-Command` PowerShell runner gotcha; the < 300 s wall-clock guard + the ~57 s `ReferenceCombatDriver` cost; the pre-export release checklist; the interface-posture clarification for `PlatformServices`. **INVARIANTS HELD (nothing moved): 191 PASS / 0 `^FAIL`, the same 6 documented stderr negatives (int64-overflow ×2 / malformed-JSON ×3 / `invalid_node_type` ×1), the 23-key `RunSnapshot` gate, the 7 named RNG streams, the 42-event `DomainEvent.Type` enum (tail `boss_defeated` @ 47), `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` 1, every generator/route/finale fingerprint byte-identical, `scripts/rules/conditions/` EMPTY + `scripts/rules/operations/` exactly one file, NO new autoload.** MVP verdict: `READY_WITH_GATES`. The Epic-10 retrospective + the `epic-10: in-progress → done` transition are orchestrator/Phase-8-owned after 10-7.)

Prior update: 2026-07-08 (refreshed after Epic 12 [Interactive Tactical Combat], plus the Epic-10 first tranche 10-1/10-2/10-3/10-8 which shipped since the Epic-11 refresh. EPIC 12 (inserted between Stories 10.3 and 10.4 by the 2026-07-07 sprint change, closing the Epic-11 retro T1/T2) wired the INTERACTIVE tactical-combat surface: 12-1 shipped the scene-free step-driven `InteractiveCombatSession` (one action per tap through `TacticalCommandBridge` + `TacticalAttackCommitFlow`) + the additive `RunOrchestrator.begin_interactive_combat_node`/`finish_interactive_combat_node` seams + the live board render on the gameplay shell; 12-2 wired the class-kit → live-combat `CombatLoadout` (the live hero is armed from `run.starting_kit` — 18 HP baseline / kit weapon / kit support — instead of the flat `LiveCombatResolver.DEFAULT_HERO_HP` 60 / sword; a null-kit run falls open to the driver default), the LoS-aware `ReferenceCombatDriver` winnability PROOF harness (the approved live-combat seed batch [4242, 8080, 6006, 2048, 512] is winnable by all three playable classes at 18 HP — dodging ash-seer detonations + kiting/committing per weapon — an unwinnable seed fails loud `live_combat_did_not_resolve`), and the HERO-DEFENSE SHIELD seam (`AttackCommand.roll_shield_block` extracted + reused — NOT forked — on INCOMING enemy attacks via `EnemyCommandAdapter` + `EnemyTurnResolver.player_defender_support`; the shield protects its OWNER; the enemy-phase simulation block draw is synced back to the run-level `combat` stream — a byte-identical no-op when no shield is seated; the adapter's player id is kept in sync via `set_player_id` at the late-bind site). The 11.2 "class-kit → combat-loadout is a later story" boundary was formally REVISED by 12-2 (lines 262 + 453) — the interactive live loadout is now class-derived. INVARIANTS HELD across Epic 12: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams unchanged (`required_streams()` == 7; the class-support shield-block/tome draw rides the EXISTING `combat` stream — no new stream, no new draw site), the 42-event `DomainEvent.Type` enum unchanged (NO new event all epic — tail `boss_defeated` @ 47), `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, every generator/route/finale fingerprint byte-identical (the neutral DEFAULT `LiveCombatResolver.resolve(...)` + `run_to_completion` + boss auto-play stay byte-identical — only the CLASS path draws the `combat` stream differently, an intentional seeded re-pin), `scripts/rules/conditions/` stays EMPTY + `scripts/rules/operations/` stays exactly one file (NO passive-combat-effect engine — the kit's passive ids are RECORDED on the loadout, distinctness carried by equipment + support + preview), the in-node fight stays EPHEMERAL (no in-node fight save key), NO new autoload. THE EPIC-10 FIRST TRANCHE (10-1/10-2/10-3/10-8) added the readiness/measurement + consolidated-regression + fairness-batch layer, all read-only over the domain (touched no generator/gameplay/RNG/save source; NO seed-regression fingerprint re-pinned): 10-1 the LOCAL/OFFLINE dev-build-gated `PerformanceBudgetReport` (`scripts/diagnostics/`, the `LocalTimingRecorder`/`BossAttemptDiagnostics` sibling — the four MVP budgets NFR4/NFR5/NFR6) + `tools/dump_performance_budgets.gd`; 10-2 the CONSOLIDATED headless seed-regression suite (`tests/integration/test_seed_regression_suite.gd` — one uniform `fingerprint + seed/system/phase/reason` contract over the six named systems, REUSING each system's single canonical fingerprint source, no second format) + `tools/dump_seed_regression_report.gd` (+ the route sample expanded 8→20); 10-3 the generator-safety fairness BATCH (`tests/integration/test_generator_fairness_batch.gd` COMPOSING `LevelValidator` + `DarknessFairnessQuery`) + `tools/dump_generator_fairness_report.gd` + the durable `generator-fairness-batch-readiness.md`; 10-8 strengthened `DarknessFairnessQuery` predicate (b) from static-from-entrance to MOVING reduced-radius LoS "seen-before-contact" (resolving the FR58 `darkness_unseen_hazard` Medium 4004/5005 finding) + the COORDINATED seed-sample expansion (the shared Small/Medium 50-seed catalog, tactical 25, reward 20, boss 10, affinity →40 curated 10-per-implemented-affinity live-proven; route 20). The curated `AFFINITY_SEED_BY_AFFINITY` list is REGENERATED via the affinity-seed search / `tools/dump_*`, NEVER hand-edited; the three Epic-10 harnesses share the Small/Medium catalog and expand TOGETHER. Suite baseline: 189 test files / 189 PASS / 0 `^FAIL` / exit 0 (~50s) via the headless runner; exactly the 6 documented stderr negatives (int64-overflow ×2 / malformed-JSON ×3 / `invalid_node_type` ×1, no new one); the false-PASS grep guard (`SCRIPT ERROR|Parse Error|^FAIL`) stands.)

Prior update: 2026-07-06 (refreshed after Epic 11: live run flow, scene navigation, in-run HUD, live affinity on screen, the outpost scene + run-end→profile bridge, and meta-spend/unlock application — the LIVE-ON-SCREEN epic that wires the existing Epic-1..9 domain into a hands-off start→hero-select→route→live-fight→boss→run-end→outpost→another-descent loop on real scenes under `godot/scenes/`, PLUS the FIRST meta-SPEND command flipping a class locked→selectable. The FIRST real presentation/scene layer: the flow scenes (`main`→`hero_select`→`route_map`→`gameplay_shell` [board + in-run HUD]→`run_end` [fail-loud non-terminal landing]→`outpost` [meta dashboard]), the presenter `Control`s (`scripts/ui/presenters/`), and the thin `RefCounted` flow seams (`scripts/ui/flow/`: `RunFlowController`, `RunFlowRouter`, `RunEndProfileBridge`, `OutpostSpendBridge`) that OBSERVE domain outcomes + DELEGATE to the unchanged `RunOrchestrator`/`LiveCombatResolver`/commands (own no decision a command does not — NO new autoload). THE HEADLESS HARNESS IS SCENE-FREE (`script.new().run()`, no SceneTree) — `.tscn`/`Control` surfaces are verified by construction + a scene-load COMPILE guardrail; all assertable logic lives in exact-pinned-key `RefCounted` projections (`RunHudViewModel`/`RouteMapViewModel`/`LiveAffinityReadModel`/`OutpostRenderView`) + the pure `RunFlowRouter` resolver (do NOT write a SceneTree presenter test; a dead `has_method` probe silently no-ops — the 11.3 M2 lesson). RESOLVE-THEN-ADVANCE is the shared live sequencing seam (an advance-then-resolve ordering seals the guaranteed depth-0 opening combat node unplayed — the 11.3 H1). HERO HP is TWO concerns: the auto-resolve/hands-off default driver uses `LiveCombatResolver.DEFAULT_HERO_HP` (60), the HUD displays the class `baseline_hp` between levels + the live board entity's `current_hp` during a fight (NO run-level HP field; the scripted live hero is the seed-batch/AC-proof driver, not a shipped hands-off game loop). [SUPERSEDED by Epic 12: the INTERACTIVE live loadout is now class-derived via `CombatLoadout` — see the current Epic-12 rollup above; the DEFAULT auto-resolve driver stays 60/sword.] LIVE AFFINITY (11.4) is an ADDITIVE opt-in on `LiveCombatResolver.resolve(...)` (`affinity_id`/`affinity_repository` default neutral/null — pre-11.4 callers byte-identical) applied POST-generation on a BUILT board; `resolve_combat_node_live` assigns the affinity ONCE (the 7.4 once-per-node call site), runs `DarknessFairnessQuery.check_board` live, seats the Cursed rule source idempotently; `LiveAffinityReadModel` REFLECTS the fairness verdict. THE RUN-END→PROFILE BRIDGE (11.5, `RunEndProfileBridge`) is the caller-driven load→record-latch→persist→build seam the live flow never had (closing the FR1 loop) — it builds `RunSummary.build(run, [])` (EMPTY events: passives/loot/discovery + `outcome_or_cause` blank → key victory/death off `phase`); `OutpostRenderView` is the fail-closed render-decision seam (recovery mode, reveal beats, deferred named-space markers, the G3 `profile.oath_shards` EARNED-count coupling — `RunSummary.profile_meta.oath_shards_earned` stays 0). META-SPEND (11.6, the crux deferred since Epic 8): `SpendOathShardsCommand` (the `AwardMetaProgressCommand` idiom at the opposite sign — fail-close `unknown_unlock`/`unlock_already_applied`/`insufficient_oath_shards`; the CALLER persists) spends `profile.oath_shards`; `MetaSpendRules` (necromancer 3 / shadeblade 5) is the SINGLE `unlocked_class_ids_for` seam BOTH the profile-aware `HeroSelectViewModel` + the authoritative `RunStartCommand` gate read (optional trailing `ProfileSnapshot`; null ⇒ byte-identical static behavior) — the STATIC `ClassDefinition.lock_state` is NEVER mutated (an applied unlock is a profile-aware OVERLAY; the `<class>_unlocked` flags + ledger live INSIDE `unlock_progress` — NO new key, NO migration); `OutpostSpendBridge` is the load→spend→persist→rebuild seam; the `oath_shards_spent` SYSTEM event (honest `before - amount == after`) is the ONLY new event all epic. STILL DEFERRED (re-recorded, not silently closed): the Necromancer/Shadeblade CLASS-KIT content (the later content story must also thread the profile into the standalone `hero_select_presenter.gd` + make `re_derive_kit` profile-aware); the LIVE `content_discovered` source (nothing emits it — 11.6 SEEDS AC2); the LIVE in-node board SAVE; the run-level event STORE; G4 the settings view model (no settings scene / NO difficulty selector — the hard non-goal, PARKED); the Flooded electric `_placeholder` (still Epic-10 readiness); the affinity-driven GENERATION modifier. INVARIANTS HELD: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams unchanged (`required_streams()` == 7), `ProfileSnapshot`/`SettingsSnapshot` `SCHEMA_VERSION` stays 1, every generator/route/finale fingerprint byte-identical (the DEFAULT `run_to_completion` byte-identical), `scripts/rules/conditions/` EMPTY + `scripts/rules/operations/` exactly one file, NO new autoload. Full headless suite green at the Epic-11 close: "Headless tests passed." (Godot 4.6.3, exit 0, 183 test files / 182 PASS / 0 `^FAIL` — 6 documented stderr negatives: int64-overflow ×2 / malformed-JSON ×3 / `invalid_node_type` ×1, no new one; `git diff --check` clean). [Suite is now 189 PASS after Epic 12 — see the current rollup above.])

Prior update: 2026-07-04 (refreshed after Epic 9: Larval Avatar MVP Finale — the boss node's REAL pre-completion behavior (setup + fight), the boss content + phases + telegraphed AI, the boss VICTORY -> run-victory + first-victory reveal, and the finale regression + tuning-diagnostics closeout (FR31/FR62/FR63). It swaps ONLY the boss node's pre-completion behavior at the SAME `boss` node + the SAME `run_completed` boundary Epic 4 shipped (NOT the route model, node types, or the `run_completed` event). The Larval Avatar is a NEW `BossDefinition` family (+ ordered `BossPhaseDefinition`/`BossActionDefinition` sub-resources, `BossRepository` — the FIFTEENTH content repository through the `ContentRepository` boundary; a code-constant baseline, no `.tres`/JSON), NOT an `EnemyDefinition`; `validate()` fail-loud on `boss_id == larval_avatar`/`max_hp > 0`/`>= 2` phases/phase-0 threshold 100/strictly-decreasing thresholds/unique ids. `BossNodeEnterCommand` (9.1) is the SETUP half only — build + validate a deterministic `BossEncounterRequest` + a FIXED strict-validated `BossArenaBuilder` arena, transition `ACTIVE_ROUTE -> NODE_RESOLUTION` (the LAST infallible step, ZERO RNG, atomic), emit `boss_encounter_started`; the boss NO LONGER auto-completes on arrival. `BossTurnResolver` (9.3, the `EnemyTurnResolver` analogue) runs `BossAi` -> `BossCommandAdapter` on a SIMULATION COPY then `board.apply_events` (the boss never mutates state directly); `BossAi` is a PURE ZERO-RNG utility scorer over the active phase's `legal_action_ids` (reproducibility is pure determinism); telegraphs reuse `tile_marked` (the one-turn window), damage reuses `damage_applied`. The LIVE phase seam recomputes `current_phase_index` from HP (no stored phase state — 23-key gate stays 23) and emits `boss_phase_changed` per transition. The run-VICTORY IS `run_completed` + `outcome == victory` (`RUN_COMPLETED_OUTCOME_VICTORY` unblocked, joining `completed` in `CompleteRunCommand`'s completion classifier — NO parallel event, NO new edge, driven via the caller-driven `resolve_run_end`); the tactical fact is the NEW `boss_defeated` from `BossTurnResolver.detect_boss_defeat` (carries `phase_id`). The first-victory latch is an ADDITIVE `ProfileSnapshot.first_victory_recorded` bool at `SCHEMA_VERSION == 1` (NO migration — 8.7's matrix stays green) + `RecordFirstVictoryCommand` (the 8.5 `RecordFirstDeathCommand` twin, VICTORY-only `PHASE_COMPLETED` gate, once-only per-PROFILE-lifetime latch — the FOURTH INDEPENDENT run-end idempotency marker, Option A eligibility-INDEPENDENT) + a NEW `first_victory_recorded` event; `FirstVictoryRevealBeat` (the `FirstDeathNarrativeBeat` twin, ONE line "It did not die. It learned the way back." — `line_id == first_victory`, no narrative pipeline) is a SEPARATE off-critical-path pure-read DTO (line delivery DECOUPLED from the flag mutation — a skip is structurally a no-op; the AC3 outpost render is DEFERRED). The interleaved boss streams thread a SHARED monotonic sequence-id counter (`resolve_phase_transitions` -> `detect_boss_defeat` -> the run-END append; never the board baseline). 9.5 closed the finale: the composite finale seed-regression suite (an INLINE 5-seed catalog pinning setup/phases/telegraphs/victory/defeat run TWICE — no layout fingerprint; the arena is fixed + the AI is ZERO-RNG), the full-run-through-the-shell integration (`RunOrchestrator.resolve_boss_victory` reconciles `boss_cleared` by clearing the boss node before the infallible `resolve_run_end(victory)`; `CompleteRunCommand` unchanged), and the LOCAL/OFFLINE dev-build-gated `BossAttemptDiagnostics` recorder (ZERO telemetry/RNG/save-key, emits NO event). Epic-9 events (`boss_encounter_started`/`boss_phase_changed`/`first_victory_recorded`/`boss_defeated`, tail index 47) are append-only DETERMINISTIC SYSTEM events. INVARIANTS HELD: the 23-key `RunSnapshot` gate stays 23, the 7 named RNG streams are unchanged (Epic 9 draws ZERO RNG — no new stream), `ProfileSnapshot.SCHEMA_VERSION` stays 1, every generator/route fingerprint is byte-identical (generators untouched), `scripts/rules/conditions/` stays EMPTY + `scripts/rules/operations/` stays exactly one file (the boss AI/adapter live in `ai/`/`tactical/turns/`, NOT the kernel). v0 stays CALLER-DRIVEN at the run-end seam (the boss victory is caller-/test-driven, not auto-wired into `run_to_completion`; the live combat-death SOURCE stays deferred). NO new autoload. Full headless suite green: "Headless tests passed." (Godot 4.6.3, exit 0, 166 test files PASS / 0 FAIL / 0 script-or-parse errors).)

Prior update: 2026-07-03 (refreshed after Epic 8: outpost, meta progression & run summary — the run-END + FIRST persistent CROSS-RUN META layer. `CompleteRunCommand` (8.1) resolves run END: it DRIVES `PHASE_FAILED` (reachable since Epic 4, never before triggered) via a death CAUSE (`RUN_FAILED_CAUSES`) + the new `run_failed` event, BROADENS the 4.5 `run_completed` with a generic `completed` outcome, and carries `next_destination = outpost` (FR32) — reusing the existing transition edges (a completion from `ACTIVE_ROUTE` runs the boss two-step), adding NONE, and rejecting a re-resolution `run_already_terminal` (the seam the award/merge/first-death sit behind). BREAKING: `run_completed.boss_node_id` is now BOSS-PLACEHOLDER-ONLY. `RunSummary` (8.2/8.4) is a PURE-READ AGGREGATOR over the terminal `RunState` + a caller-supplied `DomainEvent` list (no persisted event store; no `RunSnapshot` key — 23-key gate stays 23) with three SEPARABLE state-boundary sub-dicts; `notable_loot` is single-sourced from `item_gained` ONLY (no `reward_resolved` — avoids double-count). THE PROFILE is the FIRST persistent cross-run state — its OWN `ProfileSnapshot` + `ProfileRepository` (`user://profile.json`, STRICTLY independent of the run autosave) + `SCHEMA_VERSION = 1` + `unsupported_profile_schema` reject, mirroring `RunSnapshot` verbatim (8.3). THREE run-end profile-mutating commands, all the 4.3 idiom + CALLER-DRIVEN + ZERO RNG: `AwardMetaProgressCommand` (8.3 — the capped/sparse Oath-Shard currency via the pure `MetaAwardRules`, `min(1+1*nodes, 5)`), `MergeRunDiscoveriesCommand` (8.4 — Echoes/Seal-Fragments/class-mastery/unlock flags derived from `content_discovered` events + deterministic unlock-threshold crossings via `UnlockProgressRules`), `RecordFirstDeathCommand` (8.5 — the first-death latch). THREE INDEPENDENT run-end idempotency mechanisms (award `last_awarded_run_seed`; merge `unlock_progress["_last_merged_run_seed"]`; first-death latch) safe in ANY caller order. ELIGIBILITY IS SPLIT: the award + merge DENY a manual-seed run (`run_not_meta_eligible`), but the first-death latch is eligibility-INDEPENDENT (the ratified FR28 narrative-vs-meta boundary precedent). Seal Fragments live under `unlock_progress["seal_fragments"]` (NO top-level field — merges without a migration; the story TITLE notwithstanding). `FirstDeathNarrativeBeat` (8.5) is a SEPARATE off-critical-path pure-read DTO (line delivery DECOUPLED from the latch mutation — a skip is structurally a no-op; ONE line const, no narrative pipeline). `OutpostViewModel` (8.6) is a pure-read ASSEMBLY of the five prior read surfaces + the start-another-descent REQUEST seam + the four data-only `deferred` NAMED_SPACES + the two-mode `for_recovery` (load-fail = fresh 0-shard; write-fail = intact profile behind a retry banner). 8.7 added the profile/summary save-load + migration matrix (the JSON int-coercion `Dictionary ==` footgun surfaced). Epic-8 events (`run_failed`/`oath_shards_awarded`/`content_discovered`/`profile_progress_merged`/`first_death_recorded`) are append-only DETERMINISTIC SYSTEM events. v0 stays EXPLANATION/RECORD-ONLY + caller-driven (no live combat-death source; nothing auto-wired into `run_to_completion`); NO new autoload, NO content family/repository (discoveries tracked by-id off events); the 23-key gate + 7 named RNG streams + every generator/route fingerprint are INVARIANT (Epic 8 draws ZERO RNG). Full headless suite green: "Headless tests passed." (Godot 4.6.3, exit 0, 153 test files PASS / 0 FAIL / 0 script-or-parse errors).)

Prior update: 2026-06-30 (refreshed after Epic 7: risk economy, curses, events & the COMPLETE affinity domain — the live `RiskEconomyState` (gold wallet + corruption + `risk_flags`) persisted NESTED under `route_state` (23-key gate stays 23, no migration); curse resolution via a SEPARATE `Array[CurseDefinition]` kernel registry (`register_curse`/`resolve_curses`; `explain()` unified, `resolve()` passive-only); the caller-driven risk/reward `event` offer/choose pair (`generate_event_offer` on the `events` stream — the FIRST `events` consumer — + `ChooseEventOptionCommand`, the sole `add_risk_flag` PRODUCER, NOT auto-wired); the full affinity domain — `AffinityDefinition`/`AffinityRepository` (4 MVP + neutral `none`) + deterministic CALLER-DRIVEN `RunOrchestrator.assign_affinity` on the `map` stream (mirrored into the `RunSnapshot.affinities` placeholder, re-derivable on resume), with LIVE tactical EFFECTS: Scorched/Flooded/Cursed as board-cell HAZARD/mark/kernel effects via the SINGLE new `scripts/rules/operations/affinity_effect_resolver.gd` (the first + only `operations/` occupant; Scorched DoT REUSES `DAMAGE_APPLIED`), and Darkness as a SEPARATE visibility/fog/memory layer `scripts/tactical/fog/darkness_visibility_layer.gd` (reduced LoS radius through `TacticalVisibilityQuery` + a read-layer memory annotation — NOT a hazard effect); the FR58 Darkness fairness guardrail as a board-scoped `scripts/generation/level/darkness_fairness_query.gd` (extends `LevelValidator`'s safe-first-reveal vocabulary, NOT wired into the generation success path); the `CURSE_APPLIED`/`EVENT_OFFERED`/`EVENT_RESOLVED` append-only SYSTEM events (NO affinity event — effects reuse existing events). v0 is EXPLANATION/RECORD-ONLY: `scripts/rules/conditions/` STAYS EMPTY by ratified design (no generic operation engine); the 7 named RNG streams + 23-key gate remain INVARIANT; generators untouched all epic → seed-regression fingerprints byte-identical. No new autoload; `data/source` + `data/resources` gained per-family SUBDIRECTORIES but ZERO content files — all content is still code-constant baselines through the `ContentRepository` boundary. Full headless suite green: "Headless tests passed." (Godot 4.6.3, exit 0, 140 PASS / 0 FAIL). The Flooded electric-interaction `_placeholder` ids are a tracked Epic-10 readiness item.)

Prior update: 2026-06-29 (refreshed after Epic 6: loot, rewards, inventory & Consume/Destroy — the seven-family loot CONTENT roster under `scripts/content/` (`Armor`/`Jewelry`/`Consumable`/`Pickup`/`GoldReward`/`RewardTable` definitions + repositories, the no-repo `DestroyOutcomeTableDefinition`, `ItemRollModel`, and the pure `RewardOfferBuilder`) with the EIGHT MVP loot categories; the central duplicate-id FAIL-LOUD guard (`ContentRepository.register_definition` now returns `ActionResult` + rejects `duplicate_definition` — RESOLVED the carried Epic-5 `[Review][Defer]` across all repos); the small `InventoryState` backpack/equipment model recorded on `RunState` (no stacking, equipment STRUCTURE only) + the `RewardOffer` pending-offer value object; the deterministic reward-offer flow (caller-driven `RunOrchestrator.generate_reward_offer`/`generate_passive_reward_offer`, the builder drawing the run-level `rewards`/`loot` streams — the 4.6 inert-run-level-`RngStreamSet` fix); the five run-domain commands `PickupItem`/`ResolveReward`/`ConsumePassive`/`DestroyPassive`/`UseConsumable` (the 4.3 idiom verbatim, a passive offer resolved by exactly one of resolve/consume/destroy); the exact-integer 70/20/10 Destroy distribution + the visible-tuning-exception-marker pattern; the six new append-only SYSTEM events; and the scene-free passive-reward modal data contract (`PassiveDefinition` additively extended with FR47/FR77 fields + the modal/commit-flow view-models, real `.tscn`/icon deferred). v0 is OUTCOME-RECORD-ONLY + EXPLANATION-ONLY: rewards/Destroy/consumable-use RECORD their effect but mutate NO HP/wallet/curse field (none exists — the live mutation is Epic 7/8, wired off the recorded event); `scripts/rules/{conditions,operations}` stay EMPTY (the per-effect operation engine + combat hooks are the later Epic-6 operations story). No new autoload; `data/source` + `data/resources` still empty — all loot/reward content is code-constant baselines through the `ContentRepository` boundary. Full headless suite green: "Headless tests passed." (Godot 4.6.3, exit 0, 0 FAIL).)
