# Unused / Spare Icons

Generated this session (Recraft) but **not** selected for the MVP set. Kept and documented per NFR18 (extra assets clearly separated from production). Filenames keep Recraft's prompt-derived codes (`bold-XX (N)`); the `XX` decode is in [`../../prompt-pack.md`](../../prompt-pack.md) §B.

## `alternates/` — extra versions + reusable distinct icons
Mostly duplicate takes of glyphs where we shipped one version. A few are distinct enough to reuse for future content:

| File | What it is | Possible future use |
|---|---|---|
| `…bold-pa (1)/(2)/(5)` | alternate **crossed-swords** | a different weapon/combat skill icon |
| `…bold-co (4)` | spiral "**closing portal**" | teleport / void / portal effect icon |
| `…bold-ho (1)/(2)/.svg` | alternate **horned skulls** | enemy / curse / undead marker |
| `…bold-ho (3)` | alternate **hourglass** | alt time/turn passive |
| `…bold-ro (3)` | alternate **aegis ward** | alt defense passive |
| `…bold-ro (5)` | alternate **spinning coin** | luck/economy icon |
| `…bold-cu (1)/(2)/(3)` | alternate **fangs** | beast/predator passive |
| `…bold-pa (4)` | alternate **boots** | movement passive |
| `…bold-cl (3)` | alternate **portcullis gate** | door/containment icon |
| frame/banner `(1)/(2)/(3)` | alternate **UI frames + banners** | swap-in options for the chosen kit |

Plus assorted single-take duplicates (`(1)` vs `.svg`) of the shipped glyphs — swap into `passives/` if a chosen one needs replacing.

## `superseded/` — earlier, lower-quality attempts
`single-bold-flat-symbol*` (the pre-treatment batch) and `digital-vector*` (early skulls), replaced by the final `emblem-icon` set. Reference only — not for production.
