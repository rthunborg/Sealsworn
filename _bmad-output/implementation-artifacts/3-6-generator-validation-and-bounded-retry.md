---
created: 2026-06-16
source_story_key: 3-6-generator-validation-and-bounded-retry
---

# Story 3.6: Generator Validation and Bounded Retry

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want generated levels to reject soft-locks and unfair starts,
so that seeded generation creates fair uncertainty rather than unavoidable punishment.

## Acceptance Criteria

1. Given generation produces a candidate level, when validation runs, then it checks entrance-to-exit reachability, no soft-locks, legal enemy placement, reachable intended rewards, fog/readability constraints, and safe first reveal, and validation emits a pass/fail report.
2. Given validation fails and retries are allowed, when bounded retry runs, then generation attempts a limited number of deterministic retries, and final failure returns a structured `GenerationResult` error with seed, phase, reason, and diagnostics.
3. Given validation tests run headlessly, when known bad fixture layouts are tested, then each bad layout fails for the expected reason, and known good fixture layouts pass.
4. Given generation phase fixtures are tested, when route context, recipe selection, layout/pathing, blocker placement, enemy placement, reward placement, and final validation run, then each phase has at least one focused regression assertion, and phase failures are reported separately from final-map fingerprint failures.

[Source: epics.md Epic 3 / Story 3.6]

## Tasks / Subtasks

- [ ] 3.6.1 Confirm the Epic 3 boundary and write FAILING tests FIRST (TDD red phase). (AC: 1-4)
  - [ ] Verify in `_bmad-output/implementation-artifacts/sprint-status.yaml` that `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-...: done`, `3-3-...: done`, `3-4-...: done`, `3-5-enemy-and-reward-placement: done`, `epic-3: in-progress`, and this story (`3-6-generator-validation-and-bounded-retry`) is `ready-for-dev`. If any earlier Epic 1/2/3.1-3.5 status regressed, STOP and restore the boundary before implementing. This story is NOT the first in epic 3, so leave `epic-3: in-progress` (orchestrator owns epic status).
  - [ ] Confirm the working tree is clean or that dirty files are intentional user work; preserve unrelated changes. The untracked orchestrator-owned `_bmad-output/auto-gds/` directory is expected and is NOT your change.
  - [ ] Add focused tests FIRST. New file `godot/tests/unit/generation/test_level_validation.gd` covering AC1 (a `LevelValidator` that runs every named check — entrance->exit reachability, no soft-lock, no-required-class/item-gate, legal enemy placement, entity-aware reachable intended rewards, fog/readability, safe first reveal — over a built candidate and emits a structured pass/fail report; a clean generated candidate PASSES; a hand-built candidate failing EACH check fails with that check's stable code) and AC3 (known-bad fixtures fail for the expected reason; known-good fixtures pass). New file `godot/tests/unit/generation/test_bounded_retry.gd` covering AC2 (a deterministic retry orchestration: same root_seed+recipe reproduces the SAME final result; an injected always-fail validator exhausts the attempt cap and returns a structured `GenerationResult.error` carrying seed/phase/reason + an `attempts` diagnostic; an injected fail-then-pass validator succeeds on a later attempt and reports the attempt index; attempt N's candidate differs deterministically from attempt 0's). New file `godot/tests/unit/generation/test_generation_phase_fixtures.gd` covering AC4 (one focused regression assertion per phase — recipe, layout/pathing, blockers, enemies, rewards, final validation — each asserting that phase's failure surfaces a DISTINCT `failed_phase`/`error_code`, reported separately from the seed-regression terrain `fingerprint` failures). Update `test_level_generator.gd` for any new success-diagnostics keys (e.g. `attempts`/`validated: true`) and the `validate`-phase success path.
  - [ ] Reuse existing primitives EXACTLY — reuse `LevelGenerator.generate(request, recipe_repository, enemy_repository)` (the dispatch + phase methods), the generators' `generate_layout` / `validate_readability` / `build_board_snapshot`, `EntityRewardPlacer.validate_reward_reachability` + its `_flood_reachable`, `BoardState.try_from_snapshot` (the strict board + entity validator that already enforces legal occupancy and occupant cross-consistency), `BoardState.entities()`/`entity_at()`/`get_cell()` for entity-aware reachability, `GenerationRequest.draw_layout_int` + `level_seed()`, `GenerationResult.ok`/`error` + the existing PHASE constants (incl. the UNUSED `PHASE_REWARDS`/`PHASE_PATHING`/`PHASE_FINALIZE`), and `ActionResult`. No new RNG stream, no new result type, no new board representation, no new snapshot parser.
  - [ ] Did NOT (scope guard): add a `RewardTableDefinition`/repository or concrete loot contents (Epic 6); add a new `TacticalEntityState.EntityType`; place the player/hero entity (Epic 4 owns it — so "safe first reveal" is validated against the entrance cell + its LoS neighbourhood, NOT a placed hero); realize `door`/`affinity_placeholder`/`risky_side_branch` wrinkle kinds; add enemy AI/turn/hazard-damage/combat/rules-kernel behavior; add real affinity rules (Epic 7); build the 3.7 manual-seed/batch CLI; touch commands/live tactical truth/save/settings/UI/meta; add fields to `LevelRecipeDefinition`/`EnemyDefinition`/`GenerationRequest`/`GenerationResult` or change the baseline recipes/enemies; add dependencies or a new test framework.

- [ ] 3.6.2 Implement the comprehensive `LevelValidator` as separate named checks over a built candidate. (AC: 1, 3)
  - [ ] New file `godot/scripts/generation/level/level_validator.gd` — `class_name LevelValidator extends RefCounted`. A PURE query layer (draws NO RNG, mutates nothing) that takes a built candidate (the layout dict + the validated `BoardState` + the `rewards` marker list) and runs each check below, returning a structured pass/fail REPORT (`ActionResult.ok([], {...check counts...})` on pass; `ActionResult.error(<check_code>, <compact diagnostics>)` on the FIRST failing check). Run the checks in a FIXED, documented order; the first failure short-circuits. Each diagnostic stays COMPACT (counts/coordinates/ratios — NEVER a full grid dump), mirroring `validate_readability`.
  - [ ] Implement these checks as separate private methods, each with a stable lower-snake code:
    - (a) `entrance_to_exit_reachability` -> reuse a 4-neighbour BFS over non-WALL cells from the entrance; fail `unreachable_exit` if the exit is not in the reachable set (carry entrance/exit + reachable count). This is the SAME check Medium's `validate_readability` already does — reuse/centralize it, do not fork the BFS.
    - (b) `no_soft_lock` -> there must be a path from the entrance to the exit AND back is implicit (movement is symmetric over non-WALL cells), so v0 "no soft-lock" = the exit is reachable AND no MANDATORY reward (optional == false) is sealed off by terrain OR by blocking entities (this is the entity-aware reachability the 3.5 deferred Low calls for). Fail `soft_lock_detected` with the offending cell. (Entity-aware: flood over non-WALL cells AND non-blocking-entity-occupied cells; see 3.6.3.)
    - (c) `no_required_class_or_item_gate` -> v0 generation places NO class/item gates (no door/lock/keyed wrinkle is realized — `door`/`affinity_placeholder`/`risky_side_branch` are NOT realized through 3.5). So this check asserts the candidate contains no gate marker requiring a class/weapon/item to progress; it PASSES by construction for v0 candidates and FAILS `required_gate_present` only if a future gated wrinkle/marker appears on the mandatory path. Document that v0 has no gates so the check is a forward guardrail, not dead code (drive its FAIL branch with a hand-built candidate carrying a synthetic gate marker, mirroring the Medium hand-built-candidate pattern). [Source: epics.md Story 3.6 AC1 "absence of required class or item gates"; FR36]
    - (d) `legal_enemy_placement` -> every entity on the board is on a legal occupiable cell (not WALL, not entrance, not exit), no two blocking entities share a cell, and every enemy cell is reachable from the entrance. `BoardState.try_from_snapshot` ALREADY enforces occupancy legality + dup-id + occupant cross-consistency at build time, so this check RE-ASSERTS placement legality at the validation layer (entrance/exit exclusion + entrance-reachability), failing `illegal_enemy_placement` with the offending entity id + cell. [Source: epics.md Story 3.6 AC1; Story 3.5 AC1]
    - (e) `reachable_intended_rewards` -> every MANDATORY reward (optional == false) must be entity-aware reachable from the entrance; an OPTIONAL reward (behind danger) must still be terrain-reachable (it may sit adjacent to a hazard but must not be sealed). Fail `unreachable_reward` with the offending reward coordinate. This SUBSUMES + STRENGTHENS `EntityRewardPlacer.validate_reward_reachability` (which is terrain-only — see the 3.5 deferred Low) by also accounting for blocking entities on the mandatory path. [Source: epics.md Story 3.6 AC1; deferred-work.md 3-5 Round 2 terrain-only reward-reachability Low]
    - (f) `fog_readability` + (g) `safe_first_reveal` -> reuse Medium's excessive-blockage interior-WALL-ratio bound (a) and the first-reveal bounded flood (c) from `validate_readability`. `safe_first_reveal` (FR36 "safe first reveal") additionally asserts the entrance's immediate LoS neighbourhood (within the baseline LoS radius 4, FR5) contains no unavoidable-damage source the player cannot see coming — for v0 that means: the entrance cell itself is NOT a HAZARD cell, and no enemy is adjacent to (Chebyshev <= 1 of) the entrance (so the player is not spawned already-in-melee/already-on-a-hazard). Fail `unsafe_first_reveal` with the entrance + offending cell/entity. [Source: epics.md Story 3.6 AC1 "safe first reveal"; FR5; FR58 Darkness-fairness intent]
  - [ ] CENTRALIZE the reachability BFS: extract ONE shared 4-neighbour flood helper (terrain-only AND an entity-aware variant) used by both the validator and — if you choose to delegate — by the generators' existing checks. Keep `MediumLevelLayoutGenerator.validate_readability` and `EntityRewardPlacer.validate_reward_reachability` GREEN: either (preferred) leave them in place and have `LevelValidator` call/wrap them so the new comprehensive validator is the superset, OR delegate their bodies to the shared helper. Do NOT delete them and do NOT change their existing error codes/diagnostics keys (the 3.3/3.5 tests pin them). The validator may run the readability subset by calling `validate_readability` directly. Document the relationship (3.6's `LevelValidator` is the comprehensive superset; the focused checks remain callable).
  - [ ] The validator is SIZE-AGNOSTIC: it runs the SAME checks for Small and Medium candidates (Small currently has no validator of its own; 3.6 gives both sizes the full pass). Small's first-reveal/excessive-blockage bounds must be satisfied by the existing 8x8 construction (verify the approved Small seeds PASS the new checks; if a baseline bound is too strict for 8x8, set a size-appropriate bound as a NAMED constant with a documented rationale — do NOT silently weaken a check).

- [ ] 3.6.3 Implement entity-aware reachability (closes the 3.5 terrain-only reward-reachability Low). (AC: 1)
  - [ ] Add an entity-aware flood over the built `BoardState`: a cell is traversable iff it is non-WALL AND not occupied by a `blocks_movement` entity (the entrance cell is the flood origin and is always traversable even if it carries terrain). Use `BoardState.entity_at(cell)` / `BoardState.entities()` (returns copies, sorted by id) or read `occupant_id` from `get_cell()` — but NOTE `try_from_snapshot` STRIPS cell `occupant_id` and RE-DERIVES it from blocking entities, so read occupancy via the entity list / `entity_at`, not a raw cell field. The flood is deterministic (fixed `NEIGHBOUR_OFFSETS` order) and pure.
  - [ ] Use the entity-aware flood for the no-soft-lock (b), legal-enemy-placement reachability (d), and mandatory-reward reachability (e) checks. The exit and mandatory rewards must be reachable AROUND blocking enemies, not just over open terrain. Because the baseline boards leave ample open cells (the corridor is reserved blocker-free and enemies are sparse), generated candidates pass by construction; the FAIL branches are driven by hand-built candidates that ring/pinch a target with blocking entities (mirror the Medium hand-built-candidate pattern + the 3.5 `test_enemy_reward_placement.gd` independent-BFS style). [Source: deferred-work.md 3-5 Round 2 "EntityRewardPlacer.validate_reward_reachability is TERRAIN-only ... a reward ... could be sealed off by blocking enemies"]

- [ ] 3.6.4 Implement bounded deterministic retry in `LevelGenerator`. (AC: 2)
  - [ ] Wire the `LevelValidator` into the success path of BOTH `_run_small_layout_phase` and `_run_medium_layout_phase`: after `build_board_snapshot` succeeds, build the candidate (layout + `BoardState` + rewards) and run `LevelValidator.validate(candidate, recipe)`. On PASS, return the existing `_ok_with_payload` result (now also tagged `validated: true` + `attempts` count). On FAIL, the candidate is rejected and a retry is attempted (see below). A FINAL failure (all attempts exhausted) returns `GenerationResult.error` against the failing check's phase (reachability/soft-lock/enemy/reward -> the architecture phase; readability/first-reveal -> `PHASE_VALIDATION`) carrying seed + phase + reason + compact diagnostics + the `attempts` count.
  - [ ] BOUNDED DETERMINISTIC RETRY — the central design decision (read "Bounded-Retry Determinism Contract" in Dev Notes). A generated candidate today is a PURE function of `(root_seed, recipe)`, so re-running with the same seed yields the SAME candidate — retrying without changing the input would loop forever on a genuinely-bad seed. To make retries produce DIFFERENT-but-DETERMINISTIC candidates: for attempt index `i` (0-based), derive the attempt's layout seed by mixing `i` into `request.level_seed()` with a documented stable mix (e.g. a fixed multiplier/xor like the `RngStreamSet._derive_seed` discipline), build `RngStreamSet.new(attempt_seed)`, and run the layout+placement phase with it. Attempt 0 MUST use the UNPERTURBED `request.level_seed()` so the existing seed-regression terrain fingerprints + all 3.2-3.5 generated-layout tests stay byte-identical (no re-pin). Cap attempts at a NAMED constant `MAX_GENERATION_ATTEMPTS` (recommend a small bound, e.g. 8 — document the choice; NFR4 load budget is < 3s so a small bound is safe). Same `(root_seed, recipe)` -> identical final result (same attempt count, same final candidate or same final error). [Source: epics.md Story 3.6 AC2 "limited number of deterministic retries"; project-context.md Determinism rules; rng_stream_set.gd `_derive_seed`]
  - [ ] The retry is testable WITHOUT a genuinely-bad baseline seed (baseline candidates pass on attempt 0). Provide a SEAM so the test can inject a validator outcome: e.g. `LevelGenerator.generate` takes an OPTIONAL injected validator (defaulting to the real `LevelValidator`), OR factor the retry loop into a method that accepts a validator callable/object. The test injects an always-fail validator (assert attempt cap reached + structured error + `attempts == MAX`) and a fail-then-pass validator (assert success on attempt k + `attempts == k+1`). Keep the public 3-arg `generate(request, recipe_repository, enemy_repository)` signature working for all existing callers (add the validator as an optional 4th param with a real default, mirroring how `recipe_repository`/`enemy_repository` were threaded — do NOT break the BREAKING-but-settled 3-arg signature from 3.5). [Source: deferred-work.md 3-5 "unreachable_reward -> PHASE_VALIDATION ... NOT exercised end-to-end ... add a test seam"]
  - [ ] FINAL-FAILURE diagnostics: the returned `GenerationResult.error` carries `seed` (the request seed string via `_seed_text`), `failed_phase` (the failing check's phase), `error_code` (the failing check's stable code), `reason` (lower-snake), and `diagnostics` including `attempts` (how many were tried) + the last check's compact diagnostics + `recipe_id`/`size_class`. NEVER a full grid dump. [Source: epics.md Story 3.6 AC2; architecture "Generator validation failures must report seed, phase, reason, and compact debug output"]

- [ ] 3.6.5 Add per-phase fixture assertions reported separately from the fingerprint failures. (AC: 4)
  - [ ] In `test_generation_phase_fixtures.gd`, add at least one focused regression assertion per generation phase, each asserting a DISTINCT `failed_phase`/`error_code` so a phase failure is diagnosable on its own:
    - recipe -> an unknown recipe id fails `PHASE_RECIPE`/`unknown_level_recipe`; an invalid request fails `PHASE_RECIPE`/`invalid_generation_request` (reuse the existing `test_level_generator.gd` cases or assert them here).
    - layout/pathing -> a candidate whose entrance cannot reach the exit fails the reachability check (PHASE chosen for reachability — document whether reachability reports `PHASE_VALIDATION` or `PHASE_PATHING`; recommend `PHASE_VALIDATION` for consistency with the existing Medium readability mapping, OR use the now-available `PHASE_PATHING` constant for the reachability/soft-lock checks specifically — pick ONE and document it).
    - blockers -> excessive interior-WALL-ratio fails `PHASE_VALIDATION`/`excessive_blockage` (drive via a hand-built over-walled candidate through the validator).
    - enemies -> a missing/empty enemy repository fails `PHASE_ENEMIES` (reuse the 3.5 case); an illegal enemy placement (hand-built) fails the `illegal_enemy_placement` check.
    - rewards -> an unreachable mandatory reward (hand-built) fails `unreachable_reward`. ALSO close the 3.5 deferred Low: drive an unreachable reward through `LevelGenerator.generate` end-to-end (via the injected-validator seam or a custom recipe/candidate) and assert the `PHASE_VALIDATION`/`unreachable_reward` result — not just the placer unit-tested in isolation.
    - final validation -> a clean candidate PASSES the full `LevelValidator` (`validated: true`).
  - [ ] Assert phase failures are SEPARATE from fingerprint failures: the seed-regression tests (`test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd`) pin the TERRAIN `fingerprint`; the phase-fixture tests assert `failed_phase`/`error_code`. A phase regression must surface as a phase-fixture failure, NOT as a fingerprint drift, and vice-versa. Document this separation in the new test's header.

- [ ] 3.6.6 Keep the terrain fingerprints stable; only validation + the attempt-0 success path may change. (AC: 2, 4)
  - [ ] Attempt 0 uses the UNPERTURBED `request.level_seed()`, so every approved-seed terrain `fingerprint` (Small + Medium) MUST stay byte-identical. Do NOT re-pin `APPROVED_FINGERPRINTS` in either seed-regression test. If a fingerprint drifts, you accidentally perturbed attempt-0 generation (e.g. the validator drew RNG, or the retry seed-mix touched attempt 0) — that is a BUG, fix it; do NOT re-pin to make it pass.
  - [ ] The `LevelValidator` draws NO RNG (it is a pure query), so wiring it into the success path must not advance the `level` stream. Verify the `_layout_draws_only_from_level_stream` assertions stay green and that the validator does not perturb the stream. If a Small/Medium generated candidate now goes through validation, the terrain + placement + stream-draw behaviour for attempt 0 must be byte-identical to 3.5.

- [ ] 3.6.7 Run required validation and update story records. (AC: 1-4)
  - [ ] Run through PowerShell (NOT the Bash PATH — `godot` resolves only via `C:\Users\Rasmus\bin\godot.cmd`): `godot --version` (expect `4.6.3.stable`); `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10` (expect exit code `0` / "Headless tests passed."); `git diff --check` (no whitespace errors).
  - [ ] Verify: full headless runner exits `0`; ALL prior Epic 1/2/3.1-3.5 tests stay green (incl. `test_project_structure.gd` — the new `level_validator.gd` lives in the already-required `res://scripts/generation/level/` root and the new tests in `res://tests/unit/generation`, so the structure gate stays green with NO change); the Small AND Medium seed-regression terrain fingerprints stay GREEN with NO re-pin; the Medium `validate_readability` + `EntityRewardPlacer` tests stay green (their codes/diagnostics unchanged); the updated `test_level_generator.gd` passes with the `validated`/`attempts` diagnostics; the three new test files pass. The two `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure diagnostics (NOT regressions).
  - [ ] Update Dev Agent Record, Completion Notes, File List, Change Log, Status (review). Record: the comprehensive-validator design + each check's code; the bounded-retry attempt-seed mix + `MAX_GENERATION_ATTEMPTS`; the attempt-0-unperturbed invariant (fingerprints did NOT drift); the entity-aware reachability decision (closing the 3.5 terrain-only Low); the validator/focused-check relationship (superset, not replacement); the reachability-phase choice (`PHASE_VALIDATION` vs `PHASE_PATHING`); the no-gate v0 forward-guardrail; the injected-validator test seam. Synchronize `sprint-status.yaml` to `review`. Leave `epic-3` as `in-progress` (orchestrator owns epic status).
  - [ ] No `user://` artifact created (tests build candidates/boards in-memory only). Remove any throwaway probe tool/script; `git status --short` is clean of temp files. Record any deferred Low in `_bmad-output/implementation-artifacts/deferred-work.md` with the originating story/review date.

## Dev Notes

### Pre-Implementation Gate

This is the **sixth Epic 3 implementation story** and the validation/safety capstone of the generation pipeline. Story 3.1 delivered the generation CONTRACT layer; 3.2 the deterministic **Small (~8x8) layout phase**; 3.3 the deterministic **Medium (~14x12) layout phase + AC2 readability validation** (`validate_readability`); 3.4 the **first tactical-wrinkle placement + the first `HAZARD` terrain**; 3.5 **deterministic enemy + reward placement** (board entities + payload markers + the focused terrain-only `validate_reward_reachability`). Story 3.6 builds the **comprehensive validator + bounded deterministic retry** that 3.2-3.5 deliberately deferred — it is the FIRST story to (a) give BOTH size classes a full validation pass, (b) make reward/exit reachability ENTITY-AWARE (around blocking enemies), and (c) add a retry orchestration loop.

Story-creation analysis on 2026-06-16 found:

- `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-...: done`, `3-3-...: done`, `3-4-...: done`, `3-5-enemy-and-reward-placement: done` (all code-review Approve, full headless suite green), `epic-3: in-progress`. Story 3.6 was `backlog` and is now `ready-for-dev`. Story 3.7 remains `backlog`. Story 3.6 is NOT the first story in epic 3, so the epic status stays `in-progress` (do not change it).
- The generation pipeline (`level_generator.gd`, `small_level_layout_generator.gd`, `medium_level_layout_generator.gd`, `entity_reward_placer.gd`, `tactical_wrinkle_placer.gd`, `generation_request.gd`, `generation_result.gd`) all exist and are the DIRECT template. The board model (`BoardState`, `BoardCell`, `TacticalEntityState`) + the strict snapshot path (`TacticalSnapshot.parse` / `BoardState.try_from_snapshot`) are the candidate-validation target. `GenerationResult` ALREADY defines the full phase vocabulary including the currently-UNUSED `PHASE_PATHING`, `PHASE_REWARDS`, and `PHASE_FINALIZE`. EXTEND these; do NOT fork or re-author them.
- Two focused validators already exist and must be PRESERVED (their codes/diagnostics are pinned by tests): `MediumLevelLayoutGenerator.validate_readability` (excessive_blockage `0.35` / unreachable_exit / unreadable_first_reveal) and `EntityRewardPlacer.validate_reward_reachability` (terrain-only). Story 3.6's `LevelValidator` is the comprehensive SUPERSET that subsumes and strengthens them (entity-aware reachability); it does not delete them.

Before implementing, re-confirm the local tree is clean or that dirty files are intentional user work. If any Story 1.x/2.x/3.1-3.5 status regressed, stop and restore that boundary first.

### Scope Boundary

This story delivers a **comprehensive deterministic level validator** (`LevelValidator`) that checks a built candidate against ALL the FR36/Story-3.6 fairness constraints (entrance->exit reachability, no soft-lock, no required class/item gate, legal enemy placement, entity-aware reachable intended rewards, fog/readability, safe first reveal) and emits a structured pass/fail report; plus a **bounded deterministic retry** loop in `LevelGenerator` that re-attempts a limited number of deterministically-perturbed candidates when validation fails and returns a structured `GenerationResult` error (seed + phase + reason + diagnostics + attempt count) on final failure; plus **per-phase fixture assertions** reported separately from the seed-regression terrain fingerprints.

In scope:

- **`LevelValidator`** — a pure-query `RefCounted` running every named check as a separate method with a stable lower-snake code, returning a compact pass/fail report, size-agnostic (both Small + Medium), short-circuiting on the first failure.
- **Entity-aware reachability** — a flood over the built `BoardState` treating non-WALL AND non-blocking-entity-occupied cells as traversable; used by the no-soft-lock, enemy-placement-reachability, and mandatory-reward-reachability checks. Closes the 3.5 terrain-only reward-reachability deferred Low.
- **Bounded deterministic retry** — `LevelGenerator` re-attempts up to `MAX_GENERATION_ATTEMPTS` candidates, attempt 0 using the UNPERTURBED `request.level_seed()` (fingerprints unchanged) and attempt `i` mixing `i` into the seed deterministically; final failure -> structured `GenerationResult.error` with `attempts`. An injected-validator test seam exercises the cap + fail-then-pass without a genuinely-bad baseline seed.
- **Per-phase fixture assertions (AC4)** — one focused regression per phase (recipe/layout-pathing/blockers/enemies/rewards/final-validation), each asserting a DISTINCT `failed_phase`/`error_code`, reported separately from the terrain fingerprint tests.
- **Closing 3.5 deferred Lows that overlap this story** — entity-aware reward reachability (Low #2) and the end-to-end `unreachable_reward -> PHASE_VALIDATION` integration coverage (Low #3). The behind-danger under-placement Low (#1) is NOT in scope unless a recipe that can hit it exists (it cannot with the baseline recipes) — note it stays deferred.
- **Tests** — comprehensive-validator pass + per-check fail; entity-aware reachability fail (enemy-ringed target); bounded-retry determinism + cap + fail-then-pass; per-phase distinct failures separate from fingerprints; Small + Medium terrain fingerprints stay GREEN with NO re-pin.

Out of scope (owned elsewhere — do NOT build here):

- **A standalone `RewardTableDefinition` / `RewardTableRepository` and concrete loot CONTENT.** DEFERRED to Epic 6 (decided in Story 3.5). This story validates abstract reward markers only. [Source: epics.md Epic 6; Story 3.5.5]
- **The player / hero entity.** The generated board carries ENEMIES; the player/hero is placed by the run/level-entry flow (Epic 4), NOT generation. So "safe first reveal" is validated against the ENTRANCE cell + its LoS neighbourhood (no hazard on/at-entrance, no enemy adjacent to entrance), NOT against a placed hero. [Source: epics.md Epic 4; Story 3.5 scope]
- **Realizing `door` / `affinity_placeholder` / `risky_side_branch` wrinkle kinds.** v0 generation realizes only `choke_point`/`blocker_cluster`/`flank_route` (WALL) + `hazard` (HAZARD). The no-required-gate check is therefore a forward GUARDRAIL (no gates exist to fail today), driven only by a hand-built synthetic-gate candidate. Do NOT realize gated wrinkles this story. [Source: Story 3.4 scope; level_recipe_definition.gd wrinkle kinds]
- **Real affinity rules** (Scorched/Flooded/Cursed/Darkness) and the `PHASE_AFFINITY` content. Epic 7. The `affinity_placeholder` request field stays a placeholder; the validator does not apply affinity rules. [Source: epics.md Epic 7; generation_request.gd AFFINITY_NONE]
- **Enemy AI, enemy turns, hazard damage, combat, or any rules-kernel behavior.** The validator positions/inspects domain data only; behaviors are Epic 1 systems NOT invoked by generation. "Safe first reveal" uses STATIC proximity/terrain checks, NOT simulated enemy turns or hazard detonation. [Source: project-context.md Rules/AI rules]
- **The manual-seed loader + batch fingerprint CLI + the bland/unfair-seed-preservation playtest loop.** Story 3.7. This story owns the validator + retry engine that 3.7's batch loader will USE. [Source: epics.md Story 3.7]
- **HUD / scene presenter for validation diagnostics.** The pass/fail report is structured domain data; surfacing it in UI is a later story. [Source: project-context.md presentation rules]
- **Changing `BoardCell`/`BoardState`/`TacticalEntityState`/`GenerationRequest`/`GenerationResult`/`TacticalSnapshot` contracts, the baseline recipes, or the baseline enemies.** The entity model + the strict validator already enforce occupancy legality; the phase constants already exist (incl. `PHASE_PATHING`/`PHASE_REWARDS`/`PHASE_FINALIZE`); the recipes/enemies already exist. Do NOT add fields to these or change the baselines. [Source: Story 3.5 scope]
- **Any player-selectable difficulty tier.** Validation bounds are READABILITY/FAIRNESS constants, NEVER difficulty knobs. HARD non-goal. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- **Re-pinning the terrain fingerprints, removing/repurposing `wall_density`, jittering the board size, or randomizing entrance/exit.** The 3.2 retro names 3.6 as the owner of "may then relax the corridor reservation and randomize entrance/exit" — but that is OPTIONAL and OUT OF SCOPE for this story unless validation + retry genuinely require it. Keeping entrance/exit fixed + the corridor reserved keeps the construction guarantee (and the fingerprints) intact while the formal validator is added on top. Do NOT randomize entrance/exit or relax the corridor this story — it would force a fingerprint re-pin and widen blast radius with no AC benefit. [Source: retro-notes epic-3 Story 3-2]
- Cloud saves, accounts, multiplayer, leaderboards, telemetry, Godot .NET/C#, React/Vite production dependencies, or new test frameworks / new engine libraries.

### Current Repository Baseline (READ THIS FIRST — add a new `LevelValidator` + a retry loop in `LevelGenerator`; do NOT fork the generators, the placer, or the focused validators)

The most important constraint: **the comprehensive validator is a PURE-QUERY layer over a BUILT candidate (it draws NO RNG), and the bounded retry is an orchestration loop in `LevelGenerator` whose ATTEMPT 0 uses the UNPERTURBED `request.level_seed()`. The Small + Medium terrain seed-regression fingerprints MUST stay byte-identical with NO re-pin. The focused `validate_readability` (Medium) and `validate_reward_reachability` (placer) checks STAY in place (their codes/diagnostics are test-pinned); `LevelValidator` is the comprehensive superset that subsumes + strengthens them with entity-aware reachability.**

- `godot/scripts/generation/level/level_generator.gd` — `LevelGenerator.generate(request, recipe_repository, enemy_repository) -> GenerationResult` (3.5 BREAKING signature, now settled). Dispatches on size class to `_run_small_layout_phase` / `_run_medium_layout_phase`; both build a fresh `RngStreamSet.new(request.level_seed())`, call `generate_layout(...)`, (Medium) `validate_readability(...)`, `build_board_snapshot(...)`, then `_ok_with_payload(...)`. `_map_layout_error` routes `missing_enemy_repository`/`no_placeable_enemy` -> `PHASE_ENEMIES`, `unreachable_reward` -> `PHASE_VALIDATION`, else `PHASE_LAYOUT`. WIRE the new `LevelValidator` into the success path of BOTH phase methods (after `build_board_snapshot`) and WRAP the phase body in a bounded-retry loop (attempt 0 unperturbed, attempt `i` seed-mixed). ADD the optional injected-validator 4th param (real default). ADD `validated`/`attempts` to the success diagnostics. Keep the request-validation + recipe-resolution + `unknown_level_recipe` (`PHASE_RECIPE`) paths intact. The `_ok_with_payload` payload shape (`board`/`entrance`/`exit`/`blockers`/`rewards`/`size_class`/`recipe_id`/`level_seed`) is settled — do not change keys, only add diagnostics.
- `godot/scripts/generation/level/medium_level_layout_generator.gd` — `MediumLevelLayoutGenerator`. HAS `validate_readability(layout)` (3 AC2 checks, compact diagnostics, the canonical `_flood_reachable` + `NEIGHBOUR_OFFSETS`, `_first_reveal_cell_count`, `_interior_wall_count`, the `MAX_INTERIOR_WALL_RATIO=0.35` / `FIRST_REVEAL_RADIUS=4` / `MIN_FIRST_REVEAL_CELLS=8` constants). REUSE this BFS + these bounds for the validator's reachability/readability/first-reveal checks — either call `validate_readability` directly from `LevelValidator` or extract a shared helper. Do NOT change its codes (`excessive_blockage`/`unreachable_exit`/`unreadable_first_reveal`/`invalid_layout_shape`) or diagnostics keys (pinned by `test_medium_level_layout_generator.gd`). `build_board_snapshot` sets `occupant_id` on each blocking enemy's cell (occupant invariant).
- `godot/scripts/generation/level/small_level_layout_generator.gd` — `SmallLevelLayoutGenerator`. 8x8, fixed entrance `(1,4)` / exit `(6,4)`, reserved corridor, border-WALL ring. Has NO validator of its own (it relied on the construction guarantee + the placer's reward-reachability). 3.6 gives Small the FULL validation pass via the shared `LevelValidator`. VERIFY the approved Small seeds PASS the new checks at 8x8; if a Medium-tuned bound (e.g. `MIN_FIRST_REVEAL_CELLS=8`) is too strict for the smaller 8x8 first-reveal neighbourhood, use a size-appropriate NAMED constant with a documented rationale — do NOT silently weaken a check, and do NOT re-pin the Small fingerprints. `build_board_snapshot` lacks the per-row/leading shape guard the Medium one has (3.2-deferred Low, not reopened) — fine for generated input.
- `godot/scripts/generation/level/entity_reward_placer.gd` — `EntityRewardPlacer`. HAS `validate_reward_reachability(layout, rewards)` (TERRAIN-only 4-neighbour flood from the entrance; `unreachable_reward` + compact diagnostics) — the 3.5 deferred Low notes this IGNORES blocking enemies. 3.6's `LevelValidator` STRENGTHENS reward reachability to be entity-aware for MANDATORY rewards. Do NOT change `validate_reward_reachability`'s existing terrain-only contract/code (pinned by `test_enemy_reward_placement.gd`); ADD the entity-aware check in the validator (it may call this terrain-only check first, then additionally verify entity-aware reachability for mandatory rewards). `_reward_behind_danger` (cell or 4-neighbour is HAZARD) defines `optional`. `PLACEMENT_ENEMY_ORDER` + `ENEMY_FACTION=&"labyrinth"` are the placement constants.
- `godot/scripts/generation/level/generation_result.gd` — `GenerationResult`. `ok(payload, diagnostics)` / `error(failed_phase, error_code, reason, seed: String, diagnostics)`; `seed` is a String; `error()` normalizes an unknown phase to `&"invalid_phase"` and a non-lower-snake code/reason to `invalid_error_code`/`invalid_reason` (so EVERY phase you report MUST be in `generation_phases()` and every code/reason MUST be lower-snake). Available phases: `PHASE_ROUTE`/`PHASE_RECIPE`/`PHASE_LAYOUT`/`PHASE_PATHING`/`PHASE_BLOCKERS`/`PHASE_HAZARDS`/`PHASE_ENEMIES`/`PHASE_REWARDS`/`PHASE_AFFINITY`/`PHASE_VALIDATION`/`PHASE_FINALIZE`. `PHASE_PATHING`/`PHASE_REWARDS`/`PHASE_FINALIZE` are currently UNUSED — you MAY use `PHASE_PATHING` for the reachability/soft-lock checks and `PHASE_REWARDS` for reward-reachability if you want phase-granular reporting, OR keep mapping reachability/readability to `PHASE_VALIDATION` (consistent with the existing Medium mapping). PICK ONE mapping and document it; the AC4 test asserts DISTINCT phases per failure, so use distinct phases where the architecture provides them. Do NOT add a new phase constant.
- `godot/scripts/generation/level/generation_request.gd` — `GenerationRequest`. `level_seed()` returns `root_seed` (NOT `request.seed`); `draw_layout_int/float` route EXCLUSIVELY through `RngStreamSet.STREAM_LEVEL`. For bounded retry, derive the per-attempt seed from `level_seed()` + the attempt index (a pure local mix — do NOT mutate the request; build a fresh `RngStreamSet.new(attempt_seed)` per attempt). The validator draws NO RNG. Do NOT add a field to `GenerationRequest`.
- `godot/scripts/core/state/rng_stream_set.gd` — `RngStreamSet`. `new(root_seed)` configures the named streams; `_derive_seed(base, stream)` shows the stable mix discipline (`(base & 0x7fffffff) ^ hash`, `* 1103515245 + 12345`, `& 0x7fffffff`, avoid 0) — MIRROR this style for the per-attempt seed mix so attempt seeds are well-distributed and never 0. `to_snapshot()` exposes per-stream `draw_index` (the `_layout_draws_only_from_level_stream` tests read it to prove only `STREAM_LEVEL` advanced) — the validator must not advance any stream.
- `godot/scripts/tactical/board/board_state.gd` — `BoardState`. The candidate's validated board. `try_from_snapshot` already enforces: dup-id rejection, `_validate_entity_for_setup` (blocking entity on a WALL or occupied cell REJECTED via `_validate_cell_for_occupancy`), occupant cross-consistency, and STRIPS+RE-DERIVES cell `occupant_id` from blocking entities. So legal-enemy-placement legality is partly enforced at BUILD time — the validator RE-ASSERTS it at the validation layer (entrance/exit exclusion + entrance-reachability) and ADDS entity-aware reachability. Read entities via `entities()` (sorted copies) / `entity_at(cell)` / `entity_count()`; read terrain via `get_cell(cell).terrain`; bounds via `in_bounds`. Do NOT mutate the board.
- `godot/scripts/tactical/board/board_cell.gd` — `BoardCell`. `enum Terrain { FLOOR, WALL, HAZARD, ENTRANCE, EXIT }`. `terrain_blocks_occupancy()` / `blocks_line_of_sight()` are `true` ONLY for `WALL`. So FLOOR/HAZARD/ENTRANCE/EXIT are walkable; the validator's "walkable" predicate is "not WALL" (matches the generators + placer). HAZARD is walkable but is the v0 "danger" terrain for the behind-danger + safe-first-reveal checks. Do NOT add a terrain value or change the blocking predicate.
- `godot/scripts/tactical/entities/tactical_entity_state.gd` — `TacticalEntityState`. The enemy entity DTO already on the board. Read `entity_id`, `position`, `blocks_movement` for the entity-aware flood + legal-placement check. Do NOT add an entity type or change the DTO.
- `godot/scripts/save/snapshots/tactical_snapshot.gd` — `TacticalSnapshot`. Strict `parse`/`from_domain`. The validated candidate board already rides this path in the 3.5 tests; the validator operates on the `BoardState` (in-process), not the serialized snapshot. Do NOT persist to `user://`.
- `godot/scripts/core/results/action_result.gd` — `ActionResult` (`succeeded`, `is_error()`, `error_code`, deep-copied `metadata`). `LevelValidator` returns `ActionResult.ok([], {...})` / `ActionResult.error(<code>, {...})`; `LevelGenerator` maps a validator failure onto a `GenerationResult.error`.
- `godot/tests/unit/test_case.gd` — the custom headless harness base. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, call `assert_true/false/equal`, return `result()`. Do NOT add GUT/GdUnit.
- `godot/tests/unit/generation/test_level_generator.gd` — the level-generator integration test (Small/Medium payloads + diagnostics + unknown-recipe/invalid-request/null-repo/repository-boundary/missing-enemy-repo cases + the 3.5 `enemy_count`/`reward_count`/`optional_reward_count` diagnostics). UPDATE: assert the new `validated`/`attempts` success diagnostics; thread the optional validator param where needed; confirm the Medium success path now reports the validation phase (already does).
- `godot/tests/unit/generation/test_medium_level_layout_generator.gd` — has the AC2-rejection helpers (`_open_terrain_grid`, `_set_terrain`, `_layout_from_grid`) + the strict-path tests. REUSE these hand-built-candidate helpers (copy the pattern) for the validator's per-check fail tests. Keep this file GREEN (its `validate_readability` codes/diagnostics are unchanged).
- `godot/tests/unit/generation/test_enemy_reward_placement.gd` — the 3.5 focused placement test with INDEPENDENT BFS floods (the structural model for the validator tests — verify behaviour with an independent flood, do NOT re-assert the impl against itself). Keep GREEN (its `validate_reward_reachability` terrain-only code is unchanged; 3.6 ADDS entity-aware checks in the validator, it does not change the placer's check).
- `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` — the TERRAIN `APPROVED_FINGERPRINTS` fixtures + the DELIBERATE-UPDATE contract. MUST stay GREEN with NO re-pin (attempt 0 is unperturbed; the validator draws no RNG). If one drifts, it is a BUG.
- `godot/tools/dump_small_layout_fingerprints.gd` / `dump_medium_layout_fingerprints.gd` — one-shot dev tools printing the pinned TERRAIN fingerprints. You should NOT need to re-run them (terrain unchanged). NOT `test_*.gd`, NOT auto-discovered.
- `godot/tests/unit/core/test_project_structure.gd` — the structural fail-loud gate. The new `level_validator.gd` + new tests live inside already-required roots, so the gate stays green with NO change. Do NOT add a new top-level root.

### Existing Files To Update Or Preserve

| Path | Current State | This Story Changes | Preserve |
|---|---|---|---|
| `godot/scripts/generation/level/level_generator.gd` | `generate(request, recipe_repo, enemy_repo)`; per-phase methods build streams, generate layout, (Medium) validate readability, build board, ok-with-payload; `_map_layout_error` phase mapping. | UPDATE — wire `LevelValidator` into both phase success paths; WRAP the phase body in a bounded-retry loop (attempt 0 unperturbed, attempt `i` seed-mixed, cap `MAX_GENERATION_ATTEMPTS`); add optional injected-validator 4th param (real default); add `validated`/`attempts` diagnostics; final failure -> `GenerationResult.error` with the failing check's phase + `attempts`. | The 3-arg public signature (add the validator as an OPTIONAL 4th param), the size dispatch, the request-validation/recipe-resolution/`unknown_level_recipe` paths, `_seed_text`, the existing diagnostics keys, the `_ok_with_payload` payload key set, the existing `_map_layout_error` mappings. |
| `godot/scripts/generation/level/medium_level_layout_generator.gd` | `validate_readability` (3 AC2 checks) + the canonical BFS/first-reveal/interior-wall helpers + bounds. | NO change to its existing API/codes — REUSE its BFS + bounds from `LevelValidator` (call it or share a helper). Optionally extract a shared flood helper, keeping `validate_readability` green. | `validate_readability` codes/diagnostics/bounds, `_flood_reachable`/`NEIGHBOUR_OFFSETS`/`_first_reveal_cell_count`/`_interior_wall_count`, the fingerprint format, `build_board_snapshot`. |
| `godot/scripts/generation/level/small_level_layout_generator.gd` | Deterministic Small layout + placement; NO validator of its own. | NO code change expected — Small candidates now go through the shared `LevelValidator` via `LevelGenerator`. VERIFY approved Small seeds PASS the new checks at 8x8. | The draw order, the border-ring + reserved-corridor shape, the fingerprint format, the method surface. |
| `godot/scripts/generation/level/entity_reward_placer.gd` | `validate_reward_reachability` (TERRAIN-only); placement helpers. | NO change to its existing terrain-only check/code — `LevelValidator` ADDS entity-aware mandatory-reward reachability on top. | `validate_reward_reachability` terrain-only contract/code/diagnostics, `_reward_behind_danger`, `PLACEMENT_ENEMY_ORDER`, the placement draw order. |
| `godot/scripts/generation/level/generation_result.gd` | `ok`/`error`, full phase vocabulary (incl. unused `PHASE_PATHING`/`PHASE_REWARDS`/`PHASE_FINALIZE`), String `seed`, code normalization. | NO class change — reuse the existing phase constants (you MAY now use `PHASE_PATHING`/`PHASE_REWARDS`); extend the diagnostics dict only. | `ok`/`error` factories, phase constants, String `seed`, code/phase normalization. |
| `godot/scripts/generation/level/generation_request.gd` | `level_seed()` (= root_seed), `draw_layout_int/float` (STREAM_LEVEL only). | NO change — derive the per-attempt seed locally from `level_seed()` + attempt index; do NOT mutate the request or add a field. | `level_seed()`, the `STREAM_LEVEL`-only draw contract, request validation. |
| `godot/scripts/tactical/board/board_state.gd` / `board_cell.gd` | Strict `try_from_snapshot` (entity legality + occupant cross-consistency); WALL-only blocking; read-only helpers. | NO change — READ the built board (`entities()`/`entity_at()`/`get_cell()`/`in_bounds`) for entity-aware reachability + legal-placement checks. | `to_snapshot`/`try_from_snapshot` contract, WALL-only blocking predicate, the Terrain enum, the read-only helpers. |
| `godot/tests/unit/generation/test_level_generator.gd` | Payloads/diagnostics + recipe/request/repo cases + 3.5 placement diagnostics. | UPDATE — assert `validated`/`attempts` success diagnostics; thread the optional validator param where a test injects one. | All existing assertions. |
| `godot/tests/unit/generation/test_medium_level_layout_generator.gd` / `test_enemy_reward_placement.gd` | `validate_readability` / `validate_reward_reachability` tests + hand-built-candidate helpers + independent BFS. | NO change expected (their checks/codes are unchanged) — REUSE their hand-built-candidate + independent-flood patterns in the new validator tests. | All assertions + the helper methods. |
| `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` | Pinned TERRAIN `APPROVED_FINGERPRINTS`. | NO change — terrain is unchanged (attempt 0 unperturbed); fingerprints stay GREEN with NO re-pin. | All assertions + the deliberate-update contract. |
| `godot/tests/unit/core/test_project_structure.gd` | Asserts required roots exist. | NO change expected (`scripts/generation`/`tests/unit` already covered). | All required-root assertions. |

### Recommended New Files

Use these names unless implementation discovers a clearer local pattern:

- `godot/scripts/generation/level/level_validator.gd` — `class_name LevelValidator extends RefCounted`. The comprehensive pure-query validator (every named check as a separate method with a stable lower-snake code; compact pass/fail report; size-agnostic; first-failure short-circuit; entity-aware reachability). Lives under the already-required `res://scripts/generation/level/` root (no structure-gate change). Mirror the focused-validator structure of `MediumLevelLayoutGenerator.validate_readability` + `EntityRewardPlacer.validate_reward_reachability`.
- `godot/tests/unit/generation/test_level_validation.gd` — the focused AC1/AC3 validator test (clean candidate passes; each named check fails for a hand-built candidate with the expected code + compact diagnostics; known-good/known-bad fixtures). Mirror `test_medium_level_layout_generator.gd`'s hand-built-candidate + `test_enemy_reward_placement.gd`'s independent-flood structure.
- `godot/tests/unit/generation/test_bounded_retry.gd` — the AC2 bounded-retry test (determinism; attempt cap with injected always-fail validator + structured error + `attempts`; fail-then-pass succeeds on attempt k; attempt N's candidate differs deterministically from attempt 0's).
- `godot/tests/unit/generation/test_generation_phase_fixtures.gd` — the AC4 per-phase fixture test (one focused regression per phase asserting a DISTINCT `failed_phase`/`error_code`, reported separately from the seed-regression fingerprints; includes the end-to-end `unreachable_reward -> PHASE_VALIDATION` case closing the 3.5 Low).

Avoid: a new RNG draw path or stream; a new result type; a second/lenient board parser; a new `TacticalEntityState.EntityType`; a `RewardTableDefinition`/repository (Epic 6); a SECOND fingerprint format; placing the player/hero entity; enemy AI/turn/damage/hazard rules; real affinity rules (Epic 7); a manual-seed/batch CLI (3.7); randomizing entrance/exit or relaxing the reserved corridor (would force a fingerprint re-pin); deleting the focused `validate_readability`/`validate_reward_reachability` checks; touching live tactical truth / run save / settings / UI presenters; a difficulty knob; a new test framework; adding fields to the recipe/enemy/request/result definitions or the baselines; re-pinning the terrain fingerprints.

### Comprehensive Validation Contract (AC1 — every named check, compact pass/fail report, size-agnostic)

- AC1 verbatim: "Given generation produces a candidate level, when validation runs, then it checks entrance-to-exit reachability, no soft-locks, legal enemy placement, reachable intended rewards, fog/readability constraints, and safe first reveal, and validation emits a pass/fail report." [Source: epics.md Story 3.6 AC1]
- FR36 verbatim: "Procedural generation validation must check entrance-to-exit pathing, absence of required class or item gates, legal enemy placement, reachable intended rewards, and safe first reveal." [Source: epics.md FR36]
- **Pure query, no RNG, no mutation** (the `validate_readability` precedent): `LevelValidator` reads the layout + the validated `BoardState` + the `rewards` markers and returns a structured report. It MUST NOT draw from the `level` stream (or any stream), execute commands, apply events, or mutate the board/source state. Wiring it into the success path must keep `_layout_draws_only_from_level_stream` GREEN. [Source: project-context.md "Do not let a snapshot compose/restore consume RNG draws or mutate source state"; NFR14]
- **Separate named checks, first-failure short-circuit, compact diagnostics:** each check is a private method returning a stable lower-snake code on failure with COMPACT diagnostics (counts/coordinates/ratios — NEVER a full grid dump, mirroring `validate_readability`). The checks run in a FIXED documented order; the first failure short-circuits and is the reported failure. The pass report carries the same kind of compact counts `validate_readability` returns (reachable count, interior-wall count, first-reveal count, entity/reward counts checked). [Source: architecture "compact debug output"; validate_readability precedent]
- **Size-agnostic:** the SAME checks run for Small + Medium candidates. Small currently has no validator; 3.6 gives it the full pass. VERIFY approved Small seeds PASS; if a Medium-tuned bound is too strict for 8x8, set a size-appropriate NAMED constant with a documented rationale (do NOT silently weaken a check; do NOT re-pin Small fingerprints).
- **The check set + recommended v0 semantics** (each a stable code):
  - `unreachable_exit` — entrance->exit 4-neighbour BFS over non-WALL cells (REUSE Medium's `_flood_reachable`).
  - `soft_lock_detected` — entity-aware: the exit AND every MANDATORY reward must be reachable AROUND blocking entities (flood over non-WALL AND non-blocking-entity cells). Movement is symmetric over walkable cells, so "can't get back" is not a separate v0 case; the soft-lock guard is "a required objective is sealed off."
  - `required_gate_present` — v0 has NO realized gates, so PASS by construction; FAIL only if a synthetic gate marker sits on the mandatory path (forward guardrail, hand-built-candidate-driven).
  - `illegal_enemy_placement` — every entity on a legal occupiable non-entrance/exit cell, no dup blocking occupancy, every enemy cell entrance-reachable (re-asserts what `try_from_snapshot` enforces at build time + adds entrance-reachability/exclusion).
  - `unreachable_reward` — every MANDATORY reward entity-aware reachable; every OPTIONAL (behind-danger) reward terrain-reachable (not sealed). Strengthens the placer's terrain-only check.
  - `excessive_blockage` — REUSE Medium's interior-WALL-ratio bound (`0.35`).
  - `unreadable_first_reveal` — REUSE Medium's first-reveal bounded flood (radius 4, min cells).
  - `unsafe_first_reveal` — the entrance cell is NOT HAZARD AND no enemy is Chebyshev <= 1 of the entrance (player not spawned on a hazard or already-in-melee). v0 STATIC check (no simulated turns/detonation). [Source: FR5; FR58 fairness intent]

### Bounded-Retry Determinism Contract (AC2 — the central design decision)

A generated candidate is TODAY a pure function of `(root_seed, recipe)` (each phase builds `RngStreamSet.new(request.level_seed())`). So re-running the same seed yields the SAME candidate — naive retry would loop forever on a genuinely-bad seed. The retry must produce DIFFERENT-but-DETERMINISTIC candidates:

- **Per-attempt seed mix:** for attempt index `i` (0-based), derive `attempt_seed` from `request.level_seed()` mixed with `i` using a documented stable mix (MIRROR `RngStreamSet._derive_seed`'s style: xor + LCG-style multiply/add, mask to 31 bits, avoid 0). Build `RngStreamSet.new(attempt_seed)` and run the layout+placement+validation for that attempt. [Source: rng_stream_set.gd `_derive_seed`; project-context.md Determinism rules]
- **Attempt 0 is UNPERTURBED:** `attempt_seed(0)` MUST equal `request.level_seed()` exactly (e.g. the mix returns the base seed for `i == 0`), so the existing seed-regression terrain fingerprints + all 3.2-3.5 generated-layout/placement tests stay byte-identical. This is the hard invariant — verify fingerprints do NOT drift. [Source: retro-notes epic-3 Story 3-5 "placement draws APPENDED ... fingerprints stay stable"]
- **Bounded:** cap at `MAX_GENERATION_ATTEMPTS` (a NAMED constant; recommend a small bound such as 8 — document the choice; NFR4 < 3s load budget means a small cap is safe and a large one risks the budget). On exhaustion, return a structured `GenerationResult.error` with the LAST failing check's phase + code + reason + `attempts == MAX_GENERATION_ATTEMPTS` + compact diagnostics. [Source: epics.md Story 3.6 AC2; NFR4]
- **Deterministic final result:** the SAME `(root_seed, recipe)` produces the SAME final result every time (same attempt count, same final passing candidate OR same final error). A retry that succeeds on attempt `k` reports `attempts == k+1` and the successful candidate. [Source: epics.md Story 3.6 AC2]
- **Test seam (no genuinely-bad baseline seed needed):** baseline candidates pass on attempt 0, so to exercise the retry loop + cap, INJECT a validator outcome. Add an OPTIONAL injected-validator param to `generate` (real `LevelValidator` default) OR factor the retry loop to accept a validator object/callable. The test injects: (a) an always-fail validator -> assert the cap is reached + a structured error + `attempts == MAX`; (b) a fail-for-the-first-k-then-pass validator -> assert success + `attempts == k+1`; (c) assert attempt `i>0`'s candidate fingerprint differs from attempt 0's (deterministic perturbation). [Source: deferred-work.md 3-5 "add a test seam"]

### AC3 Bad/Good Fixture Contract (each bad layout fails for the expected reason; good layouts pass)

- AC3 verbatim: "Given validation tests run headlessly, when known bad fixture layouts are tested, then each bad layout fails for the expected reason, and known good fixture layouts pass." [Source: epics.md Story 3.6 AC3]
- For EACH validator check, build a hand-built candidate that fails ONLY that check (mirror the Medium `_open_terrain_grid`/`_set_terrain`/`_layout_from_grid` pattern + the 3.5 enemy-ringed-target pattern) and assert the expected stable code + compact diagnostics. Build a clean generated candidate (approved Small + Medium seeds) and assert it PASSES the full validator. Verify the bad-fixture candidate is genuinely isolating its target check (e.g. for `unsafe_first_reveal`, confirm the exit is still reachable so you are not accidentally tripping `unreachable_exit`) — the Medium AC2 tests already demonstrate this isolation discipline. [Source: test_medium_level_layout_generator.gd AC2 rejection tests]

### AC4 Per-Phase Reporting Contract (each phase a focused regression, separate from fingerprint failures)

- AC4 verbatim: "Given generation phase fixtures are tested, when route context, recipe selection, layout/pathing, blocker placement, enemy placement, reward placement, and final validation run, then each phase has at least one focused regression assertion, and phase failures are reported separately from final-map fingerprint failures." [Source: epics.md Story 3.6 AC4]
- One focused assertion per phase, each asserting a DISTINCT `failed_phase`/`error_code` (use distinct `GenerationResult` phases where the architecture provides them — `PHASE_RECIPE`, `PHASE_ENEMIES`, `PHASE_VALIDATION`, and optionally `PHASE_PATHING`/`PHASE_REWARDS`). `route` has no generation today (Epic 4 owns route generation) — assert the request/recipe seam stands in for it, or note `PHASE_ROUTE` is not exercised by v0 level generation. [Source: epics.md Epic 4 route generation]
- **Separation invariant:** a PHASE regression surfaces in `test_generation_phase_fixtures.gd` (asserting `failed_phase`/`error_code`); a TERRAIN regression surfaces in the seed-regression tests (asserting the `fingerprint`). The two must not be conflated — document this in the new test header and ensure a phase failure does not silently drift a fingerprint (it cannot — phase failures are structured errors that return no payload). [Source: architecture "phase failures are reported separately from final-map fingerprint failures"]
- **Close the 3.5 deferred Low (#3):** add a `LevelGenerator.generate`-level case that drives an UNREACHABLE reward through the full pipeline (via the injected-validator seam or a custom recipe/candidate) and asserts the `PHASE_VALIDATION`/`unreachable_reward` result — not just the placer unit-tested in isolation. [Source: deferred-work.md 3-5 Round 2 "add a LevelGenerator-level case that injects an unreachable reward"]

### Epic-3 Inherited Constraints (carried from earlier stories — apply directly to this story)

- **`level_seed()` not `request.seed`; `GenerationResult.seed` is a String.** All seed handling uses `request.level_seed()` and the String `seed` carried by `GenerationResult.error`. Verify retry/final-error seed stability through the real String/int64-string transport, never native dicts. [Source: retro-notes epic-3 Story 3-1]
- **`Array[Dictionary]` does NOT survive `ActionResult`/`GenerationResult` metadata deep-copy** — it returns a plain `Array` and crashes a strict re-typed receiver. Receive metadata-carried dictionary lists (rewards, entities, diagnostics arrays) as UNTYPED `Array`. [Source: retro-notes epic-3 Story 3-4; small/medium generators' `wrinkles`/`enemies`/`rewards` handling]
- **Occupant invariant:** `BoardState.try_from_snapshot` RECORDS the cell occupant, STRIPS it to `""`, and CROSS-CHECKS it against blocking entities (a blocking entity whose cell carries `""` is REJECTED `invalid_cell_occupant`). The generators' `build_board_snapshot` already sets `occupant_id` on each blocking enemy's cell. The validator must read occupancy via the ENTITY list / `entity_at`, NOT a raw cell `occupant_id` field (which is stripped). [Source: retro-notes epic-3 Story 3-5 occupant-invariant CORRECTION; board_state.gd]
- **Placement uses `STREAM_LEVEL`, not `rewards`/`loot`.** The `rewards`/`loot` streams are reserved for Epic 6 runtime resolution; ALL generation draws (incl. the retry attempts) use `STREAM_LEVEL` via `draw_layout_int`. The validator draws nothing. [Source: retro-notes epic-3 Story 3-5 RNG-stream resolution]
- **`wall_density` is intentionally inert** for blocker-count derivation (the budget band is authoritative); the AC2 excessive-blockage ratio (0.35) is the independent readability constant. Do NOT wire `wall_density` into validation. [Source: retro-notes epic-3 Story 3-3; deferred-work.md 3-3 Low]
- **`door`/`affinity_placeholder`/`risky_side_branch` wrinkle kinds remain unrealized; HAZARD is walkable + sight-transparent** (only WALL blocks). The validator treats HAZARD as walkable terrain that is "danger" for the behind-danger + safe-first-reveal checks; do NOT make HAZARD block movement. [Source: retro-notes epic-3 Story 3-4 HAZARD; level_recipe_definition.gd]
- **Small `build_board_snapshot` lacks the per-row/leading shape guard** the Medium one has (3.2-deferred Low, NOT reopened) — fine for generated input; the validator does not rely on Small rejecting a hand-built malformed grid. [Source: retro-notes epic-3 Story 3-3; deferred-work.md 3-2 Low]

### Deferred-Work Items Relevant To This Story (fold in; the rest of the ledger is out of scope)

These three entries from `_bmad-output/implementation-artifacts/deferred-work.md` overlap this story's area and should be ADDRESSED here (the rest of the ledger is unrelated and stays deferred):

- **(3-5 Round 2, Low) `EntityRewardPlacer.validate_reward_reachability` is TERRAIN-only** — ignores blocking enemies; the 3.5 note explicitly scopes "entity-aware / no-soft-lock reachability to Story 3.6's comprehensive validator, which will likely SUBSUME this focused check." -> Story 3.6 IMPLEMENTS entity-aware reachability for mandatory rewards + the exit (Tasks 3.6.2(b)(e), 3.6.3). Do NOT change the placer's terrain-only check (test-pinned); ADD the entity-aware check in `LevelValidator`. [Source: deferred-work.md 3-5 Round 2]
- **(3-5 Round 2, Low) `unreachable_reward -> PHASE_VALIDATION` NOT exercised end-to-end** — the placer is unit-tested but no test drives an unreachable reward through `LevelGenerator.generate`. -> Story 3.6 ADDS the end-to-end case via the injected-validator seam / a custom candidate (Task 3.6.5). [Source: deferred-work.md 3-5 Round 2]
- **(3-2 Low) entrance->exit reachability is coupled to the fixed 8x8 footprint + reserved corridor** — "Story 3.6 owns the formal reachability validator + retry that would tighten this." -> Story 3.6 adds the formal reachability validator (it does NOT jitter the size or randomize entrance/exit — that stays optional/out-of-scope; the formal validator is added ON TOP of the construction guarantee). [Source: deferred-work.md 3-2 Low; retro-notes epic-3 Story 3-2]

NOT in scope (stay deferred — unrelated to 3.6's validator/retry mandate): the 3-5 behind-danger UNDER-PLACEMENT Low (#1) is unreachable for the baseline recipes (only `small_combat_basic` has `allow_reward_behind_danger=false`, and Small never emits HAZARD) — note it stays deferred unless a HAZARD-permitting Small-class recipe is added (it is not this story); the 3-4 wrinkle pool-exhaustion Low; the 3-3 `wall_density` dead-field decision; the 3-1 `LevelRecipeDefinition.validate()` negative-test gaps; and all the Epic 1/2 save/settings/view-model Lows. Do NOT reopen or re-defer these.

### Testing Standards Summary

- Headless, scene-free, deterministic. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, use `assert_true/false/equal`, return `result()`. New tests live under `res://tests/unit/generation/` (auto-discovered by the runner). Do NOT add GUT/GdUnit. [Source: project-context.md Testing rules]
- Build candidates/boards in-memory only — NO `user://` writes. The validator operates on the in-process `BoardState`; if a test rides the board through `TacticalSnapshot`, JSON-round-trip it (`JSON.stringify` -> `parse_string`) per the Epic 1-2 lesson, never assert only on native dicts. [Source: project-context.md Save/snapshot test rules]
- Use INDEPENDENT reachability floods in the tests (not the validator's own helper) so the test verifies BEHAVIOUR, not the implementation against itself (the 3.5 `test_enemy_reward_placement.gd` pattern). [Source: test_enemy_reward_placement.gd header]
- Run the full suite via PowerShell ONLY (`godot` is not on the Bash PATH; it resolves via `C:\Users\Rasmus\bin\godot.cmd`): `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10` -> exit `0` / "Headless tests passed." Then `git diff --check`. The two `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure diagnostics, NOT regressions. [Source: project-context.md Testing rules; CLAUDE.md]
- TDD: write the failing `test_level_validation.gd` / `test_bounded_retry.gd` / `test_generation_phase_fixtures.gd` FIRST, then implement to green. Every new check needs a pass case AND a focused fail case; the retry loop needs determinism + cap + fail-then-pass; the phase fixtures need DISTINCT phases per failure separate from the fingerprints.

### Project Structure Notes

- `LevelValidator` lives at `godot/scripts/generation/level/level_validator.gd` (the already-required `scripts/generation/level/` root — no structure-gate change). It is a `RefCounted` data-layer service, NOT a Node, with NO scene/UI/audio dependency (NFR14). [Source: project-context.md Code Organization rules]
- New tests live at `godot/tests/unit/generation/` mirroring the domain (the already-required `tests/unit` root). [Source: project-context.md Testing rules]
- The bounded-retry loop lives in `LevelGenerator` (the generation orchestrator) — NOT in an autoload, NOT in a scene, NOT in the run/save layer. [Source: project-context.md "Do not put gameplay decision logic in autoloads"]

### Project Context Rules

- **Determinism:** every generation draw (incl. retry attempts) uses `GenerationRequest.draw_layout_int` -> `RngStreamSet.STREAM_LEVEL`; NEVER `randi()`/`randf()`/a `RandomNumberGenerator`/another stream. The validator draws NO RNG. Same `(root_seed, recipe)` -> identical final result. [Source: project-context.md Determinism & Simulation Rules]
- **Generator validation failures report seed, phase, reason, and compact diagnostics** — the final-failure `GenerationResult.error` and every check's report follow this. NEVER a full grid dump. [Source: project-context.md Rules/AI/Generation Rules; architecture]
- **Repositories are the content boundary** — enemies resolved through `EnemyRepository`, recipes through `LevelRecipeRepository` (already threaded through `LevelGenerator`); the validator reads the already-built board, it does not read content files. [Source: project-context.md "Do not access files directly from gameplay systems; use repositories"]
- **Snapshots/validation are pure reads** — composing/validating a candidate consumes NO RNG and mutates neither the source state nor any save. [Source: project-context.md Save/Snapshot rules; NFR14]
- **Difficulty is a HARD non-goal** — validation bounds are readability/fairness constants, never difficulty knobs; nothing scales enemy stats/HP/damage/rewards/RNG/run length. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- **No new cloud/account/multiplayer/telemetry/.NET/C# dependencies; no React prototype dependency; no new test framework.** Typed GDScript, Godot 4.6.3 standard build. [Source: project-context.md Technology Stack; Critical Don't-Miss Rules]
- **Performance:** generated level load target < 3s (NFR4) — keep `MAX_GENERATION_ATTEMPTS` small so the worst-case retry path stays well inside the budget; the validator is a cheap per-cell pass. [Source: project-context.md Performance Rules]

### References

- [Source: epics.md Epic 3 / Story 3.6 — Generator Validation and Bounded Retry (ACs 1-4, Tasks 3.6.1-3.6.4)]
- [Source: epics.md FR36 — generator validation must check entrance-to-exit pathing, absence of required class/item gates, legal enemy placement, reachable intended rewards, safe first reveal]
- [Source: epics.md FR5 — baseline line-of-sight radius 4 tiles (first-reveal radius)]
- [Source: epics.md FR58 — Darkness fairness intent (no unavoidable damage from unseen space)]
- [Source: epics.md NFR4 — generated level load time < 3s for MVP]
- [Source: epics.md NFR13/NFR14 — deterministic under seeded execution; headless without rendering/audio/UI]
- [Source: _bmad-output/implementation-artifacts/3-5-enemy-and-reward-placement.md — the direct template; placement/board/snapshot conventions]
- [Source: _bmad-output/auto-gds/retro-notes/epic-3.md — Stories 3-1..3-5 epic-wide constraints (level_seed, Array[Dictionary] deep-copy, occupant invariant, STREAM_LEVEL placement, HAZARD walkable, fixed draw order, 3.6 owns formal validator+retry)]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md — 3-5 Round 2 entity-aware reward reachability + end-to-end unreachable_reward Lows; 3-2 reachability-coupling Low (all assigned to 3.6)]
- [Source: project-context.md — determinism, generation, validation, save/snapshot, performance, difficulty-non-goal, testing, code-organization rules]
- [Source: godot/scripts/generation/level/level_generator.gd — dispatch, phase methods, `_map_layout_error`, `_ok_with_payload`, `_seed_text`]
- [Source: godot/scripts/generation/level/medium_level_layout_generator.gd — `validate_readability` (3 AC2 checks), `_flood_reachable`, bounds]
- [Source: godot/scripts/generation/level/entity_reward_placer.gd — `validate_reward_reachability` (terrain-only), `_reward_behind_danger`, `PLACEMENT_ENEMY_ORDER`]
- [Source: godot/scripts/generation/level/generation_result.gd — phase vocabulary incl. unused `PHASE_PATHING`/`PHASE_REWARDS`; String `seed`; code/phase normalization]
- [Source: godot/scripts/core/state/rng_stream_set.gd — `_derive_seed` mix discipline for the per-attempt seed]
- [Source: godot/scripts/tactical/board/board_state.gd / board_cell.gd — strict `try_from_snapshot`, `entities()`/`entity_at()`, WALL-only blocking, Terrain enum]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-16 | 0.1 | Initial story context created (comprehensive create-story analysis: comprehensive validator + bounded deterministic retry + per-phase fixtures; entity-aware reachability closing the 3.5 terrain-only Low; attempt-0-unperturbed fingerprint invariant; injected-validator test seam). Status set ready-for-dev. | Bob (Scrum Master) |
