---
created: 2026-06-16
source_story_key: 3-4-tactical-wrinkles-blockers-and-hazards
baseline_commit: 50f2341
---

# Story 3.4: Tactical Wrinkles, Blockers, and Hazards

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want generated combat levels to include at least one readable tactical wrinkle,
so that generated spaces create decisions rather than empty rooms.

## Acceptance Criteria

1. Given a combat level recipe requires a tactical wrinkle, when generation runs, then it places at least one wrinkle such as hazard, door, choke point, flank route, blocker cluster, affinity placeholder, enemy formation, reward behind danger, or risky side branch, and the wrinkle type is recorded in generation diagnostics.
2. Given blockers or hazards are placed, when pathing validation runs, then mandatory entrance-to-exit progress remains possible, and no required progress depends on a specific class, weapon, or item.
3. Given a hazard is included in a generated snapshot, when the tactical board loads the snapshot, then the hazard is represented as domain data, and future presentation can mirror it without owning hazard truth.

[Source: epics.md Epic 3 / Story 3.4]

## Tasks / Subtasks

- [ ] 3.4.1 Confirm the Epic 3 boundary and write FAILING tests FIRST (TDD red phase). (AC: 1-3)
  - [ ] Verify in `_bmad-output/implementation-artifacts/sprint-status.yaml` that `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-seed-stable-small-level-layouts: done`, `3-3-seed-stable-medium-level-layouts: done`, `epic-3: in-progress`, and this story (`3-4-tactical-wrinkles-blockers-and-hazards`) is `ready-for-dev`. If any earlier Epic 1/2/3.1/3.2/3.3 status regressed, STOP and restore the boundary before implementing.
  - [ ] Confirm the working tree is clean or that dirty files are intentional user work; preserve unrelated changes. The untracked orchestrator-owned `_bmad-output/auto-gds/` directory is expected and is NOT your change.
  - [ ] Add focused FAILING tests before any production edit. Recommended new file: `godot/tests/unit/generation/test_tactical_wrinkle_placement.gd` (wrinkle-placed-when-required + wrinkle-kind-recorded-in-diagnostics + at-least-`min_tactical_wrinkles` wrinkles + HAZARD-walkable-and-reachable + entrance->exit-still-reachable-with-wrinkles + class/weapon/item-agnostic-progress + `level`-stream-only draws + determinism + hazard rides the strict board/`TacticalSnapshot` path). Extend the EXISTING `godot/tests/unit/generation/test_medium_level_layout_generator.gd` and `test_small_level_layout_generator.gd` to assert wrinkles now appear in the generated layout/diagnostics (they currently assert NO wrinkle placement). UPDATE the seed-regression fixtures (`test_medium_level_layout_seed_regression.gd`, `test_small_level_layout_seed_regression.gd`) ONLY after the generator change is final, by re-pinning via the dump tools — the fingerprints WILL drift because the terrain grid now carries wrinkle cells (see 3.4.6). UPDATE `test_level_generator.gd` if the success-result diagnostics gain a wrinkle field. The headless runner auto-discovers `test_*.gd` under `godot/tests/unit` and `godot/tests/integration` ONLY (NOT `tests/fixtures`, NOT `tests/headless`); no registry/`.tscn` edit is needed.
  - [ ] Reuse existing primitives EXACTLY — the 3.1-3.3 contracts (`GenerationRequest.draw_layout_int`/`draw_layout_float` + `level_seed()`, `GenerationResult.ok/error` + `PHASE_LAYOUT`/`PHASE_HAZARDS`/`PHASE_VALIDATION`, `LevelGenerator.generate` + `_run_small_layout_phase`/`_run_medium_layout_phase`, `LevelRecipeDefinition` + `tactical_wrinkle_requirements()`/`allowed_wrinkle_kinds`/`min_tactical_wrinkles`, `LevelRecipeRepository`), `RngStreamSet` + `STREAM_LEVEL`, `BoardState`/`BoardCell` + `BoardCell.Terrain` (`FLOOR`/`WALL`/`HAZARD`/`ENTRANCE`/`EXIT`), `BoardState.try_from_snapshot()` / `TacticalSnapshot.parse`, and the existing `SmallLevelLayoutGenerator`/`MediumLevelLayoutGenerator` patterns (fixed draw order, shrinking-pool selection, `fingerprint(layout)`, `build_board_snapshot(layout)`, `_blocker_candidate_cells`, `validate_readability` on Medium). Do NOT invent a new RNG draw path, a new result type, a new board representation, a parallel snapshot parser, a second terrain enum, or a second fingerprint scheme.
  - [ ] Do NOT in this story: implement enemy or reward placement (Story 3.5 — generated entities stay an EMPTY `entities: []`; `reward_behind_danger`/`enemy_formation` wrinkle KINDS that require entities are out of scope this story — see Scope Boundary); the FULL validation pass + bounded retry across ALL categories incl. legal-enemy-placement and reachable-reward (Story 3.6); the manual-seed loader + batch fingerprint CLI (Story 3.7); touch `MoveCommand`/`AttackCommand`, live tactical truth, the run autosave, settings, UI/scene presenters, or meta progression; add cloud/account/telemetry or any new engine/library dependency; add hazard DAMAGE/effect rules (the rules kernel owns hazard effects; this story only places the hazard as domain terrain data).

- [ ] 3.4.2 Implement deterministic tactical-wrinkle placement in BOTH layout generators. (AC: 1)
  - [ ] A combat recipe carries `min_tactical_wrinkles >= 1` and an `allowed_wrinkle_kinds` allowlist (read them via `recipe.tactical_wrinkle_requirements()` / the typed fields — do NOT hardcode). Stories 3.2/3.3 satisfied that requirement ONLY STRUCTURALLY (they placed floor/wall/blocker/entrance/exit and NO actual wrinkle). Story 3.4 is the story that must PLACE at least `min_tactical_wrinkles` readable wrinkles, drawn deterministically from the recipe allowlist, and RECORD each placed wrinkle's kind in generation diagnostics (AC1 verbatim: "the wrinkle type is recorded in generation diagnostics").
  - [ ] Implement wrinkle placement inside the generator's layout construction (mirror the fixed-draw-order discipline of the existing blocker placement). EVERY wrinkle-affecting random choice (which kinds, how many beyond the minimum if any, which cells) MUST be drawn through `request.draw_layout_int(streams, ...)` / `draw_layout_float(streams, ...)` (→ `RngStreamSet.STREAM_LEVEL`). NEVER call `randi()`/`randf()`, `RandomNumberGenerator.new()`, or any other stream. The draw ORDER is fixed and documented; wrinkle draws come AFTER the existing blocker-count + blocker-position draws (appending to the fixed order) so existing blocker behaviour is preserved and the new fingerprints are reproducible. Two generations with the same `root_seed` + recipe + starting `level`-stream state MUST produce byte-identical layouts incl. wrinkles.
  - [ ] Wrinkle realization for v0 (keep it terrain-only and scene-free): realize each wrinkle as terrain placement on the EXISTING `BoardCell.Terrain` enum — `WALL` clusters/columns for `choke_point`/`blocker_cluster`/`flank_route` shaping, and `HAZARD` cells for `hazard`. Treat the existing interior `WALL` blockers as the substrate the structural wrinkles (choke point / blocker cluster / flank route) are expressed through; a `hazard` wrinkle introduces `BoardCell.Terrain.HAZARD` cells (the FIRST story to emit HAZARD — 3.2/3.3 explicitly reserved it for here). Do NOT add a new terrain value, a doors subsystem, or any non-terrain wrinkle representation. The set of wrinkle KINDS you can REALIZE in v0 is the terrain-expressible subset of the recipe allowlist: `choke_point`, `blocker_cluster`, `flank_route`, `hazard`. Kinds that need entities/rewards (`enemy_formation`, `reward_behind_danger`) or a feature subsystem (`door`, `affinity_placeholder`, `risky_side_branch`) are NOT realized this story — see Scope Boundary for how to honor the recipe minimum without them.
  - [ ] CRITICAL HAZARD nuance (verify before relying on it): `BoardCell.terrain_blocks_occupancy()` and `blocks_line_of_sight()` are BOTH `true` ONLY for `WALL`. A `HAZARD` cell is therefore WALKABLE and sight-TRANSPARENT at the board level — it is a domain hazard, not an obstacle. This is exactly why AC2 (entrance->exit progress must remain possible) is satisfiable with hazards present, and why the reachability flood (which treats ONLY `WALL` as not-walkable) correctly treats a hazard tile as passable. Do NOT make HAZARD block movement or LoS in the board model, and do NOT change `BoardCell`. (Hazard DANGER — taking damage for standing on it — is a rules-kernel concern owned by a later story, NOT this one. This story places the hazard as readable domain terrain only.) [Source: board_cell.gd `terrain_blocks_occupancy`/`blocks_line_of_sight`]
  - [ ] Diagnostics: extend the successful `GenerationResult.ok(...)` diagnostics (and/or the layout dict the generator returns) with the placed-wrinkle record — at minimum a list of placed wrinkle kinds (e.g. `wrinkles: ["choke_point", "hazard"]`) and a `wrinkle_count`. Keep diagnostics COMPACT (kinds + counts + maybe coordinates), never a full grid dump. The diagnostics shape is what AC1 asserts; pin it in a test. Mirror the existing diagnostics keys (`phase`, `recipe_id`, `size_class`, `blocker_count`) — ADD `wrinkles`/`wrinkle_count` alongside, do not remove the existing keys.

- [ ] 3.4.3 Preserve entrance-to-exit reachability and class/weapon/item-agnostic progress with wrinkles present. (AC: 2)
  - [ ] AC2 part 1 — mandatory progress remains possible: placing blockers AND hazards must NOT wall off the exit. The existing construction guarantee (reserved blocker-free central corridor + fixed entrance `(1, h/2)` / exit `(w-2, h/2)` on opposite interior edges) keeps a floor path between entrance and exit regardless of draws. Wrinkle placement MUST continue to respect that guarantee: never place a `WALL` wrinkle cell on the reserved corridor row, the entrance, or the exit (the existing `_blocker_candidate_cells` already excludes those for blockers — route wrinkle WALL cells through the SAME candidate discipline). HAZARD cells are walkable so they do NOT threaten reachability, but for readability keep the entrance, exit, and the reserved corridor free of hazard too (a hazard on the only path would be an unavoidable-danger fairness problem; the corridor is the safe mandatory route).
  - [ ] AC2 part 2 — reachability re-verified: after wrinkles are placed, the entrance MUST still reach the exit over non-WALL (walkable, incl. HAZARD) cells. For Medium, the existing `validate_readability` AC2 reachability check (4-neighbour BFS flood over non-WALL cells) already covers this — confirm it runs on the post-wrinkle layout and that HAZARD cells are treated as walkable by the flood (they are, since the flood blocks only on `WALL`). For Small (which has NO `validate_readability` today — 3.2 guaranteed fairness structurally), EITHER reuse the same 4-neighbour reachability check on the post-wrinkle Small layout to prove the corridor survived, OR rely on the construction guarantee and document it; PREFER adding the reachability assertion in the Small generator test at minimum (the corridor reservation is the guarantee; the test is the tripwire). Do NOT build a general validator/retry engine (that is Story 3.6).
  - [ ] AC2 part 3 — no required class/weapon/item gate: the mandatory entrance->exit progress must be traversable by the baseline hero with NO class/weapon/item dependency. v0 has no doors/keys/locks (those are deferred), so the only progress requirement is walkable floor + walkable hazard. Assert this explicitly in a test: the entrance->exit path exists over `FLOOR`/`HAZARD`/`ENTRANCE`/`EXIT` cells only (never requiring passing through a `WALL`, and never requiring an item/key). Document that this story introduces NO gating feature, so "no required class/item gate" holds by construction. [Source: epics.md Story 3.4 AC2; FR36 "absence of required class or item gates"]

- [ ] 3.4.4 Represent the hazard as domain terrain and prove it loads + is mirror-able by presentation. (AC: 3)
  - [ ] AC3 part 1 — hazard is domain data: a generated layout that includes a `hazard` wrinkle emits `BoardCell.Terrain.HAZARD` cells in the row-major terrain grid, and the converted board snapshot (EXACT `BoardState.to_snapshot()` shape) MUST pass `BoardState.try_from_snapshot()` with the hazard cell present. Confirm `BoardState._is_valid_terrain` accepts HAZARD (it does — the valid range is `FLOOR..EXIT`, and `HAZARD` sits inside it). The hazard is therefore versioned, serializable domain data — NOT a scene node. `entities` stays an EMPTY array.
  - [ ] AC3 part 2 — survives the real transport + strict snapshot path: the hazard-bearing board snapshot must survive a real `JSON.stringify` -> `parse_string` round-trip and re-convert via `BoardState.try_from_snapshot`, AND ride the strict `TacticalSnapshot.from_domain` -> JSON -> `parse` path (mirror the existing `_board_rides_strict_tactical_snapshot_path` test). Assert the restored board's hazard cell still reports `BoardCell.Terrain.HAZARD`. (Epic 3 retro: verify generated-level seed stability through the real `TacticalSnapshot` + int64-string transport, never native dicts.)
  - [ ] AC3 part 3 — presentation can mirror it without owning it: AC3 says "future presentation can mirror it without owning hazard truth." The presentation surface already exists: `TacticalInspectView` carries a `hazards: []` slot (currently always empty). You do NOT need to build the HUD/presenter this story — proving the hazard is readable domain terrain on the board snapshot (queryable via `board.get_cell(cell).terrain == HAZARD`) is sufficient. Document that the inspect-view `hazards` slot is the intended mirror point and that wiring it to read board hazard terrain is a later HUD story (do NOT wire it now — that would pull UI/scene work into a generation story). Do NOT let presentation own hazard truth; the board terrain is the truth. [Source: tactical_inspect_view.gd `hazards: []`; project-context.md presentation/view-model rules]

- [ ] 3.4.5 Honor the recipe wrinkle minimum and allowlist deterministically; decide non-realizable-kind handling. (AC: 1)
  - [ ] `small_combat_basic`: `min_tactical_wrinkles = 1`, allowed kinds = `choke_point` + `blocker_cluster` (BOTH terrain-realizable). `medium_combat_basic`: `min_tactical_wrinkles = 2`, allowed kinds = `choke_point` + `flank_route` + `blocker_cluster` + `hazard` + `reward_behind_danger`. So Small can fully satisfy its minimum from realizable kinds, and Medium can satisfy its minimum of 2 from the realizable subset (`choke_point`/`flank_route`/`blocker_cluster`/`hazard`) WITHOUT needing `reward_behind_danger` (which needs a reward entity — Story 3.5). DECIDE and DOCUMENT how you select kinds: select `min_tactical_wrinkles` kinds deterministically from the recipe's allowlist FILTERED to the v0-realizable subset (`choke_point`, `blocker_cluster`, `flank_route`, `hazard`); if the recipe requires more wrinkles than the realizable subset can supply, document the chosen policy (e.g. repeat a realizable kind, or cap at the realizable count with a diagnostic note) — but for the TWO baseline recipes this never binds (both minimums are satisfiable from the realizable subset). Do NOT place a `reward_behind_danger`/`enemy_formation`/`door`/`affinity_placeholder`/`risky_side_branch` wrinkle this story.
  - [ ] Whatever selection policy you pick MUST be deterministic (drawn from the `level` stream, fixed order) and the placed kinds MUST be a subset of the recipe's `allowed_wrinkle_kinds` (assert this in a test — a placed kind outside the allowlist is a bug). At least `min_tactical_wrinkles` wrinkles are placed for a combat recipe (assert the count meets the minimum). A non-combat recipe (or `min_tactical_wrinkles = 0`) places no required wrinkle (but MAY still place blockers per its budget). Record the decision in the generator header + Completion Notes.
  - [ ] Keep the Small and Medium wrinkle approach SIBLINGS (same method surface, same fixed-draw-order discipline, same diagnostics shape) exactly as the two generators are already siblings. If a shared helper is cleaner (e.g. a small `tactical_wrinkle_placer.gd` or shared static methods) you MAY extract one, but do NOT over-engineer a general framework — keep it scoped to the four realizable kinds and the two existing generators.

- [ ] 3.4.6 Re-pin the seed-regression fingerprints (the terrain grid now carries wrinkle cells). (AC: 1)
  - [ ] Placing wrinkles changes the row-major terrain grid, so the `fingerprint(layout)` value for every approved seed WILL drift for BOTH generators. This is an INTENTIONAL generator change — re-pin the fixtures in the SAME change: regenerate the Small fingerprints via `godot/tools/dump_small_layout_fingerprints.gd` and the Medium fingerprints via `godot/tools/dump_medium_layout_fingerprints.gd`, then update `APPROVED_FINGERPRINTS` in `test_small_level_layout_seed_regression.gd` and `test_medium_level_layout_seed_regression.gd`. The deliberate-update contract is honored BY re-pinning in this PR and logging it in the Change Log — never silently. Keep the failing-seed-in-assert-message behaviour intact.
  - [ ] If the dump tools need a small change to also print the wrinkle diagnostics (optional), keep them as `*.gd` dev tools (NOT `test_*.gd`, NOT auto-discovered). The fingerprint format itself should NOT change (it already serializes the full terrain grid incl. HAZARD `2` cells via `str(int(row[x]))`); only the pinned VALUES change. Confirm the fingerprint still uniquely captures the wrinkle terrain (it does — HAZARD is terrain value `2`, distinct from FLOOR `0`/WALL `1`).
  - [ ] Re-confirm AC1 divergence still holds after wrinkles: different seeds still produce meaningfully different layouts (now varying in wrinkle kinds/positions too); approved seeds remain internally deterministic (same seed -> byte-identical). Update the `_different_seeds_diverge` / `_approved_seeds_show_meaningful_divergence` assertions only if needed; they should still pass.

- [ ] 3.4.7 Run required validation and update story records. (AC: 1-3)
  - [ ] Run through PowerShell (the bare `godot` resolves only as `C:\Users\Rasmus\bin\godot.cmd` via PowerShell; the Bash tool PATH cannot find it): `godot --version`, then `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`, then `git diff --check`.
  - [ ] Expect: Godot `4.6.3.stable.official...`; the full headless runner exits code `0`; ALL prior Epic 1 + Epic 2 + Story 3.1 + Story 3.2 + Story 3.3 tests stay green (including `test_project_structure.gd` — any new file lives inside the already-required `res://scripts/generation`/`res://tests/unit` roots, so the gate stays green with no extension; the UPDATED Small/Medium generator + seed-regression + `test_level_generator` tests must pass with the new wrinkle behaviour + re-pinned fingerprints); `git diff --check` reports no whitespace errors. NOTE: the two pre-existing `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure diagnostics, NOT failures — do not treat them as a regression, and do NOT add a new deliberate malformed-JSON parse test (no save-WRITE work this story).
  - [ ] Update this story's Dev Agent Record, Completion Notes, File List, Change Log, and Status. RECORD: the wrinkle-kind selection policy + which kinds are v0-realizable (3.4.2/3.4.5); the HAZARD-as-walkable-terrain decision + that hazard DANGER is deferred to the rules kernel (3.4.2/3.4.4); how Small reachability is re-verified with wrinkles present (3.4.3); the fingerprint re-pin (3.4.6) and that it was deliberate; and the diagnostics shape (`wrinkles`/`wrinkle_count`). Keep `sprint-status.yaml` synchronized with this story's status (`ready-for-dev` -> `in-progress` -> `review`). The orchestrator owns the `epic-3` status and any epic closeout — do not flip `epic-3` yourself.
  - [ ] Clean up any `user://` artifact your tests create. This story does no save WRITE work; if a test writes `user://`, sweep it (`*.json`, `*.json.tmp`, `*.json.bak`). Leave `user://` clean (the tests should build `TacticalSnapshot`/boards in-memory only).

## Dev Notes

### Pre-Implementation Gate

This is the **fourth Epic 3 implementation story**. Story 3.1 delivered the generation CONTRACT layer; Story 3.2 implemented the deterministic **Small (~8x8) layout phase**; Story 3.3 implemented the deterministic **Medium (~14x12) layout phase + the first AC2 readability validation (reject excessive blockage / unreachable exit / unreadable first reveal)**. Story 3.4 is the FIRST story to **place actual tactical wrinkles** (the recipes have carried `min_tactical_wrinkles`/`allowed_wrinkle_kinds` since 3.1, but 3.2/3.3 satisfied the requirement only STRUCTURALLY) and the FIRST story to **emit `BoardCell.Terrain.HAZARD`** (3.2/3.3 explicitly reserved HAZARD for here).

Story-creation analysis on 2026-06-16 found:

- `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-seed-stable-small-level-layouts: done`, `3-3-seed-stable-medium-level-layouts: done` (all code-review Approve, full headless suite green), `epic-3: in-progress`. Story 3.4 was `backlog` and is now `ready-for-dev`. Stories 3.5-3.7 remain `backlog`. Story 3.4 is NOT the first story in epic 3, so the epic status stays `in-progress` (do not change it).
- Both layout generators (`small_level_layout_generator.gd`, `medium_level_layout_generator.gd`) and the wired layout phases in `level_generator.gd` exist and are the DIRECT template. The board model (`BoardState`, `BoardCell`) and the strict snapshot path (`TacticalSnapshot.parse` / `BoardState.try_from_snapshot`) exist and are the conversion target. The recipes (`small_combat_basic`, `medium_combat_basic`) already carry the wrinkle requirements. EXTEND these; do NOT fork them.
- HAZARD terrain is ALREADY fully board-supported: `BoardCell.Terrain.HAZARD` exists; `BoardState._is_valid_terrain` accepts it; and crucially `terrain_blocks_occupancy()`/`blocks_line_of_sight()` are BOTH false for HAZARD (only WALL blocks). So a hazard tile is walkable + sight-transparent at the board level — no board change is needed to add hazards.

Before implementing, re-confirm the local tree is clean or that dirty files are intentional user work. If any Story 1.x/2.x/3.1/3.2/3.3 status regressed, stop and restore that boundary first.

### Scope Boundary

This story delivers **deterministic tactical-wrinkle placement** in BOTH the Small and Medium layout generators: at least `min_tactical_wrinkles` readable wrinkles per combat recipe, drawn deterministically from the recipe allowlist (filtered to the v0-terrain-realizable subset), realized as `WALL`-shaped structure (`choke_point`/`blocker_cluster`/`flank_route`) and/or `HAZARD` terrain (`hazard`), recorded in generation diagnostics, with entrance->exit reachability preserved and the hazard proven loadable + mirror-able as domain data.

In scope:

- **Wrinkle placement in both generators** — deterministic, `level`-stream-only, fixed-draw-order (appended AFTER the existing blocker draws). Realizes the four terrain-expressible kinds: `choke_point`, `blocker_cluster`, `flank_route` (WALL-shaped), and `hazard` (HAZARD terrain). At least `min_tactical_wrinkles` placed for a combat recipe; placed kinds are a subset of the recipe `allowed_wrinkle_kinds`.
- **The first HAZARD emission** — `BoardCell.Terrain.HAZARD` cells in the generated terrain grid, walkable + sight-transparent (board model unchanged), kept off the entrance/exit/reserved-corridor so the mandatory path stays safe.
- **AC2 reachability preserved** — wrinkle WALL cells route through the SAME `_blocker_candidate_cells` discipline (never the corridor/entrance/exit); Medium's existing `validate_readability` BFS re-verifies entrance->exit over non-WALL cells; Small re-verifies via the construction guarantee + a reachability assertion in its test. No class/weapon/item gate is introduced.
- **AC3 domain-data proof** — the hazard rides the strict `BoardState.try_from_snapshot` + `TacticalSnapshot.from_domain`/`parse` path, survives a real JSON round-trip, and is queryable as board terrain; the `TacticalInspectView.hazards` slot is documented as the intended (future) presentation mirror point.
- **Diagnostics** — successful `GenerationResult.ok(...)` diagnostics gain `wrinkles` (list of placed kinds) + `wrinkle_count`, alongside the existing `phase`/`recipe_id`/`size_class`/`blocker_count`.
- **Fingerprint re-pin** — both Small and Medium `APPROVED_FINGERPRINTS` re-pinned via the dump tools (the terrain grid now carries wrinkle cells); deliberate-update contract honored by re-pinning in this PR + logging it.
- **Tests** — wrinkle-placed-when-required, kind-recorded-in-diagnostics, count >= minimum, placed kinds within the allowlist, HAZARD walkable + reachable, entrance->exit still reachable with wrinkles, class/item-agnostic progress, `level`-stream-only + stream-isolation, determinism + divergence, hazard rides the strict board/`TacticalSnapshot` path + real JSON transport, and the re-pinned seed regressions.

Out of scope (owned elsewhere — do NOT build here):

- **Enemy and reward placement** and the wrinkle KINDS that require them. Story 3.5 places enemies/rewards on legal cells. The `enemy_formation` and `reward_behind_danger` wrinkle kinds (and the standalone reward-table decision) are Story 3.5 — even though `reward_behind_danger` is in the `medium_combat_basic` allowlist, do NOT realize it this story (Medium's minimum of 2 is satisfiable from `choke_point`/`flank_route`/`blocker_cluster`/`hazard`). Generated boards keep an EMPTY `entities: []`. [Source: epics.md Story 3.5]
- **Doors, affinity placeholders, risky side branches as first-class features.** `door`/`affinity_placeholder`/`risky_side_branch` are wrinkle kinds in the GDD allowlist but have NO v0 terrain/feature realization and are NOT in the two baseline recipes' realizable needs. Do NOT build a doors subsystem, an affinity placement system (real affinities are Epic 7), or a side-branch feature. If a future recipe requires one of these, that is its owning story. [Source: epics.md Epic 7; level_recipe_definition.gd wrinkle kinds]
- **Hazard DANGER / damage / effects.** This story places the hazard as readable domain TERRAIN only. Standing-on-hazard damage, telegraphed hazard danger, and any hazard rule belong to the rules kernel and a later combat/hazard-rules story — NOT here. Do NOT add hazard damage to commands, the board, or events. [Source: project-context.md Rules kernel rules; epics.md Story 3.4 AC3 "represented as domain data ... future presentation can mirror it"]
- **The FULL generator validation pass + bounded deterministic retry.** Story 3.6 implements the complete reachability / no-soft-lock / no-required-class-or-item-gate / legal-enemy-placement / reachable-intended-reward / safe-first-reveal validators + bounded retry + per-phase reporting, and will likely subsume Medium's focused `validate_readability`. This story only PLACES wrinkles and re-verifies the existing reachability guarantee — do NOT build a general validator/retry engine. [Source: epics.md Story 3.6]
- **The manual-seed loader + batch fingerprint CLI.** Story 3.7. This story re-pins a small set of approved-seed fingerprints in unit tests + the one-shot dump tools only. [Source: epics.md Story 3.7]
- **HUD / scene presenter for hazards.** Proving the hazard is readable board terrain (and documenting `TacticalInspectView.hazards` as the mirror point) is sufficient. Wiring the inspect view to render hazards is a later HUD story — do NOT pull UI/scene work into this generation story. [Source: project-context.md presentation rules]
- **Changing `BoardCell`/`BoardState`/`GenerationRequest`/`GenerationResult`/`TacticalSnapshot` or the baseline recipes.** HAZARD is already supported; the phase constants (incl. `PHASE_HAZARDS`) already exist; the recipes already carry the wrinkle requirements. Do NOT add fields to these or change the baseline recipes.
- **Any player-selectable difficulty tier.** `difficulty_band` stays the internal v0 `standard` band; wrinkle count/kinds derive from the recipe + `level` seed, never a player-facing difficulty selector. HARD non-goal. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- Cloud saves, accounts, multiplayer, leaderboards, telemetry, Godot .NET/C#, React/Vite production dependencies, or new test frameworks / new engine libraries.

### Current Repository Baseline (READ THIS FIRST — extend the two existing layout generators + the 3.1 contracts + the board model; do NOT fork them)

The most important constraint: **wrinkle placement is a deterministic, `level`-stream-only EXTENSION of the existing fixed draw order in BOTH `SmallLevelLayoutGenerator` and `MediumLevelLayoutGenerator`. It realizes wrinkles as EXISTING terrain (`WALL` structure + `HAZARD`), keeps the reserved corridor/entrance/exit clear so AC2 reachability holds, records placed kinds in diagnostics for AC1, and proves the hazard is loadable domain data (AC3) through the strict board/`TacticalSnapshot` path. The terrain grid changes, so both seed-regression fingerprints must be re-pinned.**

- `godot/scripts/generation/level/small_level_layout_generator.gd` — `SmallLevelLayoutGenerator` (Story 3.2). 8x8, fixed entrance `(1,4)` / exit `(6,4)` on the central row (`corridor_row = 4`), border-WALL ring, FIXED DRAW #1 = blocker count over `blocker_budget_min..max` (clamped to candidate count), FIXED DRAW #2.. = shrinking-pool blocker positions. NO `validate_readability` (fairness is structural). `_blocker_candidate_cells(width, height, corridor_row, entrance, exit_cell)` already excludes the corridor row + entrance + exit. `_build_layout(...)` builds the row-major terrain grid (border=WALL, interior=FLOOR except entrance/exit/blockers) and the row-major blocker list. `static fingerprint(layout)` serializes `WxH|eX,Y|xX,Y|<row-major terrain joined by '/'>`. ADD wrinkle placement here (append wrinkle draws after the blocker draws; realize `choke_point`/`blocker_cluster` as additional WALL structure via the SAME candidate discipline). `small_combat_basic` needs `min_tactical_wrinkles = 1` from {`choke_point`, `blocker_cluster`}.
- `godot/scripts/generation/level/medium_level_layout_generator.gd` — `MediumLevelLayoutGenerator` (Story 3.3). 14x12, fixed entrance `(1,6)` / exit `(12,6)` on the central row (`corridor_row = 6`), same blocker draw discipline as Small. HAS `validate_readability(layout)` with three AC2 checks (excessive_blockage via `MAX_INTERIOR_WALL_RATIO = 0.35`; unreachable_exit via 4-neighbour BFS `_flood_reachable` over non-WALL cells; unreadable_first_reveal via bounded flood at Chebyshev radius `FIRST_REVEAL_RADIUS = 4`, min `MIN_FIRST_REVEAL_CELLS = 8`). `build_board_snapshot(layout)` has the `invalid_layout_shape` leading shape guard (incl. per-row width). ADD wrinkle placement here (realize `choke_point`/`flank_route`/`blocker_cluster` WALL structure + `hazard` HAZARD cells), then CONFIRM `validate_readability` still passes for approved seeds (the wrinkle WALL count must stay under the `0.35` interior bound — keep wrinkles modest; the `3..8` blocker budget + a few wrinkle cells stays far below 0.35 of the 120-cell interior). `medium_combat_basic` needs `min_tactical_wrinkles = 2` from {`choke_point`, `flank_route`, `blocker_cluster`, `hazard`} (NOT `reward_behind_danger`).
- `godot/scripts/generation/level/level_generator.gd` — `LevelGenerator.generate(request, recipe_repository) -> GenerationResult`. Dispatches on size class: Small -> `_run_small_layout_phase` (real payload, diagnostics `phase: "layout"`); Medium -> `_run_medium_layout_phase` (generate_layout -> `validate_readability` -> build_board_snapshot, diagnostics `phase: "validation"`). The successful-result diagnostics carry `phase`/`recipe_id`/`size_class`/`blocker_count`. ADD `wrinkles`/`wrinkle_count` to those diagnostics (from the generator's layout output). Keep the request-validation + repository-resolution + `unknown_level_recipe` (`PHASE_RECIPE`) paths and `_seed_text` intact. If you record hazard placement against a phase, `PHASE_HAZARDS := &"hazards"` already exists — but a successful result still reports its single `phase` diagnostic (layout for Small, validation for Medium); do NOT change the success phase unless you have a documented reason.
- `godot/scripts/content/repositories/level_recipe_repository.gd` — `LevelRecipeRepository`. `create_baseline_repository()` registers `small_combat_basic` (Small, `min_tactical_wrinkles=1`, kinds `choke_point`+`blocker_cluster`) and `medium_combat_basic` (Medium, `min_tactical_wrinkles=2`, kinds `choke_point`+`flank_route`+`blocker_cluster`+`hazard`+`reward_behind_danger`). Read the wrinkle requirements via `recipe.tactical_wrinkle_requirements()` / `recipe.min_tactical_wrinkles` / `recipe.allowed_wrinkle_kinds`. Do NOT change the baseline recipes.
- `godot/scripts/content/definitions/level_recipe_definition.gd` — `LevelRecipeDefinition`. Wrinkle kinds are constants (`WRINKLE_HAZARD`/`WRINKLE_CHOKE_POINT`/`WRINKLE_FLANK_ROUTE`/`WRINKLE_BLOCKER_CLUSTER`/`WRINKLE_REWARD_BEHIND_DANGER`/`WRINKLE_DOOR`/`WRINKLE_AFFINITY_PLACEHOLDER`/`WRINKLE_ENEMY_FORMATION`/`WRINKLE_RISKY_SIDE_BRANCH`). `tactical_wrinkle_requirements()` returns `{min_tactical_wrinkles, allowed_wrinkle_kinds}`. `static valid_wrinkle_kinds()` lists all 9. `validate()` requires a combat recipe to have `min_tactical_wrinkles >= 1` and (if `min > 0`) a non-empty allowlist. Do NOT add fields to this definition.
- `godot/scripts/generation/level/generation_request.gd` — `GenerationRequest`. `level_seed()` (NOT `request.seed`); `draw_layout_int(streams, min, max, context)` / `draw_layout_float(streams, context)` are the ONLY sanctioned layout-RNG entry points (route through `RngStreamSet.STREAM_LEVEL`, tag `system: "generation"`). USE THESE for every wrinkle draw. `validate()` allows ONLY size classes `small`/`medium` and difficulty band `standard`.
- `godot/scripts/generation/level/generation_result.gd` — `GenerationResult`. `ok(payload, diagnostics)` / `error(failed_phase, error_code, reason, seed: String, diagnostics)`. `seed` is a String. Named phases include `PHASE_LAYOUT`, `PHASE_BLOCKERS := &"blockers"`, `PHASE_HAZARDS := &"hazards"`, `PHASE_VALIDATION`. `error_code`/`reason` must be lower-snake. `ok`/`error` deep-copy payload/diagnostics — reuse, don't reimplement.
- `godot/scripts/tactical/board/board_cell.gd` — `BoardCell`. `enum Terrain { FLOOR, WALL, HAZARD, ENTRANCE, EXIT }`. CRITICAL: `terrain_blocks_occupancy()` and `blocks_line_of_sight()` are BOTH `true` ONLY for `WALL`. `HAZARD` is walkable + sight-transparent at the board level — it is a hazard, not an obstacle. Use `WALL` for interior structure/blockers/choke shaping; `HAZARD` for hazard cells; `ENTRANCE`/`EXIT`/`FLOOR` as today. Do NOT add a new terrain value; do NOT make HAZARD block movement/LoS.
- `godot/scripts/tactical/board/board_state.gd` — `BoardState`. `to_snapshot()` shape `{width, height, next_sequence_id, cells:[...], entities:[...]}`. `static try_from_snapshot(snapshot) -> ActionResult` is the STRICT validator; `_is_valid_terrain(terrain)` returns true for `FLOOR..EXIT` (so `HAZARD` is valid). A HAZARD cell passes `try_from_snapshot` unchanged. Useful read-only query helpers for reachability/hazard assertions: `in_bounds(cell)`, `get_cell(cell)` (returns a `BoardCell` with `.terrain` and `terrain_blocks_occupancy()`), `cells()`, `entity_count()`.
- `godot/scripts/save/snapshots/tactical_snapshot.gd` — `TacticalSnapshot` (`SCHEMA_VERSION=1`, `CONTENT_VERSION="mvp-0"`). `parse(data)` routes the embedded `board` through `BoardState.try_from_snapshot` and rejects forbidden reference strings / non-serializable values. `from_domain(board_state, streams, ...)` builds a snapshot from a live `BoardState` + streams. Use it (as the existing `_board_rides_strict_tactical_snapshot_path` test does) to prove the hazard-bearing board rides the SAME strict path the resume layer uses. Do NOT persist to `user://` — construct + validate in-memory.
- `godot/scripts/core/state/rng_stream_set.gd` — `RngStreamSet`. `new(root_seed)` configures all named streams deterministically. `rand_int`/`rand_float` return an `ActionResult` with `value`/`stream_name`/`draw_index`/`state_before`/`state_after`. `to_snapshot()` exposes per-stream `draw_index` (the existing `_layout_draws_only_from_level_stream` test reads it to prove only `STREAM_LEVEL` advanced). `required_streams()` lists all named streams. `randi_range` is INCLUSIVE — `draw_layout_int(streams, min, max)` returns a value in `[min..max]`, and `draw_layout_int(streams, 0, pool.size()-1)` indexes the shrinking pool.
- `godot/scripts/core/results/action_result.gd` — `ActionResult` (`succeeded`, `is_error()`, `error_code`, deep-copied `metadata`). Generator methods return `ActionResult.ok([], {...})` / `ActionResult.error(<code>, {...})`.
- `godot/scripts/ui/view_models/tactical_inspect_view.gd` — the inspect view model carries a `hazards: []` slot (currently always empty). This is the documented future mirror point for AC3 ("future presentation can mirror it without owning hazard truth"). Do NOT wire it this story (UI/scene work belongs to a later HUD story) — just document it.
- `godot/tests/unit/test_case.gd` — the custom headless harness base. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, call `assert_true/false/equal`, return `result()`. Do NOT add GUT/GdUnit.
- `godot/tests/unit/generation/test_small_level_layout_generator.gd` and `test_medium_level_layout_generator.gd` — the generator-test templates. The Medium test has rich AC2-rejection helpers (`_open_terrain_grid`, `_set_terrain`, `_layout_from_grid`) and the strict-path tests (`_board_round_trip_matches_acceptance_criteria`, `_payload_survives_real_json_transport`, `_board_rides_strict_tactical_snapshot_path`). These currently assert NO wrinkle placement — EXTEND them to assert wrinkles are now placed/recorded and that approved layouts still pass `validate_readability` with wrinkles present. Mirror their structure for any new wrinkle-focused test.
- `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` and `test_medium_level_layout_seed_regression.gd` — the seed-regression fixtures (`APPROVED_FINGERPRINTS` dict, byte-identical regeneration, `>= 2` distinct, failing-seed-in-message, deliberate-update contract). RE-PIN both `APPROVED_FINGERPRINTS` after the generator change (the terrain grid now carries wrinkle cells).
- `godot/tests/unit/generation/test_level_generator.gd` — the level-generator integration test. `_known_small_recipe_returns_real_layout_payload` (diagnostics `phase: layout`), `_medium_recipe_returns_real_validated_layout_payload` (diagnostics `phase: validation`), plus unknown-recipe / invalid-request / null-repo / repository-boundary cases. If the success diagnostics gain `wrinkles`/`wrinkle_count`, assert them here. Keep all other assertions intact.
- `godot/tools/dump_small_layout_fingerprints.gd` and `dump_medium_layout_fingerprints.gd` — the one-shot dev tools that print the pinned fingerprints (run via `godot --headless --script res://tools/dump_*_layout_fingerprints.gd`). Re-run them to (re)generate the pinned values after the generator change. NOT `test_*.gd`, NOT auto-discovered.
- `godot/tests/unit/core/test_project_structure.gd` — the structural fail-loud gate. It already asserts `res://scripts/generation` exists; any new file lives inside already-required roots, so the gate stays green with NO change. Do NOT add a new top-level root.

### Existing Files To Update Or Preserve

| Path | Current State | This Story Changes | Preserve |
|---|---|---|---|
| `godot/scripts/generation/level/small_level_layout_generator.gd` | Deterministic Small layout; blockers only; NO wrinkles; NO `validate_readability`. | UPDATE — ADD deterministic wrinkle placement (`choke_point`/`blocker_cluster` realized as WALL structure via the corridor-respecting candidate discipline), appended to the fixed draw order; record placed kinds. Optionally add a reachability assertion path. | The existing blocker draw order, the border-ring + reserved-corridor shape, `build_board_snapshot`, the `fingerprint` FORMAT, the method surface. |
| `godot/scripts/generation/level/medium_level_layout_generator.gd` | Deterministic Medium layout + AC2 `validate_readability`; blockers only; NO wrinkles; HAZARD reserved. | UPDATE — ADD deterministic wrinkle placement (`choke_point`/`flank_route`/`blocker_cluster` WALL structure + `hazard` HAZARD cells), appended to the fixed draw order; keep wrinkles off corridor/entrance/exit; confirm `validate_readability` still passes (interior WALL ratio stays under 0.35). | The blocker draw order, `validate_readability` + its bounds, the `invalid_layout_shape` guard, `build_board_snapshot`, the `fingerprint` FORMAT, the method surface. |
| `godot/scripts/generation/level/level_generator.gd` | Small -> layout-phase payload; Medium -> validation-phase payload; diagnostics carry `phase`/`recipe_id`/`size_class`/`blocker_count`. | UPDATE — ADD `wrinkles`/`wrinkle_count` to the success diagnostics (from the generator output). | The size dispatch, both phase methods, request-validation/repository-resolution/`unknown_level_recipe` paths, `_seed_text`, the existing diagnostics keys, the success `phase` value. |
| `godot/scripts/generation/level/generation_request.gd` | `draw_layout_int`/`draw_layout_float` (STREAM_LEVEL only), `level_seed()`, `validate()`. | NO change — REUSE the layout-RNG helpers for wrinkle draws. | The `STREAM_LEVEL`-only contract; `level_seed()`. |
| `godot/scripts/generation/level/generation_result.gd` | `ok`/`error`, phases incl. `PHASE_HAZARDS`, String `seed`. | NO change to the class — extend the diagnostics dict only. | `ok`/`error` factories, phase constants, String `seed`, code normalization. |
| `godot/scripts/content/repositories/level_recipe_repository.gd` | Baseline `small_combat_basic` + `medium_combat_basic` with wrinkle requirements. | NO change — READ the wrinkle requirements; generate from the baseline recipes. | Baseline recipes + fail-closed factory + `ContentRepository` boundary. |
| `godot/scripts/content/definitions/level_recipe_definition.gd` | Recipe fields + wrinkle kinds + `tactical_wrinkle_requirements()` + `validate()`. | NO change — READ `min_tactical_wrinkles`/`allowed_wrinkle_kinds`/the `WRINKLE_*` constants; do not add fields. | All definition behavior + tests. |
| `godot/scripts/tactical/board/board_cell.gd` / `board_state.gd` | `Terrain` enum incl. HAZARD; HAZARD walkable + sight-transparent; strict `try_from_snapshot` accepts HAZARD. | NO change — EMIT HAZARD into the generated terrain; validate via `try_from_snapshot`. | `Terrain` enum values; the WALL-only blocking predicate; `to_snapshot`/`try_from_snapshot` contract. |
| `godot/scripts/save/snapshots/tactical_snapshot.gd` | Strict `parse`/`from_domain`, embedded-board validation. | NO change — route the hazard-bearing board through it (in tests). | Schema/parse/embedded-board contract. |
| `godot/scripts/ui/view_models/tactical_inspect_view.gd` | `hazards: []` slot always empty. | NO change — document as the future mirror point for AC3. | The view-model contract (do not pull UI work into this story). |
| `godot/tests/unit/generation/test_small_level_layout_generator.gd` / `test_medium_level_layout_generator.gd` | Assert blockers + (Medium) AC2; assert NO wrinkles. | UPDATE — assert wrinkles placed/recorded; (Medium) approved layouts still pass `validate_readability` with wrinkles. | All existing determinism/divergence/stream-isolation/budget/fairness/board-round-trip/strict-path assertions. |
| `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` | Pinned `APPROVED_FINGERPRINTS` (no wrinkles). | UPDATE — RE-PIN both dicts via the dump tools (terrain grid now carries wrinkle cells); log the deliberate change. | The byte-identical/divergence/failing-seed assertions + deliberate-update contract. |
| `godot/tests/unit/generation/test_level_generator.gd` | Asserts Small `phase: layout` + Medium `phase: validation` payloads. | UPDATE (if diagnostics gain `wrinkles`/`wrinkle_count`) — assert them. | Small/Medium payload, unknown-recipe, invalid-request, null-repo, repository-boundary assertions. |
| `godot/tools/dump_small_layout_fingerprints.gd` / `dump_medium_layout_fingerprints.gd` | Print the pinned fingerprints. | RE-RUN to regenerate the pins (optional small tweak to also print wrinkle diagnostics). | Their non-`test_*.gd` dev-tool nature. |
| `godot/tests/unit/core/test_project_structure.gd` | Asserts required roots exist. | NO change expected (`scripts/generation`/`tests/unit` already covered). | All required-root assertions. |

### Recommended New Files

Use these names unless implementation discovers a clearer local pattern:

- `godot/tests/unit/generation/test_tactical_wrinkle_placement.gd` — the focused AC1/AC2/AC3 wrinkle test: at least `min_tactical_wrinkles` wrinkles placed for each combat recipe; placed kinds recorded in diagnostics and a subset of the recipe allowlist; HAZARD cells are walkable (`terrain_blocks_occupancy() == false`) and reachable; entrance->exit still reachable over non-WALL cells with wrinkles present; the mandatory path is FLOOR/HAZARD/ENTRANCE/EXIT only (no class/item gate); `level`-stream-only draws + stream isolation; determinism (same seed -> identical incl. wrinkles); hazard-bearing board rides the strict `BoardState.try_from_snapshot` + `TacticalSnapshot.from_domain`/`parse` path + real JSON round-trip and the restored hazard cell still reads HAZARD.
- (OPTIONAL) `godot/scripts/generation/level/tactical_wrinkle_placer.gd` — a small shared `RefCounted` helper if extracting the wrinkle selection/placement keeps both generators DRY (deterministic kind selection from the allowlist filtered to the realizable subset, WALL-structure/HAZARD realization via the corridor-respecting candidate discipline, placed-kind recording). Only extract if it genuinely de-duplicates; do NOT build a general framework. If you add it, it lives under the already-required `res://scripts/generation/level/` root (no structure-gate change).

Avoid: a new RNG draw path or stream; a new result type; a second/lenient board parser; a new terrain enum; a SECOND fingerprint format (reuse the shared one); a doors/affinity/side-branch subsystem; placing enemies/rewards or realizing `enemy_formation`/`reward_behind_danger`; hazard DAMAGE/effect rules; making HAZARD block movement/LoS; a GENERAL reachability/validation framework + retry engine (3.6); a manual-seed/batch CLI (3.7); touching live tactical truth / run save / settings / UI presenters; a difficulty knob; a new test framework; adding fields to the recipe definition or the baseline recipes.

### Wrinkle Determinism Contract (AC1 — same seed reproduces wrinkles; the `level` stream is the only randomness source)

The single most important constraint, identical in spirit to 3.2/3.3: **wrinkle placement is a pure deterministic function of (root seed, recipe, starting `level`-stream state). Every wrinkle-affecting choice flows through `GenerationRequest.draw_layout_int`/`draw_layout_float` (→ `RngStreamSet.STREAM_LEVEL`), the draw ORDER is fixed (wrinkle draws APPENDED after the existing blocker draws), and no other randomness source touches the layout.**

- Build the streams as `RngStreamSet.new(request.level_seed())` (as the phase methods already do). Draw blocker count, blocker positions, THEN the wrinkle selection + wrinkle placement, in a FIXED, documented order. Appending wrinkle draws after blocker draws preserves the existing blocker behaviour and keeps the new fingerprints reproducible. [Source: Story 3.2/3.3 fixed-draw-order; project-context.md Determinism rules]
- Different seeds must still produce meaningfully different layouts (now varying in wrinkle kinds/positions too) — keep the `>= 2 distinct` divergence assertion green.
- Stream isolation (carried from Story 1.4 / 3.1 / 3.2 / 3.3): making `cosmetic`/`combat` draws before generation must NOT change the layout incl. wrinkles. Keep the `_cosmetic_and_combat_noise_do_not_perturb_layout` and `_layout_draws_only_from_level_stream` assertions green (extend them to cover the wrinkle draws — all wrinkle draws must advance ONLY `STREAM_LEVEL`).
- No global fallback: an unknown stream or invalid range surfaces a structured `ActionResult` error from `RngStreamSet`; the generator must never silently fall back to `randi()`/`randf()`. The `GenerationRequest` helpers enforce this — just don't bypass them. [Source: project-context.md "Do not use one global RNG for gameplay"]

### AC2 Reachability-With-Wrinkles Contract (mandatory progress remains possible; no required class/item gate)

- AC2 verbatim: "Given blockers or hazards are placed, when pathing validation runs, then mandatory entrance-to-exit progress remains possible, and no required progress depends on a specific class, weapon, or item." [Source: epics.md Story 3.4 AC2]
- **Mandatory progress remains possible:** wrinkle WALL cells route through the SAME corridor-respecting candidate discipline as blockers (`_blocker_candidate_cells` excludes the reserved central corridor row + entrance + exit), so a floor path between entrance and exit always survives. HAZARD cells are walkable so they never threaten reachability — but keep the entrance/exit/reserved-corridor free of hazard too, so the mandatory route is a SAFE walkable path (an unavoidable hazard on the only path would be the kind of "unfair start" Epic 3 exists to prevent). Re-verify reachability after placement: Medium via the existing `validate_readability` BFS (confirm it runs post-wrinkle and treats HAZARD as walkable); Small via the construction guarantee + a reachability assertion in its test.
- **No required class/weapon/item gate:** v0 introduces NO doors/keys/locks (deferred), so the only progress requirement is walkable terrain. Assert the entrance->exit path exists over `FLOOR`/`HAZARD`/`ENTRANCE`/`EXIT` only. This story adds no gating feature, so the no-gate property holds by construction — but assert it so a future story that adds doors cannot silently break it. [Source: epics.md Story 3.4 AC2; FR36]
- **SCOPE GUARD:** this is the existing reachability guarantee re-verified with wrinkles present, NOT the full 3.6 validator. Do NOT add no-soft-lock, legal-enemy-placement, or reachable-reward checks (no entities/rewards until 3.5), and do NOT build a bounded-retry engine. Keep the Medium `validate_readability` scoped to its three AC2 checks; 3.6 will subsume it.

### Hazard-As-Domain-Data Contract (AC3 — loadable, serializable, mirror-able; the board owns hazard truth)

- AC3 verbatim: "Given a hazard is included in a generated snapshot, when the tactical board loads the snapshot, then the hazard is represented as domain data, and future presentation can mirror it without owning hazard truth." [Source: epics.md Story 3.4 AC3]
- **Domain data, not a scene node:** the hazard is a `BoardCell.Terrain.HAZARD` cell in the row-major terrain grid; the converted board snapshot passes the STRICT `BoardState.try_from_snapshot` (validate-then-reject, never coerce — the Story 1.3 precedent) with the hazard present, and survives a real `JSON.stringify`/`parse_string` round-trip + the `TacticalSnapshot.from_domain`/`parse` path. `entities` stays EMPTY. [Source: board_state.gd `try_from_snapshot`/`_is_valid_terrain`; project-context.md Save/Serialization rules]
- **The board owns hazard truth; presentation mirrors it:** the hazard is queryable as board terrain (`board.get_cell(cell).terrain == BoardCell.Terrain.HAZARD`). The future presentation mirror point is the existing `TacticalInspectView.hazards` slot — document this, but do NOT wire it this story (UI/scene work is a later HUD story). Presentation reads board state through view models and never owns tactical truth. [Source: project-context.md presentation/view-model rules; tactical_inspect_view.gd `hazards: []`]
- **Hazard DANGER is deferred:** placing the hazard terrain is this story; standing-on-hazard damage / telegraphed danger / hazard rules are a rules-kernel concern owned by a later story. Do NOT add hazard effects to commands/board/events here. [Source: project-context.md Rules kernel rules]

### Generated-Layout -> Board-Snapshot Conversion Contract (validate-then-reject through the strict path; scene-free)

Identical to 3.2/3.3 (reuse the existing `build_board_snapshot` approach, now with HAZARD cells possible):

- The generated layout (incl. wrinkle WALL + HAZARD cells) converts into a board snapshot in the EXACT `BoardState.to_snapshot()` shape and MUST pass `BoardState.try_from_snapshot()` (integral `width`/`height` > 0; exactly `width*height` valid `BoardCell` dicts; valid terrain in `[FLOOR..EXIT]` — HAZARD included; integral `next_sequence_id > 0`; `entities` an Array). `entities` is EMPTY this story. [Source: board_state.gd `try_from_snapshot`]
- **Validate, do NOT coerce:** route the board through the strict validator (and `TacticalSnapshot.from_domain`/`parse` in tests) so a malformed cell is REJECTED as a generator bug, never silently fixed. The Medium `build_board_snapshot` already has the `invalid_layout_shape` leading + per-row shape guard; the Small one does NOT (see deferred-work below). [Source: deferred-work.md "code review of 1-3..."; project-context.md "Route an embedded tactical payload through the STRICT TacticalSnapshot.parse"]
- No scene nodes: the board and payload are pure serializable domain data (`assert_false(board is Node, ...)`); payload strings must be JSON-clean (`TacticalSnapshot._copy_serializable_value` rejects `res://`/`.tscn`/"presentation"). [Source: NFR14; project-context.md serialization rules]

### Seed Regression Contract (AC1/AC3 — approved fixtures stable after a deliberate re-pin, failing seed reported)

- Placing wrinkles changes the terrain grid, so the `fingerprint(layout)` for every approved seed WILL drift for BOTH generators. Re-pin `APPROVED_FINGERPRINTS` in the SAME change via `dump_small_layout_fingerprints.gd` + `dump_medium_layout_fingerprints.gd`. The deliberate-update contract is honored by re-pinning in this PR + logging it in the Change Log — never silently. [Source: project-context.md Testing rules; Story 3.3 deliberate-update contract]
- The fingerprint FORMAT does not change (it serializes the full terrain grid incl. HAZARD `2` via `str(int(row[x]))`); only the pinned VALUES change. Keep the failing-seed-in-assert-message behaviour (AC3 verbatim) and the `>= 2 distinct` divergence assertion.

### Difficulty Non-Goal Note (carry it — wrinkle count/kinds may not become a difficulty knob)

`difficulty_band` stays the internal v0 `standard` band. The number of wrinkles, their kinds, and their placement derive from the RECIPE (`min_tactical_wrinkles`, `allowed_wrinkle_kinds`, `blocker_budget`) and the `level` seed — NOT from any player-facing difficulty selector. Sealsworn has a HARD non-goal against player-selectable difficulty ladders. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"; Story 3.1/3.2/3.3 Difficulty Non-Goal Note]

### State / No-Mutation Contract

Wrinkle placement and board-snapshot construction are pure data operations this story. They must never mutate live tactical truth, the run save, settings, or progression, and must not draw RNG except through the named `level` stream via the `GenerationRequest` helpers. The reachability re-verification is a pure READ query over the generated terrain (no RNG, no mutation).

Never during this story's work:

- Execute move/attack commands, the command-bridge execute path, or any live tactical mutation. The generated board is data; do NOT apply events to it as live state, and do NOT add hazard-damage events.
- Read, write, or mutate `user://run_autosave.json` or `user://settings.json` (generation has no save WRITE work). If a test builds a `TacticalSnapshot`, it does so in-memory only.
- Draw gameplay RNG from any stream other than `level` for wrinkle/blocker decisions; never call global `randi()`/`randf()` or `RandomNumberGenerator.new()` directly.
- Place enemies/rewards, realize `enemy_formation`/`reward_behind_danger`/`door`/`affinity_placeholder`/`risky_side_branch`, or add a doors/affinity/side-branch subsystem (Stories 3.5 / Epic 7 / later).
- Make `HAZARD` block movement or line of sight, or change `BoardCell`/`BoardState`.
- Coerce malformed board cells at conversion time — reject them through `try_from_snapshot`/`TacticalSnapshot.parse`.
- Alter rewards, gold, Oath Shards, corruption, affinities, passives, route, or meta progression.

### Previous Story Intelligence (Story 3.3 + Story 3.2 + Story 3.1 + Epic 3 retro + deferred-work — folded in as direct constraints, not a pointer)

Stories 3.1-3.3 are all `done` (each code-review Approve, full headless suite green). Their Completion Notes, Review Findings, the Epic 3 retro-notes, and the deferred-work ledger carry hard constraints into THIS story:

- **Both layout generators are SIBLINGS — keep them so (Story 3.2/3.3 Completion Notes).** Same fixed draw order (blocker count, then shrinking-pool blocker positions; APPEND wrinkle draws after), same `generate_layout`/`build_board_snapshot`/`fingerprint` surface, same border-ring + reserved-corridor + fixed-entrance/exit shape, same payload shape, same "non-`level` streams stay at `draw_index == 0`" stream-isolation proof. Add wrinkle placement to BOTH as a parallel extension, not a redesign of one.
- **HAZARD was reserved for THIS story (Story 3.2/3.3 headers + retro).** Both generator headers and the Small/Medium scope explicitly say "NO HAZARD terrain ... (3.4)". Story 3.4 is the one that emits `BoardCell.Terrain.HAZARD`. It is already board-valid and walkable — no board change.
- **`wall_density` is intentionally UNUSED for blocker counts in BOTH generators (Story 3.3.4 decision, closed as a deferred-work decision).** The `blocker_budget_min..max` band is authoritative; the AC2 excessive-blockage bound (`0.35` interior WALL ratio, Medium only) guards readability. Wrinkle WALL cells ADD to the interior WALL count — so keep wrinkle WALL placement modest enough that Medium's interior WALL ratio stays under `0.35` (the `3..8` blocker budget + a few wrinkle cells over a 120-cell interior stays far below). Do NOT start deriving counts from `wall_density`; if you ever wanted to, you would have to widen the band and re-pin both fingerprints (this story already re-pins for the wrinkle terrain — do NOT also repurpose `wall_density`). [Source: deferred-work.md "code review of 3-3 ... wall_density"; epic-3.md Story 3-3 notes]
- **Medium `validate_readability` is reject-by-construction; Story 3.6 will subsume it (Story 3.3.3 decision).** Keep it scoped to its three AC2 checks; re-confirm approved layouts still PASS it with wrinkles present (the wrinkle WALL count is the thing to watch against the `0.35` bound). Do NOT expand it into the 3.6 framework. [Source: epic-3.md Story 3-3 notes]
- **Small `build_board_snapshot` lacks the `invalid_layout_shape` shape guard (open deferred Low from 3-2, NOT closed by 3-3).** 3.3 added the guard to the Medium `build_board_snapshot` only; Small's remains a deferred Low owned by its originating 3-2 review. This story does NOT have to close it, BUT if your wrinkle work touches the Small `build_board_snapshot` or `_build_layout` shape handling, it is cheap and in-area to add the symmetric leading shape guard (width/height vs terrain-grid dims + per-row width) returning a structured `invalid_layout_shape`, and a short-row candidate test — if you do, note it in Completion Notes and check off the deferred-work item; if not, leave it for its owning task. [Source: deferred-work.md "code review of 3-2 ... build_board_snapshot() indexes the layout grid without a shape/bounds guard"]
- **Medium `validate_readability` per-row width guard is an open deferred Low from 3-3.** `validate_readability` guards `terrain_grid.size() != height` but not each inner row's width (unlike `build_board_snapshot`). If your wrinkle work routes hand-built candidates through `validate_readability`, consider the symmetric per-row guard (or a shared shape-validation helper across both methods); not required, but in-area. [Source: deferred-work.md "code review of 3-3 ... validate_readability() guards the layout shape with terrain_grid.size() != height only"]
- **`GenerationRequest` exposes `level_seed()`, not `request.seed`; `GenerationResult.seed` is a String (Story 3-1 retro).** Verify generated-level seed stability through the real `TacticalSnapshot` + int64-string transport, never native dicts — keep the hazard-bearing board's real-JSON-round-trip test. [Source: epic-3.md Story 3-1 notes; epic-2 retro Action Item #4]
- **The `{layout_pending: true}` Medium payload is gone (Story 3-2/3-3 retro).** Both Small and Medium now return real `{board, entrance, exit, blockers, size_class, recipe_id, level_seed}` payloads. Read `payload.board` / `payload.blockers`, never `payload.placeholder`/`payload.layout_pending`.
- **`difficulty_band` is internal `standard`; never a layout/wrinkle input (Story 3.1/3.2/3.3 + project-context).** Hard non-goal.
- **No new content pipeline (Story 3-1 retro).** No `.tres`/JSON content exists under `godot/data/` yet — definitions are code constants. Do NOT introduce the JSON-source pipeline for wrinkles; place them in code via the generator, drawn from the recipe's typed fields.

Deferred-work ledger items NOT relevant to this story (do not reopen): the 3-1 `LevelRecipeDefinition.validate()` negative-test gaps; the 3-2 blocker-count clamp-to-candidate-count unexercised branch and the fixed-even-footprint reachability coupling (Story 3.6 owns the formal reachability validator + jittered-size handling); all Epic 1/2 save/settings/view-model/accessibility deferrals. The two in-area items (Small shape guard; Medium per-row guard) are noted above as optional in-area cleanups, not requirements.

### Project Context Rules

- Read and follow root `project-context.md` before implementation; read `_bmad-output/game-architecture.md` before architecture-sensitive changes (Procedural Generation, the generation phase list, the generator-validation checklist, the board/tactical-snapshot model).
- Determinism: wrinkle/blocker randomness uses the named `level` RNG stream ONLY (via the `GenerationRequest` helpers); never global `randi()`/`randf()`, never another stream; cosmetic randomness never affects outcomes; draw order is fixed (wrinkle draws appended after blocker draws); reachability re-verification draws no RNG. [project-context.md Determinism rules]
- Procedural generation runs in explicit phases (this story = wrinkle/hazard placement within the `layout`/`blockers`/`hazards` phase region for both sizes) and selects ONLY from approved static definitions (the recipe allowlist); the generator never reads arbitrary files in the hot path. [project-context.md Rules/AI/Generation + Static Content rules]
- Save/serialization (directly applicable): the generated level (incl. HAZARD cells) converts through the STRICT `BoardState.try_from_snapshot` / `TacticalSnapshot.parse` path (validate-then-reject, never coerce); tested through a real `JSON.stringify`/`parse_string` round-trip; save truth is versioned domain snapshots, never scene nodes. [project-context.md Save/Serialization rules; Story 1.3 precedent]
- Structured results, not exceptions: reuse `ActionResult` (generator methods) and `GenerationResult` (the phase result); lower-snake error/reason codes; compact diagnostics (kinds/counts/coords, never a full grid). [project-context.md determinism + generation rules; Story 3.1 GenerationResult]
- `scripts/generation/` is `RefCounted` services + DTOs over the domain board model, independent of scene nodes; headless tests run without rendering/audio/UI; generated boards are scene-free; hazard truth lives in the board terrain, not presentation. [project-context.md Code Organization + Testing + presentation rules; NFR14]
- DIFFICULTY IS A HARD NON-GOAL: `difficulty_band` is internal; wrinkle count/kinds derive from the recipe + `level` seed, never a player-facing difficulty tier. [project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- No cloud/accounts/multiplayer/telemetry/.NET/new-frameworks/prototype dependencies.
- Naming: `snake_case` files/folders, `PascalCase` classes, `UPPER_SNAKE_CASE` constants, `*Result` conventions, `test_*.gd` tests, stable lower-snake codes for `GenerationResult`/`ActionResult` errors. [project-context.md Naming rules]
- Environment: the bare `godot` is NOT on the Bash/`where` PATH; it resolves only as `C:\Users\Rasmus\bin\godot.cmd` via PowerShell. Run the headless test command through PowerShell (`powershell.exe -NoProfile -Command "..."`), not the Bash tool's PATH lookup. [project-context.md Technology Stack]

### References

- [Source: epics.md#Epic 3: Procedural Level Generation v0 / Story 3.4] — user story + AC1-AC3 (verbatim basis for this story).
- [Source: epics.md#Epic 3 Story 3.5] — enemy/reward placement + the standalone reward-table decision (owns `enemy_formation`/`reward_behind_danger` wrinkle kinds and entity placement; this story keeps `entities: []`).
- [Source: epics.md#Epic 3 Story 3.6] — the FULL generator validation pass + bounded retry (subsumes Medium's focused `validate_readability`; this story only re-verifies the existing reachability guarantee with wrinkles present).
- [Source: epics.md#Epic 3 Story 3.7] — manual-seed loader + batch fingerprint CLI (this story re-pins a small set in unit tests + the dump tools only).
- [Source: epics.md Additional Requirements] — named RNG streams (`level`), explicit generation phases (incl. hazards/blockers), generator validation failure reporting (seed/phase/reason/compact diagnostics), runtime definitions incl. `LevelRecipe`.
- [Source: epics.md FR35] — procedural combat levels must include a clear entrance, clear exit, enough blockers/cover for LoS play, AND at least one tactical wrinkle (the core of AC1).
- [Source: epics.md FR36] — generator validation must check entrance-to-exit pathing AND absence of required class/item gates (the core of AC2).
- [Source: game-architecture.md#Procedural Generation] — the explicit phase list (route -> recipe -> layout -> pathing -> blockers -> hazards -> enemies -> rewards -> affinity -> validation -> final snapshot); wrinkle/hazard placement sits in the layout/blockers/hazards region.
- [Source: gdd.md#Procedural Levels] — tactical wrinkle requirement + the wrinkle-kind menu (hazard/door/choke point/flank route/blocker cluster/affinity placeholder/enemy formation/reward behind danger/risky side branch).
- [Source: project-context.md] — determinism (`level` stream), generation phases + approved-static-content rule, repository boundary, structured results, save/serialization (strict parse + int64-string + real-JSON round-trip), presentation/view-model rules (board owns hazard truth), difficulty non-goal, naming, code organization, testing rules, environment (`godot` via PowerShell only).
- [Source: epic-3.md (Story 3-3 retro notes)] — Medium `validate_readability` is reject-by-construction + reachability via 4-neighbour BFS over non-WALL cells; `wall_density` is decided-unused (budget band authoritative); HAZARD reserved for 3.4; `LevelGenerator.generate()` Medium payload is now a real layout.
- [Source: epic-3.md (Story 3-2 retro notes)] — Small reachability is structural (reserved corridor on fixed even 8x8); entrance/exit NOT seed-randomized; the real-payload shape (`{board, entrance, exit, blockers, size_class, recipe_id, level_seed}`); HAZARD reserved for 3.4.
- [Source: epic-3.md (Story 3-1 retro notes)] — `request.level_seed()` (not `request.seed`); `GenerationResult.seed` is a String; verify seed stability through the real `TacticalSnapshot` transport; no JSON content pipeline yet (definitions are code constants).
- [Source: deferred-work.md (code review of 3-2)] — Small `build_board_snapshot` missing `invalid_layout_shape` shape guard (open Low, in-area optional cleanup); the blocker-count clamp branch unexercised (3.6 territory, not this story).
- [Source: deferred-work.md (code review of 3-3)] — Medium `validate_readability` missing per-row width guard (open Low, in-area optional cleanup); `wall_density` documented-unused (do not repurpose it for wrinkle counts).
- [Source: deferred-work.md (Story 1.3 board-snapshot items)] — validate-then-reject malformed snapshot fields rather than coerce (precedent carried here for the hazard-bearing board).
- Reference code to extend/reuse: `godot/scripts/generation/level/{small_level_layout_generator,medium_level_layout_generator,level_generator,generation_request,generation_result}.gd`, `godot/scripts/content/{definitions/level_recipe_definition,repositories/level_recipe_repository}.gd`, `godot/scripts/tactical/board/{board_state,board_cell}.gd`, `godot/scripts/save/snapshots/tactical_snapshot.gd`, `godot/scripts/core/state/rng_stream_set.gd`, `godot/scripts/core/results/action_result.gd`, `godot/scripts/ui/view_models/tactical_inspect_view.gd`; reference tests/tools: `godot/tests/unit/generation/{test_small_level_layout_generator,test_medium_level_layout_generator,test_small_level_layout_seed_regression,test_medium_level_layout_seed_regression,test_level_generator}.gd`, `godot/tests/unit/core/test_project_structure.gd`, `godot/tools/{dump_small_layout_fingerprints,dump_medium_layout_fingerprints}.gd`.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

### Change Log

| Date | Change |
|---|---|
| 2026-06-16 | Created story context file for Story 3.4 (Tactical Wrinkles, Blockers, and Hazards). Status -> ready-for-dev. |
