---
created: 2026-06-29
source_story_key: 6-7-loot-and-passive-build-smoke-run
epic: 6
story: 7
baseline_commit: bb194de2ef0584ea999704faec5e3edff8abf70a
---

# Story 6.7: Loot and Passive Build Smoke Run

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want a short run to include loot, inventory, consumables, and passive choices,
so that the MVP can prove early build identity before deeper balance work.

## Acceptance Criteria

Source: `_bmad-output/planning-artifacts/epics.md` -> Epic 6 -> Story 6.7 (verbatim AC text, with the implementation-grounded detail this final smoke-run story adds). FRs covered: **FR46** (the MVP passive pool — the build-defining passive an early-mid offer surfaces), **FR47/FR48/FR49/FR50** (the Consume/Destroy passive-reward moment, made felt end-to-end), **FR51/FR52** (the small inventory + loot categories the run threads), **FR53** (consumables are semi-rare and worth using — the consumable-use surface + the rarity-as-data scarcity claim), and **FR1/FR70** (every milestone preserves a playable build that launches and validates a small test loop). The 2026-06-04 readiness backlog (epics.md line 391/394) maps **GDD FR48** ("early-mid build-defining passive") and **GDD FR73** ("semi-rare, worth-using consumables") to **Story 6.7 + Story 10.4** — so 6.7 owns the FIRST observable evidence of those two, and the deeper balance pass is Epic 10.

This is the **EPIC-6 CLOSING smoke-run capstone**: structurally the Epic-6 analogue of Story 5.5 (the class-start playable smoke slice). It ties the 6.1 loot/reward definitions -> 6.2 inventory/equipment model + `PickupItemCommand` -> 6.3 reward-offer flow (`RunOrchestrator.generate_reward_offer` / `generate_passive_reward_offer` + `RewardOffer` + `ResolveRewardCommand`) -> 6.4 passive-reward modal data contract -> 6.5 `ConsumePassiveCommand` -> 6.6 `DestroyPassiveCommand` together into ONE observable, deterministic "a short run generates a passive/loot offer at the early-mid band, the offer can be consumed / destroyed / picked up / skipped, a consumable can be USED, and the whole thing reproduces from the same seed" surface, PLUS the reward-pacing smoke survey + the consumable-value + Consume-vs-Destroy tracked-note deliverables.

It is mostly an **ASSEMBLE + SURFACE + PROVE** story (the 5.5 posture) — it builds NO new content engine, NO per-effect operation engine, NO scene — with ONE genuinely new mechanic: a **`UseConsumableCommand`** (AC3 requires "the consumable command executes -> the item effect resolves through domain events -> the consumable is removed or reduced according to inventory rules" — that command does NOT exist yet). The consumable-use is OUTCOME-RECORD-ONLY (v0 has NO live HP / wallet — the run carries `starting_kit.baseline_hp`, NOT a mutable current-HP field), exactly the Story-6.3 gold / Story-6.6 Destroy outcome-record-only posture.

1. **A shell run clears eligible nodes; when rewards are generated at least one passive or loot offer can appear by an early-mid run configuration, and the offer can be consumed, destroyed, picked up, or skipped according to its type.**
   **Given** a shell run clears eligible nodes (a `RunOrchestrator.start(seed, false, class_id)` run drives forward through its 8-12-node route, clearing combat/elite/placeholder nodes — the 6.3-ratified boundary: the orchestrator does NOT auto-generate a reward offer on each combat node; reward GENERATE is an EXPLICIT `generate_reward_offer`/`generate_passive_reward_offer` call a caller fires at a node-completion boundary)
   **When** rewards are generated (the smoke harness calls `RunOrchestrator.generate_reward_offer(&"standard_combat_reward")` and `generate_passive_reward_offer(&"passive_reward_choice")` at the relevant node-completion boundaries, drawing through the run-level `RngStreamSet` `rewards`/`loot` stream)
   **Then** at least one passive OR loot offer can appear by early-mid run configuration (the offer is stored on `RunState.pending_reward_offer` as `pending`; the loot offer draws a `weapon`/`armor`/`jewelry`/`support`/`consumable`/`pickup`/`gold` entry, the passive offer draws THREE DISTINCT `passive` choices via the AC4 `passive_reward_choice` table)
   **And** the offer can be consumed, destroyed, picked up, or skipped according to its type (a `passive` offer routes to `ConsumePassiveCommand` (adopt) OR `DestroyPassiveCommand` (roll the 70/20/10 outcome) OR `ResolveRewardCommand` (the 6.3 generic outcome-only resolve = the "skip"/decline fallback); a backpack-item offer (`weapon`/`armor`/`jewelry`/`support`/`consumable`/`pickup`) routes to `PickupItemCommand` (record the item) OR is skipped via `ResolveRewardCommand`; a `gold` offer resolves outcome-only via `ResolveRewardCommand`. The smoke harness drives at least one of EACH disposition end-to-end against the SAME run, proving every offer type has a real resolving command behind it. "Skip" = the generic `ResolveRewardCommand` outcome-only resolve, which flips the offer to `resolved` and applies nothing — the explicit decline path).

2. **By the early-mid run band, at least one build-defining passive offer is available by the configured node/depth target, and a pacing failure points to reward-table weights / passive-pool coverage / node pacing — not a hidden hand-authored fix.**
   **Given** the route reaches the early-mid run band (the early band is the route's first third by depth — `RouteNode.depth` ratio `<= 0.34` of the last non-boss depth, the `route_generator.gd::_type_weights_for_band` boundary; the constant-8-tier MVP route puts depths 0-2 in the early band, the mid band around depths 3-4)
   **When** reward pacing checks run across approved smoke seeds (a deterministic headless SURVEY across a fixed seed sample, the `test_run_pacing_survey.gd` AC5 precedent — measure + record the structural pacing surface, do NOT "fix" pacing by hand)
   **Then** at least one build-defining passive offer is available by the configured node/depth target (the `passive_reward_choice` table — referenced by `RewardTableRepository` — yields a 3-choice passive offer that can be generated by the early-mid band; "build-defining" = the offer surfaces a passive the player can Consume to adopt into the run's `RulesResolver`, i.e. the six Story-5.4 baseline passives, each carrying its 6.4 modal `consume_text`/`exact_mechanical_effects` — the FR48 early-mid build-defining-passive readiness item; the survey asserts the table is reachable + yields a genuine multi-choice passive offer deterministically)
   **And** a pacing failure points to reward-table weights / passive-pool coverage / node pacing rather than a hidden hand-authored fix (the survey's assertions are structural — the table exists in the validated repository, its entries reference REAL baseline passive ids, the 3-choice draw yields 3 distinct passives — so a failure names the table/weights/pool, NOT a fixture poke. Like 5.5/4.6, 6.7 does NOT change the route generator, the reward-table weights, or the passive pool to "hit" pacing; it MEASURES + records the structural pacing surface and flags any gap as an Epic-10-tuning tracked note. **The constant-8-tier route depth is a KNOWN tracked pacing characteristic (the 4.2/4.6 follow-up) — 6.7 re-records it, does NOT fix it.**).

3. **The player uses a consumable; the consumable command executes -> the item effect resolves through domain events -> the consumable is removed or reduced according to inventory rules.**
   **Given** the player uses a consumable (a consumable id sits in the run's `InventoryState` backpack — recorded by a prior `PickupItemCommand` of a `consumable`-category item, e.g. `minor_healing_draught`)
   **When** the consumable command executes (a NEW `UseConsumableCommand` — the `PickupItemCommand` / `ConsumePassiveCommand` run-command idiom VERBATIM: extends `game_command.gd`, takes the live `RunState` directly, reject `sequence_id <= 0` FIRST, validate-then-mutate, byte-identical no-mutation + ZERO events + ZERO RNG on every reject)
   **Then** the item effect resolves through domain events (a NEW `item_consumed` SYSTEM `DomainEvent` appended at the enum end after `PASSIVE_DESTROYED`, NEVER renumbered — carrying the lower_snake `item_id`, the resolved consumable's `effect`/`outcome_effect` marker + `explanation` (read from the `ConsumableDefinition`), and the post-use `backpack_size_after`/`slot_index` provenance, mirroring `item_gained`. **SCOPE — the consumable-use is OUTCOME-RECORD-ONLY (the EXACT parallel of 6.3 gold + 6.6 Destroy): v0 has NO live HP / wallet domain field — the run carries `starting_kit.baseline_hp`, NOT a mutable current-HP — so a "heal" consumable RECORDS its effect deterministically via the `item_consumed` event; it does NOT mutate an HP/wallet/curse field because none exists. The live heal/cure mutation is Epic 7's risk-economy state, wired off the recorded effect.**)
   **And** the consumable is removed or reduced according to inventory rules (the used consumable's backpack slot is REMOVED — v0 has NO stacking, so each occupied slot is exactly one item: using it removes the WHOLE slot, the inverse of `PickupItemCommand`'s `append_slot`; the `quantity`-field "reduce" path is a forward-shape no-op in v0 since no item ships with quantity > 1. The removal is a real `InventoryState` mutation, asserted byte-identical-except-the-removed-slot; a reject removes NOTHING).

4. **Consumables that appear in MVP reward tables each have an explicit intended use case + target frequency band showing semi-rarity, and at least one observed/simulated use demonstrates player-perceived value before the consumable pool expands.**
   **Given** consumables appear in MVP reward tables (the three Story-6.1 baseline consumables — `minor_healing_draught` (common), `warding_salve` (uncommon), `ember_flask` (rare) — are referenced by `standard_combat_reward` / `elite_combat_reward`; each carries a `rarity` tier + a `value` int as DATA, the Story-6.1 scarcity-as-data posture)
   **When** smoke runs or playtest notes are reviewed
   **Then** each consumable has an explicit intended use case + a target frequency band showing it is semi-rare (a TRACKED-NOTE deliverable in Completion Notes, the 4.6/5.5 playtest-note precedent: record each baseline consumable's intended use case (from its `tactical_identity` string + the new `effect`/`outcome_effect`) and its semi-rare frequency band, evidenced structurally by the rarity tier (uncommon/rare) + the reward-table weight (a consumable's weight relative to the table total is its observed-frequency band))
   **And** at least one observed/simulated use demonstrates player-perceived value before expanding the consumable pool (the smoke harness SIMULATES at least one consumable use end-to-end via `UseConsumableCommand` — the `item_consumed` event records the effect + the slot removal — and the tracked note records the player-perceived-value pass; flag honestly that v0 is OUTCOME-RECORD-ONLY (the felt value at this milestone is the recorded effect + the slot consumption, NOT a mutated HP bar — that mutation is Epic 7), and do NOT expand the consumable pool here (FR53 pool growth is a later Epic-6/10 content story)).

5. **A loot/passive smoke test run executes headlessly; reward, inventory, Consume, Destroy, and consumable flows run; the run stays deterministic for the same seed; invalid/no-mutation cases are covered.**
   **Given** a loot/passive smoke test run executes headlessly
   **When** reward, inventory, Consume, Destroy, and consumable flows run (the headless smoke harness drives: generate a loot offer -> pick it up / skip it; generate a passive offer -> Consume one and (a second run) Destroy one; use a consumable — ALL through the existing commands + the new `UseConsumableCommand`, NO scene)
   **Then** the run remains deterministic for the same seed (the SAME `(root_seed, class_id)` + the SAME ordered command/generate sequence -> byte-identical `RunState.to_dictionary()` + byte-identical generated offers (the `rewards`/`loot` draw reproduces) + byte-identical resolver output + byte-identical Destroy outcome (the 70/20/10 roll through the `rewards` stream reproduces) + byte-identical inventory + byte-identical event ids; a divergent seed CAN diverge — the determinism 6.3/6.5/6.6 already guarantee, now asserted across the full loot/passive loop)
   **And** invalid/no-mutation cases are covered by tests (the `UseConsumableCommand` invalid branches — non-positive sequence id, not-a-`RunState`, structurally-invalid run, an item id not in the backpack (`item_not_in_inventory`), a non-`consumable`-category slot (`not_a_consumable`), an unresolvable consumable id (`unknown_consumable`) — each rejects with a stable code, ZERO events, ZERO RNG, byte-identical `RunState`; the existing Consume/Destroy/Pickup no-mutation guarantees are re-exercised through the smoke harness).

6. **An internal tester reaching at least two passive choices, choosing Consume once and Destroy once, can explain what they gained and what they gave up in both cases; unclear/emotionally-flat choice text is flagged before the passive pool expands.**
   **Given** an internal tester reaches at least two passive choices
   **When** they choose Consume once and Destroy once
   **Then** they can explain what they gained and what they gave up in both cases (a TRACKED-NOTE deliverable, the 5.5 AC4 / 4.6 AC4-AC5 precedent: the smoke harness makes the Consume vs Destroy contrast OBSERVABLE — a Consume adopts the passive into the run's `RulesResolver` (surfaced via `resolver.explain(window)`) and the 6.4 modal `consume_text`/`exact_mechanical_effects` says what is gained; a Destroy rolls a 70/20/10 outcome (the `passive_destroyed` event's `outcome_category`/`outcome_id`/`outcome_effect`/`explanation`) and the 6.4 `destroy_text`/`has_unknown_consequences`/`consequences_text` says what is gained + refused — so a tester can attach a "I gained X by consuming / I gave up Y but got Z by destroying" note to a real resolved offer)
   **And** unclear or emotionally flat choice text is flagged before the passive pool expands (the tracked note records the human-felt "can I explain the gain/give-up in both cases?" pass against the six baseline passives' 6.4 modal text; any salvage-y / flat `consume_text`/`destroy_text` is FLAGGED as a content-quality forward residual for the later passive-pool / 10.4 comprehension story — 6.7 does NOT re-author the 6.4 modal fields or expand the pool to "fix" flat text).

### Acceptance-criteria interpretation notes (READ BEFORE CODING)

- **This is the SEVENTH and FINAL Epic 6 story — the loot/passive build SMOKE RUN capstone (the Epic-6 analogue of Story 5.5).** It is mostly ASSEMBLE + SURFACE + PROVE: it threads the ALREADY-shipped 6.1-6.6 capabilities (loot definitions, inventory + pickup, reward-offer generate flow, passive modal contract, Consume, Destroy) into ONE deterministic headless smoke surface + the reward-pacing survey + the consumable-value + Consume/Destroy tracked notes. The ONE genuinely new mechanic is a `UseConsumableCommand` + a small additive `effect`/`outcome_effect` field on `ConsumableDefinition` + an `item_consumed` event + an `InventoryState` slot-removal helper (AC3/AC4 need the consumable to actually be USED — that command did not exist after 6.1-6.6). Resist scope creep into Epic-7 risk-economy (the live HP/heal/wallet) and the later Epic-6 operations engine (a passive/consumable CHANGING a combat number) — v0 stays OUTCOME-RECORD-ONLY + EXPLANATION-ONLY.

- **CRITICAL — the consumable-use is OUTCOME-RECORD-ONLY; there is NO live HP / wallet in v0.** Grep-confirmed at story creation (`run_state.gd`): the run carries `phase`/`root_seed`/`is_manual_seed`/`meta_progression_eligible`/`route`/`selected_class_id`/`starting_kit`/`rules_resolver`/`inventory`/`pending_reward_offer` — there is NO mutable current-HP field (HP lives ONLY as `starting_kit.baseline_hp`, a fixed kit value) and NO gold/wallet field. So a "heal"/"ward"/"ember" consumable cannot mutate an HP bar or a wallet — none exists. `UseConsumableCommand` therefore RECORDS the consumable's effect deterministically via the `item_consumed` event (the `effect`/`outcome_effect` marker + the `explanation`), exactly as `ResolveRewardCommand` records a gold reward outcome-only (resolve_reward_command.gd:26-31 [Decision]) and `DestroyPassiveCommand` records a Destroy outcome-only (destroy_passive_command.gd:41-49 [Decision]). The live heal/cure/buff MUTATION is **Epic 7's risk-economy state** (the gold wallet + scarce healing — deferred-work.md "dev of 6-3" / "dev of 6-6": owner Epic 7), wired off the recorded `item_consumed` effect when that domain field lands. Record this as a `[Decision]` + a forward residual. The CONSUMABLE-USE ITSELF IS DETERMINISTIC — it draws ZERO RNG (there is no roll; it removes the slot + records the authored effect). Unlike Destroy (which rolls the 70/20/10), Use is a deterministic content-lookup + slot-removal + record, exactly like `PickupItemCommand`/`ConsumePassiveCommand`. Grep-confirm the new command/definition have NONE of `randi`/`randf`/`RandomNumberGenerator` (the false-PASS guard will look).

- **CRITICAL — `ConsumableDefinition` needs a small ADDITIVE `effect`/`outcome_effect` + `explanation` field (it currently has only `consumable_id`/`rarity`/`value`/`tactical_identity` — NO effect field).** Read `scripts/content/definitions/consumable_definition.gd` start-to-end FIRST. AC3 ("the item effect resolves through domain events") + AC4 ("each consumable has an explicit intended use case") require the consumable to carry a machine-readable effect the `item_consumed` event records. EXTEND `ConsumableDefinition` ADDITIVELY (the 6.4 `PassiveDefinition` additive-extension precedent — NO field renames/removals): add `@export var outcome_effect: String` (or `effect`) + `@export var explanation: String` (non-empty, validated; the deterministic OUTCOME-RECORD description + the player/debug-readable known result, mirroring `DestroyOutcomeTableDefinition`'s `effect`/`explanation` pair). Tighten `validate()` to REJECT a blank `outcome_effect`/`explanation` (a dedicated per-field negative each). **Extend all THREE baseline consumables (`minor_healing_draught`/`warding_salve`/`ember_flask`) with the new fields in the SAME change** (the 6.4 lesson: tightening `validate()` to require a new field retroactively invalidates the baselines + breaks `create_baseline_repository()`/the reward-table suites that reference them — extend them or the repo returns null and the suites break). The `outcome_effect` is OUTCOME-RECORD-ONLY in v0 (the authored string describes the intended deterministic heal/ward/buff; no live mutation). New constructor param(s) go LAST (after `tactical_identity`) so the positional `ConsumableDefinition.new(...)` calls in `consumable_repository.gd::_baseline_definitions()` are extended, not reordered.

- **CRITICAL — the `UseConsumableCommand`: mirror `PickupItemCommand` / `ConsumePassiveCommand` VERBATIM; the difference is it REMOVES a backpack slot (the inverse of pickup) + resolves the consumable through `ConsumableRepository`.** Read `pickup_item_command.gd` + `consume_passive_command.gd` start-to-end FIRST. Same: `extends "res://scripts/core/commands/game_command.gd"`; the preloads (`DomainEvent`, `InventoryState`, `RunState`) PLUS `ConsumableRepository` + `ConsumableDefinition`; reject `sequence_id <= 0` FIRST (`invalid_event_sequence_id`); reject a non-`RunState`/structurally-invalid run via the `_invalid_context(inner)` helper copied VERBATIM (`invalid_context`); validate-then-mutate (re-run validate() in execute, then mutate); ZERO RNG; byte-identical no-mutation on every reject. **THE DIFFERENCES:** (a) it resolves the offered consumable id through an INJECTED `ConsumableRepository` (default the baseline repo — the `ConsumePassiveCommand._passive_repository` injection posture) and fails closed `unknown_consumable` on a miss; (b) it finds the consumable slot in `run.inventory.backpack` by `item_id` (and the slot's `category` MUST be `consumable` — reject `not_a_consumable` otherwise) and rejects `item_not_in_inventory` if no matching slot exists; (c) on success it REMOVES that backpack slot (a new `InventoryState.remove_first_slot_with_id(item_id)` / `remove_slot_at(index)` helper — the inverse of `append_slot`, which must be ADDED) and emits ONE `item_consumed` event AFTER the (infallible-once-validated) removal. Constructor shape: `_init(new_item_id: StringName = &"", new_sequence_id: int = 1, new_consumable_repository: ConsumableRepository = null)` with `command_id = &"use_consumable"`. **DECISION TO RECORD:** the command takes the `item_id` directly (decoupled from any view-model); the `run.inventory` backpack is the authoritative source of what can be used (it reads the slot, not an external id list). [Source: pickup_item_command.gd:1-164 (the run-command idiom + append_slot to mirror/invert); consume_passive_command.gd:69-135 (the repository-injection + fail-closed gate sequence); inventory_state.gd:118-139 (make_slot/append_slot — add the removal counterpart)]

- **CRITICAL — the `InventoryState` slot-REMOVAL helper (the inverse of `append_slot` — it does NOT exist yet).** `InventoryState` has `append_slot` / `make_slot` / `is_full` / `has_capacity` / `size` but NO removal. ADD a small ADDITIVE removal helper (the 6.2 additive-method precedent): e.g. `remove_first_slot_with_id(item_id: StringName) -> int` (removes the FIRST backpack slot whose `item_id` matches, returns the removed index or -1 if none) and/or `remove_slot_at(index: int) -> Dictionary` (removes + returns the slot, or `{}` on an out-of-range index). It is a real mutation of the ordered `backpack` array (the inverse of `append_slot`); it must NOT touch `equipment`, NOT change `capacity`, and NOT renumber the `SLOT_KEYS`/`DICTIONARY_KEYS`. The exact-key `to_dictionary()` contract + the deep-copy `copy()` + the lenient `try_from_dictionary` stay byte-identical (the removal only shortens the backpack list). v0 has NO stacking, so removal drops the WHOLE slot (no quantity-- path — the `quantity` field stays the forward-shape no-op it is). Add `remove_first_slot_with_id`/`remove_slot_at` to the `InventoryState` test coverage. [Source: inventory_state.gd:99-139 (the slot helpers to extend); test_inventory_state.gd (the exact-key + slot coverage to extend)]

- **CRITICAL — the `item_consumed` SYSTEM event: APPEND at the enum end (NEVER renumber) + wire end-to-end, the `item_gained`/`passive_destroyed` pattern; it is DETERMINISTIC (NO draw provenance).** The current `Type` enum ends `..., REWARD_RESOLVED, PASSIVE_CONSUMED, PASSIVE_DESTROYED` (domain_event.gd:29-33). Add ONE `ITEM_CONSUMED` `Type` member AFTER `PASSIVE_DESTROYED` — NEVER renumber an existing member (project-context.md "append new SYSTEM events at the end (never renumbered)"). For it: a new `Type.ITEM_CONSUMED` member at the end; a stable `EVENT_ID_ITEM_CONSUMED := &"item_consumed"` const; a `static func item_consumed(sequence_id, payload)` factory that `duplicate(true)`s + defensively normalizes the payload (`item_id`/`outcome_effect`/`explanation` -> String, `backpack_size_after`/`slot_index` -> int); a `_validate_item_consumed_payload(...)` branch wired into `_validate_payload_for_event` (a match arm after `Type.PASSIVE_DESTROYED`): reject a non-lower_snake `item_id` (field `item_id`), a blank/non-string `outcome_effect` (field `outcome_effect`), a blank/non-string `explanation` (field `explanation`), a non-nonnegative-integral `backpack_size_after`/`slot_index`; the `id_for_type`/`type_for_id` round-trip map entries; and (a SYSTEM event, no actor) do NOT add it to `_event_requires_actor`. Mirror `item_gained` for the shell (it carries `item_id`/`category`/`backpack_size_after`/`slot_index` + is a SYSTEM event) — but `item_consumed` carries `item_id`/`outcome_effect`/`explanation`/`backpack_size_after`/`slot_index` (the resolved effect + the post-removal provenance). **NO `roll`/`draw_index` — Use draws ZERO RNG** (unlike `passive_destroyed`/`reward_offered`, which carry draw provenance because they DRAW). Add a JSON round-trip test + a malformed-payload negative per field in `test_domain_event.gd`, mirroring `item_gained`, AND **add `DomainEvent.Type.ITEM_CONSUMED: &"item_consumed"` to the EXHAUSTIVE `expected_ids` map in `_event_identifiers_are_stable_machine_ids`, immediately after the `PASSIVE_DESTROYED` entry** — that test asserts `id_for_type`/`type_for_id` round-trip for EVERY enum member AND is the de-facto no-renumber/stable-id pin (the existing ids stay byte-identical; the new id is appended exactly as 6.6 appended `passive_destroyed`). Missing this entry leaves the new event's id-map round-trip unpinned (a reviewer gap). [Source: domain_event.gd:29-33 (enum tail), :58 (EVENT_ID_ITEM_GAINED), the item_gained factory + `_validate_item_gained_payload` (the deterministic SYSTEM-event shell to mirror), :64-92 (ITEM_GAINED_CATEGORIES local-allowlist precedent — `item_consumed` needs NO new allowlist since it carries no category), the `passive_destroyed` append (6.6) as the most-recent append precedent; test_domain_event.gd `_event_identifiers_are_stable_machine_ids` exhaustive map — append after the PASSIVE_DESTROYED entry; project-context.md "RUN EVENT LIFECYCLE ... appended to the END of the event enum (never renumbered)"]

- **CRITICAL — reward GENERATE is an EXPLICIT caller-driven `RunOrchestrator` method, NOT auto-wired into `run_to_completion` (the 6.3 [Decision], re-affirmed by 6.5/6.6).** The smoke harness MUST call `orchestrator.generate_reward_offer(table_id)` / `generate_passive_reward_offer(table_id)` itself at a node-completion boundary — `run_to_completion` does NOT generate offers (auto-generating on every combat node would advance the run-level `rewards` stream mid-run with no auto-resolve policy, trip the `reward_offer_pending` guard on the next generate, and perturb the determinism / interrupted==uninterrupted tests). 6.7 does NOT change this — it CONSUMES the caller-driven generate path (the smoke harness IS the caller). A generate while an offer is still `pending` fails closed `reward_offer_pending` (resolve the prior offer first — the harness resolves each offer before generating the next). 6.7 does NOT auto-wire generate-on-complete into the orchestrator loop (that is the later HUD/orchestrator story that owns a resolution policy — the SAME residual 6.3/6.5/6.6 left). [Source: run_orchestrator.gd:280-422 (`generate_reward_offer`/`generate_passive_reward_offer`/`_reward_generate_precheck` — the caller-driven generate path, the `reward_offer_pending` guard); deferred-work.md "dev of 6-3" orchestrator auto-generate policy [Decision]; epic-6 retro-notes Story 6-3]

- **CRITICAL — the HUD wiring of the commit-intent -> Consume/Destroy/Use command call site stays DEFERRED; 6.7 is HEADLESS, it constructs the commands DIRECTLY.** Story 6.4's `PassiveRewardCommitFlow.confirm()` produces the `{choice:"consume"|"destroy", passive_content_id, table_id}` commit-INTENT; 6.5/6.6 built the COMMANDS that consume that data shape; the HUD wiring of the commit-flow -> command call site is owned by a later HUD story (UI-scene-last — deferred-work.md "dev of 6-5"/"dev of 6-6"). 6.7 does NOT build that HUD wiring and does NOT build a `.tscn` scene. The smoke harness constructs `ConsumePassiveCommand.new(passive_id, table_id, sequence_id)` / `DestroyPassiveCommand.new(passive_id, table_id, sequence_id, orchestrator.streams)` / `PickupItemCommand.new(item_id, category, sequence_id)` / `UseConsumableCommand.new(item_id, sequence_id)` DIRECTLY (the test-driver posture the 6.5/6.6 command tests already use), threading the orchestrator's `streams` into Destroy (the named-stream rule). It is a HEADLESS smoke harness, NOT a scene/HUD — the 5.5 "playable proven headless" precedent. [Source: deferred-work.md "dev of 6-5"/"dev of 6-6" HUD-wiring residual; consume_passive_command.gd / destroy_passive_command.gd constructors; project-context.md UI-scene-last]

- **CRITICAL — DESTROY rolls through the run-level `RngStreamSet` `rewards` stream; thread `orchestrator.streams` into `DestroyPassiveCommand`.** The Destroy in the smoke harness MUST use the orchestrator's run-level `streams` (the SAME set the reward GENERATE path drew on) so the 70/20/10 roll advances the `rewards` stream coherently with the run's prior reward draws — `DestroyPassiveCommand.new(passive_id, table_id, sequence_id, orchestrator.streams)`. It rejects a null stream set (`missing_rng_streams`). The DETERMINISM AC: the SAME run-level stream-set seed + the SAME outcome table -> the SAME rolled outcome. 6.7 does NOT mint a fresh `RngStreamSet` for the Destroy — it hands the orchestrator's run-level one (never `randi`/`randf`/a local RNG). [Source: destroy_passive_command.gd:64-90 (the injected RngStreamSet + STREAM_REWARDS roll); run_orchestrator.gd:65 (`streams` — the run-level set); epic-6 retro-notes Story 6-6]

- **The reward-pacing SURVEY (AC2) = the `test_run_pacing_survey.gd` structural-measurement precedent, NOT a generator/weight change.** AC2 wants "at least one build-defining passive offer is available by the configured node/depth target" across "approved smoke seeds". Mirror `test_run_pacing_survey.gd` (Story 4.6 AC5): a fixed `SURVEY_SEEDS` sample, a deterministic headless measurement, structural assertions (the `passive_reward_choice` table exists in the validated `RewardTableRepository`; it yields a 3-choice passive offer; the offered passive ids are REAL baseline ids; the offer can be generated by the early-mid band), and a tracked-note overlay for the human pacing target. Do NOT change `route_generator.gd`, the depth-band weights, or the passive pool to "hit" pacing — MEASURE + record the structural surface; flag any gap (e.g. the constant-8-tier depth) as an Epic-10-tuning tracked note (the known 4.2/4.6 follow-up, re-recorded NOT fixed). The "early-mid band" is anchored on `RouteNode.depth` (the route's first-third ratio, `route_generator.gd::_type_weights_for_band` boundary). [Source: test_run_pacing_survey.gd (the AC5 structural-survey precedent); route_generator.gd:344-380 (`_draw_node_type`/`_type_weights_for_band` depth bands); reward_table_repository.gd:128-139 (the `passive_reward_choice` table — 3-choice, references the six baseline passive ids)]

- **AC1 "skip" = the generic `ResolveRewardCommand` outcome-only resolve (the explicit decline path).** AC1 lists four dispositions: consumed / destroyed / picked up / skipped. The first three have dedicated commands (`ConsumePassiveCommand` / `DestroyPassiveCommand` / `PickupItemCommand`); "skip"/decline is the Story-6.3 `ResolveRewardCommand` (the generic outcome-only resolve that flips the offer to `resolved` and applies nothing — it is ALSO the gold-reward outcome-only path + the passive "decline without consuming or destroying" fallback). The smoke harness drives each disposition end-to-end against the SAME (or a parallel) run, proving every offer type has a real resolving command. Do NOT invent a new "skip" command — `ResolveRewardCommand` is the decline. [Source: resolve_reward_command.gd (the generic outcome-only resolve — the skip/decline + gold path); epic-6 retro-notes Story 6-3/6-5/6-6 (a passive offer is resolved by EXACTLY ONE of {ResolveReward | Consume | Destroy})]

- **The Consume/Destroy/consumable tracked-NOTES (AC4/AC6) are deliverables, not code (the 5.5 AC4 / 4.6 AC4-AC5 precedent).** AC4 (consumable intended-use + semi-rare frequency + observed value) and AC6 (the tester can explain gain/give-up for Consume + Destroy) are recorded as local playtest/tracked notes in this story's Completion Notes — the smoke harness makes the surfaces OBSERVABLE (the consumable USE via `UseConsumableCommand` + the `item_consumed` effect; the Consume-vs-Destroy contrast via the resolver explanation + the 6.4 modal text + the `passive_destroyed` outcome). Flag honestly that v0 is OUTCOME-RECORD-ONLY + EXPLANATION-ONLY (the felt value/difference at this milestone is the recorded effect + the explanation surface, NOT a mutated combat number / HP bar — that mutation is Epic 6 operations / Epic 7 economy). Do NOT expand scope to "fix" a felt-value or flat-text gap (that is the later passive-pool / 10.4 comprehension story). Any salvage-y/flat 6.4 modal text is FLAGGED, NOT re-authored.

- **Scope discipline — what 6.7 MUST NOT do:**
  - NO change to / fork of `ConsumePassiveCommand` (6.5), `DestroyPassiveCommand` (6.6), `ResolveRewardCommand` (6.3), `PickupItemCommand` (6.2), or the `RunOrchestrator` reward-generate path (6.3) — `UseConsumableCommand` is a NEW distinct command; the others are CONSUMED as-is (`done`). The orchestrator's generate methods + sequence-counter wiring are UNCHANGED (whether the orchestrator gets a `use_consumable` convenience method is OPTIONAL — recommended NO; keep 6.7's new command caller-driven, exactly as 6.3/6.5/6.6 left theirs).
  - NO live HP / wallet / heal / cleanse / curse / Oath-Shards / meta MUTATION (Epic 7 risk-economy + Epic 8 meta own those domain fields). v0 consumable-use + Destroy are OUTCOME-RECORD-ONLY (the event records the effect; no HP/wallet/curse field is mutated because none exists).
  - NO per-effect passive/consumable OPERATION engine / `RuleCondition`/`RuleTarget`/`RuleOperation` / combat HOOK sites / a passive or consumable CHANGING an HP/damage/movement number (`scripts/rules/{conditions,operations}` stay EMPTY) — the later Epic-6 operations story. 6.7 surfaces EXPLANATIONS + records effects only.
  - NO authoring of the broader 20-30 MVP passive POOL (FR46) — 6.7 consumes the SIX existing baseline passives (the FR48 early-mid build-defining passive is satisfied by the existing `passive_reward_choice` table referencing them; the pool growth is a later Epic-6 content story). It authors NO new passive content.
  - NO expansion of the CONSUMABLE pool (FR53) — 6.7 USES the three existing baseline consumables (extending them with the additive effect field); it authors NO new consumable. The pool growth is a later Epic-6/10 content story.
  - NO re-authoring of Story 6.4's `PassiveDefinition` modal fields (`consume_text`/`destroy_text`/`has_unknown_consequences`/`consequences_text`/`served_pillars`) or `PassiveRewardModalViewModel`/`PassiveRewardCommitFlow` — AC5/AC6's modal text already ships from 6.4; 6.7 consumes it. (Flag flat text, do NOT fix it.)
  - NO change to `RewardOffer` / `RewardTableDefinition` / `DestroyOutcomeTableDefinition` / the 23-key `RunSnapshot` gate / `_allowed_run_snapshot_keys()` / `RngStreamSet.required_streams()` / the generator/route/level fingerprints. The ONLY additive shape changes are: `ConsumableDefinition` (+`outcome_effect`/`explanation`), `InventoryState` (+a removal helper), `DomainEvent` (+`item_consumed`). No new `RunState`/`RewardOffer` field, no new save key.
  - NO mutation of the route-position save / `to_run_snapshot_fields()` (the in-node reward-offer / consumed-item save is the SAME tracked forward residual 6.3/6.5/6.6 deferred — the later in-node-save / live-resume story, NOT this one). The inventory + the offer + the Destroy outcome ride the FULL `RunState.to_dictionary()` only; they are DELIBERATELY NOT in the route-position 23-key gate. 6.7 does NOT persist them into the route-position save.
  - NO route-generator / depth-band / reward-weight / passive-pool change to "hit" pacing (AC2 MEASURES + records the structural pacing surface; the constant-8-tier depth + exact frequency tuning is Epic-10, re-recorded as a tracked note, NOT fixed here).
  - NO `Control`/`.tscn` modal SCENE / presenter / icon ART (UI-scene-last). Do NOT add anything under `godot/scenes/` or `godot/assets/`. The smoke run is a HEADLESS harness + tests.
  - NO new RNG site beyond the existing draws — the smoke run's RNG is: route generation's `map` stream (in `RunStartCommand`), level generation's `level` stream (orchestrator combat nodes), the reward GENERATE's `rewards`/`loot` stream (6.3 builder), and the Destroy's `rewards` stream (6.6). `UseConsumableCommand` draws ZERO RNG (deterministic). NO global/un-named RNG; NO passive/consumable RNG beyond Destroy's existing roll.
  - NO active class skill (FR45 — keep the absence), NO difficulty knob (project-context.md hard non-goal — nothing scales enemy stats/HP/damage/rewards/RNG/run-length by depth/difficulty), NO duplicate-id repository fork (the duplicate-id guard is the 6.1 central guard — 6.7 forks no repo, registers no duplicate id), NO JSON/CSV pipeline / `.tres` mirror (`data/{source,resources}/` stay EMPTY), NO cloud/account/multiplayer/telemetry/.NET/new-test-framework/new-engine-library/prototype dependency.

## Tasks / Subtasks

- [x] **Task 1 — Pre-implementation gate + FAILING tests first (TDD red) (AC: 1-6)**
  - [x] 1.1 Confirm in `_bmad-output/implementation-artifacts/sprint-status.yaml` that `epic-1`..`epic-5` are `done`, `epic-6` is `in-progress`, `6-1`/`6-2`/`6-3`/`6-4`/`6-5`/`6-6` are `done`, and this story (`6-7-loot-and-passive-build-smoke-run`) is `ready-for-dev`. If any earlier Epic 1-6.6 status regressed, STOP and restore the boundary first. (The orchestrator owns the `epic-6` closeout — do NOT flip `epic-6` yourself.)
  - [x] 1.2 Confirm the working tree is clean or that dirty files are intentional user work; preserve unrelated changes. The untracked orchestrator-owned `_bmad-output/auto-gds/` directory is expected and is NOT your change.
  - [x] 1.3 Re-read the ACTUAL implemented files before writing a line: `pickup_item_command.gd` (the run-command idiom + `append_slot`), `consume_passive_command.gd` + `destroy_passive_command.gd` (the repository-injection + fail-closed gate sequence + the offer-resolution posture + the Destroy `streams` roll), `resolve_reward_command.gd` (the generic outcome-only skip/decline + the gold-as-outcome-only [Decision]), `inventory_state.gd` (the backpack/slot model + `append_slot`/`make_slot` — the removal counterpart to ADD), `consumable_definition.gd` + `consumable_repository.gd` (the three baselines to extend + the by-id resolve), `run_orchestrator.gd` (`generate_reward_offer`/`generate_passive_reward_offer`/`_reward_generate_precheck` + the caller-driven boundary + `streams`), `reward_offer.gd` (`pending`/`resolved`/`has_offered_entry`/`selected_entry`), `reward_table_repository.gd` (the `standard_combat_reward`/`elite_combat_reward`/`passive_reward_choice` baselines + the consumable references), `domain_event.gd` (the enum tail + the `item_gained` deterministic SYSTEM-event shell + the `expected_ids` pin), `route_node.gd` (the `depth` field), `route_generator.gd::_type_weights_for_band` (the early/mid/late depth bands), `run_state.gd` (CONFIRM no live current-HP / wallet field — only `starting_kit.baseline_hp`), `test_run_pacing_survey.gd` (the AC5 structural-survey precedent). CONFIRM by grep that `scripts/rules/{conditions,operations}` are EMPTY (0 `.gd` files).
  - [x] 1.4 GREP-VERIFY before coding: (a) `grep -rnE "current_hp|wallet|gold_balance|hp_remaining" godot/scripts/run/` returns NOTHING (no live HP/wallet field — confirms the OUTCOME-RECORD-ONLY decision); (b) `grep -rn "use_consumable\|item_consumed\|UseConsumableCommand" godot/` returns NOTHING (the command/event are greenfield); (c) the inventory/offer/Destroy-outcome are NOT serialized into the route-position save — confirm `inventory`/`pending_reward_offer` do NOT appear in `RunState.to_run_snapshot_fields()`. Record the grep results.
  - [x] 1.5 Add focused FAILING tests before any production edit. NEW `godot/tests/unit/core/test_use_consumable_command.gd` (mirror `test_pickup_item_command.gd`/`test_consume_passive_command.gd`): a valid use removes the consumable slot + emits `item_consumed` recording the effect; ZERO RNG (a held external `RngStreamSet` is byte-identical after a success AND a reject); every invalid/no-mutation case (non-positive sequence id, not-a-`RunState`, structurally-invalid run, `item_not_in_inventory`, `not_a_consumable`, `unknown_consumable`) rejects with a stable code + ZERO events + byte-identical `RunState`. EXTEND `godot/tests/unit/run/test_inventory_state.gd` (the removal helper: `remove_first_slot_with_id`/`remove_slot_at` removes the right slot, returns the index/slot, leaves the rest byte-identical, no-op on a miss; the exact-key contract unchanged). EXTEND `godot/tests/unit/content/test_consumable_definition.gd` (the additive `outcome_effect`/`explanation` fields validate; a blank `outcome_effect`/`explanation` rejects per-field; the three baselines still validate). EXTEND `godot/tests/unit/core/test_domain_event.gd` (the `item_consumed` round-trip + a malformed-payload negative per field + the EXHAUSTIVE `expected_ids` entry after `PASSIVE_DESTROYED`). NEW `godot/tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd` (the headless end-to-end smoke harness — the AC1/AC2/AC5 capstone; the `tests/integration/reward_flow/` dir already exists). NEW or EXTENDED reward-pacing survey (`godot/tests/unit/run/test_run_pacing_survey.gd` OR a new `test_reward_pacing_survey.gd`): across a fixed seed sample, the `passive_reward_choice` table is reachable + yields a 3-choice passive offer of REAL baseline ids by the early-mid band. Confirm RED (missing command / missing event / missing helper / missing field) BEFORE the production edit, and confirm the runner does NOT print a false PASS for a failing-to-load test file (grep the raw output for `SCRIPT ERROR`/`Parse Error`).
  - [x] 1.6 Reuse existing primitives — `ActionResult` (`scripts/core/results/action_result.gd`); the `PickupItemCommand`/`ConsumePassiveCommand` run-command shell (constructor / sequence-id-first / validate-then-mutate / `_invalid_context` / repository-injection — mirror them); the `InventoryState` backpack/slot model (`append_slot`/`make_slot` — add the removal counterpart); the `ConsumableRepository.get_consumable(id)` fail-closed gate; the `RunOrchestrator` caller-driven generate path; the `RewardOffer`/`ResolveRewardCommand`/`ConsumePassiveCommand`/`DestroyPassiveCommand`/`PickupItemCommand` resolution commands; the `DomainEvent` append-at-end deterministic-SYSTEM-event pattern (`item_gained`); the `test_run_pacing_survey.gd` structural-survey shape. Do NOT invent a new result type, a parallel command base, a second event system, a hand-minted `RandomNumberGenerator`, a parallel inventory, or a parallel reward path.

- [x] **Task 2 — Extend `ConsumableDefinition` with the additive effect field + extend the three baselines (AC: 3, 4)**
  - [x] 2.1 ADD `@export var outcome_effect: String = ""` (the OUTCOME-RECORD effect marker) + `@export var explanation: String = ""` (the player/debug-readable known result) to `consumable_definition.gd`, AFTER `tactical_identity` (the additive-field, new-param-last posture). Extend `_init(...)` with the two new defaulted params LAST. Mirror `DestroyOutcomeTableDefinition`'s `effect`/`explanation` pair (the OUTCOME-RECORD-ONLY description + the Readability-Rule explanation).
  - [x] 2.2 Tighten `validate()` to REJECT a blank `outcome_effect` (`_invalid(&"outcome_effect")`) + a blank `explanation` (`_invalid(&"explanation")`) — a dedicated per-field negative each. The existing field checks (`consumable_id`/`rarity`/`value`/`tactical_identity`) stay byte-identical (additive — NO rename/removal).
  - [x] 2.3 EXTEND all THREE baseline consumables in `consumable_repository.gd::_baseline_definitions()` (`minor_healing_draught`/`warding_salve`/`ember_flask`) with a non-empty `outcome_effect` + `explanation` (the OUTCOME-RECORD description of the intended heal/ward/ember effect — v0 records, does NOT apply). The positional `ConsumableDefinition.new(...)` calls gain the two trailing args. Confirm `create_baseline_repository()` still succeeds + the reward-table suites (`standard_combat_reward`/`elite_combat_reward` reference these ids) stay green (the 6.4 lesson: a tightened validate() + un-extended baselines silently returns null + breaks the referencing suites).
  - [x] 2.4 EXTEND `test_consumable_definition.gd`: the additive fields validate on the baselines; a blank `outcome_effect`/`explanation` rejects with the field; the three baselines (resolved via the repo) carry non-empty effect/explanation.

- [x] **Task 3 — The `InventoryState` slot-removal helper (the inverse of `append_slot`) (AC: 3)**
  - [x] 3.1 ADD a removal helper to `inventory_state.gd` (the additive-method posture): `remove_first_slot_with_id(item_id: StringName) -> int` (find + remove the FIRST backpack slot whose `item_id` matches, return the removed index or -1 if none) and/or `remove_slot_at(index: int) -> Dictionary` (remove + return the slot, `{}` on out-of-range). A real mutation of the ordered `backpack` array (the inverse of `append_slot`); it does NOT touch `equipment`, NOT change `capacity`, NOT renumber `SLOT_KEYS`/`DICTIONARY_KEYS`. v0 removal drops the WHOLE slot (NO quantity-- — no stacking).
  - [x] 3.2 EXTEND `test_inventory_state.gd`: removal removes the right slot + returns the index/slot; the remaining backpack is byte-identical (order preserved, the other slots untouched); removing a missing id is a no-op (-1 / `{}`, no mutation); the exact-key `to_dictionary()` + `copy()` + `try_from_dictionary` contracts stay byte-identical after a removal (the backpack just shortens).

- [x] **Task 4 — The `item_consumed` SYSTEM `DomainEvent` (append-at-end, deterministic, wired end-to-end) (AC: 3)**
  - [x] 4.1 In `domain_event.gd`: APPEND an `ITEM_CONSUMED` `Type` member at the END of the enum (after `PASSIVE_DESTROYED`, NEVER renumber); add `EVENT_ID_ITEM_CONSUMED := &"item_consumed"`; add a `static func item_consumed(sequence_id, payload)` factory that `duplicate(true)`s + defensively normalizes (`item_id`/`outcome_effect`/`explanation` -> String, `backpack_size_after`/`slot_index` -> int); wire the `id_for_type`/`type_for_id` map entries. SYSTEM event (no actor) -> do NOT add to `_event_requires_actor`. Mirror `item_gained` for the deterministic SYSTEM-event shell (NO `roll`/`draw_index` — Use draws ZERO RNG).
  - [x] 4.2 Add `_validate_item_consumed_payload(payload_value)` wired into `_validate_payload_for_event` (a match arm after `Type.PASSIVE_DESTROYED`): reject a non-lower_snake `item_id` (field `item_id`), a blank/non-string `outcome_effect` (field `outcome_effect`), a blank/non-string `explanation` (field `explanation`), a non-nonnegative-integral `backpack_size_after` (field `backpack_size_after`) + `slot_index` (field `slot_index`). Mirror `_validate_item_gained_payload` (the deterministic shell — NO category allowlist, NO draw provenance).
  - [x] 4.3 EXTEND `test_domain_event.gd`: an `item_consumed` round-trip through real JSON (factory -> `to_dictionary()` -> `JSON.stringify` -> `parse_string` -> `try_from_dictionary` succeeds, payload stable — mirror the `item_gained` round-trip); a malformed-payload negative per field; register the new test method(s) in `run()`. **CRITICAL — add `DomainEvent.Type.ITEM_CONSUMED: &"item_consumed"` to the EXHAUSTIVE `expected_ids` map in `_event_identifiers_are_stable_machine_ids`, immediately AFTER the `PASSIVE_DESTROYED` entry.** That test is the de-facto no-renumber/stable-id pin; the existing ids stay byte-identical, the new id appends exactly as 6.6 appended `passive_destroyed`.

- [x] **Task 5 — The `UseConsumableCommand` (use a backpack consumable -> record effect + remove slot) (AC: 3, 5)**
  - [x] 5.1 Add `godot/scripts/core/commands/use_consumable_command.gd` (`class_name UseConsumableCommand extends "res://scripts/core/commands/game_command.gd"`), mirroring `pickup_item_command.gd` + `consume_passive_command.gd`. Preloads: `DomainEvent`, `InventoryState`, `RunState`, `ConsumableRepository`, `ConsumableDefinition`. Constructor `_init(new_item_id: StringName = &"", new_sequence_id: int = 1, new_consumable_repository: ConsumableRepository = null)` with `command_id = &"use_consumable"`; default the repo to the baseline (`ConsumableRepository.create_baseline_repository()`), injectable LAST (the `ConsumePassiveCommand` posture). Copy the `_invalid_context(inner)` helper + the `_is_lower_snake_id` helper VERBATIM.
  - [x] 5.2 `validate(state)` (pure read, no mutation/event/RNG): reject `sequence_id <= 0` FIRST (`invalid_event_sequence_id`); reject a non-`RunState`/structurally-invalid run via `_invalid_context` (`invalid_context`); reject a null `run.inventory` defensively (`invalid_context`); reject an `item_id` not lower_snake (`invalid_item_id`); reject NO matching backpack slot (`item_not_in_inventory`, the offending id in metadata); reject a matching slot whose `category` is NOT `consumable` (`not_a_consumable`); reject an `item_id` that does NOT resolve through `ConsumableRepository.get_consumable(id)` (`unknown_consumable` — fail-closed, never apply a null). Order the gates so the cheapest/most-specific fire first (sequence -> context -> id shape -> in-inventory -> is-consumable -> resolves).
  - [x] 5.3 `execute(state)` (validate-then-mutate): re-run `validate(state)`; on success REMOVE the consumable's backpack slot (`run.inventory.remove_first_slot_with_id(item_id)` -> the removed `slot_index`), read the resolved `ConsumableDefinition`'s `outcome_effect`/`explanation`, and emit ONE `item_consumed` event AFTER the (infallible-once-validated) removal carrying `{item_id, outcome_effect, explanation, backpack_size_after, slot_index}`. Return `ActionResult.ok([event], {records_consumption: true, item_id, outcome_effect, slot_index, backpack_size_after})`. ZERO RNG; runs no sub-command; on any reject ZERO events + byte-identical `RunState`.
  - [x] 5.4 Confirm `test_use_consumable_command.gd` proves: the valid use (slot removed + `item_consumed` emitted recording the effect); ZERO RNG (a held external `RngStreamSet` byte-identical after success AND reject); every invalid/no-mutation branch (byte-identical `RunState`, ZERO events). Assert the removed slot is gone + the other slots are byte-identical (the AC3 "removed according to inventory rules" + the no-collateral-damage guarantee).

- [x] **Task 6 — The headless loot/passive build SMOKE RUN + the reward-pacing survey (AC: 1, 2, 5)**
  - [x] 6.1 Add `godot/tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd` (the AC1/AC5 end-to-end capstone). Drive a real run via `RunOrchestrator.start(seed, false, &"warrior")` (or another selectable class). At node-completion boundaries, the harness (the caller) GENERATES offers: `orchestrator.generate_reward_offer(&"standard_combat_reward")` (a loot offer) and `orchestrator.generate_passive_reward_offer(&"passive_reward_choice")` (a 3-choice passive offer). For EACH offer disposition, drive the matching command against `orchestrator.run` (threading the orchestrator's run-level `_next_sequence_id`-style ids + `orchestrator.streams` into Destroy): a backpack-item offer -> `PickupItemCommand` (pick up) AND a parallel path -> `ResolveRewardCommand` (skip/decline); a passive offer -> `ConsumePassiveCommand` (adopt) AND a parallel run -> `DestroyPassiveCommand` (roll the 70/20/10); a gold offer -> `ResolveRewardCommand` (outcome-only). Prove every disposition resolves the offer + emits the right event. Resolve each offer BEFORE generating the next (the `reward_offer_pending` guard).
  - [x] 6.2 Prove the CONSUMABLE use end-to-end (AC3/AC4): pick up a `consumable` item (`PickupItemCommand.new(&"minor_healing_draught", &"consumable", seq)`), then USE it (`UseConsumableCommand.new(&"minor_healing_draught", seq)`) — assert the `item_consumed` event records the effect + the slot is removed from `run.inventory.backpack`. This is the AC4 "at least one observed/simulated use demonstrates value" evidence.
  - [x] 6.3 Prove DETERMINISM (AC5): run two independent drives on the SAME `(seed, class_id)` with the SAME ordered generate/command sequence; cross-check byte-identical `RunState.to_dictionary()` (incl. the inventory + the resolved offer + the Destroy outcome) + byte-identical generated-offer payloads + byte-identical event ids. A divergent seed CAN diverge (assert at least one field differs for a different seed). Keep the seed set small + deterministic.
  - [x] 6.4 Add the reward-pacing SURVEY (AC2) — EXTEND `test_run_pacing_survey.gd` OR a new `godot/tests/unit/run/test_reward_pacing_survey.gd` (mirror the AC5 structural-survey shape): across a fixed `SURVEY_SEEDS` sample, assert the `passive_reward_choice` table is present in the validated `RewardTableRepository`, its entries reference REAL `PassiveRepository` baseline ids (cross-check against `PassiveRepository.BASELINE_PASSIVE_IDS`), a `generate_passive_reward_offer(&"passive_reward_choice")` yields a 3-choice passive offer of distinct real ids, and the offer can be generated by the early-mid band (anchor on `RouteNode.depth` first-third). The assertions are STRUCTURAL (table/weights/pool), so a failure names the table/weights/pool NOT a fixture poke. Record the structural pacing surface + flag the constant-8-tier depth as the known Epic-10 tuning note.
  - [x] 6.5 Prove the invalid/no-mutation surface is covered (AC5): the `UseConsumableCommand` rejects (Task 5.4) + re-exercise (lightly, via the harness) that a Consume/Destroy/Pickup against a `resolved`/full/off-offer state fails closed byte-identical (the 6.2/6.5/6.6 guarantees, now under the smoke harness). The empty/legacy-run + every 1.x-6.6 path stays byte-identical (the back-compat gate — the new event/field/helper are additive).

- [x] **Task 7 — Run the full headless suite + diff check (gate before review) (AC: 1-6)**
  - [x] 7.1 Run through PowerShell: `godot --version`, the full headless runner, `git diff --check`. (`godot` resolves via PowerShell — run the headless suite through `powershell.exe -NoProfile -Command ...` / the project `Godot_v4.6.3-stable_win64_console.exe`, NOT the Bash PATH.)
  - [x] 7.2 Expect Godot `4.6.3.stable.official...`; the runner exits code `0`, final line `Headless tests passed.`; ALL prior Epic 1-6.6 tests green; `git diff --check` no whitespace errors. The pre-existing `Parse JSON failed` (save/settings) + int64-overflow (`run_started.root_seed` boundary) stderr lines are documented-expected negative-path diagnostics from OTHER suites — NOT failures.
  - [x] 7.3 **False-PASS guard:** grep the run output DIRECTLY for `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to your new/edited files (the new command + the new event + the extended definition/inventory + their tests + the smoke harness + the survey) — must be NONE. Zero FAIL lines. Do NOT trust the PASS summary alone (the epic-4 §5#1 lesson, re-proven by 5.1's `get_class()` false PASS — a reserved-method or typed-array collision can compile-fail a file yet still print PASS). Grep-confirm the new command/definition have NO `randi`/`randf`/`RandomNumberGenerator` (the consumable-use is deterministic).

- [x] **Task 8 — Story wrap + the AC4/AC6 tracked-note deliverables (AC: 1-6)**
  - [x] 8.1 Update this story's Dev Agent Record, Completion Notes, File List, Change Log, and Status (-> `review`). Record every `[Decision]`: the consumable-use OUTCOME-RECORD-ONLY posture (no live HP/wallet — the Epic-7 wire-off seam); the additive `ConsumableDefinition.outcome_effect`/`explanation` (new-param-last); the `InventoryState` removal-helper shape (whole-slot, no quantity-- in v0); the `item_consumed` deterministic SYSTEM event (no draw provenance); `UseConsumableCommand` caller-driven (no orchestrator convenience method — recommended NO, exactly as 6.3/6.5/6.6); "skip" = `ResolveRewardCommand`; the smoke run is HEADLESS (no HUD/scene); the reward-pacing survey MEASURES (no generator/weight/pool change).
  - [x] 8.2 Author the AC4 consumable tracked NOTE (the intended use case + semi-rare frequency band + observed value per baseline consumable) and the AC6 Consume-vs-Destroy tracked NOTE (the tester can explain the gain/give-up for both) in Completion Notes (the 5.5 AC4 / 4.6 AC4-AC5 precedent). Flag honestly that v0 is OUTCOME-RECORD-ONLY + EXPLANATION-ONLY (the felt value/difference is the recorded effect + the explanation surface, NOT a mutated HP bar / combat number — that is Epic 6 operations / Epic 7 economy). Flag any salvage-y/flat 6.4 modal `consume_text`/`destroy_text` as a content-quality forward residual for the later passive-pool / 10.4 comprehension story. Do NOT expand scope to "fix" a felt gap.
  - [x] 8.3 Record forward residuals for the epic-6 retrospective / `deferred-work.md` as `[Review][Decision]`/`[Review][Defer]` per the pipeline convention: confirm the live HP/heal/wallet/cure consumable MUTATION stays deferred (owner Epic 7 risk-economy, wired off the `item_consumed` recorded effect); confirm the per-effect passive/consumable OPERATION engine + combat HOOK sites stay deferred (the later Epic-6 operations story); confirm the HUD wiring of the commit-intent -> Consume/Destroy/Use call site + the orchestrator auto-generate-on-complete policy stay deferred (a later HUD/orchestrator story); confirm the in-node reward-offer / consumed-item route-position SAVE stays deferred (the later in-node-save / live-resume story); confirm the broader 20-30 passive POOL (FR46) + the consumable-pool growth (FR53) stay deferred (later Epic-6/10 content stories); confirm the constant-8-tier route-depth pacing tuning stays deferred (Epic 10); confirm 6.7 forks no repo + draws no new RNG (the duplicate-id all-repos guard is the 6.1 central guard; the level-gen-injection half of the inert run-level `RngStreamSet` stays owned by a later level-gen-RNG story).

## Dev Notes

### ALREADY IMPLEMENTED (consume — do NOT rebuild) vs WHAT 6.7 DELIVERS (the only new work)

| Already implemented by 6.1-6.6 + 4.6 (CONSUME) | Where | What 6.7 ADDS (the only new code) |
|---|---|---|
| Loot/reward `*Definition`s + repos (armor/jewelry/consumable/pickup/gold/reward-table + roll-model) | `scripts/content/definitions/*`, `scripts/content/repositories/*` | A small ADDITIVE `outcome_effect`/`explanation` on `ConsumableDefinition` (+ the three baselines) |
| `InventoryState` backpack/equipment model + `append_slot`/`make_slot` | `inventory_state.gd` | A slot-REMOVAL helper (`remove_first_slot_with_id`/`remove_slot_at` — the inverse of pickup) |
| `PickupItemCommand` (record an item, `item_gained`) | `pickup_item_command.gd` | A NEW `UseConsumableCommand` (use a backpack consumable -> record effect + remove slot) |
| `RunOrchestrator.generate_reward_offer`/`generate_passive_reward_offer` (caller-driven, run-level `streams`) | `run_orchestrator.gd` | The HEADLESS smoke harness that CALLS generate at node boundaries + drives each disposition |
| `RewardOffer` + `ResolveRewardCommand` (the generic outcome-only resolve / skip / gold) | `reward_offer.gd`, `resolve_reward_command.gd` | (nothing — consume; "skip" = `ResolveRewardCommand`) |
| `ConsumePassiveCommand` (adopt into resolver, `passive_consumed`) | `consume_passive_command.gd` | (nothing — consume in the smoke harness) |
| `DestroyPassiveCommand` (roll 70/20/10 through `rewards`, `passive_destroyed`) | `destroy_passive_command.gd` | (nothing — consume; thread `orchestrator.streams` into it) |
| `passive_reward_choice` 3-choice table (the six baseline passive ids) | `reward_table_repository.gd` | The reward-pacing SURVEY proving it is reachable + yields a 3-choice passive offer by the early-mid band |
| `DomainEvent` append-at-end deterministic SYSTEM-event pattern (`item_gained`) | `domain_event.gd` | A NEW `item_consumed` SYSTEM event (deterministic — no draw provenance) |
| The `test_run_pacing_survey.gd` structural-survey shape (Story 4.6 AC5) | `test_run_pacing_survey.gd` | The reward-pacing structural survey overlay (the FR48 early-mid build-defining-passive reachability) |

**The dev agent must NOT re-prove the offer-generate / Consume / Destroy / pickup that `test_run_orchestrator.gd`/`test_reward_offer_generate.gd`/`test_consume_passive_command.gd`/`test_destroy_passive_command.gd`/`test_pickup_item_command.gd` already cover — 6.7's NEW assertions are: the `UseConsumableCommand` + its no-mutation surface, the `InventoryState` removal helper, the additive `ConsumableDefinition` effect field, the `item_consumed` event, the headless end-to-end smoke harness threading all dispositions, the reward-pacing survey, and the AC4/AC6 tracked notes.**

### Relevant architecture patterns and constraints

- **This is the EPIC-6 CLOSING smoke-run capstone — mostly an ASSEMBLE + SURFACE + PROVE story (the 5.5 posture), with ONE new mechanic (the consumable-use command).** Almost everything the ACs need is already implemented (6.1 definitions, 6.2 inventory + pickup, 6.3 reward-offer flow, 6.4 modal contract, 6.5 Consume, 6.6 Destroy). 6.7's NEW value is the `UseConsumableCommand` (+ its tiny additive support: the consumable effect field, the inventory removal helper, the `item_consumed` event), the headless end-to-end smoke harness, the reward-pacing survey, and the consumable-value + Consume/Destroy tracked notes. The biggest risk is SCOPE CREEP into Epic-7 risk-economy (a live HP/heal/wallet mutation) and the later Epic-6 operations engine (a passive/consumable CHANGING a combat number) — resist both: v0 stays OUTCOME-RECORD-ONLY + EXPLANATION-ONLY.
- **Domain vs commands vs content vs UI placement (load-bearing):** the run model (`RunState`/`RouteState`/`InventoryState`) + the `RunOrchestrator` live in `scripts/run/`; run-domain commands (`PickupItemCommand`/`ConsumePassiveCommand`/`DestroyPassiveCommand`/`ResolveRewardCommand`/the new `UseConsumableCommand`) live in `scripts/core/commands/`; content (`ConsumableDefinition`/`RewardTableDefinition` + their repos) lives in `scripts/content/`; events live in `scripts/core/events/`. The smoke harness is a HEADLESS test (`tests/integration/reward_flow/`), NOT a scene. Keep these homes distinct.
- **The run-command idiom (4.3-ratified, used by every 6.x command): extends `game_command.gd`, takes the live `RunState` directly, reject `sequence_id <= 0` FIRST, validate-then-mutate, byte-identical no-mutation + ZERO events on every reject, the `_invalid_context(inner)` helper.** `UseConsumableCommand` follows it VERBATIM; the only structural differences are the slot-removal mutation (inverse of pickup) + the `ConsumableRepository` resolve. It draws ZERO RNG (deterministic — no roll).
- **Determinism + named-RNG: 6.7 adds NO new RNG site.** The smoke run's draws are the EXISTING ones: route generation's `map` stream (`RunStartCommand`), level generation's `level` stream (orchestrator combat), the reward GENERATE's `rewards`/`loot` stream (the 6.3 builder through `orchestrator.streams`), and the Destroy's `rewards` stream (6.6, threaded `orchestrator.streams`). `UseConsumableCommand` draws ZERO RNG. Never global/un-named RNG; no consumable RNG (the consumable-use is deterministic).
- **OUTCOME-RECORD-ONLY is the v0 reward/consumable posture (the 6.3 gold + 6.6 Destroy [Decision], now extended to consumables).** v0 has NO live HP / wallet / curse / meta domain field — so a gold reward, a Destroy outcome, AND a consumable use all RECORD their effect deterministically via the event; none mutates a felt resource. The live mutation is Epic 7 (risk-economy: gold wallet + scarce healing + curse) / Epic 8 (meta), wired off the recorded `category`/`outcome_id`/`outcome_effect` when those fields land. Inventing them here pulls Epic 7/8 forward — out of scope.
- **Headless/scene-free (the 5.5 precedent):** no required `Control`/`.tscn`/scene/HUD/presenter/art. The smoke run is proven HEADLESS (the orchestrator + the commands drive deterministically; the offers generate + resolve; the consumable is used) — NOT by building a scene. The HUD wiring of the commit-intent -> command call site + the modal scene are later HUD stories.
- **Append-only event enum + the exhaustive `expected_ids` pin:** `item_consumed` appends at the enum END after `PASSIVE_DESTROYED`, NEVER renumbering an existing member; the `_event_identifiers_are_stable_machine_ids` exhaustive map gets the new entry (the de-facto no-renumber/stable-id pin).

### Source tree components to touch

- NEW: `godot/scripts/core/commands/use_consumable_command.gd` — the use-a-backpack-consumable command (record effect + remove slot; deterministic, ZERO RNG).
- EXTEND (additive): `godot/scripts/content/definitions/consumable_definition.gd` (+`outcome_effect`/`explanation`, `validate()` rejects blanks, new params LAST); `godot/scripts/content/repositories/consumable_repository.gd` (extend the three baselines with the new fields); `godot/scripts/run/inventory_state.gd` (+`remove_first_slot_with_id`/`remove_slot_at`); `godot/scripts/core/events/domain_event.gd` (+`item_consumed` SYSTEM event, append-at-end, deterministic).
- NEW tests: `godot/tests/unit/core/test_use_consumable_command.gd`; `godot/tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd`; the reward-pacing survey (extend `godot/tests/unit/run/test_run_pacing_survey.gd` OR new `test_reward_pacing_survey.gd`). EXTEND: `test_inventory_state.gd`, `test_consumable_definition.gd`, `test_domain_event.gd`.
- CONSUME (do NOT modify): `pickup_item_command.gd`, `consume_passive_command.gd`, `destroy_passive_command.gd`, `resolve_reward_command.gd`, `run_orchestrator.gd`, `reward_offer.gd`, `reward_table_repository.gd`, `reward_table_definition.gd`, `destroy_outcome_table_definition.gd`, `passive_repository.gd`, `passive_definition.gd`, `passive_reward_modal_view_model.gd`, `passive_reward_commit_flow.gd`, `run_state.gd`, `route_node.gd`, `route_generator.gd`.

### Testing standards summary

- Headless `test_*.gd` under `res://tests/unit/...` (and `res://tests/integration/...`) extending `res://tests/unit/test_case.gd` (a minimal `assert_true`/`assert_false`/`assert_equal` harness; `run()` returns `result()`). The runner auto-discovers + sorts `test_*.gd` under `tests/unit` + `tests/integration` ONLY (NOT `tests/fixtures`, NOT `tests/headless`) and exits with the failure count. Run via PowerShell: `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`.
- Assert against RESOLVED baselines (`ConsumableRepository`/`RewardTableRepository`/`PassiveRepository`), not hardcoded literals, where practical.
- Determinism is asserted with byte-identical `JSON.stringify(...)` cross-checks of the `RunState` + the generated offers + the resolved outcomes + the inventory + the event ids (the `test_run_orchestrator.gd`/`test_reward_offer_generate.gd` determinism precedent).
- No-mutation is asserted byte-identical: a rejected `UseConsumableCommand` leaves the `RunState` (and a held external `RngStreamSet`) byte-identical with ZERO events (the `PickupItemCommand`/`ConsumePassiveCommand` no-mutation precedent).
- The FALSE-PASS guard is mandatory: grep the raw run output for `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to your new files — a reserved-method/typed-array collision can compile-fail a file yet still print PASS (the 5.1 `get_class()` lesson). Grep-confirm NO `randi`/`randf`/`RandomNumberGenerator` in the new command/definition.
- No test depends on rendering/audio/UI scenes/scene-tree-only state.

### Project Structure Notes

- Alignment: the new command goes in `scripts/core/commands/` (the `PickupItemCommand` home); the additive definition/inventory/event edits stay in their existing homes; the smoke harness goes in `tests/integration/reward_flow/` (the dir already exists); the reward-pacing survey mirrors `tests/unit/run/test_run_pacing_survey.gd`. No new top-level roots; no `data/` content (still EMPTY — an Epic-6 decision NOT taken here).
- Variance: if the reward-pacing survey is placed as a new `test_reward_pacing_survey.gd` rather than extending `test_run_pacing_survey.gd`, that is a defensible call (keeps the 4.6 survey byte-identical) — document as a `[Decision]`. If the consumable effect field is named `effect` rather than `outcome_effect`, document the name choice (RECOMMENDED `outcome_effect` for consistency with `DestroyOutcomeTableDefinition`).

### Project Context Rules

(Extracted from `project-context.md` / `AGENTS.md` — the rules that bind THIS story's implementation.)

- **Engine/language:** Godot 4.6.3 stable standard build, typed GDScript; production under `godot/`. No .NET/C#. No prototype dependency. `godot` resolves via PowerShell (run the headless suite through `powershell.exe -NoProfile -Command ...` / the project console binary `C:\Users\Rasmus\Godot_v4.6.3-stable_win64.exe\Godot_v4.6.3-stable_win64_console.exe`, NOT the Bash PATH).
- **Domain owns truth; presentation observes:** the scene-independent domain model owns run/tactical truth; the smoke run drives the domain via commands + the orchestrator; there is NO scene/HUD/view-model required (headless). `InventoryState`/`RewardOffer` have exact-key `to_dictionary()` contracts pinned by tests — the additive edits keep them byte-identical (a key never silently appears/vanishes).
- **Commands/results/events:** gameplay actions are commands that validate-before-mutate and return `ActionResult`; successful commands emit deterministic past-tense `DomainEvent`s. `UseConsumableCommand` is the ONE new command; `item_consumed` is the ONE new event (appended at the enum end, never renumbered).
- **Named RNG streams only:** `map`/`level`/`combat`/`loot`/`rewards`/`events`/`cosmetic` from the root seed; gameplay randomness uses its assigned stream; never one global RNG. 6.7 adds NO new RNG site; `UseConsumableCommand` draws ZERO RNG; the Destroy in the harness rolls through the run-level `rewards` stream (threaded from `orchestrator.streams`).
- **Rules kernel:** explicit trigger windows + stable resolver ordering + explainable output; v0 passives are EXPLANATION-ONLY (the per-effect operations + combat hook sites are the later Epic-6 operations story). 6.7 surfaces the Consume explanation via the resolver + records the Destroy/consumable outcome via events — it mutates NO combat number.
- **Save:** versioned domain snapshots, never scene nodes. The inventory + the offer + the Destroy/consumable outcome ride the FULL `RunState.to_dictionary()` only; they are DELIBERATELY NOT in the route-position 23-key `RunSnapshot` gate (the in-node reward-offer/consumed-item save is a later story). 6.7 adds NO new save key + does NOT change `to_run_snapshot_fields()` / `_allowed_run_snapshot_keys()`.
- **Difficulty is a hard non-goal:** nothing scales enemy stats/HP/damage/rewards/RNG/run-length by depth/difficulty; the reward-pacing survey MEASURES the structural surface (it does NOT add a difficulty knob or tune weights). The internal `level`/`route` difficulty bands are NOT player-selectable tiers.

## Dev Agent Record

### Context Reference

- This story file is the implementation context. Source AC: `_bmad-output/planning-artifacts/epics.md` Epic 6 Story 6.7. Epic-wide constraints folded from `_bmad-output/auto-gds/retro-notes/epic-6.md` (the 6.1-6.6 ratified conventions) + `_bmad-output/implementation-artifacts/deferred-work.md` (the forward residuals overlapping this story's area).

### Agent Model Used

Opus 4.8 (1M context) — auto-gds dev-story delegate.

### Debug Log References

- Full headless suite (PowerShell, `Godot_v4.6.3-stable_win64_console.exe`): final line `Headless tests passed.`, runner exit code `0`, ZERO FAIL. Godot `4.6.3.stable.official.7d41c59c4`.
- False-PASS guard: grep of the raw run output for `SCRIPT ERROR`/`Parse Error`/`Compile Error` -> NONE. The only stderr `ERROR:` lines are the documented-expected negative-path diagnostics from OTHER suites (the int64-overflow `run_started.root_seed` boundary; the `RouteNode parse failed` negative; the two save/settings `Parse JSON failed` negatives) — NOT failures.
- No-RNG grep on the new/extended source (`use_consumable_command.gd`, `consumable_definition.gd`, `inventory_state.gd`): NO `randi`/`randf`/`RandomNumberGenerator` — the consumable-use + the removal helper are deterministic.
- `git diff --check`: clean (no whitespace errors). `scripts/rules/{conditions,operations}` still 0 `.gd` (scope held). No `scenes/`/`assets/`/`.tscn` added (scope held).
- Two transient bugs found + fixed during the red->green loop: (1) the smoke harness compared a passive offer dict AFTER Consume had already flipped it to `resolved` (populated `selected_entry`) against a still-`pending` parallel offer — fixed by capturing the generated offer dict BEFORE the Consume mutation. (2) The pre-existing `test_consumable_repository.gd` built `ConsumableDefinition.new(...)` with 4 args in its duplicate-id + factory cases, which now fail the tightened `validate()` (blank `outcome_effect`/`explanation`) before reaching the duplicate guard — fixed by extending those constructions with the new fields (the 6.4 "tightened validate retroactively invalidates referencing suites" lesson, anticipated by the story).

### Completion Notes List

**What 6.7 delivered (the only new work — an ASSEMBLE + SURFACE + PROVE capstone with ONE new mechanic):**
- A NEW `UseConsumableCommand` (`scripts/core/commands/use_consumable_command.gd`) — the `PickupItemCommand`/`ConsumePassiveCommand` run-command idiom VERBATIM, with the differences being it resolves the consumable through an injected `ConsumableRepository` and REMOVES a backpack slot (the inverse of pickup). Constructor `_init(item_id, sequence_id = 1, consumable_repository = null)`, `command_id = &"use_consumable"`. Gate order: sequence-id -> context -> null-inventory -> id-shape -> in-inventory (`item_not_in_inventory`) -> is-consumable (`not_a_consumable`) -> resolves (`unknown_consumable`). Draws ZERO RNG.
- An additive `outcome_effect: String` + `explanation: String` on `ConsumableDefinition` (new params LAST after `tactical_identity`; `validate()` rejects a blank of each), and the three baselines extended (`minor_healing_draught` -> `restore_minor_health`, `warding_salve` -> `apply_protective_ward`, `ember_flask` -> `unleash_ember_burst`).
- An `InventoryState` slot-removal helper pair: `remove_first_slot_with_id(item_id) -> int` (removes the FIRST matching slot, returns its index or -1 on a miss) + `remove_slot_at(index) -> Dictionary` (removes + returns the slot, `{}` out-of-range). Whole-slot removal (no quantity-- in v0); never touches `equipment`/`capacity`/the key sets.
- A NEW `item_consumed` SYSTEM `DomainEvent` appended at the enum END after `PASSIVE_DESTROYED` (NEVER renumbered): factory + `_validate_item_consumed_payload` (per-field: lower_snake `item_id`, non-empty `outcome_effect`/`explanation`, non-negative `backpack_size_after`/`slot_index`) + both id maps + the exhaustive `expected_ids` pin. DETERMINISTIC — NO `roll`/`draw_index` provenance (unlike `passive_destroyed`), because Use draws ZERO RNG.
- The headless end-to-end smoke harness (`tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd`) + the reward-pacing survey (`tests/unit/run/test_reward_pacing_survey.gd`).

**[Decision]s recorded:**
- **OUTCOME-RECORD-ONLY consumable-use** (the EXACT parallel of the 6.3 gold + 6.6 Destroy decisions): v0 has NO live HP / wallet / curse domain field (grep-confirmed on `scripts/run/` — the run carries `starting_kit.baseline_hp`, a fixed kit value, NOT a mutable current-HP, and no gold/wallet field). `UseConsumableCommand` RECORDS the consumable's `outcome_effect`/`explanation` via the `item_consumed` event; it mutates no HP/wallet/curse because none exists. The live heal/cure/buff MUTATION is the Epic-7 risk-economy wire-off seam (off the recorded `item_consumed` effect).
- **`UseConsumableCommand` is CALLER-DRIVEN** — NO orchestrator convenience method was added (recommended NO, exactly as 6.3/6.5/6.6 left their commands caller-driven). The smoke harness / a later HUD story constructs it directly.
- **The additive `ConsumableDefinition.outcome_effect`/`explanation`** mirror `DestroyOutcomeTableDefinition`'s `effect`/`explanation` pair; the name `outcome_effect` (NOT bare `effect`) is the recommended consistency choice. New params LAST so the positional baseline `ConsumableDefinition.new(...)` calls extend, not reorder.
- **The `InventoryState` removal helper is whole-slot** (no quantity-- in v0 — no item ships with quantity > 1; the `quantity` field stays the forward-shape no-op it is).
- **The `item_consumed` event is a DETERMINISTIC SYSTEM event** — no draw provenance (no `roll`/`draw_index`).
- **AC1 "skip" = the generic `ResolveRewardCommand`** outcome-only resolve (the explicit decline path) — no new "skip" command invented. The smoke harness drives every disposition (pickup / skip / consume / destroy / gold-resolve / use-consumable) against a real resolving command.
- **The reward-pacing survey is placed as a NEW `test_reward_pacing_survey.gd`** (NOT an extension of `test_run_pacing_survey.gd`), keeping the 4.6 route-pacing survey byte-identical. The survey MEASURES + records the structural pacing surface — it makes NO route-generator / depth-band / reward-weight / passive-pool change.
- **The smoke run is HEADLESS** (no HUD/scene/`.tscn`/presenter/icon ART) — the 5.5 "playable proven headless" precedent.

**AC2 reward-pacing structural-survey results (recorded):** the `passive_reward_choice` table is present in the validated `RewardTableRepository`, validates, and declares `choice_count = 3`. Its six entries are all `passive`-category and reference REAL `PassiveRepository.BASELINE_PASSIVE_IDS` (cross-checked + each resolves); the table references 6 distinct baseline passives (comfortably over the 3-choice floor). A `generate_passive_reward_offer(&"passive_reward_choice")` through the run-level `rewards` stream yields a 3-choice offer of DISTINCT real adoptable baseline ids, deterministically (the same seed reproduces the offer byte-identically; verified across seeds 0/42/777/2026). The early band (RouteNode.depth ratio <= 0.34 of the last non-boss depth) is reachable on every survey seed — the last non-boss depth is a CONSTANT 6 (the constant-8-tier route), so the early band is depths 0-2 and the depth-0 start is always in it, so an early-mid build-defining passive offer is always reachable. **The constant-8-tier route depth is RE-RECORDED as the known 4.2/4.6 Epic-10 pacing-tuning characteristic — NOT fixed here.** The survey assertions are STRUCTURAL (table/weights/pool), so a pacing failure names the table/weights/pool, not a fixture poke.

**AC4 consumable tracked NOTE (intended use case + semi-rare frequency band + observed value) — v0 is OUTCOME-RECORD-ONLY:**
- `minor_healing_draught` — intended use: restore a little health (the common field draught; `outcome_effect: restore_minor_health`). Frequency band: **common** (rarity `common`; weight 4 of 19 in `standard_combat_reward` ~= 21% — the most common loot category alongside gold). The "always-available" baseline heal.
- `warding_salve` — intended use: a protective ward for a hard fight (`outcome_effect: apply_protective_ward`). Frequency band: **semi-rare / uncommon** (rarity `uncommon`; NOT in `standard_combat_reward`, referenced only as the structural uncommon tier — a save-it-for-a-hard-fight item). Worth saving.
- `ember_flask` — intended use: an offensive burst of bottled ember (`outcome_effect: unleash_ember_burst`). Frequency band: **rare** (rarity `rare`; weight 3 of 12 in `elite_combat_reward` ~= 25% of the ELITE table only, absent from the standard table — genuinely worth using, gated behind elite nodes). The scarcity is modeled as DATA (the `rarity` tier + the reward-table weight relative to the table total IS the observed-frequency band — the Story-6.1 scarcity-as-data posture).
- **Observed/simulated value:** the smoke harness USES `minor_healing_draught` (and `warding_salve` in the determinism drive) end-to-end via `UseConsumableCommand` — the `item_consumed` event records the effect + the slot is removed. **Flagged honestly:** the felt value at this milestone is the RECORDED effect + the slot consumption (the consumable leaves the backpack), NOT a mutated HP bar — that mutation is Epic 7's risk-economy state. The consumable pool was NOT expanded (FR53 pool growth is a later Epic-6/10 content story).

**AC6 Consume-vs-Destroy tracked NOTE (a tester can explain the gain/give-up for both) — v0 is EXPLANATION-ONLY:** the smoke harness makes the contrast OBSERVABLE against a real resolved passive offer. A **Consume** adopts the chosen passive into the run's `RulesResolver` (`registered_passive_count` increments) — what is GAINED is surfaced by the resolver's `explain(window)` line plus the 6.4 modal `consume_text`/`exact_mechanical_effects` (e.g. for `warrior_unbreakable_guard`: "Consume to carry the guard for the rest of the run as standing damage reduction against incoming blows"). A **Destroy** rolls the 70/20/10 outcome through the `rewards` stream and does NOT adopt the passive (the resolver is UNTOUCHED) — what is GIVEN UP (the passive) and what is GOT (the rolled outcome) is surfaced by the `passive_destroyed` event's `outcome_category`/`outcome_id`/`outcome_effect`/`explanation` plus the 6.4 `destroy_text`/`has_unknown_consequences`/`consequences_text` (e.g. "Destroy to release the oath, cleansing a measure of corruption from the hero"). So a tester CAN attach an "I gained X by consuming / I gave up the passive but got Z by destroying" note to a real resolved offer. **Content-quality flag (NOT re-authored here):** the six baseline passives' 6.4 `consume_text`/`destroy_text` is evocative and explains the gain/give-up adequately for v0; the felt DIFFERENCE is the explanation surface + the recorded outcome (NOT a mutated combat number — that is Epic-6 operations / Epic-7 economy). No salvage-y/flat text required re-authoring at this milestone; any deeper comprehension pass is the later passive-pool / 10.4 comprehension story (flagged, not fixed).

**Scope held:** NO change to/fork of `ConsumePassiveCommand`/`DestroyPassiveCommand`/`ResolveRewardCommand`/`PickupItemCommand`/the `RunOrchestrator` reward-generate path; NO live HP/wallet/heal/cleanse/curse/Oath-Shards/meta mutation; NO per-effect operation engine (`scripts/rules/{conditions,operations}` still EMPTY); NO new passive POOL / consumable POOL authoring; NO re-author of the 6.4 modal fields; NO change to `RewardOffer`/`RewardTableDefinition`/`DestroyOutcomeTableDefinition`/the 23-key `RunSnapshot` gate/`_allowed_run_snapshot_keys()`/`RngStreamSet.required_streams()`/fingerprints; NO route-position save mutation (inventory + offer + outcome ride the FULL `RunState.to_dictionary()` only — confirmed absent from `to_run_snapshot_fields()`); NO new RNG site (Use draws ZERO RNG; Destroy in the harness rolls the existing `rewards` stream via `orchestrator.streams`); NO `.tscn`/scene/presenter/asset.

**Forward residuals (recorded for the epic-6 retrospective / `deferred-work.md` — `[Review][Decision]`/`[Review][Defer]`):**
- (owner: Epic 7 risk-economy) The live HP/heal/cure/ward/wallet/curse consumable MUTATION that `item_consumed` RECORDS but does not yet apply — wired off the recorded `item_consumed` effect when the domain field lands.
- (owner: the later Epic-6 operations story) The per-effect passive/consumable OPERATION engine + combat HOOK sites (`scripts/rules/{conditions,operations}` stay empty) — 6.7 surfaces EXPLANATIONS + records effects only.
- (owner: a later HUD/orchestrator story) The HUD wiring of the commit-intent -> Consume/Destroy/Use command call site + the orchestrator auto-generate-on-complete resolution policy (the SAME residual 6.3/6.5/6.6 left — generate stays caller-driven).
- (owner: the later in-node-save / live-resume story) The in-node reward-offer / consumed-item / consumed-passive route-position SAVE stays deferred (the offer + inventory ride the FULL run dict only, not the 23-key route-position gate).
- (owner: later Epic-6/10 content stories) The broader 20-30 passive POOL (FR46) + the consumable-pool growth (FR53).
- (owner: Epic 10) The constant-8-tier route-depth pacing tuning (re-recorded by the survey, NOT fixed).
- 6.7 forks no repo + draws no new RNG (the duplicate-id all-repos guard is the 6.1 central guard; the level-gen-injection half of the inert run-level `RngStreamSet` stays owned by a later level-gen-RNG story).

### File List

NEW:
- `godot/scripts/core/commands/use_consumable_command.gd` — the use-a-backpack-consumable command (record effect + remove slot; deterministic, ZERO RNG).
- `godot/tests/unit/core/test_use_consumable_command.gd` — the command's valid + no-mutation/no-RNG coverage.
- `godot/tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd` — the headless end-to-end loot/passive build smoke harness (AC1/AC2/AC5 + the consumable-use + determinism + invalid-surface).
- `godot/tests/unit/run/test_reward_pacing_survey.gd` — the AC2 reward-pacing structural survey (the FR48 early-mid build-defining-passive reachability).

EXTENDED (additive):
- `godot/scripts/content/definitions/consumable_definition.gd` — +`outcome_effect`/`explanation` (new params LAST), `validate()` rejects a blank of each.
- `godot/scripts/content/repositories/consumable_repository.gd` — the three baselines extended with the new fields.
- `godot/scripts/run/inventory_state.gd` — +`remove_first_slot_with_id`/`remove_slot_at` (the inverse of `append_slot`).
- `godot/scripts/core/events/domain_event.gd` — +`item_consumed` SYSTEM event (enum append + factory + `_validate_item_consumed_payload` + the `_validate_payload_for_event` arm + both id maps).
- `godot/tests/unit/content/test_consumable_definition.gd` — +the additive-field validate/round-trip/blank-reject + baseline coverage.
- `godot/tests/unit/content/test_consumable_repository.gd` — the duplicate-id + factory cases extended with the new fields (the tightened-validate referencing-suite fix).
- `godot/tests/unit/run/test_inventory_state.gd` — +the removal-helper coverage (removes the right slot, no-op on a miss, first-match-only, exact-key contracts byte-identical after a removal).
- `godot/tests/unit/core/test_domain_event.gd` — +the `item_consumed` round-trip + per-field malformed negatives + the exhaustive `expected_ids` entry.

CONSUMED (NOT modified): `pickup_item_command.gd`, `consume_passive_command.gd`, `destroy_passive_command.gd`, `resolve_reward_command.gd`, `run_orchestrator.gd`, `reward_offer.gd`, `reward_table_repository.gd`, `reward_table_definition.gd`, `destroy_outcome_table_definition.gd`, `passive_repository.gd`, `passive_definition.gd`, `run_state.gd`, `route_node.gd`, `route_generator.gd`.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-29 | 0.1 | Story context created (gds-create-story) — Epic-6 closing loot/passive build smoke-run capstone; AC + interpretation notes + tasks grounded against the shipped 6.1-6.6 code + the epic-6 retro-notes + the overlapping deferred-work residuals. | gds-create-story |
| 2026-06-29 | 1.0 | Implemented (gds-dev-story) — NEW `UseConsumableCommand` (deterministic, ZERO RNG, remove-slot inverse of pickup) + additive `ConsumableDefinition.outcome_effect`/`explanation` (+ three baselines) + `InventoryState.remove_first_slot_with_id`/`remove_slot_at` + the `item_consumed` SYSTEM event (append-at-end, deterministic, exhaustive id-map pin) + the headless end-to-end loot/passive build smoke harness + the AC2 reward-pacing structural survey. All 6 ACs met; full headless suite green (Godot 4.6.3, "Headless tests passed.", exit 0, ZERO FAIL); false-PASS grep clean; no-RNG grep clean; `git diff --check` clean; scope held (no `RewardOffer`/`RunState`/23-key-gate/fingerprint change, no scene/asset, `scripts/rules/{conditions,operations}` still empty). AC4/AC6 tracked notes + survey results + forward residuals recorded in Completion Notes. Status -> review. | gds-dev-story |
| 2026-06-29 | 1.1 | Code review (gds-code-review, Round 1 of 3) — verdict **Approve**. 0 Critical / 0 High / 0 Med / 3 Low. Full suite re-run green (114 PASS / 0 FAIL, exit 0). Adversarial three-layer pass (Blind Hunter / Edge Case Hunter / Acceptance Auditor): all 6 ACs literally satisfied; `UseConsumableCommand` mirrors the `ConsumePassiveCommand`/`PickupItemCommand` idiom verbatim; `item_consumed` event wiring complete + the exhaustive no-renumber id-map pin present; additive shape changes preserve every exact-key contract; scope held; no live-HP/wallet/route-position-save leak. 3 Low test-coverage/content-pacing observations logged as `[Review][Defer]`; architecture seams confirmed as `[Review][Decision]`. No blocking findings. | gds-code-review |
| 2026-06-29 | 1.2 | External re-review fix (commit `db64298`, test-only) — addresses 3 P2 test-rigor findings from an external (Codex) review of PR #33 on the headless smoke test: (1) prove the pickup disposition against a GENERATED backpack offer entry resolved via `ResolveRewardCommand` (`applied_to_backpack` + backpack +1), not a hard-coded id; (2) drive a REAL `resolve_current_node()` node-completion before generating rewards, asserting `cleared_node_ids` grew by one; (3) make the skip/decline a GENUINE no-apply resolve of an offered gold entry (`applied_to_backpack` false, backpack unchanged). NO production code changed. | external (Codex) + fix |
| 2026-06-29 | 1.3 | Code review (gds-code-review, Round 2 of 3 — SECONDARY alternate-model re-review of the external-review fix) — verdict **Approve**. 0 Critical / 0 High / 0 Med / 0 Low; 0 open `[Review][Decision]`; 0 new `[Review][Defer]`. Independently verified each of the 3 external findings is genuinely resolved (test read against production source), adversarially re-checked the new test for regression/weakened-assertions/non-determinism/flakiness/non-termination/false-PASS (all clean; bounded seed loops probe-proven to find their target — backpack 11/16, gold 5/16 / 2/10 — and they fail loud on a miss, never silent-pass or loop forever), confirmed NO production code changed (only the one test file in the fix diff), and re-ran the full headless suite green (Godot 4.6.3, exit 0, `Headless tests passed.`, false-PASS grep clean). | gds-code-review |

### Review Findings

**Round 1 of 3** — gds-code-review, 2026-06-29. Verdict: **Approve**. Severity counts: 0 Critical / 0 High / 0 Med / 3 Low. Scope: this branch's diff vs `main` (merge-base `70f0757`) restricted to `godot/**` (12 files: 1 new command, 4 additive production edits, 7 new/extended test files); excludes `_bmad`/`_bmad-output`/caches. Spec: this story file. Full headless suite re-run by the reviewer: `Headless tests passed.`, exit 0, 114 PASS / 0 FAIL; `git diff --check` clean; no `randi`/`randf`/`RandomNumberGenerator` in the new command; `scripts/rules/{conditions,operations}` still 0 `.gd`; no scene/asset/`.tscn`; `to_run_snapshot_fields()` confirmed to NOT carry `inventory`/`pending_reward_offer`. The three adversarial layers found no Critical/High/Med correctness defect, no regression, and confirmed all 6 ACs are literally satisfied.

What was verified clean (no finding): the run-command idiom is mirrored verbatim from `ConsumePassiveCommand`/`PickupItemCommand` (sequence-id-first, `_invalid_context` copied verbatim, validate-then-mutate, byte-identical no-mutation + ZERO events on every reject); the gate order (sequence -> context -> null-inventory -> id-shape -> in-inventory -> is-consumable -> resolves) is correct and the fail-closed `unknown_consumable` repository gate never records a null; the `item_consumed` SYSTEM event is fully wired (enum-append after `PASSIVE_DESTROYED`, `EVENT_ID_*` const, factory with defensive normalization + NO draw provenance, `_validate_item_consumed_payload` per-field, `_validate_payload_for_event` arm, `id_for_type`/`type_for_id`, NOT in `_event_requires_actor`, AND the exhaustive `expected_ids` round-trip pin); the `InventoryState` removal helpers mutate only the ordered `backpack` (never `equipment`/`capacity`/the key sets) and keep the exact-key `to_dictionary()`/`copy()`/`try_from_dictionary` contracts byte-identical after a removal; the additive `ConsumableDefinition` fields are new-params-LAST so positional baseline ctors extend not reorder, `validate()` rejects each blank per-field, and all three baselines were extended (so `create_baseline_repository()` stays non-null); the smoke-harness determinism cross-checks are sound because `RewardOffer.to_dictionary()` stores only plain strings/ints + a decimal-string `state_after` (no object refs); the AC4 reward-table weight arithmetic checks out (`standard_combat_reward` total 19: `minor_healing_draught` w4 ~21%, gold w5 highest; `elite_combat_reward` total 12: `ember_flask` w3 ~25%).

- [Review][Defer] Low — `warding_salve` (the uncommon baseline consumable) is NOT weighted into ANY reward table (`standard_combat_reward`/`elite_combat_reward` reference only `minor_healing_draught` and `ember_flask`). In v0 it is obtainable ONLY via a direct `PickupItemCommand` (which the smoke harness uses); a real run cannot currently roll it from a reward offer. The AC4 tracked note frames it as "the structural uncommon tier — a save-it-for-a-hard-fight item", which is accurate, but the practical consequence is that its "semi-rare frequency band" is currently zero-from-rewards, not merely rare. NOT a code defect and explicitly inside the story's deferred consumable-frequency tuning (Epic 10 / a later Epic-6 content story); flagged so the frequency-tuning pass either weights `warding_salve` into a table or records the deliberate omission. (Originating: code review of 6-7, Round 1, 2026-06-29.)

- [Review][Defer] Low — the `_event_identifiers_are_stable_machine_ids` pin iterates `expected_ids.keys()` and round-trips each listed member, but there is NO assertion tying the map's size to the `DomainEvent.Type` enum member count. So a FUTURE appended event that the author forgets to add to `expected_ids` would be silently un-pinned (the test would still pass). For 6.7 this is moot — `ITEM_CONSUMED` IS in the map and round-trips — but the guard would be stronger with an enum-exhaustiveness assertion (e.g. assert `expected_ids.size() == Type.size() - 1` to account for `UNKNOWN`, or iterate the enum and assert each non-`UNKNOWN` member is a key). Test-hardening only; not blocking. (Originating: code review of 6-7, Round 1, 2026-06-29.)

- [Review][Defer] Low — the no-RNG guard test `_use_draws_no_rng_on_success_and_reject` exercises a SUCCESS path and ONE reject path (`item_not_in_inventory`, via `&"not_in_backpack"`). It does not re-assert the held `RngStreamSet` is byte-identical across the OTHER reject branches (`invalid_event_sequence_id`/`invalid_context`/`invalid_item_id`/`not_a_consumable`/`unknown_consumable`). Those branches return before any conceivable draw and the command holds no `RngStreamSet` at all (verified by inspection + the false-PASS grep), so the guarantee holds — but the per-branch no-RNG assertion is only spot-checked, not exhaustive. Low test-completeness nit; the determinism is otherwise proven end-to-end by the smoke harness. Not blocking. (Originating: code review of 6-7, Round 1, 2026-06-29.)

- [Review][Decision] The consumable-use is OUTCOME-RECORD-ONLY — `UseConsumableCommand` RECORDS `outcome_effect`/`explanation` via `item_consumed` and mutates no HP/wallet/curse because no such live domain field exists in v0 (grep-confirmed: nothing matching `current_hp|wallet|gold_balance|hp_remaining` under `scripts/run/`). The live heal/cure/ward/burst MUTATION is the Epic-7 risk-economy wire-off seam, keyed off the recorded effect. This is a deliberate human-confirmable design call (the exact parallel of the 6.3 gold + 6.6 Destroy outcome-only decisions), not a gap. Confirmed correct for this milestone. **— RESOLVED 2026-06-29 (Auto-GDS, user-accepted): precedent-confirmed per merged 6.3/6.5/6.6; no code change.**

- [Review][Decision] `UseConsumableCommand` is CALLER-DRIVEN — no `RunOrchestrator` convenience method was added (the smoke harness + a later HUD story construct it directly), exactly as 6.3/6.5/6.6 left their commands. The HUD wiring of an in-flight "use this item" intent -> the command call site stays deferred to the later HUD story. Confirmed an intentional architecture posture, not an omission. **— RESOLVED 2026-06-29 (Auto-GDS, user-accepted): precedent-confirmed per merged 6.3/6.5/6.6; no code change.**

- [Review][Decision] The inventory + the pending reward offer + the Destroy/consumable outcome ride the FULL `RunState.to_dictionary()` ONLY and are DELIBERATELY absent from the route-position 23-key `RunSnapshot` gate (`to_run_snapshot_fields()`) — verified. The in-node reward-offer/consumed-item route-position SAVE is the SAME tracked forward residual 6.3/6.5/6.6 deferred (a later in-node-save / live-resume story). Confirmed deliberate. **— RESOLVED 2026-06-29 (Auto-GDS, user-accepted): precedent-confirmed per merged 6.3/6.5/6.6; no code change.**

**Round 2 of 3** — external re-review (gds-code-review, SECONDARY alternate-model diversity pass; reviewer model Opus 4.8 (1M), auto-gds delegate), 2026-06-29. Verdict: **Approve**. Severity counts: 0 Critical / 0 High / 0 Med / 0 Low. **0 open `[Review][Decision]`. 0 new `[Review][Defer]`.** Scope: ONLY the integrity of the external-review (Codex, PR #33) test-rigor fix — commit `db64298` (test-only: `godot/tests/integration/reward_flow/test_loot_passive_build_smoke_run.gd`, +131/-52). Round 1 (Approve) is unaltered. This round re-verified each of the 3 external findings against production source, adversarially re-checked the new test logic for regression/weakened-assertions/non-determinism/flakiness/non-termination/false-PASS, confirmed NO production code changed, and independently re-ran the full headless suite (Godot 4.6.3, exit 0, `Headless tests passed.`, false-PASS grep `SCRIPT ERROR`/`Parse Error`/`Compile Error` = NONE; the only `ERROR:` lines are the documented-expected negative-path diagnostics — int64-overflow `root_seed` x2, `RouteNode parse failed: invalid_node_type`, two save/settings `Parse JSON failed`).

Each external finding INDEPENDENTLY confirmed GENUINELY resolved (read against production, not taken at comment face value):
- **(1) Pickup proven against a GENERATED backpack offer.** `_resolve_first_offered_backpack_entry()` generates a real `standard_combat_reward` offer, gates on `InventoryState.is_backpack_category(...)` (skips gold), and resolves THAT offered entry's own `category`/`content_id` via `ResolveRewardCommand` — which composes `PickupItemCommand` for a backpack category (`resolve_reward_command.gd:115-129`, `applied_to_backpack = item_gained_event != null` at :154). Asserts `applied_to_backpack` true + `backpack_after == backpack_before + 1` + `item_gained` emitted. NO hard-coded id drives the disposition (Round 1's removed code picked up a literal `&"minor_healing_draught"` via a direct `PickupItemCommand` — the external complaint; now strictly stronger). An offer->pickup regression would flip `applied_to_backpack`/`backpack_after` and fail loud.
- **(2) A REAL node-completion is driven before reward generation, with a cleared-node assertion.** The harness captures `cleared_node_ids.size()`, calls `orchestrator.resolve_current_node()` (`run_orchestrator.gd:157` — the depth-0 start is a combat node routing enter -> `LevelGenerator.generate` -> auto-resolve -> `NodeExitCommand` clear), asserts it succeeded, asserts `not run_completed`, asserts `cleared_node_ids.size() == cleared_before + 1`, and asserts the cleared id IS the start node. The node-completion->reward boundary is genuinely exercised — the +1 assertion fails if the node is not actually cleared. No longer asserted-but-skipped.
- **(3) Skip/decline is a GENUINE no-apply resolve.** `_resolve_first_offered_gold_entry()` finds an offered GOLD entry and resolves it; gold is NOT a backpack category, so `resolve_reward_command.gd` takes the no-op gold branch (:130-131), `item_gained_event == null` -> `applied_to_backpack` false, no backpack mutation. Asserts `applied_to_backpack` FALSE + `backpack_after == backpack_before` + `reward_resolved` emitted + `is_resolved`. Consistent with `_gold_offer_resolves_outcome_only`. No longer a backpack resolve that silently applies.

Adversarial re-check of the NEW test logic (all clean):
- **Non-termination / generate-until: NONE.** Both bounded helpers iterate a FIXED 16-element `OFFER_SAMPLE_SEEDS` array exactly once per element (`for seed_value in OFFER_SAMPLE_SEEDS`) — no `while true`, no generate-until-target. On exhaustion they return `{"found": false}` and the call site asserts `found` true => fails LOUD (never silent-pass). `_gold_offer_resolves_outcome_only` similarly iterates a fixed 10-seed list and ends with `assert_true(false, ...)` if no gold surfaces.
- **Bounded-loop target GUARANTEED to be found (probe-proven, temp probe removed after).** A headless probe enumerated every seed's draw through the same `RunOrchestrator.start -> generate_reward_offer(standard_combat_reward)` path: backpack entries surface on 11/16 `OFFER_SAMPLE_SEEDS` (first @seed 1); gold surfaces on 5/16 `OFFER_SAMPLE_SEEDS` (first @seed 5) and on 2/10 of the `_gold_offer_resolves_outcome_only` list (first @seed 5). The draw is a pure deterministic function of (seed, fixed table) through the named `rewards` stream, so these counts are fixed forever — the loops cannot miss on a re-run; even a miss fails loud, not silently.
- **Hidden non-determinism / RNG drift: NONE.** Every offer draws through `orchestrator.streams` (the run-level `RngStreamSet`) via `RewardOfferBuilder`; `UseConsumableCommand` draws ZERO RNG (pure lookup+removal+record); the Destroy in `_drive_full_loop` threads `orchestrator.streams` (named-stream rule honored). No `randi`/`randf`/`RandomNumberGenerator` in the test.
- **Weakened assertions / regression: NONE — strictly strengthened.** The fix only touched the header, the `InventoryState` preload, `_loot_offer_can_be_picked_up_then_skipped_against_the_same_run`, and the two new helpers; the other six test functions (passive consume/destroy, gold-resolve, consumable-use, determinism drive, divergent-seed, invalid/no-mutation) are byte-unchanged. The determinism cross-checks remain sound (`RewardOffer`/`RunState` `to_dictionary()` serialize plain strings/ints, no object refs).
- **No production code changed: CONFIRMED.** `db64298` touches ONLY the smoke test; `ec354f4..HEAD` shows only the smoke test; the branch-vs-`main` production files are the merged-and-Round-1-reviewed work, byte-identical.

_No items deferred from this review round._ The fix is test-only, resolves all 3 external findings, and surfaces no forward residual beyond those already recorded under Round 1 + the cross-story ledger. (Originating: external re-review of 6-7, Round 2, 2026-06-29.)
