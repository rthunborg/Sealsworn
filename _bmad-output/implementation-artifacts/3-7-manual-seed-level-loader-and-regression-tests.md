---
created: 2026-06-17
source_story_key: 3-7-manual-seed-level-loader-and-regression-tests
---

# Story 3.7: Manual Seed Level Loader and Regression Tests

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want to enter or reuse seeds for replay, practice, sharing, and debugging,
so that generated levels can be reproduced without granting meta progression.

## Acceptance Criteria

1. Given a manual seed is entered for a level-generation test, when the level loads, then the same seed and recipe produce the same generated level, and the generated level reports that the run is manual-seed ineligible for meta progression.
2. Given a seed is invalid or malformed, when the loader parses it, then it returns a clear validation error, and no generation starts from ambiguous seed input.
3. Given seed regression tests are run, when approved seeds are generated in batch, then their validation status and compact fingerprints are stable, and failures report seed, recipe, phase, and reason.
4. Given an internal tester loads generated Small and Medium levels from approved seeds, when they complete a short tactical pass, then they record whether each level has at least one meaningful movement, line-of-sight, or risk-positioning decision, and bland or unfair seeds are preserved for tuning rather than silently discarded.

[Source: epics.md Epic 3 / Story 3.7]

## Tasks / Subtasks

- [x] 3.7.1 Confirm the Epic 3 boundary and write FAILING tests FIRST (TDD red phase). (AC: 1-4)
  - [x] Verify in `_bmad-output/implementation-artifacts/sprint-status.yaml` that `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-...: done`, `3-3-...: done`, `3-4-...: done`, `3-5-...: done`, `3-6-generator-validation-and-bounded-retry: done`, `epic-3: in-progress`, and this story (`3-7-manual-seed-level-loader-and-regression-tests`) is `ready-for-dev`. If any earlier Epic 1/2/3.1-3.6 status regressed, STOP and restore the boundary first. This story is NOT the first in epic 3, so leave `epic-3: in-progress` (orchestrator owns epic status). [VERIFIED: boundary intact; working tree clean at start.]
  - [x] Confirm the working tree is clean or that dirty files are intentional user work; preserve unrelated changes. The untracked orchestrator-owned `_bmad-output/auto-gds/` directory is expected and is NOT your change. [VERIFIED: `git status --short` clean at start.]
  - [x] Add focused tests FIRST. New file `godot/tests/unit/generation/test_manual_seed_loader.gd` covering AC1 (same-seed-same-level + manual-seed meta-ineligibility) and AC2 (invalid/malformed seed -> clear error, no generation). New file `godot/tests/unit/generation/test_seed_batch_regression.gd` covering AC3 (batch validation-status + compact fingerprints stable across approved Small + Medium seeds; failure message carries seed + recipe + phase + reason). Add the AC4 preserved-seed catalog as data the batch test reads (the bland/unfair seeds are kept, not discarded). Watch for them FAILING before implementation. [Red phase confirmed: loader test parse-failed on the missing `ManualSeedLoader` type.]
  - [x] Reuse existing primitives EXACTLY (do NOT fork): `LevelGenerator.generate(request, recipe_repository, enemy_repository)` (the full retry pipeline from 3.6 — the loader CALLS it, it does not re-implement generation), `GenerationRequest` (`level_seed()`, `validate()`), `GenerationResult` (`ok`/`error`, String `seed`, the PHASE constants, `succeeded`/`failed_phase`/`error_code`/`reason`/`diagnostics`/`payload`), `LevelRecipeRepository.create_baseline_repository()`/`recipe_ids()`/`get_recipe()`, `EnemyRepository.create_baseline_repository()`, `SmallLevelLayoutGenerator.fingerprint(layout)` / `MediumLevelLayoutGenerator.fingerprint(layout)` (the existing TERRAIN fingerprint — reuse, do NOT invent a second format), the `is_manual_seed` + `meta_progression_eligible` split on `RunSnapshot`, and the int64-from-value seed-decode discipline from `RngStreamSet._int64_from_value`. No new RNG stream, result type, board parser, snapshot model, or second fingerprint format. [DONE — all reused; no fork.]
  - [x] Did NOT (scope guard): added no `RewardTableDefinition`/repository/loot content (Epic 6); placed no player/hero entity (Epic 4); realized no `door`/`affinity_placeholder`/`risky_side_branch` wrinkle and no real affinity rules (Epic 7); added no enemy AI/turn/hazard-damage/combat/rules-kernel behavior; built no HUD/scene/UI seed-entry widget (this is the DOMAIN loader + headless harness — UI seed entry is a later UX story); touched no commands/live tactical truth/run autosave write/settings/meta; did NOT modify `LevelGenerator`'s generation logic, the validator, the generators, the placer, the baseline recipes/enemies, or `GenerationRequest`/`GenerationResult`/`RunSnapshot` contracts; did NOT re-pin the 3.2/3.3 terrain seed-regression `APPROVED_FINGERPRINTS`; added no dependency or test framework. [VERIFIED — only 4 new untracked files; no existing file modified.]

- [x] 3.7.2 Implement the manual-seed loader as a pure domain service. (AC: 1, 2)
  - [x] New file `godot/scripts/generation/level/manual_seed_loader.gd` — `class_name ManualSeedLoader extends RefCounted`. Pure data-layer service (NOT a Node, NO scene/UI/audio/`user://` dependency, NFR14). Two responsibilities: (a) PARSE + VALIDATE a raw seed input into a normalized non-negative `root_seed` (AC2); (b) BUILD a `GenerationRequest` flagged manual-seed and run it through `LevelGenerator.generate(...)`, surfacing manual-seed meta-ineligibility on the result (AC1). It does NOT generate layouts itself — it orchestrates the existing pipeline.
  - [x] AC2 seed parsing: implement `parse_seed(raw: Variant) -> ActionResult` returning `ActionResult.ok([], {"root_seed": <int>})` on success or `ActionResult.error(<lower-snake code>, {...compact reason...})` on failure. Accept a `String` decimal seed (the share/replay format) and a plain `int`; reject empty/blank, non-integer text (e.g. "abc", "12x"), and anything ambiguous — mirror `RngStreamSet._int64_from_value`'s String/int discipline (use `String.is_valid_int()` + `to_int()`; full-64-bit decimal strings must round-trip losslessly per the int64-string rule). On rejection return a clear stable code (e.g. `empty_seed` / `non_integer_seed`) so AC2's "no generation starts from ambiguous seed input" holds — the caller MUST NOT proceed to `generate` on an error result. [DONE — `empty_seed` / `non_integer_seed` (also for floats) / `unsupported_seed_type`.]
  - [x] AC2 + seed-sign provenance (carried from the 3.1 review Decision): `GenerationRequest.validate()` REJECTS `root_seed < 0` (`invalid_generation_request` on field `root_seed`). A legitimate full-64-bit shared seed can decode NEGATIVE as a signed int64. So `parse_seed` MUST NORMALIZE a parsed seed to non-negative BEFORE building the request (e.g. mask to the non-negative range `& 0x7fffffffffffffff`, or reinterpret as unsigned) — document the exact normalization and pin it with a regression so a negative-decoding seed loads instead of being silently rejected by the request validator. Do NOT change `GenerationRequest.validate()`'s non-negative rule; normalize at the loader boundary. [DONE — `NON_NEGATIVE_MASK = 0x7fffffffffffffff`, pinned by a regression feeding `-4523566890123456789`.]
  - [x] AC1 load + manual-seed eligibility: implement `load_level(raw_seed: Variant, recipe_id: StringName, recipe_repository, enemy_repository) -> Dictionary` (or a small typed result) that: parses the seed (return the parse error verbatim if invalid), builds a `GenerationRequest` for the given `recipe_id`/`size_class` with the normalized seed, calls `LevelGenerator.generate(request, recipe_repository, enemy_repository)`, and returns the `GenerationResult` PLUS an explicit manual-seed eligibility signal — `is_manual_seed: true` and `meta_progression_eligible: false` (the EXISTING `RunSnapshot` split-field vocabulary; do NOT invent a new flag and do NOT re-add the ambiguous `manual_seed_eligible_for_progression` key that the save layer deliberately dropped). AC1 verbatim: "the generated level reports that the run is manual-seed ineligible for meta progression." [DONE — eligibility reported on every return; dropped key never reintroduced.]
  - [x] AC1 determinism: the SAME `(raw_seed, recipe_id)` MUST produce the SAME `GenerationResult` (same payload terrain fingerprint, same `attempts`, same `validated`) every time — this is just the 3.6 pipeline determinism surfaced through the loader. Assert two loads of the same seed are byte-identical (terrain fingerprint + diagnostics). Use the existing `level`-stream-only generation (no new randomness in the loader). [DONE — determinism asserted via payload terrain fingerprint + attempts + validated.]

- [x] 3.7.3 Implement the batch seed-regression harness + the preserved approved-seed catalog. (AC: 3, 4)
  - [x] New file `godot/tests/unit/generation/test_seed_batch_regression.gd` — drives a CURATED set of approved seeds for BOTH baseline recipes (`small_combat_basic`, `medium_combat_basic`) through `LevelGenerator.generate(...)` (the FULL pipeline, NOT `generate_layout` directly — this is the integration-level batch that the 3.6 deferred Low asked for) and asserts, per seed: (a) `validation status` is stable (the approved seeds all PASS validation on attempt 0 -> `succeeded == true`, `diagnostics.validated == true`, `diagnostics.attempts == 1`); (b) the compact TERRAIN fingerprint (`<Size>LevelLayoutGenerator.fingerprint(<payload-derived layout>)` or the existing fingerprint over the generated terrain) matches a pinned value. A drift fails with the FAILING seed + recipe + phase + reason in the assert message (AC3 verbatim: "failures report seed, recipe, phase, and reason"). [DONE — fingerprint reconstructed from the generate payload board.]
  - [x] AC3 fingerprint source: the batch harness pins the SAME terrain fingerprints already pinned in `test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` for the OVERLAPPING seeds (do NOT diverge the values — a mismatch between the two would itself be a bug). The batch test ADDS the `LevelGenerator.generate`-level assertion (validation status + `attempts == 1`) on top, closing the 3.6 deferred Low (no `LevelGenerator`-level regression existed; the seed-regression tests called `generate_layout` directly, bypassing the validator/retry). Reuse the existing `fingerprint(...)` static — do NOT author a second fingerprint format. If deriving the layout from the `GenerationResult.payload` board is awkward, it is acceptable to assert the terrain fingerprint via the same `generate_layout` path AND separately assert the full-`generate` validation-status/attempts for the same seeds — but the seed sets MUST be identical and documented as the same catalog. [DONE — did BOTH: full-generate payload pin AND a generate_layout cross-check, plus an explicit assert that the two paths agree.]
  - [x] AC3 batch failure shape: when a seed fails generation (not expected for the approved catalog, but the harness must REPORT correctly if one regresses), assert/print `seed`, `recipe_id`, `failed_phase`, and `reason` from the `GenerationResult` — these fields already exist on `GenerationResult.error`. Keep diagnostics COMPACT (no grid dump). [DONE — failure-report-shape test forces an unknown-recipe error and asserts seed + recipe + failed_phase + reason; batch asserts embed those fields in the message.]
  - [x] AC4 preserved-seed catalog: include the approved seed catalog AS DATA (a documented `const` list/dict in the batch test or a sibling fixture), with bland/unfair seeds KEPT and ANNOTATED (e.g. a `notes` field per seed) rather than deleted. AC4 verbatim: "bland or unfair seeds are preserved for tuning rather than silently discarded." The catalog is the persisted artifact of the AC4 tester pass; the human playtest-notes themselves are recorded in the Completion Notes (see 3.7.5) — the CODE deliverable for AC4 is the preserved catalog + the batch coverage proving those seeds still load deterministically. [DONE — `APPROVED_SEED_CATALOG` const with a `notes` field per seed; bland seeds annotated KEPT; a test asserts every seed carries a note.]
  - [x] DELIBERATE-UPDATE CONTRACT: the batch fingerprints, like the existing seed-regression fixtures, change ONLY with an intentional generator/recipe change re-pinned in the same PR (regenerate via the dump tools). They must NEVER be updated silently to make a drifting test pass. Document this in the new test header (mirror the existing seed-regression test headers). [DONE — header documents the contract.]

- [x] 3.7.4 (Optional) Add a one-shot batch-loader dev tool mirroring the existing dump tools. (AC: 3)
  - [x] If useful for regenerating/eyeballing the batch catalog, add `godot/tools/dump_seed_batch_report.gd` (`extends SceneTree`, NOT a test, NOT auto-discovered — mirror `tools/dump_small_layout_fingerprints.gd` / `dump_medium_layout_fingerprints.gd`). It prints, per approved seed + recipe, the validation status (`succeeded`/`attempts`/`validated`) + the compact terrain fingerprint via `LevelGenerator.generate(...)`. This is a DEBUG/manual-seed tool — it must be build-profile-appropriate (a `tools/` SceneTree script, never shipped as gameplay) and must NOT grant progression or write `user://`. Skip this if the batch test alone suffices; do not over-build. [DONE — added; prints only, no `user://`, no progression. Used to ground the AC4 review.]

- [x] 3.7.5 Run required validation and update story records (incl. the AC4 playtest pass). (AC: 1-4)
  - [x] Run through PowerShell (`godot` resolves via `C:\Users\Rasmus\bin\godot.cmd`, NOT on the Bash PATH): `godot --version` -> `4.6.3.stable`; full headless runner `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10` -> exit `0` / "Headless tests passed."; `git diff --check` -> exit 0 (LF/CRLF normalization notices are fine; no whitespace errors). The two `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure fixtures, NOT regressions. [DONE — runner exit 0, "Headless tests passed.", `git diff --check` exit 0.]
  - [x] Verify: full runner exits `0`; ALL prior Epic 1/2/3.1-3.6 tests green (incl. `test_project_structure.gd` — NO change, new files live in already-required roots `scripts/generation/level` + `tests/unit/generation`); the Small AND Medium terrain seed-regression `APPROVED_FINGERPRINTS` stay GREEN with NO re-pin (the loader/batch harness only READS the pipeline; it perturbs nothing); the new `test_manual_seed_loader.gd` + `test_seed_batch_regression.gd` pass; the batch fingerprints AGREE with the existing seed-regression fixtures for the overlapping seeds. [VERIFIED — 63 test files PASS / 0 FAIL; both seed-regression fixtures green with no re-pin; batch agrees with fixtures.]
  - [x] AC4 playtest pass (record findings — do not skip): for the approved Small + Medium seeds, do a short structured tactical-decision review (a headless walkthrough of the generated layout + entrance/exit/blockers/wrinkles/enemy positions is sufficient — no rendered scene required) and RECORD in the Completion Notes whether each seed offers at least one meaningful movement / line-of-sight / risk-positioning decision. Preserve bland/unfair seeds in the catalog with a note rather than deleting them. If the autonomous delegate cannot perform a genuine human playtest, record that the structured per-seed decision review was done programmatically/by inspection and the catalog notes capture the AC4 findings — do NOT silently drop AC4. [DONE — per-seed inspection review recorded in Completion Notes + catalog `notes`; done by inspection (autonomous), grounded in the dump tool's full-pipeline output. Human feel/readability playtest remains a project-level requirement.]
  - [x] Update Dev Agent Record, Completion Notes, File List, Change Log, Status (review). Synchronize `sprint-status.yaml` (`3-7-...` -> `review`). Leave `epic-3` as `in-progress` (orchestrator finalizes the epic). No `user://` artifact created. `git status --short` clean of temp files. Fold in the resolved 3.6 deferred Low if the batch `generate`-level coverage closes it (see Deferred-Work section) — mark it resolved in `deferred-work.md` only if genuinely closed. [DONE — records updated; sprint-status `3-7 -> review`; 3.6 Round 1 Low marked RESOLVED in deferred-work.md.]

## Dev Notes

### Pre-Implementation Gate

This is the **seventh and FINAL Epic 3 implementation story** — the manual-seed reproduction + batch-regression capstone that closes the procedural-generation v0 epic. Story 3.1 delivered the generation CONTRACT layer (`GenerationRequest`/`GenerationResult`/`LevelRecipeDefinition`); 3.2 the deterministic Small layout phase; 3.3 the deterministic Medium layout phase + AC2 readability validation; 3.4 the first tactical-wrinkle placement + first `HAZARD`; 3.5 deterministic enemy + reward placement; 3.6 the comprehensive `LevelValidator` + bounded deterministic retry (with `LevelGenerator.generate` now the full pipeline returning `validated`/`attempts` diagnostics). Story 3.7 adds a **thin manual-seed loader** (parse/validate a seed -> build a manual-seed `GenerationRequest` -> run `LevelGenerator.generate`, surfacing manual-seed meta-ineligibility) and a **batch seed-regression harness** that drives approved Small + Medium seeds through the FULL pipeline asserting stable validation status + compact terrain fingerprints, plus a **preserved approved-seed catalog** (bland/unfair seeds kept for tuning, not discarded).

Story-creation analysis on 2026-06-17 found:

- `epic-1: done`, `epic-2: done`, `3-1-...: done`, `3-2-...: done`, `3-3-...: done`, `3-4-...: done`, `3-5-...: done`, `3-6-generator-validation-and-bounded-retry: done` (all code-review Approve, full headless suite green), `epic-3: in-progress`. Story 3.7 was `backlog` and is now `ready-for-dev`. It is the last Epic 3 story; epic status stays `in-progress` (do not change it — the orchestrator closes the epic after this story + retrospective).
- The generation pipeline is COMPLETE and is the loader's dependency, not its build target: `LevelGenerator.generate(request, recipe_repository, enemy_repository, validator=null)` runs recipe-resolution -> layout -> placement -> comprehensive validation -> bounded retry and returns a `GenerationResult` with a payload (`board`/`entrance`/`exit`/`blockers`/`rewards`/`size_class`/`recipe_id`/`level_seed`) + diagnostics (`validated`/`attempts`/`enemy_count`/`reward_count`/etc.). The manual-seed loader CALLS this — it does NOT re-author generation, validation, or retry.
- **The manual-seed eligibility vocabulary already exists** in the save layer: `RunSnapshot` carries `is_manual_seed: bool` (default `false`) + `meta_progression_eligible: bool` (default `true`), and `from_between_level` sets `meta_progression_eligible = not is_manual_seed`. The ambiguous `manual_seed_eligible_for_progression` key was DELIBERATELY dropped (a 2.7/2.8 decision, test-pinned absent in `test_run_snapshot.gd`). Story 3.7's loader MUST surface manual-seed status with THIS split (`is_manual_seed`/`meta_progression_eligible`) and must NOT re-introduce the ambiguous key. The actual save WRITE of a manual-seed run is Epic 4/8 (run-start + outpost return); 3.7 only proves the GENERATED LEVEL reports manual-seed ineligibility (AC1) — it does not wire run save or meta progression.
- **Seed-sign provenance is the central AC2 subtlety** (carried verbatim from the 3.1 code-review Decision): `GenerationRequest.validate()` rejects `root_seed < 0`. A legitimate full-64-bit shared/replay seed can decode as a NEGATIVE signed int64. So the loader must NORMALIZE a parsed seed to non-negative BEFORE building the request, or legitimate seeds are silently rejected. Normalize at the loader boundary (mask/unsigned-reinterpret) — do NOT relax `GenerationRequest.validate()`'s non-negative rule.

Before implementing, re-confirm the local tree is clean or that dirty files are intentional user work. If any Story 1.x/2.x/3.1-3.6 status regressed, stop and restore that boundary first.

### Scope Boundary

This story delivers a **pure-domain manual-seed loader** (`ManualSeedLoader`) that (a) parses + validates a raw seed into a normalized non-negative `root_seed` with a clear error on malformed/ambiguous input (AC2), and (b) builds a manual-seed-flagged `GenerationRequest`, runs it through the existing `LevelGenerator.generate(...)`, and reports manual-seed meta-ineligibility on the result (AC1); plus a **batch seed-regression harness** driving approved Small + Medium seeds through the FULL pipeline asserting stable validation status + compact terrain fingerprints, with failures reporting seed + recipe + phase + reason (AC3); plus a **preserved approved-seed catalog** keeping bland/unfair seeds for tuning (AC4).

In scope:

- **`ManualSeedLoader`** — a pure-query `RefCounted`: `parse_seed(raw) -> ActionResult` (AC2 normalize + validate + clear error) and `load_level(raw_seed, recipe_id, recipe_repository, enemy_repository)` (AC1 build request -> `generate` -> surface `is_manual_seed: true`/`meta_progression_eligible: false`). It CALLS the 3.6 pipeline; it does not re-implement generation/validation/retry. No `user://`, no scene, no autoload.
- **Seed normalization (seed-sign provenance)** — parse a decimal-string or int seed; reject empty/non-integer/ambiguous input with a stable lower-snake code; normalize to the non-negative range so a negative-decoding full-64-bit seed loads instead of being rejected by `GenerationRequest.validate()`. Mirror `RngStreamSet._int64_from_value`'s String/int discipline; honor the int64-string lossless rule.
- **Batch seed-regression harness (AC3)** — `test_seed_batch_regression.gd` drives the approved Small + Medium seed catalog through `LevelGenerator.generate(...)` and asserts per seed: validation status stable (`succeeded`, `validated == true`, `attempts == 1`) + the compact terrain fingerprint matches the pinned value (REUSING the existing `fingerprint(...)` and AGREEING with the existing seed-regression fixtures for overlapping seeds). Failure messages carry seed + recipe + phase + reason. This ALSO closes the 3.6 deferred Low (no prior `LevelGenerator.generate`-level seed-regression existed — the seed-regression tests called `generate_layout` directly, bypassing the validator/retry).
- **Preserved approved-seed catalog (AC4)** — the approved seeds (Small + Medium) kept as documented data, bland/unfair ones ANNOTATED not deleted, with per-seed tactical-decision notes recorded in the Completion Notes from the AC4 review pass.
- **Tests** — manual-seed load determinism + manual-seed meta-ineligibility (AC1); invalid/malformed/ambiguous seed -> clear error + no generation, AND negative-decoding seed normalizes-and-loads (AC2); batch validation-status + fingerprint stability with seed/recipe/phase/reason failure reporting (AC3); preserved catalog read by the batch test (AC4). Small + Medium terrain fingerprints stay GREEN with NO re-pin.

Out of scope (owned elsewhere — do NOT build here):

- **A HUD / scene / UI seed-entry widget or run-map seed field.** AC1 says "entered for a level-generation TEST" — this story is the DOMAIN loader + headless harness. The player-facing seed-entry UI (hero-select / run-start / settings) is a later UX story (Epic 4/5 run flow + the FR68 UI flows). Do NOT build a Control/scene. [Source: epics.md FR68; project-context.md presentation rules]
- **Writing a manual-seed RUN save or enforcing meta-progression ineligibility in the outpost.** The `is_manual_seed`/`meta_progression_eligible` split lives on `RunSnapshot` and the actual save WRITE + meta gate is Epic 4 (run start) / Epic 8 (outpost return, FR28 manual-seed no-progression enforcement). 3.7 only proves the GENERATED LEVEL reports ineligibility; it does not persist a run or touch meta progression. Do NOT modify `RunSnapshot` or `SaveRepository`. [Source: epics.md FR28 -> Epic 8; Story 2.7/2.8 save layer]
- **Route/run-level seed reproduction (the `map` stream, 8-12 node route).** FR29's "major run structure / node structure" reproduction is Epic 4 route generation (`map` stream). 3.7 reproduces a single GENERATED LEVEL from the `level` stream via the existing pipeline. Do NOT generate a route or use the `map` stream. [Source: epics.md Epic 4; FR29]
- **Changing `LevelGenerator` generation/validation/retry logic, the `LevelValidator`, the generators, the placer, the baseline recipes/enemies, or `GenerationRequest`/`GenerationResult` contracts.** The pipeline is COMPLETE (3.1-3.6). The loader is a thin orchestration + parse layer ON TOP. Do NOT add fields to these, change `GenerationRequest.validate()`'s non-negative-seed rule, or re-author any generation step. [Source: Story 3.6 scope; the settled pipeline]
- **A SECOND fingerprint format.** REUSE `SmallLevelLayoutGenerator.fingerprint` / `MediumLevelLayoutGenerator.fingerprint` (the pinned TERRAIN fingerprint). Inventing a new format would fork the regression contract. [Source: Stories 3.2/3.3 fingerprint]
- **Re-pinning the 3.2/3.3 terrain `APPROVED_FINGERPRINTS`.** The loader/batch harness only READS the pipeline; attempt 0 is unperturbed (3.6 invariant) so the terrain is byte-identical. If a batch fingerprint disagrees with the existing seed-regression fixture, it is a BUG, not a re-pin. [Source: Story 3.6 attempt-0-unperturbed invariant; retro-notes epic-3 Story 3-2/3-3]
- **A standalone `RewardTableDefinition`/repository + concrete loot CONTENT** (Epic 6); **the player/hero entity** (Epic 4); **realizing `door`/`affinity_placeholder`/`risky_side_branch` wrinkles or real affinity rules** (Epic 7); **enemy AI / turns / hazard damage / combat / rules-kernel behavior** (Epic 1 systems, not invoked by generation). [Source: epics.md Epics 4/6/7; Story 3.5/3.6 scope]
- **Any player-selectable difficulty tier.** A seed is replay/practice/debug input, never a difficulty knob; nothing scales enemy stats/HP/damage/rewards/RNG/run length. HARD non-goal. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- Cloud saves, accounts, multiplayer, leaderboards, telemetry, Godot .NET/C#, React/Vite production dependencies, or new test frameworks / new engine libraries. [Source: project-context.md Technology Stack; Critical Don't-Miss Rules]

### Current Repository Baseline (READ THIS FIRST — add a thin `ManualSeedLoader` + a batch regression test; CALL the existing pipeline, do NOT fork it)

The most important constraint: **`ManualSeedLoader` is a thin orchestration + seed-parse layer that CALLS the complete `LevelGenerator.generate(...)` pipeline; it draws NO RNG of its own, generates no layout itself, and changes NO settled contract. The batch harness only READS the pipeline (attempt 0 unperturbed) so the Small + Medium terrain seed-regression fingerprints MUST stay byte-identical with NO re-pin. Manual-seed eligibility uses the EXISTING `is_manual_seed`/`meta_progression_eligible` split (never the dropped `manual_seed_eligible_for_progression` key). The seed parser must NORMALIZE to non-negative before building the request (seed-sign provenance) so a negative-decoding full-64-bit seed is not silently rejected by `GenerationRequest.validate()`.**

- `godot/scripts/generation/level/level_generator.gd` — `LevelGenerator.generate(request, recipe_repository, enemy_repository, validator: Object = null) -> GenerationResult`. The COMPLETE pipeline (recipe -> layout -> placement -> comprehensive validation -> bounded retry). Success payload: `board`/`entrance`/`exit`/`blockers`/`rewards`/`size_class`/`recipe_id`/`level_seed`. Success diagnostics: `phase`/`recipe_id`/`size_class`/`blocker_count`/`wrinkles`/`wrinkle_count`/`enemy_count`/`reward_count`/`optional_reward_count`/`validated: true`/`attempts`/`validation_report`. Error: `failed_phase`/`error_code`/`reason`/`seed` (String)/`diagnostics` (incl. `attempts`). `MAX_GENERATION_ATTEMPTS = 8`. The loader CALLS this with a manual-seed request; the batch test drives the approved catalog through it. Do NOT modify it.
- `godot/scripts/generation/level/generation_request.gd` — `GenerationRequest(root_seed, node_id, node_type, recipe_id, size_class, difficulty_band, affinity_placeholder, constraints)`. `level_seed()` returns `root_seed`. `validate()` rejects `root_seed < 0` (`invalid_generation_request`, field `root_seed`), non-lower-snake `node_id`/`node_type`/`recipe_id`/`affinity_placeholder`, an invalid `size_class` (only `small`/`medium`), and a non-`standard` `difficulty_band`. The loader builds a VALID request: a normalized non-negative seed, lower-snake ids (reuse `&"node_1"`/`&"combat"` like the seed-regression tests, or a documented manual-seed node id), the recipe's `size_class`, `DIFFICULTY_STANDARD`, `AFFINITY_NONE`. Do NOT add a field or relax `validate()`.
- `godot/scripts/generation/level/generation_result.gd` — `GenerationResult`. `ok(payload, diagnostics)`/`error(failed_phase, error_code, reason, seed: String, diagnostics)`; String `seed`; phase normalization. The loader/batch test READ `succeeded`/`payload`/`diagnostics`/`failed_phase`/`error_code`/`reason`/`seed`. AC3 failure reporting reads `seed` + `diagnostics.recipe_id` (or the request's `recipe_id`) + `failed_phase` + `reason` — all already present. Do NOT add a phase or change the class.
- `godot/scripts/content/repositories/level_recipe_repository.gd` — `LevelRecipeRepository.create_baseline_repository()` registers `small_combat_basic` + `medium_combat_basic`; `recipe_ids()` returns both (ordered); `get_recipe(id)` resolves a `LevelRecipeDefinition` (carries `size_class`). The batch harness iterates BOTH recipes; the loader resolves the recipe to read its `size_class` for the request. Do NOT add a recipe.
- `godot/scripts/content/repositories/enemy_repository.gd` — `EnemyRepository.create_baseline_repository()`. REQUIRED 3rd `generate` param (3.5 contract). Pass it through. Do NOT change it.
- `godot/scripts/generation/level/small_level_layout_generator.gd` / `medium_level_layout_generator.gd` — each exposes `static func fingerprint(layout: Dictionary) -> String` (the TERRAIN fingerprint: `"<w>x<h>|e<x>,<y>|x<x>,<y>|<row>/<row>/..."`, `0`=FLOOR/`1`=WALL/`2`=HAZARD/`3`=ENTRANCE/`4`=EXIT). REUSE this static for the batch fingerprints — do NOT author a second format. The batch test pins the SAME values as `test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` for overlapping seeds. `generate_layout(request, recipe, streams, enemy_repository)` is the per-attempt layout step (the seed-regression tests call it directly); the batch harness prefers the full `generate(...)` so it also asserts the validator/retry status.
- `godot/scripts/save/snapshots/run_snapshot.gd` — `RunSnapshot`. `is_manual_seed: bool` (default `false`) + `meta_progression_eligible: bool` (default `true`); `from_between_level` sets `meta_progression_eligible = not is_manual_seed`. The ambiguous `manual_seed_eligible_for_progression` key is DELIBERATELY absent (test-pinned). The loader REPORTS manual-seed status with this exact vocabulary (`is_manual_seed: true`, `meta_progression_eligible: false`). Do NOT modify `RunSnapshot` or write a run save.
- `godot/scripts/core/state/rng_stream_set.gd` — `RngStreamSet`. `_int64_from_value(value)` is the canonical seed-decode discipline: `TYPE_INT` -> ok; integral `TYPE_FLOAT` -> ok; `TYPE_STRING`/`TYPE_STRING_NAME` -> ok only if `is_valid_int()` (else not-ok); else not-ok. `to_snapshot()` string-encodes `root_seed` (int64 JSON-precision rule). MIRROR `_int64_from_value`'s String/int acceptance in `parse_seed` (reject non-integer strings/empty; accept decimal int strings + ints; honor the int64-string lossless rule). The loader does NOT instantiate a stream set itself — `generate` does that per attempt; the parser only normalizes the integer seed.
- `godot/scripts/core/results/action_result.gd` — `ActionResult` (`succeeded`, `is_error()`, `error_code`, deep-copied `metadata`). `parse_seed` returns `ActionResult.ok([], {"root_seed": <int>})` / `ActionResult.error(<code>, {...})`. Receive metadata as needed; the int seed is a scalar (no `Array[Dictionary]` deep-copy trap here, but keep diagnostics simple).
- `godot/tests/unit/test_case.gd` — the custom headless harness base. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, call `assert_true/false/equal`, return `result()`. Do NOT add GUT/GdUnit.
- `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` — the existing TERRAIN `APPROVED_FINGERPRINTS` (Small seeds `1001/2002/3003/4004/5005`; Medium same five) + the DELIBERATE-UPDATE contract + the AC3 failing-seed-in-message discipline + the divergence assertion. The batch harness REUSES these seeds + values (and may import or re-declare them, documented as the same catalog) and ADDS the `generate`-level validation-status assertion. MUST stay GREEN with NO re-pin. Mirror their headers for the DELIBERATE-UPDATE contract.
- `godot/tools/dump_small_layout_fingerprints.gd` / `dump_medium_layout_fingerprints.gd` — one-shot `SceneTree` dev tools (NOT tests, NOT auto-discovered) printing the pinned TERRAIN fingerprints via `generate_layout`. The model for an optional `tools/dump_seed_batch_report.gd` (3.7.4) that prints the full-`generate` status + fingerprint. You should NOT need to re-run the existing dumps (terrain unchanged).
- `godot/tests/unit/core/test_project_structure.gd` — the structural fail-loud gate. The new `manual_seed_loader.gd` + new tests live inside already-required roots (`scripts/generation/level`, `tests/unit/generation`), so the gate stays green with NO change. Do NOT add a new top-level root.

### Existing Files To Update Or Preserve

| Path | Current State | This Story Changes | Preserve |
|---|---|---|---|
| `godot/scripts/generation/level/level_generator.gd` | Complete `generate(request, recipe_repo, enemy_repo, validator=null)` pipeline (recipe -> layout -> placement -> validation -> bounded retry); payload + `validated`/`attempts` diagnostics. | NO change — the loader CALLS it; the batch test drives the catalog through it. | The full signature + payload/diagnostics shape + retry logic + `MAX_GENERATION_ATTEMPTS`. |
| `godot/scripts/generation/level/generation_request.gd` | `level_seed()`; `validate()` rejects `root_seed < 0`. | NO change — build a VALID request with a normalized non-negative seed; do NOT relax the non-negative rule. | `level_seed()`, the `validate()` rules (incl. non-negative `root_seed`), the field set. |
| `godot/scripts/generation/level/generation_result.gd` | `ok`/`error`, String `seed`, phase vocabulary, `succeeded`/`failed_phase`/`error_code`/`reason`/`diagnostics`/`payload`. | NO change — READ the result. | The factories, String `seed`, phase constants, field set. |
| `godot/scripts/generation/level/small_level_layout_generator.gd` / `medium_level_layout_generator.gd` | `static fingerprint(layout)` (TERRAIN) + `generate_layout(...)`. | NO change — REUSE `fingerprint(...)` for the batch fingerprints (no second format). | The `fingerprint` format, `generate_layout`, the draw order, the layout shape. |
| `godot/scripts/save/snapshots/run_snapshot.gd` | `is_manual_seed` + `meta_progression_eligible` split; ambiguous key dropped (test-pinned absent). | NO change — REUSE this vocabulary in the loader's reported eligibility; do NOT write a run save or re-add the ambiguous key. | The split fields + `from_between_level` semantics + the absent-key contract. |
| `godot/scripts/core/state/rng_stream_set.gd` | `_int64_from_value` decode discipline; int64-string `to_snapshot`. | NO change — MIRROR `_int64_from_value`'s String/int acceptance in `parse_seed`. | The decode discipline + int64-string encoding. |
| `godot/tests/unit/generation/test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` | Pinned TERRAIN `APPROVED_FINGERPRINTS` + DELIBERATE-UPDATE contract. | NO change — REUSE the seeds + values; the batch test must AGREE with them; NO re-pin. | All assertions + the deliberate-update contract + the failing-seed-in-message discipline. |
| `godot/tests/unit/core/test_project_structure.gd` | Asserts required roots exist. | NO change expected (`scripts/generation`/`tests/unit` already covered). | All required-root assertions. |

### Recommended New Files

Use these names unless implementation discovers a clearer local pattern:

- `godot/scripts/generation/level/manual_seed_loader.gd` — `class_name ManualSeedLoader extends RefCounted`. The pure-domain loader: `parse_seed(raw) -> ActionResult` (AC2 normalize/validate/clear-error) + `load_level(raw_seed, recipe_id, recipe_repository, enemy_repository)` (AC1 build manual-seed request -> `LevelGenerator.generate` -> report `is_manual_seed: true`/`meta_progression_eligible: false`). Lives under the already-required `res://scripts/generation/level/` root (no structure-gate change). NO scene/UI/`user://`/autoload dependency.
- `godot/tests/unit/generation/test_manual_seed_loader.gd` — AC1: same `(seed, recipe)` -> byte-identical `GenerationResult` (terrain fingerprint + `attempts`/`validated`); the result reports manual-seed meta-ineligibility (`is_manual_seed: true`, `meta_progression_eligible: false`). AC2: empty/blank, non-integer (`"abc"`, `"12x"`), and ambiguous input -> a clear stable error code + NO generation (assert `generate` was not reached / no payload); a negative-decoding full-64-bit seed NORMALIZES to non-negative and LOADS (proves the seed-sign provenance fix). Mirror the existing generation tests' structure.
- `godot/tests/unit/generation/test_seed_batch_regression.gd` — AC3: drive the approved Small + Medium seed catalog through `LevelGenerator.generate(...)`; assert per seed validation status stable (`succeeded`, `validated == true`, `attempts == 1`) + the compact terrain fingerprint matches the pinned value (REUSING `fingerprint(...)`, AGREEING with the existing seed-regression fixtures for overlapping seeds); a failure message carries seed + recipe + phase + reason. AC4: read the PRESERVED catalog (bland/unfair seeds annotated, not deleted). Header documents the DELIBERATE-UPDATE contract (mirror the seed-regression test headers). Closes the 3.6 deferred Low (first `LevelGenerator.generate`-level seed-regression).
- `godot/tools/dump_seed_batch_report.gd` (OPTIONAL, 3.7.4) — `extends SceneTree`, NOT a test, NOT auto-discovered. Prints per approved seed + recipe the full-`generate` status (`succeeded`/`attempts`/`validated`) + the compact terrain fingerprint. Mirror `tools/dump_small_layout_fingerprints.gd`. Skip if the batch test alone suffices.

Avoid: a new RNG draw path or stream; a new result type; a second/lenient board parser; a SECOND fingerprint format; re-authoring generation/validation/retry; relaxing `GenerationRequest.validate()`'s non-negative-seed rule; re-introducing the ambiguous `manual_seed_eligible_for_progression` key; writing a manual-seed run save or touching meta progression; a HUD/scene/UI seed-entry widget; route/`map`-stream generation; re-pinning the 3.2/3.3 terrain fingerprints; placing the player/hero entity; enemy AI/turn/damage/hazard rules; real affinity rules; a `RewardTableDefinition`/repository; a difficulty knob; a new test framework; adding fields to the recipe/enemy/request/result/snapshot definitions or the baselines.

### AC1 Manual-Seed Load + Meta-Ineligibility Contract

- AC1 verbatim: "Given a manual seed is entered for a level-generation test, when the level loads, then the same seed and recipe produce the same generated level, and the generated level reports that the run is manual-seed ineligible for meta progression." [Source: epics.md Story 3.7 AC1]
- **Same-seed-same-level** is the 3.6 pipeline determinism surfaced through the loader: `load_level(seed, recipe)` twice -> byte-identical `GenerationResult` (assert the payload terrain fingerprint AND `attempts`/`validated` match). The loader adds NO randomness; `generate` builds `RngStreamSet.new(request.level_seed())` per attempt and attempt 0 is unperturbed. [Source: Story 3.6 determinism; level_generator.gd]
- **Manual-seed meta-ineligibility**: the load result must EXPLICITLY carry `is_manual_seed: true` + `meta_progression_eligible: false` (the existing `RunSnapshot` split). This is the AC1 "reports that the run is manual-seed ineligible for meta progression" deliverable. Do NOT invent a new flag; do NOT re-add `manual_seed_eligible_for_progression`. The actual meta gate (refusing to award progression) is Epic 8 — 3.7 only REPORTS the status on the generated-level result. [Source: epics.md FR28 -> Epic 8; run_snapshot.gd split fields; FR27/FR28]
- **The loader does not persist anything** — it returns a result. No `user://` write, no run save, no settings touch.

### AC2 Seed Parse/Validation Contract (clear error; no generation from ambiguous input) — including seed-sign provenance

- AC2 verbatim: "Given a seed is invalid or malformed, when the loader parses it, then it returns a clear validation error, and no generation starts from ambiguous seed input." [Source: epics.md Story 3.7 AC2]
- **`parse_seed(raw) -> ActionResult`**: accept a `String` decimal seed (the share/replay format) and a plain `int`. Reject with a stable lower-snake code (e.g. `empty_seed`, `non_integer_seed`) and compact diagnostics: empty/blank string; non-integer text (`"abc"`, `"12x"`, `"1.5"`, whitespace-embedded); any ambiguous/unsupported type. Mirror `RngStreamSet._int64_from_value`'s discipline (`String.is_valid_int()` + `to_int()`; full-64-bit decimal strings round-trip losslessly). [Source: rng_stream_set.gd `_int64_from_value`; project-context.md int64-string rule]
- **No generation on a parse error**: `load_level` MUST short-circuit on a `parse_seed` error and return it verbatim WITHOUT calling `generate` — AC2's "no generation starts from ambiguous seed input." Test asserts the error result carries the parse code and there is no `GenerationResult` payload. [Source: epics.md Story 3.7 AC2]
- **Seed-sign provenance (the central subtlety, from the 3.1 review Decision)**: `GenerationRequest.validate()` rejects `root_seed < 0`. A legitimate full-64-bit shared seed can decode NEGATIVE as a signed int64. So a VALID-but-negative parsed seed must be NORMALIZED to non-negative BEFORE the request is built (document the exact normalization — e.g. `& 0x7fffffffffffffff`), so it LOADS rather than being silently rejected by the request validator. A test must feed a negative-decoding seed and assert it normalizes-and-generates (not an error). Normalize at the loader boundary; do NOT change `GenerationRequest.validate()`. [Source: _bmad-output/auto-gds/state/3-1-...yaml open_questions "[Review][Decision] Seed-sign provenance ... Story 3.7 ... must normalize/unsigned-decode seeds"]

### AC3 Batch Seed-Regression Contract (validation status + compact fingerprints stable; failures report seed/recipe/phase/reason)

- AC3 verbatim: "Given seed regression tests are run, when approved seeds are generated in batch, then their validation status and compact fingerprints are stable, and failures report seed, recipe, phase, and reason." [Source: epics.md Story 3.7 AC3]
- **Batch through the FULL pipeline**: drive the approved Small + Medium seed catalog through `LevelGenerator.generate(request, recipe_repository, enemy_repository)` (NOT `generate_layout` directly). Per seed assert: validation status stable — the approved seeds PASS validation on attempt 0 -> `succeeded == true`, `diagnostics.validated == true`, `diagnostics.attempts == 1`; AND the compact terrain fingerprint matches the pinned value. This is the integration-level batch the 3.6 deferred Low requested (the existing seed-regression tests bypass the validator/retry by calling `generate_layout`). [Source: deferred-work.md 3-6 Round 1 Low "add a sweep test asserting LevelGenerator.generate(seed, ...).diagnostics.attempts == 1 (real validator) across a wide seed range for BOTH size classes ... and/or a LevelGenerator-level seed-regression that pins the full-generate terrain"]
- **Compact fingerprints, REUSED format, AGREEING values**: reuse `SmallLevelLayoutGenerator.fingerprint` / `MediumLevelLayoutGenerator.fingerprint` (the TERRAIN fingerprint). For the OVERLAPPING approved seeds the batch fingerprints MUST equal the values in `test_small_level_layout_seed_regression.gd` / `test_medium_level_layout_seed_regression.gd` — a mismatch is a BUG, not a new pin. If reconstructing the layout from the `GenerationResult.payload` board for `fingerprint(...)` is awkward, it is acceptable to assert the terrain fingerprint via the same `generate_layout` path AND separately assert the full-`generate` validation status/attempts for the identical seed set (documented as the same catalog) — but do not introduce a divergent fingerprint. [Source: Stories 3.2/3.3 fingerprint; Story 3.6 attempt-0-unperturbed]
- **Failure reporting**: on a (non-expected) generation failure, the assert/print MUST carry `seed` + `recipe_id` + `failed_phase` + `reason` from the `GenerationResult` (all already present on `error`). Keep diagnostics compact (no grid dump). Mirror the existing seed-regression tests' failing-seed-in-message discipline. [Source: epics.md Story 3.7 AC3; generation_result.gd]
- **DELIBERATE-UPDATE CONTRACT**: the batch fingerprints change ONLY with an intentional generator/recipe change re-pinned in the same PR (regenerate via the dump tools). NEVER silently update to make a drift pass. Document in the test header. [Source: existing seed-regression test headers]

### AC4 Preserved-Seed Catalog + Tactical-Decision Pass

- AC4 verbatim: "Given an internal tester loads generated Small and Medium levels from approved seeds, when they complete a short tactical pass, then they record whether each level has at least one meaningful movement, line-of-sight, or risk-positioning decision, and bland or unfair seeds are preserved for tuning rather than silently discarded." [Source: epics.md Story 3.7 AC4]
- **Preserved catalog (the CODE deliverable)**: the approved Small + Medium seeds live as documented data (a `const` catalog in the batch test or a sibling fixture). Bland/unfair seeds are KEPT and ANNOTATED (e.g. a per-seed `notes` field), NOT deleted — AC4 "preserved for tuning rather than silently discarded." The batch coverage proving those seeds still load deterministically IS the persisted regression artifact. [Source: epics.md Story 3.7 AC4]
- **Tactical-decision review (recorded in Completion Notes)**: do a short structured per-seed review — for each approved Small + Medium seed, inspect the generated layout (entrance/exit/blockers/wrinkles/enemy positions, the validator's `validation_report`) and record whether it offers at least one meaningful movement / line-of-sight / risk-positioning decision. A headless walkthrough/inspection is sufficient (no rendered scene required). Record findings in the Completion Notes. If a genuine human playtest is not possible in this autonomous pass, record that the structured decision review was done by inspection and the catalog notes capture the AC4 findings — do NOT silently drop AC4. [Source: epics.md Story 3.7 AC4; project-context.md NFR14 headless]

### Epic-3 Inherited Constraints (carried from earlier stories — apply directly to this story)

- **`level_seed()` not `request.seed`; `GenerationResult.seed` is a String.** All seed handling uses `request.level_seed()`; the result's `seed` is a String. Verify load/batch seed stability through the real String/int64-string transport, never native dicts. The loader's parsed int must honor the int64-string lossless rule when a String seed arrives. [Source: retro-notes epic-3 Story 3-1; rng_stream_set.gd int64-string]
- **Placement + all generation draws use `STREAM_LEVEL`** (the `rewards`/`loot` streams are reserved for Epic 6 runtime). The loader adds NO draws; `generate` uses `STREAM_LEVEL` only. Do NOT introduce a new stream or global randomness. [Source: retro-notes epic-3 Story 3-5 RNG-stream resolution; project-context.md Determinism rules]
- **`Array[Dictionary]` does NOT survive `ActionResult`/`GenerationResult` metadata deep-copy** — it returns a plain `Array` and crashes a strict re-typed receiver. The loader's seed metadata is a scalar int (no trap here), but if you read the result's `rewards`/`entities`/diagnostics arrays in the batch test, receive them as UNTYPED `Array`. [Source: retro-notes epic-3 Story 3-4]
- **Attempt 0 is unperturbed (3.6 invariant)** — the loader/batch harness only READS the pipeline, so the Small + Medium terrain fingerprints are byte-identical; NO re-pin. If a batch fingerprint drifts, it is a BUG. [Source: retro-notes epic-3 Story 3-6 "attempt 0 unperturbed ... preserving the pinned terrain seed-regression fingerprints"; Story 3.6 Completion Notes]
- **Manual-seed eligibility uses the EXISTING `is_manual_seed`/`meta_progression_eligible` split** on `RunSnapshot`; the ambiguous `manual_seed_eligible_for_progression` key was deliberately dropped and is test-pinned absent — do NOT re-add it. [Source: run_snapshot.gd; Story 2.7/2.8; epic-2 retro]
- **Seed-sign provenance** — normalize a parsed seed to non-negative before building the request (the 3.1 review Decision assigned this to 3.7); do NOT relax `GenerationRequest.validate()`'s non-negative rule. [Source: _bmad-output/auto-gds/state/3-1-...yaml open_questions]
- **Debug/manual-seed actions must NOT grant progression** (project-context.md hard rule) — the loader reports ineligibility; the optional dump tool is a `tools/` SceneTree script, never shipped gameplay, and grants no progression. [Source: project-context.md "Do not let debug/manual-seed actions grant progression"; game-architecture.md "Debug features ... must mark progression as debug/manual-seed when used"]

### Deferred-Work Items Relevant To This Story (fold in / address; the rest of the ledger is out of scope)

These entries from `_bmad-output/implementation-artifacts/deferred-work.md` (and the 3.1 review state file) overlap this story's area; the rest of the ledger is unrelated and stays deferred:

- **(3-6 Round 1, Low — ADDRESS HERE) No broad-seed / `LevelGenerator.generate`-level seed-regression exists.** The 3.2/3.3 seed-regression tests call `generate_layout(...)` DIRECTLY (bypassing the validator/retry), so there is no integration-level proof that the comprehensive validator never silently perturbs a baseline candidate (attempts == 1) and no full-`generate` terrain pin. The deferred fix: "add a sweep test asserting `LevelGenerator.generate(seed, ...).diagnostics.attempts == 1` (real validator) across a wide seed range for BOTH size classes ... and/or a `LevelGenerator`-level seed-regression that pins the full-`generate` terrain." -> Story 3.7's batch harness (AC3) IS this `LevelGenerator.generate`-level seed-regression: it drives the approved catalog through the full pipeline asserting `attempts == 1` + `validated == true` + stable fingerprint. If it covers a broad-enough approved set, MARK the 3.6 Round 1 Low RESOLVED in `deferred-work.md` (with the 3.7 date + the test name). [Source: deferred-work.md 3-6 Round 1 Low]
- **(3-1 review, Decision — ADDRESS HERE) Seed-sign provenance.** "Story 3.7 (manual-seed loader) must either guarantee non-negative provenance or normalize/unsigned-decode seeds before building a request, so legitimate full-64-bit seeds are not silently rejected." -> Story 3.7's `parse_seed` normalizes to non-negative before building the request, with a regression feeding a negative-decoding seed. [Source: _bmad-output/auto-gds/state/3-1-...yaml open_questions]

NOT in scope (stay deferred — unrelated to 3.7's loader/batch mandate): the 3-6 Round 2 `_is_unrecoverable_layout_error` efficiency Low (worst-case retries on `invalid_layout_shape`/`no_realizable_wrinkle_kind` — unreachable for baseline recipes; a `LevelGenerator` change, not a loader change); the 3-6 Round 2 `entity_on_entrance` unreachable-branch Decision (RESOLVED, leave-as-is); the 3-5 behind-danger UNDER-PLACEMENT Low (#1, unreachable for baseline recipes); the 3-4 wrinkle pool-exhaustion Low; the 3-3 `wall_density` dead-field decision; the 3-1 `LevelRecipeDefinition.validate()` negative-test gaps; the 3-2 Small `build_board_snapshot` shape-guard + clamp-path + reachability-coupling Lows; and all the Epic 1/2 save/settings/view-model Lows. Do NOT reopen or re-defer these.

### Testing Standards Summary

- Headless, scene-free, deterministic. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, use `assert_true/false/equal`, return `result()`. New tests live under `res://tests/unit/generation/` (auto-discovered). Do NOT add GUT/GdUnit. [Source: project-context.md Testing rules]
- Build everything in-memory — NO `user://` writes (the loader returns a result; it persists nothing). If a test rides a generated board through `TacticalSnapshot`, JSON-round-trip it (`JSON.stringify` -> `parse_string`) per the Epic 1-2 lesson, never assert only on native dicts. [Source: project-context.md Save/snapshot test rules]
- For determinism, assert the terrain fingerprint (the existing `fingerprint(...)`) + `attempts`/`validated`, not raw object identity. For the batch, the fingerprints MUST AGREE with the existing seed-regression fixtures for overlapping seeds (a mismatch is a bug). [Source: Stories 3.2/3.3 fingerprint]
- Run the full suite via PowerShell ONLY (`godot` resolves via `C:\Users\Rasmus\bin\godot.cmd`, not on the Bash PATH): `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10` -> exit `0` / "Headless tests passed." Then `git diff --check`. The two `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure fixtures, NOT regressions. [Source: project-context.md Testing rules; CLAUDE.md]
- TDD: write the failing `test_manual_seed_loader.gd` / `test_seed_batch_regression.gd` FIRST, then implement to green. The loader needs: same-seed determinism + manual-seed meta-ineligibility (AC1); invalid/malformed/ambiguous -> clear error + no generation, AND negative-decoding seed normalizes-and-loads (AC2). The batch needs: validation-status + fingerprint stability across the approved Small + Medium catalog with seed/recipe/phase/reason failure reporting (AC3); the preserved annotated catalog read by the test (AC4).

### Project Structure Notes

- `ManualSeedLoader` lives at `godot/scripts/generation/level/manual_seed_loader.gd` (the already-required `scripts/generation/level/` root — no structure-gate change). It is a `RefCounted` data-layer service, NOT a Node, with NO scene/UI/audio/`user://` dependency (NFR14). [Source: project-context.md Code Organization rules]
- New tests live at `godot/tests/unit/generation/` mirroring the domain (the already-required `tests/unit` root). [Source: project-context.md Testing rules]
- The optional `dump_seed_batch_report.gd` lives at `godot/tools/` (a `SceneTree` script, NOT auto-discovered, NOT shipped gameplay), mirroring the existing dump tools. [Source: existing tools/ dump scripts]
- The loader is a domain service called by tests today (and by a future run-start flow); it is NOT an autoload and holds NO gameplay decision logic beyond seed parse + pipeline orchestration. [Source: project-context.md "Do not put gameplay decision logic in autoloads"]

### Project Context Rules

- **Determinism:** the loader adds NO randomness; all generation draws (via `generate`) use `RngStreamSet.STREAM_LEVEL`; NEVER `randi()`/`randf()`/another stream. Same `(seed, recipe)` -> identical result. [Source: project-context.md Determinism & Simulation Rules]
- **Manual-seed / debug must not grant progression:** the loader reports `is_manual_seed: true`/`meta_progression_eligible: false`; the optional dump tool is a `tools/` SceneTree script and grants no progression. [Source: project-context.md "Do not let debug/manual-seed actions grant progression unless explicitly approved by release policy"; game-architecture.md debug/manual-seed marking]
- **Int64-string seed rule:** a String seed must round-trip losslessly; do NOT persist a full-64-bit seed as a raw JSON number. `parse_seed` mirrors `RngStreamSet._int64_from_value`. [Source: project-context.md "Do not persist a full-64-bit save field ... as a raw JSON number"]
- **Repositories are the content boundary:** recipes resolve through `LevelRecipeRepository`, enemies through `EnemyRepository` (both threaded through `generate`); the loader reads no content files directly. [Source: project-context.md "Do not access files directly from gameplay systems; use repositories"]
- **Generator validation failures report seed, phase, reason, and compact diagnostics** — the batch harness's failure reporting follows this; NEVER a full grid dump. [Source: project-context.md Rules/AI/Generation Rules; architecture]
- **Difficulty is a HARD non-goal** — a seed is replay/practice/debug input, never a difficulty knob; nothing scales enemy stats/HP/damage/rewards/RNG/run length. [Source: project-context.md "DIFFICULTY IS A HARD NON-GOAL"]
- **No new cloud/account/multiplayer/telemetry/.NET/C# dependencies; no React prototype dependency; no new test framework.** Typed GDScript, Godot 4.6.3 standard build. [Source: project-context.md Technology Stack; Critical Don't-Miss Rules]
- **Performance:** generated level load target < 3s (NFR4) — the loader adds negligible overhead over `generate` (which is already within budget at `MAX_GENERATION_ATTEMPTS = 8`). [Source: project-context.md Performance Rules; NFR4]

### References

- [Source: epics.md Epic 3 / Story 3.7 — Manual Seed Level Loader and Regression Tests (ACs 1-4)]
- [Source: epics.md FR27 — manual seed entry must be allowed for replay, debug, sharing, and practice]
- [Source: epics.md FR28 — manual seed runs must not grant meta progression (enforcement is Epic 8; 3.7 reports ineligibility)]
- [Source: epics.md FR29 — seeded generation reproduces major run structure ... level layouts ... (3.7 reproduces a single generated level via the level stream; route reproduction is Epic 4)]
- [Source: epics.md NFR4 — generated level load time < 3s for MVP]
- [Source: epics.md NFR13/NFR14 — deterministic under seeded execution; headless without rendering/audio/UI]
- [Source: _bmad-output/implementation-artifacts/3-6-generator-validation-and-bounded-retry.md — the direct template; the complete generate(...) pipeline + attempt-0-unperturbed invariant + the deferred Low 3.7 closes]
- [Source: _bmad-output/auto-gds/retro-notes/epic-3.md — Stories 3-1..3-6 epic-wide constraints (level_seed/String seed, STREAM_LEVEL, Array[Dictionary] deep-copy, attempt-0 unperturbed, 3.7 builds on the retry engine)]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md — 3-6 Round 1 broad-seed / LevelGenerator.generate-level seed-regression Low (closed by this story's batch harness)]
- [Source: _bmad-output/auto-gds/state/3-1-generation-requests-results-and-level-recipes.yaml — open_questions: seed-sign provenance Decision assigned to Story 3.7]
- [Source: project-context.md — determinism, generation/validation, manual-seed/debug-no-progression, int64-string seed, repositories, difficulty-non-goal, testing, code-organization rules]
- [Source: godot/scripts/generation/level/level_generator.gd — the complete generate(...) pipeline + payload/diagnostics shape]
- [Source: godot/scripts/generation/level/generation_request.gd — level_seed(), validate() non-negative-seed rule]
- [Source: godot/scripts/generation/level/generation_result.gd — ok/error, String seed, failed_phase/error_code/reason/diagnostics]
- [Source: godot/scripts/content/repositories/level_recipe_repository.gd — create_baseline_repository / recipe_ids (small_combat_basic + medium_combat_basic)]
- [Source: godot/scripts/save/snapshots/run_snapshot.gd — is_manual_seed + meta_progression_eligible split; the dropped ambiguous key]
- [Source: godot/scripts/core/state/rng_stream_set.gd — _int64_from_value decode discipline + int64-string to_snapshot]
- [Source: godot/scripts/generation/level/small_level_layout_generator.gd / medium_level_layout_generator.gd — static fingerprint(layout) TERRAIN format (reuse, no second format)]
- [Source: godot/tests/unit/generation/test_small_level_layout_seed_regression.gd / test_medium_level_layout_seed_regression.gd — the approved seed catalog + pinned fingerprints the batch test reuses/agrees with]
- [Source: godot/tools/dump_small_layout_fingerprints.gd / dump_medium_layout_fingerprints.gd — the dump-tool pattern for the optional batch-report tool]

## Dev Agent Record

### Agent Model Used

Opus 4.8 (claude-opus-4-8[1m]) via the auto-gds dev-story delegate.

### Debug Log References

- TDD red phase: `test_manual_seed_loader.gd` parse-failed with "ManualSeedLoader is a constant but does not contain a type" (the `preload` resolves but the script file did not yet exist) — confirmed the failing-first state before implementing the loader.
- One real contract fact surfaced during green-up (12 identical failures): `GenerationResult.seed` (String) is populated ONLY on the ERROR path (`GenerationResult.error(...)`); a SUCCESS result (`GenerationResult.ok(...)`) leaves `seed == ""`. The success-path seed transport is `payload.level_seed` (set by `LevelGenerator._ok_with_payload` to `str(request.level_seed())`). Both new tests were corrected to assert success-path seed via `payload.level_seed`; the error-path `result.seed` assertion (failure-report test) was already correct and stayed.

### Completion Notes List

- **Scope delivered exactly as specified — a THIN loader + batch harness over the COMPLETE 3.1-3.6 pipeline; no fork, no contract change.** Only four new files; zero existing files modified (`git status --short` shows only the four untracked additions). The Small AND Medium terrain seed-regression `APPROVED_FINGERPRINTS` stayed GREEN with NO re-pin — confirming the attempt-0-unperturbed invariant: the loader/batch harness only READS the pipeline and perturbs nothing.
- **`ManualSeedLoader` (`scripts/generation/level/manual_seed_loader.gd`)** — `class_name ManualSeedLoader extends RefCounted`, pure data-layer (no Node/scene/UI/`user://`/autoload). `parse_seed(raw) -> ActionResult` and `load_level(raw_seed, recipe_id, recipe_repository, enemy_repository) -> Dictionary`. Draws no RNG; persists nothing.
- **AC1 (same-seed-same-level + manual-seed meta-ineligibility):** `load_level` builds a manual-seed `GenerationRequest` (normalized non-negative seed, `node_1`/`combat` ids, the recipe's own `size_class`, `DIFFICULTY_STANDARD`, `AFFINITY_NONE`) and runs `LevelGenerator.generate(...)`. The returned Dictionary ALWAYS carries `is_manual_seed: true` + `meta_progression_eligible: false` (the EXISTING `RunSnapshot` split vocabulary) — even on a parse error / generation failure. The deliberately-dropped `manual_seed_eligible_for_progression` key is NEVER reintroduced (test-pinned absent on the load result). Determinism is the 3.6 pipeline's, surfaced through the loader: two loads of the same `(seed, recipe)` produce byte-identical terrain (payload fingerprint) + identical `attempts`/`validated`.
- **AC2 (clear error; no generation from ambiguous input) + seed-sign provenance:** `parse_seed` accepts a plain `int` and a decimal `String`/`StringName` (mirroring `RngStreamSet._int64_from_value`'s `is_valid_int()`+`to_int()` discipline; full-64-bit decimal strings round-trip losslessly). Rejections (stable lower-snake codes): `empty_seed` (empty/blank), `non_integer_seed` (non-decimal text like `"abc"`/`"12x"`/`"1.5"`/`"0x1F"`/`"1e3"`/embedded-or-edge whitespace; any float input — a float seed is ambiguous, the share format is int or decimal string; AND, per the Round 1 `[Review][Patch]` fix, a decimal string OUTSIDE the signed-int64 range — `is_valid_int()` accepts it but `to_int()` saturates an over-max value to max int64 / wraps a max+1 value negative, so the loader enforces the int64-string lossless rule by re-stringifying `to_int()` and comparing to a canonicalized input, rejecting on mismatch rather than silently saturating; benign `is_valid_int`-accepted forms — leading `+`, surplus leading zeros, signed zero — are tolerated and still parse), `unsupported_seed_type` (bool/Array/Dictionary/null/object). `load_level` SHORT-CIRCUITS on a parse error — returns it verbatim with `result == null` and NEVER calls `generate`. **Seed-sign provenance** (the 3.1 review Decision, assigned to this story): a parsed seed is NORMALIZED to the non-negative range via `NON_NEGATIVE_MASK = 0x7fffffffffffffff` (clears the int64 sign bit; idempotent for a non-negative seed) BEFORE the request is built, so a negative-decoding full-64-bit seed (e.g. `-4523566890123456789`, in int OR string form) LOADS instead of being silently rejected by `GenerationRequest.validate()`'s `root_seed < 0` rule. Pinned by a regression. `GenerationRequest.validate()` was NOT relaxed (normalization is at the loader boundary).
- **AC3 (batch validation-status + compact fingerprints stable; failures report seed/recipe/phase/reason):** `test_seed_batch_regression.gd` drives the approved Small + Medium catalog through `LevelGenerator.generate(...)` (the FULL pipeline) and asserts per seed: `succeeded == true`, `diagnostics.validated == true`, `diagnostics.attempts == 1` (the approved seeds pass on the unperturbed attempt 0), AND the compact TERRAIN fingerprint (reconstructed from the generate payload board via the EXISTING `SmallLevelLayoutGenerator.fingerprint`, no second format) matches the pinned value. I implemented BOTH the full-`generate`-payload pin AND a `generate_layout` cross-check, with an explicit assertion that the two paths agree — a mismatch would be a bug, not a re-pin. Every batch failure message embeds `seed + recipe_id + failed_phase + reason`; a dedicated test forces an unknown-recipe error and asserts those four fields are present and machine-readable. Diagnostics stay compact (no grid dump).
- **AC3 closes the 3.6 Round 1 deferred Low:** this is the FIRST `LevelGenerator.generate`-level seed-regression — the 3.2/3.3 fixtures call `generate_layout(...)` directly, bypassing the validator/retry. The batch proves the comprehensive validator never silently perturbs a baseline candidate (`attempts == 1`) for the approved set and pins the full-`generate` terrain. Marked RESOLVED in `deferred-work.md` (with the 3.7 date + test name). The Round 2 efficiency Low (`_is_unrecoverable_layout_error`) stays deferred (a `LevelGenerator` change, out of scope).
- **AC4 (preserved approved-seed catalog + tactical-decision pass):** the `APPROVED_SEED_CATALOG` const (in the batch test) holds the SAME five seeds (1001/2002/3003/4004/5005) for BOTH baseline recipes — the documented shared catalog — each with a `notes` field; a test asserts every seed carries a note (preserved, not discarded). The per-seed tactical-decision review was done by inspection of the actual generated terrain (grounded in `tools/dump_seed_batch_report.gd`'s full-pipeline output: all 10 seeds `succeeded=true attempts=1 validated=true`, fingerprints byte-identical to the 3.2/3.3 fixtures). Findings (each seed offers >= 1 meaningful movement / line-of-sight / risk-positioning decision):
  - Small 1001 — open corridor + one interior wall pair (col 1 rows 2/6); a short cover step; fair, slightly bland warm-up. KEPT (bland-but-fair).
  - Small 2002 — col-3 walls (rows 1/5) + row-5 col-1 wall give a walled upper-left flank vs the open corridor; meaningful positioning. KEPT.
  - Small 3003 — row-2 cluster (cols 3, 5-6) forms a small chamber/choke; rewards a lower-corridor advance. KEPT.
  - Small 4004 — blocker_cluster on row 3 (cols 4-5) sits right above the corridor + a left wall run (row 5 cols 1-2); a clear cover/exposure trade approaching the exit. KEPT (good variety).
  - Small 5005 — dense upper-half walls (rows 1-3) form a cover maze; the lower half stays open; the richest Small LoS seed. KEPT.
  - Medium 1001 — wide-open 14x12 with sparse cover (row 5 cols 2/7); long sightlines reward ranged positioning; slightly sparse but fair. KEPT (open-but-fair).
  - Medium 2002 — cover across both halves (row 2 cols 6/9-10, row 9 col 4) + a flank gap; strong flank-vs-direct decision. KEPT (good variety).
  - Medium 3003 — exit-side cover cluster (rows 3/7 cols 10-11, row 4 col 11) shapes a guarded approach; risk-positioning near the exit. KEPT.
  - Medium 4004 — cover on both flanks PLUS a HAZARD cell (row 4 col 9, terrain `2`); the hazard pocket forces a real route trade-off. KEPT (excellent — first hazard seed).
  - Medium 5005 — a HAZARD pair on row 2 (cols 10/12, terrain `2`) makes the upper-right a danger zone to route around + mid/lower cover; the hazard pair is the standout decision. KEPT (strong hazard/risk seed).
  - Note: this is an autonomous inspection-based review (terrain/entrance/exit/blocker/wrinkle/hazard structure). A human feel/readability/frustration playtest remains a project-level requirement (project-context.md "Human playtests remain required").
- **Determinism / RNG discipline:** the loader adds no `randi()`/`randf()` and no new stream; all generation draws go through `STREAM_LEVEL` inside `generate` (per attempt). No `user://` write anywhere. No difficulty knob (a seed is replay/practice/debug input only — hard non-goal honored).
- **Tests:** full headless runner exits 0, "Headless tests passed.", 63 test files PASS / 0 FAIL; `git diff --check` exit 0. The two `ERROR: Parse JSON failed` stderr lines are the documented save/settings parse-failure fixtures, NOT regressions. `test_project_structure.gd` unchanged (new files live in already-required roots).
- **Breaking changes:** NONE. `ManualSeedLoader` is a new public class; no existing public interface, config key, schema, CLI flag, or save format changed. (Note for downstream callers: `GenerationResult.seed` is error-path-only; read a successful run's seed from `payload.level_seed`.)

### File List

New:
- `godot/scripts/generation/level/manual_seed_loader.gd` — the pure-domain manual-seed loader (parse/normalize + orchestrate `LevelGenerator.generate`, report manual-seed meta-ineligibility).
- `godot/tests/unit/generation/test_manual_seed_loader.gd` — AC1 (determinism + meta-ineligibility) + AC2 (clear error / no generation / seed-sign provenance normalize-and-load).
- `godot/tests/unit/generation/test_seed_batch_regression.gd` — AC3 (full-`generate` batch: validation status + terrain fingerprint stable, failure reports seed/recipe/phase/reason; closes the 3.6 deferred Low) + AC4 (preserved annotated `APPROVED_SEED_CATALOG`).
- `godot/tools/dump_seed_batch_report.gd` — optional one-shot `SceneTree` dev tool: prints the full-`generate` status + terrain fingerprint per approved seed/recipe (not auto-discovered; no `user://`; no progression).

Modified: none.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-17 | 0.1 | Initial story context created (comprehensive create-story analysis: thin ManualSeedLoader calling the complete 3.6 generate(...) pipeline; AC2 seed parse/validate + seed-sign-provenance normalization carried from the 3.1 review Decision; AC3 batch LevelGenerator.generate-level seed-regression reusing the existing fingerprints and closing the 3.6 deferred Low; AC4 preserved annotated approved-seed catalog; manual-seed meta-ineligibility via the existing is_manual_seed/meta_progression_eligible split; attempt-0-unperturbed fingerprint invariant; no UI/save-write/route/difficulty scope). Status set ready-for-dev. | Bob (Scrum Master) |
| 2026-06-17 | 1.0 | Implemented (TDD). New `ManualSeedLoader` (`parse_seed` + `load_level`): AC2 seed parse/validate (`empty_seed`/`non_integer_seed`/`unsupported_seed_type`) mirroring `RngStreamSet._int64_from_value`, short-circuit-no-generation on parse error, and seed-sign-provenance normalization (`& 0x7fffffffffffffff`, pinned by a negative-seed regression); AC1 manual-seed meta-ineligibility via the existing `is_manual_seed`/`meta_progression_eligible` split (dropped ambiguous key NOT reintroduced) + same-seed determinism. New `test_manual_seed_loader.gd` + `test_seed_batch_regression.gd` (the first `LevelGenerator.generate`-level seed-regression — full-`generate` validation status + terrain fingerprint stable, reusing the existing `fingerprint(...)`, cross-checked to AGREE with the 3.2/3.3 fixtures; failures report seed/recipe/phase/reason) + the preserved annotated `APPROVED_SEED_CATALOG` (AC4). Optional `tools/dump_seed_batch_report.gd`. Closed the 3.6 Round 1 deferred Low. Full headless runner exit 0 (63 PASS / 0 FAIL); Small + Medium seed-regression fingerprints GREEN with NO re-pin; `git diff --check` clean. No existing file modified; no `user://` write; no breaking change. Status -> review. | Link Freeman (Game Dev, auto-gds) |
| 2026-06-17 | 1.1 | Code-review Round 1 patch. Resolved the single `[Review][Patch]` Low: `parse_seed`'s String path now enforces the int64-string lossless rule — after `is_valid_int()` it re-stringifies `to_int()` and compares to a canonicalized input (`_decimal_string_round_trips_losslessly` / `_canonical_decimal_string`), rejecting an out-of-int64-range share string as `non_integer_seed` (reason `seed_out_of_int64_range`) instead of silently saturating to max int64 (or wrapping a max+1 value negative). Canonicalization tolerates benign `is_valid_int`-accepted forms (leading `+`, surplus leading zeros, signed zero) so legitimate seeds still parse. Added regressions `_parse_rejects_out_of_int64_range_decimal_string` + `_parse_accepts_benign_decimal_string_forms` to `test_manual_seed_loader.gd`. `RngStreamSet` NOT changed (loader-boundary hardening). Both `[Review][Decision]` items left unresolved (no chosen direction); `[Review][Defer]` `GenerationResult.seed` item untouched. Full headless runner exit 0 / "Headless tests passed."; Small + Medium seed-regression fingerprints GREEN with NO re-pin; `git diff --check` exit 0. Status stays review. | Link Freeman (Game Dev, auto-gds) |

## Review Findings

**Round 1 of 3**

Code review (gds-code-review, adversarial — Blind Hunter + Edge Case Hunter + Acceptance Auditor) of the current branch diff vs `main` for Story 3.7. Reviewed: `godot/scripts/generation/level/manual_seed_loader.gd`, `godot/tests/unit/generation/test_manual_seed_loader.gd`, `godot/tests/unit/generation/test_seed_batch_regression.gd`, `godot/tools/dump_seed_batch_report.gd`, against the settled 3.1-3.6 contracts (`LevelGenerator`, `GenerationRequest`/`GenerationResult`, `RngStreamSet._int64_from_value`, the Small/Medium `fingerprint` statics, `RunSnapshot`, the existing seed-regression fixtures).

**Verdict: Approve.** All four ACs are met with strong, independently-verified tests. Zero existing files modified (only 4 new Godot files + planning artifacts). Full headless runner exit 0 / "Headless tests passed." (`test_manual_seed_loader.gd` + `test_seed_batch_regression.gd` PASS; Small AND Medium seed-regression `APPROVED_FINGERPRINTS` GREEN with NO re-pin; `test_project_structure.gd` unchanged). `git diff --check` exit 0. The batch catalog fingerprints match the 3.2/3.3 fixtures **verbatim** (independently diffed) and are self-cross-checked against live `generate_layout` output. No Critical/High/Med findings.

Severity counts — Critical: 0, High: 0, Med: 0, Low: 2 (1 Patch, 1 Defer), Decisions: 2.

**Round 1 patch resolution (2026-06-17):** the single `[Review][Patch]` Low (out-of-int64-range decimal string silently saturating) is RESOLVED — `parse_seed` now enforces the int64-string lossless rule on the String path and rejects an out-of-range share string as `non_integer_seed`, with a regression test. Both `[Review][Decision]` items are LEFT UNRESOLVED (no human-chosen direction this round — visibility/confirmation items only). The `[Review][Defer]` Low (`GenerationResult.seed` success-path-empty contract wart) is UNTOUCHED (out of scope; stays on the cross-story ledger). Full headless suite green; no existing non-3.7 file modified; seed-regression fingerprints unchanged.

Verified during review:
- AC1: `load_level` two-load determinism asserted via payload terrain fingerprint + `attempts` + `validated` + stable `payload.level_seed`; result always carries `is_manual_seed: true` + `meta_progression_eligible: false`; the dropped `manual_seed_eligible_for_progression` key is test-pinned absent. The eligibility signal is reported even on parse-error / generation-failure returns.
- AC2: empty/blank -> `empty_seed`; non-decimal/hex/scientific/embedded-whitespace/float -> `non_integer_seed`; bool/array/dict/null/object -> `unsupported_seed_type`; `load_level` short-circuits on parse error with `result == null` (no generation). Seed-sign provenance: `& 0x7fffffffffffffff` normalizes a negative-decoding int64 (int or string form) to non-negative before the request is built; pinned end-to-end (parse + load) by a regression feeding `-4523566890123456789`. `GenerationRequest.validate()`'s non-negative rule was NOT relaxed (normalization at the loader boundary). Faithfully mirrors `RngStreamSet._int64_from_value`'s `is_valid_int()`+`to_int()` String discipline.
- AC3: the approved Small + Medium catalog driven through the FULL `LevelGenerator.generate(...)` pipeline asserts `succeeded`/`validated == true`/`attempts == 1` + the full-`generate` payload terrain fingerprint matches the pinned value AND agrees with the `generate_layout` fingerprint (explicit divergence cross-check). Failure-report shape (seed + recipe_id + failed_phase + reason) proven via a forced unknown-recipe error. The two `fingerprint` statics are byte-identical in format, so the batch's use of `SmallLevelLayoutGenerator.fingerprint` for Medium payloads is correct. Closes the 3-6 Round 1 deferred Low (first `generate`-level seed-regression).
- AC4: `APPROVED_SEED_CATALOG` preserves all five seeds for BOTH recipes with a per-seed `notes` annotation (bland/unfair KEPT, not discarded); a test asserts every seed carries a note. The per-seed tactical-decision pass was done by inspection (autonomous), recorded in Completion Notes, honestly flagged that a human feel/readability playtest remains a project-level requirement.
- Scope: no UI/scene, no run-save write, no `RunSnapshot`/`SaveRepository`/`GenerationRequest`/`GenerationResult` change, no route/`map`-stream reproduction, no second fingerprint format, no difficulty knob, no new framework. The optional `tools/dump_seed_batch_report.gd` is a `SceneTree` dev tool (not auto-discovered), prints only, no `user://`, grants no progression. All in-scope.

Findings:

- **[Review][Patch] (Low) [RESOLVED 2026-06-17] `parse_seed` silently saturates an out-of-int64-range decimal string.** A syntactically-valid decimal string beyond int64 (e.g. `"99999999999999999999"`) passes `String.is_valid_int()` and `String.to_int()` saturates to `9223372036854775807` rather than round-tripping — so the parsed seed does not equal the input, contradicting the int64-string lossless intent for that input. This is the IDENTICAL behavior of the canonical `RngStreamSet._int64_from_value` (which `parse_seed` is mandated to mirror), so it is a faithful mirror of a pre-existing engine/canonical limitation, NOT a new 3.7 defect, and no approved-catalog seed or test hits it. Fold-in when next touching `manual_seed_loader.gd`: optionally reject an out-of-range decimal string as `non_integer_seed` (e.g. re-stringify `to_int()` and compare to the trimmed input) so an over-max-int64 share string fails loudly instead of mapping to max int64. Not blocking — bounded, mirrors the canonical decoder, no AC depends on >int64 inputs.
  - **RESOLUTION:** `parse_seed`'s String/StringName path now enforces the int64-string lossless rule. After the existing `is_valid_int()` gate it computes `to_int()` and rejects as `non_integer_seed` (reason `seed_out_of_int64_range`) when the value does not round-trip — i.e. an over-max value saturated to max int64 OR a max+1 value wrapped negative. The round-trip test is `_decimal_string_round_trips_losslessly(text, parsed) := _canonical_decimal_string(text) == str(parsed)`, where `_canonical_decimal_string` tolerates the BENIGN forms `is_valid_int()` accepts (a leading `+`, surplus leading zeros, signed zero `-0`/`+0`) so those still parse — only a genuine magnitude/sign loss fails. `RngStreamSet` was NOT changed (this hardens the loader boundary, mirroring then exceeding the canonical decoder for the share-string path). Regression coverage in `test_manual_seed_loader.gd`: `_parse_rejects_out_of_int64_range_decimal_string` (max+1 wrap, min-1 saturate, far-over 20-digit, PLUS the exact `±int64` boundaries still ACCEPTED) and `_parse_accepts_benign_decimal_string_forms` (`+1001`/`007`/`-0`/leading-zero-negative still parse). Full headless runner exit 0 / "Headless tests passed." (the far-over case emits one benign `to_int()` "too large" engine stderr line — like the documented save/settings parse-failure fixtures — and is correctly REJECTED); Small + Medium seed-regression fingerprints GREEN with NO re-pin; `git diff --check` exit 0.

- **[Review][Decision] Float-seed inputs are rejected as `non_integer_seed`, stricter than `RngStreamSet._int64_from_value` (which accepts an integral float).** The loader deliberately treats any `TYPE_FLOAT` seed as ambiguous (the share/replay format is an int or a decimal string, never a float). This is a sound, documented choice already ratified by the dev (Completion Notes + epic-3 retro-notes). Recorded for human visibility only — confirm the share-format strictness is the intended manual-seed contract. No change recommended.

- **[Review][Decision] The 3-6 Round 1 deferred Low was marked RESOLVED via the full-`generate` terrain-pin arm; the optional broad-seed sweep (e.g. 1..200, `attempts == 1` only, no pin) was intentionally NOT added.** The deferred item's fix was phrased "a sweep ... and/or a `LevelGenerator`-level seed-regression that pins the full-`generate` terrain"; the batch harness decisively satisfies the latter arm (10 approved seed/recipe combos with `attempts == 1` + `validated` + full-`generate` terrain pin + a `generate_layout` agreement cross-check), and the dev's RESOLVED note in `deferred-work.md` explicitly states the wide sweep was omitted (it would only re-prove the same unreachable-by-construction property). Defensible closure. Human call: accept the closure as-is (recommended) or retain a residual note for a cheap broad `attempts == 1` sweep over a wide seed range. No code change required for this story.

- **[Review][Defer] (Low) `GenerationResult.seed` (String) is populated ONLY on the error path; on a SUCCESS result it is `""` and the seed is carried in `payload.level_seed`.** This is a pre-existing `GenerationResult` contract wart (NOT introduced by 3.7) that already cost a first-pass 12-test miss during dev (see Debug Log) and is a footgun for future success-path consumers. 3.7 cannot fix it without modifying the settled `GenerationResult` contract, which is explicitly out of scope for this story. Deferred for a future touch of `GenerationResult` (populate `seed` on the success path too, or document the split at the call site). Logged to the cross-story ledger. Not blocking — both new tests correctly read `payload.level_seed` on success and `result.seed` on the error path.
