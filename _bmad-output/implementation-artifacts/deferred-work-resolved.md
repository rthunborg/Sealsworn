# Resolved deferred work

Entries moved out of `deferred-work.md` once verified resolved. Archived during epic closeouts by auto-gds (and ad-hoc when an item is confirmed done). Newest archive batch on top.

## Archived during epic 3 closeout (2026-06-17)

### Originally deferred from: code review of 3-6-generator-validation-and-bounded-retry (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.7, 2026-06-17] (Low) No broad-seed sweep proves attempt 0 is NEVER validator-rejected, which is the silent-perturbation guard for the attempt-0-unperturbed fingerprint invariant. The pinned seeds for BOTH sizes ([1001, 2002, 3003, 4004, 5005], + 12345) ARE proven to pass the comprehensive validator on attempt 0 (`test_level_validation.gd::_clean_small/_clean_medium_candidate_passes_full_validator` build the candidate via `generate_layout`+`build_board_snapshot` and run the real validator) AND to produce the pinned terrain (the seed-regression tests), so the invariant is solidly anchored for the pinned set. Residual gap: the seed-regression tests call `MediumLevelLayoutGenerator.generate_layout(...)` / `SmallLevelLayoutGenerator.generate_layout(...)` DIRECTLY (bypassing the validator), so a hypothetical UN-pinned, UN-tested seed whose baseline placement self-soft-locks (e.g. blocking enemies that ring a mandatory reward or seal the exit) would be silently REJECTED at attempt 0 and re-rolled with a perturbed seed, drifting that seed's result from its 3.2-3.5 layout with NO fingerprint tripwire. Structurally near-impossible on the sparse, wide-open baseline 8x8/14x12 boards (enemies/rewards drawn from a candidate pool far larger than the budget) and no AC depends on it — the same "correct by construction, broad coverage missing because the failure is unreachable by construction" class as the already-deferred 3-2 reachability-coupling Low and the 3-4/3-5 pool-exhaustion Lows. Fix (when convenient): add a sweep test asserting `LevelGenerator.generate(seed, ...).diagnostics.attempts == 1` (real validator) across a wide seed range (e.g. 1..200) for BOTH size classes — proving the comprehensive validator never silently perturbs a baseline candidate — and/or a `LevelGenerator`-level seed-regression that pins the full-`generate` terrain (not just `generate_layout`). Not blocking — no current seed/board is known to hit it and no AC depends on it. (Originating review: code review of 3-6, Round 1, 2026-06-16.) — CLOSED: Story 3.7's `test_seed_batch_regression.gd` is the first `LevelGenerator.generate`-LEVEL seed-regression. It drives the approved Small + Medium catalog (seeds 1001/2002/3003/4004/5005, both baseline recipes) through the FULL pipeline and asserts per seed `succeeded == true` + `diagnostics.validated == true` + `diagnostics.attempts == 1` (proving the comprehensive validator never silently perturbs a baseline candidate on attempt 0) PLUS the full-`generate` terrain fingerprint (reconstructed from the payload board) matches the pinned value AND AGREES with the `generate_layout` fingerprint the 3.2/3.3 fixtures pin (an explicit cross-check that the two pinning paths can never silently diverge). The full-`generate` terrain pin (not just `generate_layout`) is exactly the requested closure. NOTE: this resolves the approved-catalog portion; a broad 1..200 sweep was not added (the approved-catalog `attempts == 1` proof + the agreement cross-check is the contract the loader/batch mandate required, and a wide sweep would only re-prove the same unreachable-by-construction property). The companion 3-6 Round 2 efficiency Low (`_is_unrecoverable_layout_error` worst-case retries) stays deferred (a `LevelGenerator` change, out of 3.7 scope).

### Originally deferred from: code review of 3-5-enemy-and-reward-placement (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.6, 2026-06-16] (Low) `EntityRewardPlacer.validate_reward_reachability` is TERRAIN-only: its `_flood_reachable` floods over non-WALL terrain and IGNORES blocking enemies (`entity_reward_placer.gd` lines 355-378). Enemies are placed from the shared shrinking pool BEFORE rewards and the baseline enemies all `blocks_movement = true`, so a reward that is terrain-reachable could in principle be sealed off by blocking enemies (a ring, or a single enemy on a 1-wide corridor pinch) and still PASS reachability. NOT an AC2 violation: the story Dev Notes explicitly define AC2 reachability as a 4-neighbour BFS from the entrance over non-WALL cells (terrain-only) and explicitly scope entity-aware / no-soft-lock reachability to Story 3.6's comprehensive validator, which will likely SUBSUME this focused check. The implementation faithfully matches the documented contract. Residual gap: enemy-blocking reward reachability is unmodeled by design until 3.6. Fix: 3.6's no-soft-lock / safe-reachability validator should account for blocking entities when assessing reward reachability (or document terrain-only as the permanent contract). Not blocking — no AC depends on entity-aware reward reachability this story, and the baseline boards leave ample open cells around rewards. (Originating review: code review of 3-5, Round 2, 2026-06-16.) — CLOSED: Story 3.6's `LevelValidator` adds ENTITY-AWARE reachability — a MANDATORY reward (optional == false) must be reachable AROUND blocking entities (the `_flood_entity_aware` flood treats a `blocks_movement`-entity cell as non-traversable), strengthening the placer's terrain-only check; an OPTIONAL reward stays terrain-reachable (guarded/skippable). The placer's focused terrain-only check is UNCHANGED (still test-pinned); the comprehensive validator is the superset. Driven by `test_level_validation.gd::_unreachable_mandatory_reward_fails_when_ringed_by_entities` (reward ringed by blocking enemies fails `unreachable_reward`; verified terrain-reachable-but-entity-sealed via an independent flood) + `_optional_reward_guarded_by_entity_still_passes`.
- [Review][Defer][RESOLVED in Story 3.6, 2026-06-16] (Low) The `unreachable_reward -> PHASE_VALIDATION` mapping branch in `LevelGenerator._map_layout_error` is NOT exercised end-to-end. The placer's `validate_reward_reachability` is unit-tested directly, and the sibling `missing_enemy_repository`/`no_placeable_enemy -> PHASE_ENEMIES` branches ARE driven through `LevelGenerator.generate` in `test_level_generator.gd`, but no test drives an unreachable reward through `LevelGenerator.generate` to assert `failed_phase == PHASE_VALIDATION` + `error_code == unreachable_reward` + `reason == reward_reachability_failed`. Unreachable through the integration path today because generated rewards are reachable by construction and the layout is built internally (mirrors the Medium AC2 readability-rejection mapping, which is also not driven end-to-end). The mapping is correct by inspection. Same "correct branch, integration coverage missing because the failure is unreachable by construction" class as the existing pool-exhaustion/clamp Lows. Fix (when convenient, or fold into 3.6's validator tests): add a `LevelGenerator`-level case that injects an unreachable reward (custom recipe/seed or a test seam) and asserts the `PHASE_VALIDATION`/`unreachable_reward` result. Not blocking — no AC depends on it. (Originating review: code review of 3-5, Round 2, 2026-06-16.) — CLOSED: Story 3.6 added the injected-validator test seam (`LevelGenerator.generate`'s optional 4th `validator` param) and `test_generation_phase_fixtures.gd::_rewards_phase_unreachable_reward_end_to_end` drives an `unreachable_reward` failure END-TO-END through `LevelGenerator.generate`, asserting `failed_phase == PHASE_VALIDATION` + `error_code == unreachable_reward` + a non-empty reason + no payload. The 3.6 validation path maps `unreachable_reward -> PHASE_VALIDATION` via `LevelValidator.phase_for_code` (the same phase the 3.5 `_map_layout_error` branch used), preserving the contract.

### Originally deferred from: code review of 3-3-seed-stable-medium-level-layouts (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.4, 2026-06-16] (Low) `MediumLevelLayoutGenerator.validate_readability()` guards the layout shape with `terrain_grid.size() != height` only — it does NOT guard each inner row's width, unlike the sibling `build_board_snapshot()` (which checks `row.size() != width`). A hand-built candidate with the right row COUNT but a short row would index out of bounds in `_interior_wall_count`/`_is_wall`/`_flood_reachable` and crash rather than return a structured `invalid_layout_shape`. Not reachable by any current caller: `_run_medium_layout_phase` only ever feeds `generate_layout` output (always emits `width`-wide rows), and the malformed-shape test feeds an empty terrain (caught by the row-count guard). Symmetric to the shape-guard hardening the dev DID add to `build_board_snapshot`. Fix (when convenient): extend the `validate_readability` leading guard (or share a single shape-validation helper across `validate_readability` + `build_board_snapshot`) to also reject any row whose width != `width`, and add a short-row candidate test. Not blocking — no AC depends on it and no production path can hit it. (Originating review: code review of 3-3, Round 1, 2026-06-16.) — CLOSED: Story 3.4 added the per-row width guard to `validate_readability` (rejects any row whose width != `width` with the structured `invalid_layout_shape` code), symmetric to `build_board_snapshot`.

## Archived during epic 2 closeout (2026-06-15)

### Originally deferred from: code review of 2-7-between-level-save-snapshot-foundation (2026-06-14)

- [Review][Defer][RESOLVED 2026-06-14 in Story 2.8] (Low) The Composition Contract requires run-level `RunSnapshot.rng_streams` and the embedded tactical `rng_streams` not to "silently disagree without a test." `from_between_level()` makes them equal (both are pure `streams.to_snapshot()` reads at the boundary), but no test asserted `snapshot.rng_streams == <embedded tactical>.rng_streams`; a future refactor snapshotting them at different points could diverge undetected. (Originating review: code review of 2-7, Round 1, 2026-06-14.) RESOLVED: equality assertion added in `test_run_snapshot.gd::_between_level_composes_tactical_snapshot_into_level_state` and in `test_run_resume_service.gd::_resume_run_level_streams_equal_embedded_tactical_streams`.
- [Review][Defer][RESOLVED 2026-06-14 in Story 2.8] (Low) The integration round-trip (`test_between_level_save.gd::_assemble_write_read_reparse_round_trip_preserves_fidelity`) restored the embedded tactical `rng_streams` and asserted only that the restore succeeds, not that the restored tactical streams reproduce the same next draw (the run-level streams got that stronger check; the embedded tactical ones did not). (Originating review: code review of 2-7, Round 1, 2026-06-14.) RESOLVED: `test_resume_flow.gd::_embedded_tactical_streams_reproduce_next_draw_after_resume` restores the embedded tactical streams and asserts the exact next-draw reproduction across all seven streams, symmetric with the run-level check.
