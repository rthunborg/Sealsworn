# Resolved deferred work

Entries moved out of `deferred-work.md` once verified resolved. Archived during epic closeouts by auto-gds (and ad-hoc when an item is confirmed done). Newest archive batch on top.

## Archived during epic 13 closeout (2026-07-16)

### Originally deferred from: code review of 13-1-live-board-render-and-tap-input (2026-07-14)

- **RESOLVED (by Story 13-2, 2026-07-16, Task 5): [Review][Defer] (Low) — inspect taps produce no on-screen feedback.** `interactive_inspect(cell)` routed into `_session.inspect(cell)` (metadata-only) but neither re-rendered nor surfaced the returned result, so every inspect-routed tap left the inspect region reading "Inspect: tap a cell". Story 13-2's Task 5 fixed it: `interactive_inspect` now stores the returned `CommandBridgeResult` cell facts and re-renders, so the inspect region surfaces the tapped cell (coords, visibility, occupant id/type/HP) via `_inspect_base_text`; still a pure metadata-only read — no mutation, no turn advance; suite 195 PASS. Round-1 review of 13-2 independently verified the metadata keys match `TacticalCommandBridge._build_inspect_result` + `TacticalVisibilityQuery.visible_facts_for_cell` (not a dead probe).

### Originally deferred from: code review of 12-1-interactive-combat-tap-loop-and-live-board-render (2026-07-07)

- **RESOLVED (by Story 13-1, 2026-07-14): [Review][Defer] (Low) — gesture→cell pixel hit-testing deliberately not implemented in 12.1.** No board-geometry hit-test existed and the presenter/session tap seam operated at the already-resolved cell-intent level. Story 13-1 built the pixel→cell hit-test reusing the tested `TacticalBoardZoomState.screen_to_cell` via the new `TacticalBoardGridFit`/`TacticalBoardTapRouter` seams, routed into the existing `interactive_*` seams; suite 193 PASS at resolution. (The symmetric two-step move-confirm VM remains a presentation-flow choice, not a required VM, per the 11.1 appendix.)

## Archived during epic 10 closeout (2026-07-12)

### Originally deferred from: code review of 12-2-class-loadout-and-winnable-hands-on-fights (2026-07-07)

- **RESOLVED (by Story 10.4, 2026-07-08): [Review][Defer] (Med) — Medium/elite + affinity winnability proof gap.** The 12-2 winnability proof covered only Small-neutral boards for the approved batch; the deferral asked for a Medium/elite seed × class row + at least one Scorched-affinity live seed, OR an explicit AC2 re-scope with the proof assigned to 10.4/10.6. Story 10.4's Task-5 catalog extension delivered the EXTENDED disposition: 2 Medium-neutral + 2 Scorched-affinity seeds × 3 classes = 12 full `ReferenceCombatDriver` runs, all VICTORY (the 10-4 ledger section explicitly records "the 12-2 Med Medium/affinity winnability deferral is RESOLVED here via the EXTENDED disposition"). The residual costs it exposed (harness runtime ~57s; determinism-loop coverage of the new catalogs) live on as the two ACTIVE 10-4 entries — the root Medium/affinity proof gap itself is closed.
- **RESOLVED — SUPERSEDED (by the 10-4 Med entry, 2026-07-08): [Review][Defer] (Low) — `ReferenceCombatDriver._relocate_scratch` per-cell snapshot round-trip cost.** The predicted cost materialized exactly as this deferral warned when 10.4 grew the catalog (Medium/elite boards); the 10-4 Med entry ("a MATERIALIZATION of the 12-2 Round-1 Low defer … Supersedes/updates the 12-2 Round-1 Low `_relocate_scratch` defer, same root cause, now realized") is now the single live tracking entry, with the measured ~57s figure and the in-place relocate-and-restore fix direction. Archived as superseded — the live item is the 10-4 Med entry; owner unchanged (reference-driver harness-perf pass).

### Originally deferred from: code review of 10-5-accessibility-and-readability-audit (2026-07-08)

- **RESOLVED (by Story 10.7, 2026-07-12): [Review][Defer] (Low → F-1) — the Flooded conductive-danger `_placeholder` replace / de-scope / block call.** The deferral's ask was precisely the 10.7-owned disposition call. Story 10.7's gate (`planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md`) made it: **de-scoped for v0 as an approved, documented limitation** — the placeholder already carries a non-color `[SHAPE,LABEL,TEXT]`/`danger` channel (never a color-only violation), no live conductive mechanic exists yet, and ids stay unchanged. The residual (live conductive-interaction art/VFX + final cue) is now gate follow-up **AG-4**, owned by a later affinity-effects/VFX story. The 10.5-lens deferral itself is discharged.

## Archived during epic 12 closeout (2026-07-08)

### Originally tracked from: dev of 12-2-class-loadout-and-winnable-hands-on-fights (2026-07-07)

- **RESOLVED (owner: this story): Epic-11 retro T2 — a universally-winning hands-on hero path.** 12-2 is the story the
  2026-07-07 sprint change + the Epic-11 retro assigned T2 to (retro line 185, MED-HIGH). It delivers BOTH prongs the
  T2 item offered: (a) the class-kit → combat-LOADOUT wiring (the live hero is armed from `run.starting_kit` — HP /
  weapon / support — instead of the flat 60/sword default; a null-kit run falls open to the driver default), and (b)
  the STRENGTHENED LoS-aware reference driver (`ReferenceCombatDriver`) that dodges ash-seer detonations + kites
  (ranged) / commits to the nearest melee body (melee), making the 18-HP class loadout WINNABLE on the approved seed
  batch where the naive focus-fire `LiveCombatResolver` provably dies. An unwinnable seed still fails loud
  (`live_combat_did_not_resolve` — seed + reason) for triage before 10.4. The retro T2 residual is CLOSED. (The T1
  interactive tap-loop it paired with was resolved by 12-1.)

### Originally deferred from: code review of 12-2-class-loadout-and-winnable-hands-on-fights, Round 2 (2026-07-07)

- [x] **[Review][Defer]** (Low, from code review of 12-2 **Round 2**, 2026-07-07) — `project-context.md` line 478 (the
  dated "Last Updated: 2026-07-06" Epic-11 rollup paragraph) still says "the live driver uses
  `LiveCombatResolver.DEFAULT_HERO_HP` (60)" and "183 test files / 182 PASS", both superseded by Story 12.2 (the
  interactive live loadout is now class-derived via `CombatLoadout`; the suite is 189 PASS). The two AC1-pinned boundary
  lines the story had to revise (line 262 class_kit_rules + line 453 run-flow-scene-hud-rules) ARE both correctly updated
  and accurate — line 478 is a historical epic-retrospective snapshot line outside the AC1-pinned scope, so this is a
  cosmetic doc-staleness nit, not an AC1 miss. Fold the "DEFAULT_HERO_HP (60)" clause + the PASS count into the next
  `project-context.md` "Last Updated" refresh (e.g. at the Epic-12 close) so the rollup does not contradict 262/453.
  - **RESOLVED BY THE EPIC-12 CONTEXT REFRESH (2026-07-08, `gds-generate-project-context`).** The stale Epic-11 rollup
    clauses were corrected in the `project-context.md` refresh: the "the live driver uses `LiveCombatResolver.DEFAULT_HERO_HP`
    (60)" clause is now scoped to "the auto-resolve/hands-off default driver" + carries an explicit `[SUPERSEDED by Epic 12:
    the INTERACTIVE live loadout is now class-derived via CombatLoadout …]` marker pointing at the new Epic-12 rollup, and
    the "183 test files / 182 PASS" line is annotated `[Suite is now 189 PASS after Epic 12 …]`. A new "Last Updated:
    2026-07-08 (refreshed after Epic 12 …)" header was added above the (now-prior) Epic-11 line; the two AC1-pinned lines
    (262/453) stay accurate. The rollup no longer contradicts 262/453.

## Archived during epic 9 closeout (2026-07-04)

### Originally deferred from: code review of 9-2-larval-avatar-definition-and-phases (2026-07-03)

Round 1 adversarial code review (auto-gds delegate, Opus 4.8 [1m]). Verdict **Approve** — 0 Critical / 0 High / 0 Med / 2 Low. Full headless suite green (158 PASS, 0 `^FAIL`, false-PASS guard clean — only the documented negatives: int64-overflow ×2, `invalid_node_type`, malformed-JSON ×3; `git diff --check` clean). Diff: 6 new production files (`boss_definition.gd`, `boss_phase_definition.gd`, `boss_action_definition.gd`, `boss_repository.gd`, `boss_phase_resolver.gd`, `boss_phase_transition.gd`), 1 modified production file (`domain_event.gd` — append-only `boss_phase_changed` SYSTEM event), 3 new test files + the `test_domain_event.gd` extension (`boss_phase_changed` round-trip/malformed + the `expected_ids` exhaustiveness-pin entry). Scope verified exact: ZERO touch to `generation/boss` / `run` / `rules/conditions` (stays empty) / `rules/operations` (stays single-file) / `data/` / `tools/`; 23-key `RunSnapshot` gate unchanged; no seed-regression fingerprint moved. AC1/AC2/AC3 all satisfied and tested. `BossRepository` mirrors `EnemyRepository` verbatim; the definition family mirrors `EventDefinition`/`EventChoiceDefinition`; the event mirrors the `boss_encounter_started` end-to-end template. The two Low `[Review][Defer]` items below are defensive-coverage / internal-consistency gaps, both non-blocking (behavior verified correct by inspection; 9.2 ships NO live call site so neither is production-reachable):

- [Resolved 9.3] (Low) The AC2 resolver→event integration seam is untested end-to-end. **Closed by Story 9.3**: the live phase seam `BossTurnResolver.resolve_phase_transitions` calls `BossPhaseResolver.resolve` after a boss-HP change and emits `boss_phase_changed` from each `transition.to_payload()`. Closing assertions added in `test_boss_turn_resolver.gd::_live_hp_drop_emits_boss_phase_changed_from_the_resolver` (live drop → transition → event → validate → JSON round-trip) AND `test_boss_phase_resolver.gd::_resolver_transition_closes_the_live_boss_phase_changed_seam` (the direct `resolve(...)[i].to_payload()` → event → round-trip loop). (Originating review: code review of 9-2, Round 1, 2026-07-03.)

- [Resolved 9.3] (Low) `BossPhaseResolver.resolve()` emits an event-invalid `from_phase = -1` transition on a negative `current_phase_index`. **Closed by Story 9.3**: `resolve()` now clamps `current_phase_index` to `maxi(0, ...)` — a negative index is treated as "already at phase 0" (a no-op for phase 0's entry), so `resolve()` can NEVER return a transition whose `to_payload()` has `from_phase < 0`. Test: `test_boss_phase_resolver.gd::_negative_phase_index_never_emits_an_event_invalid_transition` asserts `resolve(def, -1, full_hp)` is a no-op and any transition from a negative index produces an event-VALID payload. (Originating review: code review of 9-2, Round 1, 2026-07-03.)

### Originally deferred from: code review of 8-1-run-completion-and-return-to-outpost-flow (2026-06-30)

- [Review][Defer][Resolved 9.4] `CompleteRunCommand._resolve_completed` two-step transition is not atomic vs the command's own
  "byte-identical no-mutation RunState on ANY reject" promise (`godot/scripts/core/commands/complete_run_command.gd:200-221`).
  If `ACTIVE_ROUTE → NODE_RESOLUTION` succeeded but `NODE_RESOLUTION → COMPLETED` then failed, the run would be left
  MUTATED in `NODE_RESOLUTION` (non-terminal) with a `wrong_run_phase` error + zero events. Given the current
  `RunState._legal_next_phases` table, once step 1 succeeds step 2 CANNOT fail, so this never fires today — but the
  contract is silently broken if the table ever changes. `NodeResolvePlaceholderCommand._resolve_boss` has the
  identical two-step shape (8.1 replicates, not introduces, the pattern). Cheap future hardening: snapshot `phase`
  before step 1 and restore on step-2 failure, or assert both edges' legality up front. Not actionable now (no
  reachable failure; fixing only 8.1 would diverge from the boss pattern — address both together if ever).
  (Originating review: code review of 8-1, Round 1, 2026-06-30.)
  RESOLVED (Story 9.4, 2026-07-03): 9.4 drives this exact completion path for the boss VICTORY (from ACTIVE_ROUTE, the
  two-step runs), so the owning story hardened it: `_resolve_completed` now captures `phase_before` and restores
  `run.phase = phase_before` on a step-2 failure, so a step-2 failure leaves the run byte-identical (as if the two-step
  never ran) rather than parked in NODE_RESOLUTION. Still UNREACHABLE (both edges are always legal), so the restore is
  defensive; `test_complete_run_command.gd::_two_step_completion_is_atomic_on_a_hypothetical_step_two_failure` asserts the
  observable invariant (a successful two-step is never left in NODE_RESOLUTION). The `NodeResolvePlaceholderCommand._resolve_boss`
  twin is NOT in 9.4 scope (its boss branch is untouched) — it retains the un-hardened two-step; a future story that drives
  it may mirror this fix.
- [Review][Defer][Resolved 9.4] `RunEndOutcome.for_failed` / `for_completed` do not validate the `cause` / `outcome` against the
  event allowlists (`godot/scripts/run/run_end_outcome.gd:78-102`). A caller could project
  `for_failed(run, &"garbage")` and the DTO would surface `outcome_or_cause: "garbage"`, whereas the corresponding
  `run_failed` EVENT rejects that cause via `RUN_FAILED_CAUSES`. The read DTO is a passive projector of
  already-command-validated markers (in the live flow it only ever receives an allowlisted cause/outcome from
  `CompleteRunCommand`), so there is no reachable inconsistency today — the read surface and the strict event
  validators merely disagree in principle on what a valid marker is. If a future consumer builds a `RunEndOutcome`
  from untrusted input, add an allowlist guard that falls back to `_empty()`. Not blocking for v0.
  (Originating review: code review of 8-1, Round 1, 2026-06-30.)
  RESOLVED (Story 9.4, 2026-07-03): 9.4 calls `for_completed(run, victory)` — the FIRST real non-boss_placeholder/completed
  outcome through this DTO — so the owning story added the allowlist guard: `for_completed` now accepts only
  `boss_placeholder`/`completed`/`victory` (the `COMPLETED_OUTCOMES` const) and `for_failed` only the `RUN_FAILED_CAUSES`;
  a non-allowlisted outcome/cause falls back to `_empty()` (the fail-closed projection), so the read surface and the strict
  event validators agree. Still UNREACHABLE in the live flow (defensive-correctness);
  `test_run_end_outcome.gd::_non_allowlisted_outcome_or_cause_falls_back_to_empty` asserts the guard.

## Archived during epic 8 closeout (2026-07-03)

### Originally deferred from: Tracked from: dev of 8-5-first-death-line-and-optional-narrative-delivery (2026-07-02)

- **[Resolved 8.7] The COMPREHENSIVE save-load / migration matrix** — 8.5 ships its OWN first-death round-trip + the no-migration
  proof (`test_profile_snapshot.gd::_set_first_death_flag_round_trips_without_a_migration` +
  `test_record_first_death_command.gd::_first_death_round_trips_through_the_repository`); 8.7 owns the full matrix (epics.md Story
  8.7 AC1 names FIRST-DEATH FLAGS explicitly, alongside Oath Shards / Echoes / Seal Fragments / unlock progress / class unlocks).
  **RESOLVED 8.7 (2026-07-03)** — `godot/tests/integration/save/test_meta_summary_save_load.gd` authors the full cross-cutting
  matrix: a fully-populated profile write→restart→read (all 8.3/8.4/8.5 fields incl. the SET first-death latch), the migration
  matrix (current-schema round-trip + unsupported-schema reject + legacy lenient parse + no-unintended-progress), and the
  four-case grant/deny cross-product (each GRANTED case saved+reloaded). The first-death flag round-trips inside that matrix.

### Originally deferred from: Tracked from: dev of 8-4-echoes-seal-fragments-and-unlock-progress (2026-07-02)

- **[Resolved 8.5, 2026-07-02] (8.5) The first-death flag / narrative** — 8.4 left the `ProfileSnapshot.first_death_recorded`
  home untouched. 8.5 SETS it via `RecordFirstDeathCommand` (behind the run-end seam, death-only gate, once-only latch;
  merge-without-migration at `SCHEMA_VERSION == 1`) + delivers the line via the scene-free `FirstDeathNarrativeBeat` DTO.

- **[Resolved 8.7] The COMPREHENSIVE save-load / migration matrix** — 8.4 ships its OWN merge round-trip + the
  no-migration proof (`test_profile_snapshot.gd::_populated_8_4_homes_round_trip_without_a_migration`); 8.7 owns the full
  matrix (Echoes / Seal Fragments / unlock progress / class-mastery restore + the migration matrix).
  **RESOLVED 8.7 (2026-07-03)** — `test_meta_summary_save_load.gd` restores Echoes / `unlock_progress.seal_fragments` /
  threshold flags / `class_mastery` (int-coercion aware) in the fully-populated round-trip, and merges + reloads them in the
  four-case grant/deny matrix (a seal-fragment threshold crossing is evaluated + survives the reload).

### Originally deferred from: Tracked from: dev of 8-3-meta-profile-and-oath-shard-awards (2026-07-02)

- **[Resolved 8.5, 2026-07-02] (8.5) The first-death narrative line** ("Good. You remembered how to die.") + the first-death
  flag delivery — 8.3 reserved an EMPTY `first_death_recorded: bool` HOME in `ProfileSnapshot` (so 8.5 merges without a
  migration) but did NOT track/set the flag, deliver narrative, or build a narrative surface. 8.5 SETS the flag
  (`RecordFirstDeathCommand`), delivers the line via the scene-free `FirstDeathNarrativeBeat` DTO (the prose lives on the DTO
  by-id; the event carries `line_id`, not the raw prose), and adds the `first_death_recorded` SYSTEM event.

- **[Resolved 8.7] The COMPREHENSIVE meta/summary save-load TEST MATRIX + the migration matrix** — 8.3 ships the
  versioned profile + repository + its OWN migration reject path + round-trip tests for the AWARD; 8.7 owns the
  comprehensive matrix (Oath Shards / Echoes / Seal Fragments / unlock progress / first-death flags / class unlock
  states restore correctly; the migration matrix; "no scene nodes serialized").
  **RESOLVED 8.7 (2026-07-03)** — this is the CORE of Story 8.7. `godot/tests/integration/save/test_meta_summary_save_load.gd`
  authors the full matrix over the SHIPPED surfaces (zero production change): AC1 (fully-populated profile round-trip +
  profile-vs-run-autosave separability + a reloaded-profile→outpost capstone), AC2 (current-schema round-trip + unsupported-
  schema reject native+through-repo + legacy lenient parse through a real JSON file + no-unintended-progress + `SCHEMA_VERSION`
  stays 1), AC3 (the four-case grant/deny cross-product each saved+reloaded + the three-marker order-independence + the NFR15
  no-`Object`-serialized structural guard). "Class unlock states restore correctly" = the profile `unlock_progress` +
  `class_mastery` STATE persists (the profile→class-unlock APPLICATION stays deferred — see `unlock-SPEND / meta-power
  APPLICATION`). Full headless suite green (153 PASS, 0 `^FAIL`, false-PASS guard clean, RNG-free, `git diff --check` clean).

### Originally deferred from: Tracked from: dev of 8-2-run-summary-snapshot (2026-07-01)

- **[Resolved 8.5, 2026-07-02] (8.5) The first-death narrative line** ("Good. You remembered how to die.") + first-death flag +
  skippable delivery — 8.2 aggregates the run-end facts; it tracked NO first-death flag and delivered NO narrative.
  Narrative stays off the summary CRITICAL path (retro §7 risk 2). 8.5 KEPT it off the critical path: `RunSummary`
  `DICTIONARY_KEYS` is UNCHANGED (no narrative field added), the beat is a SEPARATE optional DTO, and the start-descent path
  reads neither the flag nor the beat (proven by `test_first_death_narrative_beat.gd::_beat_is_independent_of_run_summary`).

### Originally deferred from: Tracked from: dev of 8-1-run-completion-and-return-to-outpost-flow (2026-07-01)

- **[Resolved 8.5, 2026-07-02] (8.5) The first-death narrative line** ("Good. You remembered how to die.") + skippable delivery —
  8.1 emits `run_failed`; it tracked no first-death flag and delivered no narrative. Keep narrative off the run-end CRITICAL
  path (retro §7 risk 2). 8.5 records the flag behind the SAME run-end seam (a sibling command, caller-driven, no auto-wire)
  and keeps the beat off the critical path (a skip is a structural no-op — read-only DTO, flag set independently of display).

## Archived during epic 7 closeout (2026-06-30)

### Originally deferred from: code review of 4-6-playable-run-shell-from-start-to-end (2026-06-23)

The inert run-level `RngStreamSet` deferral (owner "Epic 6/7/9") is now fully resolved across BOTH halves — the reward-draw half by Story 6.3 (2026-06-26) and the `LevelGenerator.generate`-injection half declared CLOSED by Story 7.5 (2026-06-30) at the end of the FR54–FR58 affinity/risk arc — and is moved here at the Epic 7 closeout (Story 7.6, the last story of Epic 7). It was held in the active ledger through the Epic 6 closeout because the injection half was still open (re-affirmed 7.1→7.4 to Story 7.5).

- [Review][Defer → RESOLVED across Story 6.3 (reward-draw half) + Story 7.5 (injection half), 2026-06-26 / 2026-06-30] (Med, from a `[Review][Decision]` accepted as a v0 limitation) **The `RunOrchestrator`'s threaded run-level `RngStreamSet` is INERT in v0 — no run-affecting RNG is drawn through it, so the "route/level/RNG advance in lockstep" intent (AC1) is only partially realized and the route-position save persists a never-advanced stream.** `RunOrchestrator.start()` builds `streams = RngStreamSet.new(root_seed)` as a set independent of the one `RouteGenerator.generate(root_seed)` draws internally; during a run NOTHING draws from `orchestrator.streams` (`NodeEnterCommand` draws no RNG; `LevelGenerator.generate` mints its OWN `RngStreamSet.new(request.level_seed())` and `request.level_seed()` == `root_seed`; placeholder/exit/advance draw no RNG). Determinism + route/level reproduction are STILL correct (everything keys off `root_seed`), so no AC's observable outcome fails today — this is why it is accepted as a documented v0 limitation rather than a blocking fix. BUT it is a latent trap: when Epic 6/7/9 introduce real run-affecting RNG (combat resolution, rewards, event rolls), those draws MUST be routed through the orchestrator's `streams` (and `LevelGenerator.generate` must accept an injected run-level `RngStreamSet` instead of minting a local one), otherwise the route-position save/resume seam will silently persist a stream disconnected from where draws actually happen, breaking interrupted==uninterrupted determinism once RNG advances mid-run. Also note: `test_run_route_position_save.gd::_route_position_rng_round_trips_and_reproduces_next_draw` currently gives false confidence (it round-trips the never-advanced fresh stream and would pass even if `streams` were removed from the run lifecycle). **Fix (owner = the Epic 6/7/9 story that first draws run-affecting RNG through the orchestrator):** thread the orchestrator's run-level `RngStreamSet` into `LevelGenerator.generate(...)` (and any new run-level draw site) so generation/combat/rewards advance the SAME set the route-position save persists, and replace the inert-stream RNG round-trip assertion with one that draws through the run BEFORE saving. Not blocking for 4.6 (the v0 run is a pure function of `root_seed`). Owner: Epic 6/7/9 (tracked). (Originating review: code review of 4-6, Round 1, 2026-06-23.) — **CLOSED (both halves):** (1) **Reward-draw half — Story 6.3 (2026-06-26):** the reward roll now draws through `RunOrchestrator.streams` (`RewardOfferBuilder.build_offer(orchestrator.streams, STREAM_REWARDS, table)`), so the run-level stream ADVANCES and `compose_route_position_snapshot()` persists the stream the offer actually advanced; the false-confidence assertion was superseded by `_route_position_after_a_reward_draw_round_trips_the_advanced_stream` (draws a reward through the run BEFORE saving, asserts the restored stream reproduces the NEXT draw, `draw_index` 0→1). (2) **Injection half — Story 7.5 (2026-06-30):** declared CLOSED for the FR54–FR58 affinity/risk arc — NO story in the arc needs the injected run-level set inside generation (affinity ASSIGNMENT 7.4 + every reward/event/Destroy roll route through the orchestrator's run-level `streams`; the affinity EFFECT 7.5 operates POST-generation on a built board; `LevelGenerator.generate` keeps minting its own attempt-0-unperturbed level stream from `request.level_seed()` — every generator/route fingerprint byte-identical, `scripts/generation/` untouched). A FUTURE, out-of-arc feature that needs an IN-generation affinity-driven layout draw (e.g. hazard terrain baked at generation time) re-opens the injection then on its own terms — a fresh, separate obligation, not this v0 residual. Archived at the Epic 7 closeout (Story 7.6, 2026-06-30).

## Archived during epic 6 closeout (2026-06-29)

### Originally deferred from: code review of 5-1-class-definition-content-and-repository (2026-06-25)

The cross-cutting duplicate-id fail-loud hardening deferral (human-ratified at the Story 5-1 Phase 7 gate, scheduled into the plan as Epic 6 Story 6.1 AC6 + Task 6.1.5) was RESOLVED by Story 6.1 (2026-06-26) and is moved here at the Epic 6 closeout (Story 6.7, the last story of Epic 6).

- [Review][Defer → RESOLVED by Story 6.1, 2026-06-26] Duplicate-id last-write-wins is unguarded across ALL SIX content repositories (`class_repository.gd`, `enemy_repository.gd`, `level_recipe_repository.gd`, `weapon_repository.gd`, `support_repository.gd`, `passive_repository.gd`): `register_*` overwrites by `(type, id)` while `*_ids()` keeps only the first insertion, so a duplicate id makes the id-list and the resolver (`get_*`) disagree about the canonical definition — silently, with no error. Harmless today (baseline ids are distinct code constants) but a latent fail-quiet trap once content moves to a JSON/data pipeline (copy-paste / merge authoring errors), which conflicts with the project's fail-closed/fail-loud rule. HUMAN-RATIFIED (Story 5-1 Phase 7, 2026-06-25): keep parity (do NOT fork one repo's behavior) and address as ONE cross-cutting hardening story that makes all six repositories fail-loud (reject with a structured reason, or `push_error`) on a duplicate id, with tests. (Originating: code review of 5-1, Decision 2; `passive_repository.gd` folded in by dev of 5-4, 2026-06-25 — it inherits the SAME `ContentRepository` last-write-wins behavior, not forked.) **SCHEDULED (2026-06-26):** folded into the plan as `planning-artifacts/epics.md` Epic 6 Story 6.1 — a duplicate-id fail-loud acceptance criterion (AC6) + Implementation Task `6.1.5` (retrofit all six existing repos + the new Epic 6 loot/reward repos), sequenced before/with Epic 6's first new data-pipeline content. **RESOLUTION (Story 6.1 dev, 2026-06-26):** applied ONE uniform central guard ([Decision a]) — `ContentRepository.register_definition` now returns `ActionResult` (was `void`) and REJECTS a second `(type, id)` with a stable `duplicate_definition` error (offending `{type, id}` in metadata); all SIX existing `register_<x>` call sites updated in the SAME change to surface it as a per-type `duplicate_<x>` error (`duplicate_class`/`duplicate_enemy`/`duplicate_level_recipe`/`duplicate_weapon`/`duplicate_support`/`duplicate_passive`), and every NEW Epic-6 loot/reward repo (`armor`/`jewelry`/`consumable`/`pickup`/`gold_reward`/`reward_table`) inherits the guard by construction. A rejected duplicate is neither stored (`get_<x>` still returns the FIRST) nor appended to `<x>_ids()` (kept exactly once); a duplicate in a `create_repository_from_definitions` batch returns `null` (fail-closed). NO repo was forked (parity preserved). Pinned by a duplicate-id NEGATIVE test on EVERY content repo (six existing + six new) AND a new `test_content_repository.gd` pinning the central guard directly; the full headless suite re-ran GREEN (exit 0, "Headless tests passed.", zero baseline-registration / `RunStartCommand` content-gate regression). — CLOSED: archived during the Epic 6 closeout (Story 6.7, 2026-06-29).

## Archived during epic 5 closeout (2026-06-26)

### Originally deferred from: code review of 5-2-hero-select-with-playable-and-locked-classes (2026-06-25)

- [Review][Defer → RESOLVED by Story 5.3, 2026-06-25] (Low) `RunState.selected_class_id` does not survive a route-position save/resume — it rehydrates `&""`. This is the documented Decision (c) / Option-A persistence posture: the field rides the FULL `to_dictionary()`/`try_from_dictionary` (lenient-read) but is DELIBERATELY excluded from `to_run_snapshot_fields()` / the board-free route-position save so the pinned 23-key `RunSnapshot` gate stays green untouched. A run resumed from a between-node save currently loses the selected class. AC3 only requires the class in domain state at `NEW_RUN -> ACTIVE_ROUTE` (satisfied), so this is NOT a 5.2 AC violation — it is a forward-obligation assigned to **Story 5.3**, which owns class persistence alongside the derived kit/HP and MUST nest any new run field under `route_state` (never a new top-level `RunSnapshot` key). (Originating: code review of 5-2, Round 1, 2026-06-25; locations `godot/scripts/run/run_state.gd:170` `to_run_snapshot_fields()`, `:234` `try_from_run_snapshot_fields`.) **RESOLUTION (Story 5.3, 2026-06-25):** `to_run_snapshot_fields()` now NESTS `selected_class_id` inside the `route_state` payload (new const `RunState.SELECTED_CLASS_ID_KEY`, the SAME mechanism as `run_phase`); `try_from_run_snapshot_fields()` reads it back lenient (default `&""` when absent). The kit is NOT serialized — it RE-DERIVES from the restored class id through the baseline repositories (a deterministic pure function), keeping the save minimal. NO new top-level `RunSnapshot` key (the pinned 23-key gate stays green, re-verified); `RunResumeService`/`RunSnapshot` untouched. Pinned by `test_run_route_position_save.gd::_selected_class_survives_route_position_resume`, `_pre_5_3_route_position_payload_restores_with_legacy_empty_default`, `_kit_re_derives_from_restored_class_id`, `_composed_class_run_snapshot_stays_within_the_23_key_gate`, and `test_run_state.gd::_selected_class_id_nests_in_the_snapshot_bridge_and_round_trips`. No residual deferral. (Also note: the consolidated 5.4 re-derive-BOTH obligation that extended this — re-deriving the `RulesResolver` as well as the `StartingKit` — was CLOSED by Story 5.5 via `ClassStartSummaryViewModel.re_derive_kit`/`re_derive_resolver`, pinned by `test_class_start_re_derive.gd`; that note remains in the active ledger's 5-5 dev-tracked section.)

## Archived during epic 4 closeout (2026-06-23)

### Originally deferred from: code review of 4-1-run-state-and-route-node-model (2026-06-18)

All four Story 4.1 deferrals are now closed (across Stories 4.3 / 4.4 / 4.6) — moved here during the Epic 4 closeout (Story 4.6, the last story of Epic 4).

- [Review][Defer][RESOLVED in Story 4.6, 2026-06-23] (Low) `node_count` in the `run_started` event payload is an unbounded `int` serialized as a raw JSON number, NOT decimal-string encoded like `root_seed` [godot/scripts/core/events/domain_event.gd:65, 424]. It is validated as a non-negative integral and round-trips fine for any realistic value, but it is the only count in the new event that does not follow the project's int64/real-JSON decimal-string rule for values that could (in principle) exceed 2^53. For the MVP route the node count is 8–12, so this can never bite, and 4.1 ships no `run_started` emission site. Owner: whatever story first EMITS `run_started`. (Originating review: code review of 4-1, Round 1, 2026-06-18.) — CLOSED: Story 4.6 (the first `run_started` emitter) re-confirmed `node_count` is structurally bounded to `[8,12]` (the non-boss route count from `RouteGenerator`) — a bounded count, not a seed, so it correctly stays a raw JSON integer (no encoding change). Pinned by `test_run_start_command.gd::_node_count_stays_bounded_across_seed_sample` + the bounded round-trip assertion in `test_domain_event.gd`. Closed as permanently benign.
- [Review][Defer][RESOLVED in Story 4.3, 2026-06-21] (Low) `RouteState.available_choice_ids()` derives choices as `current_node.outgoing_link_ids` minus `cleared_node_ids`, with NO reveal-state gating and NO forward-only/no-backtracking enforcement [godot/scripts/run/route_state.gd:63-76]. A `hidden` linked node is still surfaced as an available choice. CORRECT for Story 4.1 by explicit scope (4.1 records raw choices only; the choose-and-commit command is 4.3). Logged so the Story 4.3 owner adds choice-eligibility filtering in `RouteAdvanceCommand`. (Originating review: code review of 4-1, Round 1, 2026-06-18.) — CLOSED: Story 4.3 added the reveal-gated / cleared-excluded `RouteState.eligible_choice_ids()` (and `is_eligible_choice()`) filter, validated by `RouteAdvanceCommand` — exactly the deferred choice-eligibility filter.
- [Review][Decision → Deferred][RESOLVED in Story 4.4, 2026-06-21] (save-compat semantics) `RunState.try_from_run_snapshot_fields` defaults a missing/blank `route_state.run_phase` to `PHASE_NEW_RUN` while retaining mid-run `current_route_node_id`/`cleared_node_ids` [godot/scripts/run/run_state.gd]. Benign and internally consistent for 4.1 (no emission site, `validate()` passes), so NO 4.1 code change was made. The "phaseless payload → NEW_RUN" rule is a save-compatibility decision that cannot be resolved until a save writer/reader exists. Owner: Story 4.4 (the first persistence-consumer). (Dispositioned by auto-gds at code review of 4-1, Round 1, 2026-06-18.) — CLOSED: Story 4.4 RATIFIED the `PHASE_NEW_RUN` default (internally consistent; the live writer always emits `run_phase`; `validate()` passes) and added the missing test `test_run_state.gd::_phaseless_route_payload_resumes_as_new_run`.
- [Review][Defer][RESOLVED in Story 4.6, 2026-06-23] (Low) `DomainEvent._has_decimal_string_payload` validates the `run_started` payload's `root_seed` with `is_valid_int()` only — NO int64-range/lossless check [godot/scripts/core/events/domain_event.gd:756-762, validator at 417-426]. An out-of-int64 decimal string (e.g. `"99999999999999999999"`, or max-int64+1 `"9223372036854775808"`) passes `is_valid_int()` and is ACCEPTED even though `String.to_int()` saturates/wraps it. Weaker than the Story 3.7 seed-validation discipline (`_decimal_string_round_trips_losslessly` in `ManualSeedLoader.parse_seed`). Cannot bite in 4.1 (no emission site; `root_seed` never decoded to int). Owner: whatever story first EMITS `run_started`. (Originating review: code review of 4-1, Round 2, 2026-06-18.) — CLOSED: Story 4.6 tightened `_has_decimal_string_payload` to ALSO require the int64 lossless round-trip (the exact 3.7 idiom — `_decimal_string_round_trips_losslessly` / `_canonical_decimal_string` ported as private static helpers local to `domain_event.gd`), so an out-of-int64 decimal string is now REJECTED with `invalid_event_payload` + `field: root_seed`. Grep-confirmed single caller (`_validate_run_started_payload`), so no other event regressed; pinned by the out-of-range + max-int64-boundary assertions in `test_domain_event.gd`.

## Archived during epic 3 closeout (2026-06-17)

### Originally deferred from: code review of 3-6-generator-validation-and-bounded-retry (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.7, 2026-06-17] (Low) No broad-seed sweep proves attempt 0 is NEVER validator-rejected, which is the silent-perturbation guard for the attempt-0-unperturbed fingerprint invariant. The pinned seeds for BOTH sizes ([1001, 2002, 3003, 4004, 5005], + 12345) ARE proven to pass the comprehensive validator on attempt 0 (`test_level_validation.gd::_clean_small/_clean_medium_candidate_passes_full_validator` build the candidate via `generate_layout`+`build_board_snapshot` and run the real validator) AND to produce the pinned terrain (the seed-regression tests), so the invariant is solidly anchored for the pinned set. Residual gap: the seed-regression tests call `MediumLevelLayoutGenerator.generate_layout(...)` / `SmallLevelLayoutGenerator.generate_layout(...)` DIRECTLY (bypassing the validator), so a hypothetical UN-pinned, UN-tested seed whose baseline placement self-soft-locks (e.g. blocking enemies that ring a mandatory reward or seal the exit) would be silently REJECTED at attempt 0 and re-rolled with a perturbed seed, drifting that seed's result from its 3.2-3.5 layout with NO fingerprint tripwire. Structurally near-impossible on the sparse, wide-open baseline 8x8/14x12 boards (enemies/rewards drawn from a candidate pool far larger than the budget) and no AC depends on it — the same "correct by construction, broad coverage missing because the failure is unreachable by construction" class as the already-deferred 3-2 reachability-coupling Low and the 3-4/3-5 pool-exhaustion Lows. Fix (when convenient): add a sweep test asserting `LevelGenerator.generate(seed, ...).diagnostics.attempts == 1` (real validator) across a wide seed range (e.g. 1..200) for BOTH size classes — proving the comprehensive validator never silently perturbs a baseline candidate — and/or a `LevelGenerator`-level seed-regression that pins the full-`generate` terrain (not just `generate_layout`). Not blocking — no current seed/board is known to hit it and no AC depends on it. (Originating review: code review of 3-6, Round 1, 2026-06-16.) — CLOSED: Story 3.7's `test_seed_batch_regression.gd` is the first `LevelGenerator.generate`-LEVEL seed-regression. It drives the approved Small + Medium catalog (seeds 1001/2002/3003/4004/5005, both baseline recipes) through the FULL pipeline and asserts per seed `succeeded == true` + `diagnostics.validated == true` + `diagnostics.attempts == 1` (proving the comprehensive validator never silently perturbs a baseline candidate on attempt 0) PLUS the full-`generate` terrain fingerprint (reconstructed from the payload board) matches the pinned value AND AGREES with the `generate_layout` fingerprint the 3.2/3.3 fixtures pin (an explicit cross-check that the two pinning paths can never silently diverge). The full-`generate` terrain pin (not just `generate_layout`) is exactly the requested closure. NOTE: this resolves the approved-catalog portion; a broad 1..200 sweep was not added (the approved-catalog `attempts == 1` proof + the agreement cross-check is the contract the loader/batch mandate required, and a wide sweep would only re-prove the same unreachable-by-construction property). The companion 3-6 Round 2 efficiency Low (`_is_unrecoverable_layout_error` worst-case retries) stays deferred (a `LevelGenerator` change, out of 3.7 scope).

### Originally deferred from: code review of 3-5-enemy-and-reward-placement (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.6, 2026-06-16] (Low) `EntityRewardPlacer.validate_reward_reachability` is TERRAIN-only: its `_flood_reachable` floods over non-WALL terrain and IGNORES blocking enemies (`entity_reward_placer.gd` lines 355-378). Enemies are placed from the shared shrinking pool BEFORE rewards and the baseline enemies all `blocks_movement = true`, so a reward that is terrain-reachable could in principle be sealed off by blocking enemies (a ring, or a single enemy on a 1-wide corridor pinch) and still PASS reachability. NOT an AC2 violation: the story Dev Notes explicitly define AC2 reachability as a 4-neighbour BFS from the entrance over non-WALL cells (terrain-only) and explicitly scope entity-aware / no-soft-lock reachability to Story 3.6's comprehensive validator, which will likely SUBSUME this focused check. The implementation faithfully matches the documented contract. Residual gap: enemy-blocking reward reachability is unmodeled by design until 3.6. Fix: 3.6's no-soft-lock / safe-reachability validator should account for blocking entities when assessing reward reachability (or document terrain-only as the permanent contract). Not blocking — no AC depends on entity-aware reward reachability this story, and the baseline boards leave ample open cells around rewards. (Originating review: code review of 3-5, Round 2, 2026-06-16.) — CLOSED: Story 3.6's `LevelValidator` adds ENTITY-AWARE reachability — a MANDATORY reward (optional == false) must be reachable AROUND blocking entities (the `_flood_entity_aware` flood treats a `blocks_movement`-entity cell as non-traversable), strengthening the placer's terrain-only check; an OPTIONAL reward stays terrain-reachable (guarded/skippable). The placer's focused terrain-only check is UNCHANGED (still test-pinned); the comprehensive validator is the superset. Driven by `test_level_validation.gd::_unreachable_mandatory_reward_fails_when_ringed_by_entities` (reward ringed by blocking enemies fails `unreachable_reward`; verified terrain-reachable-but-entity-sealed via an independent flood) + `_optional_reward_guarded_by_entity_still_passes`.
- [Review][Defer][RESOLVED in Story 3.6, 2026-06-16] (Low) The `unreachable_reward -> PHASE_VALIDATION` mapping branch in `LevelGenerator._map_layout_error` is NOT exercised end-to-end. The placer's `validate_reward_reachability` is unit-tested directly, and the sibling `missing_enemy_repository`/`no_placeable_enemy -> PHASE_ENEMIES` branches ARE driven through `LevelGenerator.generate` in `test_level_generator.gd`, but no test drives an unreachable reward through `LevelGenerator.generate` to assert `failed_phase == PHASE_VALIDATION` + `error_code == unreachable_reward` + `reason == reward_reachability_failed`. Unreachable through the integration path today because generated rewards are reachable by construction and the layout is built internally (mirrors the Medium AC2 readability-rejection mapping, which is also not driven end-to-end). The mapping is correct by inspection. Same "correct branch, integration coverage missing because the failure is unreachable by construction" class as the existing pool-exhaustion/clamp Lows. Fix (when convenient, or fold into 3.6's validator tests): add a `LevelGenerator`-level case that injects an unreachable reward (custom recipe/seed or a test seam) and asserts the `PHASE_VALIDATION`/`unreachable_reward` result. Not blocking — no AC depends on it. (Originating review: code review of 3-5, Round 2, 2026-06-16.) — CLOSED: Story 3.6 added the injected-validator test seam (`LevelGenerator.generate`'s optional 4th `validator` param) and `test_generation_phase_fixtures.gd::_rewards_phase_unreachable_reward_end_to_end` drives an `unreachable_reward` failure END-TO-END through `LevelGenerator.generate`, asserting `failed_phase == PHASE_VALIDATION` + `error_code == unreachable_reward` + a non-empty reason + no payload. The 3.6 validation path maps `unreachable_reward -> PHASE_VALIDATION` via `LevelValidator.phase_for_code` (the same phase the 3.5 `_map_layout_error` branch used), preserving the contract.

### Originally deferred from: code review of 3-3-seed-stable-medium-level-layouts (2026-06-16)

- [Review][Defer][RESOLVED in Story 3.4, 2026-06-16] (Low) `MediumLevelLayoutGenerator.validate_readability()` guards the layout shape with `terrain_grid.size() != height` only — it does NOT guard each inner row's width, unlike the sibling `build_board_snapshot()` (which checks `row.size() != width`). A hand-built candidate with the right row COUNT but a short row would index out of bounds in `_interior_wall_count`/`_is_wall`/`_flood_reachable` and crash rather than return a structured `invalid_layout_shape`. Not reachable by any current caller: `_run_medium_layout_phase` only ever feeds `generate_layout` output (always emits `width`-wide rows), and the malformed-shape test feeds an empty terrain (caught by the row-count guard). Symmetric to the shape-guard hardening the dev DID add to `build_board_snapshot`. Fix (when convenient): extend the `validate_readability` leading guard (or share a single shape-validation helper across `validate_readability` + `build_board_snapshot`) to also reject any row whose width != `width`, and add a short-row candidate test. Not blocking — no AC depends on it and no production path can hit it. (Originating review: code review of 3-3, Round 1, 2026-06-16.) — CLOSED: Story 3.4 added the per-row width guard to `validate_readability` (rejects any row whose width != `width` with the structured `invalid_layout_shape` code), symmetric to `build_board_snapshot`.

## Archived during epic 2 closeout (2026-06-15)

### Originally deferred from: code review of 2-7-between-level-save-snapshot-foundation (2026-06-14)

- [Review][Defer][RESOLVED 2026-06-14 in Story 2.8] (Low) The Composition Contract requires run-level `RunSnapshot.rng_streams` and the embedded tactical `rng_streams` not to "silently disagree without a test." `from_between_level()` makes them equal (both are pure `streams.to_snapshot()` reads at the boundary), but no test asserted `snapshot.rng_streams == <embedded tactical>.rng_streams`; a future refactor snapshotting them at different points could diverge undetected. (Originating review: code review of 2-7, Round 1, 2026-06-14.) RESOLVED: equality assertion added in `test_run_snapshot.gd::_between_level_composes_tactical_snapshot_into_level_state` and in `test_run_resume_service.gd::_resume_run_level_streams_equal_embedded_tactical_streams`.
- [Review][Defer][RESOLVED 2026-06-14 in Story 2.8] (Low) The integration round-trip (`test_between_level_save.gd::_assemble_write_read_reparse_round_trip_preserves_fidelity`) restored the embedded tactical `rng_streams` and asserted only that the restore succeeds, not that the restored tactical streams reproduce the same next draw (the run-level streams got that stronger check; the embedded tactical ones did not). (Originating review: code review of 2-7, Round 1, 2026-06-14.) RESOLVED: `test_resume_flow.gd::_embedded_tactical_streams_reproduce_next_draw_after_resume` restores the embedded tactical streams and asserts the exact next-draw reproduction across all seven streams, symmetric with the run-level check.


# Archived at epic-11 end (2026-07-06) — auto-gds Phase 8

Entries moved from deferred-work.md: the five 'RESOLVED fences (closed by 11.x)' blocks recording the long-carried deferrals Epic 11 resolved (live combat/death source 11.2; G1/G2/polished-HUD/seam composition 11.3; live affinity call sites + assign-idempotency + L4 11.4; outpost scene/reveal renders/G3 11.5; meta-spend/unlock application 11.6).

RESOLVED fences (closed by 11.6):
- **[Resolved 11.6] The meta-SPEND / unlock APPLICATION** — the canonical "unlock-SPEND / meta-power APPLICATION" fence, carried since Epic 8 and re-recorded by EVERY subsequent meta story (dev-of-8.3/8.4 "a LATER meta-spend story"; review-of-8.7 "turning that state into a playable-class unlock stays deferred"; dev-of-11.2/11.3/11.4/11.5 "the meta-SPEND / unlock APPLICATION (11.6)"). 11.6 authors the spend command + the profile→class-selectability application (FR43) + the capped/sparse posture (FR95). **[Decision] interpretation of 11.5's "GRANT" re-record:** 11.6 makes the recorded `unlock_progress` ACTUALLY APPLY (class selectability) + spends `oath_shards`; it does NOT re-author the award/merge commands (8.3/8.4 already ship+test those; the award/merge remain caller-driven at run-end via `RunEndProfileBridge`, NOT in 11.6's scope to change). The **8.7 AC-wording divergence is now REALIZED**: the profile→class-selectability wiring 8.7's AC1 was explicitly told NOT to build now EXISTS (the profile-aware `HeroSelectViewModel` + `RunStartCommand` gate, both reading `MetaSpendRules.unlocked_class_ids_for`).
- **[Resolved 11.6 — the AC2 seam decision, recorded here] Option A (the profile-aware view model + gate, minimal).** `HeroSelectViewModel` + `RunStartCommand` accept an OPTIONAL trailing `ProfileSnapshot`; a locked class becomes selectable iff (static `LOCK_STATE_SELECTABLE`) OR (its `<class>_unlocked` applied-unlock flag is set, derived via the SINGLE `MetaSpendRules.unlocked_class_ids_for` source both read). The pinned `HeroSelectViewModel.ENTRY_KEYS` + `OutpostViewModel.DICTIONARY_KEYS` are UNCHANGED (the `selectable` field's VALUE becomes profile-aware; no new key; `OutpostViewModel` threads its existing `profile` arg into the composed VM — no new positional arg re-shift). A null profile => byte-identical static behavior.

RESOLVED fences (closed by 11.5):
- **[Resolved 11.5] The outpost SCENE + reveal RENDER + G3** (the fence carried across Epics 8/9, re-recorded by 11.2/11.3/11.4 as "the outpost SCENE + reveal RENDER + G3 (11.5)") — 11.5 renders the `OutpostViewModel` contract on `outpost.tscn` (the meta readout, the four deferred named-space tiles with explicit "coming soon" markers, the embedded run summary gated on `has_summary`, the recovery banner) and MAKES + implements the G3 coupling (below).
- **[Resolved 11.5] The first-victory REVEAL RENDER on the outpost** (the 9.4 AC3 render `[Decision]` — "a later UI story wires the reveal onto `OutpostViewModel`") — **[Decision] AC2 Option A:** added a `first_victory_beat` sub-dict to `OutpostViewModel` (a new constructor arg at position 4 + a new pinned `DICTIONARY_KEYS` entry + a `first_victory_beat()` accessor), symmetric with `first_death_beat`. The presenter renders both beats on their `has_beat` gate with a Dismiss control (a pure presentation no-op — FR65); both are off the critical path (FR64).
- **[Resolved 11.5] The G3 Oath-Shard EARNED-count summary↔profile coupling** (Epic-8 T5 / Epic-9 T4) — **[Decision] AC4 Option A (the honest as-is):** the AWARDED total is read from `profile.oath_shards` (surfaced at the outpost level via `OutpostRenderView.awarded_oath_shards()`); the summary shows an honest "not yet tallied" note. `RunSummary.profile_meta.oath_shards_earned` STAYS `0`/`not_yet_supported` (NOT wired non-zero — the 8.2/8.4 pinned contract is intact; no summary→profile coupling).
- **[Resolved 11.5] The first-death BEAT render on the outpost scene** (8.5/8.6 shipped the beat DATA) — the presenter renders `OutpostViewModel.first_death_beat` on its `has_beat` gate; the dismiss is a structural no-op (the latch was set by the record command in the bridge, independently of the display).
- **[Resolved 11.5 — the scene-level test] Epic-8 T4: the loaded-profile + recovery combination** — added as a RefCounted render-decision test (per the Epic-11 scene-free-harness constraint — NO SceneTree test): `test_outpost_render_view.gd` asserts the presenter's render seam branches correctly on the write-failure real-totals-behind-retry mode (`has_profile == true`, real oath_shards, retry reachable) vs the load-failure fresh 0-shard fallback. The scene-load compile guardrail covers `outpost.tscn`.

RESOLVED fences (closed by 11.4):
- **[Resolved 11.4] The live AFFINITY call sites + affinity board treatment** (the fence carried across Epics 5/6/7 as "the later HUD/run-flow / live-tactical-loop story," re-recorded by 11.2 + 11.3) — 11.4 wires the call sites: Darkness visibility/memory read + fairness on the live board, Scorched HAZARD stamp + burning DoT, Cursed rule-source seating, Flooded conductive/pathing MARKS surfaced via the read model, and the affinity treatment/cues/hazard cells rendered on the board/HUD/inspect. Done EXACTLY per the parked residual wording (7.5/7.6): the live path CALLS `apply_board_effects` / ticks the DoT / seats the Cursed rule source / surfaces the preview / computes the reduced visible set via the read surfaces / runs `check_board`.
- **[Resolved 11.4] The 7.4 `assign_affinity` once-per-node idempotency guard** (the 7.4 Round-1 review deferral) — 11.4 IS the per-node assign call site: `resolve_combat_node_live` assigns the node's affinity ONLY when `assigned_affinity_for(node.id)` reads neutral/absent, so a re-drive reads the recorded id back and never re-rolls the `map` stream (proven in `test_live_affinity_flow.gd::_pre_assigned_node_is_not_re_rolled_on_a_re_drive`).
- **[Resolved 11.4] The 11.3 L4 — the shell never renders the live board on a combat node** (the L3/L4 deferrals "pair with Story 11.4") — 11.4 addresses L4: `resolve_combat_node_live` now returns `"board": board` in the victory/defeat metadata, and `gameplay_shell_presenter._drive_current_stage` renders the live affinity board (`_render_live_board(...)` with the assigned affinity id + the fairness verdict). The L3 auto-resolve→tap-loop handoff STAYS deferred (below) — the affinity board renders correctly under auto-resolve, so the full interactive tap-loop is not required for 11.4's AC2 on-screen requirement.

RESOLVED fences (closed by 11.3):
- **[Resolved 11.3] Contract gap G1 — the in-run HUD run-context projection** (the 11.1 appendix §1.3 / §16 G1; recorded by 11.1, resolved by neither 11.1 nor 11.2) — 11.3 Task 2 adds `RunHudViewModel` (`godot/scripts/ui/view_models/run_hud_view_model.gd`): a thin fail-closed RefCounted read surface that aggregates the run-level context the tactical board VM does NOT carry, from the EXISTING domain sources — hero HP (the hero `TacticalEntityState` on the live board during a level / `StartingKit.baseline_hp` baseline between levels; there is NO run-level HP field on `RunState`, which is WHY the projection exists), node progress (`RouteState.cleared_node_ids` count vs `RouteState.nodes()`), gold (`RiskEconomyState.gold`), inventory occupancy (`InventoryState`). Pins an exact key set + a `has_*` absent-state gate; mints no event, consumes no RNG, mutates nothing, leaks no live handle. Unit-tested (`test_run_hud_view_model.gd`).
- **[Resolved 11.3] Contract gap G2 — the route/run-map view model** (the 11.1 appendix §5.2 / §16 G2) — 11.3 Task 3 adds `RouteMapViewModel` (`godot/scripts/ui/view_models/route_map_view_model.gd`): a thin fail-closed RefCounted route projection over the pinned route reads — `current_node_id`, `cleared_node_ids`, the SELECTION-legal `eligible_choice_ids()` (reveal-gated — NOT the looser `available_choice_ids()`), and per-node `type`/`reveal_state`/`depth`/`outgoing_link_ids`/`clues` + derived `is_current`/`is_cleared`/`is_eligible`. Owns no route truth (the node commit is the EXISTING `RouteAdvanceCommand` the flow submits). Unit-tested (`test_route_map_view_model.gd`).
- **[Resolved 11.3] The polished-HUD-scene deferral (carried from Story 2.5, ledger ~1033-1041)** — "a `Control`/scene presenter under `godot/scripts/ui/presenters/` and `godot/scenes/ui/layouts/<profile>/` left for a later story that grows the polished HUD … keep tactical layout decisions in a testable semantic profile first." 11.3 IS that story for the run-flow HUD: it builds the `Control` presenters + the profile-aware HUD scene ON TOP of the already-testable `TacticalLayoutProfile` semantic plan (which stays the source of truth — the scene honors it, does not replace it; the phone→desktop rule-invariance is proven at the VM layer in `test_run_flow_layout_invariance.gd`).
- **[Resolved 11.3] The composition of the 11.2 live-pre-boss + boss-auto-play seam** (the human-acknowledged 2026-07-05 boundary; 11.2 review `[Review][Decision]`) — 11.3 composes them at the SCENE/orchestration layer via `RunFlowController.play_hands_off_to_run_end` (drives `run_to_completion_live` to the boss terminus, then `auto_play_boss_fight` to `resolve_boss_victory()`), NEVER touching the DEFAULT fingerprint-preserving `run_to_completion`. The scene-driven boss/board placement adopts 11.2's fail-closed validate-then-reject discipline (it reuses `auto_play_boss_fight`, which carries the two Round-1 hardenings). Proven headlessly on the verified finale seed 4242 (`test_run_flow_controller.gd`).

RESOLVED fences (closed by 11.2):
- **[Resolved 11.2] The LIVE combat-DEATH CALL SITE / hero-death → `PHASE_FAILED` live SOURCE** (deferred across Epics 8-9; ledger lines ~15/~66/~83/~104/~146/~532) — 11.2 AC2 wires the live source: a live combat DEFEAT (a hero felled on the board, `CombatOutcomeState.STATE_DEFEAT`) AUTO-FIRES `RunOrchestrator.resolve_run_end(&"hero_death")` → `CompleteRunCommand` → `PHASE_FAILED` + `run_failed` cause `hero_death` + `next_destination == outpost`, and a hero death DURING the boss auto-play auto-fires `resolve_run_end(&"boss_defeat")`. FR32's loss half is now triggerable LIVE; the first-death latch (`RecordFirstDeathCommand`) records off the REAL terminal FAILED state (proven in `test_live_run_flow.gd` + `test_finale_full_run.gd`). The auto-fire runs BEHIND the `run_already_terminal` guard (a re-detection never double-fires). The default auto-resolve path stays un-wired (determinism preserved) — the live source is the OPT-IN live driver.
- **[Resolved 11.2] `RunOrchestrator.resolve_boss_victory()` has no live production call site** (the 9.5 review `[Decision]`; ledger line ~13) — 11.2 AC3 gives it a PRODUCTION call site: `auto_play_boss_fight` / `auto_play_full_run` auto-play the full boss fight (both sides simulated) to the boss at 0 HP, then call `resolve_boss_victory()` (clears the boss node + `resolve_run_end(victory)` → `PHASE_COMPLETED` + `run_completed(victory)`), threading the shared sequence-id cursor through `resolve_phase_transitions → detect_boss_defeat → the run-end append`. The first-victory latch (`RecordFirstVictoryCommand`) records off the REAL auto-played victory. Proven in `test_finale_full_run.gd::_auto_play_full_run_reaches_victory_through_resolve_boss_victory` (+ byte-determinism + the sequence-id uniqueness).
- **[Resolved 11.2] The `_resolve_completed` step-2-failure restore forcing test** (Epic-9 retro T3; the 9.4 review `[Review][Defer]`; ledger line ~42) — the live victory path (AC3) drives the `ACTIVE_ROUTE → NODE_RESOLUTION → COMPLETED` two-step for real, so 11.2 adds a FORCING test (`test_complete_run_command.gd::_two_step_completion_restores_phase_on_a_forced_step_two_failure`) via a test-only `ForcedStepTwoFailureRunState` subclass that makes step 2 reject while step 1 succeeds, asserting the run RESTORES to byte-identical `phase_before` (ACTIVE_ROUTE) — no longer only the observable-invariant assertion. The `RunEndOutcome` allowlist fallback stays directly covered (the 9.4 note — no new work).
