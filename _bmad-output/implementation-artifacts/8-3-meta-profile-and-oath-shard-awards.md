# Story 8.3: Meta Profile and Oath Shard Awards

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want eligible runs to award shallow meta progress,
so that repeated descents expand options without becoming a stat grind.

## Acceptance Criteria

(Verbatim from `planning-artifacts/epics.md` lines 2076-2107, Story 8.3; GDD FR95 + implementation FR67 — "capped, sparse meta power" (epics line 399); FR59 — "The outpost loop must support Oath Shards, Echoes, Seal Fragments, class mastery progress, unlock progress, and a shallow MVP meta menu or tree"; FR28 — "Manual seed runs must not grant meta progression." The THIRD story of Epic 8. 8.1 shipped the run-END boundary (`run_failed`/`run_completed` + terminal phase + the `run_already_terminal` idempotency guard); 8.2 shipped the DERIVED `RunSummary` read aggregator (which REPORTS `profile_meta.oath_shards_earned == 0` and `meta_progression_eligible` READ-ONLY). 8.3 is the FIRST story that AWARDS meta progress and creates the FIRST persistent CROSS-RUN state: a versioned `ProfileSnapshot` + `ProfileRepository`, an Oath-Shard award CALCULATION from approved rules, a deterministic meta-award domain EVENT, and the FR28 manual-seed denial — all sitting BEHIND the 8.1 idempotency guard so a re-completion never re-awards.)

**AC1 — An eligible non-manual run awards Oath Shards (+ eligible progress) from approved rules; the profile is updated through a versioned repository**
- **Given** a non-manual eligible run ends
- **When** meta award calculation runs
- **Then** Oath Shards and eligible progress are calculated from approved rules
- **And** the profile snapshot is updated through a versioned repository.

**AC2 — Profile changes are recorded through deterministic profile/meta events or repository results; run/profile/unlock state stay separable**
- **Given** meta awards are applied
- **When** profile state changes
- **Then** changes are recorded through deterministic profile/meta events or repository results
- **And** run state, profile state, and unlock/content state remain separable for save, replay, and tests.

**AC3 — Direct power gains are capped, sparse, and secondary; raw-stat ladders are REJECTED for MVP (unless capped onboarding smoothing)**
- **Given** meta progression awards or unlocks can affect future runs
- **When** meta content validation runs
- **Then** direct power gains are capped, sparse, and secondary to variety, knowledge, classes, loot pools, passives, secrets, codex entries, starting options, or class mastery
- **And** broad raw-stat ladders such as repeatable damage, max HP, armor, crit, or dodge upgrades are rejected for MVP unless explicitly approved as capped onboarding smoothing.

**AC4 — A manual-seed run awards NOTHING; the summary shows a replay/practice warning**
- **Given** a manual seed run ends
- **When** meta award calculation runs
- **Then** Oath Shards, class mastery, unlock progress, and other meta rewards are not granted
- **And** the summary shows a replay/practice warning.

**AC5 — A profile SAVE FAILURE exposes a structured recovery path and does NOT silently lose the run summary**
- **Given** profile save fails
- **When** the repository reports the failure
- **Then** the system exposes a structured recovery path
- **And** it does not silently lose current run summary data.

## Scope boundary (read FIRST)

This story builds the **META PROFILE + OATH-SHARD AWARD path** — the "receive eligible progress" half of Epic 8 ("die or finish a run, return to the last outpost, review what happened, **receive eligible progress**, start another descent"). It ships FOUR tightly-scoped pieces:
1. A **versioned cross-run `ProfileSnapshot`** (a scene-free serializable value object) — the FIRST persistent CROSS-RUN state in the project.
2. A **`ProfileRepository`** with atomic-write + versioned-parse + migration + structured-error semantics, mirroring `SaveRepository` VERBATIM but with its OWN path/snapshot/schema.
3. An **Oath-Shard award CALCULATION from approved rules** (deterministic, capped, sparse) that reads the terminal run + `RunSummary` + eligibility and produces an award amount BEHIND the idempotency + eligibility gates.
4. A **deterministic meta-award domain EVENT** (appended to `DomainEvent.Type`) that RECORDS the award (AC2), plus the FR28 manual-seed denial (AC4) and the AC5 save-failure structured recovery.

**⭐ THE SINGLE MOST IMPORTANT ARCHITECTURAL FACT — this is the project's FIRST persistent CROSS-RUN state.** Everything before this persisted only WITHIN a run (`RunSnapshot` = the run autosave). The profile OUTLIVES a run: it accumulates Oath Shards across many descents. The retro-notes (Epic-7 §7 Action T2 heads-up, re-stated for 8.3) mandate: **the profile is its OWN snapshot (a SEPARATE `ProfileSnapshot` + `ProfileRepository` + save file), NOT nested under `route_state` and NOT the `RunSnapshot`** — with **migration coverage from the START** (its OWN `SCHEMA_VERSION` + an `unsupported`/migration path baked in on day one). This is the ledger's + retro's most-repeated 8.3 constraint.

**⭐ THE AWARD SITS BEHIND TWO GATES — a re-completion NEVER re-awards, a manual-seed run NEVER awards:**
- **Gate 1 (idempotency, the 8.1 seam):** the award must run BEHIND the `run_already_terminal` idempotency guard. A run-end is resolved exactly ONCE (`CompleteRunCommand`/`RunOrchestrator.resolve_run_end` reject a re-resolution of an already-terminal run with `run_already_terminal` — ZERO second event, ZERO mutation). The award fires as part of / after that single successful run-end resolution — never on a re-completion. `RunSnapshot.oath_shards` (the run-scoped AWARDED count) stays 0; the CROSS-RUN awarded total lives in the profile.
- **Gate 2 (eligibility, FR28/AC4):** the award reads `RunState.meta_progression_eligible` (top-level, lockstep with `not is_manual_seed`, asserted in `RunState.validate()`) and/or `RiskEconomyState.oath_shard_eligible` (kept in lockstep). A manual-seed run (`meta_progression_eligible == false`) awards **NOTHING** — no Oath Shards, no class mastery, no unlock progress (AC4). The eligibility gate is a HARD invariant, not a soft check.

**⭐ AC3 IS A CONTENT-DESIGN CONSTRAINT WITH TEETH.** The award rule must be **capped + sparse + secondary to variety**. For v0 the RECOMMENDED award is a SMALL, CAPPED, deterministic Oath-Shard grant (e.g. a fixed small amount per eligible completed run, or a small amount scaled ONLY by a bounded, non-difficulty run signal like nodes-cleared with a hard CAP) — NOT a raw-stat ladder. `[Decision]` — pick + record the exact rule. It MUST be:
- **DETERMINISTIC** (ZERO RNG — same terminal run → same award; the named-RNG rule + the whole-epic determinism invariant). NO `randi`/`randf`/`RandomNumberGenerator`; `RngStreamSet.required_streams()` stays 7 streams.
- **CAPPED** (a hard maximum per run; the award cannot grow unbounded — AC3 "capped").
- **SPARSE + SECONDARY** (a shallow trickle that expands OPTIONS over time, not a stat grind — the user story "without becoming a stat grind" + AC3 "secondary to variety").
- **NOT a raw-stat ladder** (AC3 second half: repeatable damage / max-HP / armor / crit / dodge upgrades are REJECTED for MVP). Oath Shards are a currency toward variety/knowledge/options (Epic-8/9 unlock spend); they are NOT a direct combat-stat buff. v0 AWARDS the currency; it does NOT build the unlock-SPEND tree (8.6/later) or apply any stat.

**⭐ NOT-YET-SUPPORTED FIELDS STAY DEFERRED (8.4 owns them).** 8.3 awards **Oath Shards** (the currency). Echoes / Seal-Fragments / class-mastery-points / unlock-progress are **Story 8.4** — 8.3 does NOT author that content or merge it into the profile. AC1's "eligible progress" and AC4's "class mastery, unlock progress" are named because the FR28 DENIAL must cover them (a manual-seed run grants none of them), and because the profile shape should have a HOME for them (an empty/0 placeholder the 8.4 merge fills) — but 8.3 GRANTS only Oath Shards. Keep the `RunSummary`'s `not_yet_supported` list (`["oath_shards_earned", "echoes_discovered", "unlock_progress"]`) honest: after 8.3, `oath_shards_earned` in the summary can reflect the REAL awarded amount for the just-ended eligible run (if you choose to feed it — see Task 5), but `echoes_discovered`/`unlock_progress` STAY not-yet-supported placeholders (8.4).

**⭐ AC5 IS THE `SaveRepository` STRUCTURED-ERROR PATTERN.** A profile save failure must return a STRUCTURED `ActionResult.error(...)` (a stable lower_snake code + diagnostic metadata) — NEVER a silent swallow, NEVER a crash, NEVER a loss of the current run summary. Mirror `SaveRepository.write_run_snapshot`'s atomic temp→backup→replace write with structured `save_open_failed`/`save_backup_failed`/`save_replace_failed` codes. AC5's "does not silently lose current run summary data" means: the run `RunSummary` (a DERIVED read, 8.2) is INDEPENDENT of the profile save — a failed profile write leaves the summary fully readable; the caller can surface the summary + the structured save-error to a recovery UI (8.6). 8.3 makes the FAILURE VISIBLE and RECOVERABLE; it does not build the recovery UI (8.6).

Do NOT redefine established patterns — REUSE them. 8.3 reuses: the `SaveRepository` atomic-write + versioned-parse + structured-error discipline (its DIRECT template — copy the write/read/parse shape, change the path + schema); the `RunSnapshot`/`RiskEconomyState`/`InventoryState` exact-key `to_dictionary()` + lenient `try_from_dictionary`/`parse` value-object discipline (a key never silently appears/vanishes; pinned by test); the `DomainEvent` append-only enum + factory + validator + `expected_ids`-exhaustiveness-pin discipline (a new event is appended at the enum END, gets a factory + a payload validator, and is registered in `test_domain_event.gd` `expected_ids` — the pin TRIPS by design, exactly as 8.1's `run_failed` did); the 4.3 validate-then-mutate command idiom IF you model the award as a command (validate → award → event, ZERO events + no mutation on reject); the named-RNG rule (draw ZERO RNG — a deterministic calculation, no roll); the difficulty-non-goal guard (the award does NOT scale by difficulty). It builds NO outpost `.tscn`, NO unlock-spend tree, NO Echo/unlock content, NO first-death narrative.

---

## IN scope (this story)

1. **The versioned cross-run `ProfileSnapshot`** — a scene-free `RefCounted` value object (RECOMMENDED `godot/scripts/save/snapshots/profile_snapshot.gd`, `class_name ProfileSnapshot`) with its OWN `SCHEMA_VERSION` (start at 1) + `content_version`, an exact-key `to_dictionary()`, and a lenient/versioned `parse(data) -> ActionResult` that REJECTS an unsupported schema with a stable `unsupported_profile_schema` code (mirroring `RunSnapshot.parse`'s `unsupported_save_schema`). Fields: at minimum `schema_version`, `content_version`, `profile_id` (default "default"), `oath_shards` (the CROSS-RUN awarded total, an int), plus HOMES for the 8.4 content as empty/0 placeholders (RECOMMENDED `class_mastery: Dictionary`, `echoes: Array[String]`, `unlock_progress: Dictionary`, `first_death_recorded: bool` for 8.5) so 8.4/8.5 MERGE into an existing shape without a migration. `[Decision]` — pick the exact field set + record it. [Source: `godot/scripts/save/snapshots/run_snapshot.gd` (the versioned-snapshot template: `SCHEMA_VERSION`, exact-key `to_dictionary()`, `parse` with `unsupported_save_schema`, lenient decode helpers); `godot/scripts/run/risk_economy_state.gd` (the value-object exact-key + lenient `try_from_dictionary` + deep-copy template)]
2. **The `ProfileRepository`** — a `RefCounted` (RECOMMENDED `godot/scripts/save/profile_repository.gd`, `class_name ProfileRepository`) mirroring `SaveRepository` VERBATIM but with a SEPARATE path (RECOMMENDED `user://profile.json` — NOT the run autosave path) and the `ProfileSnapshot` schema: `write_profile(snapshot, path=DEFAULT_PROFILE_PATH) -> ActionResult` (atomic temp→backup→replace with structured `profile_save_*` codes — AC5), `read_profile(path=DEFAULT_PROFILE_PATH) -> ActionResult` (structured `profile_not_found`/`profile_open_failed`/`profile_parse_failed`; on `profile_not_found` the CALLER can start a FRESH profile — AC5 recovery, mirrors 8.6's fresh-profile path). `[Decision]` — reuse the exact `SaveRepository` write/read structure. [Source: `godot/scripts/save/save_repository.gd` (the EXACT atomic-write + structured-error template to mirror); `godot/tests/unit/save/test_save_repository.gd` (the save-failure test pattern)]
3. **The Oath-Shard award CALCULATION (AC1, AC3)** — a deterministic, capped, sparse award rule from approved rules (RECOMMENDED a small pure calculator, e.g. `godot/scripts/save/meta_award_rules.gd` or a static on the award command). It reads the terminal `RunState` (phase, eligibility) + optionally the `RunSummary` (nodes_cleared / boss_cleared for a bounded, capped signal) and returns the Oath-Shard amount (an int, ZERO for a failed run if you gate on completion — `[Decision]`, or a smaller amount for a death; record it). It MUST be capped (a hard MAX), deterministic (ZERO RNG), NOT a raw-stat ladder (AC3), and NOT scale by difficulty. Document WHY it satisfies AC3 (capped + sparse + a currency toward variety, not a stat). [Source: `_bmad-output/planning-artifacts/epics.md` Story 8.3 AC3, line 399 (FR95/FR67 capped-sparse-meta-power); the GDD meta-progression pillar]
4. **The meta-award APPLICATION path BEHIND the two gates (AC1, AC2, AC4)** — the seam that: (a) confirms the run is TERMINAL + the run-end was resolved (behind the 8.1 `run_already_terminal` idempotency guard — a re-completion never re-awards); (b) confirms ELIGIBILITY (`meta_progression_eligible` / `oath_shard_eligible`) — a manual-seed run awards NOTHING (AC4); (c) computes the award (Task 3); (d) UPDATES the `ProfileSnapshot` (adds the awarded Oath Shards to the cross-run total); (e) RECORDS the change via a DETERMINISTIC meta-award domain EVENT (Task 5) or the repository result (AC2); (f) PERSISTS via `ProfileRepository.write_profile`, returning the structured result (AC5). `[Decision]` — model this as a run-command (validate-then-mutate, the 4.3 idiom, so a manual-seed / already-awarded / non-terminal input rejects with ZERO mutation + ZERO event) OR as an orchestrator method + a pure calculator; the run-command shape is RECOMMENDED for the reject-with-no-mutation guarantee and test symmetry. Record the choice. **The awarding must be idempotent per run-end** — a re-invocation for the same already-awarded run must not double-award (mirror the 8.1 idempotency; `[Decision]` — either the caller only invokes once behind `resolve_run_end`, or track an awarded marker; record how you guarantee no double-award). [Source: `godot/scripts/core/commands/complete_run_command.gd` (the idempotency guard + validate-then-mutate idiom); `godot/scripts/run/run_orchestrator.gd` lines 705-722 (`resolve_run_end` — the run-end seam the award sits behind); `godot/scripts/run/run_state.gd` lines 215-259 (`is_terminal()`, the `meta_progression_eligible` lockstep invariant)]
5. **The deterministic meta-award domain EVENT (AC2)** — append a NEW event to the `DomainEvent.Type` enum END (RECOMMENDED `OATH_SHARDS_AWARDED` / id `oath_shards_awarded`; `[Decision]` name it) with a factory + a payload validator (mirroring `run_failed`/`economy_changed`: lower_snake fields, `_has_nonnegative_integral_payload` for the amounts, a `reason` marker). Payload RECOMMENDED: `amount` (awarded this run), `oath_shards_before`, `oath_shards_after`, `reason` (a lower_snake marker, e.g. `run_completed_eligible`), `profile_id`. **REGISTER the new id in `test_domain_event.gd` `expected_ids`** (the exhaustiveness pin — it TRIPS by design; this is the documented epic-transition heads-up, same as 8.1's `run_failed`). Draw ZERO RNG (a recorded amount, not a roll — the `economy_changed` deterministic shell). [Source: `godot/scripts/core/events/domain_event.gd` (the append-only enum + factory + `_validate_*_payload` allowlist idiom — `run_failed` lines 300-318 / validator 1053-1072, `economy_changed` lines 420-439 are the closest templates); `godot/tests/unit/core/test_domain_event.gd` lines 2120-2181 (`expected_ids` exhaustiveness pin — MUST add the new id or the suite fails by design)]
6. **The FR28 manual-seed denial (AC4) + the summary replay/practice warning** — the award path denies ALL meta rewards for a manual-seed run (Oath Shards, class mastery, unlock progress). The `RunSummary` ALREADY reports `meta_progression_eligible == false` + `is_manual_seed` for a manual-seed run (8.2) — that IS the replay/practice warning DATA the summary carries (AC4's "the summary shows a replay/practice warning"). `[Decision]` — confirm the 8.2 summary's `meta_progression_eligible`/`is_manual_seed` fields satisfy AC4's warning (they do — the warning is DATA the 8.6 UI renders), OR if a more explicit `replay_practice_warning` signal is wanted, add it to the summary read (a small additive read field; keep the exact-key discipline + update the pinned key test). RECOMMENDED: the existing `meta_progression_eligible == false` + `is_manual_seed == true` IS the warning data; do NOT over-engineer. Record the choice. [Source: `godot/scripts/run/run_summary.gd` lines 132-160 (the `meta_progression_eligible`/`is_manual_seed` fields already surfaced); AC4]
7. **The AC5 save-failure structured recovery** — `ProfileRepository.write_profile` returns a STRUCTURED error on any write failure (open/backup/replace) — never a silent swallow, never a crash. The award path SURFACES that structured result to its caller. AC5's "does not silently lose current run summary data": the `RunSummary` is a DERIVED read INDEPENDENT of the profile write — prove (test) that a failed profile write leaves the summary fully readable + the structured error surfaced (so a recovery UI can show the summary + retry the save). [Source: `godot/scripts/save/save_repository.gd` (the structured-error write template); AC5]
8. **Full unit tests (AC1-AC5)** — an ELIGIBLE COMPLETED run awards the expected capped Oath-Shard amount + updates the profile + emits the meta-award event + persists (round-trip the profile through the repository); a MANUAL-SEED run awards NOTHING (0 shards, no event, profile unchanged — AC4) + the summary carries the replay/practice warning data; a FAILED run awards per the chosen rule (`[Decision]` — 0 or a smaller amount); a RE-INVOCATION for an already-awarded / already-terminal run does NOT double-award (idempotency — AC1/8.1 seam); the profile `ProfileSnapshot` has an EXACT pinned key set + a JSON round-trip preserves it; an UNSUPPORTED profile schema rejects with `unsupported_profile_schema` (migration path — AC1/AC2); a PROFILE-NOT-FOUND read returns the structured `profile_not_found` (fresh-profile recovery — AC5); a PROFILE SAVE FAILURE returns the structured error + leaves the summary readable (AC5); the award is DETERMINISTIC (same run twice → same amount) + draws ZERO RNG; the AC3 cap is enforced (an over-cap signal clamps to the MAX); run/profile/unlock state stay SEPARABLE (the profile is its OWN snapshot, not the RunSnapshot — AC2). [Source: `godot/tests/unit/save/test_run_snapshot.gd` + `test_save_repository.gd` (the snapshot round-trip + repository save/read/failure test patterns); `godot/tests/unit/run/test_run_summary.gd` (the exact-key + manual-seed-eligibility test pattern); `CLAUDE.md` (the full-suite command)]
9. **Full headless suite + diff hygiene + scope-fence + deferred-work disposition** — run the full suite (PowerShell), false-PASS grep guard, `git diff --check`, confirm the scope fences (see Task 9 in Tasks), update `deferred-work.md` + `sprint-status.yaml` + the story status.

## OUT of scope (explicitly later stories — do NOT pull forward)

- **Echoes, Seal Fragments, class-mastery points, and unlock-progress DOMAIN content + merging into the profile** → **Story 8.4.** 8.3 awards ONLY Oath Shards; it provides EMPTY/0 profile HOMES for the 8.4 content (so 8.4 merges without a migration) but authors NO Echo/Seal-Fragment/mastery/unlock content and decides NO unlock-threshold rules. The `RunSummary`'s `echoes_discovered`/`unlock_progress` STAY not-yet-supported placeholders. [Source: `_bmad-output/planning-artifacts/epics.md` Story 8.4]
- **The OUTPOST MENU scene / view-model / the meta DISPLAY / the unlock-SPEND tree / start-another-descent / the fresh-profile recovery UI** → **Story 8.6.** 8.3 produces the profile DATA + the award + the structured save-error; it builds NO outpost `.tscn`, NO `OutpostViewModel`, NO unlock-spend UI, and NO recovery screen (it makes the failure RECOVERABLE via structured results; 8.6 renders the recovery). UI-scene-last. [Source: `_bmad-output/planning-artifacts/epics.md` Story 8.6; `project-context.md` UI-scene-last]
- **The meta + summary SAVE/LOAD TEST SUITE (headless, restart-survival, migration matrix)** → **Story 8.7.** 8.3 SHIPS the versioned profile + repository + its OWN migration reject path + round-trip tests for the AWARD; 8.7 owns the COMPREHENSIVE meta/summary save-load test matrix (Oath Shards / Echoes / Seal Fragments / unlock progress / first-death flags / class unlock states restore correctly; the migration matrix; "no scene nodes serialized"). 8.3 does NOT build the full 8.7 matrix — it builds the profile save + the award-path save + enough tests to prove AC1/AC2/AC5. [Source: `_bmad-output/planning-artifacts/epics.md` Story 8.7]
- **The first-death narrative line ("Good. You remembered how to die.") + the first-death flag delivery** → **Story 8.5.** 8.3 MAY reserve a `first_death_recorded: bool` HOME in the profile snapshot (so 8.5 merges without a migration) — `[Decision]` optional — but it does NOT track/set the first-death flag, does NOT deliver narrative, and does NOT build a narrative surface. [Source: `_bmad-output/planning-artifacts/epics.md` Story 8.5]
- **The unlock-SPEND / meta-power APPLICATION (turning Oath Shards into an actual variety/option unlock or a capped onboarding stat)** → a later meta-spend story (8.6+/Epic 9). 8.3 AWARDS the currency (the cross-run total rises); it does NOT SPEND it, does NOT apply any stat/passive/class from it, and does NOT build the unlock tree. AC3 is about the AWARD being capped/sparse/not-a-stat-ladder — not about a spend system. [Source: AC3; `_bmad-output/planning-artifacts/epics.md` Story 8.6]
- **A LIVE tactical play loop / a real combat-death source / auto-wiring the award into `run_to_completion`** — combat auto-resolves to success (no live death source); the award is CALLER-DRIVEN behind `resolve_run_end` (the caller — a test today, the later HUD/run-flow story tomorrow — invokes the run-end + award explicitly). 8.3 does NOT build the live loop, does NOT auto-wire the award into the auto-resolve loop, and does NOT perturb the interrupted==uninterrupted determinism / v0 auto-resolve posture. [Source: `_bmad-output/auto-gds/retro-notes/epic-8.md` (8.1's live-tactical-loop defer — NOT an Epic-8 dependency); `godot/scripts/run/run_orchestrator.gd` (`resolve_run_end` is caller-driven, NOT in `run_to_completion`)]
- **Replacing the boss PLACEHOLDER with a real Larval Avatar boss + the first-VICTORY reveal** → **Epic 9.** 8.3 reads the run-end outcome the boss boundary already emits (`boss_placeholder` / `completed`). When Epic 9 lands, its real boss victory reuses the SAME run-end vocabulary + the SAME award path. [Source: `_bmad-output/planning-artifacts/epics.md` Epic 9]
- **NOT overlapping (leave parked — do NOT reopen):** the two 8.1 `[Review][Defer]` items (the `CompleteRunCommand._resolve_completed` two-step atomicity + the `RunEndOutcome.for_*` allowlist-validation — command/DTO-validation concerns, NOT award concerns; 8.3 READS the terminal run + the events' already-validated markers, so neither is in scope); the 8.2 Round-2/3 `RunSummary` stale-comment nit (RESOLVED already; not an award concern); the live-tactical-loop call site + affinity HUD/VFX (Epic-7 retro T1); the seated-Cursed / assigned-affinity re-derive-on-resume (a later in-node-save/live-resume story); the 7.4 `assign_affinity` once-per-node idempotency guard; the Flooded conductive electric-interaction placeholder (Epic-10 readiness); the int64-overflow economy ceiling + the constant-8-tier route-depth pacing (Epic 10); the board-snapshot cell-coercion / occupant-schema-migration defers (code review of 1-3). None overlap 8.3's award/profile area. [Source: `_bmad-output/implementation-artifacts/deferred-work.md` passim; `_bmad-output/auto-gds/retro-notes/epic-8.md`]

If an AC seems to demand a live combat loop, an outpost scene, or a spend tree, re-read: AC1/AC2 demand an award "when a run ends" that is "calculated from approved rules" + "recorded through deterministic events or repository results" + persisted "through a versioned repository" — a pure calculator + a run-command/orchestrator seam + a `ProfileRepository` satisfies this headlessly WITHOUT a live loop (the caller supplies the terminal run; the award fires behind `resolve_run_end`). AC3 is a content-rule constraint on the AWARD (capped/sparse/not-a-stat-ladder), satisfiable by a small capped deterministic rule + a validation test. AC4 is the eligibility gate (already in `RunState`/`RiskEconomyState`). AC5 is the `SaveRepository` structured-error pattern applied to the profile. NONE of the five ACs demands a live tactical loop, an outpost scene, an unlock-spend tree, or the 8.4 Echo/unlock content.

## Tasks / Subtasks

- [ ] **Task 1 — The versioned cross-run `ProfileSnapshot` (AC1, AC2)** — a scene-free `RefCounted` value object with its OWN schema
  - [ ] **Resolve the ⭐ shape `[Decision]` first (see the scope boundary) and record it in Completion Notes:** RECOMMENDED `class_name ProfileSnapshot` in `res://scripts/save/snapshots/profile_snapshot.gd` with `const SCHEMA_VERSION: int = 1`, `content_version` (default "mvp-0"), `profile_id` (default "default"), `oath_shards: int` (the CROSS-RUN awarded total), plus empty/0 HOMES for 8.4/8.5 content (RECOMMENDED `class_mastery: Dictionary = {}`, `echoes: Array[String] = []`, `unlock_progress: Dictionary = {}`, and OPTIONALLY `first_death_recorded: bool = false` for 8.5). Mirror `RunSnapshot`'s structure VERBATIM.
  - [ ] **Exact-key `to_dictionary()` + versioned `parse(data) -> ActionResult`:** a key never silently appears/vanishes (pinned by test). `parse` REJECTS an unsupported `schema_version` with a stable `unsupported_profile_schema` code + `{expected_schema_version, actual_schema_version}` metadata (mirror `RunSnapshot.parse` lines 71-78). Lenient decode of each field (a missing/invalid value defaults cleanly — the `RiskEconomyState.try_from_dictionary`/`RunSnapshot._int64_or_zero` leniency). `oath_shards` is a SMALL bounded int (NOT a seed — no decimal-string encoding; the `RiskEconomyState.gold` precedent); if you ever store a seed-scale value, decimal-string-encode it (you should not need to).
  - [ ] **Deep-copy discipline:** `to_dictionary()` returns a FRESH dict each call (deep-copied sub-dicts/lists — the `RunSnapshot.to_dictionary()` `.duplicate(true)` precedent) so a mutation of the returned dict never perturbs the snapshot.
  - [ ] Tests: an exact pinned key set; a JSON `stringify`→`parse` round-trip preserves every field; an unsupported schema rejects with `unsupported_profile_schema`; a partial/legacy dict parses leniently (missing fields default); the 8.4/8.5 HOMES are present + empty/0 in v0.

- [ ] **Task 2 — The `ProfileRepository` (AC1, AC5)** — atomic-write + structured-error, mirroring `SaveRepository`
  - [ ] RECOMMENDED `class_name ProfileRepository` in `res://scripts/save/profile_repository.gd`, `const DEFAULT_PROFILE_PATH := "user://profile.json"` (a SEPARATE path from the run autosave `user://run_autosave.json` — the profile is its OWN save file, NOT the RunSnapshot).
  - [ ] `write_profile(snapshot: ProfileSnapshot, save_path: String = DEFAULT_PROFILE_PATH) -> ActionResult` — copy `SaveRepository.write_run_snapshot` VERBATIM (atomic temp→backup→replace), with `profile_save_*` structured error codes (`profile_save_open_failed` / `profile_save_backup_failed` / `profile_save_replace_failed`, or reuse the `save_*` names with profile-scoped metadata — `[Decision]` record). Returns `ActionResult.ok()` on success, a STRUCTURED error on any failure (AC5 — never a silent swallow).
  - [ ] `read_profile(save_path: String = DEFAULT_PROFILE_PATH) -> ActionResult` — copy `SaveRepository.read_run_snapshot` VERBATIM, returning `profile_not_found` when the file is absent (the CALLER starts a FRESH `ProfileSnapshot` on `profile_not_found` — AC5 recovery + 8.6's fresh-profile path), `profile_open_failed` / `profile_parse_failed` on read/parse errors, else `ProfileSnapshot.parse(parsed)` (which surfaces `unsupported_profile_schema` for a bad schema).
  - [ ] Tests: write→read round-trip restores the profile; a read of a NON-EXISTENT path returns `profile_not_found` (fresh-profile recovery); a save FAILURE returns a structured error (simulate via an unwritable path — mirror `test_save_repository.gd`'s failure test); a read of a MALFORMED-JSON file returns `profile_parse_failed`; a read of an UNSUPPORTED-schema file surfaces `unsupported_profile_schema` (through `ProfileSnapshot.parse`).

- [ ] **Task 3 — The Oath-Shard award CALCULATION from approved rules (AC1, AC3)** — deterministic, capped, sparse
  - [ ] **Resolve the ⭐ award-rule `[Decision]` + record it in Completion Notes.** RECOMMENDED a SMALL, CAPPED, deterministic rule: e.g. a fixed small Oath-Shard grant per ELIGIBLE COMPLETED run, OR a small amount from a bounded run signal (nodes-cleared / boss-cleared) with a HARD CAP (e.g. `min(base + per_node * nodes_cleared, MAX_AWARD)`). Whatever you pick MUST be: DETERMINISTIC (ZERO RNG — same terminal run → same award), CAPPED (a hard `MAX_AWARD` constant), SPARSE + SECONDARY (a shallow trickle, not a stat grind), and NOT a raw-stat ladder (AC3 second half — Oath Shards are a currency toward variety, NOT a combat stat). Also decide the FAILED-run amount (`[Decision]` — 0, or a smaller consolation amount; a manual-seed run is ALWAYS 0 regardless via Gate 2).
  - [ ] RECOMMENDED a small pure calculator (`godot/scripts/save/meta_award_rules.gd`, `class_name MetaAwardRules`, a static `oath_shard_award_for(run: RunState, summary: RunSummary) -> int` OR read the bounded signal off the summary) — a PURE read, ZERO RNG, ZERO mutation. Document in the header WHY it satisfies AC3 (capped + sparse + a currency, not a stat).
  - [ ] Tests: an eligible completed run yields the expected capped amount; an over-cap signal (e.g. many nodes cleared) CLAMPS to `MAX_AWARD` (AC3 cap enforced); the calculation is DETERMINISTIC (twice → identical); it draws ZERO RNG; the FAILED-run amount matches the chosen rule; a manual-seed run's amount is denied at the APPLICATION gate (Task 4), not in the calculator (the calculator is a pure amount; the gate decides whether to apply it).

- [ ] **Task 4 — The award APPLICATION path behind the two gates (AC1, AC2, AC4)** — validate-then-mutate, idempotent, eligibility-gated
  - [ ] **Resolve the ⭐ application `[Decision]` + record it:** RECOMMENDED a run-command (`godot/scripts/core/commands/award_meta_progress_command.gd`, `class_name AwardMetaProgressCommand`, extending `game_command.gd`) with the 4.3 idiom: `validate(state)` gates on (a) `state is RunState` + structurally sound; (b) `run.is_terminal()` (the run ENDED — behind the 8.1 idempotency seam; a non-terminal run rejects with a stable code + ZERO mutation); (c) ELIGIBILITY (`run.meta_progression_eligible` — a manual-seed run rejects/awards-0, AC4); (d) `sequence_id > 0` (the event self-consistency gate, mirror `CompleteRunCommand`). `execute(state)` computes the award (Task 3), UPDATES the passed-in `ProfileSnapshot`, builds the meta-award event (Task 5), and returns `ActionResult.ok([event], {amount, oath_shards_after, ...})`. On ANY reject: ZERO events, ZERO mutation (the run + the profile untouched). `[Decision]` — the command takes the terminal `RunState` as `state` + the `ProfileSnapshot` (+ optional `RunSummary`) as constructor/param inputs; record the exact signature.
  - [ ] **Idempotency / no-double-award (`[Decision]`, record how you guarantee it):** a re-invocation for the same already-awarded run must NOT double-award. RECOMMENDED: the award fires ONCE behind `resolve_run_end` (the run-end resolves exactly once — the 8.1 `run_already_terminal` guard prevents a second run-end, so a single award per run-end is structural). If the award can be invoked independently of `resolve_run_end`, track an awarded marker (e.g. the profile records the last-awarded run_id, or the caller guards) so a re-invocation is a no-op / a stable "already awarded" result with ZERO second event + ZERO double-mutation. Do NOT re-award on a re-completion (the AC1 "not granted twice" + 8.1 seam). Prove it with a test.
  - [ ] **The FR28 denial (AC4):** a manual-seed run (`meta_progression_eligible == false`) awards NOTHING — 0 Oath Shards, no class mastery, no unlock progress, no meta-award event, the profile UNCHANGED. `[Decision]` — the command either REJECTS a manual-seed run with a stable code (e.g. `run_not_meta_eligible`) OR succeeds with a 0-amount no-op-no-event; RECOMMENDED reject/0-award-no-event so a manual-seed run visibly grants nothing. Record it.
  - [ ] Tests: an eligible completed run → the profile's cross-run `oath_shards` rises by the awarded amount + the event is emitted + `oath_shards_after` matches; a manual-seed run → 0 award, no event, profile byte-identical (AC4); a non-terminal run → reject with ZERO mutation (behind the terminal gate); a re-invocation for the same already-awarded run → no double-award (idempotency); on reject the profile + run are byte-identical (the 4.3 no-mutation-on-reject guarantee).

- [ ] **Task 5 — The deterministic meta-award domain EVENT (AC2)** — append-only enum + factory + validator + the exhaustiveness pin
  - [ ] Append a NEW member at the `DomainEvent.Type` enum END (RECOMMENDED `OATH_SHARDS_AWARDED`; `[Decision]` name it) + its `EVENT_ID_*` const (id `oath_shards_awarded`, lower_snake). NEVER renumber existing members (the append-only discipline).
  - [ ] Add a factory `static func oath_shards_awarded(sequence_id: int, payload: Dictionary = {}) -> DomainEvent` (mirror `economy_changed` / `run_failed`): a SYSTEM event (no actor). Payload RECOMMENDED: `amount` (this run's award, non-negative int), `oath_shards_before` / `oath_shards_after` (non-negative ints), `reason` (a lower_snake marker, e.g. `run_completed_eligible`), `profile_id` (a string). Normalize/duplicate defensively. NO int64 encoding (bounded counts); NO roll/draw_index (a recorded amount, ZERO RNG — the `economy_changed` deterministic shell).
  - [ ] Add a payload validator `_validate_oath_shards_awarded_payload` (wired into `_validate_payload_for_event`) mirroring `_validate_run_failed_payload` / `_validate_economy_changed_payload`: `reason` lower_snake (`_has_lower_snake_payload`); `amount`/`oath_shards_before`/`oath_shards_after` non-negative integral (`_has_nonnegative_integral_payload`); `profile_id` a string (`_has_string_payload`). `[Decision]` — pick the exact required fields + record.
  - [ ] **REGISTER the new id in `test_domain_event.gd` `expected_ids`** (lines 2121-2162) with a `# Story 8.3:` comment. The pin's `expected_ids.size() == Type.size() - 1` assertion (line 2176) TRIPS if you forget — this is BY DESIGN (the documented epic-transition heads-up; 8.1's `run_failed` did the same). Add a round-trip test that the new event `try_from_dictionary`/`to_dictionary` round-trips + rejects a malformed payload (negative amount, non-lower_snake reason).
  - [ ] Tests: the new event round-trips through `to_dictionary()`/`try_from_dictionary`; a malformed payload (negative amount / bad reason / missing field) is rejected with `invalid_event_payload`; the `expected_ids` pin includes the new id + `id_for_type`/`type_for_id` round-trip; ZERO RNG (a recorded amount).

- [ ] **Task 6 — The FR28 manual-seed denial + the summary replay/practice warning (AC4)**
  - [ ] Confirm (test) the award path denies ALL meta rewards for a manual-seed run (Task 4). Confirm the `RunSummary` (8.2) reports `meta_progression_eligible == false` + `is_manual_seed == true` for a manual-seed run — that IS the AC4 replay/practice warning DATA. `[Decision]` — RECOMMENDED the existing summary fields satisfy AC4 (the warning is DATA the 8.6 UI renders); do NOT add a redundant signal unless the AC4 wording demands an explicit one (it does not — "the summary shows a replay/practice warning" is satisfied by the eligibility/manual-seed fields the summary already carries). If you DO add an explicit `replay_practice_warning` field, keep `RunSummary`'s exact-key discipline + update its pinned key test. Record the choice.
  - [ ] Tests: a manual-seed run's summary carries `meta_progression_eligible == false` + `is_manual_seed == true` (the warning data); the award path grants NOTHING for it (0 shards, no event, profile unchanged).

- [ ] **Task 7 — The AC5 save-failure structured recovery + summary independence (AC5)**
  - [ ] `ProfileRepository.write_profile` returns a STRUCTURED error on any write failure (Task 2). The award path SURFACES that structured result to the caller (it does NOT swallow/crash). AC5's "does not silently lose current run summary data": prove (test) that a FAILED profile write leaves the `RunSummary` fully readable + the structured error available — so a recovery UI (8.6) can show the summary + retry the save. The `RunSummary` is a DERIVED read INDEPENDENT of the profile write (it reads the terminal run + events; it does not read the profile file).
  - [ ] Tests: a profile write to an unwritable path returns the structured error (not a crash); after the failed write, `RunSummary.build(run, events)` still returns the full summary (AC5 — no silent loss); the structured error code + metadata are stable/diagnostic.

- [ ] **Task 8 — Full unit tests (AC1-AC5)** — the complete matrix (see IN-scope item 8 for the enumerated cases). Home them under `godot/tests/unit/save/` (profile snapshot + repository) + `godot/tests/unit/core/` (the event + the award command) mirroring the domain. Headless, no scene tree, no rendering/audio.

- [ ] **Task 9 — Full headless suite + diff hygiene + the scope-fence + the deferred-work disposition (AC1-AC5)**
  - [ ] Run the FULL headless suite via PowerShell (`godot` is NOT on the Bash PATH — see Project Context Rules). Expect "Headless tests passed.", exit 0, ZERO FAIL. Apply the **false-PASS grep guard** (`SCRIPT ERROR|Parse Error|Compile Error|Failed to load script`) — it must be clean beyond the documented-expected negative-path diagnostics (the int64-overflow `root_seed` boundary `Cannot represent … as a 64-bit signed integer`, `RouteNode parse failed: invalid_node_type`, the save/settings `Parse JSON failed`/`got 'this'`/`Expected key` negatives). Command: `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10` (or the console binary `C:/Users/Rasmus/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe` with the same args). **⭐ Diagnose from the runner's per-file `FAIL` list, NOT stderr `ERROR:` lines** (the 8.1 resume-verify lesson — a benign `String.to_int` int64-overflow push_error still PASSes).
  - [ ] `git diff --check` clean (working tree + baseline..HEAD; ignore the benign LF→CRLF normalization warning on the `.md`).
  - [ ] **Confirm the scope fences:** the new `oath_shards_awarded` event is APPENDED at the enum end (existing members NOT renumbered) + REGISTERED in `expected_ids` (the pin passes); `RngStreamSet.required_streams()` UNCHANGED (7 streams — the award draws ZERO RNG, no new stream); the **23-key `RunSnapshot` COUNT STAYS 23** + the no-surprise-key gate UNCHANGED (the PROFILE is its OWN snapshot — `ProfileSnapshot`/`ProfileRepository`/`user://profile.json` — NOT a new `RunSnapshot` key; `RunSnapshot.oath_shards` STAYS the 0 run-scoped placeholder — the CROSS-RUN total lives in the profile); `RunState`/`RouteState`/`RiskEconomyState`/`CompleteRunCommand`/`RunEndOutcome`/`RunSummary` UNCHANGED unless you make a SMALL additive read (an OPTIONAL summary warning field or a thin orchestrator award hook — a pure read/additive, no existing-behavior change; if you touch `RunSummary`, keep its exact-key pin green); `scripts/rules/{conditions,operations}` UNCHANGED (an award is not a rules-kernel concern); `data/source`/`data/resources` STAY EMPTY (no new content — the award rule is a code constant, the SAME v0 discipline as the affinity/economy markers); no `.tscn`/new asset (UI-scene-last; the outpost/meta screen is 8.6); ZERO `randi`/`randf`/`RandomNumberGenerator` in any new code; **every Small/Medium/route seed-regression fingerprint byte-identical** (8.3 touches no `scripts/generation/`).
  - [ ] **Update `deferred-work.md`** with the 8.3 dispositions (mirror the 8.2 entry structure): that 8.3 CLOSES the "META PROFILE + Oath-Shard AWARDING + the meta-save shape" defer (grep that phrase — the 8.2 disposition ~line 55 and the 8.1 disposition ~line 159) — record the shipped `[Decision]`s (the `ProfileSnapshot`/`ProfileRepository`/`user://profile.json` shape; the award RULE + cap; the `oath_shards_awarded` event; the two-gate application; the FR28 denial; the AC5 structured recovery); that Echoes/Seal-Fragments/class-mastery/unlock-progress content + profile-merge (8.4), the outpost menu + meta DISPLAY + unlock-spend tree (8.6), the COMPREHENSIVE meta/summary save-load test matrix (8.7), the first-death narrative (8.5), and the unlock-SPEND application (later) remain DEFERRED to their owning stories; that the profile is its OWN versioned snapshot WITH migration from the start (the retro T2 heads-up — HONORED); that the two 8.1 `[Review][Defer]` items are UNTOUCHED (command/DTO-validation concerns, not award concerns). Note whether any NEW `[Review][Defer]` arises (record at review time).
  - [ ] Update the story Status (→ review), `sprint-status.yaml` (`8-3-meta-profile-and-oath-shard-awards: review`, refresh `last_updated`), the File List, and Completion Notes (incl. ALL `[Decision]`s). Note that 8.3 is the "receive eligible progress" half of Epic 8 + the FIRST persistent cross-run state, and that 8.4's content-merge + 8.6's meta display + 8.7's save-load matrix build on the profile it ships.

## Dev Notes

### The big picture: 8.3 builds the META PROFILE + the OATH-SHARD AWARD — the "receive eligible progress" half of Epic 8
Epic 8 is "die or finish a run, return to the last outpost, review what happened, **receive eligible progress**, start another descent." 8.1 shipped the run-END boundary; 8.2 shipped the DERIVED `RunSummary` read (which REPORTS eligibility + a 0 awarded count). 8.3 is the AWARD: it creates the FIRST persistent CROSS-RUN state (a versioned `ProfileSnapshot` + `ProfileRepository`), CALCULATES an Oath-Shard award from approved capped/sparse rules, RECORDS it via a deterministic meta-award event, DENIES it for a manual-seed run (FR28), and PERSISTS it with a structured save-failure recovery path — all BEHIND the 8.1 idempotency guard so a re-completion never re-awards. 8.4 (Echoes/unlock content), 8.6 (outpost display), and 8.7 (save-load matrix) BUILD ON the profile it ships. [Source: `_bmad-output/planning-artifacts/epics.md` lines 2076-2107 (Story 8.3 ACs) + Epic 8 intro + line 399 (FR95/FR67 capped-sparse-meta-power); the Epic-8 retro `_bmad-output/auto-gds/retro-notes/epic-8.md`]

### ⭐ CRITICAL — 8.3 is the FIRST persistent CROSS-RUN state; the profile is its OWN snapshot (the single most important calibration)
Every save before this persisted only WITHIN a run: `RunSnapshot` = the run autosave (`user://run_autosave.json`), and its `oath_shards`/`meta_progression` fields are RUN-scoped placeholders (0/empty — VERIFIED `run_snapshot.gd` lines 38, 41, 265-266). The PROFILE is different: it OUTLIVES a run and accumulates across many descents. **The retro-notes (Epic-7 §7 Action T2, re-stated for 8.3 in the ledger) mandate the profile be its OWN `ProfileSnapshot` + `ProfileRepository` + save file (`user://profile.json`), NOT nested under `route_state`, NOT the `RunSnapshot`, WITH migration coverage from the START.** So:
- Build a SEPARATE `ProfileSnapshot` (its OWN `SCHEMA_VERSION`, exact-key `to_dictionary()`, versioned `parse` with `unsupported_profile_schema`) — do NOT add a new key to `RunSnapshot` (the 23-key gate stays 23; `RunSnapshot.oath_shards` stays the 0 run-scoped placeholder).
- Build a SEPARATE `ProfileRepository` (its OWN `user://profile.json` path) mirroring `SaveRepository` — do NOT reuse the run autosave path.
- The CROSS-RUN Oath-Shard total lives in `ProfileSnapshot.oath_shards`; the run-scoped `RunSnapshot.oath_shards` stays 0. This is AC2's "run state, profile state, and unlock/content state remain separable."
[Source: `_bmad-output/auto-gds/retro-notes/epic-7.md` §7 Action T2 (the meta boundary — plan the meta-save shape early, its OWN snapshot, migration from the start); `_bmad-output/implementation-artifacts/deferred-work.md` lines 55-60, 159-165 (the 8.3 meta-save-shape defer with the T2 heads-up); `godot/scripts/save/snapshots/run_snapshot.gd` (the versioned-snapshot template); `godot/scripts/save/save_repository.gd` (the repository template)]

### ⭐ Mirror `SaveRepository` + `RunSnapshot` VERBATIM — the versioned-snapshot + atomic-write + structured-error pattern
`RunSnapshot` (`run_snapshot.gd`) is the DIRECT template for `ProfileSnapshot`: `const SCHEMA_VERSION`, an exact-key `to_dictionary()` (with `.duplicate(true)` on sub-dicts/lists), a `static parse(data) -> ActionResult` that rejects `schema_value != SCHEMA_VERSION` with `unsupported_save_schema` (→ your `unsupported_profile_schema`), lenient decode helpers (`_int64_or_zero` / `_dictionary_or_empty` / `_string_array`), and a `from_dictionary` convenience. `SaveRepository` (`save_repository.gd`) is the DIRECT template for `ProfileRepository`: the atomic temp→backup→replace write with structured `save_open_failed`/`save_backup_failed`/`save_replace_failed` codes, and the `read_run_snapshot` with `save_not_found`/`save_open_failed`/`save_parse_failed`. COPY both shapes; change ONLY the class/path/schema/field-set + the code prefixes. The migration path is baked in from day one (the schema-version reject IS the v0 migration path — a future schema bump adds a real migrate step; 8.7 owns the migration matrix). [Source: `godot/scripts/save/snapshots/run_snapshot.gd` lines 12, 43-112, 317-333; `godot/scripts/save/save_repository.gd` (the whole file); `godot/tests/unit/save/test_run_snapshot.gd` + `test_save_repository.gd`]

### ⭐ The award sits behind TWO gates — the 8.1 idempotency seam + the eligibility gate
- **Gate 1 — idempotency (the 8.1 seam):** `CompleteRunCommand.validate` (lines 98-106) rejects a re-resolution of an already-terminal run with `run_already_terminal` (ZERO second event, ZERO mutation); `RunOrchestrator.resolve_run_end` (lines 705-722) surfaces that verbatim. The AWARD fires as part of / behind that SINGLE successful run-end resolution — so a re-completion (which is rejected) never re-awards. The 8.1 command comment explicitly names this: "the guard is the SEAM 8.3's Oath-Shard awarding must run BEHIND (the award must never re-fire on a re-completion)." Guarantee no-double-award: award ONCE behind `resolve_run_end`, OR track an awarded marker if the award can be invoked independently.
- **Gate 2 — eligibility (FR28/AC4):** `RunState.meta_progression_eligible` (lines 61, 243-248) is the run-level gate, asserted in `validate()` to equal `not is_manual_seed`; `RiskEconomyState.oath_shard_eligible` (lines 61-64, 97-104) is kept in lockstep. A manual-seed run (`meta_progression_eligible == false`) awards NOTHING (AC4). The eligibility is ALREADY computed + validated in the domain — the award READS it; it does not re-derive it.
[Source: `godot/scripts/core/commands/complete_run_command.gd` lines 38-42, 98-106; `godot/scripts/run/run_orchestrator.gd` lines 690-722; `godot/scripts/run/run_state.gd` lines 61, 215-259; `godot/scripts/run/risk_economy_state.gd` lines 61-104]

### ⭐ AC3 — capped, sparse, secondary; raw-stat ladders REJECTED for MVP (the content-rule constraint with teeth)
The award RULE (Task 3) must satisfy AC3: **direct power gains capped + sparse + secondary to variety/knowledge/classes/loot-pools/passives/secrets/codex/starting-options/class-mastery**, and **broad raw-stat ladders (repeatable damage / max-HP / armor / crit / dodge) REJECTED for MVP unless explicitly approved as capped onboarding smoothing.** For v0, Oath Shards are a CURRENCY toward variety/options (spent in a later unlock story) — NOT a direct combat stat. The v0 award RULE must:
- Be DETERMINISTIC (ZERO RNG — same terminal run → same award; the whole-epic determinism invariant + the named-RNG rule).
- Have a HARD CAP (`MAX_AWARD` — the award cannot grow unbounded; AC3 "capped").
- Be SPARSE (a shallow trickle — the user story "without becoming a stat grind").
- NOT be a raw-stat ladder (8.3 awards the CURRENCY; it does NOT apply a stat, does NOT build the spend tree). The AC3 "rejected for MVP" is satisfied because 8.3 grants a bounded currency, not a repeatable stat upgrade.
Document (a validation test + the calculator header) WHY the rule satisfies AC3. Do NOT scale the award by difficulty (difficulty is a hard non-goal). [Source: `_bmad-output/planning-artifacts/epics.md` line 399 (FR95/FR67 capped-sparse-meta-power → Story 8.3), AC3 (lines 2094-2097); `project-context.md` difficulty-non-goal guard]

### ⭐ AC2 — deterministic profile/meta EVENT (append-only enum + the exhaustiveness pin)
AC2 requires the profile change be "recorded through deterministic profile/meta events or repository results." Ship a DETERMINISTIC meta-award event (`oath_shards_awarded`) mirroring the `run_failed`/`economy_changed` pattern:
- APPEND a member at the `DomainEvent.Type` enum END (line 39 is the current last, `RUN_FAILED`) — NEVER renumber. Add its `EVENT_ID_*` const.
- A factory (mirror `economy_changed` lines 420-439 / `run_failed` lines 300-318): a SYSTEM event (no actor — NOT in `_event_requires_actor`), normalize/duplicate the payload defensively, ZERO int64 encoding (bounded counts), ZERO roll/draw_index (a RECORDED amount, not a roll — the `economy_changed` deterministic shell).
- A payload validator (`_validate_oath_shards_awarded_payload`, wired into `_validate_payload_for_event` line 850) mirroring `_validate_run_failed_payload` (lines 1053-1072): `reason` lower_snake, `amount`/`oath_shards_before`/`oath_shards_after` non-negative integral, `profile_id` a string. Use the existing helpers (`_has_lower_snake_payload`, `_has_nonnegative_integral_payload`, `_has_string_payload`).
- **REGISTER the new id in `test_domain_event.gd` `expected_ids` (lines 2121-2162).** The pin's `expected_ids.size() == Type.size() - 1` assertion (line 2176) FAILS by design if you forget — this is the documented epic-transition heads-up (the Epic-7 retro Action T3, exercised by 8.1's `run_failed`). Add the id with a `# Story 8.3:` comment.
[Source: `godot/scripts/core/events/domain_event.gd` lines 6-40 (enum), 300-318 (`run_failed` factory), 420-439 (`economy_changed` factory), 850-911 (validator dispatch), 1053-1072 (`_validate_run_failed_payload`); `godot/tests/unit/core/test_domain_event.gd` lines 2120-2181 (`expected_ids` exhaustiveness pin); `_bmad-output/auto-gds/retro-notes/epic-8.md` (8.1's `run_failed` tripped the SAME pin BY DESIGN — register the new event in the same change)]

### ⭐ AC4 — the FR28 manual-seed denial + the summary replay/practice warning (do NOT over-engineer)
A manual-seed run awards NOTHING (Oath Shards, class mastery, unlock progress — AC4). The eligibility gate (Gate 2) enforces this. AC4's "the summary shows a replay/practice warning" is satisfied by the `RunSummary` fields 8.2 ALREADY ships: `meta_progression_eligible == false` + `is_manual_seed == true` for a manual-seed run (`run_summary.gd` lines 144-149) — that IS the warning DATA the 8.6 UI renders. RECOMMENDED: do NOT add a redundant `replay_practice_warning` signal (the eligibility/manual-seed fields ARE the warning); confirm with a test. If a reviewer insists on an explicit signal, add it as a small additive read field to `RunSummary` + update its pinned key test — but the AC wording ("the summary shows a replay/practice warning") is met by the existing fields. [Source: `godot/scripts/run/run_summary.gd` lines 132-160; `_bmad-output/planning-artifacts/epics.md` FR28 (line 79); AC4 (lines 2099-2102)]

### ⭐ AC5 — the structured save-failure recovery (never silent-lose the summary)
A profile save failure must be a STRUCTURED `ActionResult.error(...)` (a stable lower_snake code + diagnostic metadata) — never a silent swallow, never a crash. `ProfileRepository.write_profile` mirrors `SaveRepository.write_run_snapshot`'s structured failure codes. AC5's "does not silently lose current run summary data" is satisfied STRUCTURALLY: the `RunSummary` is a DERIVED read (8.2) composed from the terminal run + events — it does NOT read the profile file, so a failed profile write leaves the summary fully readable. The award path SURFACES the structured save-error to its caller; a recovery UI (8.6) shows the summary + retries the save. On a `profile_not_found` read, the caller starts a FRESH `ProfileSnapshot` (the 8.6 fresh-profile recovery). 8.3 makes the failure VISIBLE + RECOVERABLE; it does not build the recovery UI. [Source: `godot/scripts/save/save_repository.gd` (structured error codes); `godot/scripts/run/run_summary.gd` (the derived read, independent of the profile); AC5 (lines 2104-2107)]

### Determinism + named-RNG + the FIRST cross-run save (the epic-wide invariants 8.3 MUST honor)
- DETERMINISM: the award is a pure function of `(terminal RunState, RunSummary)` — same inputs → same award. ZERO RNG. NEVER `randi`/`randf`/a fresh `RandomNumberGenerator`. `RngStreamSet.required_streams()` UNCHANGED (7 streams — do NOT add one; the award is a deterministic calculation, not a roll). [Source: `godot/scripts/core/state/rng_stream_set.gd` lines 6-31; `project-context.md` named-RNG rule]
- APPEND-ONLY `DomainEvent.Type` enum — 8.3 APPENDS `oath_shards_awarded` at the end (never renumber) + registers it in the exhaustiveness pin. [Source: `project-context.md` event discipline; `domain_event.gd` enum]
- EXACT-KEY `to_dictionary()` discipline — `ProfileSnapshot.DICTIONARY_KEYS` pinned by test (the `RunSnapshot`/`RiskEconomyState`/`RunSummary` precedent). [Source: `project-context.md` presentation/snapshot rules]
- SEPARATE PROFILE SAVE — the profile is its OWN `ProfileSnapshot`/`ProfileRepository`/`user://profile.json`, NOT a `RunSnapshot` key (the 23-key gate stays 23) + WITH its OWN schema-version migration path from day one. [Source: `project-context.md` Run-Progression + Save/Snapshot rules; the retro T2 heads-up]
- INT64: `oath_shards` is a SMALL bounded int (NOT a seed — no decimal-string encoding; the `RiskEconomyState.gold`/`RunSnapshot.gold` precedent). Only `root_seed`-scale values need decimal-string encoding (the profile has none). [Source: `godot/scripts/run/risk_economy_state.gd` (gold as a plain int); `project-context.md` root_seed rule]
- FINGERPRINTS: 8.3 touches NO `scripts/generation/` — every Small/Medium/route seed-regression fingerprint stays byte-identical. [Source: `project-context.md` SEED REGRESSION tripwire]

### The award seam is CALLER-DRIVEN behind resolve_run_end — v0 has no live death source
`RunOrchestrator.resolve_run_end` (lines 705-722) is the run-end seam (it dispatches `CompleteRunCommand` behind the idempotency guard + captures the run-end fields). The award naturally sits behind it: the caller resolves the run-end, then awards (or an OPTIONAL thin `resolve_run_end`-adjacent orchestrator hook awards behind the same guard — `[Decision]`, a pure additive path; if you add it, it must not perturb the run-loop/determinism/sequence counter). v0 has NO live death source (combat auto-resolves), so `resolve_run_end` + the award are CALLER-DRIVEN (a test today; the later HUD/run-flow story tomorrow). Do NOT auto-wire the award into `run_to_completion`/`_resolve_combat` (that perturbs the auto-resolve posture the whole epic preserved). [Source: `godot/scripts/run/run_orchestrator.gd` lines 690-722 (`resolve_run_end` — caller-driven, NOT in `run_to_completion`); `_bmad-output/auto-gds/retro-notes/epic-8.md` (the live-loop defer)]

### Previous story intelligence (8.1 + 8.2 — the immediate predecessors)
- **8.1 (run-END boundary)** shipped `CompleteRunCommand` (the idempotency guard the award sits behind), the `run_failed`/`run_completed` events (the run-end vocabulary), `RunEndOutcome` (the read DTO), and `RunOrchestrator.resolve_run_end` (the caller-driven run-end seam). 8.1's `run_failed` tripped the `test_domain_event.gd` `expected_ids` exhaustiveness pin BY DESIGN — 8.3's `oath_shards_awarded` will do the SAME; register it in the same change. 8.1 kept `RunSnapshot.oath_shards` at 0 (the AWARDED count 8.3 owns) and READ `meta_progression_eligible`/`oath_shard_eligible` READ-ONLY. [Source: `_bmad-output/implementation-artifacts/8-1-run-completion-and-return-to-outpost-flow.md`; `_bmad-output/auto-gds/retro-notes/epic-8.md` (8.1 dispositions)]
- **8.2 (run SUMMARY)** shipped `RunSummary` (`run_summary.gd`) — a pure-read aggregator that REPORTS `profile_meta.oath_shards_earned == 0` + `meta_progression_eligible` + `is_manual_seed` READ-ONLY, and names `["oath_shards_earned", "echoes_discovered", "unlock_progress"]` in `not_yet_supported`. 8.2's Completion Notes explicitly state: **"The summary is the read seam the 8.3 award path sits IN FRONT of — 8.3 reads eligibility + the summary, THEN awards BEHIND the 8.1 idempotency guard; 8.2 itself never awards."** After 8.3, the summary's `oath_shards_earned` CAN reflect the real awarded amount for the just-ended eligible run (if you feed it — Task 5/6, `[Decision]`), but `echoes_discovered`/`unlock_progress` STAY placeholders (8.4). The 8.2 review converged clean (3 rounds, Approve, 0 open Critical/High/Med). [Source: `_bmad-output/implementation-artifacts/8-2-run-summary-snapshot.md` (Completion Notes + Review Findings); `godot/scripts/run/run_summary.gd`]

### Epic-8 retro-note constraints that apply to THIS story (from `_bmad-output/auto-gds/retro-notes/epic-8.md`)
- **The meta-save shape is 8.3's most-flagged constraint (from 8.1 AND 8.2 dispositions):** "decides NO meta-save shape (likely its OWN snapshot, NOT nested under `route_state`, with migration coverage from the start — the retro T2 heads-up)." 8.3 HONORS this: a SEPARATE `ProfileSnapshot`/`ProfileRepository`/`user://profile.json` with its OWN `SCHEMA_VERSION` + a migration reject path from day one. [retro-notes/epic-8.md — 8.1 + 8.2 `[Defer]` (8.3 award / 8.7 save-load tests)]
- **The award sits BEHIND the 8.1 `run_already_terminal` idempotency guard (a re-completion never re-awards):** "The 8.3 award path READS this summary + the eligibility, THEN awards behind the 8.1 idempotency guard." HONORED via the two-gate application (Task 4). [retro-notes/epic-8.md — 8.2 AC3 disposition]
- **The `oath_shards_awarded` event WILL trip the Story-7.1 `expected_ids` exhaustiveness gate BY DESIGN — register it in the same change** (the exact epic-transition heads-up from the Epic-7 retro Action T3, exercised by 8.1's `run_failed`). [retro-notes/epic-8.md — 8.1 Phase-3 create-story disposition]
- **BREAKING heads-up (documented):** `run_completed` no longer always carries `boss_node_id` (boss_placeholder-only, absent for generic `completed`). If the award reads any run-end field off an event payload, GUARD on presence; PREFER reading the terminal fact from `RunState` (phase, eligibility) + `RunSummary`, not from a payload field. [retro-notes/epic-8.md — 8.1 dev BREAKING]
- **Diagnostic-noise heads-up:** the suite prints a benign `ERROR: Cannot represent … as 64-bit signed integer` (a `String.to_int` saturation on the pre-existing out-of-range-seed REJECTION test) that STILL PASSes — diagnose from the runner's per-file `FAIL` list, NOT stderr `ERROR:` lines. [retro-notes/epic-8.md — 8.1 Phase-5 resume-verify lesson]
- **Round-header discipline (from the 8.2 review-round-guard note):** when a code review appends round entries to THIS story file, keep each round header as its OWN standalone bold token `**Round N of 3**` (the review-round-guard counts rounds via that exact pattern; wrapping "of 3" inside a longer bold run defeats the counter). [retro-notes/epic-8.md — 8.2 Phase-7 review-round-guard note]

### Deferred-work ledger items that apply to THIS story (from `_bmad-output/implementation-artifacts/deferred-work.md`)
- **The META PROFILE + Oath-Shard AWARDING + the meta-save shape is 8.3's to CLOSE** (grep the ledger for "[Defer] (8.3 award / 8.7 save-load tests) The META PROFILE + Oath-Shard AWARDING + the meta-save shape" — it appears twice, ~line 55 in the 8.2 disposition and ~line 159 in the 8.1 disposition): this story implements the AWARDING half. When you finalize, update the ledger to record it as delivered (with its `[Decision]`s), mirroring how 8.2 closed its own run-summary defer. NOTE the split: 8.3 ships the AWARD + the profile save + the AWARD-path save-load tests; the COMPREHENSIVE meta/summary save-load TEST MATRIX + the migration matrix stay 8.7's ("8.3 award / 8.7 save-load tests").
- **Echoes / Seal-Fragments / class-mastery / unlock-progress content + profile-merge stay DEFERRED to 8.4** (ledger lines 61-64): 8.3 does NOT author that content; it provides EMPTY/0 profile HOMES so 8.4 merges without a migration. The `RunSummary` `echoes_discovered`/`unlock_progress` stay placeholders.
- **The two 8.1 `[Review][Defer]` items are UNTOUCHED (not award concerns)** (ledger lines 192-210): (a) `CompleteRunCommand._resolve_completed`'s two-step transition atomicity vs the no-mutation-on-reject promise (unreachable today; harden with the boss pattern if ever); (b) `RunEndOutcome.for_failed`/`for_completed` not validating the cause/outcome against the event allowlists (a passive projector of already-command-validated markers). 8.3 READS the terminal run + `RunSummary` + the eligibility (all already-validated); it does not modify `CompleteRunCommand`/`RunEndOutcome`, so neither defer is in scope.
- **The int64-overflow economy ceiling + the constant-8-tier route-depth pacing (Epic 10) are UNTOUCHED** (ledger passim): 8.3's `oath_shards` is a small bounded capped int (AC3's cap keeps it bounded); it does not touch the economy ceiling or route pacing.
[Source: `_bmad-output/implementation-artifacts/deferred-work.md` — the meta-profile/awarding `[Defer]` (~lines 55-64 and ~159-165), the two 8.1 `[Review][Defer]` items (~lines 192-210), passim; grep the `[Defer]`/`[Review][Defer]` phrases as line numbers drift]

### Project Structure Notes

- `ProfileSnapshot` (`profile_snapshot.gd`, `class_name ProfileSnapshot`) belongs under `godot/scripts/save/snapshots/` alongside `RunSnapshot`/`TacticalSnapshot` (the save-snapshot home). `ProfileRepository` (`profile_repository.gd`, `class_name ProfileRepository`) belongs under `godot/scripts/save/` alongside `SaveRepository`. The award RULE calculator (`meta_award_rules.gd`) belongs under `godot/scripts/save/` (a meta/save-domain read) OR `godot/scripts/run/` (a run-end read) — `[Decision]` pick; `save/` is RECOMMENDED (it is a profile/meta concern). The award COMMAND (`award_meta_progress_command.gd`) belongs under `godot/scripts/core/commands/` alongside `CompleteRunCommand` (the command home). The `oath_shards_awarded` event lives in `godot/scripts/core/events/domain_event.gd` (the single event file). NONE are UI (no `.tscn`, no `Control`, no scene — the outpost/meta screen is 8.6, UI-scene-last). [Source: `AGENTS.md` "Domain scripts go under `godot/scripts/` by domain: `core`, ..., `save`, ...", "Save snapshots need migration tests for schema changes"; the `run_snapshot.gd`/`save_repository.gd`/`complete_run_command.gd` sibling precedents]
- Tests go under `godot/tests/unit/save/` (the `ProfileSnapshot` + `ProfileRepository` tests, alongside `test_run_snapshot.gd`/`test_save_repository.gd`) + `godot/tests/unit/core/` (the `oath_shards_awarded` event in `test_domain_event.gd`, the award command in a new `test_award_meta_progress_command.gd`). Headless, no scene tree, no rendering/audio. [Source: `AGENTS.md` "Tests go under `godot/tests/`, mirroring the domain they cover"; the existing save/command test homes]
- No detected conflicts. 8.3 is ADDITIVE: it introduces new files (profile snapshot + repository + award rule + award command + tests) + a small append to `domain_event.gd` (the new event + validator) + the `expected_ids` pin registration + OPTIONALLY a small additive `RunSummary` read field. It modifies NO existing schema (the profile is a NEW snapshot; `RunSnapshot` stays 23-key) and no existing behavior (unless you add the OPTIONAL thin orchestrator award hook — a pure additive path).

### Project Context Rules

Extracted from `project-context.md` (the compact implementation rulebook) — the rules that bind THIS story:

- **Scene-independent domain model owns run/meta truth; presentation observes.** `ProfileSnapshot`/`ProfileRepository`/the award rule/command are domain/save logic (scene-free `RefCounted`), NOT scenes/`Control`s. Use typed GDScript. The meta/outpost UI is a later presentation concern (8.6). [project-context.md Engine-Specific Rules; UI-scene-last]
- **Gameplay actions are commands that validate before mutation and return `ActionResult`; successful commands emit deterministic past-tense domain events.** If you model the award as a command, it VALIDATES (terminal + eligible + sequence_id) BEFORE mutating the profile, returns `ActionResult`, and emits the past-tense `oath_shards_awarded` event on success (ZERO events + no mutation on reject). [project-context.md command/event rules]
- **Save versioned domain snapshots only; never serialize scene nodes as save truth.** `ProfileSnapshot` is a versioned domain snapshot (its OWN `SCHEMA_VERSION` + migration reject path). NO scene node is serialized (AC in 8.7; honored here). [project-context.md Save/Snapshot rules]
- **Snapshots/reads are PURE.** Composing a snapshot or the award calculation consumes NO RNG, executes NO commands beyond the award itself, and mutates neither the run nor an unrelated file. The award CALCULATION is a pure read; the award COMMAND mutates only the profile (validate-then-mutate). [project-context.md Save/Snapshot rules; the `RunSnapshot`/`RunSummary` precedent]
- **Named RNG streams only; the award draws NONE.** No `randi`/`randf`/`RandomNumberGenerator`. `RngStreamSet.required_streams()` UNCHANGED (7 streams). The award is a DETERMINISTIC calculation, not a roll. [project-context.md Determinism & Simulation Rules]
- **Append-only `DomainEvent.Type` enum + the exhaustiveness pin.** 8.3 APPENDS `oath_shards_awarded` at the enum end (never renumber) + registers it in `test_domain_event.gd` `expected_ids` (the pin TRIPS by design). [project-context.md event discipline; the 8.1 `run_failed` precedent]
- **Exact-key `to_dictionary()` discipline** (a key never silently appears/vanishes; pinned by test). `ProfileSnapshot.DICTIONARY_KEYS` is pinned by `test_profile_snapshot.gd`. The `RunSnapshot`/`RiskEconomyState`/`RunSummary` precedent. [project-context.md snapshot/view-model rules]
- **The 23-key `RunSnapshot` gate is a tripwire — add NO top-level key.** The PROFILE is its OWN snapshot; 8.3 adds NO `RunSnapshot` key. `RunSnapshot.oath_shards` stays the 0 run-scoped placeholder (the cross-run total lives in the profile). [project-context.md Run-Progression rules]
- **`root_seed` decimal-string encoding is for SEEDS ONLY.** `oath_shards` is a small bounded int (the `RiskEconomyState.gold`/`RunSnapshot.gold` precedent — plain int). The profile has no seed-scale field. [project-context.md Save/Snapshot rules]
- **OUTCOME-RECORD-ONLY posture holds (Epic 6/7); 8.3 is the FIRST to AWARD a cross-run value.** The award RECORDS the granted amount (the `oath_shards_awarded` event + the profile total); it does NOT apply a live stat/passive/class (that is a later spend story). AC3's cap keeps the award bounded. [project-context.md Loot/Rewards/Inventory + Risk-Economy rules]
- **Difficulty is a hard non-goal.** The award does NOT scale by difficulty; it is a capped deterministic grant. [project-context.md settings/difficulty guard]
- **Manual-seed runs must not grant meta progression (FR28).** The award path DENIES all meta rewards for a manual-seed run (Gate 2 — `meta_progression_eligible == false`). This is the FR28 ENFORCEMENT (8.1/8.2 only reported eligibility; 8.3 enforces the denial). [project-context.md; `RiskEconomyState.oath_shard_eligible` + `RunState.meta_progression_eligible` invariants]
- **Save-failure handling returns structured `ActionResult` errors — never silent swallow, never crash.** `ProfileRepository` mirrors `SaveRepository`'s structured error codes (AC5). [project-context.md save rules; the `SaveRepository` precedent]
- **Headless test command** (must pass before → review / → done): `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`. `godot` is NOT on the Bash PATH — run via PowerShell or the console binary `C:/Users/Rasmus/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe`. Apply the false-PASS grep guard (`SCRIPT ERROR|Parse Error|Compile Error|Failed to load script`) beyond documented negative-path diagnostics; diagnose from the per-file `FAIL` list, not stderr `ERROR:` lines. [CLAUDE.md; project-context.md; MEMORY godot-headless-test-binary-path + the 8.1 resume-verify lesson]

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` lines 2076-2107] — Story 8.3 ACs (verbatim); Epic 8 intro (line 2019); line 399 (GDD FR95 + implementation FR67 → capped, sparse meta power → Story 8.3).
- [Source: `_bmad-output/planning-artifacts/epics.md` FR28 (line 79), FR59 (line 140), FR60 (line 142)] — the FR coverage for no-manual-seed-progression + the outpost meta loop + the run summary.
- [Source: `_bmad-output/implementation-artifacts/8-2-run-summary-snapshot.md`] — the PREVIOUS story: `RunSummary` (the read seam 8.3 sits IN FRONT of), its `profile_meta.oath_shards_earned == 0` + `meta_progression_eligible`/`is_manual_seed` fields, its `not_yet_supported` list, and its explicit "8.3 award path sits IN FRONT of this read" Completion Note.
- [Source: `_bmad-output/implementation-artifacts/8-1-run-completion-and-return-to-outpost-flow.md`] — 8.1: `CompleteRunCommand` (the idempotency guard), the run-end vocabulary, `RunEndOutcome`, `RunOrchestrator.resolve_run_end` (the caller-driven run-end seam the award sits behind), and the `run_failed`-tripped-the-exhaustiveness-pin precedent.
- [Source: `godot/scripts/save/snapshots/run_snapshot.gd`] — the versioned-snapshot TEMPLATE for `ProfileSnapshot` (`SCHEMA_VERSION`, exact-key `to_dictionary()`, `parse` with `unsupported_save_schema`, lenient decode helpers); the `oath_shards`/`meta_progression` RUN-scoped placeholders (0/empty — the cross-run total is NOT here).
- [Source: `godot/scripts/save/save_repository.gd`] — the atomic-write + structured-error TEMPLATE for `ProfileRepository` (temp→backup→replace; `save_*` failure codes; `read` with `save_not_found`/`save_open_failed`/`save_parse_failed`).
- [Source: `godot/tests/unit/save/test_run_snapshot.gd` + `godot/tests/unit/save/test_save_repository.gd`] — the snapshot round-trip + repository save/read/failure TEST templates to mirror.
- [Source: `godot/scripts/core/commands/complete_run_command.gd`] — the idempotency guard (`run_already_terminal`, lines 98-106) + the 4.3 validate-then-mutate command idiom the award command mirrors; the explicit "8.3's Oath-Shard awarding must run BEHIND this seam" comment.
- [Source: `godot/scripts/run/run_orchestrator.gd` lines 690-722] — `resolve_run_end` (the caller-driven run-end seam the award sits behind; NOT auto-wired into `run_to_completion`).
- [Source: `godot/scripts/run/run_state.gd` lines 61, 215-259] — `is_terminal()`, `meta_progression_eligible` (the lockstep eligibility invariant), `PHASE_COMPLETED`/`PHASE_FAILED`, `selected_class_id`.
- [Source: `godot/scripts/run/risk_economy_state.gd` lines 61-104] — `oath_shard_eligible` (the run-level Oath-Shard eligibility gate, lockstep with `meta_progression_eligible`; `for_run(is_manual_seed)`; `validate(is_manual_seed)`).
- [Source: `godot/scripts/run/run_summary.gd`] — the 8.2 read the award sits IN FRONT of: `profile_meta.oath_shards_earned` (0, the field the award can feed), `meta_progression_eligible`/`is_manual_seed` (the AC4 warning data), `not_yet_supported`.
- [Source: `godot/scripts/core/events/domain_event.gd`] — the append-only enum (line 39 last member) + the factory idiom (`run_failed` 300-318, `economy_changed` 420-439) + the `_validate_*_payload` allowlist idiom (`_validate_run_failed_payload` 1053-1072) + the `_has_lower_snake_payload`/`_has_nonnegative_integral_payload`/`_has_string_payload` helpers the new `oath_shards_awarded` event mirrors.
- [Source: `godot/tests/unit/core/test_domain_event.gd` lines 2120-2181] — the `expected_ids` exhaustiveness pin (register the new id or the suite FAILS by design — the epic-transition heads-up).
- [Source: `godot/scripts/core/results/action_result.gd`] — the `ActionResult.ok(events, metadata)`/`error(code, metadata)` idiom + the lower_snake error-code validity rule (`_is_valid_error_code` — no spaces/dots/colons/hyphens).
- [Source: `godot/scripts/core/commands/game_command.gd`] — the `GameCommand` base (`command_id`, `validate`/`execute` returning `ActionResult`) the award command extends.
- [Source: `godot/scripts/core/state/rng_stream_set.gd` lines 6-31] — `required_streams()` (7 streams — UNCHANGED; the award draws ZERO RNG).
- [Source: `_bmad-output/auto-gds/retro-notes/epic-8.md`] — the 8.1/8.2 dispositions naming 8.3: the meta-save-shape-is-its-own-snapshot-with-migration constraint, the award-behind-the-idempotency-guard seam, the `oath_shards_awarded`-trips-the-exhaustiveness-pin heads-up, the `boss_node_id`-absent BREAKING heads-up, the diagnostic-noise heads-up, the round-header discipline.
- [Source: `_bmad-output/implementation-artifacts/deferred-work.md` — the 8.3 meta-profile/awarding defer ~lines 55-64 and ~159-165 (this story closes the AWARD half); the 8.4 content-merge defer; the two 8.1 `[Review][Defer]` items ~lines 192-210 (untouched); the T2 meta-save-shape heads-up. Grep the `[Defer]` phrases — line numbers drift as the ledger grows.]
- [Source: `project-context.md`] — all binding project rules (see Project Context Rules above).
- [Source: `CLAUDE.md` / `AGENTS.md`] — the full-suite command, folder/naming placement (`save` domain, save-migration tests), the mandatory story workflow, the false-PASS grep guard.

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
