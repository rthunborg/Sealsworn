---
baseline_commit: 2156772b0aca160c52639f072a7bdc4506e5af00
---

# Story 10.7: Asset, Audio, Placeholder, and UX Readiness Gate

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **Execution order (Epic-10 / Epic-11 / Epic-12 interleave):** Epic 10 executes `10-1 → 10-2 → 10-3 → 10-8 → 12-1 → 12-2 → 10-4 → 10-5 → 10-6 → 10-7` (`sprint-status.yaml` file order = execution order; Epic 11 landed before Epic 10, Epic 12 between 10-3 and 10-4, Story 10-8 between 10-3 and the Epic-12 block). **10.7 IS the LAST story of Epic 10 in execution order — the REAL Epic-10 close** (10-8 is numbered last for list continuity but ran early). After 10-7 reaches `done` the Epic-10 retrospective + epic close happen (the orchestrator's Phase-8 concern — the retro-notes flagged `is_last_in_epic` is derived from NUMBERING, so it may mis-flag 10-8 as the tail; the human/orchestrator treats 10-7 as the tail). `epic-10` is already `in-progress` and 10.7 is not the first story, so NO epic-status transition happens in THIS create-story step; do NOT mark Epic 10 `done` here. [Source: `_bmad-output/auto-gds/retro-notes/epic-10.md` §10-8 Phase-0 ("the real Epic 10 close is after 10-7"); `sprint-status.yaml`]

## Story

As a player,
I want the MVP build to use readable visuals and feedback even where production assets are deferred,
so that placeholders do not hide missing gameplay communication or readiness risk.

## Story Type & Scope Boundary (READ FIRST)

**This is a READINESS / GATE / AUDIT story — the direct sibling of Story 10.1 (device-tiers/perf-budgets readiness plan), Story 10.4 (playtest-comprehension checklist), Story 10.5 (accessibility & readability audit), and Story 10.6 (MVP-readiness gate). It is NOT a gameplay-feature story, NOT a UI/scene story, and NOT an asset-authoring story.** The primary deliverable is a **systematic Asset / Audio / Placeholder / UX Readiness GATE report (a markdown planning artifact)** authored under `_bmad-output/planning-artifacts/`, that:

1. **VERIFIES the placeholder-asset inventory (AC1)** — every required MVP asset class has a tracked placeholder (or better) id and each is readable enough to support tactical / reward / route / outpost / summary decisions. Roll up the SHIPPED `asset_sources/asset-manifest.md` inventory; do NOT re-author assets.
2. **VERIFIES the event-to-audio cue map + audio-off equivalence (AC2)** — the cue map covers all 13 required feedback meanings, and muting audio never removes required information (visual/textual equivalents remain), grounded in the SHIPPED cue map + the 10.5 audio-off audit + the existing accessibility tests.
3. **VERIFIES asset metadata / provenance completeness (AC3)** — every placeholder / AI-assisted asset records id, status, tool/source, prompt (if applicable), date, source ref, license/provenance, editable source path, runtime export path, and approval status; and an unapproved asset cannot be silently treated as production.
4. **DISPOSITIONS every open placeholder (AC4)** — the Story 4.5, Story 7.5, Story 8.2, and asset/audio-mapping placeholders, EACH as `replaced` / `de-scoped with an approved limitation` / `blocking readiness`, with the affected story + player-facing risk + owner + target replacement path.
5. **VERIFIES the UX-prerequisite coverage (AC5)** — the lightweight UX appendix (Story 11.1, `ux-appendix-run-flow.md`) covers tactical HUD, preview/confirm, inspect, passive modal, run map, outpost/meta, run summary, settings, and save/resume recovery.

- **The single required deliverable is the GATE REPORT doc.** Everything the ACs enumerate is CONTENT of that doc. Author it, cite the shipped manifest / audit / appendix / event-vocabulary each verdict rests on, and record honest availability gaps for anything a headless agent cannot perform (a human-eyes readability confirmation on a physical display, a produced audio track, a built settings scene). **This story VERIFIES + DISPOSITIONS + ROLLS UP; it does NOT author art/audio, build scenes, fix deferrals, or change production code.**
- **This is NOT a domain/tactical/save/RNG/content/generator/scene story.** Do NOT change any gameplay command, event, RNG stream, `RunSnapshot`/`ProfileSnapshot`/`SettingsSnapshot` schema, save key, generator/route/finale fingerprint, view model, content definition, presenter, `.tscn`, or `export_presets.cfg`. The full headless suite (**191 PASS / 0 `^FAIL`** at the current head — after Epic 12 + 10-4/10-5/10-6) must stay green and byte-for-byte behaviorally unchanged. If this story adds a test at all it is a NEW `test_*.gd` that ASSERTS a readiness FACT **not already covered by an existing test** — but note the audio-off / cue-coverage facts are ALREADY covered by `test_run_flow_accessibility_coverage.gd` + `test_tactical_accessibility_cues.gd` (10.5 / 2.6), so a new test is most likely UNNECESSARY (the 10.6 precedent). Do NOT duplicate an existing assertion.
- **Refining the provenance ledger IS in scope and is NOT a production-code touch.** `asset_sources/asset-manifest.md` / `provenance-log.md` live under `asset_sources/` (the project-context home for "editable asset source files, prompts, provenance, and reviews" — NOT `godot/` production code). The gate MAY refine the manifest's status/disposition columns to reflect this gate's AC4 dispositions (e.g. mark the Flooded conductive placeholder + the 0-file audio track `descoped` with the approved-limitation note). That is squarely AC3/AC4's job and does not touch `godot/`.
- **Do NOT stop and ask a human to author the audio track, produce final art, build the settings scene, or "approve the ship" as the primary outcome.** This is exactly the 10.1/10.5/10.6 honesty posture: the gate is completable and valuable as the DOCUMENTED verdict (verify what CAN be verified now + disposition each open placeholder as a documented limitation for the offline-first v0 CANDIDATE, with owner + target discharge path). Only escalate to `needs-human` if a required deliverable is genuinely un-authorable without an external secret/service — which is NOT the case here (the manifest, the audit, the appendix, and the event vocabulary all exist).
- **NO cloud/telemetry/live-service (NFR11). NO difficulty knob (hard non-goal). NO runtime AI content generation.** This gate CONFIRMS/records; it never adds any of those. AI tools MAY assist authoring assets during development, but the game must never call AI to generate runtime content, and procedural generation selects only from approved static definitions (project-context § AI and Asset Rules).

## What 10.7 VERIFIES vs DISPOSITIONS vs HANDS-OFF (the gate scope)

10.6 rolled up asset/audio/placeholder/UX at a HIGH LEVEL and explicitly **handed 10.7** the dedicated pass (`mvp-readiness-gate.md` §7.6 + the §8 gap-ledger rows "Flooded `_placeholder`" and the audio-track note). 10.7 is that dedicated gate.

| Item | 10.7's posture | Evidence / owner |
|---|---|---|
| Placeholder-asset roster (classes/enemies/boss/affinities/tiles/icons/passives/UI/SFX/ambient) | **VERIFY** each required class has a tracked-or-better id + is readability-gated | `asset_sources/asset-manifest.md` (the SHIPPED inventory + the 3-point grayscale/phone-size/silhouette gate); `provenance-log.md` |
| Event-to-audio cue map (13 required meanings) + audio-off equivalence | **VERIFY** the cue map is complete + no required info is audio-only | `asset-manifest.md` §SFX (14 cues); `accessibility-and-readability-audit.md` §7 (audio-off); `test_tactical_accessibility_cues.gd`; `test_run_flow_accessibility_coverage.gd` |
| Asset metadata / provenance completeness | **VERIFY** each entry carries the 10 required fields + the status vocabulary prevents silent promotion | `asset-manifest.md` + `provenance-log.md` + `prompt-pack.md`; project-context asset statuses (`exploration`/`placeholder`/`approved_reference`/`production`/`deprecated`) |
| **Flooded `affinity_conductive_danger_placeholder` (+ `..._vfx`) — F-1** | **DISPOSITION (10.7 OWNS this one)** — the conductive-interaction art/VFX + final cue: `replace` / `de-scope with approved limitation` / `block` | `deferred-work.md` (F-1, "owned by 10.7"); `project-context.md` line 450; `affinity_effect_resolver.gd` / `tactical_accessibility_model.gd` |
| The audio track (0 files shipped) | **DISPOSITION (10.7-owned)** — non-gating de-scope vs blocking | `accessibility-and-readability-audit.md` §7.2 (nothing is audio-only); MEMORY audio-track note (SFX 14 + ambient 7 pending, 0 files) |
| Story 4.5 node/boss placeholders (`node_placeholder_resolved` / `run_completed.outcome = boss_placeholder`) | **DISPOSITION** — boss REPLACED (Epic 9); non-combat node offers backed by Epic 6/7 domain, live-node-resolution wiring caller-driven/deferred | `deferred-work.md` "MVP placeholder / de-scope checkpoint (Story 4.5)"; `domain_event.gd` |
| Story 8.2 `RunSummary` `not_yet_supported` placeholders (echoes/unlock 0/[]) + the thin outcome label (F-2) | **DISPOSITION** — de-scoped; the actual FIX is the run-level event-store / summary-render story (origin 11.5), NOT 10.7 | `deferred-work.md` "dev of 8-2" (`not_yet_supported`; "TRACKED for the Epic-10 readiness pass … Story 10.7 names Story 8.2"); F-2 |
| Settings-SCENE UX gap (appendix §16 **G4** — settings VM/scene, PARKED) | **VERIFY (AC5)** the UX-appendix settings section exists; RECORD the paper-audit gap against the settings-scene owner; NOT 10.7's fix | `ux-appendix-run-flow.md` §12 + §16 G4; `deferred-work.md` 10-5 review F-3 |

> **⚠️ Two DIFFERENT "G4"s — do NOT conflate them** (the retro-notes flagged this ambiguity persists specifically INTO 10.7). Two independent gap-numbering schemes both use the label "G4":
> - **Device-tiers §6 G4** = *no on-device / windowed render-frame profiler for sustained 60/30 FPS frame stability* (NFR6). One of the physical-device **G1–G7** gaps (10.6-owned; NOT this story). [Source: `device-tiers-and-performance-budgets.md` §6]
> - **UX appendix §16 G4** = *the settings VIEW MODEL / settings SCENE gap* (PARKED — no settings scene; the outpost has no difficulty selector). This is the settings paper-audit gap that touches THIS story's AC5 UX-prerequisite check + the difficulty non-goal. [Source: `ux-appendix-run-flow.md` §16; `deferred-work.md` 10-5 review F-3]
> Keep them distinct in the gate report. The "G1–G7" label always means the device-tiers physical-device set; the settings-VM gap is always "appendix §16 G4 (settings)". [Source: `_bmad-output/auto-gds/retro-notes/epic-10.md` §10-6 Phase-3]

## Acceptance Criteria

Sourced verbatim from `epics.md` (Epic 10, §"Story 10.7: Asset, Audio, Placeholder, and UX Readiness Gate", lines ~2578–2611). Five Given/When/Then AC groups.

1. **Asset placeholder inventory (AC1).** GIVEN production art or audio is not yet final, WHEN MVP asset planning is reviewed, THEN production assets/audio may be deferred **only if placeholder ids exist for**: 3 playable class portraits/icons or hero silhouettes, 2 locked class silhouettes/icons, 3 enemy-pattern visuals, 1 boss visual, 4 affinity treatments, Small/Medium Labyrinth tiles/props, baseline weapon/support icons, 20–30 passive icons or placeholder glyphs, tactical/outpost/run UI frames, core SFX, and ambient loops, AND **each placeholder is readable enough to support tactical, reward, route, outpost, and summary decisions**.

2. **Event-to-audio cue map (AC2).** GIVEN audio feedback is mapped from domain outcomes, WHEN the event-to-audio cue map is reviewed, THEN it includes **movement, weapon hits, enemy actions, hazards, preview/confirm distinction, passive pickup, Consume, Destroy, curse/corruption, door sealing, death, reward reveal, and boss victory**, AND **muting audio never removes required information because visual/textual equivalents remain available**.

3. **Asset metadata validation (AC3).** GIVEN placeholder or AI-assisted assets are used, WHEN asset metadata is validated, THEN each entry records **stable id, status, tool or source, prompt if applicable, date, source reference, license/provenance notes, editable source path, runtime export path, and approval status**, AND **unapproved placeholder or exploration assets cannot be silently treated as production assets**.

4. **Placeholder disposition (AC4).** GIVEN placeholder behavior exists in Story 4.5, Story 7.5, Story 8.2, or asset/audio mappings, WHEN final MVP readiness is assessed, THEN **each placeholder is replaced, explicitly de-scoped with an approved limitation, or listed as blocking readiness**, AND **the readiness notes identify the affected story, player-facing risk, owner, and target replacement path**.

5. **UX prerequisite check (AC5).** GIVEN UI-heavy scene production is about to begin, WHEN UX prerequisites are checked, THEN **a lightweight UX appendix or equivalent implementation notes exist for tactical HUD, preview/confirm, inspect, passive modal, run map, outpost/meta, run summary, settings, and save/resume recovery**, AND **the absence of a standalone UX document remains non-blocking only for domain-first Epic 1 work and view-model/command-bridge contracts**.

### AC Verification (how "done" is checked)

- **AC1** — the gate report has an asset-inventory section that maps EVERY required roster class (3 playable class portraits/silhouettes, 2 locked class silhouettes, 3 enemy visuals, 1 boss visual, 4 affinity treatments, Small/Medium tiles + props, weapon + support icons, 20–30 passive icons/glyphs, tactical/outpost/run UI frames, core SFX, ambient loops) to a tracked-or-better id in `asset-manifest.md`, and states the readability basis (the 3-point grayscale / phone-size / silhouette gate). A class with NO placeholder id and NO produced asset is a GAP that blocks the "may be deferred only if placeholder ids exist" condition — record it (none currently: the visual roster is `approved`/`generated`; audio is a `planned` 0-file track). Missing a roster class or the readability basis = AC1 not met.
- **AC2** — the gate report has an event-to-audio cue-map section that maps EACH of the 13 named feedback meanings (movement / weapon hits / enemy actions / hazards / preview / confirm / passive pickup / Consume / Destroy / curse-corruption / door sealing / death / reward reveal / boss victory) to (a) the SHIPPED cue id in `asset-manifest.md` §SFX and (b) the past-tense `DomainEvent.Type` it fires off (e.g. door sealing → `ROUTE_SEALED` [Story 4.4 / FR46]; weapon hits + hazards + boss damage → `DAMAGE_APPLIED`; movement → `ENTITY_MOVED`; Consume → `PASSIVE_CONSUMED`; Destroy → `PASSIVE_DESTROYED`; curse → `CURSE_APPLIED`; reward reveal → `REWARD_OFFERED`/`REWARD_RESOLVED`; boss victory → `BOSS_DEFEATED` + `RUN_COMPLETED` outcome `victory`; preview/confirm → the `feedback_preview`/`feedback_committed` accessibility cues + the optional `AUDIO_FEEDBACK_PREVIEW`/`AUDIO_FEEDBACK_COMMITTED` ids). It states the audio-off equivalence conclusion (no required info is audio-only) with its contract backing (10.5 §7 + the two accessibility tests) — the two preview/commit cues are the ONLY cues that declare an audio id and both keep a non-color visual channel; warning/damage/reward carry no audio id at all. Missing a meaning, the event-source mapping, or the no-audio-only conclusion = AC2 not met.
- **AC3** — the gate report has a metadata/provenance section confirming every placeholder / AI-assisted asset entry carries the 10 required fields (id / status / tool-or-source / prompt-if-applicable / date / source-ref / license-provenance / editable-source-path / runtime-export-path / approval-status), grounded in `asset-manifest.md` + `provenance-log.md` + `prompt-pack.md`, and confirms the STATUS VOCABULARY prevents silent promotion (`exploration`/`placeholder`/`generated`/`approved`/`descoped`/`blocking`; project-context `exploration`/`placeholder`/`approved_reference`/`production`/`deprecated`; `_placeholder` in filenames; the "Approved" checkbox = the 3-point gate pass) — an unapproved/exploration asset is structurally distinguishable from production and cannot be silently shipped as production. Note any entry with a missing field as a metadata gap to fix in the ledger. Missing the field-completeness confirmation or the no-silent-promotion guarantee = AC3 not met.
- **AC4** — the gate report has a placeholder-disposition ledger with a row for EACH open placeholder in **Story 4.5** (`node_placeholder_resolved` / `run_completed.outcome = boss_placeholder`), **Story 7.5** (Flooded `affinity_conductive_danger_placeholder` + `..._vfx` — F-1, the 10.7-OWNED item), **Story 8.2** (`RunSummary` `not_yet_supported` echoes/unlock placeholders + the F-2 blank outcome label), and the **asset/audio mappings** (the 0-file audio track; the `generated`-but-not-`approved` icons; the `planned` in-engine UI frames/overlays). Each row records the disposition (`replaced` / `de-scoped with an approved limitation` / `blocking`) + the affected story + the player-facing risk + the owner + the target replacement path. **Sensible default disposition (per the offline-first v0 CANDIDATE scope + the 10.1/10.5/10.6 honesty posture): `de-scoped with an approved limitation`** for anything a headless agent cannot produce (art/audio/scene), each with its owner + target — a placeholder is `blocking` ONLY if it makes the loop genuinely unplayable or hides required gameplay communication (none currently does — every non-color cue channel is present). Missing a named-story row, a disposition, or the story/risk/owner/target quartet = AC4 not met.
- **AC5** — the gate report has a UX-prerequisite section that VERIFIES `ux-appendix-run-flow.md` (Story 11.1) covers each named surface — tactical HUD (§1), preview/confirm (§2), inspect (§3), passive modal (§4), run map (§5), outpost/meta (§7), run summary (§8), settings (§12), save/resume recovery (§13) — citing the appendix section for each, and states that the "absence of a standalone UX document" caveat is non-blocking only for domain-first Epic 1 work + view-model/command-bridge contracts (the appendix §0.3 architectural rule). Record the appendix's own Contract Gaps (§16 — G1 run-context HUD, G3 oath-shards display, appendix §16 G4 settings VM) against their owners; do NOT resolve them. Missing a named surface's coverage citation or the non-blocking-caveat statement = AC5 not met.
- **AC-support (the overall verdict + Epic-10 close note)** — the report states an overall asset/audio/placeholder/UX readiness verdict (expected: **READY_WITH_GATES** — the visual roster is present + readability-gated, the cue map is complete + audio-off-equivalent, the provenance is tracked, every open placeholder is dispositioned with an owner, and the UX appendix covers every surface; with the audio track + the Flooded conductive art/VFX + the human-eyes readability confirmation as recorded pre-ship follow-ups), consistent with `sprint-status.yaml` `readiness_status: READY_WITH_GATES` (do NOT silently flip it). It notes 10.7 is the Epic-10 close (the retrospective + epic-done transition follow, orchestrator-owned). The full suite stays green; if a test is added it is a NEW passing readiness-fact `test_*.gd` (no existing pin moved).

## Tasks / Subtasks

- [x] **Task 1 — Create the Asset/Audio/Placeholder/UX readiness gate report doc and frame it (AC1–AC5)**
  - [x] Author `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md` (a PLANNING artifact, NOT under `godot/`). Keep a `readiness`/`gate` token in the filename so the readiness/create-story globs discover it (the 10.1/10.4/10.5/10.6 discoverability discipline).
  - [x] Open with: purpose (the dedicated Epic-10 asset/audio/placeholder & UX-readiness gate — the pass 10.6 §7.6 handed here; discharges GDD FR46 [door-sealing feedback] + FR112/FR113/FR114 [MVP visual/audio baseline + preview/commit cue mapping] + NFR17/NFR18 [asset provenance/tracking] as the Epic-10 asset gate), scope (VERIFY the inventory + cue map + provenance + UX coverage that CAN be verified now; DISPOSITION each open placeholder as a documented limitation for the offline-first v0 CANDIDATE; ROLL UP the shipped `asset-manifest.md` + the 10.5 audit + the 11.1 appendix — do NOT re-author assets, build scenes, or fix deferrals), and the two-G4 disambiguation.
  - [x] State "this gate VERIFIES + DISPOSITIONS + ROLLS UP; it authors no art/audio, builds no scene, fixes no deferral, and changes no production code" explicitly. Note 10.7 IS the Epic-10 close (the retro + epic-done follow; orchestrator-owned).

- [x] **Task 2 — Verify the placeholder-asset inventory (AC1)**
  - [x] Author the AC1 asset-inventory section by ROLLING UP `asset_sources/asset-manifest.md` (the SHIPPED inventory that already declares it "Satisfies the Story 10.7 readiness gate and NFR17/NFR18"). Map EACH required roster class to its tracked-or-better id + status: 3 playable class portraits/silhouettes (`char.warrior`/`char.pyromancer`/`char.ranger` — `approved`); 2 locked class silhouettes (`char.necromancer_locked`/`char.shadeblade_locked` — `approved`); 3 enemy visuals (`enemy.iron_cultist`/`enemy.gate_brute`/`enemy.ash_seer` — `approved`); 1 boss visual (`boss.larval_avatar` — `approved`); 4 affinity treatments (`affinity.scorched`/`flooded`/`cursed`/`darkness` — `approved`); Small/Medium tiles + props (9 `tile.*` + door/door_sealed/reward_object props — `approved`); weapon icons ×9 + support icons ×2 (`generated`); 20–30 passive glyphs (`icon.passive.001–028`, 28 glyphs — `generated`, satisfies the 20–30 band); tactical/outpost/run UI frames (the `ui.*` rows — `planned`, assembled in-engine from the `generated` Recraft frame kit `button_plate`/`panel_frame`/`modal_frame`); core SFX (14 cues — `planned`/0-file); ambient loops (7 — `planned`/0-file).
  - [x] State the READABILITY basis: the 3-point grayscale / phone-size / silhouette gate (the manifest's "Approval = passes the 3-point readability gate"); the 22 Scenario board-art assets already PASSED it (regenerable via `python asset_sources/_tools/process_board_art.py`, per the manifest's §"Scenario board art — gate + runtime export"). Confirm the "may be deferred ONLY if placeholder ids exist" condition holds for every class (it does — every class has at minimum a tracked/planned id). Record the human-eyes readability confirmation on a physical display as an availability gap (ties to the 10.5 ASG-1/ASG-2 physical-device passes → the 10.6 physical-device pass owner), NOT a block.

- [x] **Task 3 — Verify the event-to-audio cue map + audio-off equivalence (AC2)**
  - [x] Author the AC2 cue-map section. For EACH of the 13 named meanings, state (a) the SHIPPED cue id in `asset-manifest.md` §SFX and (b) the past-tense `DomainEvent.Type` source: movement → `sfx.move` / `ENTITY_MOVED`; weapon hits → `sfx.weapon_hit` / `DAMAGE_APPLIED`; enemy actions → `sfx.enemy_action` / `ENTITY_MOVED`+`DAMAGE_APPLIED`+`TILE_MARKED` (telegraph); hazards → `sfx.hazard` / `DAMAGE_APPLIED`(hazard DoT)+`TILE_MARKED`; preview → `sfx.preview` / the `feedback_preview` cue (+ optional `AUDIO_FEEDBACK_PREVIEW`); confirm → `sfx.confirm` / `feedback_committed` (+ optional `AUDIO_FEEDBACK_COMMITTED`) — kept DISTINCT from preview; passive pickup → `sfx.passive_pickup` / `ITEM_GAINED`+`PASSIVE_CONSUMED`; Consume → `sfx.consume` / `PASSIVE_CONSUMED`; Destroy → `sfx.destroy` / `PASSIVE_DESTROYED`; curse/corruption → `sfx.curse` / `CURSE_APPLIED`; door sealing → `sfx.door_seal` / `ROUTE_SEALED` (Story 4.4, the "doors seal behind the hero as a containment law" cue — GDD FR46); death → `sfx.death` / `RUN_COMPLETED`(outcome `failed`/`run_failed`)+`FIRST_DEATH_RECORDED`; reward reveal → `sfx.reward_reveal` / `REWARD_OFFERED`+`REWARD_RESOLVED`; boss victory → `sfx.boss_victory` / `BOSS_DEFEATED`+`RUN_COMPLETED`(outcome `victory`)+`FIRST_VICTORY_RECORDED`.
  - [x] State the AUDIO-OFF EQUIVALENCE conclusion + its contract backing (REUSE, do NOT re-derive): the accessibility contract GUARANTEES every committed-action cue has a non-color visual/textual channel that holds with audio off (`TacticalAccessibilityModel._feedback_entry` sets `visual_available` independent of `audio_available`); ONLY the two preview/commit cues declare an audio id and both keep a visual channel; warning/damage/reward carry NO audio id. Cite `accessibility-and-readability-audit.md` §7 + `test_tactical_accessibility_cues.gd::_audio_feedback_cues_always_have_visual_or_textual_equivalents` + `test_run_flow_accessibility_coverage.gd`. Conclude: **no required information is audio-only** — and audio is a 0-file placeholder track today, so there is no audio-only surface even in principle. Do NOT add a new test for this (already covered).

- [x] **Task 4 — Verify asset metadata / provenance completeness (AC3)**
  - [x] Author the AC3 metadata section confirming every placeholder / AI-assisted asset carries the 10 required fields, grounded in `asset_sources/asset-manifest.md` (id / status / source-dir / runtime-path / approval columns + the per-section provenance notes: tool, prompt-pack id, date, license), `asset_sources/provenance-log.md` (the full provenance + CU ledger), and `asset_sources/prompt-pack.md` (the prompts/negatives). Confirm the editable-source (`asset_sources/...`) → runtime-export (`godot/assets/...`) split is recorded per asset.
  - [x] Confirm the status vocabulary PREVENTS silent promotion: the manifest statuses `planned`→`placeholder`→`generated`→`approved` (+ `descoped`/`blocking`) and the project-context asset statuses (`exploration`/`placeholder`/`approved_reference`/`production`/`deprecated`, `_placeholder` in filenames) make an unapproved/exploration asset structurally distinguishable from production — the "Approved" checkbox is the 3-point-gate pass, so a `generated`-but-unchecked icon cannot be silently treated as `production`. Record any entry MISSING a required field as a metadata gap to fix in the ledger (a manifest refinement is in-scope — `asset_sources/` is not `godot/` production code); note the icon rows are `generated` with approval pending (☐) — that is the tracked, honest state, not a violation.

- [x] **Task 5 — Disposition every open placeholder (AC4) — the core new analysis**
  - [x] Author the placeholder-disposition ledger. **Story 4.5:** `run_completed.outcome = boss_placeholder` → **REPLACED** by Epic 9 (the real Larval Avatar boss level + victory reuse the SAME `boss` node + `run_completed` boundary; the `boss_placeholder` outcome id is RETAINED in the vocabulary by design — risk none, owner Epic 9 [closed]); the non-combat `node_placeholder_resolved` types (shop/reforge/event/rest) → the backing DOMAIN systems exist (Epic 6 loot/rewards, Epic 7 risk/events) but the live-node-resolution HUD wiring is caller-driven/deferred (the same present-in-domain / thin-in-live-flow status as 10.6 §3.3) → **de-scoped with an approved limitation**, owner = the reward/passive/event live-HUD-wiring story, risk LOW (integration-proven).
  - [x] **Story 7.5 (the 10.7-OWNED marquee item):** the Flooded `affinity_conductive_danger_placeholder` (+ `..._vfx`) in `affinity_effect_resolver.gd` / `tactical_accessibility_model.gd` → **de-scope with an approved limitation** (default): it ALREADY carries a non-color `shape`(+label+text) channel (reads with color stripped even as a placeholder — proven by `test_run_flow_accessibility_coverage.gd`'s "every registered affinity_* cue incl. the tracked Flooded conductive-danger PLACEHOLDER carries a non-color channel"), and no LIVE conductive-interaction mechanic exists yet (11.4 surfaces only the deterministic MARKS). Player-facing risk: LOW (the danger reads; the FULL conductive-interaction art/VFX + the final [non-placeholder] cue id is the deferred piece). Owner: a later affinity-effects / conductive-interaction VFX story (post-MVP). Target: replace the `_placeholder` cue ids with FINAL ids + author the conductive art/VFX when the live conductive mechanic ships. It is NOT a color-only or missing-cue violation → NOT `blocking`.
  - [x] **Story 8.2:** the `RunSummary` `not_yet_supported` placeholders (echoes_discovered / unlock_progress render as 0/[] until a live source exists) + the F-2 blank `outcome_or_cause` outcome label → **de-scoped with an approved limitation** — the outcome is conveyed non-color via the reveal beats + `phase`; the ACTUAL fix (thread a run-level event store so the summary carries real echoes/unlock/outcome) is owned by the **run-level event-store / summary-render story (origin 11.5 code review; F-2 / T4)**, NOT 10.7. Risk LOW (a readability-completeness gap, not a broken step).
  - [x] **Asset/audio mappings:** the **audio track** (0 files, all 14 SFX + 7 ambient `planned`) → **de-scoped with an approved limitation** (NON-GATING — nothing is audio-only per Task 3 / 10.5 §7; owner = a post-MVP audio-production pass; target = author + import + `approve` the SFX + ambient tracks); the **`generated`-but-not-`approved` icons** (weapons/support/passives/currency) → **de-scoped** (present + tracked; owner = an asset-approval pass to confirm the 3-point gate on import + check the Approved box); the **`planned` in-engine UI frames + engine-drawn overlays** (`ui.*`, `overlay.move_range`/`attack_range`/`blocked_line`/`fog_hidden`/`fog_memory`) → **de-scoped** (assembled/drawn in-engine when the scenes are built; owner = the scene stories). For EACH row give: affected story + player-facing risk + owner + target replacement path. Classify NONE as `blocking` unless it genuinely hides required gameplay communication (none does).

- [x] **Task 6 — Verify the UX-prerequisite coverage (AC5)**
  - [x] Author the AC5 UX-prerequisite section VERIFYING `ux-appendix-run-flow.md` (Story 11.1 — the "lightweight UX appendix" delivered expressly as "consumed by Story 10.7's UX prerequisite check") covers each named surface with a section citation: tactical HUD (§1), preview/confirm (§2), inspect panel (§3), passive modal (§4), run map (§5), outpost/meta menu (§7), run summary (§8), settings (§12), save/resume recovery (§13) — plus the appendix's bonus coverage (hero select §6, first-death §9 / first-victory §10 reveal moments, manual-seed no-progression warning §11). State the appendix maps every screen to the EXISTING view-model/command-bridge contracts without inventing domain surfaces (§0.3 the one architectural rule).
  - [x] State the non-blocking caveat: "the absence of a standalone UX document remains non-blocking only for domain-first Epic 1 work and view-model/command-bridge contracts" (satisfied — the appendix exists and binds to contracts; the caveat only ever excused the domain-first Epic-1 phase). RECORD the appendix's own Contract Gaps (§16: G1 in-run run-context HUD, G3 "Oath Shards earned" display, **appendix §16 G4 settings VM/scene** — PARKED, the settings paper-audit / F-3, owner = the settings-scene owner 11.3/11.5) against their owners — 10.7 records them, it does NOT build the scenes or resolve them. Keep the settings §16 G4 DISTINCT from the device-tiers §6 G4 (FPS profiler).

- [x] **Task 7 — Roll up, state the verdict, and finalize (AC1–AC5)**
  - [x] State the overall asset/audio/placeholder/UX readiness verdict (expected **READY_WITH_GATES** — visual roster present + readability-gated (AC1); cue map complete + audio-off-equivalent (AC2); provenance tracked + no silent promotion (AC3); every open placeholder dispositioned with owner + target (AC4); UX appendix covers every surface (AC5); with the audio track + the Flooded conductive art/VFX + the human-eyes physical-display readability confirmation as recorded pre-ship follow-ups). Confirm consistency with `sprint-status.yaml` `readiness_status: READY_WITH_GATES` — do NOT silently flip it (the verdict IS "ready with documented gates").
  - [x] Note this is the Epic-10 CLOSE (10.7 is the last story in execution order): after 10.7 reaches `done`, the Epic-10 retrospective + the `epic-10: in-progress → done` transition follow (orchestrator/Phase-8-owned) — do NOT perform that transition in THIS story (create-story only marks the epic in-progress on the FIRST story; 10.7 is not the first). Cross-reference the sibling gate 10.6 (it handed 10.7 this pass) + the two audits/appendix consumed.
  - [x] Verify the full headless suite is unchanged-green (**191 PASS / 0 `^FAIL`**, READ the actual count) and `git diff --check` is clean; confirm NO production `godot/` gameplay/save/RNG/content/scene path was touched (only the planning gate doc + optionally a refined `asset_sources/asset-manifest.md`/`provenance-log.md` + at most one net-new readiness-fact test — most likely none). Apply the false-PASS grep guard.

## Dev Notes

### What this story is (and is not)

Epics 1–9 shipped a complete headless deterministic domain; Epic 11 wired the LIVE run-flow scenes + hands-off loop; Epic 12 wired the INTERACTIVE hands-on tap-loop + class loadout; the Epic-10 first tranche (10-1/10-2/10-3/10-8) shipped the readiness measurement + regression + fairness harnesses; 10-4 shipped the comprehension checklist; 10-5 shipped the accessibility audit; **10-6 shipped the final MVP-readiness gate and explicitly HANDED 10.7 the asset/audio/placeholder & UX-readiness pass** (`mvp-readiness-gate.md` §7.6 + §8). **Story 10.7 is that dedicated gate — and it is the REAL Epic-10 close.**

The single most important discipline (mirroring 10.1, 10.4, 10.5, 10.6, 11.1): **verify what CAN be verified now (the shipped asset inventory, the cue map, the provenance ledger, the UX-appendix coverage) + DISPOSITION each open placeholder as a documented limitation for the v0 candidate (owner + target) + record honest availability gaps for the human-eyes/audio-production/scene-build rest — do NOT author art/audio, build a scene, fix a deferral, or silently expand into a feature/scene change.** This is a readiness/gate/audit story; the assets, the manifest, the audit, the appendix, and the deferrals are the INPUTS this gate dispositions — it does not implement them. The one exception: refining `asset_sources/asset-manifest.md` / `provenance-log.md` disposition columns IS in scope (that IS the AC3/AC4 provenance ledger, and `asset_sources/` is not `godot/` production code).

### The verification reality (why art/audio/scene passes are availability gaps, not blockers)

- The ACs are dischargeable WITHOUT authoring final art, producing the audio track, building the settings scene, or a human-eyes readability confirmation, because the inventory + cue map + provenance + UX coverage are all SHIPPED and readable: the gate legitimately produces the AC1 inventory roll-up (grounded in `asset-manifest.md`), the AC2 cue map + audio-off conclusion (grounded in the manifest §SFX + the 10.5 audit + the two accessibility tests), the AC3 provenance confirmation (grounded in the manifest + provenance-log + prompt-pack), the AC4 disposition ledger, and the AC5 UX-appendix coverage verification (grounded in `ux-appendix-run-flow.md`).
- This is the SAME honesty posture 10.1 used (record each gap against an owner rather than invent the data), 10.5 used (the human-eyes availability gaps → the 10.6 gate), and 10.6 used (DECIDE each open gap as a documented limitation for the v0 candidate). 10.7's job is to DISPOSITION each open placeholder as an acceptable documented readiness LIMITATION for the offline-first v0 CANDIDATE (with an owner + target discharge path), NOT to author the missing art/audio or build the missing scene.
- **Do NOT stop and ask a human to author the audio track, produce final art, build the settings scene, or approve the ship as the primary outcome.** The story is completable and valuable without them (the inventory verification + the cue map + the provenance confirmation + the disposition ledger + the UX-appendix coverage). Only escalate to `needs-human` if a required deliverable is genuinely un-authorable without an external secret/service — which is not the case here (every input exists).

### The inputs 10.7 consumes (READ before authoring — do NOT rebuild them)

The gate ROLLS UP these; it does not rebuild them. An audit that re-authors an asset, re-derives the manifest, or forks a second cue map is a review miss.

| Input | Path | Load-bearing detail |
|---|---|---|
| **The SHIPPED asset manifest (AC1/AC3 backbone)** | `asset_sources/asset-manifest.md` | Declares itself "Satisfies the Story 10.7 readiness gate and NFR17/NFR18". The full v0 inventory (~92 visual + 14 SFX + 7 ambient), the id/status/source/runtime/approval columns, the per-section provenance, the §SFX cue map (the 14 cues = AC2's 13 meanings), the 3-point readability gate, the Scenario board-art gate+export note. |
| **The provenance + prompt ledgers (AC3)** | `asset_sources/provenance-log.md`, `asset_sources/prompt-pack.md`, `asset_sources/style-bible.md`, `asset_sources/scenario-workflow.md`, `asset_sources/icons/_future/UNUSED-ASSETS.md` | The full provenance + CU ledger (tool/model/date/license), the prompt-pack ids + negatives, the style direction, and the NFR18 non-MVP spare tracking. |
| **10.5 accessibility audit (AC2 audio-off backbone)** | `_bmad-output/planning-artifacts/accessibility-and-readability-audit.md` | §7 the audio-off equivalence audit (no required info is audio-only; the 0-file placeholder-track fact); §4.6 + §5 F-1 the tracked Flooded conductive-danger placeholder (10.7-owned); §8 the run-flow cue-coverage test; §10 the explicit 10.7 handoff of F-1 + the audio track. |
| **11.1 UX appendix (AC5 backbone)** | `_bmad-output/planning-artifacts/ux-appendix-run-flow.md` | §0.7 the screen roster; §1 HUD / §2 preview-confirm / §3 inspect / §4 passive modal / §5 run map / §7 outpost / §8 run summary / §12 settings / §13 save-resume (the AC5 required surfaces); §16 the Contract Gaps (G1/G3/appendix §16 G4 settings VM); §0.3 the one architectural rule; §12.3 the difficulty non-goal. |
| **10.6 gate (the handoff source)** | `_bmad-output/planning-artifacts/mvp-readiness-gate.md` | §7.6 rolls placeholder/asset/audio up + HANDS 10.7 the Flooded `_placeholder` (F-1) + the 0-file audio track; §8 gap-ledger rows name 10.7 as owner; §10 the overall `READY_WITH_GATES` verdict 10.7 stays consistent with. |
| **The domain-event vocabulary (AC2 event sources)** | `godot/scripts/core/events/domain_event.gd` | The `DomainEvent.Type` enum: `ENTITY_MOVED`, `DAMAGE_APPLIED`, `TILE_MARKED`, `ROUTE_SEALED` (the Story-4.4 door-seal containment cue / FR46), `NODE_PLACEHOLDER_RESOLVED`, `RUN_COMPLETED` (outcome `boss_placeholder`/`completed`/`victory`/failed), `PASSIVE_CONSUMED`/`PASSIVE_DESTROYED`, `ITEM_GAINED`, `REWARD_OFFERED`/`REWARD_RESOLVED`, `CURSE_APPLIED`, `FIRST_DEATH_RECORDED`/`FIRST_VICTORY_RECORDED`, `BOSS_DEFEATED`. Each AC2 cue maps to one of these past-tense events. READ, do not change. |
| **The placeholder cue-catalog source (AC4 F-1)** | `godot/scripts/ui/view_models/tactical_accessibility_model.gd`; `godot/scripts/rules/operations/affinity_effect_resolver.gd` | The `_CUE_CATALOG` holds the FINAL affinity/Darkness cues + the DISTINCT-from-final Flooded `affinity_conductive_danger_placeholder`/`..._vfx`. READ to confirm the placeholder still carries a non-color channel; do NOT change it (that is the deferred conductive-interaction story's job). |
| **On-disk asset state** | `asset_sources/**` (editable masters), `godot/assets/**` (runtime mirror) | Visual assets present (characters ×5, enemies ×3 + boss, affinities ×4, tiles ×9 + props, weapon/support/passive/currency/ui/overlay icons); `godot/assets/audio/**` dirs exist but hold **0 files** (the placeholder audio track). Confirm the editable→runtime split matches the manifest. |
| **The MVP asset/FR traceability** | `_bmad-output/planning-artifacts/epics.md` FR-map | "GDD FR46: door sealing/containment-law feedback → Story 4.4 and Story 10.7"; "GDD FR112/FR113/FR114: MVP visual/audio baseline and preview/commit cue mapping → Story 10.7"; "Placeholder replacement/de-scope checkpoints → Stories 4.5, 7.5, 8.2, and 10.7". |

### Epic-10 conventions ratified by earlier stories (constraints — from the epic retro-notes)

Folded from `_bmad-output/auto-gds/retro-notes/epic-10.md`. Only the entries that bear on THIS story:

- **[10-6, Phase 3] Two DIFFERENT "G4"s — the ambiguity persists specifically INTO 10.7.** device-tiers §6 "G4" (on-device FPS-stability profiler, physical-device G1–G7) vs UX-appendix §16 "G4" (settings view-model/scene gap, PARKED). 10.7's AC5 UX-prerequisite check + its difficulty-non-goal touch the settings surface → it references the appendix §16 G4 (settings). KEEP THE TWO DISTINCT in the gate report: "G1–G7" always = the device-tiers physical-device set (10.6-owned, NOT this story); the settings-VM gap is always "appendix §16 G4 (settings)". [Source: `epic-10.md` §10-6 Phase-3]
- **[10-8, Phase 0] `is_last_in_epic` is derived from NUMBERING — but 10-7 genuinely IS the Epic-10 tail in EXECUTION order** (10-8 is numbered last for continuity but ran early after 10-3). So for 10-7 the epic-end (Phase-8) retrospective + the `epic-10 → done` transition SHOULD follow once 10-7 is `done` (the orchestrator's concern). Recorded so 10-7 is treated as the real close; this create-story step performs NO epic-status change (10-7 is not the FIRST story, and marking `done` is a later manual/orchestrator step). [Source: `epic-10.md` §10-8 Phase-0]
- **[10-2, Phase 5] Avoid blanket "leave everything untouched" clauses that contradict a specific sanctioned task.** This story's ONLY sanctioned edits are: the NEW gate doc (required) + an OPTIONAL refinement of `asset_sources/asset-manifest.md`/`provenance-log.md` disposition columns (in-scope AC3/AC4 provenance work, NOT `godot/` code) + at most one OPTIONAL net-new readiness-fact `test_*.gd` (most likely unnecessary — the audio-off/cue facts are already covered). Those sanctioned edits OVERRIDE the generic "touch no `godot/`" reflex — but the reflex still holds for everything else (no gameplay/save/RNG/content/generator/view-model/presenter/scene/`.tscn`/`export_presets.cfg`/fingerprint/sample change). [Source: `epic-10.md` §10-2]
- **[10-1, Phase 5] iOS/G7 is a physical-device packaging gap owned by the 10.6 gate — NOT an asset-readiness item.** 10.7 does not touch export presets or packaging; if the gate mentions iOS at all it is only to note that asset packaging (icons/splash) for a producible iOS binary is downstream of the G7 macOS/Xcode follow-up (10.6-owned), not an asset-content gap this gate resolves. [Source: `epic-10.md` §10-1]

### Deferred-work items that OVERLAP this story (fold in — do NOT reopen unrelated ledger entries)

Checked every open entry in `_bmad-output/implementation-artifacts/deferred-work.md` against 10.7's asset / audio / placeholder / UX surface. The overlapping ones (10.7 DISPOSITIONS these; it does not author/build/fix them):

- **[DISPOSITION — 10.7 OWNS this one] The Flooded conductive-danger `_placeholder` (F-1, from 10-5 review; the standing "Flooded electric-interaction `_placeholder` (Epic-10 readiness, 10-7)" entry).** `affinity_conductive_danger_placeholder` (+ `..._vfx`) is a TRACKED, distinct-from-final placeholder that ALREADY carries a non-color `shape`(+label+text) channel — NOT a color-only or missing-cue violation. Default disposition: **de-scope with an approved limitation** (owner = a later conductive-interaction affinity-effects/VFX story; target = final cue ids + the conductive art/VFX when the live mechanic ships; risk LOW). This is the ONE placeholder 10.7 explicitly OWNS the disposition for. [Source: `deferred-work.md` 10-5 review F-1; `project-context.md` line 450; `mvp-readiness-gate.md` §7.6/§8]
- **[DISPOSITION — 10.7-owned] The 0-file audio track.** 10.6 §7.6 handed audio-track readiness to 10.7 (non-gating — nothing is audio-only per 10.5 §7). Default disposition: **de-scope with an approved limitation** (owner = a post-MVP audio-production pass; target = author + import + approve the 14 SFX + 7 ambient). [Source: `mvp-readiness-gate.md` §7.6 + §8 audio-track row; MEMORY sealsworn-asset-pipeline (SFX 14 + ambient 7 pending, 0 files)]
- **[DISPOSITION — AC4 names Story 8.2] The `RunSummary` `not_yet_supported` placeholders (echoes/unlock 0/[]) + the F-2 blank outcome label.** The 8.2 ledger explicitly says these are "TRACKED for the Epic-10 readiness pass (AC5; Story 10.7 names Story 8.2)". Default disposition: **de-scoped** — the ACTUAL fix (a run-level event store so the summary carries real echoes/unlock/outcome) is owned by the **run-level event-store / summary-render story (origin 11.5; F-2 / T4)**, NOT 10.7. 10.7 records it against that owner. [Source: `deferred-work.md` "dev of 8-2" (`not_yet_supported`… "Story 10.7 names Story 8.2"); F-2 in the 10-5 review]
- **[RECORD against owner — NOT 10.7's fix] The settings paper-audit gap (appendix §16 G4 / F-3).** 10.7's AC5 verifies the UX-appendix settings SECTION exists (§12) — but no settings SCENE/VM is built, so the human-eyes settings readability stays a paper audit. Default: record it against the **settings-scene owner (11.3/11.5)**; do NOT build a settings scene/VM here; do NOT propose a difficulty knob (the difficulty non-goal is confirmed at the contract level, regression-enforced). [Source: `deferred-work.md` 10-5 review F-3; `ux-appendix-run-flow.md` §16 G4]
- **[DISPOSITION — AC4 names Story 4.5] The Story-4.5 `node_placeholder_resolved` / `boss_placeholder` checkpoint.** boss → REPLACED (Epic 9); shop/reforge/event/rest non-combat node offers → backing domain exists (Epic 6/7), live-node-resolution HUD wiring caller-driven/deferred → **de-scoped** (owner = the reward/passive/event live-HUD-wiring story). [Source: `deferred-work.md` "MVP placeholder / de-scope checkpoint (Story 4.5)"]
- **Every OTHER deferred-work entry** (the run-level event STORE itself; the LIVE in-node board save; the `_relocate_scratch` reference-driver perf cost + the Medium/Scorched determinism-coverage gap; the Necromancer/Shadeblade class-kit content; the affinity-driven GENERATION modifier; the passive-combat-effect engine; the LIVE `content_discovered` source; the G1–G7 physical-device passes) **is OUT of scope for THIS gate — none is an asset/audio/placeholder/UX-readiness item; do NOT reopen, re-defer, or fix any of them.** They belong to later save-shape / content / operations / physical-device stories (several already dispositioned by 10.6).

### The asset state on disk (what already exists — READ, do not re-author)

- **Editable masters under `asset_sources/`:** characters ×5 (`char.warrior`/`pyromancer`/`ranger` playable + `char.necromancer_locked`/`shadeblade_locked`), enemies ×3 (`ash_seer`/`gate_brute`/`iron_cultist`) + boss (`boss.larval_avatar`), affinities ×4 (`scorched`/`flooded`/`cursed`/`darkness`), tiles ×9 + props (`door`/`door_sealed`/`reward_object`), icons (weapons ×9, support ×2, passives ×28, currency ×4, ui frame kit ×3, overlays/banners), plus non-MVP spares under `icons/_future/` (NFR18). The provenance docs (`asset-manifest.md`, `provenance-log.md`, `prompt-pack.md`, `style-bible.md`, `scenario-workflow.md`, `UNUSED-ASSETS.md`) are all present.
- **Runtime mirror under `godot/assets/`:** characters ×5, enemies ×3 + `boss.larval_avatar`, `tiles/affinities/` ×4, `tiles/props/` ×3, `tiles/` ×9, `art/ui/icons/icon_placeholder.svg` (a passive-fallback placeholder referenced by `passive_definition.gd`). The `godot/assets/audio/{sfx,ambience,music}/**` dirs EXIST but hold **0 files** — the placeholder audio track (confirmed on disk).
- **This gate does NOT add, move, re-export, or re-author any asset file.** It VERIFIES the inventory + provenance + readability basis are RECORDED and DISPOSITIONS the gaps. Any change it makes is to the provenance LEDGER (`asset_sources/*.md`), never to a binary asset or a `godot/` code/scene path.

### Testing standards

- Headless GDScript tests under `godot/tests/` mirroring the domain. Every test file `extends "res://tests/unit/test_case.gd"` and exposes `run() -> Dictionary` returning `result()`; assertions via `assert_true`/`assert_equal`/`assert_false` with a descriptive message. Deterministic, no rendering/audio/UI dependency. The headless harness is SCENE-FREE (`script.new().run()`, NO `SceneTree`) — do NOT write a SceneTree presenter/`.tscn` test.
- **This gate story most likely adds NO new test** (the 10.6 precedent). AC2's audio-off equivalence + the cue-coverage FACT are already asserted by `godot/tests/unit/ui/test_run_flow_accessibility_coverage.gd` (every affinity/Darkness cue incl. the tracked Flooded conductive PLACEHOLDER resolves non-color) + `test_tactical_accessibility_cues.gd` (the muted-feedback preview-vs-committed distinction + "every cue with an audio id STILL carries a non-color channel"). REFERENCE these as evidence. Add a NEW `test_*.gd` ONLY if it asserts a genuinely net-new readiness FACT not otherwise covered — homed under `godot/tests/unit/`, reading the LIVE surface (never a hand-copied cue list), a deliberate-update tripwire. Do NOT duplicate an existing assertion (which the story forbids) and do NOT perturb the byte-for-byte-green suite unnecessarily.
- Full suite (must pass before review/done):
  `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`
  The `godot` binary is NOT on the Bash/`where` PATH — run through PowerShell (`C:\Users\Rasmus\bin\godot.cmd` or `C:/Users/Rasmus/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe`). **Env gotcha (10-6 lesson): do NOT inline the PowerShell command via Bash** — Git Bash expands `$LASTEXITCODE` before PowerShell parses it (silent parse failure); write a `.ps1` and run `-File`, not `-Command`. Apply the false-PASS grep guard (grep raw output for `SCRIPT ERROR|Parse Error|^FAIL`; the 6 documented stderr negatives — int64-overflow ×2, malformed-JSON ×3, `invalid_node_type` ×1 — still PASS and are NOT a regression). Current baseline after 10.6: **191 PASS / 0 `^FAIL`** (READ the actual count from the run; do not assume).
- After any doc/ledger/test change, re-run the suite and `git diff --check`; confirm `git status --short` shows only the gate doc (+ optionally the refined `asset_sources/*.md` provenance ledger + at most one optional additive test + this story file + the sprint-status move).

### Project Structure Notes

- **Primary output location:** `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md` (a PLANNING artifact, NOT under `godot/`). Keep a `readiness`/`gate` token in the filename for discoverability (the 10.1/10.4/10.5/10.6 precedent).
- **Optional secondary edits (in-scope, NOT `godot/` code):** refine `asset_sources/asset-manifest.md` / `asset_sources/provenance-log.md` disposition/status columns to reflect this gate's AC4 dispositions. `asset_sources/` is the project-context home for provenance/reviews — updating it is AC3/AC4 work, not a production-code touch.
- **Optional test location (only a genuinely net-new readiness FACT):** a `test_*.gd` under `godot/tests/unit/`. The headless runner auto-discovers `test_*.gd` under `res://tests/unit` + `res://tests/integration` — no manifest edit needed. Most likely unnecessary (existing tests cover the audio-off/cue facts).
- **Do NOT touch:** any gameplay command / event / RNG stream / `RunSnapshot`/`ProfileSnapshot`/`SettingsSnapshot` schema / save key / generator-route-finale fingerprint / seed-regression SAMPLE / view model / content definition / presenter / `.tscn`; `export_presets.cfg`; any binary asset under `asset_sources/**` or `godot/assets/**` (this gate does not author/move/re-export art or audio); the `_CUE_CATALOG` in `tactical_accessibility_model.gd` (the Flooded `_placeholder` cue ids stay DISTINCT-from-final — 10.7 DISPOSITIONS them in the doc, it does not rename them); `prototype/` (frozen validation evidence); `_bmad/` (installer-managed). This story DISPOSITIONS gaps (records them against owners) — it does not fix, author, build, or re-pin anything.
- **`sprint-status.yaml`:** `readiness_status` already reads `READY_WITH_GATES`; update it only if the gate verdict genuinely changes it (it should not). On completion of THIS create-story step, move `10-7-...` `backlog → ready-for-dev` — the dev-story + code-review phases later move it to `review`/`done`. Do NOT touch the `epic-10` status here (stays `in-progress`; the `→ done` transition is the post-10.7 orchestrator/manual step after the Epic-10 retrospective).
- **Naming/organization:** follow the project-context Code Organization rules — `snake_case` files, `PascalCase` classes, `UPPER_SNAKE_CASE` constants. Match the sibling readiness docs' structure (`accessibility-and-readability-audit.md` / `mvp-readiness-gate.md`): purpose/scope → per-AC sections → the placeholder-disposition ledger → the availability-gaps ledger → the Epic-10 gate handoff/close note → references → change log.

### Project Context Rules

Extracted from `project-context.md` (the canonical rulebook — refreshed after Epic 12 + the Epic-10 first tranche). The rules that bear on THIS story:

- **Static Content & Asset Rules (lines 376–385):** track AI-assisted assets with tool, prompt, date, source references, license/provenance notes, editable source path, runtime export path, and approval status (== AC3's 10 fields); asset statuses are `exploration`/`placeholder`/`approved_reference`/`production`/`deprecated`; placeholder assets include `_placeholder` in filenames; content must pass schema + semantic + smoke-test + HUMAN APPROVAL before production use; AI may assist authoring but the game must NEVER call AI to generate runtime content, and procedural generation selects only from approved static definitions. AC3 VERIFIES the manifest satisfies these; AC1/AC4 rely on the status vocabulary preventing silent promotion.
- **Editable source / provenance home (lines 331–332):** editable asset source files, prompts, provenance, and reviews live in `asset_sources/`, NOT `godot/assets/`; runtime-ready art/audio/fonts/shaders live in `godot/assets/`. This is why refining the provenance ledger is NOT a production-code touch, and why the editable→runtime split is a per-asset AC3 field.
- **Accessibility is color-INDEPENDENT + audio-absent-equivalent (lines 299–300):** every committed-action cue must have a visual + textual equivalent that holds with audio off; preserve the `feedback_preview` (pre-commit) vs `feedback_committed` (post-result) distinction; `AttackPreviewContractMatrix` / `TacticalAccessibilityModel._CUE_CATALOG` is the canonical color-independence audit driver. This is AC2's audio-off backbone.
- **Keep the Flooded electric-interaction `_placeholder` ids DISTINCT-from-final (line 450):** `affinity_conductive_danger_placeholder`/`..._vfx` are a TRACKED Epic-10 readiness item (replace / de-scope / block) each with a non-color channel; `affinity_scorched_hazard`/`affinity_pathing_pressure`/the two Darkness cues are FINAL ids. AC4 DISPOSITIONS the placeholder (default de-scope) — it does NOT rename the cue ids (that is the deferred conductive-interaction story's job).
- **The Story-4.4 door-seal containment cue (`ROUTE_SEALED` / GDD FR46):** the deterministic `route_sealed` event is the door-sealing feedback AC2 maps `sfx.door_seal` to; presentation/audio MIRROR it, it never drives domain control flow. FR46 traces to Story 4.4 AND 10.7 (the cue-mapping half).
- **DIFFICULTY IS A HARD NON-GOAL; a regression test enforces the absence of difficulty keys (lines 308, 402):** no setting may scale enemy stats/HP/damage/rewards/RNG/run length; post-MVP challenge is explicit variant/trial/oath content. AC5's settings-surface check + any challenge-scope note CONFIRMS the non-goal (`SettingsSnapshot.PREFERENCE_KEYS` + `test_settings_snapshot.gd`) — it never proposes adding one.
- **Platform & Build Rules (lines 372–373):** debug/cheat tools disabled/inert in production; test content and experimental assets cannot ship unless explicitly approved for production. AC3's "unapproved cannot be silently treated as production" is this rule, applied to assets.
- **NO cloud/telemetry/multiplayer/live-service dependency (NFR11; lines 71, 396); NO production dependency on `prototype/` (NFR2; lines 72, 395).** The gate introduces none; it audits assets, not services.
- **Godot / testing (§ Testing Rules, line 360; line 78):** the headless runner auto-discovers `test_*.gd` under `res://tests/unit` + `res://tests/integration`, exits with the failure count. Run via PowerShell (the `godot` binary is NOT on the Bash/`where` PATH). Apply the false-PASS grep guard. Human playtests remain required for feel/readability (line 362) — the MAKES the human-eyes physical-display readability confirmation an availability gap the gate records against the 10.6 physical-device pass owner.
- **Actionable, compact readiness notes — never a raw dump (§ generator-diagnostics discipline):** a gate finding/disposition is compact + actionable (placeholder + affected story + disposition + player-facing risk + owner + target), never a bare "not ready" or an asset dump.

### References

- **Story source (verbatim ACs):** `_bmad-output/planning-artifacts/epics.md` — Epic 10 §"Story 10.7: Asset, Audio, Placeholder, and UX Readiness Gate" (lines ~2578–2611), incl. the 2026-07-04 Epic-11 prerequisite (the UX appendix 11.1 + the screen surfaces 11.3/11.5 are inputs). FR-map (lines ~389, ~400–401): "GDD FR46: door sealing/containment-law feedback → Story 4.4 and Story 10.7"; "GDD FR112/FR113/FR114: MVP visual/audio baseline and preview/commit cue mapping → Story 10.7"; "Placeholder replacement/de-scope checkpoints → Stories 4.5, 7.5, 8.2, and 10.7". NFR17/NFR18 (asset provenance/tracking + non-MVP spare tracking).
- **The SHIPPED provenance ledger (AC1/AC3 backbone — VERIFY, do NOT rebuild):** `asset_sources/asset-manifest.md` (the inventory + status/approval columns + the §SFX cue map + the 3-point readability gate + the Scenario board-art gate/export note), `asset_sources/provenance-log.md`, `asset_sources/prompt-pack.md`, `asset_sources/style-bible.md`, `asset_sources/scenario-workflow.md`, `asset_sources/icons/_future/UNUSED-ASSETS.md`.
- **The audio-off + cue-coverage evidence (AC2 — verify, do NOT duplicate):** `_bmad-output/planning-artifacts/accessibility-and-readability-audit.md` §7 (audio-off equivalence; §7.2 the 0-file placeholder-track fact), §4.6/§5 F-1 (the tracked Flooded conductive placeholder → 10.7), §8/§10 (the cue-coverage test + the 10.7 handoff); `godot/tests/unit/ui/test_run_flow_accessibility_coverage.gd`; `godot/tests/unit/.../test_tactical_accessibility_cues.gd`.
- **The UX appendix (AC5 backbone):** `_bmad-output/planning-artifacts/ux-appendix-run-flow.md` §0.7 roster; §1 HUD / §2 preview-confirm / §3 inspect / §4 passive modal / §5 run map / §7 outpost / §8 run summary / §12 settings / §13 save-resume; §16 Contract Gaps (G1/G3/appendix §16 G4 settings VM); §0.3 the one architectural rule; §12.3 difficulty non-goal.
- **The domain-event vocabulary (AC2 event sources):** `godot/scripts/core/events/domain_event.gd` — `ENTITY_MOVED`/`DAMAGE_APPLIED`/`TILE_MARKED`/`ROUTE_SEALED`/`NODE_PLACEHOLDER_RESOLVED`/`RUN_COMPLETED`/`PASSIVE_CONSUMED`/`PASSIVE_DESTROYED`/`ITEM_GAINED`/`REWARD_OFFERED`/`REWARD_RESOLVED`/`CURSE_APPLIED`/`FIRST_DEATH_RECORDED`/`FIRST_VICTORY_RECORDED`/`BOSS_DEFEATED`.
- **The placeholder cue-catalog source (AC4 F-1):** `godot/scripts/ui/view_models/tactical_accessibility_model.gd` (`_CUE_CATALOG` — the FINAL cues + the distinct Flooded `_placeholder`), `godot/scripts/rules/operations/affinity_effect_resolver.gd`.
- **The handoff source (10.6):** `_bmad-output/planning-artifacts/mvp-readiness-gate.md` §7.6 (rolls placeholder/asset/audio up + HANDS 10.7 the Flooded `_placeholder` F-1 + the 0-file audio track), §8 (the gap-ledger rows naming 10.7 owner), §10 (`READY_WITH_GATES`).
- **Prior readiness-doc precedents (the structural model):** `_bmad-output/implementation-artifacts/10-5-accessibility-and-readability-audit.md` + `_bmad-output/implementation-artifacts/10-6-mvp-readiness-gate-and-playable-build-preservation.md` (the closest structural twins — a readiness/audit doc + a placeholder/gap-disposition ledger + a gate handoff + honest availability gaps; touch-no-simulation).
- **Epic-10 retro (constraints folded above):** `_bmad-output/auto-gds/retro-notes/epic-10.md` — §10-6 Phase-3 (the two-G4 ambiguity persists into 10.7), §10-8 Phase-0 (10-7 is the real Epic-10 close), §10-2 (no blanket-untouched clause), §10-1 (iOS/G7 is 10.6-owned, not asset content).
- **Deferred-work ledger (overlapping items folded above):** `_bmad-output/implementation-artifacts/deferred-work.md` — F-1 Flooded `_placeholder` (→ 10.7 owns); the 0-file audio track (→ 10.7); the Story-8.2 `not_yet_supported` placeholders ("Story 10.7 names Story 8.2"); the F-2 thin-summary label (→ run-level event-store/summary-render owner); F-3 settings paper-audit (→ settings-scene owner); the Story-4.5 `node_placeholder_resolved`/`boss_placeholder` checkpoint (boss REPLACED by Epic 9).
- **Project rules:** `CLAUDE.md` / `AGENTS.md` / `project-context.md` (§ Static Content & Asset Rules lines 376–385; § Code Organization 331–332; § Presentation/View-Model/Accessibility 299–300 + line 450 the Flooded `_placeholder`; § Settings Rules 302–308 + difficulty non-goal 402; § Platform & Build 372–373; § Testing Rules 350–362 + line 78 PowerShell). `sprint-status.yaml` (`readiness_status: READY_WITH_GATES`; `10-7` is `backlog`; `epic-10` is `in-progress`; 10-7 is the execution-order tail).

## Dev Agent Record

### Agent Model Used

Opus 4.8 (`claude-opus-4-8[1m]`) — auto-gds dev-story delegate.

### Debug Log References

- Full headless suite (fresh, this gate build) via PowerShell (`godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`): **191 PASS / 0 `^FAIL`**, exit 0, final line `Headless tests passed.`, false-PASS grep guard (`SCRIPT ERROR` / `Parse Error` / `^FAIL`) **0 matches — clean**. Matches the post-10.6 baseline exactly (no test added, no `godot/` code touched). The 6 documented stderr negatives (int64-overflow ×2, malformed-JSON ×3, `invalid_node_type` ×1) are unchanged deliberate fail-path assertions, not regressions.

### Completion Notes List

- **Deliverable:** authored `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md` — the dedicated Epic-10 asset/audio/placeholder & UX readiness gate. All five ACs discharged with per-AC sections (AC1 inventory §3 / AC2 cue map + audio-off §4 / AC3 provenance §5 / AC4 disposition ledger §6 / AC5 UX coverage §7), an availability-gaps ledger (§8), the overall verdict + suite check + Epic-10 close note (§9), the no-touch invariants (§10), references (§11), change log (§12).
- **AC1:** every required roster class (3 playable + 2 locked classes, 3 enemies, 1 boss, 4 affinities, 9 tiles + 3 props, 9 weapon + 2 support icons, 28 passive glyphs, 4 currency, 9 UI frames, 5 overlays + banners, 14 SFX, 7 ambient) → a tracked-or-better manifest id; readability basis = the 3-point grayscale/phone-size/silhouette gate (board art passed; cue channels non-color). No no-id gap. Human-eyes physical-display confirmation recorded as AG-1.
- **AC2:** all 13 required feedback meanings mapped to a shipped `sfx.*` cue id + a past-tense `DomainEvent.Type` (door sealing → `ROUTE_SEALED` / FR46, etc.); audio-off equivalence REUSED from the 10.5 audit §7 + `test_tactical_accessibility_cues.gd::_audio_feedback_cues_always_have_visual_or_textual_equivalents` + `test_run_flow_accessibility_coverage.gd` (no new/duplicate test — the 10.6 precedent). Conclusion: no required info is audio-only; audio is a 0-file (now descoped) track.
- **AC3:** all 10 provenance fields confirmed present across `asset-manifest.md` + `provenance-log.md` + `prompt-pack.md`; status vocabulary + the Approved checkbox + the `_placeholder` marker prevent silent promotion; `generated` ☐ icons are a tracked approval-pending state, not a missing field.
- **AC4:** placeholder-disposition ledger — Story 4.5 boss **REPLACED** (Epic 9) + non-combat offers de-scoped; Story 7.5 Flooded `affinity_conductive_danger_placeholder` (+ `..._vfx`) **de-scoped with an approved limitation** (the 10.7-OWNED marquee item; non-color channel already present; NOT blocking); Story 8.2 `not_yet_supported`/F-2 de-scoped (run-level event-store owner); audio track de-scoped (non-gating); generated icons + planned UI frames tracked. None `blocking`. Each row carries story + risk + owner + target.
- **AC5:** the 11.1 UX appendix covers every named surface (HUD §1 / preview-confirm §2 / inspect §3 / passive modal §4 / run map §5 / outpost §7 / run summary §8 / settings §12 / save-resume §13); non-blocking caveat satisfied; §16 Contract Gaps recorded against owners (G1→11.3, G2 CLOSED by 11.3's RouteMapViewModel, G3→11.5, appendix §16 G4 settings→settings-scene owner), kept distinct from the device-tiers §6 G4 FPS profiler.
- **Sanctioned ledger refinement (AC3/AC4, NOT `godot/` code):** refined `asset_sources/asset-manifest.md` — flipped the 14 SFX + 7 ambient rows `planned → descoped` with per-section disposition notes + a new "Story 10.7 readiness-gate dispositions (AC4)" summary section. No binary asset authored/moved/re-exported; no `godot/` code, no `.tscn`, no `export_presets.cfg`, no `_CUE_CATALOG` cue id touched.
- **Verdict:** `READY_WITH_GATES` — consistent with `sprint-status.yaml` `readiness_status`, left as-is (not flipped). Pre-ship follow-ups: audio track (AG-2), Flooded conductive art/VFX (AG-4), human-eyes physical-display readability (AG-1), settings-scene human-eyes audit (AG-3).
- **Epic-10 close:** 10.7 is the execution-order tail; the Epic-10 retrospective + the `epic-10: in-progress → done` transition are orchestrator/Phase-8-owned and follow after `done` — NOT performed here (`epic-10` stays `in-progress`).
- **Git:** none run (orchestrator owns git/baseline); `baseline_commit` intentionally not added to frontmatter.

### File List

- `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md` (NEW — the gate report, the required deliverable)
- `asset_sources/asset-manifest.md` (MODIFIED — AC3/AC4 provenance refinement: 14 SFX + 7 ambient rows `planned → descoped` + disposition notes + a dispositions summary section; not `godot/` code)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (MODIFIED — `10-7-...` `ready-for-dev → in-progress → review`)
- `_bmad-output/implementation-artifacts/10-7-asset-audio-placeholder-and-ux-readiness-gate.md` (MODIFIED — tasks checked, Dev Agent Record, File List, Change Log, Status → review)

### Change Log

| Date | Version | Change | Author |
|---|---|---|---|
| 2026-07-12 | 0.1 | Story context created (create-story) — the dedicated Epic-10 Asset/Audio/Placeholder/UX readiness gate; the execution-order tail (real Epic-10 close). Five ACs framed (asset inventory / audio cue map + audio-off / metadata-provenance / placeholder disposition / UX-prerequisite), inputs + retro constraints + overlapping deferrals folded, the two-G4 disambiguation carried, the Flooded `_placeholder` F-1 + the 0-file audio track marked 10.7-owned dispositions. Status → ready-for-dev. | create-story (Opus 4.8) |
| 2026-07-12 | 1.0 | dev-story — authored the gate report `asset-audio-placeholder-ux-readiness-gate.md` (all 5 ACs + availability-gaps ledger + `READY_WITH_GATES` verdict + Epic-10 close note); refined `asset_sources/asset-manifest.md` (audio rows `planned → descoped` + dispositions summary, the sanctioned AC3/AC4 provenance edit). No `godot/` code / `.tscn` / `export_presets.cfg` / `_CUE_CATALOG` / binary asset touched; no test added (audio-off/cue facts already covered — 10.6 precedent). Full suite verified fresh: 191 PASS / 0 `^FAIL` / exit 0, false-PASS guard clean (byte-for-byte the post-10.6 baseline). All tasks/subtasks checked. Status → review. | dev-story (Opus 4.8) |

### Review Findings

**Round 1 of 3**

Primary adversarial code review (`gds-code-review` — Blind Hunter / Edge Case Hunter / Acceptance Auditor) of the current branch's diff vs the pre-dev baseline `2156772b0aca160c52639f072a7bdc4506e5af00` (the create-story context commit). **Review subject** (doc-primary GATE story): the primary deliverable `_bmad-output/planning-artifacts/asset-audio-placeholder-ux-readiness-gate.md`, the sanctioned `asset_sources/asset-manifest.md` provenance refinement, and this story file; auto-gds state/retro-note bookkeeping excluded per scope.

**Verdict: APPROVE.** Critical 0 / High 0 / Med 0 / Low 1. Open `[Review][Decision]`: 0.

Suite re-run fresh on the review head (PowerShell `-File`, false-PASS guard applied): **191 PASS / 0 `^FAIL`**, exit 0, final line `Headless tests passed.`, guard (`SCRIPT ERROR` / `Parse Error` / `^FAIL`) 0 matches, 0 unexpected errors (the 6 documented stderr negatives reproduced exactly — int64-overflow ×2, malformed-JSON ×3, `invalid_node_type` ×1 — NOT regressions). `git diff --check` clean. The diff touches NO `godot/` code/test/`.tscn`/`export_presets.cfg`/`_CUE_CATALOG`/binary-asset path (name-status confirmed: only the gate doc [new], `asset_sources/asset-manifest.md`, this story file, `sprint-status.yaml`, plus excluded auto-gds bookkeeping) — the byte-for-byte-unchanged-suite claim holds (191 → 191, matching the post-10.6 baseline).

Evidence independently cross-checked against the live source and CONFIRMED accurate (not merely asserted):
- **AC2 event map** — all 15 `DomainEvent.Type` values cited exist in `godot/scripts/core/events/domain_event.gd` (`ROUTE_SEALED`/FR46 door-seal, `ENTITY_MOVED`, `DAMAGE_APPLIED`, `BOSS_DEFEATED`, `RUN_COMPLETED` incl. the retained `boss_placeholder` outcome the validator still pins, etc.); the 13-meaning → cue-id → past-tense-event mapping is correct.
- **AC2 audio-off** — both cited tests exist with the exact asserted behavior: `test_tactical_accessibility_cues.gd::_audio_feedback_cues_always_have_visual_or_textual_equivalents` (builds the model with `{"audio_available": false}`, asserts every audio-declaring cue still carries a non-color channel) and `test_run_flow_accessibility_coverage.gd::_every_registered_affinity_and_darkness_catalog_cue_is_non_color` (asserts `has_non_color_channel("affinity_conductive_danger_placeholder")`).
- **AC4 Flooded F-1** — `_CUE_CATALOG["affinity_conductive_danger_placeholder"]` = `[SHAPE, LABEL, TEXT]` / `danger` severity in `tactical_accessibility_model.gd:79`; `affinity_effect_resolver.gd` sets `is_placeholder: true` on the conductive cue; the `_placeholder` cue/visual ids are unchanged (distinct-from-final) — the gate dispositions them in prose only, as required.
- **AC1 inventory** — the entire visual roster's ids/statuses match `asset-manifest.md` (classes/enemies/boss/affinities/tiles/props `approved` ☑; weapon/support/passive/currency icons `generated` ☐; UI frames + range/fog overlays `planned`).
- **AC5** — `ux-appendix-run-flow.md` covers every named surface incl. §13 save/resume and §16 Contract Gaps G1–G4; `RouteMapViewModel` (`godot/scripts/ui/view_models/route_map_view_model.gd`) exists → the "G2 CLOSED by 11.3" claim is substantiated; the difficulty-non-goal test `test_settings_snapshot.gd::_difficulty_non_goal_keys_are_absent` exists as cited; the device-tiers §6 G4 (FPS profiler) and appendix §16 G4 (settings) are kept distinct throughout.
- **AC3 / manifest edit** — the sanctioned `asset-manifest.md` refinement uses the manifest's own `descoped` vocabulary (defined line 6); 14 SFX + 7 ambient rows flipped `planned → descoped` with disposition notes + a "Story 10.7 readiness-gate dispositions (AC4)" summary section; no binary asset authored/moved/re-exported; the green suite on the edited head confirms nothing parses the manifest at runtime.
- **`sprint-status.yaml`** — `readiness_status: READY_WITH_GATES` left unflipped (consistent with the verdict); `10-7` moved to `review`; `epic-10` left `in-progress`.

Findings:

- [x] [Review][Patch][Low] Passive-glyph count is stated inconsistently in the new gate report (deliverable-internal precision). Ground truth on disk = **28** files (`asset_sources/icons/passives/icon.passive.001.svg`–`028.svg`; the `godot/assets/icons/passives/` runtime mirror holds 0, matching the `generated`/approval-pending state). The gate report §3.1 row 9 correctly says "28" (with "(28 ∈ [20,30])"), but its §3.1 Totals sentence (≈ line 139) says "**30** passive glyphs" — two different hard numbers for the same class within one document. Root cause is a pre-existing `asset-manifest.md` defect the gate faithfully echoed: the §"Passive glyphs" header (line 81, `icon.passive.001–030`) and the top Totals line (line 10, "30 passive glyphs") overstate by 2 while the manifest row + disk say 28. **Not a readiness issue** — AC1's "20–30" band is satisfied either way, so AC1 remains MET. Fix: reconcile the gate report §3.1 Totals to 28; and, since manifest refinement is sanctioned AC1/AC3 provenance work for this story, correct `asset-manifest.md` line 81 (`001–030` → `001–028`) and line 10 ("30 passive glyphs" → "28"). (The "~9 overlays/banners" total is hedged/approximate for the actual 8 and matches the manifest — no change needed.)
  - **[RESOLVED 2026-07-12]** Counted the on-disk truth first: `asset_sources/icons/passives/` holds exactly **28** files — a continuous run `icon.passive.001.svg`–`028.svg` (no gaps, no extras); the `godot/assets/icons/passives/` runtime mirror holds **0** (matching the `generated`/approval-pending state). Applied the counted 28: gate report §3.1 Totals sentence `30 → 28`; `asset-manifest.md` line 10 Totals `30 → 28` and line 81 header `` `icon.passive.001`–`030` `` → `` `icon.passive.001`–`028` `` (line 84 row already read `001–028`/`28`, now consistent). Left the AC1 "20–30" band, the §3.1 row-9 `28`, and the hedged `~92`/`~9 overlays` approximates unchanged (out of scope, and the `~9` matches the manifest per the finding). Doc-only edits — full suite unaffected (nothing parses these markdown files at runtime); `git diff --check` clean.

**Round 2 of 3**

Alternate-model secondary adversarial review (Opus 4.8, full reasoning depth) of the same branch diff vs the pre-dev baseline `2156772b0aca160c52639f072a7bdc4506e5af00` — an independent second pair of eyes on the doc-primary gate (deliverable `asset-audio-placeholder-ux-readiness-gate.md` + the sanctioned `asset_sources/asset-manifest.md` refinement + this story file).

**Round-1 [Patch][Low] fix verified landed.** On-disk truth counted first: exactly **28** passive source glyphs (`asset_sources/icons/passives/icon.passive.001.svg`–`028.svg`, continuous — no gaps, no extras; the `godot/assets/icons/passives/` runtime mirror holds **0**, matching the `generated`/approval-pending state). Gate §3.1 Totals + `asset-manifest.md` lines 10/81 reconciled to 28; a fresh grep found **no residual `30`/`030`** anywhere in the manifest or the gate report (only the correct AC1 "20–30 band" text remains). The Round-1 bullet is normalized to checked form.

Independently re-verified the load-bearing citations against LIVE source (not merely re-asserted): every AC2 `DomainEvent.Type` value exists in `domain_event.gd`; the three cited tests exist with the asserted method names (`_audio_feedback_cues_always_have_visual_or_textual_equivalents`, `_every_registered_affinity_and_darkness_catalog_cue_is_non_color`, `_difficulty_non_goal_keys_are_absent`); the `RunSummary` fields (`outcome_or_cause` / `echoes_discovered` / `unlock_progress` / `not_yet_supported`) + the `PHASE_COMPLETED` / `PHASE_FAILED` constants cited in §6.3 are accurate; the two-G4 disambiguation holds throughout. The diff is markdown/yaml-only (name-status: **zero** `godot/` code / test / `.tscn` / `export_presets.cfg` / `_CUE_CATALOG` / binary-asset path touched) → the suite is byte-for-byte unaffected; a fresh re-run on the review head confirmed **191 PASS / 0 `^FAIL`**, exit 0, false-PASS guard clean.

**Verdict: APPROVE.** Critical 0 / High 0 / Med 0 / Low 1. Open `[Review][Decision]`: 0.

Findings:

- [x] [Review][Patch][Low] AC2 §4.1 row 12 (Death) mis-attributes the death/failure event source. The gate mapped Death → `RUN_COMPLETED` (outcome `failed`/`run_failed`) + `FIRST_DEATH_RECORDED`, but `RUN_COMPLETED`'s validator (`domain_event.gd` `_validate_run_completed_payload`, ~lines 1355–1361) accepts ONLY the outcomes `boss_placeholder` / `completed` / `victory` — there is **no** `failed` outcome. The real death/failure boundary is a DISTINCT event, `RUN_FAILED` (enum `Type.RUN_FAILED`; `EVENT_ID_RUN_FAILED := &"run_failed"`; `cause` ∈ `RUN_FAILED_CAUSES`; drives `PHASE_FAILED`; the run-level FAILED counterpart of the Story-4.5 `run_completed` boss boundary). So `run_failed` is the event id of `RUN_FAILED`, NOT an outcome value of `RUN_COMPLETED` — a category error in the single most load-bearing AC2 table. AC2 stays MET (the `sfx.death` cue exists, the death meaning keeps its visual/textual equivalent, and the row already co-cites the correct `FIRST_DEATH_RECORDED`); this is a deliverable-internal accuracy defect, not a readiness issue. The primary Round-1 review certified this exact mapping as "correct" — the independent-second-eyes catch.
  - **[RESOLVED 2026-07-12, round 2]** Fixed in the gate report: §4.1 row 12 now maps Death → `sfx.death` / `RUN_FAILED` (`cause` ∈ `RUN_FAILED_CAUSES`, drives `PHASE_FAILED`) + `FIRST_DEATH_RECORDED`; and `RUN_FAILED` added to the §11 domain-event vocabulary list (which had listed `FIRST_DEATH_RECORDED` but conspicuously omitted the death event itself). Doc-only edit in the review-subject deliverable — full suite unaffected (nothing parses these markdown files at runtime); `git diff --check` clean.

No other new findings surfaced — AC1–AC5 remain MET, the `READY_WITH_GATES` verdict and the sanctioned `asset-manifest.md` refinement stand, and no `[Review][Decision]` or `[Review][Defer]` item arose (so no cross-story deferred-work ledger entry is needed for round 2).
