---
baseline_commit: 5be8594e398821cfbad9ca4c84bc2383d6ae94e2
---

# Story 9.4: Boss Victory and First-Victory Reveal

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want defeating the Larval Avatar to resolve victory and reveal the MVP story beat,
so that a successful run lands as a complete first endpoint.

## Acceptance Criteria

(Verbatim from `planning-artifacts/epics.md` lines 2291-2312, Story 9.4; FR62 — the first-VICTORY reveal "It did not die. It learned the way back."; FR63 — the Larval Avatar is the only required MVP boss. The FOURTH story of Epic 9 — "Larval Avatar MVP Finale." It consumes the boss RUNTIME BEHAVIOR Story 9.3 shipped — the live boss `TacticalEntityState`, the `BossAi` / `BossCommandAdapter` / `BossTurnResolver`, the two-turn telegraph→resolve flow, and the live `boss_phase_changed` phase re-resolution seam — and turns the boss reaching ZERO HP into a run VICTORY: a boss-defeated event + a run-victory event, a transition into the post-victory RETURN flow, the first-VICTORY narrative REVEAL tracked/persisted once (the 8.5 first-DEATH precedent MIRRORED at the OPPOSITE terminal phase), and a SKIPPABLE reveal that does not alter the run summary, outpost return, rewards, or progression. It does NOT build the finale seed-regression suite / run-length tuning hooks (9.5), the 9.1 encounter setup, the 9.2 definition/phase model, or the 9.3 boss fight behavior.)

**AC1 — Zero-HP boss resolves a boss-defeated + run-victory event and transitions to post-victory return**
- **Given** the Larval Avatar reaches zero HP
- **When** combat outcome evaluates
- **Then** a boss-defeated event and run-victory event are emitted
- **And** the run transitions to post-victory return flow.

**AC2 — First victory can show the reveal line and is tracked/persisted**
- **Given** the player wins for the first time
- **When** the victory reveal is displayed
- **Then** it can show "It did not die. It learned the way back."
- **And** the reveal is tracked so first-victory state is persisted.

**AC3 — Skipping the reveal still runs summary + outpost return and alters no rewards/progression**
- **Given** the reveal is skipped or dismissed
- **When** the player continues
- **Then** the run summary and outpost return still occur
- **And** skipping does not alter rewards or progression.

## Scope boundary (read FIRST)

This story closes the **boss VICTORY + first-VICTORY reveal boundary**: it makes the Larval Avatar reaching ZERO HP the run's real VICTORY endpoint and delivers the MVP story beat that closes a successful descent. Concretely it ships (a) a **boss-defeated → run-victory RESOLUTION** — when the boss entity dies (`current_hp <= 0`, `TacticalEntityState.is_dead()`), emit a **boss-defeated** event AND resolve the run to `PHASE_COMPLETED` with a **run-victory** `run_completed` event carrying the FREE-for-Epic-9 `victory` outcome (AC1), then transition to the post-victory RETURN flow (`next_destination == outpost`, FR32); (b) the **first-VICTORY narrative REVEAL** — a monotonic per-profile-lifetime `first_victory_recorded` LATCH + a `first_victory_recorded` event + a `FirstVictoryRevealBeat` read DTO carrying the FR62 line "It did not die. It learned the way back.", MIRRORING the ratified 8.5 first-DEATH precedent VERBATIM at the OPPOSITE terminal phase (AC2); and (c) the **skippable, off-critical-path reveal** — the reveal is structurally decoupled from the flag mutation (a skip cannot mutate the flag; there is no "skip command"), and the run SUMMARY + outpost RETURN + rewards + progression are COMPLETE without it (AC3). It is the "a successful run lands as a complete first endpoint" half of Epic 9's boss.

**⭐ THE SINGLE MOST IMPORTANT ARCHITECTURAL FACT — 9.4 is the FIRST story to wire a boss-defeat → run-victory CALL SITE, the long-parked run-end auto-wire seam that Epic-7 retro T1 flagged and 8.1/8.3/8.4/8.5/9.1/9.2/9.3 all consciously deferred to "the run-flow story that owns the live loop." That story is 9.4 for the VICTORY path.** 9.3 shipped the boss as a LIVE board entity (`BossTurnResolver` drives its turns; the player/test drops its HP; `resolve_phase_transitions` re-resolves phases) but STOPPED at the boss-setup terminus — it explicitly did NOT emit a boss-defeated event, did NOT resolve the run to COMPLETED, did NOT persist any first-victory flag, and did NOT build the reveal (all recorded OUT of 9.3's scope, earmarked for 9.4). **9.4 wires the seam: boss reaches 0 HP → emit boss-defeated → drive `CompleteRunCommand`/`RunOrchestrator.resolve_run_end` with the `victory` outcome → run reaches COMPLETED → (eligible only) the run-end command quartet the run-flow layer already owns (award/merge/first-death) is joined by the NEW first-victory latch.** RECORD the live-loop boundary you draw (see the OUT-of-scope live-loop note): 9.4 needs the boss-defeat → victory resolution + the reveal to be REAL and tested, but it does NOT need to auto-play the entire run-to-completion loop from `run_started` through a live tactical fight to victory with no test seams — the boss fight is still driven by explicit turns (9.3's posture), and the victory RESOLUTION is a caller-/test-driven step behind the run-end seam (exactly as the 8.3 award / 8.5 first-death are caller-driven). The COMPREHENSIVE full-run-through-the-shell integration + the finale seed regression are 9.5.

**⭐ MIRROR THE RATIFIED 8.5 FIRST-DEATH PRECEDENT VERBATIM — do NOT invent a parallel narrative-reveal system.** The first-VICTORY reveal is the EXACT structural twin of 8.5's first-DEATH beat, keyed off the OPPOSITE terminal phase (`PHASE_COMPLETED` victory vs 8.5's `PHASE_FAILED` death). Study `RecordFirstDeathCommand` + `FirstDeathNarrativeBeat` + the `first_death_recorded` event + the `ProfileSnapshot.first_death_recorded` latch (all read below in Dev Notes) and build the first-VICTORY analogue with the SAME shapes:
- **The command** — a `RecordFirstVictoryCommand` (the `RecordFirstDeathCommand` analogue) that takes the live TERMINAL `RunState` + the `ProfileSnapshot` + the run-level `sequence_id` in its constructor, validates sequence-id > 0 FIRST, gates on `run.is_terminal()` AND a **VICTORY-ONLY** gate (`run.phase == RunState.PHASE_COMPLETED` — the discriminator vs 8.5's `run_not_failed`; reject a FAILED run with a stable `run_not_completed` code), and flips `profile.first_victory_recorded` true ONCE (a subsequent victory rejects with `first_victory_already_recorded` + ZERO mutation + ZERO event). It draws ZERO RNG.
- **The latch** — a NEW `ProfileSnapshot.first_victory_recorded` bool, the FOURTH independent run-end idempotency marker (alongside the award's `last_awarded_run_seed`, the merge's `unlock_progress["_last_merged_run_seed"]`, and 8.5's `first_death_recorded`), monotonic PER-PROFILE-LIFETIME (fires on the FIRST victory across ALL runs). **This is the story's #1 `[Decision]` — see the SCHEMA `[Decision]` below.**
- **The event** — a NEW `first_victory_recorded` `DomainEvent.Type` member, appended at the enum END after `BOSS_PHASE_CHANGED` (the current tail, index 45 — NEVER renumber), a SYSTEM event (no actor), payload `{line_id, is_skippable, profile_id}` mirroring `first_death_recorded` VERBATIM, wired end-to-end (factory + `_validate_first_victory_recorded_payload` + `validate_payload` dispatch + both id maps + JSON round-trip + per-field malformed tests) AND the `expected_ids` exhaustiveness pin in `test_domain_event.gd` UPDATED (it FAILS-loud on a forgotten member BY DESIGN — do NOT loosen it).
- **The beat DTO** — a `FirstVictoryRevealBeat` (the `FirstDeathNarrativeBeat` analogue) carrying `has_beat` / `line_id` / `line` / `is_skippable` with the EXACT-key discipline, `for_first_victory(...)` + `from_event(...)` factories, a `LINE_BY_ID` const holding the FR62 line "It did not die. It learned the way back." resolved from a `first_victory` line id, and the fail-closed empty beat. A PURE read (ZERO RNG, no command, no event, no mutation) — a skip/dismiss is STRUCTURALLY a no-op because the DTO owns no truth (AC3 satisfied structurally, not behaviorally, the 8.5 posture).
- **Eligibility ([Decision] — mirror 8.5 Option A):** the first-victory latch is DELIBERATELY NOT gated on `meta_progression_eligible` — a manual-seed first victory STILL records the flag + shows the line (the line is PURE NARRATIVE FLAVOR — FR61/FR64 "story discovery optional"; setting a boolean narrative latch grants ZERO Oath Shards / unlocks / mastery, so it is not "granting meta progression" in the FR28 sense). RECORD it. (This diverges from the award/merge, which DENY a manual-seed run — but matches 8.5's ratified first-death Option A.)

**⭐ THE `victory` OUTCOME IS ALREADY RESERVED — 9.4 unblocks it; it is NOT a new marker.** 8.1 DELIBERATELY left `victory` FREE in the `run_completed` outcome allowlist for exactly this story: `RUN_COMPLETED_OUTCOME_BOSS_PLACEHOLDER` (`boss_placeholder`, the 4.5 boss stub) + `RUN_COMPLETED_OUTCOME_COMPLETED` (`completed`, the 8.1 generic completion) are the two current allowlist values, and `_validate_run_completed_payload` (`domain_event.gd:1239-1261`) REJECTS a stray `victory` today (a meaningful garbage-value guard). **9.4 ADDS `RUN_COMPLETED_OUTCOME_VICTORY := &"victory"` to the allowlist const + the validator's outcome check** so the real Larval-Avatar victory emits the SAME `run_completed` event with the `victory` outcome — WITHOUT touching the `boss_placeholder` / `completed` values or the boss node boundary. **Record this `[Decision]`.** The existing `run_completed` test that rejects a stray `victory` outcome must be UPDATED to now ACCEPT `victory` (and a NEW garbage outcome takes over as the rejection guard). Do NOT invent a parallel `run_victory` event — the run-victory IS `run_completed` with `outcome == victory` (the 8.1 "Epic 9 reuses THIS boundary, swapping only the boss's pre-completion behavior" contract).

**⭐ THE BOSS-DEFEATED EVENT — a NEW tactical/run event (AC1 requires "a boss-defeated event AND a run-victory event", i.e. TWO events).** AC1 explicitly demands a boss-defeated event that is DISTINCT from the run-victory (`run_completed`) event. The boss-defeated event records the TACTICAL fact (the Larval Avatar entity died) at the tactical layer; the run-victory (`run_completed` + `victory`) records the RUN-END fact. Add a NEW `boss_defeated` `DomainEvent.Type` member (appended at the enum END after any first-victory member you add — see the enum-ordering note), a SYSTEM or entity event recording `{boss_entity_id, defeated_at_phase_index (or phase_id), final_hp}` (RECOMMENDED shape — record the exact payload), wired end-to-end + the `expected_ids` pin. **Record whether boss-defeated is emitted by the boss turn resolver (when the boss's HP hits 0 after a `damage_applied`) or by the victory-resolution call site — RECOMMENDED: the boss turn resolver / a small victory-detection seam detects `boss.is_dead()` after damage applies and emits `boss_defeated`, then the caller drives the run-victory resolution.** Do NOT overload `level_defeat_reached` / `level_victory_reached` (those are the Epic-1 tactical level outcomes for a normal combat level, keyed to the player; the boss defeat is the boss entity dying — a distinct fact). Do NOT reuse `entity_attacked`/`damage_applied` as the "boss defeated" signal — AC1 wants a NAMED boss-defeated event.

**⭐ THE SEQUENCE-ID SEAM CONTRACT (9.3 review Round-1 Med finding — earmarked for 9.4) IS NOW DUE.** 9.3's `BossTurnResolver.resolve_phase_transitions(context, previous_phase_index, sequence_id_base := -1)` emits `boss_phase_changed` SYSTEM events that are NOT board-applied — within 9.3 their ids are log-ordering-only and benign, but the boss's ACTION events (from `resolve_boss_turn`, board-applied) and these phase-change events are produced by two independent calls that both read the same un-advanced `board.next_sequence_id()` baseline. **9.4 is the first consumer that INTERLEAVES both event streams (boss action events + phase-change events + the boss-defeated event + the run_completed victory event) into ONE ordered append-only log** (the run-end sequence the reveal/summary reads). So 9.4 MUST honor the seam contract 9.3 built: pass an explicit `sequence_id_base` (>= 0) to `resolve_phase_transitions` that does NOT collide with any id a board-applied event consumes, and thread the returned `next_sequence_id_after` as the reserved cursor for whatever it appends next (the boss-defeated + run-victory events). The `RunOrchestrator` already owns a monotonic run-level `_next_sequence_id` + `_advance_sequence_past(...)` — reuse that counter as the single shared monotonic source; NEVER let `resolve_phase_transitions` fall back to the board baseline when merging streams. **Record how you threaded the shared counter.** [Source: `godot/scripts/tactical/turns/boss_turn_resolver.gd:119-160` (the SEQUENCE-ID SEAM CONTRACT docstring + `sequence_id_base` param + `next_sequence_id_after` metadata); `godot/scripts/run/run_orchestrator.gd:913-921` (`_advance_sequence_past` — the shared run-level counter); `_bmad-output/auto-gds/retro-notes/epic-9.md` §9-3 Phase-7 note]

**⭐ EVERY run-victory/reveal event is EXPLAINABLE + DETERMINISTIC.** The boss-defeated + run-victory + first-victory events are all DETERMINISTIC records (ZERO RNG — a boss dying, a run completing, and a latch flipping are not rolls; the 8.3 `oath_shards_awarded` / 8.5 `first_death_recorded` deterministic-shell precedent — no `roll`/`draw_index`). The reveal LINE rides BY id (`line_id == first_victory`; the raw prose lives on `FirstVictoryRevealBeat`, NOT the event payload — the LINE-AS-ID posture). Same terminal `(run, profile)` → the SAME events + the SAME latch + the SAME reveal.

Do NOT redefine the established patterns — EXTEND them. 9.4 reuses: `CompleteRunCommand`'s outcome-parameterized run-END resolution (adding `victory` to the allowlist, NOT a parallel command) + its AC3 `run_already_terminal` no-double-grant guard; `RunOrchestrator.resolve_run_end` (the caller-driven run-END hook — 9.4's victory drives it from the boss-setup terminus, per its own 9.4-earmarked docstring at `:737-738`); `RecordFirstDeathCommand` (the run-end profile-mutation command idiom — VICTORY-only gate + once-only latch) → `RecordFirstVictoryCommand`; `FirstDeathNarrativeBeat` (the pure skippable read-DTO) → `FirstVictoryRevealBeat`; `ProfileSnapshot`'s versioned exact-key snapshot + lenient-parse (adding the `first_victory_recorded` field — see the SCHEMA `[Decision]`); the `first_death_recorded` event (factory + validator + id maps + exhaustiveness pin) → `first_victory_recorded`; the `BossTurnResolver` sequence-id seam contract (thread the shared counter); the `OutpostViewModel` off-critical-path beat render (the reveal joins the first-death beat as another OPTIONAL sub-surface — RECOMMENDED but see the OUT-of-scope UI-scene note); the named-RNG rule (ZERO RNG — the victory/reveal are deterministic records); the difficulty-non-goal guard (the victory is a boss reaching authored 0 HP, NOT a difficulty tier).

---

## IN scope (this story)

1. **The boss-DEFEATED tactical event + the zero-HP victory-detection seam (AC1)** — detect the Larval Avatar reaching ZERO HP (`TacticalEntityState.is_dead()` / `current_hp <= 0`) after a `damage_applied` to the boss, and emit a NEW `boss_defeated` `DomainEvent` recording the tactical defeat fact (RECOMMENDED payload: `{boss_entity_id, defeated_at_phase_index (or phase_id), final_hp}` — record the exact shape). RECOMMENDED site: the `BossTurnResolver` (or a small victory-detection method on it) checks `boss.is_dead()` after the damaging event applies and emits `boss_defeated`; the caller then drives the run-victory resolution. Append `boss_defeated` at the enum END (after any first-victory member added), wire it end-to-end + UPDATE `expected_ids`. Do NOT overload `level_victory_reached` / `level_defeat_reached`. [Source: `godot/scripts/tactical/entities/tactical_entity_state.gd:45-50` (`is_dead()`/`is_alive()`); `godot/scripts/tactical/turns/boss_turn_resolver.gd` (the boss turn driver 9.4 extends); `godot/scripts/core/events/domain_event.gd:45` (`BOSS_PHASE_CHANGED` the current enum tail)]
2. **The run-VICTORY resolution — `run_completed` with the `victory` outcome (AC1)** — add `RUN_COMPLETED_OUTCOME_VICTORY := &"victory"` to the `run_completed` outcome allowlist const + `_validate_run_completed_payload`'s outcome check (the two current values `boss_placeholder` + `completed` untouched); drive the run to `PHASE_COMPLETED` via `CompleteRunCommand` / `RunOrchestrator.resolve_run_end` with `victory`, emitting `run_completed` + `outcome == victory` + `next_destination == outpost` (FR32) → the post-victory RETURN flow. `CompleteRunCommand` classifies `victory` as a completion marker (it is NOT a death cause) — decide whether `victory` routes through `CompleteRunCommand`'s completion path (RECOMMENDED — it already handles the `completed` marker; add `victory` alongside it in `_is_completion_outcome` / the classifier) or a small dedicated path. **Record the `[Decision]`.** The existing `run_completed` test that rejects a stray `victory` must be UPDATED to ACCEPT `victory` (a new garbage value becomes the rejection guard). [Source: `godot/scripts/core/events/domain_event.gd:137-145` (`RUN_COMPLETED_OUTCOME_COMPLETED` + the "victory deliberately left free for Epic 9" note), `:1239-1261` (`_validate_run_completed_payload` outcome allowlist); `godot/scripts/core/commands/complete_run_command.gd:59,120-135,194-231` (the completion classifier + `_resolve_completed`); `godot/scripts/run/run_orchestrator.gd:741-758` (`resolve_run_end`)]
3. **The post-victory RETURN transition (AC1) — reuse the 8.1 run-end flow signal, do NOT build a new one** — the run-victory carries `next_destination == outpost` (the existing `RUN_END_DESTINATION_OUTPOST` — FR32) so the app returns to the outpost the way a death/completion does. The actual scene NAVIGATION stays deferred (UI-scene-last — 8.6 built the `OutpostViewModel` DATA + start-another-descent seam the navigation lands on; the boot/app-flow scene transition is a later story). 9.4 surfaces the run-END fact as DATA (the `RunEndOutcome` `for_completed(run, victory)` projection + the orchestrator's captured `run_end_destination`). [Source: `godot/scripts/core/events/domain_event.gd:161-166` (`RUN_END_DESTINATION_OUTPOST`); `godot/scripts/run/run_end_outcome.gd:90-102` (`for_completed`); `godot/scripts/run/run_orchestrator.gd:782-783` (`run_end_destination`)]
4. **The first-VICTORY latch + command (AC2) — mirror `RecordFirstDeathCommand` VERBATIM at the OPPOSITE phase** — add a `ProfileSnapshot.first_victory_recorded` bool (see the SCHEMA `[Decision]`); build a `RecordFirstVictoryCommand` that flips it true ONCE on the FIRST victory (VICTORY-only gate `run.phase == PHASE_COMPLETED`, reject a FAILED run with `run_not_completed`; once-only reject with `first_victory_already_recorded`), emits `first_victory_recorded`, draws ZERO RNG, and leaves run + profile byte-identical on ANY reject. Eligibility-INDEPENDENT (mirror 8.5 Option A — a manual-seed first victory still latches; record it). The caller persists the mutated profile via `ProfileRepository.write_profile` (repository UNCHANGED — the set flag rides `to_dictionary()`). [Source: `godot/scripts/core/commands/record_first_death_command.gd` (the command idiom VERBATIM — VICTORY-only gate replaces death-only); `godot/scripts/save/snapshots/profile_snapshot.gd:35-36,72,88,118,150` (the `first_death_recorded` home 9.4 adds a `first_victory_recorded` sibling to)]
5. **The first-VICTORY event (AC2) — `first_victory_recorded`, the `first_death_recorded` twin** — a NEW `DomainEvent.Type.FIRST_VICTORY_RECORDED` appended at the enum END (never renumbered), a SYSTEM event (no actor), payload `{line_id, is_skippable, profile_id}` (mirroring `first_death_recorded`), `EVENT_ID_FIRST_VICTORY_RECORDED := &"first_victory_recorded"`, `FIRST_VICTORY_LINE_ID := &"first_victory"`, wired end-to-end (factory + `_validate_first_victory_recorded_payload` reusing the same field rules + `validate_payload` dispatch + both id maps + JSON round-trip + per-field malformed tests) AND the `expected_ids` exhaustiveness pin UPDATED. [Source: `godot/scripts/core/events/domain_event.gd:43-45,84,195-200,436-454,1073-1077,1380-1395,2249-2254,2331-2336` (the full `first_death_recorded` wiring 9.4 mirrors); `godot/tests/unit/core/test_domain_event.gd:2669-2744` (the `expected_ids` pin + the `Type.size()-1` tie)]
6. **The first-VICTORY reveal beat DTO (AC2/AC3) — `FirstVictoryRevealBeat`, the `FirstDeathNarrativeBeat` twin** — a scene-free PURE-READ DTO carrying `has_beat` / `line_id` / `line` / `is_skippable` (the EXACT-key discipline), `for_first_victory(line_id := FIRST_VICTORY_LINE_ID, is_skippable := true)` + `from_event(event)` factories, a `FIRST_VICTORY_LINE := "It did not die. It learned the way back."` const + a `LINE_BY_ID` map (`{"first_victory": FIRST_VICTORY_LINE}`), and the fail-closed empty beat. ZERO RNG, no command, no event, no mutation — a skip/dismiss is STRUCTURALLY a no-op (the DTO owns no truth; AC3 satisfied structurally). [Source: `godot/scripts/run/first_death_narrative_beat.gd` (the beat DTO VERBATIM — swap the line + the by-id key + the phase framing)]
7. **AC3 — the reveal is OFF the critical path (skippable, alters no rewards/progression)** — the reveal is a SEPARATE optional surface: the run SUMMARY (8.2/8.4 `RunSummary`) + the outpost RETURN + rewards + progression are COMPLETE without it (add NO reveal field to `RunSummary`; do NOT make the summary or the outpost/start-another-descent path depend on the beat). A skip is the presentation NOT rendering the beat — there is NO "skip command" that could mutate rewards/unlocks/progression (the flag MUTATION is `RecordFirstVictoryCommand`, decoupled from the beat DELIVERY). RECOMMENDED (but see the UI-scene note): surface the reveal beat on the `OutpostViewModel` alongside the first-death beat as another optional sub-surface — OR leave the outpost render to a later UI story and ship only the DTO + the flag + the event + tests (the 8.5 "8.6 owns the render" split). **Record which you chose.** [Source: `godot/scripts/run/run_summary.gd` (COMPLETE without the reveal — add no field); `godot/scripts/ui/view_models/outpost_view_model.gd:73,90-95,184-193,228-230,310-313,358-363` (the first-death beat render the victory reveal MAY join)]
8. **The SCHEMA `[Decision]` — where `first_victory_recorded` lives without breaking 8.7's migration matrix** — 8.5's `first_death_recorded` got a FREE ride because 8.3 PRE-RESERVED its home; NO home was pre-reserved for `first_victory_recorded`. RECOMMENDED: add `first_victory_recorded: bool = false` as a NEW additive field at `SCHEMA_VERSION == 1` (KEEP the schema at 1 — the 8.4/8.5 "merge into the shape WITHOUT a bump" discipline), lenient-parse it (a pre-9.4 dict → `false` default), add it to `DICTIONARY_KEYS` + `to_dictionary()` + `parse()` + `copy()`, and UPDATE the `test_profile_snapshot.gd` `DICTIONARY_KEYS` pin (it asserts the EXACT key set — a new key REQUIRES updating the pin, but this is NOT a schema bump). **Do NOT bump `SCHEMA_VERSION` to 2** — 8.7's migration matrix asserts `SCHEMA_VERSION == 1` (test 2.5) AND that `parse({schema_version: 2})` is `unsupported_profile_schema` (test 2.2); a bump to 2 would BREAK both and force a real `migrate()` step (out of scope + 9.5/later). Adding a lenient field at v1 keeps 8.7's tests green (a pre-9.4 v1 profile still parses; the new field defaults false). **Record the reconciliation.** [Source: `godot/scripts/save/snapshots/profile_snapshot.gd:44-54,56,72,88,118,150` (the exact-key set + `SCHEMA_VERSION == 1` + the `first_death_recorded` sibling pattern); `godot/tests/unit/save/test_profile_snapshot.gd:53-59,241-272` (the `DICTIONARY_KEYS` pin + the `_set_first_death_flag_round_trips_without_a_migration` twin); `_bmad-output/implementation-artifacts/8-7-...md` tasks 2.2/2.5 (the migration matrix pins `SCHEMA_VERSION == 1` + `schema_version: 2` → unsupported)]
9. **Address the two 8.1 review defers RE-CARRIED to 9.4 (the completion/victory path is their owning story)** — these were parked "address both together if ever" / "if a future consumer builds from untrusted input" and RE-CARRIED to 9.4 at 8.1/8.2/8.3/9.3:
   - **(a) `CompleteRunCommand._resolve_completed` two-step transition atomicity** (`complete_run_command.gd:194-231`) — the completion path does `ACTIVE_ROUTE → NODE_RESOLUTION` THEN `NODE_RESOLUTION → COMPLETED` as two `transition_to` steps; if step 1 succeeded but step 2 failed, the run is left MUTATED in `NODE_RESOLUTION` (non-terminal) with a `wrong_run_phase` error, VIOLATING the command's "byte-identical no-mutation RunState on ANY reject" promise. 9.4 now drives this path for the boss VICTORY (from `ACTIVE_ROUTE`, the two-step runs). **Address it:** capture the phase BEFORE step 1 and restore on a step-2 failure, OR assert both edges' legality up front before any mutation. It is currently UNREACHABLE (both edges are always legal from the checked phases), so this is a defensive-correctness fix, not a live bug — record whether you fixed it or explicitly re-deferred with a rationale. [Source: `_bmad-output/implementation-artifacts/deferred-work.md:514-522` (the 8.1 defer verbatim)]
   - **(b) `RunEndOutcome.for_failed` / `for_completed` outcome allowlist validation** (`run_end_outcome.gd:78-102`) — the read DTO projects whatever `cause`/`outcome` it is handed WITHOUT validating against the `RUN_FAILED_CAUSES` / `run_completed` outcome allowlists, so `for_completed(run, &"garbage")` would surface `outcome_or_cause: "garbage"` whereas the `run_completed` EVENT rejects it. 9.4 calls `for_completed(run, victory)` (the FIRST real non-`boss_placeholder`/`completed` outcome through this DTO). **Address it:** add an allowlist guard (`victory` must now be in the accepted set alongside `boss_placeholder`/`completed`; a non-allowlisted outcome falls back to `_empty()` or is rejected). It is UNREACHABLE in the live flow (the DTO only ever receives an already-command-validated marker), so this is defensive-correctness — record whether you fixed it or re-deferred. [Source: `_bmad-output/implementation-artifacts/deferred-work.md:524-532` (the 8.1 defer verbatim); `godot/scripts/run/run_end_outcome.gd:78-102`]
10. **Full unit tests (all ACs)** — proving: the boss reaching 0 HP emits a `boss_defeated` event + resolves the run to `PHASE_COMPLETED` with `run_completed` + `outcome == victory` + `next_destination == outpost` (AC1); the run transitions to the post-victory return flow (the `RunEndOutcome.for_completed(run, victory)` projection + the captured destination) (AC1); a first victory flips `first_victory_recorded` true + emits `first_victory_recorded` + a subsequent victory rejects `first_victory_already_recorded` with ZERO mutation/event (AC2 once-only); a FAILED run rejects `run_not_completed` (the discriminator vs 8.5) (AC2 gate); the `first_victory_recorded` latch round-trips through `ProfileSnapshot.to_dictionary()`/`parse()` + a JSON round-trip at `SCHEMA_VERSION == 1` (NO migration; a pre-9.4 dict → false) (AC2 persistence); the `FirstVictoryRevealBeat` projects the FR62 line + is skippable + a skip mutates NOTHING (a pure read; AC3); the run SUMMARY + outpost RETURN are COMPLETE without the reveal (AC3 off-critical-path — assert the summary/outpost surface is unchanged by the beat's presence/absence); the `victory` outcome now VALIDATES on `run_completed` (+ a garbage outcome still rejects); the sequence-id seam threads a shared counter across the interleaved streams (no duplicate ids — closes the 9.3 Med); the two 8.1 defers are addressed (or re-deferred with a recorded rationale). Manual-seed first victory STILL latches (Option A). [Source: `godot/tests/unit/core/` (event + command tests), `godot/tests/unit/run/` or `godot/tests/unit/save/` (the profile/beat/outcome tests), `godot/tests/unit/tactical/` (the boss-defeat seam); the 8.5 `test_record_first_death_command.gd` / `test_first_death_narrative_beat.gd` (the exact twins to mirror)]

## OUT of scope (explicitly later stories / NOT this story — do NOT pull forward)

- **Finale REGRESSION seed tests + run-length TUNING hooks + boss tuning diagnostics (turn count / damage taken / major telegraphs / outcome) + the full-run-through-the-shell integration** → **Story 9.5 ("Finale Regression and Run-Length Tuning Hooks").** 9.4 adds its OWN victory/defeat/reveal determinism tests; the comprehensive approved-boss-seed regression suite (setup/phases/telegraphs/victory/defeat all deterministic, failures report seed+phase), the full run reaching the boss THROUGH the run shell to victory/death with the summary recording boss progress, and the local/offline tuning diagnostics are 9.5. If 9.4 needs a full-run scaffold for a victory test, keep it a focused explicit-turn fixture — NOT the comprehensive 9.5 seed matrix. [Source: `_bmad-output/planning-artifacts/epics.md` Story 9.5, lines 2314-2335]
- **Re-opening the 9.1 encounter SETUP, the 9.2 definition/phase MODEL, or the 9.3 boss FIGHT behavior** — `BossNodeEnterCommand` / `BossEncounterRequest` / `BossArenaBuilder` / `RunOrchestrator._resolve_boss` / the `boss_encounter_started` event / the arena `boss_slot` marker (9.1, done); the `BossDefinition` / `BossPhaseDefinition` / `BossActionDefinition` / `BossRepository` / `BossPhaseResolver` / `BossPhaseTransition` / the `boss_phase_changed` event (9.2, done); the `BossAi` / `BossCommandAdapter` / `BossTurnResolver` / the two-turn telegraph flow / the live phase re-resolution seam (9.3, done) are DONE + reviewed. 9.4 CONSUMES them: it drives the 9.3 boss fight to 0 HP (by explicit test turns / player damage) and detects the defeat. The ONLY 9.3-code change 9.4 makes is the victory-detection seam on the boss turn resolver (emit `boss_defeated` when `boss.is_dead()` after damage) + honoring the sequence-id seam contract — do NOT change the AI scoring, the adapter action mapping, the telegraph kinds, the phase resolver, or the `boss_phase_changed` contract. [Source: `godot/scripts/ai/boss_ai.gd`, `godot/scripts/tactical/turns/boss_command_adapter.gd`, `godot/scripts/tactical/turns/boss_turn_resolver.gd` (all 9.3, consumed; only the victory-detection seam added)]
- **Auto-playing the ENTIRE run-to-completion loop from `run_started` through a live tactical fight to victory with no test seams / a live combat-DEATH (hero-death) call site** — `RunOrchestrator.run_to_completion` still SETS UP the boss and STOPS (`_resolve_boss` is setup-only since 9.1; combat auto-resolves for non-boss nodes). 9.4 wires the boss-defeat → run-VICTORY call site (the victory half of the long-deferred run-end auto-wire), driven by explicit turns + a caller-driven victory resolution — it does NOT auto-play the full fight inside `run_to_completion`, and it does NOT add the hero-DEATH → `PHASE_FAILED` call site (a live player death path; cause `hero_death`/`boss_defeat` already in `RUN_FAILED_CAUSES`) — the DEATH-path live source stays deferred to a later run-flow/HUD story (the 8.1/8.3/8.5 "LIVE combat-death CALL SITE" defer stays OPEN for the death direction). Record the boundary: 9.4 owns the VICTORY call site; the hero-death live source is still deferred. [Source: `godot/scripts/run/run_orchestrator.gd:730-740` (the caller-driven `resolve_run_end`, "9.4's boss VICTORY drives it"); `_bmad-output/implementation-artifacts/deferred-work.md:110-113,319-322` (the LIVE combat-death CALL SITE defer — the death direction stays open)]
- **The generic rules-kernel engine (`RuleCondition`/`RuleTarget`/`RuleOperation`, stacking/conflict/duration, combat HOOK sites)** — v0 rules kernel is registration + explanation only (`scripts/rules/conditions/` EMPTY, `scripts/rules/operations/` holds EXACTLY ONE file). The boss-defeat/victory/reveal are recorded as EVENTS + a run-end transition + a latch, NOT a generic `RuleOperation`. Do NOT build the generic engine or touch `scripts/rules/conditions/` / `scripts/rules/operations/`. [Source: `project-context.md` "Keep `scripts/rules/conditions/` EMPTY … `scripts/rules/operations/` holds EXACTLY ONE file"]
- **A NEW top-level `RunSnapshot` key / persisting the boss's live phase state / an in-encounter resume save** — the 23-key `RunSnapshot` gate stays 23. The first-victory flag lives on the PROFILE (`ProfileSnapshot`, the cross-run save), NOT the `RunSnapshot` (the run autosave) — exactly as 8.5's first-death flag lives on the profile. 9.4 adds NO `RunSnapshot` key and no in-encounter save; the boss's phase state is still recomputed from HP (9.3's posture). [Source: `project-context.md` "the pinned 23-key `RunSnapshot` gate"; `godot/scripts/save/snapshots/profile_snapshot.gd` (the first-victory flag's home is the PROFILE)]
- **A new named RNG stream / seeding the victory/reveal** — the 7 named streams (`RngStreamSet.required_streams()`) are INVARIANT. The boss-defeat/victory/reveal draw ZERO RNG (deterministic records — the 8.3/8.5 deterministic-shell posture); no `randi()`/`randf()`/fresh `RandomNumberGenerator`. [Source: `project-context.md` named-RNG-stream rules; `godot/scripts/core/commands/record_first_death_command.gd` (ZERO RNG)]
- **The narrative CONTENT roster / localization pipeline / a codex** — v0 has EXACTLY ONE first-victory line (a single const on `FirstVictoryRevealBeat` + the by-id line id + the event) — NOT a `NarrativeRepository` / codex / JSON-or-`.tres` pipeline (the epic-wide by-id + no-content-pipeline defer, 8.5 precedent). A localization pass later replaces the resolved prose without touching the event/flag/command (all by-id). [Source: `godot/scripts/run/first_death_narrative_beat.gd:58-68` (the single-line const + `LINE_BY_ID`); `_bmad-output/implementation-artifacts/deferred-work.md:114-117` (the narrative-content/localization defer)]
- **The unlock-SPEND tree / meta-power APPLICATION / the Oath-Shard EARNED-count summary wiring** — 9.4 records the FIRST-VICTORY narrative latch; it awards NO Oath Shards, merges NO discoveries, and turns no unlock-progress into a playable-class unlock (classes stay static content in v0). The award/merge for an eligible VICTORY are the EXISTING 8.3/8.4 commands the run-flow layer already owns (an eligible completed run awards + merges via `AwardMetaProgressCommand`/`MergeRunDiscoveriesCommand`) — 9.4 does NOT re-implement them, and a boss VICTORY is just a `PHASE_COMPLETED` terminal run those commands already accept. Record that 9.4 does not re-award/re-merge; it adds the first-victory latch as the fourth independent run-end marker. [Source: `_bmad-output/implementation-artifacts/deferred-work.md:27-28` (unlock-spend + Oath-Shard-summary defers); `godot/scripts/core/commands/award_meta_progress_command.gd` (the eligible-completion award 9.4 does not touch)]
- **NOT overlapping (leave parked — do NOT reopen):** the two 9.1 review defers (the boss-arena board-snapshot REJECTION branch + the `BossNodeEnterCommand` ZERO-RNG tautological test) are 9.1 SETUP-layer coverage gaps — do NOT reopen; 9.4 does not touch `BossArenaBuilder`/`BossNodeEnterCommand`. The 9.3 review Low findings (the fixture-reads-the-slot-from-payload + the ZERO-RNG determinism guard) were RESOLVED in 9.3. The affinity HUD/VFX + seated-Cursed-rule re-derive + Darkness-fairness fixtures are AFFINITY concerns. The constant-8-tier route-depth + int64 economy ceiling are Epic-10. The board-snapshot cell-coercion defer is Story-1.3 combat. None overlap 9.4's victory/reveal area EXCEPT the two 8.1-earmarked defers (IN scope, item 9) + the 9.3 sequence-id seam (IN scope). [Source: `_bmad-output/implementation-artifacts/deferred-work.md` passim; `_bmad-output/implementation-artifacts/9-3-...md` OUT-of-scope "NOT overlapping"]

If an AC seems to demand the full auto-played run loop or the death-path live source, re-read: AC1 demands "a boss-defeated event and run-victory event are emitted" + "the run transitions to post-victory return flow" — the `boss_defeated` event + the `run_completed`+`victory` resolution + `next_destination == outpost`, driven by the boss reaching 0 HP (explicit turns) + the caller-driven `resolve_run_end`, satisfies this WITHOUT auto-playing the whole fight or the hero-death path. AC2 demands the reveal "can show" the line + "is tracked so first-victory state is persisted" — the `FirstVictoryRevealBeat` (the line) + the `first_victory_recorded` latch on the persisted `ProfileSnapshot` + the `first_victory_recorded` event satisfies this. AC3 demands "the run summary and outpost return still occur" + "skipping does not alter rewards or progression" — the pure read-DTO reveal decoupled from the flag mutation + the summary/outpost being complete without it satisfies this STRUCTURALLY. NONE of the three ACs demands the finale regression suite (9.5), the full-run-through-the-shell integration (9.5), or the hero-death live source (a later run-flow story).

## Tasks / Subtasks

- [x] **Task 1 — The boss-DEFEATED tactical event + the zero-HP victory-detection seam (AC1)**
  - [x] Add `DomainEvent.Type.BOSS_DEFEATED` at the enum END (after any first-victory member added — see Task 5's enum-ordering note), `EVENT_ID_BOSS_DEFEATED := &"boss_defeated"`, a SYSTEM event (no actor). Payload (RECOMMENDED — record the exact shape): `{boss_entity_id (lower_snake), defeated_at_phase_index (non-negative integral) or phase_id (lower_snake), final_hp (non-negative integral, == 0)}`. Wire it end-to-end: factory + `_validate_boss_defeated_payload` + `validate_payload` dispatch + both id maps (`id_for_type`/`type_for_id`) + JSON round-trip test + per-field malformed tests + UPDATE the `expected_ids` exhaustiveness pin (fails-loud until added). **DONE — chose `phase_id` (lower_snake, e.g. `desperation`) over `defeated_at_phase_index` (survives a phase-model change + human-readable). Enum order: `... BOSS_PHASE_CHANGED, FIRST_VICTORY_RECORDED, BOSS_DEFEATED`.**
  - [x] Add the victory-detection seam: after a damaging event drops the boss's HP (in 9.4 the damage-to-boss source is the player/test, applied to the board), detect `boss.is_dead()` (`current_hp <= 0`) and emit `boss_defeated`. RECOMMENDED site: a `BossTurnResolver` method (or a small `detect_boss_defeat(context)` seam) — record the exact site. It is the ONLY 9.3-code change (do NOT alter AI/adapter/telegraph/phase behavior). **DONE — `BossTurnResolver.detect_boss_defeat(context, sequence_id_base := -1)` (the ONLY 9.3-code change; honors the sequence-id seam contract).**
  - [x] Do NOT overload `level_victory_reached`/`level_defeat_reached` (Epic-1 tactical level outcomes keyed to the player) — the boss defeat is a distinct NAMED event. **DONE — `boss_defeated` is a new distinct named event.**
  - [x] Tests: a `damage_applied` dropping the boss to 0 HP → `boss_defeated` emitted naming the boss + the final phase; a non-lethal hit emits NO `boss_defeated`; the event round-trips (surviving typed fields, not nested re-stringify — the int→float footgun). **DONE — `test_boss_turn_resolver.gd` + `test_domain_event.gd`.**

- [x] **Task 2 — The run-VICTORY resolution: `run_completed` + `victory` outcome (AC1)**
  - [x] Add `RUN_COMPLETED_OUTCOME_VICTORY := &"victory"` to `domain_event.gd` (near `RUN_COMPLETED_OUTCOME_COMPLETED`/`_BOSS_PLACEHOLDER`) and add it to `_validate_run_completed_payload`'s outcome allowlist (the `boss_placeholder`/`completed` values UNTOUCHED). `boss_node_id` is tolerated absent for `victory` (like `completed`); `next_destination == outpost` still required. **DONE.**
  - [x] Drive the run to `PHASE_COMPLETED` via `CompleteRunCommand`/`RunOrchestrator.resolve_run_end` with `victory`. Decide + record: `victory` classifies as a COMPLETION marker in `CompleteRunCommand` (RECOMMENDED — extend the completion classifier `_is_completion_outcome`/the `COMPLETION_OUTCOME` check to accept `victory` alongside `completed`; both run `_resolve_completed`) vs a dedicated path. Emit `run_completed` + `outcome == victory` + `next_destination == outpost`. **DONE — [Decision] `victory` is a COMPLETION marker (added `COMPLETION_OUTCOMES` + `_is_completion_outcome`; both run `_resolve_completed`, which now emits the ACTUAL outcome). `resolve_run_end(victory)` works with NO orchestrator change.**
  - [x] UPDATE the existing `run_completed` test that REJECTS a stray `victory` outcome → it must now ACCEPT `victory`; introduce a NEW garbage outcome (e.g. `not_a_real_outcome`) as the rejection guard so the allowlist stays enforced. **DONE — updated `_run_completed_rejects_broadened_malformed_payloads` + `_run_completed_rejects_malformed_payloads` (both now use `not_a_real_outcome`).**
  - [x] Tests: the boss victory drives `run_completed` + `outcome == victory` + `next_destination == outpost`; a re-resolution of the already-terminal run is `run_already_terminal` (AC3 no-double-grant via `CompleteRunCommand`); `victory` VALIDATES on `run_completed` + a garbage outcome rejects. **DONE — `test_complete_run_command.gd` + `test_domain_event.gd` + `test_run_orchestrator.gd`.**

- [x] **Task 3 — The post-victory RETURN transition (AC1) — reuse the 8.1 flow signal**
  - [x] The run-victory carries `next_destination == outpost` (existing `RUN_END_DESTINATION_OUTPOST`, FR32); the `RunEndOutcome.for_completed(run, victory)` projection surfaces the run-END fact (phase COMPLETED, `outcome_or_cause == victory`, destination outpost). The orchestrator captures `run_end_destination`. **DONE.**
  - [x] Do NOT build a new destination marker or a scene transition (UI-scene-last — the boot/app-flow navigation is a later story; 8.6 built the `OutpostViewModel` DATA the navigation lands on). **DONE — no new marker/scene.**
  - [x] Tests: `RunEndOutcome.for_completed(run, victory)` projects `has_ended == true`, `phase == completed`, `outcome_or_cause == victory`, `next_destination == outpost`; the orchestrator surfaces the captured destination after `resolve_run_end(victory)`. **DONE — `test_run_end_outcome.gd` + `test_run_orchestrator.gd`.**

- [x] **Task 4 — The first-VICTORY latch + `RecordFirstVictoryCommand` (AC2) — mirror `RecordFirstDeathCommand`**
  - [x] Add `ProfileSnapshot.first_victory_recorded: bool = false` (see Task 8 — the SCHEMA `[Decision]`: additive at `SCHEMA_VERSION == 1`, add to `DICTIONARY_KEYS`/`to_dictionary()`/`parse()`/`copy()`, lenient-parse, KEEP schema 1). **DONE.**
  - [x] Build `RecordFirstVictoryCommand` (the `RecordFirstDeathCommand` analogue): constructor `(profile, sequence_id)`, `state` = the live TERMINAL `RunState`. Gate order: sequence-id > 0 → context (a terminal RunState + a profile) → `run.is_terminal()` (`run_not_terminal`) → **VICTORY-only** `run.phase == PHASE_COMPLETED` (`run_not_completed` — the discriminator vs 8.5's `run_not_failed`) → once-only `not profile.first_victory_recorded` (`first_victory_already_recorded`). On success: flip the latch, emit `first_victory_recorded`, return the fact + beat data in metadata. ZERO events + byte-identical run + profile on ANY reject. ZERO RNG. **DONE.**
  - [x] Eligibility-INDEPENDENT (mirror 8.5 Option A — NO `meta_progression_eligible` gate; a manual-seed first victory STILL latches — the line is narrative flavor, not meta progression). Record the `[Decision]`. **DONE — [Decision] Option A recorded (class doc + Completion Notes).**
  - [x] Tests: a first victory flips the latch + emits the event; a SECOND victory rejects `first_victory_already_recorded` (ZERO mutation/event); a FAILED run rejects `run_not_completed`; a non-terminal run rejects `run_not_terminal`; sequence-id <= 0 rejects FIRST; a manual-seed first victory STILL latches (Option A); the profile is byte-identical on every reject. **DONE — `test_record_first_victory_command.gd`.**

- [x] **Task 5 — The `first_victory_recorded` event (AC2) — the `first_death_recorded` twin**
  - [x] Add `DomainEvent.Type.FIRST_VICTORY_RECORDED` at the enum END (never renumber the existing members). ENUM-ORDERING NOTE: append the new members in a stable order and pin them ALL in `expected_ids`; a recommended order is `... BOSS_PHASE_CHANGED, FIRST_VICTORY_RECORDED, BOSS_DEFEATED` (or whatever you choose — the ONLY hard rule is append-at-end + never renumber + every non-UNKNOWN member pinned). `EVENT_ID_FIRST_VICTORY_RECORDED := &"first_victory_recorded"`, `FIRST_VICTORY_LINE_ID := &"first_victory"`. **DONE — order `... BOSS_PHASE_CHANGED, FIRST_VICTORY_RECORDED, BOSS_DEFEATED`.**
  - [x] SYSTEM event (no actor), payload `{line_id, is_skippable, profile_id}` (mirroring `first_death_recorded`); `_validate_first_victory_recorded_payload` reuses the same field rules (`line_id` lower_snake, `is_skippable` bool, `profile_id` plain string); wire `validate_payload` dispatch + both id maps + JSON round-trip + per-field malformed tests; UPDATE the `expected_ids` pin + the `Type.size()-1` tie. **DONE — the `Type.size()-1` tie holds (both new members pinned).**
  - [x] Tests: `first_victory_recorded` round-trips (line_id/is_skippable/profile_id survive a JSON round-trip); `id_for_type`/`type_for_id` map it; malformed payloads reject per-field; the `expected_ids` pin covers every new member. **DONE — `test_domain_event.gd`.**

- [x] **Task 6 — The `FirstVictoryRevealBeat` DTO (AC2/AC3) — the `FirstDeathNarrativeBeat` twin**
  - [x] A scene-free PURE-READ DTO: `has_beat`/`line_id`/`line`/`is_skippable` (exact-key `DICTIONARY_KEYS`), `for_first_victory(line_id := FIRST_VICTORY_LINE_ID, is_skippable := true)` + `from_event(event)` factories, `FIRST_VICTORY_LINE := "It did not die. It learned the way back."` + `LINE_BY_ID := {"first_victory": FIRST_VICTORY_LINE}`, the fail-closed empty beat. ZERO RNG, no command, no event, no mutation. **DONE — `scripts/run/first_victory_reveal_beat.gd`.**
  - [x] Tests: `for_first_victory()` projects the FR62 line + `is_skippable == true` + `has_beat == true`; `from_event(first_victory_recorded_event)` projects the same; an unknown/blank line id → the fail-closed empty beat; `to_dictionary()` is exact-key; repeated builds are byte-identical (pure); a "skip" (not rendering) mutates NOTHING. **DONE — `test_first_victory_reveal_beat.gd` (incl. a cross-event guard: `from_event(first_death_recorded)` → empty).**

- [x] **Task 7 — AC3: the reveal is OFF the critical path (skippable, alters no rewards/progression)**
  - [x] Add NO reveal field to `RunSummary`; do NOT make the summary or the outpost/start-another-descent path depend on the beat. The flag MUTATION (`RecordFirstVictoryCommand`) is decoupled from the beat DELIVERY (the DTO) — a skip cannot mutate the flag (there is no skip command). **DONE — `RunSummary` untouched (asserted in the beat test).**
  - [x] Decide + record: surface the reveal beat on the `OutpostViewModel` alongside the first-death beat as an OPTIONAL sub-surface (RECOMMENDED — a small additive read, mirroring the first-death beat wiring) OR leave the outpost render to a later UI story and ship only the DTO + flag + event + tests (the 8.5 "render is 8.6" split). If you touch `OutpostViewModel`, keep it additive + exact-key + fail-closed (its own `has_beat` gate). **DONE — [Decision] the 8.5 "render is a later UI story" split: shipped ONLY the DTO + flag + event + tests; `OutpostViewModel` UNTOUCHED (lower-risk; recorded in Completion Notes + the deferred-work ledger).**
  - [x] Tests: the `RunSummary` is byte-identical whether or not a first-victory occurred (no reveal field); the outpost surface is complete without the reveal (if `OutpostViewModel` is touched, the reveal rides as an OPTIONAL sub-dict with its own `has_beat` gate + the existing keys are unchanged); a skip alters no rewards/progression (structurally — the flag + award/merge are decoupled from the beat). **DONE — `test_first_victory_reveal_beat.gd` (`_beat_is_independent_of_run_summary` + `_build_read_dismiss_is_a_structural_no_op`).**

- [x] **Task 8 — The SCHEMA `[Decision]` + the sequence-id seam + the two 8.1 defers (AC1/AC2)**
  - [x] SCHEMA `[Decision]`: add `first_victory_recorded` as a NEW additive field at `SCHEMA_VERSION == 1` (KEEP schema 1 — the 8.4/8.5 merge-without-bump discipline); UPDATE `test_profile_snapshot.gd`'s `DICTIONARY_KEYS` pin (a new key requires updating the pin — NOT a schema bump) + add a `_set_first_victory_flag_round_trips_without_a_migration` twin. Do NOT bump `SCHEMA_VERSION` to 2 (8.7's matrix pins `SCHEMA_VERSION == 1` + `schema_version: 2` → `unsupported_profile_schema`; a bump breaks both). Record the reconciliation. **DONE — [Decision] additive at v1; reconciliation recorded (a lenient additive field at v1 keeps both 8.7 pins green).**
  - [x] SEQUENCE-ID SEAM: 9.4 interleaves the boss action events + `boss_phase_changed` events + `boss_defeated` + `run_completed` into ONE ordered log. Pass an explicit `sequence_id_base >= 0` to `BossTurnResolver.resolve_phase_transitions` (from the shared run-level `_next_sequence_id`) and thread the returned `next_sequence_id_after` — NEVER the board-baseline fallback when merging. Record how you threaded the shared counter. Test: no duplicate sequence ids across the merged stream (closes the 9.3 Round-1 Med). **DONE — `detect_boss_defeat` mirrors the seam contract (base >= 0 + `next_sequence_id_after`); a merging caller threads the shared counter through `resolve_phase_transitions` → `detect_boss_defeat` → the next append. Tested (`_sequence_id_seam_threads_a_shared_counter_across_interleaved_streams` — no duplicate ids).**
  - [x] 8.1 DEFER (a): `CompleteRunCommand._resolve_completed` two-step atomicity — capture the phase before step 1 + restore on a step-2 failure, OR assert both edges up front. Record fixed-or-re-deferred + rationale (currently unreachable). Test the restore path if fixed. **DONE — FIXED (capture `phase_before` + restore `run.phase` on a step-2 failure; still unreachable → defensive). Marked `[Resolved 9.4]` in the ledger. Tested the observable invariant (`_two_step_completion_is_atomic_on_a_hypothetical_step_two_failure`).**
  - [x] 8.1 DEFER (b): `RunEndOutcome.for_failed`/`for_completed` allowlist validation — add an allowlist guard (`victory` now accepted; a non-allowlisted outcome → `_empty()` or reject). Record fixed-or-re-deferred + rationale. Test that `for_completed(run, victory)` projects the fact + a garbage outcome falls back to empty (if fixed). **DONE — FIXED (allowlist guard: `COMPLETED_OUTCOMES` + `RUN_FAILED_CAUSES`; a non-allowlisted marker → `_empty()`). Marked `[Resolved 9.4]`. Tested (`_non_allowlisted_outcome_or_cause_falls_back_to_empty`).**

- [x] **Task 9 — Cross-checks, suite, and hygiene (all ACs)**
  - [x] Cross-check: the boss victory drives the run to COMPLETED through the SAME `run_completed` boundary the 4.5/8.1 contract reserved (the `boss_placeholder`/`completed`/`victory` allowlist); the first-victory latch lives on the PROFILE (cross-run save), NOT the 23-key `RunSnapshot`. **DONE — verified.**
  - [x] Confirm 9.4 touches NONE of: the 9.1 setup, the 9.2 definition/phase model, the 9.3 AI/adapter/telegraph/phase behavior (EXCEPT the boss-defeat detection seam), the route model, the boss node type, the 23-key `RunSnapshot` gate, `scripts/rules/conditions/` (EMPTY), `scripts/rules/operations/` (single file), any seed-regression fingerprint, or any `.tres`/JSON content file. Confirm `SCHEMA_VERSION` stays 1. **DONE — verified (the ONLY 9.3-code change is `detect_boss_defeat`; `SCHEMA_VERSION` stays 1; no content/fingerprint touched).**
  - [x] Run the full headless suite; apply the false-PASS grep guard; `git diff --check`; verify `git status --short` clean of stray temp/generated files (never remove planning/story artifacts). **DONE — 163 PASS / 0 `^FAIL`, "Headless tests passed."; false-PASS grep clean (only the documented negatives); `git diff --check` clean; no stray temp/generated files.**

## Dev Notes

### The run-END + boss-VICTORY machinery 9.4 consumes/extends — what exists today (READ these before coding)

- **`CompleteRunCommand` (`scripts/core/commands/complete_run_command.gd`)** — the outcome-parameterized run-END command (8.1): the caller supplies an `outcome` that is EITHER a death CAUSE (in `RUN_FAILED_CAUSES` → `PHASE_FAILED` + `run_failed`) OR the completion MARKER `COMPLETION_OUTCOME == RUN_COMPLETED_OUTCOME_COMPLETED` (→ `PHASE_COMPLETED` + `run_completed`). The completion path (`_resolve_completed`, `:194-231`) runs `ACTIVE_ROUTE → NODE_RESOLUTION → COMPLETED` (the boss two-step) from `ACTIVE_ROUTE`, or a single step from `NODE_RESOLUTION`. AC3: a re-resolution of an already-terminal run is `run_already_terminal` (ZERO second event + ZERO mutation). **9.4 adds `victory` as a second COMPLETION marker** (extend the classifier at `:120-135` / `_resolve_completed`, add `RUN_COMPLETED_OUTCOME_VICTORY` — do NOT touch the death path). **The `_resolve_completed` two-step atomicity defer (`:194-231`) is the 8.1 Low #1 9.4 owns (Task 8).**
- **`RunEndOutcome` (`scripts/run/run_end_outcome.gd`)** — the pure-read run-END DTO (8.1): `for_failed(run, cause)` / `for_completed(run, outcome)` project `{has_ended, phase, outcome_or_cause, next_destination, meta_progression_eligible}` off a TERMINAL run. `for_completed` requires `run.phase == PHASE_COMPLETED`, else the fail-closed empty fact. **9.4 calls `for_completed(run, victory)` — the FIRST real non-`boss_placeholder`/`completed` outcome through it.** **The missing allowlist validation (`:78-102`) is the 8.1 Low #2 9.4 owns (Task 8).**
- **`RunState` (`scripts/run/run_state.gd`)** — the run phase machine: `PHASE_NEW_RUN`/`PHASE_ACTIVE_ROUTE`/`PHASE_NODE_RESOLUTION`/`PHASE_COMPLETED`/`PHASE_FAILED`, `is_terminal()` (COMPLETED or FAILED), `can_transition_to`/`transition_to`, `_legal_next_phases` (`:550-565` — `ACTIVE_ROUTE → {NODE_RESOLUTION, FAILED}`, `NODE_RESOLUTION → {ACTIVE_ROUTE, COMPLETED, FAILED}`, COMPLETED/FAILED terminal). 9.4 drives it to COMPLETED via `CompleteRunCommand` (NO new edge — the boss two-step reuses the existing edges). The 23-key `RunSnapshot` gate stays 23 (the first-victory flag is on the PROFILE, not here).
- **`RunOrchestrator.resolve_run_end` (`scripts/run/run_orchestrator.gd:741-758`)** — the CALLER-DRIVEN run-END hook (8.1): dispatches `CompleteRunCommand.new(outcome, _next_sequence_id).execute(run)`, advances the shared `_next_sequence_id` past the event, and captures the run_failed/run_completed event + cause/outcome + `_run_end_destination`. Its docstring (`:730-740`) EXPLICITLY earmarks 9.4: "9.4's boss VICTORY drives it (via CompleteRunCommand) from the boss-setup terminus." NOT auto-wired into `run_to_completion` (no live death source; the VICTORY is caller-driven). `_resolve_boss` (`:885-910`) is setup-only (parks non-terminal in NODE_RESOLUTION, `boss_encounter_pending = true`). `_advance_sequence_past` (`:913-921`) is the shared monotonic run-level counter 9.4 threads through the sequence-id seam.
- **`NodeResolvePlaceholderCommand` (`scripts/core/commands/node_resolve_placeholder_command.gd`)** — the 4.5 boss placeholder: its boss branch emits `node_placeholder_resolved` + `run_completed` (`outcome == boss_placeholder`, `boss_node_id`). RETAINED UNCHANGED for 9.4 to reuse the run-completed boundary — but 9.4's REAL victory drives `CompleteRunCommand` with `victory` (the generic completion path), NOT this boss-placeholder branch (which is no longer the live boss dispatch since 9.1). `BOSS_OUTCOME == RUN_COMPLETED_OUTCOME_BOSS_PLACEHOLDER`. Do NOT change this command.
- **`BossTurnResolver` (`scripts/tactical/turns/boss_turn_resolver.gd`)** — the 9.3 boss turn driver: `resolve_boss_turn(context)` (simulate-then-apply the boss action) + `resolve_phase_transitions(context, previous_phase_index, sequence_id_base := -1)` (the live phase re-resolution seam, `:129-160`). **The SEQUENCE-ID SEAM CONTRACT (`:119-128`) is the 9.3 Med finding 9.4 owns**: pass a caller-reserved `sequence_id_base >= 0` + thread `next_sequence_id_after` when interleaving streams. `active_phase_index_for_hp(current_hp)` recomputes the phase (no stored state). **9.4 adds the boss-defeat detection seam here** (emit `boss_defeated` when `boss.is_dead()` after damage) — the ONLY 9.3-code change.
- **`TacticalEntityState` (`scripts/tactical/entities/tactical_entity_state.gd`)** — the boss board entity: `current_hp`/`max_hp`, `is_alive()` (`current_hp > 0`) / `is_dead()` (`not is_alive()`, `:45-50`). 9.4 reads `is_dead()` to detect the boss defeat. The boss `definition_id == "larval_avatar"` (the 9.1 slot / 9.2 definition `BOSS_ID`).

### The 8.5 first-DEATH precedent 9.4 MIRRORS at the OPPOSITE terminal phase (READ these — the exact twins to build)

- **`RecordFirstDeathCommand` (`scripts/core/commands/record_first_death_command.gd`)** — the run-end profile-mutation command 9.4's `RecordFirstVictoryCommand` mirrors VERBATIM: constructor `(profile, sequence_id)`, `state` = the live TERMINAL run; gate order sequence-id → context → `is_terminal()` (`run_not_terminal`) → **DEATH-only** `run.phase == PHASE_FAILED` (`run_not_failed`) → once-only `not profile.first_death_recorded` (`first_death_already_recorded`); flips `profile.first_death_recorded` true + emits `first_death_recorded`; ZERO events + byte-identical run + profile on reject; ZERO RNG; eligibility-INDEPENDENT (Option A — a manual-seed first death still latches). **9.4 swaps the DEATH-only gate for a VICTORY-only gate (`PHASE_COMPLETED` + `run_not_completed`) + the death latch for the victory latch — EVERYTHING else identical.** Its class doc (`:17-21`) even names the Epic-9 first-VICTORY reveal as the OPPOSITE-phase twin.
- **`FirstDeathNarrativeBeat` (`scripts/run/first_death_narrative_beat.gd`)** — the pure skippable read-DTO 9.4's `FirstVictoryRevealBeat` mirrors: `has_beat`/`line_id`/`line`/`is_skippable` (exact-key `DICTIONARY_KEYS`), `for_first_death(line_id, is_skippable)` + `from_event(event)`, `FIRST_DEATH_LINE := "Good. You remembered how to die."` + `LINE_BY_ID := {"first_death": FIRST_DEATH_LINE}`, the fail-closed empty beat. ZERO RNG/command/event/mutation — a skip is STRUCTURALLY a no-op. **9.4 swaps the line ("It did not die. It learned the way back.") + the by-id key (`first_victory`) — EVERYTHING else identical.** Its class doc (`:20-21`) names the FR62 first-victory line as Epic 9 (Story 9.4).
- **`ProfileSnapshot` (`scripts/save/snapshots/profile_snapshot.gd`)** — the versioned cross-run profile (8.3): `SCHEMA_VERSION == 1`, exact-key `DICTIONARY_KEYS` (`:44-54`), `to_dictionary()`/`parse()`/`copy()`/`fresh()`, lenient decode. It has a `first_death_recorded` field (`:72`, reserved by 8.3 for 8.5) but **NO `first_victory_recorded` home** — 9.4 ADDS one (Task 8 SCHEMA `[Decision]`: additive at v1, add to all four methods + the key pin, KEEP schema 1). `parse` rejects `schema_version != 1` with `unsupported_profile_schema` (`:97-103`).
- **The `first_death_recorded` EVENT (`scripts/core/events/domain_event.gd`)** — the full wiring 9.4's `first_victory_recorded` mirrors: enum member (`:43`, the tail region), `EVENT_ID_FIRST_DEATH_RECORDED` (`:84`), `FIRST_DEATH_LINE_ID := &"first_death"` (`:200`), factory `first_death_recorded(seq, payload)` (`:436-454`, normalizes `line_id`/`is_skippable`/`profile_id`), `_validate_first_death_recorded_payload` (`:1380-1395`, `line_id` lower_snake + `is_skippable` bool + `profile_id` string), `validate_payload` dispatch (`:1073-1074`), both id maps (`:2249-2250`/`:2331-2332`), the `expected_ids` pin (`test_domain_event.gd:2717-2719`). **The enum TAIL is `BOSS_PHASE_CHANGED` (index 45) — 9.4's new members append AFTER it (never renumber).**
- **`OutpostViewModel` (`scripts/ui/view_models/outpost_view_model.gd`)** — the outpost read surface (8.6): aggregates the profile meta (oath_shards/echoes/unlock_progress/class_mastery/`first_death_recorded`) + the run summary + the OPTIONAL `FirstDeathNarrativeBeat` sub-dict (`:73,90-95,184-193,228-230,310-313,358-363`). The first-death beat is OFF THE CRITICAL PATH (a null/absent beat never blocks the surface; its own `has_beat` gate). **9.4's victory reveal MAY join it as another OPTIONAL sub-surface (RECOMMENDED, additive + exact-key + fail-closed) OR be left to a later UI story (record which — Task 7).**

### Epic-9 retro-note constraints (from `_bmad-output/auto-gds/retro-notes/epic-9.md` — surfaced by prior stories in THIS epic; fold in, do not re-derive)

- **⚠️ THE SEQUENCE-ID SEAM (retro §9-3, Phase-7) IS NOW DUE.** `BossTurnResolver.resolve_phase_transitions` reads `board.next_sequence_id()` without advancing it — benign in 9.3 (non-board-applied system events) but the id space is SHARED with `resolve_boss_turn`'s board events. **9.4 is the "live run-to-completion loop merges both event streams into one append-only log" consumer the retro note flagged**: reserve/derive phase-change ids from the shared monotonic counter (the orchestrator's `_next_sequence_id`) to avoid duplicate sequence ids. The 9.3 review already built the contract (`resolve_phase_transitions(context, prev, sequence_id_base := -1)` returning `next_sequence_id_after`) — 9.4 MUST pass a reserved `sequence_id_base >= 0` + thread the returned cursor, NEVER the board-baseline fallback. [retro §9-3 Phase-7; `boss_turn_resolver.gd:119-160`] (Task 8.)
- **⚠️ JSON int→float coercion footgun (recurred in 9.1's arena payload; §9-1 Phase-5) — relevant to 9.4's `boss_defeated` / `run_completed` / `first_victory_recorded` round-trip tests.** Byte-identity re-`stringify` across a JSON boundary is IMPOSSIBLE for nested ints (JSON numbers are IEEE-754 doubles). For ANY event JSON round-trip test in 9.4, assert the SURVIVING typed fields after `parse_string` (`int(parsed.final_hp) == 0`, `String(parsed.outcome) == "victory"`, `String(parsed.line_id) == "first_victory"`), NEVER `JSON.stringify(a) == JSON.stringify(parse(stringify(a)))` on a nested payload. The `final_hp`/`from_phase`/counts are small (well under 2^53 — NO decimal-string encoding); the profile's `last_awarded_run_seed` (a full int64) is DECIMAL-STRING encoded already (8.3) — do NOT touch that. [retro §9-1 Phase-5; `project-context.md` "ALWAYS JSON-round-trip snapshots in tests … never assert only on native dictionaries"]
- **The `GenerationRequest` size/difficulty restriction (retro §9-1) is IRRELEVANT to 9.4** — 9.4 does NOT touch the level-request or arena-build layer at all (the boss arena is already built by 9.1; the boss is already live from 9.3). 9.4 acts on the run-END + profile + reveal layer.
- **9.2's boss-as-DEFINITION pivot → 9.3 made it a LIVE entity (retro §9-2) → 9.4 makes its DEFEAT a run VICTORY.** The boss became a real board entity in 9.3 (`max_hp = 36` satisfies `TacticalEntityState.validate()`); 9.4 detects that entity reaching 0 HP and resolves the victory. No new board-entity work. [retro §9-2 Phase-3]

### Deferred-work overlap (the ONLY ledger entries whose subject overlaps 9.4's area — fold in, do not copy the whole ledger)

- **[Review][Defer] (8.1 review Round 1, RE-CARRIED to 9.4) `CompleteRunCommand._resolve_completed` two-step transition atomicity** (`deferred-work.md:514-522`) — the completion path's `ACTIVE_ROUTE → NODE_RESOLUTION → COMPLETED` two-step can leave the run mutated in `NODE_RESOLUTION` on a step-2 failure, violating the "byte-identical no-mutation on ANY reject" promise. Parked "fixing only 8.1 would diverge from the boss pattern — address both together if ever"; RE-CARRIED to 9.4 at 8.2/8.3 ("the completion/victory path"). **9.4 OWNS it (Task 8/9a): the boss victory drives this exact path — fix the atomicity (capture+restore or up-front legality) or re-defer with a recorded rationale (it is currently unreachable — both edges are always legal from the checked phases).**
- **[Review][Defer] (8.1 review Round 1, RE-CARRIED to 9.4) `RunEndOutcome.for_failed`/`for_completed` outcome allowlist validation** (`deferred-work.md:524-532`) — the read DTO projects an unvalidated `cause`/`outcome` (a `for_completed(run, &"garbage")` would surface `outcome_or_cause: "garbage"` the event rejects). RE-CARRIED to 9.4. **9.4 OWNS it (Task 8/9b): 9.4 calls `for_completed(run, victory)` (the first real non-placeholder outcome through it) — add the allowlist guard (accept `victory` + fall back to `_empty()` for a non-allowlisted outcome) or re-defer with a rationale (unreachable in the live flow).**
- **[Defer] (Epic 9, Story 9.4) The first-VICTORY reveal** (`deferred-work.md:31,70,118-120`) — "It did not die. It learned the way back." (FR62), carried across 8.5/8.6/8.7/9.1/9.2/9.3, keys off the OPPOSITE terminal phase (COMPLETED) from 8.5's first-death latch (FAILED). **9.4 BUILDS it (the whole point of the story): the `first_victory_recorded` latch + event + the `FirstVictoryRevealBeat` DTO, mirroring the 8.5 death precedent at the COMPLETED phase.** Do NOT re-litigate FR28 — the latch is eligibility-independent narrative flavor (Option A).
- **[Defer] (later run-flow / HUD story) The LIVE combat-death CALL SITE + the run-end auto-wire** (`deferred-work.md:30,47,68,110-113,319-322`) — carried across Epic 8 + 9.1/9.2/9.3. **9.4 wires the VICTORY half** (boss-defeat → run-victory call site — the first live run-end auto-wire); the DEATH half (a live hero-death → `PHASE_FAILED` source) stays DEFERRED to a later run-flow/HUD story. Knowingly split: 9.4 owns the victory call site; the hero-death live source is still parked.
- **No other ledger entry overlaps 9.4's victory/reveal area** — the unlock-SPEND / meta-power APPLICATION + the Oath-Shard EARNED-count summary wiring are later stories (9.4 records the narrative latch, awards/merges nothing beyond the EXISTING 8.3/8.4 commands an eligible completed run already triggers); the 9.1/9.3 review defers are setup/resolved; the affinity/Darkness defers are affinity concerns; the constant-8-tier route-depth + int64 economy ceiling are Epic-10; the board-snapshot cell-coercion defer is Story-1.3 combat.

### Determinism, RNG & save invariants (hold ALL of these)

- **ZERO RNG in the victory/defeat/reveal path** — the boss-defeat detection, the run-victory resolution, the first-victory latch, and the reveal DTO are all DETERMINISTIC (a boss dying, a run completing, a latch flipping, a beat projecting are not rolls — the 8.3 `oath_shards_awarded` / 8.5 `first_death_recorded` deterministic-shell posture; no `roll`/`draw_index`). No `randi()`/`randf()`/fresh `RandomNumberGenerator`. The 7 named streams are INVARIANT — no new stream. Same terminal `(run, profile)` → the same events + latch + reveal.
- **Determinism (AC1/AC2/AC3):** the victory events + the latch + the reveal are pure functions of their inputs. The boss fight itself is 9.3's already-deterministic behavior (ZERO-RNG AI + the pending-telegraph flow); 9.4 drives it to 0 HP by explicit turns / player damage.
- **The first-victory flag is a PROFILE latch, NOT run save** — it lives on `ProfileSnapshot` (the cross-run `user://profile.json`), NOT the `RunSnapshot` (the run autosave). The 23-key `RunSnapshot` gate stays 23. The flag round-trips through the profile save at `SCHEMA_VERSION == 1` (NO bump — Task 8 SCHEMA `[Decision]`). ALWAYS JSON-round-trip the profile in tests (surviving typed fields, not native dicts / not nested re-stringify).
- **`SCHEMA_VERSION` stays 1** — the `first_victory_recorded` field is additive at v1 (the 8.4/8.5 merge-without-bump discipline). 8.7's migration matrix asserts `SCHEMA_VERSION == 1` + `schema_version: 2` → `unsupported_profile_schema`; a bump would break both. Update the `DICTIONARY_KEYS` pin (a new key, NOT a schema bump).
- **Append-only events, wired end-to-end** — the NEW `first_victory_recorded` + `boss_defeated` events append at the enum END (after `BOSS_PHASE_CHANGED`), NEVER renumbered, each wired factory + `_validate_*_payload` + `validate_payload` dispatch + both id maps + JSON round-trip + malformed tests + the `expected_ids` exhaustiveness pin (which FAILS a forgotten member BY DESIGN — register BOTH). The `run_completed` `victory` outcome is an ADDITIVE allowlist value (not a new event).
- **The `victory` outcome is the run-victory record** — do NOT invent a parallel `run_victory` event; the run-victory IS `run_completed` + `outcome == victory` (the 8.1 "Epic 9 reuses THIS boundary" contract). The `boss_defeated` event is the DISTINCT tactical fact AC1 also requires.
- **No seed-regression fingerprint moves** — 9.4 touches no generator/route/arena LAYOUT (the boss content + behavior + victory is orthogonal to layout). Every Small/Medium/route/arena fingerprint stays byte-identical; no `tools/dump_*` re-pin.
- **Difficulty is a HARD non-goal** — the victory is a boss reaching its authored 0 HP; nothing scales stats/HP/damage/reward rates/RNG/run length by a selectable knob. The reveal is authored narrative content, not a difficulty tier.

### Project Structure Notes

- **Production code homes (per `AGENTS.md` File Placement + the established Epic-8/9 layout):** the `RecordFirstVictoryCommand` under `godot/scripts/core/commands/` (alongside `record_first_death_command.gd` + `complete_run_command.gd`); the `FirstVictoryRevealBeat` under `godot/scripts/run/` (alongside `first_death_narrative_beat.gd` + `run_end_outcome.gd`); the `first_victory_recorded` + `boss_defeated` events extend `godot/scripts/core/events/domain_event.gd`; the `first_victory_recorded` profile latch extends `godot/scripts/save/snapshots/profile_snapshot.gd`; the boss-defeat detection seam extends `godot/scripts/tactical/turns/boss_turn_resolver.gd`; the `victory` outcome + `_resolve_completed` atomicity fix extend `godot/scripts/core/commands/complete_run_command.gd`; the `RunEndOutcome` allowlist guard extends `godot/scripts/run/run_end_outcome.gd`; the OPTIONAL outpost reveal render extends `godot/scripts/ui/view_models/outpost_view_model.gd` (if you wire it). Tests mirror the domain: event tests EXTEND `godot/tests/unit/core/test_domain_event.gd`; command tests under `godot/tests/unit/core/` (a new `test_record_first_victory_command.gd` mirroring `test_record_first_death_command.gd`); the beat/outcome/profile tests under `godot/tests/unit/run/` + `godot/tests/unit/save/` (a `test_first_victory_reveal_beat.gd` twin + extend `test_profile_snapshot.gd` + `test_run_end_outcome.gd`); the boss-defeat seam + sequence-id test under `godot/tests/unit/tactical/`; the `CompleteRunCommand` victory + atomicity tests EXTEND `godot/tests/unit/core/test_complete_run_command.gd`.
- **Naming:** `snake_case` files/folders; `PascalCase` classes (`RecordFirstVictoryCommand` / `FirstVictoryRevealBeat`); `UPPER_SNAKE_CASE` consts (`FIRST_VICTORY_LINE_ID` / `RUN_COMPLETED_OUTCOME_VICTORY` / `EVENT_ID_BOSS_DEFEATED`); lower_snake event ids + payload keys + line ids (`first_victory` / `boss_defeated` / `victory`).
- **No conflicts detected** with the unified structure — 9.4 slots the victory/reveal into the existing commands + run + events + save + tactical-turns homes; it introduces NO new top-level directory and NO new autoload (the command + DTO + events are pure `RefCounted`, called directly, mirroring the 8.5 `RecordFirstDeathCommand`/`FirstDeathNarrativeBeat` posture — no autoload added since Epic 8).

### Project Context Rules

Extracted from `project-context.md` (the canonical implementation rulebook, refreshed after Epic 8) — the rules that bind 9.4's victory/reveal tasks:

- **Domain owns tactical truth; presentation mirrors it.** The `RecordFirstVictoryCommand` + `FirstVictoryRevealBeat` + the boss-defeat seam are scene-free `RefCounted` domain services/DTOs — NO Node/scene/autoload, NO Godot signals for control flow. Godot scenes/UI observe domain events + submit commands; they never own the victory/reveal state. 9.4 adds NO new autoload (the registered set `GameSession`/`SceneManager`/`SaveManager`/`AudioManager`/`SettingsManager`/`Diagnostics` is unchanged through Epic 8 — the run-end quartet incl. `CompleteRunCommand`/`RecordFirstDeathCommand` are pure `RefCounted` called directly; the first-victory command joins them; `ProfileRepository` is the cross-run `user://profile.json`, driven by the caller — no live boot-flow wiring exists yet).
- **Commands validate before mutation and return `ActionResult`; successful commands emit deterministic past-tense `DomainEvent`s; events drive all state changes.** `RecordFirstVictoryCommand` validate-then-mutates the profile latch + emits `first_victory_recorded` (past-tense); `CompleteRunCommand` drives the transition + emits `run_completed`+`victory`; the boss-defeat seam emits `boss_defeated`. ZERO events + byte-identical run/profile on ANY reject.
- **Named RNG streams; gameplay randomness uses its assigned stream; cosmetic randomness cannot affect outcomes.** The victory/reveal draw ZERO RNG (deterministic records). The 7-stream set is invariant.
- **Headless simulation is first-class — no rendering/audio/UI/scene-tree state.** The victory/reveal resolve headlessly; the reveal DTO is pure data (the outpost render is a later UI concern / an optional additive read-surface); the tests run without scenes.
- **Rules kernel is registration + explanation ONLY (v0).** Do NOT build the generic `RuleCondition`/`RuleTarget`/`RuleOperation` engine for the victory/reveal. `scripts/rules/conditions/` stays EMPTY; `scripts/rules/operations/` stays at its single file. The victory/defeat/reveal are events + a run-end transition + a latch.
- **Save versioned domain snapshots only; JSON numbers are IEEE-754 doubles — string-encode any field > 2^53 + ALWAYS JSON-round-trip in tests.** The first-victory flag rides the versioned `ProfileSnapshot` (SCHEMA_VERSION stays 1); its round-trip test asserts surviving typed fields (a bool + the small ints), never a byte-identical nested re-stringify. The profile's `last_awarded_run_seed` int64 stays decimal-string encoded (do NOT touch it). Restore exposes NO partial corrupt state (the `parse` fail-loud discipline).
- **Content stays code-constant baselines through the `ContentRepository` boundary; NO `.tres`/JSON pipeline; `data/` stays empty scaffolding.** 9.4 authors ONE narrative line (a const on `FirstVictoryRevealBeat` + the by-id id + the event) — NOT a content family / repository / codex / JSON pipeline (the epic-wide by-id + no-content-pipeline defer).
- **The cross-run `ProfileSnapshot` lives OUTSIDE the run, on its own `ProfileRepository`.** The first-victory latch is a PROFILE field (cross-run), NOT a `RunSnapshot` field (the run autosave). The two saves are strictly independent. The caller persists the mutated profile via `ProfileRepository.write_profile` (repository UNCHANGED).
- **Difficulty is a HARD non-goal** — no selectable difficulty tier; the boss victory is a boss reaching authored 0 HP.
- **On THIS machine `godot` is NOT on the Bash PATH** — run the headless suite via PowerShell (`C:\Users\Rasmus\bin\godot.cmd`, or `C:/Users/Rasmus/Godot_v4.6.3-stable_win64.exe/Godot_v4.6.3-stable_win64_console.exe`), not the Bash tool's PATH lookup.

### Testing standards summary

- **Full headless suite (must pass before `review`/`done`):** `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`. On THIS machine `godot` is NOT on the Bash PATH — run via PowerShell (`C:\Users\Rasmus\bin\godot.cmd`, or the console exe). **Apply the false-PASS grep guard:** `SCRIPT ERROR|Parse Error|Compile Error|Failed to load script` = NONE beyond the documented negative-path diagnostics (the int64-overflow `Cannot represent … as a 64-bit signed integer` ×2 + the deliberate malformed-JSON / `invalid_node_type` parse diagnostics). Diagnose any interruption from the runner's per-file `FAIL` list, NEVER stderr `ERROR:` lines. **The post-9.3 baseline is 161 PASS / 0 `^FAIL`** — 9.4 adds tests (the boss-defeat event, the run-victory outcome, the first-victory command + latch + event + beat, the sequence-id seam, the two 8.1 defer fixes) so the count RISES; keep 0 `^FAIL`.
- **Tests mirror the domain:** the `first_victory_recorded` + `boss_defeated` event tests + the `victory` outcome EXTEND `godot/tests/unit/core/test_domain_event.gd`; the `RUN_COMPLETED_OUTCOME_VICTORY` + `_resolve_completed` atomicity EXTEND `godot/tests/unit/core/test_complete_run_command.gd`; a new `test_record_first_victory_command.gd` mirrors `test_record_first_death_command.gd`; a new `test_first_victory_reveal_beat.gd` mirrors `test_first_death_narrative_beat.gd`; the profile latch EXTENDS `godot/tests/unit/save/test_profile_snapshot.gd` (+ the `DICTIONARY_KEYS` pin + a `_set_first_victory_flag_round_trips_without_a_migration` twin); the `RunEndOutcome` allowlist guard EXTENDS `godot/tests/unit/run/test_run_end_outcome.gd`; the boss-defeat seam + the sequence-id-no-collision test under `godot/tests/unit/tactical/`. New systems require unit test locations before implementation.
- **Coverage every AC demands:** the boss reaching 0 HP emits `boss_defeated` + resolves `run_completed`+`victory`+`outpost` (AC1) + the post-victory return projection (AC1); a first victory latches + emits `first_victory_recorded` + a second victory rejects `first_victory_already_recorded` (AC2 once-only) + a FAILED run rejects `run_not_completed` (AC2 gate) + the latch round-trips at `SCHEMA_VERSION == 1` (AC2 persistence); the `FirstVictoryRevealBeat` projects the FR62 line + is skippable + a skip mutates nothing (AC3) + the summary/outpost are complete without the reveal (AC3 off-critical-path); the `victory` outcome VALIDATES + a garbage outcome rejects; the sequence-id seam threads a shared counter (no duplicate ids — closes the 9.3 Med); the two 8.1 defers are addressed (or re-deferred with a recorded rationale); a manual-seed first victory STILL latches (Option A).
- **After implementation:** rerun the suite + `git diff --check`; verify `git status --short` is clean of stray temp/generated files (never remove planning/story artifacts). If any review finding is deferred, record it in `_bmad-output/implementation-artifacts/deferred-work.md` with the originating story/date; if the two 8.1 defers are fixed, mark them `[Resolved 9.4]` in the ledger.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md` lines 2291-2312 (Story 9.4 ACs), lines 2206-2335 (Epic 9 context: 9.1/9.2/9.3/9.5 seams), line 471/2208 (Epic 9 objective: "reach and defeat the Larval Avatar, see the first-victory reveal, and return to the open-ended roguelite loop")]
- [Source: `_bmad-output/implementation-artifacts/9-3-boss-actions-telegraphs-and-ai-decisions.md` (the boss FIGHT behavior 9.4 drives to 0 HP; the OUT-of-scope "Boss VICTORY + the first-victory reveal → Story 9.4"; the sequence-id seam Med finding earmarked for 9.4; the recorded `[Decision]`s)]
- [Source: `_bmad-output/implementation-artifacts/8-5-first-death-line-and-optional-narrative-delivery.md` (the first-DEATH precedent 9.4 mirrors at the OPPOSITE phase; the Option-A eligibility-independent latch; the off-critical-path skippable beat)]
- [Source: `_bmad-output/implementation-artifacts/8-1-run-completion-and-return-to-outpost-flow.md` (the run-END boundary + the `victory`-left-free-for-Epic-9 decision + the two review defers RE-CARRIED to 9.4)]
- [Source: `_bmad-output/implementation-artifacts/8-7-meta-and-summary-save-load-tests.md` tasks 2.2/2.5 (the migration matrix pinning `SCHEMA_VERSION == 1` + `schema_version: 2` → unsupported — the SCHEMA `[Decision]` constraint)]
- [Source: `_bmad-output/implementation-artifacts/deferred-work.md:31,70,110-120,514-532` (the first-VICTORY reveal defer + the LIVE combat-death CALL SITE defer + the two 8.1 review defers 9.4 owns)]
- [Source: `_bmad-output/auto-gds/retro-notes/epic-9.md` §9-3 (the sequence-id seam — reserve phase-change ids from the shared monotonic counter), §9-1 (the JSON int→float footgun), §9-2 (boss-as-definition → live entity → 9.4 makes its defeat a victory)]
- [Source: `godot/scripts/core/commands/complete_run_command.gd` (the outcome-parameterized run-END command 9.4 extends with `victory`; the `_resolve_completed` two-step atomicity defer at :194-231)]
- [Source: `godot/scripts/run/run_end_outcome.gd:78-102` (the run-END read DTO 9.4 calls `for_completed(run, victory)`; the missing allowlist guard defer)]
- [Source: `godot/scripts/run/run_orchestrator.gd:730-758,885-921` (`resolve_run_end` — "9.4's boss VICTORY drives it"; `_resolve_boss` setup-only; `_advance_sequence_past` — the shared run-level sequence counter)]
- [Source: `godot/scripts/tactical/turns/boss_turn_resolver.gd:59-160` (the 9.3 boss turn driver + `resolve_phase_transitions` + the SEQUENCE-ID SEAM CONTRACT; the boss-defeat detection seam site)]
- [Source: `godot/scripts/tactical/entities/tactical_entity_state.gd:45-50` (`is_dead()` — the boss zero-HP detection)]
- [Source: `godot/scripts/core/commands/record_first_death_command.gd` + `godot/scripts/run/first_death_narrative_beat.gd` (the 8.5 command + beat DTO 9.4 mirrors VERBATIM at the COMPLETED phase)]
- [Source: `godot/scripts/save/snapshots/profile_snapshot.gd:44-56,72,88,118,150` (the exact-key `SCHEMA_VERSION == 1` profile + the `first_death_recorded` sibling the `first_victory_recorded` field mirrors)]
- [Source: `godot/scripts/core/events/domain_event.gd:43-45,84,137-145,195-200,436-454,1073-1077,1239-1261,1380-1395,2249-2254,2331-2336` (the enum tail + the `first_death_recorded` wiring 9.4 mirrors + the `run_completed` outcome allowlist 9.4 adds `victory` to + the "victory left free for Epic 9" note)]
- [Source: `godot/scripts/ui/view_models/outpost_view_model.gd:73,90-95,184-193,228-230,310-363` (the OPTIONAL first-death beat render the victory reveal MAY join)]
- [Source: `godot/tests/unit/core/test_domain_event.gd:2669-2744` (the `expected_ids` exhaustiveness pin + the `Type.size()-1` tie); `godot/tests/unit/save/test_profile_snapshot.gd:53-59,241-272` (the `DICTIONARY_KEYS` pin + the `_set_first_death_flag_round_trips_without_a_migration` twin)]
- [Source: `project-context.md` (Determinism & Simulation Rules; the named-RNG rule; the 23-key `RunSnapshot` gate; the cross-run `ProfileSnapshot`/`ProfileRepository`; content-as-code-constant; the difficulty non-goal; the `godot`-not-on-PATH note); `AGENTS.md` (File Placement, Naming, Testing Expectations, the Mandatory Story Workflow — deferred-work ledger discipline); `CLAUDE.md` (the full headless test command)]

## Dev Agent Record

### Agent Model Used

Opus 4.8 (1M context) — auto-gds dev-story delegate.

### Debug Log References

- Baseline full headless suite before changes: **161 PASS / 0 `^FAIL`** ("Headless tests passed."), false-PASS grep clean beyond the documented negatives (int64-overflow ×2, `invalid_node_type`, malformed-JSON ×3).
- First run after implementation: 1 `^FAIL` (`test_domain_event.gd`) — the pre-existing `_run_completed_rejects_malformed_payloads` used `victory` as its "wrong outcome" example; now that `victory` is a VALID marker the payload proceeded past the outcome check and failed on the (missing) `next_destination`. FIX: swapped the example to `not_a_real_outcome`.
- Final full headless suite: **163 PASS / 0 `^FAIL`** ("Headless tests passed."), false-PASS grep clean (only the documented negatives), `git diff --check` clean, `git status --short` clean of stray temp/generated files.

### Completion Notes List

Story 9.4 mirrors the ratified 8.5 first-DEATH precedent VERBATIM at the OPPOSITE terminal phase (`PHASE_COMPLETED` victory). All 3 ACs met + tested; suite green. Recorded `[Decision]`s:

- **SCHEMA [Decision]:** `first_victory_recorded` added as a NEW ADDITIVE `ProfileSnapshot` field at `SCHEMA_VERSION == 1` (no home was pre-reserved for it, unlike 8.5's first-death), lenient-parsed (a pre-9.4 dict → false), the `DICTIONARY_KEYS` pin updated (a new key, NOT a schema bump). Reconciled with 8.7's migration matrix (pins `SCHEMA_VERSION == 1` + `schema_version:2` → `unsupported_profile_schema`) — a lenient additive field at v1 keeps both 8.7 pins green.
- **`victory` outcome [Decision]:** `victory` classifies as a COMPLETION marker in `CompleteRunCommand` (extended `COMPLETION_OUTCOMES` + `_is_completion_outcome` to accept it alongside `completed`; both run `_resolve_completed`, which now emits the ACTUAL requested outcome) — NOT a dedicated path, NOT a parallel `run_victory` event. The run-victory IS `run_completed` + `outcome == victory`. `resolve_run_end(victory)` drives it end-to-end with NO orchestrator change (the hook already dispatches any outcome).
- **`boss_defeated` payload [Decision]:** `{boss_entity_id (lower_snake), phase_id (lower_snake — the boss's active phase at defeat, e.g. `desperation`), final_hp (non-negative integral, 0 at defeat)}`. Chose `phase_id` over `defeated_at_phase_index` (survives a phase-model change + human-readable). Emitted by `BossTurnResolver.detect_boss_defeat` (the boss-turn-resolver seam — the ONLY 9.3-code change), NOT the victory-resolution call site.
- **Eligibility [Decision] — Option A (mirror 8.5):** the first-victory latch is DELIBERATELY NOT gated on `meta_progression_eligible` — a manual-seed first victory STILL records the flag + shows the line (pure narrative flavor — FR61/FR64; grants ZERO Oath Shards/unlocks/mastery). Diverges from the award/merge but matches 8.5's ratified first-death Option A.
- **Sequence-id seam [Decision]:** 9.4 threads a SHARED monotonic counter across the interleaved streams — `detect_boss_defeat(context, sequence_id_base)` mirrors `resolve_phase_transitions`'s contract (explicit base >= 0, returns `next_sequence_id_after`). A stream-merging caller passes the run-level counter to `resolve_phase_transitions` FIRST, threads the returned cursor into `detect_boss_defeat`, then to the next append (the `run_completed` victory event) — never the board-baseline fallback. Closes the 9.3 review Round-1 Med.
- **AC3 render [Decision] — the 8.5 "render is a later UI story" split:** the reveal ships as ONLY the DTO + flag + event + tests; NOT wired into `OutpostViewModel` (the story permits either). Rationale: lower-risk (minimal change, avoids expanding `OutpostViewModel`'s exact-key pin + recovery-mode tests; the outpost render is naturally a later HUD/boot-flow concern). A later UI story wires it onto `OutpostViewModel` as another optional sub-surface alongside the first-death beat.
- **8.1 defer (a) — FIXED:** `CompleteRunCommand._resolve_completed` two-step atomicity — capture `phase_before` + restore `run.phase` on a step-2 failure (still unreachable → defensive). Marked `[Resolved 9.4]` in the ledger. The identical `NodeResolvePlaceholderCommand._resolve_boss` twin is OUT of 9.4 scope (its boss branch is untouched) — a future story that drives it may mirror the fix.
- **8.1 defer (b) — FIXED:** `RunEndOutcome.for_failed`/`for_completed` allowlist guard (`COMPLETED_OUTCOMES` = boss_placeholder/completed/victory; `RUN_FAILED_CAUSES`); a non-allowlisted marker → `_empty()`. Marked `[Resolved 9.4]`.

Residuals OPEN after 9.4 (knowing splits, recorded in the ledger): the LIVE combat-DEATH call site (hero-death → `PHASE_FAILED` live source) stays deferred — 9.4 wired only the VICTORY half; the first-victory reveal RENDER on the outpost is a later UI story; the `NodeResolvePlaceholderCommand._resolve_boss` two-step atomicity twin is out of scope.

Invariants held: ZERO RNG in any new code (deterministic records); `SCHEMA_VERSION` stays 1 (no migration); 23-key `RunSnapshot` gate stays 23 (the latch is a PROFILE field); 7 named RNG streams invariant; no seed-regression fingerprint moved; `scripts/rules/conditions/` EMPTY + `scripts/rules/operations/` single-file; no `.tscn`/content file; no new autoload.

### File List

Production — modified:
- `godot/scripts/core/events/domain_event.gd` (2 new enum members `FIRST_VICTORY_RECORDED` + `BOSS_DEFEATED` + their event-id consts, factories, validators, `validate_payload` dispatch, both id maps; `RUN_COMPLETED_OUTCOME_VICTORY` const + the `run_completed` outcome-allowlist extension; `FIRST_VICTORY_LINE_ID` const)
- `godot/scripts/core/commands/complete_run_command.gd` (`VICTORY_OUTCOME` + `COMPLETION_OUTCOMES` consts + `_is_completion_outcome`; `_resolve_completed` emits the ACTUAL outcome + the two-step atomicity fix)
- `godot/scripts/save/snapshots/profile_snapshot.gd` (the `first_victory_recorded` additive field — `DICTIONARY_KEYS`/field/`to_dictionary()`/`parse()`/`copy()`)
- `godot/scripts/tactical/turns/boss_turn_resolver.gd` (the `detect_boss_defeat` seam + `_phase_id_for_index` helper — the ONLY 9.3-code change)
- `godot/scripts/run/run_end_outcome.gd` (the allowlist guard on `for_completed`/`for_failed` — the 8.1 defer (b) fix)

Production — new:
- `godot/scripts/core/commands/record_first_victory_command.gd` (the `RecordFirstDeathCommand` twin — VICTORY-only gate)
- `godot/scripts/run/first_victory_reveal_beat.gd` (the `FirstDeathNarrativeBeat` twin — the FR62 reveal DTO)

Tests — modified:
- `godot/tests/unit/core/test_domain_event.gd` (`first_victory_recorded` + `boss_defeated` round-trip/malformed tests; the `victory` run_completed accept test; the two updated stray-`victory`→`not_a_real_outcome` reject tests; the `expected_ids` exhaustiveness-pin entries)
- `godot/tests/unit/core/test_complete_run_command.gd` (the `victory` resolution test; the two-step atomicity test; removed `victory` from the reject-unknown list)
- `godot/tests/unit/run/test_run_end_outcome.gd` (the `victory` projection test; the allowlist-fallback test)
- `godot/tests/unit/run/test_run_orchestrator.gd` (the `resolve_run_end(victory)` end-to-end test)
- `godot/tests/unit/save/test_profile_snapshot.gd` (the `first_victory_recorded` round-trip/v0-home assertions + the `_set_first_victory_flag_round_trips_without_a_migration` twin)
- `godot/tests/unit/tactical/test_boss_turn_resolver.gd` (the boss-defeat seam tests + the sequence-id-seam no-collision test)

Tests — new:
- `godot/tests/unit/core/test_record_first_victory_command.gd` (the `test_record_first_death_command.gd` twin)
- `godot/tests/unit/run/test_first_victory_reveal_beat.gd` (the `test_first_death_narrative_beat.gd` twin)

Tracking — modified:
- `_bmad-output/implementation-artifacts/deferred-work.md` (the 9.4 dev-tracking section + the two 8.1 defers marked `[Resolved 9.4]`)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (9.4 → in-progress → review; `last_updated`)
- `_bmad-output/implementation-artifacts/9-4-boss-victory-and-first-victory-reveal.md` (this story: `baseline_commit` frontmatter, task checkboxes, Dev Agent Record, Status)

### Change Log

- 2026-07-03 — Story 9.4 implemented: boss-defeat → run-victory resolution (`boss_defeated` event + the `victory` `run_completed` outcome) + the first-victory latch/command/event + the `FirstVictoryRevealBeat` DTO, mirroring the 8.5 first-death precedent at `PHASE_COMPLETED`. Fixed the two 8.1 review defers (`_resolve_completed` two-step atomicity; `RunEndOutcome` allowlist guard). Suite 161 → 163 PASS / 0 FAIL. Status → review.

### Review Findings
