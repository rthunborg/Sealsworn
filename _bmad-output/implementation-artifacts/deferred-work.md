## Deferred from: code review of 6-2-small-inventory-and-equipment-model (2026-06-26)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-26; reviewer model Opus 4.8): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 0 blocking. **0 NEW cross-story `[Review][Defer]` items.** 0 `[Review][Patch]`. 2 `[Review][Decision]` (forward-looking, human-visible — both already owned by later stories with no fresh obligation on 6.2), recorded in the story's `### Review Findings` Round-1 subsection. Reviewer INDEPENDENTLY re-ran the full headless suite (Godot 4.6.3 final line "Headless tests passed.", runner exit 0, **102 PASS / 0 FAIL**, all four relevant suites `test_inventory_state.gd`/`test_pickup_item_command.gd`/`test_domain_event.gd`/`test_run_state.gd` PASS) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to any new/modified file — the five stderr `ERROR:` lines are the documented-expected negative-path diagnostics from OTHER suites: the int64-overflow `run_started.root_seed` boundary assertion, `RouteNode parse failed: invalid_node_type`, and the settings/resume `Parse JSON failed` negatives); `git diff --check` exit 0; the diff is confined to the 8 declared `godot/` files (1144 insertions / 5 deletions) — the 23-key `RunSnapshot` gate, `_allowed_run_snapshot_keys()`, `RngStreamSet.required_streams()`, and every generator/route+level fingerprint are byte-identical / NOT in the diff. AC1/AC2/AC3 all MET; every scope boundary held (a DETERMINISTIC pickup — `PickupItemCommand` holds no `RngStreamSet`, mints no `RandomNumberGenerator`, calls no `randi`/`randf`, asserted via a held external stream byte-identical after a success AND a reject; the `item_gained` event appended at the END of the `DomainEvent.Type` enum with NO member renumbered and NOT in `_event_requires_actor`; the additive `RunState.inventory` field default-empty/never-null, NOT a required `validate()` field, LAST constructor param, deep-copied, riding the FULL run dict ONLY; the load-bearing AC3 "no silent delete" verified — a full-backpack reject leaves the run byte-identical with every original slot intact and emits zero events; no content re-authored, no new top-level `RunSnapshot` key, no UI scene, no difficulty knob).

_No items deferred from this review round._ The two `[Review][Decision]` items (the equip-gate character-level CHECK deferral — owner: the later story that introduces a hero character-level system, OR Story 6.3 if it needs it; the inventory/equipment SAVE wiring into the existing `RunSnapshot.inventory`/`equipment` fields — owner: the later story that owns the in-node save) are forward-looking human-visible calls wholly about DOWNSTREAM consumers, and were ALREADY captured by the dev under "Tracked from: dev of 6-2" below — so per this file's convention (matching the 5-1…6-1 Round-1 entries) they are recorded in-story + carried in the dev-of-6-2 section rather than re-logged here as fresh cross-story deferrals. The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Story 6.3) + the 6.1 reward-table-validate-before-draw `[Review][Decision]` (owner Story 6.3) are UNTOUCHED — 6.2 adds a deterministic pickup (zero RNG, no orchestrator wiring), so the first live reward ROLL (and thus the inert-stream fix + the reward-table validation) remains Story 6.3's. The duplicate-id last-write-wins all-repos `[Review][Defer]` (RESOLVED by Story 6.1) is UNTOUCHED — 6.2 authors no content repository.

## Tracked from: dev of 6-2-small-inventory-and-equipment-model (2026-06-26)

Story 6.2 built the small inventory + equipment DOMAIN MODEL (`InventoryState`: a fixed-capacity backpack default 6, no stacking; + a named equipment-slot structure), a validate-then-mutate `PickupItemCommand` (`inventory_full` fail-closed, no silent delete), the new `item_gained` SYSTEM `DomainEvent` (appended at the enum end, wired end-to-end), and the additive `RunState.inventory` field. These are the dev-side forward residuals (no code review has run yet — that is the next pipeline phase). All ACs MET; full headless suite green (Godot 4.6.3, "Headless tests passed.", exit 0, 102 PASS / 0 FAIL); zero RNG drawn; no content re-authored; no new top-level `RunSnapshot` key; the 23-key gate + `_allowed_run_snapshot_keys()` + `RngStreamSet.required_streams()` UNCHANGED.

- **(NEW — owner: the later story that introduces a hero character-level system, OR Story 6.3 if it needs it) The EQUIP-GATE character-level CHECK is DEFERRED.** Story 6.2's title is "...and Equipment Model," and it builds the equipment STRUCTURE (a named-slot `weapon`/`armor`/`jewelry`/`support` Dictionary on `InventoryState`, with an `equipped_in(slot)` accessor + the `RunSnapshot.equipment` `{slot: item_id}` forward-shape). But the equip-gate ENFORCEMENT — compare an item's `character_level_requirement` (Story-6.1 `ArmorDefinition`/`JewelryDefinition`/etc.) to the hero's character level — is NOT built: there is NO hero character-level system yet (the run carries `starting_kit.baseline_hp`, not a character level). This is the seam the Story-6.1 `[Review][Decision]` parked at "the equip-check is Story 6.2/6.3". 6.2 builds NO equip/unequip command at all (the three ACs are entirely about the BACKPACK — capacity/pickup/item-gained/inventory-full). When an equip path is built, it consults `def.character_level_requirement` / `requires_character_level()` vs the hero's character level and NEVER gates on run-depth (the 6.1 AC4 rule; `LEVEL_REQUIREMENT_NONE`/0 is the real "no gate" sentinel, never "missing"). (Originating: dev of 6-2, 2026-06-26.)
- **(NEW — owner: the later story that owns the in-NODE inventory save) The inventory/equipment SAVE wiring into `RunSnapshot.inventory`/`equipment` is DEFERRED.** 6.2's model rides ONLY the FULL `RunState.to_dictionary()`/`try_from_dictionary` (lenient-read, like `starting_kit`). It is DELIBERATELY NOT in `to_run_snapshot_fields()` / the 23-key gate — the `RunSnapshot.inventory`/`equipment` placeholder fields (Story-2.7) stay EMPTY this story (asserted by `test_run_state.gd`). The route-position save (the only live save path in v0) deliberately leaves `level_state`/`inventory`/`equipment` empty (a between-NODE choice has no live board/inventory), and the live tactical inventory exists only inside a node — there is no end-to-end run save path that needs the backpack persisted yet. The later story that owns the in-node save serializes INTO the EXISTING `RunSnapshot.inventory`/`equipment` fields (compose, don't fork — the slot shape is `{item_id, category, quantity}`; the placeholder illustrative shape uses `definition_id`, so that story reconciles the key name) and keeps `_allowed_run_snapshot_keys()` + the 23-key gate UNCHANGED. (Originating: dev of 6-2, 2026-06-26.)
- **(NEW — forward option, owner: a later story IF a pickup ever accepts an unvalidated external id) The pickup command does NOT resolve the item id against a Story-6.1 repository.** `PickupItemCommand.validate()` checks the item id is lower_snake (`invalid_item_id`) + the category is in the backpack allowlist (`invalid_item_category`), but does NOT look the id up in `ConsumableRepository`/`ArmorRepository`/etc. — the reward-offer flow (Story 6.3) hands a REAL rolled id, so a repo-existence check here would couple the command to the content layer for no v0 benefit. If a future caller ever feeds the command an unvalidated external id, tighten `validate()` to resolve the id through the matching `get_<x>` accessor (the duplicate-id-guarded fail-closed path) and reject a non-existent id. (Originating: dev of 6-2, 2026-06-26.)
- **(Untouched) The inert run-level `RngStreamSet` `[Review][Defer]` (owner Story 6.3) + the 6.1 reward-table-validate-before-draw `[Review][Decision]` (owner Story 6.3) are UNTOUCHED by 6.2.** A pickup is DETERMINISTIC — `PickupItemCommand` holds no `RngStreamSet`, calls no `randi`/`randf`, mints no `RandomNumberGenerator` (grep-confirmed), and wires no live roll through the orchestrator. The first live reward ROLL (and thus the inert-stream fix + the reward-table validation) is Story 6.3's. (Originating: dev of 6-2, 2026-06-26.)
- **(Untouched) The duplicate-id last-write-wins all-repos `[Review][Defer]` is UNTOUCHED by 6.2** — 6.2 authors NO content repository (it tracks 6.1's content by-id) and registers no duplicate id in any fixture. (Originating: dev of 6-2, 2026-06-26.)

## Deferred from: code review of 6-1-item-loot-and-reward-definitions (2026-06-26)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-26; reviewer model Opus 4.8): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 0 blocking. **0 NEW cross-story `[Review][Defer]` items.** 2 `[Review][Decision]` (forward-looking, human-visible — owned by Stories 6.2/6.3) + 0 `[Review][Patch]`, all recorded in the story's `### Review Findings` Round-1 subsection.

_No items deferred from this review round._ The two `[Review][Decision]` items (`requires_character_level()` treating a level-1 requirement as a real gate — the equip-check is Story 6.2/6.3; `RewardTableDefinition.total_weight()`/`reward_entries()` tolerating an unvalidated table — the offer flow must validate before drawing, Story 6.3) are forward-looking human-visible calls wholly about DOWNSTREAM consumers, recorded in-story per this file's convention (matching the 5-1…5-5 Round-1 entries) rather than re-logged here as cross-story deferrals. The carried Epic-5 duplicate-id last-write-wins `[Review][Defer]` (under "code review of 5-1" below) was THIS story's load-bearing AC6 deliverable and is now CORRECTLY marked `RESOLVED by Story 6.1` (one uniform central `ContentRepository` guard surfaced per-type across all 12 repos, with a duplicate-id negative on every repo + a central-guard test) — verified by the reviewer, who independently re-ran the full headless suite (Godot 4.6.3 exit 0, "Headless tests passed.", 100 PASS / 0 FAIL) and the false-PASS guard grep (zero `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to any new/modified file). The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Story 6.3) is UNTOUCHED — 6.1 draws RNG only through an injected `RngStreamSet` inside FIXTURES, wires no live roll into the orchestrator, and authors the offer-builder to accept an injected set so 6.3 owns the T2 threading without forking it.

## Deferred from: code review of 5-5-class-start-playable-smoke-slice (2026-06-26)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-26; reviewer model Opus 4.8): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 2 — no blocking findings; **0 NEW cross-story `[Review][Defer]` items**; 0 open `[Review][Decision]` (no fresh human call); 2 non-blocking Low `[Review][Patch]` next-touch fold-ins + 1 candidate dismissed as correct-by-design noise, all recorded in the story's `### Review Findings` Round-1 subsection. Reviewer INDEPENDENTLY re-ran the full headless suite (Godot 4.6.3 final line "Headless tests passed.", runner exit 0, all three new files `test_class_start_summary_view_model.gd`/`test_class_start_smoke_slice.gd`/`test_class_start_re_derive.gd` PASS, zero FAIL) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to `class_start_summary_view_model.gd` or any new test; the only stderr `ERROR:` lines are the documented-expected negative-path diagnostics from OTHER suites — the int64-overflow from `test_manual_seed_loader.gd`/`test_domain_event.gd`, `RouteNode parse failed: invalid_node_type` from `test_route_node.gd`, and `Parse JSON failed` from `test_run_resume_service.gd`/`test_settings_repository.gd`); `git diff --check` clean; the diff is PURELY ADDITIVE — the 4 declared new files only (883 insertions, 0 deletions), NO existing seam modified (`run_start_command.gd`/`run_orchestrator.gd`/`run_state.gd`/`rules_resolver.gd`/`rule_trigger.gd`/`passive_*`/`class_*`/`starting_kit.gd`/`hero_select_view_model.gd`/all generators/all fingerprints NOT in the diff — consumed, not modified). AC1/AC2/AC3/AC4 all MET; every scope boundary held (v0 smoke slice surfaces passive EXPLANATIONS + resolver presence via a PURE-read window resolution, NOT a mutated combat number — `scripts/rules/{conditions,operations}` empty, no `RuleCondition`/`RuleTarget`/`RuleOperation`, no combat HOOK site, no Epic-6 over-build; the slice drives a REAL selectable-class start through the three fail-closed gates class->kit->passive; `re_derive_kit`/`re_derive_resolver` re-build BOTH from the restored `selected_class_id` exactly as `RunStartCommand` seats them — class passive registered FIRST then equipment-synergy, byte-equal to a fresh start, with `restored_run.starting_kit`/`restored_run.rules_resolver` verified null-by-design after a real save/restore round-trip; the pinned 23-key `RunSnapshot` save gate verified untouched — `rules_resolver` absent from `to_dictionary()`/`to_run_snapshot_fields()`; named-RNG-streams-only — zero `randi`/`randf`/`RandomNumberGenerator` in any new file; FR45 no-active-skill absence pinned).

_No items deferred from this review round._ The two Low `[Review][Patch]` items (the `passive_explanations` flat field re-resolving from the injected `PassiveRepository` rather than the run's resolver — a benign source-of-truth split with no reachable divergence in v0; the untested defensive kit-absent fallback in `summarize()`) are in-story next-touch fold-ins wholly inside this story's new `class_start_summary_view_model.gd` — NOT cross-story deferrals, so per this file's convention (matching the 5-1/5-2/5-3/5-4 Round-1 entries) they are recorded in-story rather than re-logged here. The consolidated 5.4 re-derive-both obligation is confirmed CLOSED by this story (see "Tracked from: dev of 5-5" below). The per-effect passive OPERATIONS + combat HOOK sites stay deferred to Epic 6 (5.5 surfaces explanations only — verified). The duplicate-id last-write-wins all-six-repos `[Review][Defer]` (owner: the dedicated all-repos hardening story) is UNTOUCHED — 5.5 forks no repo and registers no duplicate id in any fixture. The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Epic 6/7/9) is UNTOUCHED — 5.5 draws zero new RNG.

## Tracked from: dev of 5-5-class-start-playable-smoke-slice (2026-06-26)

Story 5.5 is the EPIC-5 CLOSING smoke slice: it ASSEMBLED + SURFACED + PROVED what 5.1-5.4 + the 4.6 orchestrator already deliver into one observable "start a run as a class -> see the class identity + its passive nudges at the first tactical level" surface, and CLOSED the consolidated 5.4 re-derive-both obligation. The only NEW production code is one scene-free projection (`ClassStartSummaryViewModel`) + its two static re-derive helpers; everything else is consumed. These are the dev-side forward residuals (no code review has run yet — that is the next pipeline phase). All ACs MET; full headless suite green (Godot 4.6.3, "Headless tests passed.", exit 0); zero new RNG drawn; no production file other than the new view model was modified.

- **(CLOSED by Story 5.5) The consolidated 5.4 RE-DERIVE-BOTH obligation is now CLOSED.** The "Tracked from: dev of 5-4" forward note (owner Story 5.5) required a resumer to RE-DERIVE BOTH the `StartingKit` AND the `RulesResolver` from a restored `selected_class_id` after a route-position resume (`restored_run.starting_kit` AND `restored_run.rules_resolver` are NULL by design). 5.5 added the ONE canonical re-derive — `ClassStartSummaryViewModel.re_derive_kit(class_id)` + `re_derive_resolver(class_id)` (deterministic pure static functions, zero RNG): re-derive the kit (`ClassRepository.get_class_definition` -> `StartingKit.new(...)`), resolve the two passive ids through `PassiveRepository.get_passive(...)`, rebuild a `RulesResolver` registering the class passive FIRST then the equipment-synergy passive (the SAME order `RunStartCommand.execute` uses). Pinned by `test_class_start_re_derive.gd` (a real `SaveRepository` route-position round-trip restores `starting_kit == null` + `rules_resolver == null`, then the helper re-derives both to byte-equal a fresh `RunOrchestrator.start(same_seed, false, same_class_id)`'s kit + `registered_passive_ids` + per-window `run_started`/`before_attack` explanations; an empty/unknown/locked class re-derives null fail-closed). The helper is the single place a future LIVE resume path (an Epic-6+ tactical resume) re-derives the class start — 5.5 exercises the helper + the save/restore precondition but does NOT wire a live resume into production (no current call site needs one; the obligation was to close the re-derive, which is done). No residual deferral on this item.
- **(Re-affirmed — owner Story 5.5 done / Epic 6) Per-effect passive OPERATIONS + the combat HOOK sites stay deferred to Epic 6.** 5.5 is the "felt-in-combat smoke slice", and per its AC2 SCOPE CLARIFICATION (inherited verbatim from 5.4) the felt difference at this v0 milestone is the EQUIPMENT SHAPE + the passive EXPLANATION surface + the resolver's window resolution + per-class differentiation — NOT a mutated combat number. 5.5 surfaces the explanations (resolving `run_started`/`before_attack` on the run's resolver) and PROVES each class differs; it wires NO `RuleCondition`/`RuleTarget`/`RuleOperation`, NO combat HOOK site inside `AttackCommand`/`MoveCommand`/attack-preview, and NO resolver that mutates combat state. `scripts/rules/{conditions,operations}` stay EMPTY scaffolding. v0 passives remain EXPLANATION-ONLY. The per-effect operations + combat hook sites + the 20-30 passive POOL (FR46) remain Epic 6. (Originating: dev of 5-5, 2026-06-26; re-affirms the "Tracked from: dev of 5-4" forward note now that its owner story has shipped.)
- **(Untouched) The duplicate-id last-write-wins all-six-repos `[Review][Defer]` (owner: the dedicated all-repos hardening story) is UNTOUCHED by 5.5.** 5.5 forks NO repository and registers NO duplicate id in any fixture — it consumes `ClassRepository`/`PassiveRepository`/`WeaponRepository`/`SupportRepository` through their accessors (`get_class_definition`/`get_passive`/`get_weapon`/`get_support`, NEVER `get_class`). The cross-cutting defer stays owned by the dedicated hardening story.
- **(Untouched) The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Epic 6/7/9) is UNTOUCHED by 5.5** — 5.5 draws ZERO new RNG (the run start draws RNG only via RouteGenerator's `map` stream inside `RunStartCommand`; first-level generation draws the `level` stream through the orchestrator; the Epic-1 loop drive on the generated board uses the existing Epic-1 stream; the resolver + the re-derive helpers + the projection draw zero RNG, grep-confirmed no `randi`/`randf`/`RandomNumberGenerator` in any new 5.5 file). When a future passive ROLLS (Epic 6), that draw MUST route through the orchestrator's `streams`.

## Deferred from: code review of 5-4-starting-passive-rule-integration (2026-06-25)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-25; reviewer model Opus 4.8): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 0 blocking; 0 `[Review][Patch]`; 2 `[Review][Decision]` (human-aware forward notes, recorded in the story's `### Review Findings` Round-1 subsection); **0 NEW cross-story `[Review][Defer]` items**; 3 candidate nits dismissed as convention-consistent noise. Reviewer independently re-ran the full headless suite (Godot 4.6.3 final line "Headless tests passed.", 82 PASS / 0 FAIL, all five new/extended suites `test_passive_definition.gd`/`test_passive_repository.gd`/`test_rule_trigger.gd`/`test_rules_resolver.gd`/`test_run_start_command.gd` PASS) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to `passive_definition.gd`/`passive_repository.gd`/`rule_trigger.gd`/`rules_resolver.gd`/`run_start_command.gd`/`run_state.gd`/their tests; `get_passive` used, never `get_class`); `git diff --check` clean; the diff is confined to the declared production + test files (`run_snapshot.gd`/`domain_event.gd`/`starting_kit.gd`/the class/weapon repos NOT in the diff — consumed, not modified). AC1/AC2/AC3 all MET; every scope boundary held (EXPLANATION-ONLY v0 — no combat-effect mutation, no `RuleCondition`/`RuleTarget`/`RuleOperation`, no combat HOOK site, `scripts/rules/{conditions,operations}` left empty; the three sequential fail-closed gates class -> kit -> passive verified validate-then-mutate / no-partial-mutation; `PassiveRepository` mirrors its siblings and is NOT forked for the duplicate-id trap; `RunState.rules_resolver` verified genuinely NOT serialized — absent from `to_dictionary()`/`to_run_snapshot_fields()`/both round-trip read paths, the pinned 23-key `RunSnapshot` gate untouched, an empty-class start byte-identical to a seed-only start; ZERO RNG drawn — grep-confirmed no `randi`/`randf`/`RandomNumberGenerator` in any new file; `RunOrchestrator.start(...)` signature unchanged with the new `PassiveRepository` param LAST and all existing call sites preserved).

_No items deferred from this review round._ The two `[Review][Decision]` forward notes (per-effect passive OPERATIONS + combat HOOK sites -> Story 5.5 / Epic 6; re-derive `RunState.rules_resolver` from `selected_class_id` after a route-position resume -> Story 5.5) are deliberate, documented, test-bounded design lines with no open obligation on any current story — per this file's convention (matching the 5-1/5-2/5-3 Round-1 entries) they are noted in-story rather than re-logged here as cross-story `[Review][Defer]`, AND they were ALREADY captured by the dev under "Tracked from: dev of 5-4" below. The duplicate-id last-write-wins cross-cutting `[Review][Defer]` (now SIX repos with `passive_repository.gd` folded in by the dev) remains OPEN and owned by the dedicated all-repos hardening story — benign for 5.4 (distinct baseline passive ids, no duplicate fixture); 5.4 correctly does not fork `PassiveRepository`. The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Epic 6/7/9) is untouched — 5.4 draws zero RNG.

## Tracked from: dev of 5-4-starting-passive-rule-integration (2026-06-25)

Story 5.4 introduced the passive system (`PassiveDefinition` + fail-closed `PassiveRepository`, six baseline starting passives) + the minimal rules kernel (`RuleTrigger` fixed-window vocabulary + `RulesResolver`) + the `RunStartCommand` passive-resolution gate that seats a `RulesResolver` on `RunState`. These are the dev-side forward residuals (no code review has run yet — that is the next pipeline phase). All ACs MET; full headless suite green; zero RNG drawn.

- **(Forward — owner Story 5.5 / Epic 6) Per-effect passive OPERATIONS + the combat HOOK sites remain deferred.** 5.4 delivers the STARTING-passive REGISTRATION + the explainable trigger-window RESOLUTION ONLY (a registered passive surfaces with its `explanation`; the resolver is a pure read). Wiring a passive's effect into `AttackCommand`/`MoveCommand`/attack-preview so a passive CHANGES a combat number, and the `RuleOperation`/`RuleCondition`/`RuleTarget` evaluation model + stacking/conflict/duration, are **Story 5.5** (the felt-in-combat smoke slice) + **Epic 6** (the effect model + the 20-30 passive POOL, FR46). `scripts/rules/{conditions,operations}` stay EMPTY scaffolding. v0 starting passives are EXPLANATION-ONLY rule-benders. Not blocking 5.4 (no AC requires a mutated number — see the story's AC2 SCOPE CLARIFICATION). (Originating: dev of 5-4, 2026-06-25.)
- **(Forward — owner Story 5.5, extends the 5-3 FORWARD NOTE) The seated `RunState.rules_resolver` is NOT serialized and must be RE-DERIVED from `selected_class_id` after a route-position resume.** Mirroring the 5.3 kit decision exactly: the resolver is a live `RefCounted` service (the inert-`RngStreamSet` precedent), so `to_dictionary()` / `to_run_snapshot_fields()` / the 23-key `RunSnapshot` are UNTOUCHED — a route-position save persists the class id ONLY. A resumer (Story 5.5's tactical slice) that needs the resolver AFTER a route-position resume MUST re-derive it from the restored `selected_class_id` (-> re-derive the kit -> resolve the two passive ids through the baseline `PassiveRepository` -> rebuild the resolver); `restored_run.rules_resolver` is null by design (exactly as `restored_run.starting_kit` is). Keep the re-derive a deterministic pure function. Not blocking 5.4 (AC1 only requires the resolver in domain state at `NEW_RUN -> ACTIVE_ROUTE`, satisfied). (Originating: dev of 5-4, 2026-06-25; extends the deferred-work 5-3 Round-1 FORWARD NOTE for Story 5.5.)
- **(Folded into the existing duplicate-id ledger — now SIX repos) The NEW `PassiveRepository` inherits `ContentRepository`'s last-write-wins behavior by construction**, exactly like its five siblings. The duplicate-id last-write-wins cross-cutting `[Review][Defer]` under "code review of 5-1" below now covers SIX content repositories (`class_repository.gd`, `enemy_repository.gd`, `level_recipe_repository.gd`, `weapon_repository.gd`, `support_repository.gd`, **`passive_repository.gd`**). 5.4 did NOT fork `PassiveRepository` to fail-loud on a duplicate id (it matches the sibling convention); the new repo is folded into that entry's scope rather than opening a separate defer. Benign for 5.4 (the six baseline passive ids are distinct; no duplicate fixture). (Originating: dev of 5-4, 2026-06-25; folded into the 5-1 Phase-7 human-ratified duplicate-id `[Review][Defer]`.)
- **(Untouched) The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Epic 6/7/9) is UNTOUCHED by 5.4** — 5.4 draws zero RNG. When a future passive ROLLS something (Story 5.5 / Epic 6), that draw MUST route through the orchestrator's `streams` (the named-stream rule), NEVER global/un-named RNG or a fresh `RandomNumberGenerator`.

## Deferred from: code review of 5-3-starting-kit-application (2026-06-25)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-25): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 2; 0 `[Review][Patch]`; 2 `[Review][Decision]` (human calls, recorded in the story's `### Review Findings` Round-1 subsection); **0 new cross-story `[Review][Defer]` items**; 3 candidate nits dismissed as convention-consistent noise. Reviewer independently re-ran the full headless suite (Godot 4.6.3 exit 0, "Headless tests passed.", new `test_starting_kit.gd` + extended `test_run_start_command.gd`/`test_run_state.gd`/`test_run_route_position_save.gd` all PASS, zero FAIL) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to `run_start_command.gd`/`run_state.gd`/`starting_kit.gd`/their tests); `git diff --check` clean; diff confined to the 7 declared files (`run_snapshot.gd`/`run_resume_service.gd`/`run_orchestrator.gd`/the content repos NOT in the diff — consumed, not modified). AC1/AC2/AC3 all MET; every scope boundary held (no tactical board player, no passive/rules wiring, no `ClassDefinition`/`ClassRepository`/`Weapon*`/`Support*`/baseline change, no RNG draw, no difficulty knob, no new top-level `RunSnapshot` key; `get_class_definition`/`get_weapon`/`get_support` used throughout; the two new repo params are LAST with all existing `RunStartCommand.new(...)` call sites preserved). The SAVE/serialization change (class id nested under `route_state`, kit re-derived on restore) was verified backward-compatible with pre-5.3 saves and the pinned 23-key `RunSnapshot` gate verified genuinely untouched; the fail-closed kit gate verified validate-then-mutate / no-partial-mutation; `&"none"` verified to resolve (Ranger not falsely rejected). The two Low `[Review][Decision]` items (kit resolved twice on the success path; route-position persists class-only so the restored `RunState.starting_kit` is null and a future resumer must re-derive) are deliberate, documented, test-proven design choices with no open obligation on any current story — per this file's convention (matching the 5-1/5-2/4-6 Round-1 entries) they are noted in-story, NOT logged here as cross-story `[Review][Defer]`.

_No items deferred from this review round. The 5.2 Round-1 class-survives-resume `[Review][Defer]` that Story 5.3 was assigned to close is marked **RESOLVED by Story 5.3** under the "code review of 5-2" heading below (verified during this review). The duplicate-id last-write-wins cross-cutting `[Review][Defer]` (consumed by `ClassRepository`/`WeaponRepository`/`SupportRepository`) remains open and owned by the dedicated all-five hardening story — benign for 5.3 (distinct baseline ids, no duplicate fixture); 5.3 correctly does not fork any repo. The 4.6 inert run-level `RngStreamSet` `[Review][Defer]` (owner Epic 6/7/9) is untouched — 5.3 draws zero RNG._

## Deferred from: code review of 5-2-hero-select-with-playable-and-locked-classes (2026-06-25)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-25): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 2; 0 `[Review][Patch]`; 0 `[Review][Decision]`; 3 dismissed as noise. Reviewer independently re-ran the full headless suite (Godot 4.6.3 exit 0, "Headless tests passed.", new `test_hero_select_view_model.gd` + extended `test_run_start_command.gd` / `test_run_orchestrator.gd` all PASS, zero FAIL lines) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to `hero_select_view_model.gd` / `run_start_command.gd` / `run_state.gd` / `run_orchestrator.gd` / their tests); `git diff --check` clean; the pinned 23-key `RunSnapshot` no-surprise-key gate verified untouched (`selected_class_id` confirmed absent from `to_run_snapshot_fields()` / the save path / `test_run_snapshot.gd`). AC1/AC2/AC3 all MET; every scope boundary held (no kit application, no passive/rules wiring, no `ClassDefinition`/`ClassRepository`/baseline change, no real `Control`/scene, no `RouteGenerator`/`LevelGenerator`/`run_completed` change, no RNG draw, no difficulty knob, no new top-level `RunSnapshot` key; `get_class_definition` used throughout; `class_id` is the LAST optional param with all existing call sites preserved). The two Low items below are forward/latent, both deferred.

- [Review][Defer → RESOLVED by Story 5.3] (Low) `RunState.selected_class_id` route-position save/resume persistence — **RESOLVED by Story 5.3; full entry archived to `deferred-work-resolved.md` during the Epic 5 closeout (2026-06-26).**
- [Review][Defer] (Low) `RunOrchestrator.start` re-called with a bad class on a REUSED orchestrator leaves the prior run's `run`/`streams` seated (stale). On a `class_not_selectable`/`unknown_class` reject `start()` returns early BEFORE re-assigning `run`/`streams`, so a fresh orchestrator stays correctly unseeded (the tests pin `orchestrator.run == null`). The stale-state edge only manifests if a SINGLE `RunOrchestrator` instance is reused across two `start()` calls (first success, then a bad-class reject) — no current call site does this (every run uses a fresh `RunOrchestrator.new()`), and the identical early-return-without-reset shape pre-existed for the 4.6 negative-seed reject path, so 5.2 introduces no new exposure (it only adds two more error codes that can hit the same benign-today path). Fix (future hardening): reset `run`/`streams` to null at the top of `start()` (or on any `start()` reject) so a reused orchestrator cannot surface a stale run after a rejected start. Not blocking. (Originating: code review of 5-2, Round 1, 2026-06-25; location `godot/scripts/run/run_orchestrator.gd:98-107`.)

_Not re-logged here (already an open cross-story defer): the duplicate-id last-write-wins trap across all five content repositories (incl. `ClassRepository`, which `HeroSelectViewModel.classes()` + `RunStartCommand` consume) — see the human-ratified entry under "code review of 5-1" below. Benign for 5.2 (distinct baseline ids; no duplicate fixture) and owned by the dedicated all-five hardening story; 5.2 correctly does not fork `ClassRepository`._

## Deferred from: code review of 5-1-class-definition-content-and-repository (2026-06-25)

Round 1 (primary adversarial code review — Blind Hunter + Edge Case Hunter + Acceptance Auditor, 2026-06-25): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 0 blocking; 3 `[Review][Decision]` (human calls) + 5 dismissed as convention-consistent noise. Reviewer independently re-ran the full headless suite (Godot 4.6.3 exit 0, "Headless tests passed.", both new suites `test_class_definition.gd`/`test_class_repository.gd` PASS) and the false-PASS guard grep (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to the new class files); `git diff --check` clean; diff confined to the 4 declared new `godot/` files (no `RunState`/`RunStartCommand`/UI/passive/rules-kernel/JSON-pipeline change — scope held). AC1/AC2/AC3 all MET. **No new cross-story `[Review][Defer]` items were produced this round.** The 3 `[Review][Decision]` findings are recorded in the story's `### Review Findings` Round-1 subsection (a human call per pipeline Phase 7): (1) LOCKED classes accept a malformed kit — the spec-flagged asymmetry, harmless in v0; (2) `register_class` duplicate-id last-write-wins — byte-identical to every existing sibling repository, a convention-consistency call not a class regression; (3) Decision E accessor rename `get_class` → `get_class_definition` — mandatory (native `Object.get_class()` collision) but spec-deviating, needing 5.2/5.3 to call the new name. None is a cross-story deferral, so per this file's convention (matching the 4.6 Round-1/Round-2 entries) they are noted in-story rather than logged here as `[Review][Defer]`.

**Update (2026-06-25, Story 5-1 Phase 7 HITL gate — human ratification):** Decision 2 was resolved as "keep parity in 5-1 + log a cross-cutting hardening deferral," which promotes it to a genuine `[Review][Defer]` below.

- [Review][Defer → RESOLVED by Story 6.1, 2026-06-26] Duplicate-id last-write-wins is unguarded across ALL SIX content repositories (`class_repository.gd`, `enemy_repository.gd`, `level_recipe_repository.gd`, `weapon_repository.gd`, `support_repository.gd`, `passive_repository.gd`): `register_*` overwrites by `(type, id)` while `*_ids()` keeps only the first insertion, so a duplicate id makes the id-list and the resolver (`get_*`) disagree about the canonical definition — silently, with no error. Harmless today (baseline ids are distinct code constants) but a latent fail-quiet trap once content moves to a JSON/data pipeline (copy-paste / merge authoring errors), which conflicts with the project's fail-closed/fail-loud rule. HUMAN-RATIFIED (Story 5-1 Phase 7, 2026-06-25): keep parity (do NOT fork one repo's behavior) and address as ONE cross-cutting hardening story that makes all six repositories fail-loud (reject with a structured reason, or `push_error`) on a duplicate id, with tests. (Originating: code review of 5-1, Decision 2; `passive_repository.gd` folded in by dev of 5-4, 2026-06-25 — it inherits the SAME `ContentRepository` last-write-wins behavior, not forked.) **SCHEDULED (2026-06-26):** folded into the plan as `planning-artifacts/epics.md` Epic 6 Story 6.1 — a duplicate-id fail-loud acceptance criterion (AC6) + Implementation Task `6.1.5` (retrofit all six existing repos + the new Epic 6 loot/reward repos), sequenced before/with Epic 6's first new data-pipeline content. **RESOLUTION (Story 6.1 dev, 2026-06-26):** applied ONE uniform central guard ([Decision a]) — `ContentRepository.register_definition` now returns `ActionResult` (was `void`) and REJECTS a second `(type, id)` with a stable `duplicate_definition` error (offending `{type, id}` in metadata); all SIX existing `register_<x>` call sites updated in the SAME change to surface it as a per-type `duplicate_<x>` error (`duplicate_class`/`duplicate_enemy`/`duplicate_level_recipe`/`duplicate_weapon`/`duplicate_support`/`duplicate_passive`), and every NEW Epic-6 loot/reward repo (`armor`/`jewelry`/`consumable`/`pickup`/`gold_reward`/`reward_table`) inherits the guard by construction. A rejected duplicate is neither stored (`get_<x>` still returns the FIRST) nor appended to `<x>_ids()` (kept exactly once); a duplicate in a `create_repository_from_definitions` batch returns `null` (fail-closed). NO repo was forked (parity preserved). Pinned by a duplicate-id NEGATIVE test on EVERY content repo (six existing + six new) AND a new `test_content_repository.gd` pinning the central guard directly; the full headless suite re-ran GREEN (exit 0, "Headless tests passed.", zero baseline-registration / `RunStartCommand` content-gate regression). This entry is CLOSED — archive to `deferred-work-resolved.md` at the epic-6 closeout.

## Story 4.6 dispositions + playtest/pacing tracked notes (2026-06-23)

This is the AC4/AC5 tracked-notes deliverable for Story 4.6 (Playable Run Shell from Start to End) PLUS the disposition record for the defers 4.6 OWNS. 4.6 wired the finished 4.1-4.5 run domain + the Epic-3 level pipeline + the Epic-2 save layer into ONE end-to-end playable loop: the NEW `RunStartCommand` (the first `run_started` emitter), the thin `RunOrchestrator` type-dispatch driver, the board-free route-position save/resume bridge, and the pacing/playtest measurement seams.

**Defers CLOSED by Story 4.6 (the two 4.1 `run_started` payload defers — OWNER was "whatever story first EMITS `run_started`" = this story):**

- **`run_started` `node_count` raw-JSON-number defer (4.1 Round-1) — CLOSED as PERMANENTLY BENIGN.** Re-confirmed `node_count` stays small/bounded: `RunStartCommand` sets it from the route's NON-BOSS count, which `RouteGenerator` structurally bounds to `[MIN_NON_BOSS_NODES=8, MAX_NON_BOSS_NODES=12]`. NO encoding change (it is a bounded count, not a seed — correctly stays a raw JSON integer, NOT decimal-string encoded). Pinned by `test_run_start_command.gd::_node_count_stays_bounded_across_seed_sample` (across a 9-seed sample) + the structural `test_run_pacing_survey.gd` survey + the bounded `[8,12]` round-trip assertion added to `test_domain_event.gd::_run_started_serializes_and_parses_stable_payload`. (Per the 4.2 retro note: 4.2 structurally bounds the route to 8-12 non-boss nodes so this defer could be closed as permanently benign rather than re-litigated.)
- **`run_started` `_has_decimal_string_payload` loose-`is_valid_int()` defer (4.1 Round-2) — CLOSED.** `DomainEvent._has_decimal_string_payload` (`domain_event.gd`) now ALSO enforces the int64 lossless round-trip (the EXACT Story 3.7 idiom — `_decimal_string_round_trips_losslessly` / `_canonical_decimal_string` ported AS PRIVATE STATIC HELPERS local to `domain_event.gd`, NOT a cross-call to `ManualSeedLoader`): after `is_valid_int()` it requires `_canonical_decimal_string(text) == str(text.to_int())`, so an out-of-int64-range decimal string (e.g. `"9223372036854775808"` which wraps negative, or `"99999999999999999999"` which saturates) is now REJECTED with the stable `invalid_event_payload` + `field: root_seed`. Grep-confirmed `_has_decimal_string_payload` is used in EXACTLY ONE place (`_validate_run_started_payload`), so the tighten regresses no other event. The live emit path encodes `root_seed` via `str(root_seed)` (always in-range), so this hardens the VALIDATOR against hand-edited/foreign payloads without changing any emitted event. Pinned by the out-of-range + max-int64-boundary rejection assertions added to `test_domain_event.gd::_run_started_serializes_and_parses_stable_payload` (the epic-4 retro ratified: "any new seed-string validator must adopt the 3.7 lossless/canonicalize idiom, not bare `is_valid_int()`").

**Defer re-affirmed + KEPT-TRACKED by Story 4.6 (the 3.7 `GenerationResult.seed` success-path split — owner moved to 4.6 by the 4.4 review):**

- [Review][Defer → re-affirmed, KEPT-TRACKED] (Low) `GenerationResult.seed` is populated ONLY on the error path; on a SUCCESS result it is `""` and the seed lives in `payload.level_seed`. Story 4.6 is the FIRST 4.x story to actually run `LevelGenerator.generate(...)` and consume a SUCCESS-path level result (the combat-node generation in `RunOrchestrator._resolve_combat`). DISPOSITION CHOSEN: the orchestrator reads `payload.level_seed` on success (NEVER `result.seed`), the minimal SAFE path — and 4.6 did NOT fold the 3.7 fix (it did NOT touch the settled `GenerationResult` contract — `generation_result.gd` is byte-identical, NOT in the 4.6 diff). The defer therefore STAYS OPEN with this re-affirmed disposition. Pinned: `test_run_orchestrator.gd` asserts a combat node generates a playable level with a non-empty `level_seed` READ FROM `resolved.metadata.level_seed` (sourced from `payload.level_seed`), never `result.seed`. Fix (when a future story next touches `GenerationResult`): populate `seed` on the success path too and add a regression asserting a successful `generate(...)` carries the seed string. Not blocking — no AC depends on it. Owner: the next story that touches `GenerationResult` (still tracked).

**AC4 playtest deliverable (route-information-changed-my-decision moment + missing tradeoffs) — tracked note, human-feel pass pending:**

The headless shell is genuinely PLAYABLE/observable: a 4.2-generated route is fully traversable from a fresh seed to a `run_completed` endpoint via `RunOrchestrator`, the reveal-gated `eligible_choice_ids()` + per-node clue tags (Story 4.2 `RouteNode.clues`) are the information surface a tester chooses against, and `test_run_orchestrator.gd` records the concrete structural evidence (a deterministic walk that resolves every node by type, surfaces the per-node clue-driven choice points, and reaches the boss). The STRUCTURAL evidence a route-information moment CAN attach to is present (clue tags differ by node type/depth; reveal-on-arrival means a tester sees the next tier's clues only after committing, so a clue genuinely informs the next choice). The HUMAN-FELT "route information changed my decision" note is the tester's to complete on a real play pass (exactly as the Story 1.11 / 3.7 playtest ACs did — the dev shell makes it observable; the felt note is a human deliverable). Missing-tradeoff route tuning notes: the v0 clue economy is a defensible placeholder (the 4.2 `_assign_clues` type-driven base clue + one `map`-draw secondary), NOT a tuned tradeoff system — exact clue weighting / a richer tradeoff vocabulary is Epic-10 tuning (GDD line 708), tracked there. NO clue/generation change was made in 4.6.

**AC5 pacing observations (structural surface measured; minute targets are a tester-note overlay) — tracked note:**

v0 has NO real per-node content time (placeholder nodes resolve instantly; combat is auto-resolved on successful level generation), so the literal "20-35 min / 5-15 min / 45 min" wall-clock CANNOT be measured honestly from the headless shell — those minute targets are recorded as a TESTER-NOTE OVERLAY on the structural data, pending a real human play pass. The STRUCTURAL pacing surface 4.6 MEASURES + RECORDS (deterministic, `test_run_pacing_survey.gd` + `tools/dump_run_pacing_survey.gd` over an 18-seed sample `[0,1,2,3,5,7,11,13,17,42,99,100,256,314,777,1000,2026,9999]`):
- **Route length / tier count: CONSTANT 8 tiers for every seed** (boss always at depth 7 — observed `BOSS_DEPTH_DIST = {7: 18}`). This is the known constant-route-depth pacing CHARACTERISTIC flagged by the 4.2 `[Review][Decision → tracked follow-up]` (owner Epic-10 or 4.6). 4.6 MEASURES + flags it; the FIX (seed-varied route depth for more structural replay variety) is the Epic-10 node-weighting/pacing pass — NO generator change was made in 4.6 to vary depth.
- **Non-boss node-count distribution (observed): `{8: 7, 9: 2, 10: 6, 12: 3}`** across 18 seeds — all bounded `[8,12]` (+1 boss). The `[8,12]` variation lives entirely in column WIDTH, never route LENGTH.
- **Node-type mix (observed totals across the sample): `combat 67, elite_combat 40, shop 20, event 18, reforge 11, secret 7, gambling 7, boss 18`** — a genuine variety (combat-heavy early per the 4.2 depth-banded weighting, elites/support mid/late). Exact node FREQUENCY tuning is explicitly deferred (GDD line 708, Epic-10).

Pacing failures (if a human pass finds them) produce TUNING NOTES for node count / encounter length / reward frequency / boss approach — recorded here / in the story Completion Notes — rather than untracked scope expansion. NO generator/route-weight/recipe change was made in 4.6 to "fix" pacing (Epic-10 / tracked-follow-up territory; the constant route depth + exact node-frequency tuning are the 4.2 follow-up items owned there).

## Deferred from: code review of 4-6-playable-run-shell-from-start-to-end (2026-06-23)

Round 1 (primary adversarial code review, 2026-06-23): **APPROVE.** Critical 0 / High 0 / Med 2 / Low 2 — no blocking findings. Full headless suite GREEN (runner exit 0, "Headless tests passed.", 74 PASS / 0 FAIL), `git diff --check` clean (exit 0), diff confined to the 12 declared `godot/` files (the 4.2 route + Epic-3 Small/Medium/batch level fingerprints, the 23-key `RunSnapshot` gate, and the `run_completed`/`node_*`/`route_*` events + 4.3/4.4/4.5 command behaviors all untouched/byte-identical; `generation_result.gd` is NOT in the diff, confirming the 3.7 `GenerationResult.seed` read-around disposition). The two Med + two Low findings are recorded in the story's `### Review Findings` Round-1 subsection: one `[Review][Decision]` (a human call — the inert run-level `RngStreamSet`, tracked-forward below), one `[Review][Patch]` Med + two `[Review][Patch]` Low (all in-story next-touch fold-ins wholly inside this story's new orchestrator/save seam — NOT cross-story deferrals, so noted in-story rather than logged here, matching the 4.3/4.4/4.5 convention in this file).

Round 2 (independent re-review, alternate model, 2026-06-23): **APPROVE.** Critical 0 / High 0 / Med 0 / Low 1 — no blocking findings. The four Round-1 fixes (callback-position move; already-cleared no-op guard; compose-side seed-mismatch guard; `start_from` ActionResult/validation) were re-read against the current code + their regression tests and are VERIFIED correct and complete; the accepted `[Review][Decision]` (inert run-level `RngStreamSet`) was NOT reopened. Full headless suite re-run GREEN (runner exit 0, "Headless tests passed."), `git diff --check` clean (exit 0), and the settled generator/command/state/fingerprint files were independently re-confirmed byte-identical / NOT in the diff. **No new cross-story `[Review][Defer]` items were produced this round** — the single new Round-2 finding is a Low `[Review][Patch]` (the seed-mismatch cross-check is WRITE-side only; the resume/read side has no symmetric check, so a hand-edited/corrupt route-position save with divergent seeds restores silently) which is an in-story next-touch fold-in wholly inside this story's new save/resume seam, recorded in the story's `### Review Findings` Round-2 subsection, NOT logged here.

**Tracked-forward obligation (the `[Review][Decision]` disposition — owner Epic 6/7/9):**

- [Review][Defer] (Med, from a `[Review][Decision]` accepted as a v0 limitation) **The `RunOrchestrator`'s threaded run-level `RngStreamSet` is INERT in v0 — no run-affecting RNG is drawn through it, so the "route/level/RNG advance in lockstep" intent (AC1) is only partially realized and the route-position save persists a never-advanced stream.** `RunOrchestrator.start()` builds `streams = RngStreamSet.new(root_seed)` as a set independent of the one `RouteGenerator.generate(root_seed)` draws internally; during a run NOTHING draws from `orchestrator.streams` (`NodeEnterCommand` draws no RNG; `LevelGenerator.generate` mints its OWN `RngStreamSet.new(request.level_seed())` and `request.level_seed()` == `root_seed`; placeholder/exit/advance draw no RNG). Determinism + route/level reproduction are STILL correct (everything keys off `root_seed`), so no AC's observable outcome fails today — this is why it is accepted as a documented v0 limitation rather than a blocking fix. BUT it is a latent trap: when Epic 6/7/9 introduce real run-affecting RNG (combat resolution, rewards, event rolls), those draws MUST be routed through the orchestrator's `streams` (and `LevelGenerator.generate` must accept an injected run-level `RngStreamSet` instead of minting a local one), otherwise the route-position save/resume seam will silently persist a stream disconnected from where draws actually happen, breaking interrupted==uninterrupted determinism once RNG advances mid-run. Also note: `test_run_route_position_save.gd::_route_position_rng_round_trips_and_reproduces_next_draw` currently gives false confidence (it round-trips the never-advanced fresh stream and would pass even if `streams` were removed from the run lifecycle). **Fix (owner = the Epic 6/7/9 story that first draws run-affecting RNG through the orchestrator):** thread the orchestrator's run-level `RngStreamSet` into `LevelGenerator.generate(...)` (and any new run-level draw site) so generation/combat/rewards advance the SAME set the route-position save persists, and replace the inert-stream RNG round-trip assertion with one that draws through the run BEFORE saving. Not blocking for 4.6 (the v0 run is a pure function of `root_seed`). Owner: Epic 6/7/9 (tracked).

## MVP placeholder / de-scope checkpoint (Story 4.5, 2026-06-21)

This is the AC4 deliverable for Story 4.5 (MVP Node Types and Boss Placeholder): the auditable, single-location ledger of every MVP node-type placeholder introduced by 4.5, with the stable marker/cue ids, the affected node-type set, the forward owners, and the explicit replace/de-scope/blocking disposition. It is the readiness/de-scope checkpoint the 2026-06-04 readiness patch assigns to Stories 4.5/7.5/8.2/10.7 (epics line 401). Each placeholder below is a **tracked MVP limitation to be REPLACED by its owning epic — NOT shipped as final**; none is currently de-scoped, and none blocks the route shell being playable (the route is fully traversable start-to-end with no soft-lock, which is exactly what 4.5 delivers).

**Placeholder marker / cue ids introduced (the stable vocabulary downstream stories key off):**

- `node_placeholder_resolved.resolution = placeholder_completed` — the stable resolution marker emitted for EVERY placeholder node resolution (the five non-combat types AND the boss). Carried on the new `node_placeholder_resolved` system `DomainEvent` (lower_snake, value-pinned by the event validator + the command constant `NodeResolvePlaceholderCommand.RESOLUTION_PLACEHOLDER`).
- `run_completed.outcome = boss_placeholder` — the stable run-end outcome marker emitted when the boss placeholder resolves the run to `PHASE_COMPLETED`. Carried on the new `run_completed` system `DomainEvent` (lower_snake, value-pinned by the event validator + the command constant `NodeResolvePlaceholderCommand.BOSS_OUTCOME`).

**Affected node-type set + behavior (what is placeholder vs real today):**

- `shop`, `reforge`, `gambling`, `event`, `secret` — resolve as **no-op placeholders** via `NodeResolvePlaceholderCommand`: transition `ACTIVE_ROUTE -> NODE_RESOLUTION`, emit `node_placeholder_resolved` (`resolution = placeholder_completed`), then the existing `NodeExitCommand` clears + returns to `ACTIVE_ROUTE` exactly like a combat node. There is NO real shop UI / reforge logic / gambling / event-choice / secret-lore reveal, NO reward/currency entity, and NO `RewardTableDefinition` in v0 — "placeholder completion" is a deterministic event + metadata flag only.
- `boss` — resolves as a **placeholder run-END** via the SAME `NodeResolvePlaceholderCommand` (boss path): clear the boss + transition `ACTIVE_ROUTE -> NODE_RESOLUTION -> COMPLETED`, emit `node_placeholder_resolved` + `run_completed` (`outcome = boss_placeholder`). There is NO real Larval Avatar boss level + victory in v0 — the boss is a placeholder run-end at the correct node boundary.
- `combat`, `elite_combat` — NOT placeholders (out of this checkpoint): they are the real combat level entry/exit (Story 4.4, `NodeEnterCommand` / `NodeExitCommand`), unchanged by 4.5.

**Forward owners + replace/de-scope/blocking disposition (each REPLACE, none de-scoped, none blocking the shell):**

- Real **shop** + **reforge** systems — **REPLACE**, owner **Epic 6/7** (concrete loot/rewards are Epic 6; the risk economy + affinities are Epic 7). They hook the SAME `shop`/`reforge` node boundary (resolve through the placeholder command's node boundary; swap the placeholder resolution for the real system without changing the route model or node types).
- Real **gambling** + **event** (risk/reward) + **secret** (secret/lore) + the **risk economy** — **REPLACE**, owner **Epic 7**. Same node-boundary swap.
- Real **Larval Avatar boss level + victory** — **REPLACE**, owner **Epic 9**. Epic 9 reuses the SAME `boss` node + the SAME `run_completed` boundary 4.5 ships, swapping ONLY the boss's pre-completion behavior (run a real boss level + a real victory) — NOT the `run_completed` event, the route model, or the `boss` node type. AC3's "future Larval Avatar implementation can replace the placeholder through the same node boundary" is satisfied by this design.
- The full **start-to-end playable shell** + `run_started` emission + pacing measurement + route-position save/resume + wiring the type-dispatch into a run orchestrator/HUD — owner **Story 4.6**. 4.6 **CONSUMES** the `run_completed` boundary 4.5 ships (its AC "a deterministic run-ended event is emitted ... later meta progression can consume that event" is satisfied by 4.5's `run_completed`) — it must NOT re-create or rename it. 4.6 also owns the still-open `run_started` `node_count` raw-JSON-number + `_has_decimal_string_payload` loose-`is_valid_int()` defers (4.5 emits no `run_started` and did not touch them).

## Deferred from: code review of 4-5-mvp-node-types-and-boss-placeholder (2026-06-21)

Round 1 (primary adversarial code review, 2026-06-21): **APPROVE**. **No cross-story `[Review][Defer]` items were created.** Story 4.5 (the `NodeResolvePlaceholderCommand` run-domain command that safely resolves the five non-combat MVP node types `shop`/`reforge`/`gambling`/`event`/`secret` as no-op placeholders AND the `boss` placeholder as a run-END, plus the two new SYSTEM `DomainEvent`s `node_placeholder_resolved` + `run_completed`, plus a comment-only clarification on `NodeEnterCommand`'s `unsupported_node_entry` branch, plus the AC4 placeholder/de-scope catalog above) met all four ACs with strong, independently-verified tests. Full headless suite GREEN (runner exit 0, "Headless tests passed.", 70 PASS / 0 FAIL incl. the two new test files + the unchanged-behavior `test_node_enter_command.gd`), `git diff --check` clean (exit 0), and the diff is confined to the 6 declared `godot/` code files — NO generator / route-fingerprint / Epic-3 level-fingerprint / `RunSnapshot`-top-level / autoload / phase-transition-table perturbation (the 23-key snapshot gate, the 4.2 route fingerprints, and the Small/Medium/batch level fingerprints all re-ran GREEN). The command draws ZERO RNG (snapshot-proven), is byte-identical no-mutation on every reject, gates both boss event sequence ids `> 0` before any state read/mutation, and builds the boss's two events only after both transitions succeed.

The two Round-1 Low findings are non-blocking `[Review][Patch]` next-touch fold-ins recorded in the story's `### Review Findings` (NOT deferred-work items): (1) the boss-clear idempotency guard's "already present" branch is unexercised and the boss path is never called twice (a `test_node_resolve_placeholder_command.gd` fold-in); (2) a doc-precision nit on the `run_completed` validator's `cleared_node_count` "8-13" bound (it conflates per-route node COUNT with traversed PATH length on the fixed-8-tier route — comment-only, no behavior impact). Neither is a cross-story deferral, an AC gap, or a `[Review][Decision]` (no human call). The dev's two `[Decision]`s in Completion Notes (the 4.5/4.6 `run_completed`-boundary split that 4.6 must CONSUME not re-create; the boss-emits-two-events / Option-A type-dispatch choice) were reviewed and concurred with — no new decision raised.

Round 2 (secondary, alternate-model adversarial review, 2026-06-22): **APPROVE**. **No NEW cross-story `[Review][Defer]` items were created** — nothing in this story's diff crosses to another story. Independent re-verification of the WHOLE diff (not just the Round-1 fix) at full reasoning depth, with the reused contracts (`run_state.gd` / `route_state.gd` / `route_node.gd` / `node_exit_command.gd` / `route_advance_command.gd`) read in full to re-derive the correctness claims first-hand: full headless suite re-run GREEN (runner exit 0, "Headless tests passed.", 71 PASS / 0 FAIL), `git diff --check` clean (exit 0), diff confined to the 6 declared `godot/` code files (no generator / route-fingerprint / Epic-3 level-fingerprint / `RunSnapshot`-top-level / autoload / transition-table perturbation; the 4.2 route + Small/Medium/batch level fingerprints and the 23-key gate all re-ran GREEN/byte-identical). Both Round-1 Low `[Review][Patch]` fixes were re-verified correct and complete (the `_boss_resolve_idempotent_when_boss_already_cleared` test genuinely exercises the previously-unexercised idempotency guard branch; the `_validate_run_completed_payload` comment rewording dropped the imprecise "8-13" bound, comment-only). The boss mutate-before-transition half-state was independently confirmed UNREACHABLE against `_legal_next_phases` (both boss edges always-legal from their pinned source states). Round 2 produced NO new findings of any severity (Critical/High/Med/Low all 0), NO new `[Review][Patch]`, NO `[Review][Defer]`, and NO `[Review][Decision]`. Findings recorded in the story's `### Review Findings` Round-2 subsection.

## Deferred from: code review of 4-4-node-entry-and-exit-resolution (2026-06-21)

Round 1 (primary adversarial code review, 2026-06-21): APPROVE. **No NEW cross-story `[Review][Defer]` items were created.** Story 4.4 (the `NodeEnterCommand` + `NodeExitCommand` run-domain commands that bridge route↔level flow, plus the three new SYSTEM `DomainEvent`s `node_entered`/`node_exited`/`route_sealed` with the `door_sealed_placeholder` cue, plus the one-line `RouteAdvanceCommand` left-node-clear idempotency guard) met all four ACs with strong, independently-verified tests; full headless suite GREEN (runner exit 0), `git diff --check` clean (exit 0), diff confined to the 9 declared `godot/` files (no generator / fingerprint / `RunSnapshot`-top-level / repository perturbation — the 23-key snapshot gate, the 4.2 route fingerprint, and the Epic-3 Small/Medium/batch level fingerprints are untouched). The three Round-1 Low findings are non-blocking `[Review][Patch]` next-touch fold-ins recorded in the story's `### Review Findings` (not deferred-work items): a missing negative-`root_seed` enter test for the `invalid_level_request` branch; an exit mutate-before-(infallible)-transition ordering nit; an undocumented shared-by-reference `level_request` metadata contract.

This review's two `[Review][Decision]` items are DISPOSITIONS of existing ledger deferrals (recorded here so the epic-4 closeout reflects them), not new deferrals:

- [Review][Decision → RESOLVED] The 4.1 `[Review][Decision → Deferred]` "phaseless route payload → `PHASE_NEW_RUN` resume default, OWNER = Story 4.4" (logged under "Deferred from: code review of 4-1" below) is now CLOSED. Story 4.4 RATIFIED the existing `PHASE_NEW_RUN` default (no code change — internally consistent, `validate()` passes, the live writer always emits `run_phase`) and added the missing test (`godot/tests/unit/run/test_run_state.gd::_phaseless_route_payload_resumes_as_new_run`: a `route_state` payload with no `run_phase`, retaining a non-empty current pointer + a cleared node, resolves to `PHASE_NEW_RUN` and still validates). Reviewer concurs the ratification is sound. The epic-4 closeout can archive the 4.1 item as RESOLVED.
- [Review][Decision → owner transfer] The 3.7 `[Review][Defer]` `GenerationResult.seed` success-path split (logged under "Deferred from: code review of 3-7" below as OWNER = Story 4.4) stays OPEN, with the owner moved to **Story 4.6**. Story 4.4 builds a `GenerationRequest` but never runs `LevelGenerator.generate(...)`, so it consumes no success-path level `GenerationResult` and cannot hit the `result.seed == ""` footgun (and reads `result.seed` nowhere). The fix legitimately belongs to the first story that actually runs level generation (4.6, the playable start-to-end shell). Action for the ledger: treat the 3.7 entry's owner as Story 4.6 going forward. Not blocking for 4.4.

## Deferred from: code review of 4-3-route-choice-and-forward-commitment (2026-06-21)

Round 2 (independent second-reviewer pass, 2026-06-21): no new cross-story deferred-work items. APPROVE. The four Round-1 Low findings were verified RESOLVED/sound with no regression; the single NEW Round-2 finding is a non-blocking Low `[Review][Patch]` test-coverage fold-in (the `revealed_node_ids` duplicate-rejection validator branch lacks a dedicated negative test — next touch of `test_domain_event.gd`), recorded in the story's `### Review Findings` Round-2 subsection. It is a `[Review][Patch]`, not a `[Review][Defer]`, and not a `[Review][Decision]`; full headless suite GREEN (runner exit 0), `git diff --check` clean, code diff is the 6 declared `godot/` files only (no generator/snapshot/fingerprint perturbation).

No cross-story deferred-work items. The Round-1 APPROVE review of Story 4.3 (the `RouteAdvanceCommand` route CHOICE / forward-commitment command — the first run-domain command — plus the reveal-gated `RouteState.eligible_choice_ids()`/`is_eligible_choice()` selection filter and the new `route_advanced` system `DomainEvent`) produced four findings, all non-blocking Low `[Review][Patch]` fold-ins recorded in the story's `### Review Findings` and scoped to the next touch of `route_advance_command.gd` / `domain_event.gd` (caller-supplied `sequence_id` not gated `> 0`; terminal-phase `wrong_run_phase` negatives not pinned; `invalid_context` discards the inner structural error code; a "de-dup" doc/impl mismatch on the factory). None is a cross-story deferral, none is a `[Review][Decision]`, and none is an AC gap. The full headless suite is GREEN (runner exit 0, "Headless tests passed."), `git diff --check` is clean (exit 0), the diff is the 6 declared files only (no `RunSnapshot`/`RunState`/`RouteNode`/generator/save change — the 23-key snapshot gate, the 4.2 route fingerprint, and the Epic-3 Small/Medium/batch level fingerprints re-ran GREEN/byte-identical), and all three ACs are met. This review also CLOSES the standing 4.1 Round-1 deferral "`RouteState.available_choice_ids()` has NO reveal gating — OWNER = Story 4.3" (the new `eligible_choice_ids()` reveal-gated/cleared-excluded filter, validated by the command, is the deferred filter; the epic-4 closeout can archive that item as RESOLVED).

## Deferred from: code review of 4-2-seeded-8-12-node-route-generation (2026-06-18)

Round 1 produced no cross-story deferred-work items from the Approve review of Story 4.2 (the seeded, deterministic, scene-free `RouteGenerator` under `scripts/generation/route/` that draws the `map` stream EXCLUSIVELY to build an 8-12-non-boss-node forward-only DAG + one terminal boss inside a 4.1 `RouteState`, with deterministic ids/types/forward-edges/reveal-state/clues, plus the new `RouteValidator` forward-only edge pass and the `map`-seed route-regression fingerprint suite + dump tool). The full headless suite is GREEN (runner exit 0, "Headless tests passed."), `git diff --check` is clean (exit 0), the diff is purely additive (5 NEW files, ZERO existing-file modifications), the three Epic-3 LEVEL fingerprints are byte-identical to `main` (the `level` stream was not perturbed), the reviewer independently re-ran `tools/dump_route_fingerprints.gd` and confirmed all 8 pinned route fingerprints match byte-for-byte (not hand-edited), and the forward-only + full-reachability + forced-branch-point invariants were EXHAUSTIVELY verified offline for every target in [8,12] across every legal column-width assignment. All four ACs are met and scope was held tight (no commit command / choice filter / level-request / snapshot / `run_started` pulled forward; `available_choice_ids()` untouched). The Round-1 review notes are recorded in the story's `### Review Findings` and are next-touch fold-ins (two Low `[Review][Patch]`: a FIXED-DRAW-ORDER header imprecision for the depth-0 start node that draws no type selector; an AC4 branch-clue test slightly weaker than the note's literal phrasing though the property holds by construction) or a forward design note (one Low `[Review][Decision]`: route DEPTH is constant at 8 columns for every seed — the [8,12] variation is all in column WIDTH — a defensible v0 the Epic-10/Story-4.6 pacing pass may revisit). The ONE Round-2 cross-story deferral is logged below.

- [Review][Decision → tracked follow-up] (Low) Route DEPTH is constant for every seed: the boss placeholder is always at depth 7 (depth-0 start + `RouteGenerator.INTERIOR_COLUMN_COUNT = 6` interior tiers + boss), so every run is exactly 8 tiers long; the 8-12 node-count variation lives entirely in column WIDTH, never route length [godot/scripts/generation/route/route_generator.gd:73,227]. This is a defensible deterministic v0 (AC1/AC3 bound node COUNT, not depth, and a fixed-length layered DAG satisfies them) and required NO 4.2 code change — but constant run-length is a noticeable pacing characteristic, so at the Phase 7 human checkpoint (2026-06-18) the call was to TRACK it as explicit backlog rather than leave it only as a story-file note. Follow-up: evaluate seed-varied route depth (a variable number of tiers from start to boss) for more structural replay variety. Not blocking for 4.2. Owner: the Epic-10 node-weighting/pacing pass or Story 4.6 (playable start-to-end shell). (Originating review: code review of 4-2, Round 1 `[Review][Decision]`, human-confirmed for tracking 2026-06-18.)
- [Review][Defer] (Low) The route seed-regression fingerprint excludes `clues` and `reveal_state` [godot/scripts/generation/route/route_generator.gd:166-185 `fingerprint()` emits `count|id:type@depth ...|edges|bossN` only]. This faithfully pins AC2's literal five fields (node count, ids, types, links, boss placeholder depth) and is correct for AC2 — but it means an intentional-yet-unnoticed change to clue VALUES alone (editing `_assign_clues`'s match arms without changing any `map`-draw COUNT or position) would NOT trip the regression fixture. Clue logic is exactly the future tuning surface (GDD line 708, Epic 10) most likely to be edited. Clue drift is only PARTIALLY covered today: `test_route_generator.gd::_clue_assignment_is_seed_stable` asserts regen-determinism (not a PINNED value), and a clue-DRAW reorder WOULD perturb the fingerprint indirectly (it desyncs the `map` stream and shifts downstream type/edge draws). The genuine residual gap is a value-only clue edit that keeps the draw order intact. Fix (when the Epic-10 node-weighting / clue-tuning story, or 4.5/4.6, next touches clue assignment): extend the route fingerprint to append a per-node `clues` segment (and optionally `reveal_state`) and re-pin via `tools/dump_route_fingerprints.gd` in that same PR, so clue tuning is pinned by the same tripwire as topology. Not blocking for 4.2 — AC2 is satisfied by the current five-field pin and clues are an AC4 concern with separate determinism coverage. Owner: the Epic-10 node-weighting/clue-tuning story (or whichever of 4.5/4.6 first edits clue assignment). (Originating review: code review of 4-2, Round 2, 2026-06-18.)

## Deferred from: code review of 3-7-manual-seed-level-loader-and-regression-tests (2026-06-17)

One non-blocking Low note from the Round 1 Approve review of Story 3.7 (thin `ManualSeedLoader` over the complete 3.1-3.6 `LevelGenerator.generate(...)` pipeline + the first `generate`-level batch seed-regression harness + the preserved annotated approved-seed catalog). The full headless suite is green (runner exit 0, "Headless tests passed."), `git diff --check` exit 0, no existing file modified (only 4 new Godot files), Small AND Medium seed-regression fingerprints GREEN with NO re-pin (and the batch catalog fingerprints match those fixtures verbatim + are self-cross-checked against live `generate_layout`), and all four ACs are met with strong, independently-verified tests. The item below is not an AC gap or blocker. (Two `[Review][Decision]` items — the deliberate float-seed rejection, and accepting the 3-6 Round 1 Low's closure via the full-`generate` terrain-pin arm without a broad 1..200 sweep — plus one `[Review][Patch]` Low — an out-of-int64-range decimal string saturating in `parse_seed`, identical to the canonical `RngStreamSet._int64_from_value` and fold-in on the next touch of `manual_seed_loader.gd` — are recorded in the story's Review Findings and are NOT deferred-work items.)

- [Review][Defer] (Low) `GenerationResult.seed` (String) is populated ONLY on the error path; on a SUCCESS result it is `""` and the seed is carried in `payload.level_seed` instead. This is a PRE-EXISTING `GenerationResult` contract wart (NOT introduced by Story 3.7) that already cost a first-pass 12-test miss during 3.7 dev (the loader/batch tests initially asserted `result.seed` on success and had to be corrected to read `payload.level_seed`) and is a latent footgun for any future success-path consumer of `GenerationResult`. Story 3.7 could not fix it without modifying the settled `GenerationResult` contract, which is explicitly OUT OF SCOPE for this story (the loader CALLS the pipeline; it does not change its contracts). Fix (when a future story next touches `GenerationResult`): populate `seed` on the success path too (e.g. set `result.seed = new_seed` in `GenerationResult.ok` callers / `_ok_with_payload`, or have `ok(...)` accept the seed), so success and error results expose the seed uniformly — and/or document the split at the `GenerationResult` definition. Add a regression asserting a successful `generate(...)` result carries the seed string. Not blocking — no AC depends on it, and both new 3.7 tests correctly read `payload.level_seed` on success and `result.seed` on the error path. (Originating review: code review of 3-7, Round 1, 2026-06-17.)

## Deferred from: code review of 3-6-generator-validation-and-bounded-retry (2026-06-16)

One non-blocking Low coverage note from the Round 1 Approve review of Story 3.6 (comprehensive `LevelValidator` + bounded deterministic retry + per-phase fixtures). The full headless suite is green (runner exit 0, 57 PASS / 0 FAIL), `git diff --check` is clean, the working tree is clean, and all four ACs are met with strong, independently-verified tests (independent entity-aware floods for the soft-lock / enemy-reachability / mandatory-reward checks; pure-query no-RNG assertion via per-stream `draw_index` invariance; attempt-0-unperturbed invariant with Small + Medium fingerprints GREEN and NO re-pin; injected-validator seam exercising the cap + fail-then-pass + deterministic perturbation; distinct per-phase `(failed_phase, error_code)` pairs separate from the fingerprint tests). The two overlapping 3.5 deferred Lows (#2 entity-aware reward reachability, #3 end-to-end `unreachable_reward -> PHASE_VALIDATION`) were CLOSED by this story (marked RESOLVED above). The item below is not an AC gap or blocker. (A separate `[Review][Patch]` Low — stale check-letter labels + a contradictory "Chebyshev <= 1" inline comment in `level_validator.gd` doc text, code correct — is recorded in the story's Review Findings for fold-in on the next touch of that file and is NOT a deferred-work item.)

- [Review][Defer] (Low) `LevelGenerator._is_unrecoverable_layout_error` omits two layout error codes that are DETERMINISTIC functions of `(recipe, dimensions)` and would recur identically on every retry attempt: `invalid_layout_shape` (a malformed grid in `generate_layout`/`build_board_snapshot`/the placer) and `no_realizable_wrinkle_kind` (a recipe whose `allowed_wrinkle_kinds` contains no v0-realizable kind, from `TacticalWrinklePlacer.place_wrinkles`). Not being in the unrecoverable set, hitting either would burn all `MAX_GENERATION_ATTEMPTS` (8) seed-perturbed re-rolls — each re-running the identical deterministic failure — before returning the structured error, instead of short-circuiting on attempt 1 like the sibling `missing_enemy_repository`/`no_placeable_enemy`/`invalid_layout_request|recipe|streams` codes do. UNREACHABLE for the baseline recipes today: both baseline recipes expose realizable wrinkle kinds (so `no_realizable_wrinkle_kind` never binds) and `generate_layout` always emits a `width*height` row-major grid (so `invalid_layout_shape` cannot bind through the integration path — same class as the 3-2/3-3 shape-guard Lows that documented these as hand-built-candidate-only). Pure WORST-CASE EFFICIENCY edge (wasted retries on an already-doomed generation), NOT a correctness defect: the final structured error is still correct, the determinism contract holds (same seed -> same final error), and the NFR4 < 3s budget is unaffected because the path is unreachable for current content. Same "correct by construction, the wasteful branch is unreachable for the baseline" family as the already-deferred 3-4/3-5 pool-exhaustion Lows. Fix (when a recipe/generator that can emit these at runtime is introduced — e.g. a future jittered/degenerate-board generator or a recipe with a non-realizable wrinkle allowlist): add `invalid_layout_shape` and `no_realizable_wrinkle_kind` to `_is_unrecoverable_layout_error` so they short-circuit, and add a regression asserting `attempts == 1` for a deterministic layout-construction failure. Not blocking — no current recipe/board can hit either code through `generate`, and no AC depends on the retry-vs-short-circuit distinction for these codes. (Originating review: code review of 3-6, Round 2, 2026-06-17.)

## Deferred from: code review of 3-5-enemy-and-reward-placement (2026-06-16)

One non-blocking Low robustness note from the Round 1 Approve review of Story 3.5 (deterministic enemy + reward placement in both layout generators). The full headless suite is green (runner exit 0), `git diff --check` (vs `main`) is clean, the working tree is clean, and all four ACs are met with strong, independently-verified tests (independent-BFS enemy/reward reachability for both sizes, the budget clamp actually exercised, hand-built unreachable-reward rejection with compact diagnostics, same-seed determinism + >=2-distinct divergence + `level`-stream-only/stream-isolation, the enemy-bearing board through the strict `try_from_snapshot` + real-JSON + `TacticalSnapshot` round-trip, occupant-invariant cross-check, and terrain seed-regression fingerprints GREEN with NO re-pin). The item is not an AC gap or blocker.

- [Review][Defer] (Low) `EntityRewardPlacer.place_rewards` silently UNDER-PLACES when a behind-danger cell is drawn under a recipe with `allow_reward_behind_danger == false`: it `continue`s after `pool.remove_at(picked_index)` (`entity_reward_placer.gd` lines 257-258), consuming the candidate without placing a marker and WITHOUT re-drawing another (non-hazard) cell to fill the slot — so the placed reward count can fall below the drawn count with no structured signal. UNREACHABLE for the baseline recipes: `small_combat_basic` (the only recipe with `allow_reward_behind_danger=false`) allows only `choke_point`/`blocker_cluster` wrinkles (both WALL), so Small never emits HAZARD and `_reward_behind_danger` is always false — the skip branch never binds (verified by `_small_never_places_behind_danger_reward` across 79 seeds). This is the same class as the already-deferred wrinkle/enemy pool-exhaustion Lows and is documented intentionally in the placer header. Residual gap: a future Small-class recipe that permits HAZARD wrinkles while keeping `allow_reward_behind_danger=false` could silently drop a reward with no loud signal. Fix (when such a recipe can occur): either re-draw a replacement cell from the remaining pool when a slot is skipped, or surface a structured under-placement signal (mirroring the existing fail-loud discipline), and add a regression recipe/board. Not blocking — no current recipe/board can hit it and no AC depends on it. (Originating review: code review of 3-5, Round 1, 2026-06-16.)

## Deferred from: code review of 3-4-tactical-wrinkles-blockers-and-hazards (2026-06-16)

One non-blocking Low robustness note from the Round 1 Approve review of Story 3.4 (deterministic tactical-wrinkle placement + the first HAZARD emission). The full headless suite is green (runner exit 0), `git diff --check` is clean, the working tree is clean, and all three ACs are met with strong, independently-verified tests (independent BFS reachability for both sizes, no-gate path assertion, strict-snapshot + real-JSON + TacticalSnapshot round-trips for the hazard, stream-isolation, determinism, fail-loud non-realizable allowlist). The item is not an AC gap or blocker. (The 3-3 `validate_readability` per-row width-guard Low was CLOSED by this story — see its entry below.)

- [Review][Defer] (Low) `TacticalWrinklePlacer.place_wrinkles` silently under-places when the shared candidate pool empties before the minimum is met (`tactical_wrinkle_placer.gd` lines 123-125). The placement loop breaks on `pool.is_empty()` and returns fewer than `min_tactical_wrinkles` wrinkles WITHOUT a structured error, unlike the explicit fail-loud `no_realizable_wrinkle_kind` path it uses for an empty realizable-kind set. Unreachable for the two baseline recipes: after the corridor/entrance/exit exclusion the Small board has ~30 interior candidates (max 5 blockers + min 1 wrinkle) and the Medium board ~108 (max 8 blockers + min 2 wrinkles), so the pool is always far larger than blockers + the wrinkle minimum; the per-recipe `>= minimum` count assertions in `test_tactical_wrinkle_placement.gd` are the tripwire and the placer header documents the behaviour intentionally. Residual gap: a future tiny/jittered board, or a recipe whose `min_tactical_wrinkles` is large relative to its blocker budget, could exhaust the pool and silently drop a required wrinkle with no loud signal. Fix (when a degenerate board can occur): after the loop, if `placed.size() < minimum`, return a structured error (e.g. `wrinkle_pool_exhausted`) carrying the placed/required counts, mirroring the existing fail-loud discipline, and add a tiny-board regression test. Not blocking — no AC depends on it and no current recipe/board can hit it. (Originating review: code review of 3-4, Round 1, 2026-06-16.)

## Deferred from: code review of 3-3-seed-stable-medium-level-layouts (2026-06-16)

Both are non-blocking quality/robustness notes from the Round 1 Approve review of Story 3.3 (Medium layouts + AC2 readability validation). The full headless suite is green (runner exit 0), `git diff --check` is clean, and all three ACs are met with real, independently-verified tests (incl. an independent BFS reaching the exit for all 5 pinned seeds and the first-reveal candidate isolating check (c)). Neither item is an AC gap or blocker.

- [Review][Defer] (Low) The `wall_density` recipe field is now DOCUMENTED as intentionally unused for v0 blocker-count derivation in BOTH generators (Task 3.3.4, option b — closing the 3.2 deferred Med item as an accepted decision), but it remains a fully dead field: `terrain_rules()` exposes `wall_density` and `medium_combat_basic` carries `0.28` (and `small_combat_basic` `0.22`) with zero layout effect, and the AC2 excessive-blockage ratio (`0.35`) is an independent readability constant, not derived from the recipe. This is the correct, well-reasoned v0 call (honoring density would clamp to a constant 8 blockers and collapse AC1 count divergence) and is NOT a defect — recorded so a future story that wants density-driven counts (or that removes/repurposes the field) knows the field is presently inert and that re-pinning the Medium AND Small fingerprints + widening the band would be required. Supersedes/closes-as-decision the 3.2 `wall_density` Med deferral below. (Originating review: code review of 3-3, Round 1, 2026-06-16.)

## Deferred from: code review of 3-2-seed-stable-small-level-layouts (2026-06-16)

All four are non-blocking quality/robustness/coverage notes from the Round 1 Approve review; the full headless suite is green and all three ACs are met with real tests. None is an AC gap or blocker.

- [Review][Defer] (Med) `wall_density` is silently ignored by `SmallLevelLayoutGenerator`. Task 3.2.2 lists it among the recipe terrain rules to "honor," but the interior blocker count is derived purely from `blocker_budget_min..max` (clamped to candidate count); `wall_density` / `terrain_rules()` is never read (no reference in `scripts/generation` or the generation tests). Baseline `small_combat_basic` carries `wall_density = 0.22` with zero layout effect. Defensible v0 simplification (the budget band is a concrete recipe-driven bound and satisfies AC1) and NOT a blocker, but the omission is undocumented in the Completion Notes. Fix: either wire `wall_density` into the blocker-count target (+ a density test, re-pinning fingerprints) or explicitly document it as intentionally unused in Small v0 with the budget band authoritative. (Originating review: code review of 3-2, Round 1, 2026-06-16.)
- [Review][Defer] (Low) `SmallLevelLayoutGenerator.build_board_snapshot()` indexes the layout grid (`terrain_grid[y]`, `row[x]`) without a shape/bounds guard on `width`/`height` vs the actual `terrain` dimensions. Safe today — its only callers pass `generate_layout` output, which always emits a `width*height` row-major grid, and the assembled cells route through the strict `BoardState.try_from_snapshot`. Residual gap: a hand-built/malformed layout dict (width/height disagreeing with the terrain grid) would index-out-of-bounds and crash rather than return a structured error. Not blocking — no current caller can hit it. Fix: add a leading shape check returning a structured `invalid_layout_shape` ActionResult, mirroring the null-input guards in `generate_layout`. (Originating review: code review of 3-2, Round 1, 2026-06-16.)
- [Review][Defer] (Low) The entrance-to-exit reachability guarantee is coupled to the fixed 8x8 footprint and an even `SMALL_HEIGHT` (`corridor_row = height / 2`, entrance/exit pinned to that reserved blocker-free interior row). Provably fair and well tested for the current `8x8` constants. Fragility: a future jittered Small size (called out as a future change in the Completion Notes) with an odd/very small height could land `corridor_row` on/adjacent to the border ring or degenerate the reserved-corridor invariant, silently weakening fairness with no test tripwire. Story 3.6 owns the formal reachability validator + retry that would tighten this; recorded here at the code level. Fix (when jitter is added): guarantee `corridor_row` stays strictly interior with a non-empty reserved span and add a parametric reachability test across the jittered size range. Not blocking for fixed-8x8 v0. (Originating review: code review of 3-2, Round 1, 2026-06-16.)
- [Review][Defer] (Low) The blocker-count clamp-to-candidate-count path in `_draw_blocker_count` (`maximum = min(maximum, candidate_count)`, `minimum = min(minimum, maximum)`) is unexercised: the baseline 8x8 board has 30 interior-floor candidates vs a 2..5 budget, so the clamp never engages. Correct by inspection (guarantees the position-draw pool never under-runs the requested count) and no AC depends on it. Fix: add a test recipe whose budget exceeds the available candidate count (or a future shrunk/jittered board) and assert the placed-blocker count equals the candidate count without error. (Originating review: code review of 3-2, Round 1, 2026-06-16.)

## Deferred from: code review of 3-1-generation-requests-results-and-level-recipes (2026-06-16)

All three are Low test-coverage gaps in `LevelRecipeDefinition.validate()` — the branches are correct by inspection; only their dedicated negative tests are missing, and none are in the story's enumerated required-negatives list (so none are an AC gap or blocker). Approve verdict; full headless suite green. Cheap to close: add three `_rejects_field(...)` cases to `godot/tests/unit/content/test_level_recipe_definition.gd` mirroring the existing per-field negative tests.

- [Review][Defer] (Low) The `not allow_blockers and blocker_budget_max > 0` validation branch (`level_recipe_definition.gd` lines 89-90) — a recipe that disallows blockers yet declares a positive blocker budget — has no negative test. Add a case setting `allow_blockers = false` + `blocker_budget_max > 0` and assert rejection on field `blocker_budget_max`. (Originating review: code review of 3-1, Round 1, 2026-06-16.)
- [Review][Defer] (Low) The `min_tactical_wrinkles < 0` branch (lines 101-102) is unproven on its own; only the combat-specific `min_tactical_wrinkles < 1` branch (103-104) is tested. A negative `min_tactical_wrinkles` (especially on a non-combat recipe, which would skip the 103-104 guard) exercises 101-102 untested. Add a case asserting rejection on field `min_tactical_wrinkles` for a negative value. (Originating review: code review of 3-1, Round 1, 2026-06-16.)
- [Review][Defer] (Low) The `min_tactical_wrinkles > 0 and allowed_wrinkle_kinds.is_empty()` coupling guard (lines 108-109) — a recipe that requires wrinkles must permit at least one wrinkle kind — has no negative test (the analogous `SupportDefinition` `bonus_damage`/`bonus_weapon_ids` coupling IS tested). Add a case with `min_tactical_wrinkles >= 1` and `allowed_wrinkle_kinds = []` asserting rejection on field `allowed_wrinkle_kinds`. (Originating review: code review of 3-1, Round 1, 2026-06-16.)

## Deferred from: code review of 2-9-settings-and-difficulty-non-goal-guardrails (2026-06-15)

- [Review][Defer] (Low) A syntactically-valid JSON settings file that is a Dictionary but MISSING the `schema_version` key (e.g. an empty `{}`, a minimal hand-written file, or a future "partial" settings file an external tool emits) takes the hard-error `unsupported_settings_schema` path, NOT the documented graceful fallback-to-defaults. Trace: `SettingsRepository.read_settings()` passes the `parsed is Dictionary` guard, calls `SettingsSnapshot.parse({})`, which computes `int(data.get("schema_version", -1)) == -1 != SCHEMA_VERSION(1)` and returns the schema-mismatch error. This collides with the story's stated "preferences must degrade gracefully so a slightly stale/partial settings file still loads" / "unreadable or malformed -> defaults" policy: a valid-JSON-but-schemaless object is treated as an incompatible-build mismatch rather than a recoverable partial. The seam is UNTESTED — `test_settings_snapshot.gd::_parse_honors_partial_settings_and_defaults_the_rest` always supplies `schema_version` in its partial dict, and no repository test feeds a `{}`/schemaless valid-JSON file. Impact is SAFE and NOT blocking: `SettingsManager._ready()` catches the error, logs a `push_warning`, and keeps in-memory defaults (the player is never hard-blocked at boot), no crash, no data corruption, and the repository's own writer always emits `schema_version` so the live round-trip never hits this. It is a policy/coverage gap, not a correctness defect. Fix (when convenient): either (a) in `read_settings`, treat a parsed Dictionary that lacks `schema_version` as the graceful `_defaults_result({recovered:true, recovered_reason:"settings_missing_schema"})` path (reserving `unsupported_settings_schema` for a PRESENT-but-mismatched version), or (b) explicitly document that a schemaless valid-JSON file is intentionally a hard mismatch and add a `read_settings({})`/schemaless regression test pinning whichever policy is chosen. (Originating review: code review of 2-9, Round 1, 2026-06-15.)

## Deferred from: code review of 2-8-resume-flow-and-mid-level-save-feasibility (2026-06-14)

- [Review][Defer] (Low) The `save_open_failed` resume failure path is untested. `SaveRepository.read_run_snapshot()` returns `save_open_failed` when `FileAccess.open` fails on an existing-but-unreadable save, and `RunResumeService.resume()` propagates it verbatim, but no AC2 test exercises it. Story 2.8 deliberately scoped AC2 to five deterministic, cross-platform-reproducible cases (`save_not_found`, `save_parse_failed`, `unsupported_save_schema`, `invalid_tactical_snapshot`, `invalid_rng_snapshot`) and did not include `save_open_failed` (a permission/locked-file condition awkward to trigger reliably on Windows/CI). Behavior is correct by inspection — the service returns the read error unchanged before exposing any restored state, so no-partial-state holds. Not blocking. Fix: if a deterministic unreadable-file harness is later available (e.g. a directory-at-save-path, or a held file lock), add a `save_open_failed` no-partial-state case to `test_run_resume_service.gd` for symmetry with the other read-error paths. (Originating review: code review of 2-8, Round 1, 2026-06-14.)

## Deferred from: code review of 2-7-between-level-save-snapshot-foundation (2026-06-14)

- [Review][Defer] (Low) `RngStreamSet.try_restore()` tolerantly accepts the full-64-bit `state` and `root_seed` fields encoded as a raw JSON float via `_int64_from_value()`'s `TYPE_FLOAT` branch. Production `to_snapshot()` always emits these as decimal strings so the live save/resume path is safe, but a hand-edited / machine-generated / future-format save storing `state` as a number beyond 2^53 would pass the `is_equal_approx(v, round(v))` integral-float check and be silently truncated by `int(numeric_value)` — re-introducing the precision loss the int64-string fix prevents, this time accepted rather than rejected. The float branch is needed for small bounded fields (`seed` <= 2^31, `draw_index`) but the unbounded fields arguably should accept only int or String. Fix: tighten `state`/`root_seed` to reject `TYPE_FLOAT` (or reject finite doubles with magnitude > 2^53) and add a regression test feeding a too-large numeric `state`. Not blocking — no production writer emits numeric `state`; all malformed-input rejection tests stay green. (Originating review: code review of 2-7, Round 1, 2026-06-14.)

_Two further Low items deferred from the 2-7 review (run-level vs embedded tactical `rng_streams` equality assertion; embedded-tactical next-draw reproduction in the round-trip) were RESOLVED in Story 2.8 and archived to `deferred-work-resolved.md` during the epic-2 closeout (2026-06-15)._

## Deferred from: code review of 2-6-accessibility-and-tactical-readability-baseline (2026-06-14)

- [Review][Defer] The `feedback_preview` false-positive guard (`kind == "attack"`) in `TacticalAccessibilityModel._preview_active_from_options()` has no regression test. `_feedback_maps_from_active_preview_and_committed_result` only exercises an attack-mode `commit_flow` and a committed result; no test passes a valid *movement* preview into `from_state({"preview": <move_preview>})` to assert `feedback_preview` stays inactive. The guard is correct today (movement previews carry `commit_available` but `kind == "move"`), but a future refactor could silently start activating `feedback_preview` for movement with no test to catch it. Add a case: build a valid movement preview via `TacticalMovementPreview.from_query(...)`, pass it as `preview`, assert `feedback.preview.active == false` and `feedback_preview` absent from `cue_ids`. Not blocking — current behavior verified correct by inspection. (Originating review: code review of 2-6, Round 1, 2026-06-14.)

## Deferred from: code review of 2-5-adaptive-layout-profiles (2026-06-13)

- [Review][Defer] Degenerate stacked-layout rebalance branch in `TacticalLayoutProfile._build_stacked_layout()` is untested. When a safe-area-shrunk content area is too short to fit board + four control bands + optional log strip, recomputed `control_height` can fall below the 44px minimum touch target. Behavior stays consistent (slots honestly report `reachable: false`, no crash) but no regression test covers it. Add a fixture with an extreme content-area shrink asserting either controls stay ≥ touch target + inside content, or slots report `reachable: false`. Not blocking for v0 — real target viewports do not reach this branch. (Originating review: Round 1, 2026-06-13.)
- [Review][Defer] v0 profile classifier (`TacticalLayoutProfile._profile_id_for()`) maps any `height < 700 and width > height` viewport to `phone_landscape` regardless of width, so large-but-short desktop windows (e.g. `1300x650`) get the compact phone side-rail layout with no distinguishing cue. Matches documented v0 thresholds and passes all five AC fixtures, but is a latent surprise for unusual desktop sizes. Defer to later device-tier threshold tuning (story keeps thresholds as named constants for exactly this); consider a width ceiling on `phone_landscape` or a `layout_low_height_desktop` cue. (Originating review: Round 1, 2026-06-13.)
- [Review][Defer] Populated `log_or_outcome` region (only non-empty on `tablet`/`desktop` profiles) is never asserted to stay inside `content_area` by any committed test — the safe-area test only exercises the `390x844` phone fixture, where `log_or_outcome` is empty, and the region is not a primary control so the reachability helper skips it. Behavior is correct by construction and was verified with a throwaway tablet+desktop offset-safe-area probe (passed), but the persisted suite leaves it uncovered. Add a tablet (and/or desktop) fixture with an offset safe area asserting the populated `log_or_outcome` strip stays inside the content area; could be combined with the rebalance-branch coverage defer above into one short-content + offset-safe-area regression test. Not blocking for v0. (Originating review: Round 2, 2026-06-13.)

## Deferred from: 2-5-adaptive-layout-profiles (2026-06-13)

- Optional scene/presenter proof (Story 2.5.7) was intentionally not built. The adaptive layout
  contract is fully proven scene-free through `TacticalLayoutProfile` plus the sanitized
  `TacticalBoardViewModel.layout` slot, and state preservation is proven by feeding board,
  preview, commit-flow, inspect, zoom, and action-availability contracts through
  `TacticalBoardViewModel.from_domain()` across profile changes in headless tests. A
  `Control`/scene presenter under `godot/scripts/ui/presenters/` and
  `godot/scenes/ui/layouts/<profile>/` is left for a later story that grows the polished HUD,
  per the architecture guidance to keep tactical layout decisions in a testable semantic
  profile first (Story 2.5 Dev Notes "Latest Technical Information").

## Deferred from: code review of 1-3-actionresult-and-domain-event-foundation (2026-06-05)

- Board snapshot cell parsing still coerces malformed cell fields and lacks a `cells` container type guard. `BoardState.try_from_snapshot()` assigns `cells` directly to a typed `Array`, while `BoardCell.from_dictionary()` coerces missing or malformed position, terrain, visibility, and explored values before validation can reject the corrupt snapshot.
- Board entity snapshot restore has unresolved occupant-schema migration and consistency behavior. Cell-only occupant snapshots from the earlier board format now fail without a schema migration, while snapshots with entities but missing matching cell occupants can be accepted and restored into a different shape.
- Mutable `get_cell()` access can bypass new entity occupancy invariants. External callers can mutate the stored `BoardCell.occupant_id` directly and desynchronize `_cells` from `_entities`; deciding whether to return read-only copies or add setup-only mutators belongs with the board snapshot/domain API cleanup.
