---
baseline_commit: 4408c785af65e19bebf79b9e41d06669c3455a6d
---

# Story 7.1: Risk Economy State

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want gold, healing pressure, curses/corruption, and Oath Shard eligibility to be tracked clearly,
so that risk choices have visible consequences.

## Acceptance Criteria

(Verbatim from `planning-artifacts/epics.md` lines 1867-1888, Story 7.1; FR54.)

**AC1 — Initialization + domain/save presence**
- **Given** a run starts
- **When** risk economy state is initialized
- **Then** it tracks gold, healing resources or availability, curse/corruption state, Oath Shard eligibility, and risk flags
- **And** the fields are part of domain state **and save snapshots**.

**AC2 — Deterministic economy events + explanation log**
- **Given** gold or healing changes
- **When** an economy command or reward outcome resolves
- **Then** deterministic currency or healing events are emitted
- **And** the explanation log records the reason.

**AC3 — Invalid change rejection (no mutation)**
- **Given** invalid economy changes are requested
- **When** validation fails
- **Then** the command returns a stable error
- **And** currency, health, curse, and reward state remain unchanged.

## Scope boundary (read FIRST — this story is STATE + COMMANDS + SAVE, not the full risk economy)

This is the **opening story of Epic 7** and the **headline handoff from Epic 6**: it introduces the live risk-economy domain field that the whole Epic-6 loot/passive layer has been *recording against but not applying* (the retro's HIGH item T1). Keep scope tight to the AC:

**IN scope (this story):**
1. A new `RiskEconomyState` run-domain value object (scene-free `RefCounted`, recorded on `RunState`) tracking **gold**, **healing availability**, **curse/corruption state**, **Oath Shard eligibility**, and **risk flags** — the additive-lenient `RunState` field pattern (the exact 5.3 `starting_kit` / 6.2 `inventory` shape).
2. One (or a small set of) caller-driven run-domain **economy command(s)** that mutate gold/healing through the 4.3 run-command idiom (validate-then-mutate, reject `sequence_id <= 0` first, byte-identical no-mutation on reject), emitting **deterministic** currency/healing `DomainEvent`s carrying the reason (AC2's explanation log).
3. **Wire the gold credit off the EXISTING Epic-6 `reward_resolved` `category == gold` hook** — the one Epic-6 recorded-outcome hook that is purely economic (no curse/event semantics). This makes a gold reward FELT (the wallet rises) instead of merely recorded. This is the central T1 wire-off the retro assigns here.
4. **Persist economy state into the save** (AC1's "and save snapshots") — see the SAVE STRATEGY section below; this is the one scope item that genuinely differs from Epic 6 (inventory/offer were off the route-position save).
5. Full unit tests: command valid/invalid-no-mutation, event factory/validator/round-trip/malformed, save round-trip + migration, determinism.

**OUT of scope (explicitly later Epic-7 stories — do NOT pull forward):**
- **Curse/corruption RULES + cursed-reward tradeoff UX + the cleanse/curse application** → **Story 7.2** (curse/corruption rules, FR55). 7.1 introduces the curse/corruption *state fields + the structural setter/event*, but does NOT author curse trigger windows, the rules-kernel curse resolution, or the cursed-reward view model. (The `passive_destroyed` "cleanse or reduce curse" outcome wiring is 7.2's, not 7.1's.)
- **Risk/reward EVENT CHOICES + the `events`-stream event roll + the "risk flags future systems can query" producers** → **Story 7.3** (FR54). 7.1 introduces the `risk_flags` *container + a structural setter*, but does NOT build the event node, the event definitions, or the `events`-stream draw.
- **Affinity definitions / assignment / tactical effects / Darkness** → **Stories 7.4-7.6** (FR56-FR58). The `RunSnapshot.affinities` placeholder key is NOT 7.1's to populate.
- **Oath Shard AWARDING / meta-profile** → **Epic 8** (FR59). 7.1 tracks Oath-Shard *eligibility* (a run-level flag, the gate that decides whether a finished run MAY award shards) — NOT the award amount, the meta profile, or the outpost. Do NOT introduce a meta-profile snapshot.
- **Live current-HP / the tactical-board damage→HP wiring.** v0 still has NO live tactical play loop in the headless orchestrator (combat auto-resolves). "Healing availability" is a run-economy *resource/charge count*, NOT a live tactical current-HP that combat decrements. Model healing as availability (the AC's exact word: "healing resources **or availability**"), not as a current-HP the board mutates. (Whether a future story unifies `starting_kit.baseline_hp` + a live current-HP is Epic 7/9's; 7.1 does not invent a board-coupled current-HP.)
- The **consumable heal/ward MUTATION off `item_consumed`** and the **Destroy-outcome MUTATION off `passive_destroyed`** — these wire healing/curse-removal off Epic-6 events. They are a natural sibling of item 3, but they touch healing/curse semantics that overlap 7.2 (curse) and need the heal-availability + curse model to be settled first. **Default: defer the `item_consumed`/`passive_destroyed` wiring to the story that owns the consumed effect (7.2 for curse-removal; a heal-availability application may land here IF the heal model is trivially a charge increment — but if it forces any curse coupling, defer it).** Record the chosen disposition in Completion Notes either way (do not silently skip).

If an AC seems to demand a curse RULE or an event ROLL, re-read: AC1 demands the curse/corruption **state** is *tracked* (a field + save), not that curses *trigger* (7.2) or that events *offer* them (7.3).

## Tasks / Subtasks

- [x] **Task 1 — `RiskEconomyState` run-domain value object (AC1)** — `godot/scripts/run/risk_economy_state.gd`
  - [x] Create a scene-free `RefCounted` value object mirroring `StartingKit`/`InventoryState` discipline: exact-key `to_dictionary()` (a `DICTIONARY_KEYS` const pinned by test — a key never silently appears/vanishes), lenient `try_from_dictionary` (value object, no reject path — a partial/legacy dict defaults cleanly), deep `copy()`, and a `_init(...)` with EVERY new param defaulted.
  - [x] Fields (AC1's five categories): `gold: int` (>= 0, default 0), `healing_charges: int` (>= 0, default 0 — a resource/charge count, NOT a live current-HP), `curse_count` + `corruption` (the curse/corruption tracking — MINIMAL count containers; NO curse rules), `oath_shard_eligible: bool` (the run-level eligibility gate, default derived), and `risk_flags: Array[String]` (lower_snake risk-flag ids; EMPTY in v0, 7.3 populates it via the `add_risk_flag` structural setter).
  - [x] `gold`/`healing_charges`/curse-corruption counts are **small bounded ints** (numeric in `to_dictionary()`, NO decimal-string encoding; the 5.3/6.2 `baseline_hp`/`capacity` precedent).
  - [x] **Oath-Shard eligibility invariant** enforced — see `[Decision] Oath-Shard eligibility` in Completion Notes. `oath_shard_eligible` is a NEW field kept in LOCKSTEP with `RunState.meta_progression_eligible` (the source of truth); `RiskEconomyState.validate(is_manual_seed)` asserts `oath_shard_eligible == not is_manual_seed`, and `RunState.validate()` calls it with the run's `is_manual_seed`.
  - [x] Added `godot/tests/unit/run/test_risk_economy_state.gd` pinning `DICTIONARY_KEYS`, the lenient decode (partial/absent/negative-clamp), the deep copy, the eligibility invariant (both seed modes), the gold/healing apply + floor guard, and the structural setters.

- [x] **Task 2 — Seat `RiskEconomyState` on `RunState` as an additive-lenient field (AC1)** — `godot/scripts/run/run_state.gd`
  - [x] Added `var risk_economy: RiskEconomyState` following the EXACT 6.2 `inventory` pattern: defaults to a fresh state (via `RiskEconomyState.for_run(new_is_manual_seed)` so eligibility derives at init; never null) with a `_risk_economy_or_new()` guard, new constructor param **LAST**, rides `to_dictionary()`/`try_from_dictionary` (lenient-read), `copy()` **deep-copies** it. NOT a required `validate()` field — but `RunState.validate()` DOES assert the economy's eligibility invariant when present (so eligibility can never silently diverge from the run's seed mode).
  - [x] Confirmed EVERY existing `RunState.new(...)` call site preserved (grep: 12 files; all pass positional args up to `route` — the new param is last + defaulted; full suite green).
  - [x] Extended `test_run_state.gd`: the default-eligible economy for a non-manual run, the never-eligible economy for a manual run, the full-run-dict round-trip + deep copy, a pre-7.1 dict (no economy key) parsing with the default (incl. the manual-run eligibility re-derivation), the eligibility-mismatch `validate()` reject, AND the nested `route_state` bridge round-trip (the AC1 "and save snapshots" — see Task 6).

- [x] **Task 3 — Economy command(s) + deterministic currency/healing events (AC2, AC3)** — `godot/scripts/core/commands/apply_economy_change_command.gd`
  - [x] Authored `ApplyEconomyChangeCommand` following the 4.3 run-command idiom VERBATIM: extends `game_command.gd`, takes the live `RunState` DIRECTLY, caller supplies `sequence_id` (default 1), `validate(state)` rejects `sequence_id <= 0` FIRST (`invalid_event_sequence_id`), validate-then-mutate, ZERO events + byte-identical no-mutation on ANY reject, builds the event ONLY AFTER the mutation.
  - [x] Single parameterized command (`new_gold_delta`, `new_healing_delta`, `new_reason`, `new_sequence_id`) — one validate-then-mutate path. **AC3 reject cases** covered with stable codes (offending value in `metadata`): `invalid_context` (non-`RunState` + structurally-invalid run), `invalid_economy_reason` (non-lower_snake/blank reason), `invalid_economy_change` (no-op — both deltas 0), `insufficient_gold` (spend below 0), `insufficient_healing` (spend below 0). Every reject: ZERO events, ZERO RNG, byte-identical run.
  - [x] **Events DETERMINISTIC** — the command draws ZERO RNG (grep-confirmed no `randi`/`randf`/`RandomNumberGenerator` in source; the only matches are in the doc-comment stating it draws none). The `economy_changed` payload carries `reason` (lower_snake) + `gold_before`/`gold_after`/`gold_delta` + `healing_before`/`healing_after`/`healing_delta` (before/after non-negative integral; deltas SIGNED integral; the validator also enforces `before + delta == after` arithmetic consistency). See `[Decision] economy_changed (unified event)` in Completion Notes.
  - [x] Added `godot/tests/unit/core/test_apply_economy_change_command.gd`: a valid credit/spend/heal/combined (field change + event payload), EACH reject branch (byte-identical no-mutation + ZERO events + stable code), and a no-RNG assertion across success + ALL SIX reject branches (per the 6.7 Round-1 Low note), plus determinism.

- [x] **Task 4 — New SYSTEM event(s) wired end-to-end, append-only (AC2)** — `godot/scripts/core/events/domain_event.gd`
  - [x] Appended ONE unified `ECONOMY_CHANGED` member at the END of the `DomainEvent.Type` enum (after `ITEM_CONSUMED`) — NO existing member renumbered. It is a SYSTEM event (no actor — NOT in `_event_requires_actor`).
  - [x] Wired EVERY touch-point: the `EVENT_ID_ECONOMY_CHANGED` const, the `economy_changed(sequence_id, payload)` factory (defensive normalize/duplicate), the `_validate_economy_changed_payload` validator (per-field fail-loud — `reason` lower_snake; before/after non-negative integral; deltas signed integral via the new `_has_integral_payload` helper; `before + delta == after` arithmetic consistency), the `_validate_payload_for_event` arm, BOTH id maps, and the exhaustive `expected_ids` pin.
  - [x] Naming: ONE unified past-tense `economy_changed` event (unifies gold + healing into a single deterministic event) — see `[Decision] economy_changed (unified event)`.
  - [x] **T3 hardening landed:** added an enum-exhaustiveness assertion tying `expected_ids.size()` to `DomainEvent.Type.size() - 1` (for `UNKNOWN`) AND iterating the enum asserting each non-`UNKNOWN` member is a pinned key — closing the silently-un-pinned-future-event gap (the 6.7 Round-1 Low / retro T3).
  - [x] Added per-event tests to `test_domain_event.gd`: a clean round-trip (incl. a negative-delta healing-spend, the signed path) + per-field malformed rejections (non-lower_snake/blank reason, negative before, non-integral delta, arithmetic inconsistency, missing field).

- [x] **Task 5 — Wire the gold credit off the Epic-6 `reward_resolved` gold hook (AC2 — the T1 wire-off)** — `godot/scripts/core/commands/resolve_reward_command.gd` + `godot/scripts/run/run_orchestrator.gd` + `godot/scripts/run/reward_offer.gd`
  - [x] Took the **GENERATE-rolls-gold / RESOLVE-credits-deterministically** path (the primary, not the fallback) — see `[Decision] GENERATE rolls gold, RESOLVE credits deterministically`.
  - [x] At GENERATE: `RunOrchestrator.generate_reward_offer` — when the drawn entry's category is `gold`, resolves its `GoldRewardDefinition` through a new injectable `_gold_reward_repository` (default baseline), rolls `gold_min..gold_max` via `streams.rand_int(stream_name, ...)` on the run-level set (a SECOND draw on the same `rewards`/`loot` stream — deterministic + reproducible), and stores the rolled `gold_amount` on the `RewardOffer` (new additive field, mirroring `roll`/`draw_index`/`state_after` — small bounded int, numeric). A non-gold offer rolls no gold. The roll routes EXCLUSIVELY through the run-level set (grep-confirmed no `randi`/`randf`/`RandomNumberGenerator` in `run_orchestrator.gd`); fail-closed `unknown_gold_reward` on a repo miss. The passive 3-choice path never offers gold (gold_amount 0).
  - [x] At RESOLVE: `ResolveRewardCommand.execute` gold branch REPLACED the no-op with a deterministic wallet credit into `run.risk_economy` by `offer.gold_amount` (ZERO new RNG), emitting `economy_changed` with `sequence_id + 1` (the `item_gained` distinct-id precedent) alongside `reward_resolved`. The passive branch STAYS a no-op. Added a defensive null-economy guard in `validate()` for the gold category (mirroring the pickup's null-inventory guard).
  - [x] Preserved the `ResolveRewardCommand` contract exactly: the backpack compose-`PickupItemCommand` path, `inventory_full`-leaves-offer-pending, the offer flip + `reward_resolved`, the no-double-apply guarantee (a re-resolve rejects at `validate()` BEFORE any credit -> ZERO second gold, byte-identical run — proven by a dedicated test).
  - [x] The gold roll goes through `RunOrchestrator.streams` (already proven live by 6.3) — no new inert-stream exposure (the `LevelGenerator`-injection residual stays parked). Updated `test_run_route_position_save.gd` + `test_reward_offer_generate.gd`'s rewards-stream `draw_index` assertions to `>= 1` (a gold offer advances the stream by 2 — the offer draw + the amount draw — not 1; the round-trip/peek logic reads the post-draw state, so it stays correct).
  - [x] Extended `test_resolve_reward_command.gd` (gold credits + economy_changed, re-resolve credits no second gold, zero-amount edge, gold-resolve no-new-RNG) + `test_reward_offer_generate.gd` (gold GENERATES a rolled amount within the band, deterministic for a seed, RESOLVE credits exactly that amount drawing ZERO new RNG, a non-gold offer rolls no gold) + `test_reward_offer.gd` (the `gold_amount` field round-trips + pre-7.1 lenient default).

- [x] **Task 6 — Persist economy state into the save (AC1 "and save snapshots") + migration** — `godot/scripts/run/run_state.gd`, `godot/scripts/save/snapshots/run_snapshot.gd`
  - [x] Took SAVE STRATEGY **Option 1 (nest under `route_state`)** — see `[Decision] Save strategy (Option 1)`. Added `RunState.RISK_ECONOMY_KEY`, nested the economy dict inside the `route_state` payload in `to_run_snapshot_fields()`, and read it back in `try_from_run_snapshot_fields()` (lenient — a pre-7.1 payload defaults to the economy derived from `is_manual_seed`). This is the SOURCE OF TRUTH on resume. `RunResumeService.resume_route_position` already threads `route_state` through, so NO change to its 6-field bridge was needed (the key win of Option 1). The 23-key gate stays green (nested, not a new top-level key).
  - [x] ALSO populated the EXISTING top-level `RunSnapshot.gold`/`corruption` placeholders from the economy in `from_route_position` (a human-readable + Epic-8-readable mirror; `oath_shards` stays 0 — it is the AWARDED meta count, Epic 8, not the eligibility gate; `curses` stays `[]` — the curse-id LIST is 7.2's, 7.1 tracks only a count).
  - [x] Added migration/round-trip coverage in `test_run_route_position_save.gd`: economy survives a real-SaveRepository route-position resume (the nested source of truth), the top-level mirror is populated + survives a JSON round-trip within the 23-key gate (COUNT stays 23), AND a pre-7.1 (economy-absent) save restores with the default economy (the lenient-decode / back-compat guarantee). Plus the `test_run_state.gd` nested-bridge round-trip + the `RunState` full-dict JSON round-trip.
  - [x] The two no-surprise-key tests: `test_run_route_position_save.gd::_composed_route_position_snapshot_stays_within_the_23_key_gate` checks key MEMBERSHIP only (not default values), so it stayed green with the populated top-level keys — and I ADDED a deliberate `_economy_top_level_mirror_is_populated_within_the_23_key_gate` test asserting the POPULATED values (`gold == 50`, `corruption == 4`) + the COUNT stays 23. `test_run_snapshot.gd::_between_level_field_contract...` asserts defaults on the BOARD-centric `from_between_level` path (which I deliberately did NOT thread economy into — economy lives on the route-position path), so its `gold == 0` defaults remain CORRECT and unchanged — see `[Decision] from_between_level economy-free`. The **23-key COUNT stays 23**; `_allowed_run_snapshot_keys()` is unchanged.

- [x] **Task 7 — Full headless suite + diff hygiene**
  - [x] Ran the full headless suite: **116 PASS / 0 FAIL, "Headless tests passed.", exit 0** (Godot 4.6.3). The **false-PASS grep guard** is clean (no `SCRIPT ERROR`/`Parse Error`/`Compile Error` beyond the documented-expected negative-path diagnostics: the int64-overflow `run_started.root_seed` boundary x2, `RouteNode parse failed: invalid_node_type`, and the save/settings `Parse JSON failed` negatives).
  - [x] `git diff --check` clean (the only output is the benign LF→CRLF normalization warning on the story `.md`).
  - [x] Confirmed `scripts/rules/{conditions,operations}` stay EMPTY (0 `.gd` each); `data/source`/`data/resources` stay empty (0 content files); no `.tscn`/asset added (UI-scene-last); `RngStreamSet.required_streams()`/`reward_table_*`/every fingerprint UNCHANGED (not in the diff).
  - [x] Updated story Status (-> review), sprint-status, File List, and Completion Notes (incl. all `[Decision]`s).

## Dev Notes

### The big picture: this is the story that makes Epic 6 FELT
Epic 6 deliberately shipped **OUTCOME-RECORD-ONLY**: gold rewards, Destroy outcomes, and consumable-use all *record* a marker via an event but mutate NO domain field, **because no live wallet/HP/curse field existed**. The retro (Action Item T1, HIGH) names 7.1 as the owner of introducing that field and wiring the gold credit off the existing `reward_resolved` hook. So the headline of 7.1 is: **introduce `RiskEconomyState` + credit gold off `reward_resolved.category == gold`.** Everything else (curse rules, event rolls, affinities) is a later Epic-7 story. [Source: `epic-6-retro-2026-06-29.md` §7 + §8 T1; `project-context.md` "OUTCOME-RECORD-ONLY IS THE EPIC-6 v0 POSTURE"]

### Reuse, do NOT reinvent — the exact precedents to copy
- **The additive-lenient `RunState` field**: copy the 6.2 `inventory` field verbatim (`run_state.gd:83-93` for the field doc, `:117/:129` constructor default, `:225` `to_dictionary`, `:249` deep-copy in `copy()`, `:327` lenient decode in `try_from_dictionary`). `RiskEconomyState` is the next field of exactly that shape. [Source: `godot/scripts/run/run_state.gd`]
- **The value object**: copy `StartingKit`/`InventoryState` — exact-key `to_dictionary()` (`DICTIONARY_KEYS` const pinned by test), lenient `try_from_dictionary` (no reject path), deep `copy()`. Counts are small bounded ints (NO decimal-string encoding). [Source: `godot/scripts/run/starting_kit.gd`, `godot/scripts/run/inventory_state.gd`]
- **The run command**: copy `ResolveRewardCommand`/`PickupItemCommand` — extends `game_command.gd`, takes `RunState` directly, `sequence_id <= 0` rejected FIRST, validate-then-mutate, `_invalid_context` for the not-a-run / invalid-run cases, ZERO events + byte-identical run on reject. [Source: `godot/scripts/core/commands/resolve_reward_command.gd`]
- **The append-only SYSTEM event**: copy the 6.7 `item_consumed` wiring (it is DETERMINISTIC, no RNG provenance — the exact shape for a deterministic economy event). Touch-points: enum end, `EVENT_ID_*`, factory, validator, both id-maps, `expected_ids` pin. [Source: `godot/scripts/core/events/domain_event.gd:321-337` (factory), `:944-961` (validator), `:1523/:1583` (id maps); `godot/tests/unit/core/test_domain_event.gd:1583-1623` (`expected_ids` pin)]
- **The gold hook**: the `gold` category no-op in `ResolveRewardCommand.execute` (`resolve_reward_command.gd:130-131`) is the exact RESOLVE-time replace-point (credit the already-rolled amount, ZERO new RNG). The `item_gained` distinct-`sequence_id + 1` pattern (`:120`) is how to emit a second event with a unique id. The GENERATE-time gold roll goes in `RunOrchestrator.generate_reward_offer` (`run_orchestrator.gd:294-311`) alongside the existing offer draw, stored on the `RewardOffer` (`reward_offer.gd` — add an additive `gold_amount` field next to the existing `roll`/`draw_index`/`state_after`). [Source: `godot/scripts/core/commands/resolve_reward_command.gd`, `godot/scripts/run/run_orchestrator.gd`, `godot/scripts/run/reward_offer.gd`, `godot/scripts/content/definitions/gold_reward_definition.gd`]

### SAVE STRATEGY — the one genuinely-new decision (read carefully)
AC1 requires economy in **save snapshots** — unlike Epic 6, where inventory/offer rode ONLY the full `RunState.to_dictionary()` and were deliberately kept OUT of the route-position save. There are TWO existing save surfaces and TWO ways economy reaches them:

- **The `RunSnapshot` already has the economy top-level keys.** `RunSnapshot` declares `gold: int`, `oath_shards: int`, `corruption: int`, `curses: Array[String]` (plus `passives`/`inventory`/`equipment`/`affinities`/`meta_progression`) — all CURRENTLY inert placeholders defaulted to 0/`[]`/`{}`, all ALREADY inside the pinned **23-key gate** (`run_snapshot.gd:32-40,62-66,97-101`; the 23-key list at `test_run_snapshot.gd:251-261`). So persisting economy does NOT need a new top-level key — populate the EXISTING placeholders. [Source: `godot/scripts/save/snapshots/run_snapshot.gd`]
- **BUT the route-position resume bridge does NOT carry them today.** `RunState.to_run_snapshot_fields()` emits only 6 run/route fields (root_seed, is_manual_seed, meta_progression_eligible, route_state-with-nested-run_phase+selected_class_id, current_route_node_id, revealed_route_node_ids — `run_state.gd:265-294`), and `RunResumeService.resume_route_position` rebuilds the `RunState` from only those same 6 fields (`run_resume_service.gd:110-121`). The top-level `RunSnapshot.gold`/etc. are parsed but NEVER fed back into the restored `RunState`.

**Two implementation options:**
1. **(RECOMMENDED) Nest economy under `route_state`** — the PROVEN `run_phase` (4.3) / `selected_class_id` (5.3) pattern. Add a `RunState.RISK_ECONOMY_KEY` const, nest the economy dict inside the `route_state` payload in `to_run_snapshot_fields()`, and read it back in `try_from_run_snapshot_fields()` (lenient, default empty for a pre-7.1 save). This threads economy through the route-position save/resume **end-to-end** (the way the class id survives a between-node resume), keeps the 23-key gate green untouched (it is nested, not a new top-level key), and is the pattern project-context explicitly ratifies ("nest run-progression fields under `route_state`; never flatten a new top-level `RunSnapshot` key"). The top-level `RunSnapshot.gold`/`curses`/etc. MAY ALSO be populated from the economy (so the snapshot is human-readable + the Epic-8 run-summary can read them), but the **source of truth on resume is the nested copy**. This is the cleanest fit and is what the retro's "compose into existing nested `route_state` mechanism where possible; do not fork a parallel save format" directs.
2. (Alternative) Populate the top-level `RunSnapshot` economy keys AND extend `to_run_snapshot_fields()` + `resume_route_position`'s 6-field bridge to carry them. This uses the existing top-level keys but requires touching the resume bridge's field list. More surface, and it spreads economy across both nested and top-level — prefer option 1.

**Whichever you choose:** the 23-key COUNT stays 23 (no new top-level key), the two no-surprise-key tests' default-value assertions WILL flip and must be updated deliberately (Task 6), and you MUST add the pre-7.1-save-back-compat + JSON-round-trip migration tests. Keep the `route_position_seed_mismatch` cross-check intact (compose + resume). Record the chosen option as a `[Decision]`.

### Named RNG — economy draws ZERO RNG (but mind the inert-stream residual)
A credit/spend/heal is a **recorded amount, not a roll** — the economy command and the gold-credit wire-off draw ZERO RNG (deterministic, like `item_consumed`). Grep-confirm no `randi`/`randf`/`RandomNumberGenerator` in any new command. [Source: project-context "Determinism & Simulation Rules"]

The **4.6 inert run-level `RngStreamSet`** carried deferral (owner Epic 6/7/9) is RELEVANT context but NOT 7.1's to close: Epic 6 closed the *reward-draw* half (reward rolls now thread `RunOrchestrator.streams`); the `LevelGenerator.generate`-injection half and any *new* run-affecting draw remain. **7.1 introduces no new RNG draw site, so it does not bind this** — but the FIRST Epic-7 story to ROLL (likely **7.3** event choices via the `events` stream, or **7.4/7.5** affinity assignment via level-gen) MUST route through `RunOrchestrator.streams`. If 7.1's economy command is ever extended to a *gambling*-style random outcome, that draw MUST go through the named `rewards`/`events` stream via the run-level set — but no AC here requires it. [Source: `deferred-work.md` "code review of 4-6" T2 entry; `epic-6-retro` §7 prep item 4]

### What stays UNTOUCHED (scope fences — confirm in Completion Notes)
- `scripts/rules/{conditions,operations}` stay EMPTY (0 `.gd`). The per-effect operation engine / combat hook sites are 7.5's call (retro T2). Do NOT author them. [Source: `project-context.md`; `epic-6-retro` §8 T2]
- No `RewardOffer` / `RewardTableDefinition` / `RngStreamSet.required_streams()` / generator-fingerprint / route-fingerprint change. No `.tscn`/asset (UI-scene-last — the economy HUD is a later story).
- The append-only event enum: NEVER renumber an existing member; only append at the end.
- Difficulty is a HARD non-goal: nothing in economy state may be a difficulty knob, and no economy field scales enemy stats/run length. [Source: project-context "DIFFICULTY IS A HARD NON-GOAL"]

### Deferred-work items that overlap this story (from `deferred-work.md`)
- **T1 (the Epic-6 outcome-record → felt wire-off, owner Epic 7 / 7.1):** apply the live gold-wallet credit off `reward_resolved`. **Addressed by Task 5** (note: this also requires GENERATE to finally ROLL the concrete gold amount within the `GoldRewardDefinition` band — Epic 6 stored gold by-id only and never rolled it; Task 5 rolls it at GENERATE through the run-level `rewards` stream and credits the rolled amount deterministically at RESOLVE). The heal/ward/burst (off `item_consumed`) and cleanse/curse (off `passive_destroyed`) halves are this same wire-off family — Task 5 does the GOLD half (purely economic); the heal/curse halves default to 7.2 (curse) / the heal-availability disposition noted in the scope boundary. [Source: `deferred-work.md` "Tracked from: dev of 6-6"/"dev of 6-7" NEW-owner-Epic-7 entries; `epic-6-retro` §8 T1]
- **The 4.6 inert run-level `RngStreamSet` (Med, owner Epic 6/7/9):** NOT closed here (7.1 draws no RNG). Flagged so 7.1 does not regress determinism and the later rolling story (7.3/7.4/7.5) owns the `LevelGenerator.generate`-injection half. **Knowingly worked around** (no new draw site). [Source: `deferred-work.md` "code review of 4-6" T2]
- **The event-id-map exhaustiveness pin (6.7 Round-1 Low, owner = next story touching `domain_event.gd` = THIS story):** **Addressed by Task 4** (add the `expected_ids.size()`-vs-enum-count assertion). [Source: `deferred-work.md` "code review of 6-7" Round 1; `epic-6-retro` §8 T3]
- **`GenerationResult.seed` success-path split (Low, owner = next story touching `GenerationResult`):** NOT this story — 7.1 does not touch `GenerationResult`. Leave parked. [Source: `deferred-work.md` "code review of 3-7"]
- **`warding_salve` un-rollable / consumable-frequency tuning (Low, Epic 10):** NOT this story. [Source: `deferred-work.md`]

### Project Structure Notes
- New value object → `godot/scripts/run/risk_economy_state.gd` (the run-progression domain home, alongside `starting_kit.gd`/`inventory_state.gd`).
- New command(s) → `godot/scripts/core/commands/` (run-domain commands live with the tactical commands, NOT under `scripts/run/` — the 4.x ratified placement).
- New event(s) → appended in `godot/scripts/core/events/domain_event.gd`.
- Tests → `godot/tests/unit/run/` (state) + `godot/tests/unit/core/` (command + event) + `godot/tests/unit/save/` (save round-trip/migration), mirroring the domain. The headless runner auto-discovers `test_*.gd` under `res://tests/unit` and `res://tests/integration` only.
- No conflicts with the unified structure; all files land in existing domain folders.

### Project Context Rules (extracted from `project-context.md` — the rules this story's domain MUST follow)
- **Run-command idiom (4.3, ratified):** take `RunState` directly (no `RunActionContext` wrapper); caller supplies `sequence_id`; reject `sequence_id <= 0` FIRST; validate-then-mutate; structured `ActionResult.error` with ZERO events + byte-identical no-mutation on reject; build the success event only AFTER the mutation. ONE stable top-level error code per failure class; the precise reason in `metadata`, never embedded in a lower_snake error code.
- **Additive-lenient `RunState` field:** new field defaults cleanly, is NOT a required `validate()` field, new constructor param LAST, rides the full `to_dictionary()`/`try_from_dictionary` lenient-read; `copy()` deep-copies mutable containers.
- **Save/snapshot:** snapshots are pure reads (NO RNG draw, NO command, NO mutation on compose/restore). Do NOT add a new top-level `RunSnapshot` key for run-progression state — nest under `route_state` (the pinned 23-key gate). ALWAYS JSON-round-trip snapshots in tests (`JSON.stringify` -> `parse_string`). Restore exposes NO partial corrupt state. Schema changes need migration tests. Small bounded ints (gold/healing/curse counts) stay numeric — only RNG `root_seed`/`state` need decimal-string encoding.
- **Named RNG:** gameplay-affecting randomness uses its assigned stream (`rewards`/`events`/...); cosmetic randomness cannot affect outcomes. (Economy draws none.)
- **Events:** successful commands emit deterministic past-tense `DomainEvent` records; new run-domain events are SYSTEM events (no actor) appended at the enum END (never renumbered), wired end-to-end (factory + payload validator + both id maps + round-trip + malformed tests + `expected_ids` pin).
- **Rules kernel:** `scripts/rules/{conditions,operations}` stay EMPTY scaffolding (the per-effect operation engine is later); the `RulesResolver` is a pure read (no RNG, no command, no tactical mutation).
- **Difficulty is a hard non-goal:** no economy field may scale enemy stats/HP/damage/rewards/RNG/run length.
- **Headless:** tests run without rendering/audio/UI scenes/presentation nodes. Run via PowerShell (`godot` is not on the Bash PATH).
- **Naming:** `snake_case` files, `PascalCase` classes (`RiskEconomyState`), `UPPER_SNAKE_CASE` consts, imperative command names, past-tense event names, lower_snake content/marker ids.

### References
- [Source: `_bmad-output/planning-artifacts/epics.md#Story 7.1: Risk Economy State` (lines 1867-1888) — the AC]
- [Source: `_bmad-output/planning-artifacts/epics.md` lines 453-459 — Epic 7 objective + FR54-FR58 + "explicit enough for player understanding and deterministic enough for replay and save/resume"]
- [Source: `_bmad-output/planning-artifacts/gdds/gdd-Game-2026-05-31/gdd.md` lines 443-471 — Economy and Resources: gold/healing/curses/Oath Shards MVP intent + risk examples]
- [Source: `_bmad-output/planning-artifacts/gdds/gdd-Game-2026-05-31/gdd.md` lines 264-281 — meta-currency Oath Shards: awarded only after run end, only in eligible non-manual-seed runs]
- [Source: `_bmad-output/implementation-artifacts/epic-6-retro-2026-06-29.md` §7 (Epic 7 prep), §8 (Action Items T1/T2/T3) — the epic-transition prep folded into this story]
- [Source: `_bmad-output/implementation-artifacts/deferred-work.md` — the overlapping deferrals (T1 wire-off, inert-stream residual, event-id-map exhaustiveness)]
- [Source: `project-context.md` — run-command idiom, additive-lenient field, save/23-key gate, named RNG, append-only events, difficulty non-goal]
- [Source: `godot/scripts/run/run_state.gd`, `godot/scripts/run/inventory_state.gd`, `godot/scripts/run/starting_kit.gd` — the field + value-object precedents]
- [Source: `godot/scripts/core/commands/resolve_reward_command.gd` — the run-command + the gold-hook replace-point]
- [Source: `godot/scripts/core/events/domain_event.gd`, `godot/tests/unit/core/test_domain_event.gd` — the append-only event wiring + the `expected_ids` pin to extend]
- [Source: `godot/scripts/save/snapshots/run_snapshot.gd`, `godot/tests/unit/save/test_run_snapshot.gd`, `godot/tests/unit/save/test_run_route_position_save.gd`, `godot/scripts/save/run_resume_service.gd` — the 23-key gate + the route-position save/resume bridge to extend]

## Dev Agent Record

### Agent Model Used

Opus 4.8 (1M context) — auto-gds dev-story delegate.

### Debug Log References

- Full headless suite (Godot 4.6.3, `test_runner.tscn`): 116 PASS / 0 FAIL, "Headless tests passed.", exit 0. False-PASS grep (`SCRIPT ERROR|Parse Error|Compile Error`) clean (only the documented-expected negative-path diagnostics).
- During Task 5 two tests initially failed (`test_reward_offer_generate.gd`, `test_run_route_position_save.gd`): the new GENERATE gold roll advances the `rewards` stream a SECOND time for a gold offer (draw_index 0 -> 2), so the `draw_index == 1` assertions were updated to `>= 1` (the stream-advanced/round-trip INTENT is preserved; the exact count now depends on whether the drawn offer is gold). This is the EXPECTED consequence of the second named-stream draw, not a regression.

### Completion Notes List

Story 7.1 introduces the live **`RiskEconomyState`** run-domain field (gold / healing availability / curse+corruption counts / Oath-Shard eligibility / risk flags), the direct **`ApplyEconomyChangeCommand`**, ONE unified append-only **`economy_changed`** SYSTEM event, the **T1 wire-off** (a gold reward now CREDITS the wallet — felt, not merely recorded), and **economy persistence into the route-position save**. All three ACs met; full suite green; scope fences held (`scripts/rules/{conditions,operations}` empty, no `.tscn`/asset, no fingerprint/`required_streams`/`RewardTableDefinition` change). The Epic-6 OUTCOME-RECORD-ONLY posture is now FELT for gold.

**[Decision] Oath-Shard eligibility (NEW field, lockstep):** `oath_shard_eligible` is a NEW `RiskEconomyState` field, NOT an alias of `RunState.meta_progression_eligible`. The source of truth REMAINS `meta_progression_eligible` (Epic 8 owns the actual meta gate); the economy field is kept in LOCKSTEP — `RiskEconomyState.validate(is_manual_seed)` asserts `oath_shard_eligible == not is_manual_seed`, `RunState.new(...)` seeds it via `RiskEconomyState.for_run(is_manual_seed)`, and `RunState.validate()` calls the invariant with the run's `is_manual_seed`. A manual-seed run is NEVER eligible (the GDD invariant). A divergence fails `RunState.validate()` fail-loud (`invalid_oath_shard_eligibility`).

**[Decision] Healing model = availability (charge count, not current-HP):** healing is modeled as `healing_charges` (a resource/charge count, the AC's exact "resources OR availability" wording), NOT a live tactical current-HP. v0 still has no live tactical play loop (combat auto-resolves), so there is no board-coupled current-HP for combat to decrement. The `item_consumed` heal MUTATION off Epic-6 consumables is DEFERRED (see deferred-work disposition below).

**[Decision] economy_changed (unified event):** a SINGLE deterministic `economy_changed` event carries both gold and healing changes (`reason` + `gold_before/after/delta` + `healing_before/after/delta`), rather than split `gold_changed`/`healing_changed` events. Rationale: one validate-then-mutate path, one event touch-point, simpler. Before/after are non-negative integral; deltas are SIGNED integral (a credit positive, a spend negative — the new `_has_integral_payload` helper); the validator ALSO enforces `before + delta == after` arithmetic consistency (the record must be honest). NO `roll`/`draw_index` (deterministic — zero RNG, the `item_consumed` shell). T3 hardening folded in: an enum-exhaustiveness assertion now ties `expected_ids.size()` to the enum member count.

**[Decision] GENERATE rolls gold, RESOLVE credits deterministically (the T1 wire-off, primary path — NOT the fallback):** `GoldRewardDefinition` declares a `gold_min..gold_max` BAND (Epic 6 never rolled it). The concrete gold is rolled at GENERATE (`RunOrchestrator.generate_reward_offer`, through the run-level `rewards`/`loot` stream via `streams.rand_int` — a SECOND named-stream draw, deterministic + reproducible) and stored on `RewardOffer.gold_amount`; RESOLVE credits exactly that amount DETERMINISTICALLY (ZERO new RNG — the Epic-6 zero-new-RNG-on-resolve invariant holds). A re-resolve of a `resolved` offer rejects at `validate()` BEFORE any credit (no second gold). The gold roll routes EXCLUSIVELY through `RunOrchestrator.streams` (the 6.3-proven run-level set) — no new inert-stream exposure; the `LevelGenerator`-injection residual stays parked.

**[Decision] Save strategy = Option 1 (nest economy under `route_state`):** the economy is nested inside the `route_state` payload (`RunState.RISK_ECONOMY_KEY`, the proven `run_phase`/`selected_class_id` mechanism) as the SOURCE OF TRUTH on resume — no new top-level `RunSnapshot` key, the 23-key gate stays green, and `RunResumeService.resume_route_position`'s 6-field bridge needed NO change (it already threads `route_state`). The EXISTING top-level `RunSnapshot.gold`/`corruption` placeholders are ALSO populated (a human-readable + Epic-8-readable mirror) in `from_route_position`; `oath_shards` stays 0 (the AWARDED meta count is Epic 8) and `curses` stays `[]` (the curse-id LIST is 7.2's — 7.1 tracks only a count).

**[Decision] `from_between_level` stays economy-free:** the BOARD-centric `from_between_level` (a mid-level save) was deliberately NOT threaded with economy — economy rides the route-position (between-NODE) save path. So `test_run_snapshot.gd::_between_level_field_contract...`'s `gold == 0` default assertions remain CORRECT and unchanged. The deliberate "populated-value" registration the story called for lives in the new `test_run_route_position_save.gd::_economy_top_level_mirror_is_populated_within_the_23_key_gate` (asserting `gold == 50`, `corruption == 4`, COUNT stays 23). A future live-in-node-save story may thread economy into the board-centric save too; not required by AC1.

**Deferred-work dispositions (per the scope-boundary requirement to record the chosen disposition, not silently skip):**
- The `item_consumed` heal-availability MUTATION off Epic-6 consumables and the `passive_destroyed` curse-removal MUTATION: **DEFERRED.** The heal model is a trivial charge increment and COULD land here, but the consumable-use effect surface overlaps the curse model (7.2) and the HUD use-command call site (a later HUD story owns the in-flight "use this item" intent). 7.1 ships the `ApplyEconomyChangeCommand` healing path (the direct economy mutation) + the `RiskEconomyState.apply_healing_delta`/floor API, so a later story wires `item_consumed.outcome_effect` -> `ApplyEconomyChangeCommand` (heal) and `passive_destroyed`/curse -> the curse setter without new domain shape. The DIRECT economy command (gold + healing) is fully live this story; only the EVENT-driven wire-offs of the heal/curse halves defer. (A new `deferred-work.md` entry should record this — owner: 7.2 curse-removal half + a later HUD/consumable story for the heal half.)
- The risk/reward EVENT roll that populates `risk_flags`, the curse/corruption RULES, and the cursed-reward UX: **OUT of scope** (7.2/7.3) — 7.1 ships only the `risk_flags` container + `add_risk_flag`/`set_curse_count`/`set_corruption` STRUCTURAL setters.
- Oath-Shard AWARDING + meta profile: **Epic 8** — 7.1 tracks only the eligibility gate (no meta-profile snapshot introduced).
- The 4.6 inert run-level `RngStreamSet` `LevelGenerator`-injection half: **still parked** — the gold roll goes through the already-live `RunOrchestrator.streams`, so 7.1 adds no new inert-stream exposure; the later rolling story (7.3/7.4/7.5) owns the level-gen-injection half.

### File List

New production:
- `godot/scripts/run/risk_economy_state.gd` — the `RiskEconomyState` value object.
- `godot/scripts/core/commands/apply_economy_change_command.gd` — the direct economy-change command.

Modified production:
- `godot/scripts/run/run_state.gd` — seated `risk_economy` (additive-lenient field + `RISK_ECONOMY_KEY` nested save + the eligibility-invariant check in `validate()`).
- `godot/scripts/core/events/domain_event.gd` — appended the `ECONOMY_CHANGED` SYSTEM event end-to-end (enum + const + factory + validator + dispatch arm + both id maps + the `_has_integral_payload` helper).
- `godot/scripts/run/reward_offer.gd` — additive `gold_amount` field (the GENERATE-rolled concrete gold).
- `godot/scripts/run/run_orchestrator.gd` — the GENERATE-time gold roll (`_roll_gold_amount` via the run-level streams + injectable `_gold_reward_repository`).
- `godot/scripts/core/commands/resolve_reward_command.gd` — the RESOLVE-time gold credit + `economy_changed` emission (the T1 wire-off) + a defensive null-economy guard.
- `godot/scripts/save/snapshots/run_snapshot.gd` — `from_route_position` populates the top-level `gold`/`corruption` mirror from the economy.

New tests:
- `godot/tests/unit/run/test_risk_economy_state.gd`
- `godot/tests/unit/core/test_apply_economy_change_command.gd`

Modified tests:
- `godot/tests/unit/run/test_run_state.gd` — the additive economy field + the eligibility invariant + the nested-bridge round-trip.
- `godot/tests/unit/core/test_domain_event.gd` — the `economy_changed` round-trip + malformed rejections + the T3 enum-exhaustiveness assertion.
- `godot/tests/unit/run/test_reward_offer.gd` — the `gold_amount` round-trip + pre-7.1 lenient default.
- `godot/tests/unit/run/test_reward_offer_generate.gd` — the GENERATE gold roll (band/deterministic/RESOLVE-credit/non-gold) + the `>= 1` stream-advance update.
- `godot/tests/unit/core/test_resolve_reward_command.gd` — the gold wallet credit + `economy_changed` + re-resolve-no-second-gold + zero-amount edge + gold-resolve no-new-RNG.
- `godot/tests/unit/save/test_run_route_position_save.gd` — economy survives a route-position resume + the top-level mirror + the pre-7.1 back-compat migration + the `>= 1` stream-advance update.

Tracking:
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 7-1 -> review.
- `_bmad-output/implementation-artifacts/7-1-risk-economy-state.md` — this story file (frontmatter `baseline_commit`, task checkboxes, Dev Agent Record, Status).

### Review Findings

Primary code review (gds-code-review, 2026-06-29, round 1) — three adversarial layers (Blind Hunter diff-only, Edge Case Hunter diff+repo, Acceptance Auditor spec+context). Diff reviewed: baseline `4408c785` -> HEAD `0ebb4d8` (16 code files, ~1711/-38), excluding `_bmad`/`_bmad-output`/cache. Reviewer model Opus 4.8 (1M), auto-gds code-review delegate. **Verdict: Changes Requested (cosmetic only)** — Critical 0 / High 0 / Med 0 / Low 4. All three ACs verified satisfied; scope fences held; every `[Decision]` claim verified true against the code; 8 raised findings dismissed as noise/verified-correct. The 6.7 event-id-map exhaustiveness pin assigned to this story is confirmed closed (the `expected_ids`-vs-enum-count assertion in `test_domain_event.gd`).

- [x] [Review][Decision] `economy_changed` accepts an all-zero "change" on the gold-resolve path (asymmetry with the direct command's no-op reject) — `ResolveRewardCommand.execute` always enters the gold branch and emits `economy_changed` with `gold_delta == offer.gold_amount`; when `gold_amount == 0` (a collapsed `0..0` gold band, or a hand-built/pre-7.1 offer carrying no rolled amount) it writes a zero-delta `before==after` record into the AC2 explanation log, while `ApplyEconomyChangeCommand` rejects the same all-zero as `invalid_economy_change`, and `_validate_economy_changed_payload` imposes no "at least one non-zero delta" guard. The story made this a deliberate `[Decision]` and pins it with a test (`_resolves_a_gold_reward_with_zero_rolled_amount_credits_nothing_but_still_records`), so the code is internally consistent — but whether the explanation log SHOULD contain zero-delta records (vs the gold branch skipping the event when `gold_amount == 0`) is a product/design call that changes the AC2 log's meaning. NOTE: unreachable with baseline content (both baseline gold rewards are 5..15 and 20..50); purely a latent/future-content concern. [evidence: `godot/scripts/core/commands/resolve_reward_command.gd:156-166`; `godot/scripts/core/events/domain_event.gd:990-1016`] Options: (a) accept as-is (keep the honest "a gold reward was resolved, amount 0" record); (b) skip the `economy_changed` emission when `gold_amount == 0` on the reward path; (c) add a non-zero-delta guard to the validator (would also force the direct command's invariant onto the reward path). Default if unresolved: (a) accept — leave behavior unchanged. **RESOLVED — ACCEPTED AS-IS (2026-06-29, option (a), human-chosen):** NO code change to `ResolveRewardCommand`, the `economy_changed` event, or `_validate_economy_changed_payload`; the existing zero-delta pin test (`_resolves_a_gold_reward_with_zero_rolled_amount_credits_nothing_but_still_records`) stays unchanged. Rationale: resolving a reward (`ResolveRewardCommand`) is semantically DISTINCT from a direct economy mutation (`ApplyEconomyChangeCommand`) — the resolve legitimately records its economic OUTCOME via `economy_changed` (AC2's explanation log) even when the rolled amount is 0, documenting that "a gold reward was resolved, amount 0"; whereas a direct all-zero `ApplyEconomyChangeCommand` is a meaningless caller no-op the direct command rightly rejects (`invalid_economy_change`). The asymmetry is INTENTIONAL: one records a real reward-resolution event, the other guards against a no-op mutation request. (Latent/future-content only — unreachable with baseline gold rewards 5..15 / 20..50.)
- [x] [Review][Patch] Untyped local + dead-comment tidy in the top-level economy mirror [`godot/scripts/save/snapshots/run_snapshot.gd:262-266`] — `var economy = source_run.risk_economy` is the only untyped local in the function (the file/project convention is typed locals); the trailing `oath_shards` comment documents an intentional no-op but reads like missing code. Unambiguous, safe cleanup: annotate `var economy: RiskEconomyState = source_run.risk_economy` (requires adding a `const RiskEconomyState = preload(...)` to `run_snapshot.gd`) and/or tighten the trailing comment. Cosmetic only — no behavior change. **RESOLVED (2026-06-29):** added `const RiskEconomyState = preload("res://scripts/run/risk_economy_state.gd")` alongside the existing class preloads, typed the local as `var economy: RiskEconomyState = source_run.risk_economy`, and moved + reworded the trailing `oath_shards` note to a leading comment that reads as a deliberate "intentionally left at its 0 default (Epic-8 awarded count, not the eligibility gate)" rather than a stub. Correctness-neutral (no behavior change); full headless suite re-run green.
- [x] [Review][Defer] No upper bound / int64-overflow ceiling on `gold`/`healing_charges`/`curse_count`/`corruption` [`godot/scripts/run/risk_economy_state.gd:110-114,225-243`] — deferred, pre-existing convention. The counts floor at 0 but have no ceiling; repeated credits (or a hand-edited save with a near-int64 gold) could overflow, and a gold value > 2^53 would also lose precision through the numeric (non-decimal-string) `to_dictionary()` + the top-level `RunSnapshot.gold` mirror. Consistent with every other "small bounded int" field in the codebase (`baseline_hp`/`capacity`/`roll` are all un-ceilinged); not a 7.1 regression and practically unreachable for a wallet. Owner: a future economy-bounds/tuning pass (Epic 10) if it ever matters.
- [x] [Review][Defer] A pending gold offer at a route-position save point would be dropped on resume while the stream stays advanced [`godot/scripts/run/run_orchestrator.gd:240-260`; `godot/scripts/save/snapshots/run_snapshot.gd` (`from_route_position` does not serialize `pending_reward_offer`)] — deferred, pre-existing design. Unreachable in v0 (the reward flow is caller-driven and NOT wired into the auto-resolve `run_to_completion` loop, so no offer can be pending at a between-node save boundary). This is the documented Epic-6 "inventory/offer not in the route-position save" posture, not caused by 7.1. The future story that wires the reward flow into a node boundary must handle composing a route-position save with an offer pending (the stream advances correctly, but the unresolved offer would be lost). Owner: the later reward-flow-wiring / in-node-save story.

**Round 2 of 3** — Secondary INDEPENDENT verification pass (gds-code-review, 2026-06-29, round 2). Reviewer model Opus 4.8 (1M context, auto-gds code-review delegate) — a second, model-diverse pair of eyes re-checking the full diff at full reasoning depth specifically to catch anything round 1 missed. Diff re-reviewed: baseline `4408c785` -> HEAD `18d84bb` (16 code files, 1713/-38; HEAD now includes the round-1 cosmetic patch `18d84bb`), excluding `_bmad`/`_bmad-output`/cache. All three adversarial lenses re-run independently (Blind Hunter diff-only correctness; Edge Case Hunter diff+repo boundaries; Acceptance Auditor against the AC + project-context). **Verdict: Approve — convergence confirmed.** Critical 0 / High 0 / Med 0 / Low 0 NEW. **Zero NEW findings** — every issue independently probed was already correctly handled, already test-covered, or already captured as a round-1 `[Decision]`/`[Defer]` (not re-litigated). Independently verified this round:
  - **AC1** (init + domain + save): `RiskEconomyState` seats on `RunState` via the exact additive-lenient `inventory` pattern (new ctor param LAST, never null, deep-copied, lenient-read); economy nested under `route_state` (`RISK_ECONOMY_KEY`) as the resume source-of-truth + the top-level `RunSnapshot.gold`/`corruption` mirror; the 23-key gate stays 23 (verified `_allowed_run_snapshot_keys()` body UNCHANGED in the diff — tests only ASSERT it). Both seed modes + the eligibility lockstep (`oath_shard_eligible == not is_manual_seed`) enforced in `RunState.validate()` and tested.
  - **AC2** (deterministic events + explanation log): `economy_changed` wired end-to-end (enum appended at END, const, factory with defensive normalize, per-field fail-loud validator + the new `_has_integral_payload` signed-delta helper + before+delta==after arithmetic-consistency, dispatch arm, BOTH id maps, the T3 `expected_ids`-vs-`Type.size()-1` exhaustiveness pin). Confirmed the round-trip restore path (`try_from_dictionary`) routes `ECONOMY_CHANGED` through the validator (a malformed payload is rejected on restore, not masked). Zero RNG (grep-confirmed; only the doc-comment mentions `RandomNumberGenerator`).
  - **AC3** (invalid-change rejection, no mutation): all six `ApplyEconomyChangeCommand` reject branches (`invalid_event_sequence_id` FIRST, `invalid_context`, `invalid_economy_reason`, `invalid_economy_change`, `insufficient_gold`, `insufficient_healing`) — byte-identical no-mutation + ZERO events + ZERO RNG, asserted across success + all six branches.
  - **T1 wire-off**: GENERATE rolls `gold_min..gold_max` through `streams.rand_int` on the run-level set (fail-closed `unknown_gold_reward`; `GoldRewardDefinition.validate()` guarantees `0<=min<=max`, and `rand_int` itself fail-closes `invalid_rng_range` on a bad band — no crash); RESOLVE credits the rolled `gold_amount` deterministically (ZERO new RNG, asserted by a before/after stream snapshot). Confirmed `generate_reward_offer` is NOT auto-wired into `_resolve_combat`/`run_to_completion`, so the route-position save composed in the auto-resolve loop is never perturbed (interrupted==uninterrupted determinism holds).
  - **Scope fences**: `scripts/rules/{conditions,operations}` EMPTY (0 .gd each); `data/source`/`data/resources` empty; no `.tscn`/asset; no `required_streams()`/`REWARD_CATEGORIES`/fingerprint/route-fingerprint change in the diff. Migration/back-compat: pre-7.1 run-dict AND pre-7.1 route-position-save (nested key stripped) both restore the default economy and validate — tested with a real SaveRepository round-trip.
  - **Independent full headless suite re-run** (Godot 4.6.3, PowerShell): **116 PASS / 0 FAIL, "Headless tests passed.", exit 0.** False-PASS grep guard (`SCRIPT ERROR|Parse Error|Compile Error|Failed to load script`) CLEAN — the only stderr diagnostics are the documented-expected negative-path lines (int64-overflow `root_seed` x2, `RouteNode parse failed: invalid_node_type`, two deliberate `Parse JSON failed`).
  No new `[Review][*]` bullets persisted; no new `[Review][Defer]` added to the cross-story ledger. The round-1 `[Review][Decision]` (zero-delta `economy_changed` on the gold-resolve path) remains ACCEPTED AS-IS (option (a)) — independently re-confirmed internally consistent and latent/future-content-only (unreachable with baseline gold rewards 5..15 / 20..50); not reopened.
