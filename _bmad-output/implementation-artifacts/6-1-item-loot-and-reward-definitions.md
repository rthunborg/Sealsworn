---
created: 2026-06-26
source_story_key: 6-1-item-loot-and-reward-definitions
epic: 6
story: 1
---

# Story 6.1: Item, Loot, and Reward Definitions

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a player,
I want loot and reward options to come from approved definitions,
so that runs can offer readable build choices without hardcoded item logic.

## Acceptance Criteria

Source: `_bmad-output/planning-artifacts/epics.md` -> Epic 6 -> Story 6.1 (verbatim AC, with the implementation-grounded detail this story adds). FRs covered: FR46 (passive pool *definitions* — the content the class passive ids point at), FR51/FR52/FR53 (loot categories, item handling, consumable scarcity/value); GDD FR75/FR76 (item character-level requirement + roll model).

1. **MVP loot/reward definitions for every category are available through typed repository lookups; gameplay never reads source files directly.**
   **Given** MVP loot definitions are authored
   **When** the content repository loads them
   **Then** weapons, armor, jewelry, support items, consumables, pickups, passives, and gold-reward definitions are available through typed lookups (each category resolved through its repository -> `ContentRepository`, never `FileAccess`/`load()`/`JSON.parse` in a gameplay/hot path)
   **And** gameplay systems do not read source files directly.

2. **A definition referencing a missing id, invalid value range, or unsupported category fails closed with a structured content error.**
   **Given** a definition references a missing id, invalid value range, or unsupported category
   **When** content validation runs
   **Then** validation fails with a structured content error (a stable lower_snake `invalid_*_definition` / `invalid_*_repository` `ActionResult.error` with `{reason, field}` metadata — never a crash, a coerced field, or a silently-fabricated default)
   **And** invalid content is not available to gameplay (a fail-closed factory returns `null` / a fail-closed lookup returns `null`; no partial registration survives a rejected definition).

3. **Reward-offer rolls use the named `rewards`/`loot` stream and reproduce from the same seed and state.**
   **Given** reward selection uses randomness
   **When** reward offers are generated (in this story: through deterministic FIXTURES that prove the stream contract — the live reward-offer command/flow is Story 6.3)
   **Then** rolls use the named `rewards` or `loot` stream as appropriate (drawn through `RngStreamSet.rand_int`/`rand_float` on `STREAM_REWARDS`/`STREAM_LOOT`, NEVER `randi()`/`randf()`, NEVER a local `RandomNumberGenerator`, NEVER the `map`/`level`/`combat`/`events`/`cosmetic` stream)
   **And** the same seed and state reproduce the same reward offer (a re-run from the same `root_seed` + the same pre-draw stream state yields a byte-identical offer; a different seed diverges).

4. **Equippable item definitions declare a character-level requirement (or explicit `none`); run-depth is never the equip gate.**
   **Given** equippable item definitions are authored
   **When** content validation runs
   **Then** each equippable item declares a character-level requirement (a small non-negative int) OR an explicit `none` requirement (a named sentinel that resolves and means "no level gate")
   **And** minimum run-depth requirements are NOT used as the item equip gate (no `min_run_depth`/`min_node_depth`/route-depth field gates equipping — character level is the only equip gate; GDD FR75/FR76).

5. **Item instance variation uses roll ranges / affix-enhancement ids / affinity tags, not fixed item levels; un-implemented advanced roll families are explicitly deferred while preserving the data boundary.**
   **Given** MVP item instances can vary
   **When** item stat, affix, affinity, or enhancement data is defined
   **Then** the data model uses roll RANGES (min/max bands), affix/enhancement IDs, and affinity TAGS rather than fixed item levels (no `item_level` field drives stats)
   **And** any advanced roll family NOT implemented for MVP is explicitly deferred with a stable marker on the definition/data model (an MVP-deferral flag/note per advanced family) while PRESERVING the repository/data boundary for later rollout (the field/shape exists and validates inert, so Epic 7+/post-MVP can light it up without a schema fork).

6. **Duplicate-id registration fails loud across ALL content repositories (closes the carried Epic-5 cross-cutting hardening item).**
   **Given** two content definitions are registered under the same id in ANY content repository (carried cross-cutting `[Review][Defer]` from Epic 5 — see `deferred-work.md` "code review of 5-1")
   **When** the repository ingests them
   **Then** registration fails loud with a structured `duplicate_*` error (e.g. `duplicate_weapon` / `duplicate_class` / `duplicate_loot` carrying the offending id in metadata) instead of silently last-write-wins (where `*_ids()` keeps the FIRST and `get_*` returns the SECOND, so the id-list and the resolver disagree)
   **And** the guard is applied UNIFORMLY — retrofitting the six existing repositories that currently accept duplicates (`class`, `enemy`, `level_recipe`, `weapon`, `support`, `passive`) AND the new Epic-6 loot/reward repositories — done in this first-data-pipeline story so a duplicate id can never silently shadow another, with negative tests on every repo.

### Acceptance-criteria interpretation notes (READ BEFORE CODING)

- **This is the FIRST Epic 6 story and it is a CONTENT/DATA + REPOSITORY-HARDENING story.** It delivers: (a) the eight new loot/reward `*Definition` types + their fail-closed repositories (registered through `ContentRepository`, mirroring `WeaponDefinition`/`WeaponRepository` EXACTLY); (b) the item roll-model data shape (ranges/affixes/affinities/enhancements + per-family MVP-deferral markers); (c) deterministic reward-offer FIXTURES that prove the `rewards`/`loot` named-stream contract; (d) the cross-cutting **duplicate-id fail-loud** retrofit across all repositories (the carried Epic-5 hardening item). It does NOT build the inventory model (Story 6.2), the live reward-offer FLOW/command (Story 6.3), the passive-reward modal contract (Story 6.4), Consume/Destroy commands (Stories 6.5/6.6), or the build smoke run (Story 6.7). Keep this story to the content + repository-boundary + duplicate-id-hardening scope.

- **CRITICAL — duplicate-id hardening is the load-bearing cross-cutting deliverable, and it touches code outside the new files.** The carried `[Review][Defer]` (human-ratified at Story 5-1 Phase 7) is scheduled to close HERE (epics.md AC6 + Task 6.1.5). All six existing repositories (`class`/`enemy`/`level_recipe`/`weapon`/`support`/`passive`) currently do `register_definition(...)` (overwrites by `(type, id)`) then `if not _<x>_order.has(id): _<x>_order.append(id)` — so a duplicate id keeps the FIRST in `<x>_ids()` but resolves the SECOND via `get_<x>`. The fix must be ONE uniform mechanism (do NOT fork one repo's behavior — parity was explicitly preserved through Epic 5). **Strongly recommended: add the guard ONCE in `ContentRepository.register_definition` (return/raise a structured `duplicate_definition` on a second `(type, id)`), and have each `register_<x>` surface it as a per-type `duplicate_<x>` `ActionResult.error` carrying the offending id** — this retrofits all six existing repos AND every new Epic-6 repo in one place, and is the cleanest "applied uniformly" reading of AC6. Each repo gets a negative test asserting a second registration under the same id fails loud and leaves `*_ids()`/`get_*` consistent (no silent shadow). This is the single highest-risk part of the story for breaking a prior epic's tests — re-run the FULL suite and watch the content + class + passive + run-start suites specifically (see Regression Watch below).

- **CRITICAL — `ContentRepository.register_definition` currently returns `void`. Changing its signature to return `ActionResult` is a deliberate, breaking-but-contained API change** — every caller is a `register_<x>` inside the six repositories (grep-confirmed: `register_definition` is called only from the six `*_repository.gd` files). If you change the return type, update all six call sites in the SAME change. Alternatively (lower blast radius) add a `has_definition(type, id)` pre-check inside each `register_<x>` BEFORE calling `register_definition` and return the `duplicate_<x>` error there, leaving `ContentRepository.register_definition` untouched. **PICK ONE and record it as a `[Decision]`.** The pre-check-in-each-repo option is the most surgical (zero `ContentRepository` change, each repo already null-guards + validates before registering); the central-guard option is the most DRY. Either satisfies AC6's "applied uniformly" as long as EVERY repo (six existing + new) rejects a duplicate with a structured per-type error and a negative test pins it.

- **CRITICAL — the named `rewards`/`loot` streams already EXIST on `RngStreamSet` and are INERT until this story.** `RngStreamSet.required_streams()` includes `STREAM_LOOT` (`&"loot"`) and `STREAM_REWARDS` (`&"rewards"`); `rand_int`/`rand_float` draw through them and return an `ActionResult` carrying `{value, stream_name, draw_index, state_before, state_after, ...}`. Story 6.1's AC3 is proven with **deterministic FIXTURES** (not a live command): build a `RngStreamSet.new(seed)`, draw a reward offer through `STREAM_REWARDS`/`STREAM_LOOT` via a small pure offer-builder/fixture, and assert (a) the same seed + same pre-draw state reproduces the SAME offer, (b) a different seed diverges, (c) the draw used `STREAM_REWARDS`/`STREAM_LOOT` (assert `metadata.stream_name`), (d) NO `randi`/`randf`/`RandomNumberGenerator` appears in any new file. The Epic-3 generation layer DELIBERATELY reserved these two streams for exactly this runtime reward/loot resolution (see `entity_reward_placer.gd` header: "the `rewards`/`loot` streams are reserved for RUNTIME reward/loot RESOLUTION (Epic 6+), NOT level-generation placement"). 6.1 is the first consumer.

- **CRITICAL — the inert run-level `RngStreamSet` threading (Epic-5-retro T2 / Story-4.6 `[Review][Defer]`) is a HEADS-UP, NOT this story's fix.** The retro/ledger warns: the moment a passive/reward first ROLLS run-affecting RNG, that draw MUST route through the orchestrator's run-level `streams` (and `LevelGenerator.generate` must accept an injected run-level `RngStreamSet`), or the route-position save persists a stream disconnected from where draws happen. **Story 6.1 does NOT wire a live reward roll into the orchestrator/run lifecycle** — it proves the stream contract at the FIXTURE level only (a standalone `RngStreamSet` in a test). Therefore 6.1 does NOT trip the inert-stream trap and does NOT own the T2 fix. BUT: author the offer-builder so it ACCEPTS an injected `RngStreamSet` + stream name (it draws through whatever set it is handed) rather than minting its own — so Story 6.3 (the live reward-offer flow, the first story that ROLLS a reward through the run) can hand it the orchestrator's run-level `streams` and own the T2 threading without reshaping 6.1's builder. Record this seam explicitly in Completion Notes so 6.3's dev does not fork a parallel offer path. [Source: deferred-work.md "code review of 4-6" inert-RngStreamSet `[Review][Defer]`; epic-5-retro-2026-06-26.md §7 risk #2 + §8 T2]

- **CRITICAL — passive DEFINITIONS already exist; this story does NOT re-author or re-shape `PassiveDefinition`/`PassiveRepository`, and does NOT build the full 20-30 passive pool.** Story 5.4 shipped `PassiveDefinition` (typed `Resource`: `passive_id`/`display_name`/`passive_kind`/`trigger_windows`/`explanation`, EXPLANATION-ONLY v0) + the fail-closed `PassiveRepository` (six baseline starting passives). AC1 lists "passives" among the categories "available through typed lookups" — that is SATISFIED for the EXISTING passive repository (do not re-create it). The broader **20-30 MVP passive POOL (FR46)** and the **per-effect passive OPERATION model** (`RuleCondition`/`RuleTarget`/`RuleOperation`, stacking/conflict/duration, combat hook sites) are LATER Epic-6 work (the pool is content that can grow across 6.x; the operation engine + felt-in-combat effect is the documented 5.4/5.5 -> Epic-6 handoff). For THIS story: (a) `PassiveRepository` gets the duplicate-id retrofit (AC6) + a negative test, and (b) if reward offers reference passives, they reference EXISTING passive ids by-id (shape-validated), they do NOT author new passive content. Do NOT build the effect/operation engine or the full pool here. Record as a `[Decision]` whether you add any NEW passive definitions this story (recommended: NO — keep the six baselines; grow the pool in a later 6.x passive-pool story) — but at MINIMUM the passive repo must reject a duplicate id. [Source: epics.md FR46; deferred-work.md "dev of 5-4"/"dev of 5-5" per-effect-operations deferred-to-Epic-6; project-context.md "broader 20-30 MVP passive POOL (FR46) is Epic 6"]

- **Reward/loot DEFINITION content vs the Epic-3 generation-layer reward MARKERS — keep them distinct.** The generation layer (`EntityRewardPlacer`, Story 3.5) places abstract reward MARKERS (`{x, y, optional}` payload dicts) on the board; it has NO reward CONTENT (no `RewardTableDefinition`, no concrete loot). Story 6.1 builds the reward/loot CONTENT (the definitions + tables a reward marker would later resolve INTO) — it does NOT touch `EntityRewardPlacer`, the level generators, the reward markers, or the level fingerprints. The marker->content resolution (a board reward marker yields a concrete reward offer) is a LATER story (6.3 reward-offer flow + the run/level-entry wiring); 6.1 only provides the approved content + the deterministic offer FIXTURE proving the stream contract. [Source: godot/scripts/generation/level/entity_reward_placer.gd header "RewardTableDefinition / concrete loot -> Epic 6 (Story 3.5.5)"; project-context.md "There is NO RewardTableDefinition in v0 ... concrete loot tables/repository are Epic 6"]

- **Reward category / value model (AC1 + FR52/FR53): MVP loot categories are weapons, armor, jewelry, support items, consumables, pickups, passives, gold.** Weapons + support items already have repositories (`WeaponRepository`/`SupportRepository`) — REUSE them for those categories (a reward of category `weapon` references an existing `weapon_id`; a reward of category `support` references an existing `support_id`; the duplicate-id retrofit covers both). The NEW definition types this story introduces are: **armor**, **jewelry**, **consumable**, **pickup**, **gold reward**, and an **item roll-model** shape (ranges/affixes/affinities/enhancements). Plus a **reward-table / reward-pool** definition (the approved table a reward offer draws from). Consumables are SEMI-RARE and should feel worth using (FR53/GDD line 325) — model scarcity/value as DATA on the consumable/reward-table definition (a rarity tier + a value/weight field), not as runtime logic this story (the live drop-rate application is Story 6.3+). [Source: epics.md Story 6.1 AC1; FR52/FR53; gdd.md lines 325-328]

- **Equip gate (AC4 + GDD FR75/FR76): character-level requirement OR explicit `none`; NEVER run-depth.** Each EQUIPPABLE item definition (weapon/armor/jewelry/support — the equippable categories) carries a character-level requirement: a small non-negative int, OR a named `none` sentinel (mirroring `SupportDefinition.SUPPORT_NONE := &"none"` — a real, resolving "no requirement" value, NEVER treated as missing). `validate()` REJECTS a definition that gates on run-depth (do NOT add a `min_run_depth`/`min_node_depth` field; if forward-shape tempts you, omit it — AC4 forbids run-depth as the equip gate). Consumables/pickups/gold are NOT "equipped" so they need no equip gate (decide + document whether they carry a level requirement at all — cleanest: only the equippable categories carry it). [Source: epics.md Story 6.1 AC4; gdd.md line 327 "Items use character-level requirements, not minimum run requirements"]

- **Item roll model (AC5 + GDD FR76): ranges, affix/enhancement ids, affinity tags — NOT fixed item levels.** The item instance-variation data shape uses: a roll RANGE (min/max int band) for variable stats; affix IDs + enhancement IDs (lower_snake string references — the affix/enhancement CONTENT itself may be MVP-deferred); affinity TAGS (lower_snake — the affinity content is Epic 7, so these are forward-shape tags this story shape-validates but does not resolve). There is NO `item_level` field. Each advanced roll family NOT implemented for MVP (e.g. multi-affix rolls, enhancement application, affinity rolls) carries an explicit MVP-DEFERRAL marker (a per-family `*_mvp_deferred: bool` / a deferral note) so the field/shape exists and validates INERT, preserving the data boundary for Epic 7+/post-MVP without a schema fork. Record which families ship "live" (a single roll range is the realistic MVP) vs "shape-only-deferred" (affixes/enhancements/affinities) as a `[Decision]`. [Source: epics.md Story 6.1 AC5 + Task 6.1.3; gdd.md line 328 "Items roll ranges, affixes, affinities, and enhancements rather than fixed item levels"; epics.md "Hundreds of loot affixes" listed as a NON-MVP / later item (gdd.md line 678)]

- **Scope discipline — what 6.1 MUST NOT do (the biggest risk, exactly as for every first-epic content story + the cross-cutting retrofit):**
  - NO inventory / backpack / equipment-slot model or item-gained/inventory-full events (Story 6.2 — the small-inventory + equipment model).
  - NO live reward-offer FLOW / reward command / "offer stored in domain state until resolved" / reward-applied events (Story 6.3 — the reward-offer flow; 6.1 only proves the stream contract via FIXTURES + provides the content the flow draws from).
  - NO passive-reward MODAL data contract (icon/name/flavor/effects/Consume/Destroy fields surfaced to UI — Story 6.4).
  - NO Consume / Destroy commands or the 70/20/10 Destroy outcome distribution (Stories 6.5/6.6; FR82).
  - NO loot-and-passive build SMOKE run (Story 6.7).
  - NO per-effect passive OPERATION engine (`RuleCondition`/`RuleTarget`/`RuleOperation`, stacking/conflict/duration) or combat HOOK sites (the documented 5.4/5.5 -> Epic-6 handoff; a LATER Epic-6 story owns the effect model). `scripts/rules/{conditions,operations}` STAY EMPTY this story.
  - NO change to `EntityRewardPlacer` / the level generators / reward MARKERS / level or route fingerprints (the generation reward markers stay as-is; 6.1 builds reward CONTENT, not placement).
  - NO change to `RunState` / `RunStartCommand` / `RunOrchestrator` / the route-position save / the 23-key `RunSnapshot` gate (no new top-level snapshot key; reward/loot state is NOT persisted this story — there is no run reward state yet, that is 6.3+).
  - NO live run-affecting RNG roll wired into the orchestrator/run lifecycle (the offer-builder draws through an INJECTED `RngStreamSet` only inside fixtures; the T2 orchestrator threading is Story 6.3's, not 6.1's).
  - NO JSON/CSV content pipeline / `.tres` mirror / schema file UNLESS you deliberately choose to introduce it now (see the JSON-pipeline `[Decision]` below) — the safe default is code-constant baselines (the existing weapon/enemy/support/recipe/class/passive convention).
  - NO active class skills, difficulty knob, cloud/account/multiplayer/telemetry/.NET/new-test-framework/new-engine-library/prototype dependency.

- **JSON content-pipeline `[Decision]` (epics.md flags Epic 6 as where the data pipeline MAY arrive; the duplicate-id AC is sequenced "before/with the first new data-pipeline content").** `project-context.md` records that the JSON-source -> typed-`Resource` mirror is "a later (Epic 6) decision; do not introduce it early," and `data/source` + `data/resources` are STILL EMPTY. You may introduce the JSON pipeline now OR keep code-constant baselines for 6.1. **RECOMMENDED for 6.1: keep CODE-CONSTANT baselines** (mirror the existing eight content types exactly), and let a later story introduce the JSON pipeline if/when content volume demands it — this keeps 6.1 focused on the definition shapes + the duplicate-id retrofit without coupling the (highest-risk) cross-cutting hardening to a brand-new file-loading boundary. If you DO introduce JSON: route every read through the repository/import boundary (NEVER a gameplay/hot path), handle `JSON.parse_string` returning `null` as a structured error inside the repo, source under `godot/data/source/loot/`, mirror `.tres` under `godot/data/resources/loot/`, and the duplicate-id guard becomes EVEN more load-bearing (copy-paste/merge authoring errors are exactly the trap). Record the choice as a `[Decision]` either way. [Source: project-context.md Technology Stack "The JSON-source -> typed-Resource mirror is a later (Epic 6) decision; do not introduce it early"; epics.md Story 6.1 Task 6.1.5 "before/with the first new data-pipeline content"]

## Tasks / Subtasks

- [ ] **Task 1 — Pre-implementation gate + FAILING tests first (TDD red) (AC: 1-6)**
  - [ ] 1.1 Confirm in `_bmad-output/implementation-artifacts/sprint-status.yaml` that `epic-1`..`epic-5` are `done`, `epic-6` is `in-progress` (create-story flips it), and this story (`6-1-item-loot-and-reward-definitions`) is `ready-for-dev`. If any earlier Epic 1-5 status regressed, STOP and restore the boundary before implementing loot content. (The orchestrator owns the `epic-6` flip + closeout — do NOT flip `epic-6` yourself.)
  - [ ] 1.2 Confirm the working tree is clean or that dirty files are intentional user work; preserve unrelated changes. The untracked orchestrator-owned `_bmad-output/auto-gds/` directory is expected and is NOT your change.
  - [ ] 1.3 Add focused FAILING tests before any production edit. New test files under `godot/tests/unit/content/` (one per new definition + repository) and `godot/tests/unit/rules/` or `godot/tests/unit/content/` for the reward-offer fixture (the headless runner auto-discovers `test_*.gd` under `godot/tests/unit` and `godot/tests/integration` ONLY — NOT `tests/fixtures`, NOT `tests/headless`; no registry/`.tscn` edit needed). Mirror `test_weapon_repository.gd` / `test_passive_repository.gd` / `test_support_definition.gd` structure exactly: an `EXPECTED_*` dict, a stable-ordered registration assertion, a per-field validation positive+negative assertion, a generic-`ContentRepository`-boundary assertion, and a fail-closed-factory assertion. ADD a **duplicate-id negative test to EVERY repository test** (the six existing + every new one). Confirm RED (compile-fail / fail) before implementing.
  - [ ] 1.4 Reuse existing primitives — `ActionResult` (`scripts/core/results/action_result.gd`), the `ContentRepository` + `*Definition`/`*Repository` PATTERN (`scripts/content/`), the `_is_lower_snake_id`/`_invalid` validation idiom (copied verbatim in every definition), the `&"none"` sentinel precedent (`SupportDefinition.SUPPORT_NONE`), and `RngStreamSet` (`scripts/core/state/rng_stream_set.gd`) with its `STREAM_REWARDS`/`STREAM_LOOT` + `rand_int`/`rand_float`. Do NOT invent a new result type, a parallel content loader, a second RNG type, or a new repository abstraction.
  - [ ] 1.5 Do NOT in this story: build the inventory/equipment model (6.2), the live reward-offer flow/command (6.3), the passive-reward modal contract (6.4), Consume/Destroy (6.5/6.6), the build smoke run (6.7), the per-effect passive operation engine / combat hook sites (later Epic-6), the full 20-30 passive pool (later Epic-6); touch `EntityRewardPlacer`/the level generators/reward markers/level+route fingerprints, `RunState`/`RunStartCommand`/`RunOrchestrator`/the route-position save/the 23-key `RunSnapshot` gate, settings, meta, or tactical truth; wire a live reward roll into the orchestrator; add cloud/account/telemetry or any new engine/library/test-framework dependency.

- [ ] **Task 2 — Duplicate-id fail-loud retrofit across ALL content repositories (AC: 2, 6) — the cross-cutting hardening (do this FIRST so every new repo inherits it)**
  - [ ] 2.1 Choose the mechanism + record it as a `[Decision]`: (Option Central) add the guard once in `ContentRepository.register_definition` (return a structured `duplicate_definition` `ActionResult` on a second `(type, id)`; change its return type from `void` to `ActionResult` and update all SIX call sites in the same change); OR (Option Per-Repo) add a `has_definition(type, id)` pre-check inside each `register_<x>` that returns the per-type `duplicate_<x>` error BEFORE calling `register_definition` (zero `ContentRepository` change). Either must reject a duplicate with a structured per-type error (`duplicate_<x>`, offending id in metadata) and leave `<x>_ids()` + `get_<x>` consistent (no silent shadow — the rejected definition is neither in the id-list nor resolvable).
  - [ ] 2.2 Apply the guard to the SIX existing repositories: `class_repository.gd`, `enemy_repository.gd`, `level_recipe_repository.gd`, `weapon_repository.gd`, `support_repository.gd`, `passive_repository.gd`. Each `register_<x>` now returns a `duplicate_<x>` `ActionResult.error` (`{reason: "duplicate_id", id: <offending>}` or similar) on a second registration under an already-present id; the baselines (all distinct ids) still register cleanly with NO behavior change. The `create_repository_from_definitions` factories already fail closed on a `register_<x>` error, so a duplicate in a definitions batch now returns `null` (fail-closed) — assert this.
  - [ ] 2.3 Add a duplicate-id NEGATIVE test to each of the six existing repository test files (`test_class_repository.gd`, `test_enemy_repository.gd`, `test_level_recipe_repository.gd`, `test_weapon_repository.gd`, `test_support_repository.gd`, `test_passive_repository.gd`): register a baseline, then register a SECOND distinct definition under the SAME id; assert the second `register_<x>` returns the `duplicate_<x>` error, `<x>_ids()` still lists the id exactly once, `get_<x>(id)` still returns the FIRST (or assert the rejected def did not become canonical), and a `create_repository_from_definitions([defA, defA_dup])` returns `null`. This CLOSES the carried Epic-5 `[Review][Defer]`.
  - [ ] 2.4 Re-run the FULL headless suite NOW (before adding new content) to prove the retrofit broke nothing — the content/class/passive/run-start suites must stay green (a duplicate-id reject must not trip any baseline registration or any `RunStartCommand` content gate). This is the single highest-risk change; isolate it.

- [ ] **Task 3 — Implement the new loot/reward `*Definition` types (typed `Resource`s, repository-loaded) (AC: 1, 2, 4, 5)**
  - [ ] 3.1 Add the new definitions under `godot/scripts/content/definitions/` (each `extends Resource`, data-layer only, NOT a Node). Mirror `WeaponDefinition`/`SupportDefinition`/`LevelRecipeDefinition` EXACTLY: `const DEFINITION_TYPE := &"<unique>"` (verify the string does NOT collide with existing types `weapon`/`support`/`enemy`/`level_recipe`/`class`/`passive`), named consts for enum-like fields, `@export` typed fields, `_init(...)` with defaulted args IN FIELD-DECLARATION ORDER, `validate() -> ActionResult` returning `_invalid(<field>)` (`invalid_<type>_definition` + `{reason:"invalid_field", field:...}`) on the FIRST bad field (REJECT, never coerce), and the `_is_lower_snake_id`/`_invalid` helpers copied verbatim. New types: `armor_definition.gd`, `jewelry_definition.gd`, `consumable_definition.gd`, `pickup_definition.gd`, `gold_reward_definition.gd`, `reward_table_definition.gd` (the approved reward pool/table), and an item roll-model shape (either a standalone `item_roll_model.gd` value type OR roll fields embedded on the equippable definitions — decide + document). Use names unless implementation finds a clearer local pattern (see Recommended New Files).
  - [ ] 3.2 EQUIP-GATE field (AC4) on each EQUIPPABLE definition (armor/jewelry, plus reuse the weapon/support categories): a `character_level_requirement: int` (>= 0) with a named `LEVEL_REQUIREMENT_NONE` sentinel (e.g. `0` meaning "no gate", OR a `requires_level: bool` + int — PICK ONE; the `&"none"`-sentinel idiom from `SupportDefinition` is the project precedent for "a real, resolving no-requirement value"). `validate()` rejects a negative requirement and rejects ANY run-depth gate (do NOT add `min_run_depth`/`min_node_depth`; AC4 forbids it). Consumables/pickups/gold are not equipped — decide whether they carry the field at all (cleanest: only equippable categories carry it).
  - [ ] 3.3 ROLL-MODEL fields (AC5) for item instance variation: a roll RANGE (`roll_min: int`/`roll_max: int`, `roll_max >= roll_min`), affix IDs (`affix_ids: Array[StringName]`, lower_snake shape), enhancement IDs (`enhancement_ids: Array[StringName]`, lower_snake shape), affinity TAGS (`affinity_tags: Array[StringName]`, lower_snake shape — Epic-7 content, forward-shape only). NO `item_level` field. Per-advanced-family MVP-DEFERRAL markers (e.g. `affixes_mvp_deferred: bool` / `enhancements_mvp_deferred: bool` / `affinities_mvp_deferred: bool`) so a deferred family validates INERT (shape present, not resolved) — the data boundary is preserved for Epic 7+ without a schema fork. `validate()` shape-checks the id arrays and the range; it does NOT resolve affix/enhancement/affinity content against any repository (that content is later — same precedent as `ClassDefinition` carrying passive ids by-shape-only, and `LevelRecipeDefinition` carrying reward/wrinkle refs without resolving them).
  - [ ] 3.4 CATEGORY + VALUE fields (AC1, FR52/FR53): each definition carries its category (the `DEFINITION_TYPE` distinguishes the repo; a `reward_table` entry carries an explicit `category` + a referenced content `id`). Consumable/reward rarity + value: a `rarity` (named allowlist, e.g. `common`/`uncommon`/`rare`) + a `value`/`weight` int on the consumable + reward-table entries (data only — the live drop-rate/scarcity APPLICATION is Story 6.3+). Gold-reward carries an amount band (`gold_min`/`gold_max`, both >= 0, `max >= min`). Keep every enum-like string lower_snake + allowlist-validated.
  - [ ] 3.5 REWARD-TABLE / pool definition (`reward_table_definition.gd`): an approved table a reward offer draws from — a `table_id` (lower_snake) + an ordered list of weighted entries `{category, content_id, weight}` (each category in the allowlist, each `content_id` lower_snake shape, each `weight` > 0). `validate()` rejects an empty table, a bad category, a non-lower_snake content id, or a non-positive weight. This is the content the AC3 offer-fixture draws from deterministically. It references content by-id (shape only) — it does NOT resolve the referenced weapon/armor/consumable/etc. against their repositories in `validate()` (resolution is the offer flow's job, Story 6.3 — same by-id-defer precedent as `LevelRecipeDefinition`).

- [ ] **Task 4 — Implement the new repositories (fail closed, register through `ContentRepository`, reject duplicates) (AC: 1, 2, 6)**
  - [ ] 4.1 Add the new repositories under `godot/scripts/content/repositories/` (each `extends RefCounted`). Mirror `WeaponRepository`/`PassiveRepository` EXACTLY: hold a `ContentRepository`; expose `register_<x>(def) -> ActionResult` (null-guard -> `def.validate()` -> duplicate-id check (Task 2 mechanism) -> `register_definition(<TYPE>, def.<id>, def)` -> track stable order), a COLLISION-FREE accessor `get_<x>(id) -> <X>Definition` (return `null` on a miss — fail-closed; **AVOID reserved `Object` method names** `get_class`/`get_script`/`get_meta`/`get_instance_id`/etc. — the Epic-5 `get_class` parse-error lesson; e.g. use `get_armor`/`get_jewelry`/`get_consumable`/`get_pickup`/`get_gold_reward`/`get_reward_table`, all collision-free), `has_<x>(id) -> bool`, `<x>_ids() -> Array[StringName]` (stable, `.duplicate()`d), `content_repository() -> ContentRepository`, `register_baseline_<x>s() -> ActionResult`, a `static create_baseline_repository(content_repository=null)`, and a `static create_repository_from_definitions(defs, content_repository=null)` that PRE-VALIDATES every def and returns `null` on the first invalid/null/DUPLICATE (FAIL CLOSED — use the `load("res://.../<x>_repository.gd").new(...)` factory idiom to avoid the self-`class_name` cyclic-preload quirk). Add a `BASELINE_<X>_IDS: Array[StringName]` const.
  - [ ] 4.2 `static _baseline_definitions()` for each new type: a small set of APPROVED MVP baselines with stable lower_snake ids and REAL referenced ids (a reward-table entry that references a weapon uses a real `WeaponRepository` baseline id; an entry that references a consumable uses a baseline consumable id this story authors). Keep the baselines minimal but representative (enough to prove the offer fixture draws a real, varied offer). These are approved static definitions — do NOT generate at runtime.
  - [ ] 4.3 AC2 fail-closed proof per repo: `get_<x>(unknown)` -> `null`; `register_<x>(null)` / `register_<x>(<invalid def>)` -> structured `ActionResult.error`; `register_<x>(<dup id>)` -> `duplicate_<x>`; `create_repository_from_definitions([<invalid>])` / `([defA, defA_dup])` -> `null`; the factory does NOT mutate a provided `ContentRepository` on failure. No silent default fabrication anywhere.

- [ ] **Task 5 — Deterministic reward-offer fixture proving the `rewards`/`loot` named-stream contract (AC: 3)**
  - [ ] 5.1 Add a small pure offer-builder (e.g. `godot/scripts/content/reward_offer_builder.gd`, `RefCounted`, OR a fixture-local helper) that, GIVEN an injected `RngStreamSet` + a stream name (`STREAM_REWARDS` or `STREAM_LOOT`) + a `RewardTableDefinition`, draws a deterministic offer (e.g. weighted-pick N entries by drawing `rand_int` on the stream) and returns a serializable offer dict (the selected `{category, content_id}` entries + any rolled range value). It draws EXCLUSIVELY through the injected set's `rand_int`/`rand_float`; it NEVER mints a `RandomNumberGenerator`, NEVER calls `randi`/`randf`, NEVER touches another stream. It is a PURE read of the table + a draw through the handed-in stream (no global state, no save, no command). Author it to ACCEPT the injected `RngStreamSet` so Story 6.3 can hand it the orchestrator's run-level `streams` (the T2 seam) without reshaping it.
  - [ ] 5.2 Add `godot/tests/unit/.../test_reward_offer_fixture.gd` (or `test_reward_offer_builder.gd`) asserting: (a) DETERMINISM — `RngStreamSet.new(SEED)` -> build offer -> capture; a fresh `RngStreamSet.new(SEED)` (same pre-draw state) -> build the SAME offer (byte-equal dict); (b) DIVERGENCE — a different seed yields a different offer across a small seed sample (>= 2 distinct outcomes), proving the draw actually keys off the stream; (c) STREAM PROOF — the draw's `ActionResult.metadata.stream_name` equals `"rewards"` / `"loot"` (assert the named stream was used, not another); (d) STATE-REPRODUCTION — after drawing, the stream's `state_after` (or a restored same-state set) reproduces the SAME next draw (the existing `to_snapshot`/`try_restore` round-trip idiom — mirror `test_run_route_position_save.gd`'s next-draw reproduction); (e) NO-RNG-LEAK — grep-confirm (and assert by construction) the builder + fixture contain no `randi`/`randf`/`RandomNumberGenerator`. This proves AC3 at the fixture level; the live flow is Story 6.3.
  - [ ] 5.3 Do NOT wire this fixture into the orchestrator / run lifecycle / save. It is a standalone deterministic proof. Record in Completion Notes that the live reward roll (routing through the orchestrator's run-level `streams` — the T2 inert-stream fix) is Story 6.3's, and that the builder already accepts an injected set so 6.3 extends rather than forks it.

- [ ] **Task 6 — Run the full headless suite + diff check (gate before review) (AC: 1-6)**
  - [ ] 6.1 Run through PowerShell (the bare `godot` resolves only as `C:\Users\Rasmus\bin\godot.cmd` via PowerShell; the Bash tool PATH cannot find it): `godot --version`, then `godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10`, then `git diff --check`.
  - [ ] 6.2 Expect: Godot `4.6.3.stable.official...`; the full headless runner exits code `0`, final line `Headless tests passed.`; ALL prior Epic 1-5 tests stay green — ESPECIALLY the content suite (`test_weapon_repository.gd`/`test_enemy_repository.gd`/`test_support_repository.gd`/`test_level_recipe_definition.gd`/`test_level_recipe_repository.gd`/`test_class_definition.gd`/`test_class_repository.gd`/`test_passive_definition.gd`/`test_passive_repository.gd`) given the duplicate-id retrofit, AND the run-start suite (`test_run_start_command.gd`) since `RunStartCommand`'s three content gates resolve through these repos; `test_project_structure.gd` stays green (your new files live inside the already-required `scripts/content` root — do NOT add a new domain/data root unless you introduced the JSON pipeline, which adds files under the already-required `data/source`+`data/resources` roots, still no NEW root). `git diff --check` reports no whitespace errors.
  - [ ] 6.3 **Heavy/cross-domain false-PASS guard (Epic-5-retro P2 — standing gate):** the headless runner can print PASS for a script that FAILED to load (a return-type/parse error emits "Compile Error at line 0" / "SCRIPT ERROR" yet the PASS sentinel still prints — this bit Story 5-1). You add MANY new content/test files AND change six existing repositories' return types — after the suite, grep the run output DIRECTLY for `SCRIPT ERROR` / `Parse Error` / `Compile Error` attributable to ANY new or modified file (the new definitions/repositories/builder/tests AND the six retrofitted repos), do NOT trust the PASS summary line alone. This is cheap and prevents a silent green-suite miss. The pre-existing `ERROR: Parse JSON failed` (save/settings suites) + the int64-overflow / `invalid_node_type` stderr lines are documented-expected negative-path diagnostics from OTHER suites, NOT failures.

- [ ] **Task 7 — Update story records (AC: 1-6)**
  - [ ] 7.1 Update this story's Dev Agent Record, Completion Notes, File List, Change Log, and Status (-> `review` when implementation + tests pass). Record the `[Decision]`s: (a) duplicate-id mechanism (central `ContentRepository` guard vs per-repo `has_definition` pre-check); (b) JSON pipeline vs code-constant baselines; (c) roll-model shape (standalone value type vs embedded fields) + which roll families ship live vs shape-only-deferred; (d) equip-gate sentinel shape (named `none` vs `0`-means-none vs bool+int); (e) whether any NEW passive definitions were added (recommended NO); (f) the reward-table + offer-builder shape + the injected-`RngStreamSet` seam left for Story 6.3.
  - [ ] 7.2 Keep `sprint-status.yaml` synchronized with this story's status. This is the FIRST Epic 6 story; the orchestrator owns the `epic-6` status flip and any epic closeout — do NOT flip `epic-6` yourself.
  - [ ] 7.3 **CLOSE the carried duplicate-id `[Review][Defer]` in `deferred-work.md`:** the entry under "code review of 5-1" (and its "NOW SCHEDULED" note pointing at Story 6.1 Task 6.1.5) stays OPEN "until that task lands; close it then." When the retrofit + negative tests are green, mark that ledger entry CLOSED-by-Story-6.1 (or move it to `deferred-work-resolved.md` per the project's archive convention) with the date — that is THIS story's responsibility, not a later one's.
  - [ ] 7.4 Clean up any `user://` artifact your tests create. This story does NO save/resume work and should not write `user://` at all — if a test does (e.g. the offer-fixture state round-trip uses an in-memory `RngStreamSet`, not a file), sweep it (`*.json`, `*.json.tmp`, `*.json.bak`). Leave `user://` clean.

## Dev Notes

### Pre-Implementation Gate

This is the **FIRST Epic 6 implementation story** (Epic 6: Loot, Passives, and Consume/Destroy). It establishes the LOOT/REWARD CONTENT layer (the eight category definitions + repositories + the item roll model), the deterministic reward-offer FIXTURE proving the `rewards`/`loot` named-stream contract, and the cross-cutting **duplicate-id fail-loud** retrofit across all content repositories. Stories 6.2 (inventory/equipment), 6.3 (reward-offer flow), 6.4 (passive-reward modal), 6.5/6.6 (Consume/Destroy), and 6.7 (build smoke run) build on it. Story-creation analysis on 2026-06-26 found:

- `epic-1: done`, `epic-2: done`, `epic-3: done`, `epic-4: done`, `epic-5: done` (all retros `done`); Story 6.1 was `backlog` and is now `ready-for-dev`; `epic-6` flips `backlog -> in-progress` by create-story. Stories 6.2-6.7 are `backlog` (no story files yet).
- **NO loot/reward/consumable/item/equipment content exists anywhere** in `godot/` (a project-wide grep for `RewardTable`/`loot_definition`/`ConsumableDefinition`/`ItemDefinition`/`GoldReward`/`jewelry`/`armor_definition`/`pickup`/`reward_repository`/`loot_repository` finds only the Epic-3 generation-layer reward MARKER code in `entity_reward_placer.gd`/`level_validator.gd` and the `reward_offered`/`reward_*` STREAM/window names — NOT a content table). The loot/reward content layer is the greenfield deliverable here.
- The content layer (`ContentRepository`, `WeaponDefinition`/`EnemyDefinition`/`SupportDefinition`/`LevelRecipeDefinition`/`ClassDefinition`/`PassiveDefinition` + their repositories), `ActionResult`, the lower-snake-id validation idiom, the `&"none"` sentinel, and `RngStreamSet` (with INERT `STREAM_REWARDS`/`STREAM_LOOT`) all exist and are the PATTERN/integration points — mirror them; do NOT fork them. `PassiveDefinition`/`PassiveRepository` exist (Story 5.4) — do NOT re-author them; they get the duplicate-id retrofit + a negative test only.
- The **duplicate-id last-write-wins trap is OPEN and human-ratified** as a cross-cutting hardening item (deferred-work.md "code review of 5-1", folded to six repos by 5-4), and is SCHEDULED to close here (epics.md AC6 + Task 6.1.5). This story OWNS closing it.

Before implementing, re-confirm the local tree is clean or that dirty files are intentional user work. If any Story 1.x-5.x status regressed, stop and restore that boundary first.

### Scope Boundary

This story delivers the **loot/reward content layer + the duplicate-id hardening**: eight new/extended content categories' definitions + fail-closed repositories (armor, jewelry, consumable, pickup, gold-reward, reward-table; reusing weapon/support; passive already exists), the item roll-model data shape (ranges/affixes/affinities/enhancements + per-family MVP-deferral markers), a deterministic reward-offer FIXTURE proving the `rewards`/`loot` named-stream contract, and the fail-loud duplicate-id guard retrofit across ALL content repositories (closing the carried Epic-5 `[Review][Defer]`). NO inventory, NO live reward flow, NO modal contract, NO Consume/Destroy, NO effect/operation engine, NO save/RunState change.

In scope:

- New `*Definition` (`Resource`) types: `armor_definition.gd`, `jewelry_definition.gd`, `consumable_definition.gd`, `pickup_definition.gd`, `gold_reward_definition.gd`, `reward_table_definition.gd` (+ an item roll-model shape), each with `DEFINITION_TYPE`, named consts, typed `@export` fields, `_init(...)` in field-declaration order, fail-closed `validate()` (REJECT, never coerce; character-level-requirement-or-`none`, run-depth-gate REJECTED, roll-ranges/affix-ids/affinity-tags + MVP-deferral markers), lower-snake helpers copied verbatim.
- New `*Repository` (`RefCounted`) types mirroring `WeaponRepository`/`PassiveRepository`: register/get/has/ids (stable, collision-free accessor names), `content_repository()`, `register_baseline_<x>s()`, fail-closed `create_baseline_repository`/`create_repository_from_definitions`, `BASELINE_<X>_IDS`, `_baseline_definitions()`, AND the duplicate-id reject.
- Duplicate-id retrofit across the SIX existing repos (`class`/`enemy`/`level_recipe`/`weapon`/`support`/`passive`) + the new ones, with a negative test per repo.
- A pure reward-offer-builder + a deterministic fixture proving the `rewards`/`loot` stream contract (determinism + divergence + named-stream + state-reproduction + no-RNG-leak).
- Tests: per-definition validation (positive + every-field negative), per-repository registration/fail-closed/duplicate-reject, the reward-offer fixture, and the six existing repos' new duplicate-id negatives.

Out of scope (owned elsewhere — do NOT build here):

- **Small inventory + equipment model + item-gained/inventory-full events.** Story 6.2 builds the 6-slot backpack, equip slots, item-instance/id tracking, and the inventory events. 6.1 only DEFINES the content the inventory will hold. [Source: epics.md Story 6.2]
- **Live reward-offer FLOW + reward command + offer-in-domain-state + reward-applied events.** Story 6.3 generates a deterministic reward offer from approved tables, stores it in domain state until resolved, applies the selected reward through domain events, and is the FIRST story that ROLLS a reward through the run (owning the T2 orchestrator `RngStreamSet` threading). 6.1 only provides the content + proves the stream contract via FIXTURES. [Source: epics.md Story 6.3; deferred-work.md "code review of 4-6" inert-stream `[Review][Defer]`]
- **Passive-reward MODAL data contract** (icon/evocative name/flavor/exact effects/Consume/Destroy fields surfaced for UI). Story 6.4. [Source: epics.md Story 6.4; gdd.md lines 331-338]
- **Consume / Destroy commands + the 70/20/10 Destroy outcome distribution.** Stories 6.5/6.6 (FR82, GDD 70/20/10). 6.1 builds no command and no outcome distribution. [Source: epics.md Stories 6.5/6.6; FR82]
- **Loot-and-passive build SMOKE run.** Story 6.7. [Source: epics.md Story 6.7]
- **Per-effect passive OPERATION engine + combat HOOK sites + the full 20-30 passive POOL.** The effect model (`RuleCondition`/`RuleTarget`/`RuleOperation`, stacking/conflict/duration) + wiring a passive to mutate a combat number is the documented 5.4/5.5 -> Epic-6 handoff (a LATER Epic-6 story); the 20-30 pool (FR46) is content that grows across 6.x. 6.1 keeps the six baseline passives and authors NO effect engine. `scripts/rules/{conditions,operations}` STAY EMPTY. [Source: deferred-work.md "dev of 5-4"/"dev of 5-5"; epics.md FR46; project-context.md rules-kernel section]
- **`EntityRewardPlacer` / the level generators / reward MARKERS / level + route fingerprints.** Untouched — 6.1 builds reward CONTENT, not placement; the marker->content resolution is Story 6.3+. [Source: godot/scripts/generation/level/entity_reward_placer.gd header]
- **`RunState` / `RunStartCommand` / `RunOrchestrator` / the route-position save / the 23-key `RunSnapshot` gate.** Untouched — no run reward state is persisted this story (that is 6.3+); add NO new top-level snapshot key. [Source: project-context.md save/snapshot rules; Epic-4 run-progression rules]
- A live RNG roll wired into the orchestrator/run lifecycle (the offer-builder draws through an INJECTED `RngStreamSet` inside fixtures only); difficulty knobs; cloud/accounts/multiplayer/telemetry/.NET/new-frameworks/prototype dependencies.

### Current Repository Baseline (READ THIS FIRST — loot content is greenfield; mirror the content + ActionResult + RngStreamSet patterns)

The most important constraint: **the loot/reward content is NEW. Mirror the proven content/result/RNG patterns; do NOT fork `ActionResult`, the `ContentRepository`, the `*Definition`/`*Repository` shape, or `RngStreamSet`, and do NOT invent a parallel content loader or a second RNG type.** The closest precedents are `WeaponDefinition`/`WeaponRepository` (a definition with bands + a multi-field `validate()`), `SupportDefinition` (the `&"none"` sentinel + a coupling guard), `LevelRecipeDefinition` (references other content BY-ID without resolving it in `validate()` — the precedent for reward-table entries + affix/affinity ids), and `PassiveRepository` (the most recent fail-closed repo with a pre-validate-then-register factory).

- `godot/scripts/content/repositories/content_repository.gd` — `ContentRepository extends RefCounted`. The generic boundary: `register_definition(definition_type, definition_id, definition)` (**currently returns `void`** and OVERWRITES by `(type, id)` — this is the duplicate-id trap), `get_definition(...)` (returns `null` on a miss — the fail-closed lookup), `has_definition(...)`. AC6's central-guard option changes `register_definition` to return `ActionResult` and reject a second `(type, id)`; the per-repo option leaves it untouched and pre-checks `has_definition` in each `register_<x>`. Grep-confirmed `register_definition` is called ONLY from the six `*_repository.gd` files.
- `godot/scripts/content/definitions/weapon_definition.gd` — the richest `validate()` model: `extends Resource`, `const DEFINITION_TYPE := &"weapon"`, named consts, `@export` typed fields, `_init(...)` in field-declaration order, multi-field `validate()` with band/coupling checks (e.g. `adjacency_modifier_id` <-> `adjacency_damage_multiplier`), `_is_lower_snake_id`/`_invalid` static helpers (copy verbatim). The error code is `invalid_weapon_definition` + `{reason:"invalid_field", field:...}`. THIS is the shape for armor/jewelry/consumable/etc.
- `godot/scripts/content/definitions/support_definition.gd` — the `&"none"` sentinel precedent: `SUPPORT_NONE := &"none"` is a REAL, resolving support id, NEVER "missing" — use the same idiom for the equip-gate "no requirement" value. Also a coupling guard (`bonus_damage` <-> `bonus_weapon_ids`) useful for the roll-model `*_mvp_deferred` <-> field-population couplings.
- `godot/scripts/content/definitions/level_recipe_definition.gd` (Story 3.1) — the precedent for "a definition that references other content BY ID without resolving it in `validate()`" (it carries reward/wrinkle/budget refs; resolution happens at consumption time). This is the precedent for reward-table entries (`{category, content_id, weight}` shape-only) and for affix/enhancement/affinity ids on the roll model (shape-only forward refs).
- `godot/scripts/content/repositories/weapon_repository.gd` AND `passive_repository.gd` — the `*Repository` PATTERN to copy: hold a `ContentRepository`; `register_<x>(def) -> ActionResult` (null-guard -> `validate()` -> [duplicate check] -> `register_definition` -> track stable order); `get_<x>(id)`; `has_<x>(id)`; `<x>_ids()` (ordered, `.duplicate()`d); `content_repository()`; `register_baseline_<x>s()`; `static create_baseline_repository(...)`; `static create_repository_from_definitions(defs, ...)` that PRE-VALIDATES and returns `null` on the first invalid/null (FAIL CLOSED — `_*_factory_fails_closed_on_invalid_definitions` asserts it). Note the factory uses `load("res://.../<x>_repository.gd").new(...)` (not `<X>Repository.new`) to avoid the self-`class_name` cyclic-preload quirk — mirror that idiom. `BASELINE_*_IDS` lists the stable baseline ids. `WeaponRepository.BASELINE_WEAPON_IDS` = `[sword,dagger,spear,axe,mace,bow,crossbow,staff,wand]` and `SupportRepository.BASELINE_SUPPORT_IDS` = `[none,tome,shield]` are the REAL ids a reward-table weapon/support entry references.
- `godot/scripts/core/state/rng_stream_set.gd` — `RngStreamSet` (`RefCounted`). `STREAM_REWARDS := &"rewards"`, `STREAM_LOOT := &"loot"` are in `required_streams()` (built per `RngStreamSet.new(root_seed)`, currently INERT — nothing draws them). `rand_int(stream, min, max, context={})` / `rand_float(stream, context={})` return an `ActionResult` carrying `{value, stream_name, draw_index, state_before, state_after, draw_type, consumer_context}` and ADVANCE the stream's draw index. `unknown_rng_stream` / `invalid_rng_range` are the structured error codes. `to_snapshot()`/`try_restore(...)` round-trip the stream state (int64-safe). The offer-builder draws EXCLUSIVELY through this — NEVER `randi`/`randf`/`RandomNumberGenerator`.
- `godot/scripts/core/results/action_result.gd` — `ActionResult` (`succeeded`/`is_error()`/`error_code`/`metadata` deep-copied). `ok(events, metadata)` / `error(code, metadata)`. Error codes MUST be lower-snake (no spaces/dots/colons/dashes/slashes/quotes) or they collapse to `invalid_error_code` — so `invalid_armor_definition`/`duplicate_armor`/`invalid_reward_table_definition` are valid; a hyphenated/spaced code is NOT. REUSE — do not reimplement.
- `godot/tests/unit/test_case.gd` — the custom headless harness base. Tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, call `assert_true/false/equal`, return `result()`. Do NOT add GUT/GdUnit.
- `godot/tests/unit/content/test_weapon_repository.gd`, `test_passive_repository.gd`, `test_support_definition.gd`, `test_level_recipe_definition.gd` — the closest test TEMPLATES (EXPECTED_* dict, stable-id registration, generic-`ContentRepository`-boundary assertion, fail-closed factory, per-field positive+negative). `test_run_route_position_save.gd` is the template for the AC3 stream state-reproduction round-trip.
- `godot/scripts/generation/level/entity_reward_placer.gd` — the BOUNDARY marker: its header explicitly defers `RewardTableDefinition` + concrete loot to "Epic 6 (Story 3.5.5)" and reserves the `rewards`/`loot` streams for "RUNTIME reward/loot RESOLUTION (Epic 6+), NOT level-generation placement." 6.1 is that Epic-6 consumer; it does NOT modify this file or the markers.
- `godot/tests/unit/core/test_project_structure.gd` — the structural "fail-loud" gate. `REQUIRED_DOMAIN_ROOTS` ALREADY includes `res://scripts/content`; `REQUIRED_PROJECT_ROOTS` ALREADY includes `res://data/source` + `res://data/resources`. Your new files live inside already-required roots; do NOT add a new root.

### Existing Files To Update Or Preserve

| Path | Current State | This Story Changes | Preserve |
|---|---|---|---|
| `godot/scripts/content/repositories/content_repository.gd` | Generic type-keyed registry; `register_definition` returns `void` and OVERWRITES by `(type,id)` (the duplicate-id trap); `get_definition` returns `null` on a miss. | DUPLICATE-ID GUARD: either change `register_definition` to return `ActionResult` rejecting a second `(type,id)` (central option — update all 6 callers) OR leave untouched and pre-check in each repo (per-repo option). PICK ONE + record `[Decision]`. | `get_definition`/`has_definition` contract; the fail-closed-lookup-returns-null behavior. |
| `godot/scripts/content/repositories/{class,enemy,level_recipe,weapon,support,passive}_repository.gd` | Fail-closed repos; `register_<x>` last-write-wins on a duplicate id (`*_ids()` keeps first, `get_<x>` returns second). | RETROFIT: `register_<x>` returns a `duplicate_<x>` `ActionResult.error` on a second registration under an already-present id; baselines (distinct ids) unchanged. ADD a duplicate-id negative test to each repo's test file. | ALL existing register/get/has/ids/factory behavior + every baseline + every existing test (the retrofit must not change any baseline registration). |
| `godot/scripts/content/definitions/{weapon,support,level_recipe}_definition.gd` | `*Definition` + `validate()` + lower-snake helpers + the `&"none"` sentinel + by-id-defer precedent. | NO change — COPY the patterns into the new definitions. | All existing definition behavior + tests. |
| `godot/scripts/content/definitions/passive_definition.gd` / `repositories/passive_repository.gd` | Story-5.4 EXPLANATION-ONLY passive + fail-closed repo (six baselines). | NO re-author; `passive_repository.gd` gets the duplicate-id retrofit + a negative test ONLY. Do NOT add the full pool or an effect engine. | The passive schema, the six baseline ids (must keep matching the class baselines or a class start fails closed), EXPLANATION-ONLY v0. |
| `godot/scripts/core/state/rng_stream_set.gd` | `RngStreamSet` with INERT `STREAM_REWARDS`/`STREAM_LOOT`; `rand_int`/`rand_float` + snapshot round-trip. | NO change — REUSE; the offer-builder draws through it. (Do NOT add a stream, rename one, or reorder `required_streams()` — that would break the RNG-snapshot draw-count/round-trip tests + the 23-key gate's stream set.) | All stream + snapshot behavior + every RNG test. |
| `godot/scripts/generation/level/entity_reward_placer.gd` + the level generators + fingerprints | Reward MARKERS (`{x,y,optional}`); the `rewards`/`loot` streams reserved for runtime. | NO change — 6.1 builds reward CONTENT, not placement. | All placement + the Small/Medium/batch + route fingerprints. |
| `godot/scripts/core/results/action_result.gd` | Structured ok/error, lower-snake codes, deep-copied metadata. | NO change — REUSE for `validate()` + repo error paths + `duplicate_*`. | `ok`/`error`/`is_error`/code normalization. |
| `godot/scripts/run/run_state.gd` + `run_start_command.gd` + `RunSnapshot` | Epic-4/5 run model; three content gates resolve through the repos; 23-key snapshot gate. | NO change this story (no run reward state yet). The duplicate-id retrofit must NOT break the three `RunStartCommand` gates. | All run-state/run-start behavior, the 23-key gate, the three fail-closed content gates. |
| `godot/tests/unit/core/test_project_structure.gd` | Asserts `scripts/content` + `data/source` + `data/resources` exist. | NO change (new files live inside already-required roots). | All required-root assertions. |
| `_bmad-output/implementation-artifacts/deferred-work.md` | Carries the OPEN duplicate-id `[Review][Defer]` (code review of 5-1) "NOW SCHEDULED" -> Story 6.1 Task 6.1.5. | CLOSE that entry (or archive to `deferred-work-resolved.md`) when the retrofit + negative tests land — THIS story's responsibility. | All other ledger entries (do NOT reopen/re-defer unrelated items). |

### Recommended New Files

Use these names unless implementation discovers a clearer local pattern:

- `godot/scripts/content/definitions/armor_definition.gd` — `ArmorDefinition extends Resource`, `DEFINITION_TYPE := &"armor"`, equip-gate (`character_level_requirement` + `none` sentinel), roll-model fields, `validate()` (REJECT run-depth gate), helpers verbatim.
- `godot/scripts/content/definitions/jewelry_definition.gd` — `JewelryDefinition`, `DEFINITION_TYPE := &"jewelry"`, equip-gate + roll-model + `validate()`.
- `godot/scripts/content/definitions/consumable_definition.gd` — `ConsumableDefinition`, `DEFINITION_TYPE := &"consumable"`, rarity/value (FR53 scarcity-as-data), `validate()` (no equip gate unless you choose to carry it).
- `godot/scripts/content/definitions/pickup_definition.gd` — `PickupDefinition`, `DEFINITION_TYPE := &"pickup"`, `validate()`.
- `godot/scripts/content/definitions/gold_reward_definition.gd` — `GoldRewardDefinition`, `DEFINITION_TYPE := &"gold_reward"`, `gold_min`/`gold_max` band, `validate()`.
- `godot/scripts/content/definitions/reward_table_definition.gd` — `RewardTableDefinition`, `DEFINITION_TYPE := &"reward_table"`, weighted `{category, content_id, weight}` entries (by-id shape-only), `validate()` (reject empty/bad-category/non-positive-weight).
- `godot/scripts/content/definitions/item_roll_model.gd` (OR embed on the equippable definitions) — the roll RANGE + affix/enhancement IDs + affinity TAGS + per-family `*_mvp_deferred` markers; `validate()` shape-checks (no `item_level`).
- `godot/scripts/content/repositories/{armor,jewelry,consumable,pickup,gold_reward,reward_table}_repository.gd` — `*Repository extends RefCounted`, mirroring `WeaponRepository`/`PassiveRepository` (register/get/has/ids with COLLISION-FREE accessor names, `content_repository`, baselines, fail-closed factories, duplicate-id reject, `BASELINE_*_IDS`).
- `godot/scripts/content/reward_offer_builder.gd` (OR a fixture-local helper) — pure `RefCounted`: `build_offer(streams: RngStreamSet, stream_name: StringName, table: RewardTableDefinition) -> Dictionary` (draws through the INJECTED set's `rand_int`/`rand_float` on `STREAM_REWARDS`/`STREAM_LOOT`; returns a serializable offer dict; zero `randi`/`randf`/`RandomNumberGenerator`; accepts the injected set so Story 6.3 hands it the orchestrator's run-level `streams`).
- Tests under `godot/tests/unit/content/`: `test_armor_definition.gd`, `test_armor_repository.gd`, `test_jewelry_definition.gd`, `test_jewelry_repository.gd`, `test_consumable_definition.gd`, `test_consumable_repository.gd`, `test_pickup_definition.gd`, `test_pickup_repository.gd`, `test_gold_reward_definition.gd`, `test_gold_reward_repository.gd`, `test_reward_table_definition.gd`, `test_reward_table_repository.gd`, `test_item_roll_model.gd` (if standalone), `test_reward_offer_builder.gd` (AC3). PLUS the duplicate-id negative added to the SIX existing repository tests.

Avoid: a parallel content loader; a new result base type; a second RNG type or a local `RandomNumberGenerator`; reading JSON/CSV from a gameplay/hot path; resolving affix/enhancement/affinity/reward-table content against repositories inside `validate()` (defer to consumption time — the `LevelRecipeDefinition` precedent); a reserved-`Object`-method accessor name (`get_class`/`get_script`/`get_meta`/…); an `item_level` field; a run-depth equip gate; forking ONE repo's duplicate-id behavior while leaving others; building inventory/reward-flow/modal/Consume/Destroy/effect-engine; a difficulty knob; a new test framework.

### Item / Reward Content Contract (AC1/AC2/AC4/AC5 — definitions expose the category contract; fail closed; character-level-or-`none` gate; roll-ranges not fixed levels)

The single most important correctness constraint: **every `*Definition.validate()` fails closed (REJECT, never coerce) — a bad id/range/category/equip-gate is a structured `invalid_*_definition` error, never a coerced field or an accepted junk definition; the repository's fail-closed factory returns `null` on any rejected definition; and a duplicate id is now a loud `duplicate_*` error, not a silent shadow.** This is the "reject, don't coerce" posture the Story 1.3 board-snapshot deferral warns about (see Deferred-Work Ledger Check) and exactly what every existing `*Definition` does.

- A loot/reward definition exposes its category contract (AC1): the eight MVP categories (weapons, armor, jewelry, support items, consumables, pickups, passives, gold) are reachable through typed repository lookups; weapons/support/passives reuse the existing repos, armor/jewelry/consumable/pickup/gold-reward are new. Gameplay resolves through the repository -> `ContentRepository`, never `FileAccess`/`load()`/`JSON.parse` in a hot path. [Source: epics.md Story 6.1 AC1; FR52; gdd.md line 326]
- Equip gate (AC4, GDD FR75/FR76): each EQUIPPABLE item declares a character-level requirement (non-negative int) OR an explicit `none` (a real resolving sentinel, the `SupportDefinition.SUPPORT_NONE` idiom); `validate()` REJECTS a negative requirement and REJECTS any run-depth gate. Character level — not run depth — is the only equip gate. [Source: epics.md Story 6.1 AC4; gdd.md line 327]
- Roll model (AC5, GDD FR76): instance variation uses roll RANGES (min/max bands), affix/enhancement IDs, affinity TAGS — NO `item_level`. Advanced roll families not in MVP carry an explicit per-family deferral marker and validate INERT, preserving the data boundary for Epic 7+. [Source: epics.md Story 6.1 AC5 + Task 6.1.3; gdd.md line 328; gdd.md line 678 "Hundreds of loot affixes" = later/non-MVP]
- Consumable scarcity/value (FR53): semi-rare, worth-using — modeled as DATA (rarity tier + value/weight) on the consumable + reward-table entries, NOT runtime logic this story. [Source: epics.md FR53; gdd.md line 325]
- Approved-content rule: loot/reward definitions are APPROVED static content selected at runtime, never generated at runtime. The baselines are approved static definitions. [Source: project-context.md Static Content & Asset Rules; AGENTS.md "Procedural generation selects from approved static definitions only"]

### Duplicate-ID Hardening Contract (AC6 — close the carried Epic-5 cross-cutting `[Review][Defer]`)

The single most important cross-cutting constraint: **a second registration under an already-present `(type, id)` must FAIL LOUD with a structured `duplicate_*` error in EVERY content repository (six existing + new), so `*_ids()` and `get_*` can never disagree about the canonical definition.** This closes the human-ratified `[Review][Defer]` (deferred-work.md "code review of 5-1"), scheduled to land HERE per epics.md AC6 + Task 6.1.5, and aligns the repos with the project's fail-closed/fail-loud rule.

- The trap today: `register_<x>` calls `register_definition(...)` (overwrites by `(type, id)`) then `if not _<x>_order.has(id): _<x>_order.append(id)` — so a duplicate id keeps the FIRST in `<x>_ids()` but resolves the SECOND via `get_<x>`. Harmless for the distinct-baseline-id status quo; a latent fail-QUIET trap once content volume / a JSON pipeline arrives (copy-paste / merge authoring errors). [Source: deferred-work.md "code review of 5-1" `[Review][Defer]`; project-context.md duplicate-id section]
- The fix is ONE uniform mechanism (do NOT fork one repo — parity was explicitly preserved through Epic 5). Recommended: a central guard in `ContentRepository.register_definition` surfaced as a per-type `duplicate_<x>` error; acceptable alternative: a `has_definition` pre-check in each `register_<x>`. Either way EVERY repo rejects a duplicate, leaves `<x>_ids()`/`get_<x>` consistent (the rejected def is neither listed nor resolvable), and has a negative test. [Source: epics.md Story 6.1 AC6 + Task 6.1.5; deferred-work.md HUMAN-RATIFIED 5-1 Phase 7]
- New Epic-6 repos inherit the guard by construction (build the guard first, in Task 2). The `create_repository_from_definitions` factories already fail closed on a `register_<x>` error, so a duplicate in a definitions batch returns `null` — assert it per repo.
- After the retrofit, CLOSE the ledger entry (Task 7.3). This is THIS story's responsibility, not a later one's.

### RNG Stream Contract (AC3 — reward rolls use the named `rewards`/`loot` stream and reproduce; the INERT-stream heads-up)

The single most important determinism constraint: **a reward-offer roll draws EXCLUSIVELY through the named `rewards`/`loot` stream of an injected `RngStreamSet` (via `rand_int`/`rand_float`), never `randi`/`randf`/a local `RandomNumberGenerator`/another stream — so the same seed + same pre-draw state reproduces the same offer.** Story 6.1 proves this at the FIXTURE level (a standalone `RngStreamSet`); the live roll wired through the orchestrator's run-level `streams` is Story 6.3.

- `STREAM_REWARDS := &"rewards"` and `STREAM_LOOT := &"loot"` already exist in `RngStreamSet.required_streams()` and are INERT (nothing draws them yet). The Epic-3 generation layer DELIBERATELY reserved them for exactly this runtime reward/loot resolution (`entity_reward_placer.gd` header). 6.1 is the first consumer. [Source: godot/scripts/core/state/rng_stream_set.gd; entity_reward_placer.gd header; project-context.md "the `rewards`/`loot` streams are RESERVED for runtime reward/loot resolution (Epic 6), NOT generation placement"]
- AC3 fixture proof: determinism (same seed -> byte-equal offer), divergence (different seed -> different offer across a small sample), named-stream proof (`metadata.stream_name == "rewards"/"loot"`), state-reproduction (the post-draw state reproduces the next draw — the `to_snapshot`/`try_restore` round-trip idiom from `test_run_route_position_save.gd`), and no-RNG-leak (no `randi`/`randf`/`RandomNumberGenerator` in any new file). [Source: epics.md Story 6.1 AC3 + Task 6.1.4]
- **INERT-STREAM HEADS-UP (Epic-5-retro T2 / Story-4.6 `[Review][Defer]`, owner Story 6.3 — NOT 6.1):** the moment a reward/passive first ROLLS run-affecting RNG through the RUN, that draw MUST route through the orchestrator's run-level `streams` (and `LevelGenerator.generate` must accept an injected run-level `RngStreamSet`), or the route-position save persists a stream disconnected from where draws happen, breaking interrupted==uninterrupted determinism. 6.1 does NOT wire a live roll into the orchestrator — it draws through an INJECTED set inside fixtures only, so it does NOT trip the trap and does NOT own the fix. Author the offer-builder to ACCEPT an injected `RngStreamSet` + stream name so Story 6.3 hands it the orchestrator's run-level `streams` and owns the T2 threading without reshaping the builder. Record this seam in Completion Notes. [Source: deferred-work.md "code review of 4-6" inert-RngStreamSet `[Review][Defer]`; epic-5-retro-2026-06-26.md §7 risk #2 + §8 T2; project-context.md "Do not draw run-affecting RNG from anything but the map/level stream ... do not bypass the run-level RngStreamSet when real run RNG is added in Epic 6/7/9"]

### State / No-Mutation Contract

Loot/reward content construction is a pure data/definition operation this story; the offer fixture draws through an in-memory `RngStreamSet` only. Nothing mutates tactical truth, the run save, settings, route, generation, or progression beyond the deliberate duplicate-id retrofit (which only changes repository REGISTRATION behavior, never a baseline outcome).

Never during this story's work:

- Execute move/attack/run commands or any tactical/run mutation, build a `RunState`, or wire a reward roll into the orchestrator/run lifecycle.
- Read, write, or mutate `user://run_autosave.json` or `user://settings.json` (loot content has no save work; the offer fixture's stream round-trip is in-memory, not a file).
- Draw gameplay RNG through the orchestrator's run-level streams or any LIVE run path (the offer-builder draws ONLY through an injected `RngStreamSet` inside fixtures; it never touches global RNG or a fresh `RandomNumberGenerator`).
- Alter the route, the level generators, the reward markers, the Small/Medium/batch level fingerprints, or the route fingerprints.
- Add a new top-level `RunSnapshot` key, change the 23-key gate, or change `RngStreamSet.required_streams()` (adding/renaming/reordering a stream breaks the RNG-snapshot tests + the gate's stream set).
- Read JSON/CSV from a gameplay/hot path (AC1) — all definition access is through the repository boundary; baselines are code constants (unless you deliberately introduce the JSON pipeline, routed through the repo/import boundary).
- Build inventory/reward-flow/modal/Consume/Destroy logic, the per-effect passive operation engine, combat hook sites, or the full 20-30 passive pool.

### Previous Story Intelligence + Epic-5 -> Epic-6 Transition Prep (folded in — these are epic-transition items, not a generic "see the retro" pointer)

Story 5.5 (Class-Start Playable Smoke Slice) was the LAST Epic 5 story; `epic-5` is `done` and the Epic-5 retrospective (`epic-5-retro-2026-06-26.md`) is `done`. The retro's FORWARD-looking guidance for Epic 6 (§7 Next-Epic Preview, §8 Action Items, §10 Significant Discovery) is folded in below as concrete constraints for THIS story (and flagged where an item lands on a LATER Epic 6 story so the dev does not chase it):

- **T1 (HIGH) — Duplicate-id fail-loud hardening across all six repos lands HERE, before/with the first new data-pipeline content (retro §7 risk #3 + §8 T1).** Verbatim: "Epic 6 is where content volume (loot, the 20-30 passive pool) and likely a JSON/data pipeline arrive — exactly the conditions that turn the fail-quiet duplicate-id trap from latent to real. Strong recommendation: land the all-six-repos fail-loud hardening story before or alongside the first data-pipeline content, with tests." This is THIS story's AC6 + Task 2 + Task 7.3 (close the ledger entry). The gate/check will fail-loud on a duplicate in the NEW loot/reward repos by construction — that is EXPECTED; register/extend the duplicate-id negative on every repo (six existing + new). [Source: epic-5-retro-2026-06-26.md §7 risk #3 + §8 T1; deferred-work.md "code review of 5-1" "NOW SCHEDULED -> Story 6.1 Task 6.1.5"]
- **T2 (HIGH, determinism-critical) — the first run-affecting RNG roll must thread the run-level `RngStreamSet`; owner is the first Epic-6 story that ROLLS a reward = Story 6.3, NOT 6.1 (retro §7 risk #2 + §8 T2).** Verbatim: "when a passive/reward first ROLLS, that draw MUST route through the orchestrator's run-level `streams` (and `LevelGenerator.generate` must accept an injected run-level `RngStreamSet` instead of minting a local one), or the route-position save/resume seam silently persists a stream disconnected from where draws happen ... This is a critical-path prerequisite." For 6.1: prove the stream contract at the FIXTURE level (a standalone `RngStreamSet`), and author the offer-builder to ACCEPT an injected set so 6.3 owns the threading. 6.1 adds NO live roll and does NOT own the fix. [Source: epic-5-retro-2026-06-26.md §7 risk #2 + §8 T2; deferred-work.md "code review of 4-6" inert-stream item]
- **P3 — reserved-name screen for content accessors (retro §8 P3).** "Before naming any `get_<thing>` accessor, check it against native `Object`/`Resource`/`RefCounted` method names (the `get_class` lesson)." You add ~six new repository accessors — use collision-free names (`get_armor`/`get_jewelry`/`get_consumable`/`get_pickup`/`get_gold_reward`/`get_reward_table`); NEVER `get_class`/`get_script`/`get_meta`/`get_instance_id`. A reserved-name collision is a HARD parse error that can compile-fail a test while still printing PASS (see P2). [Source: epic-5-retro-2026-06-26.md §8 P3; project-context.md content-accessor-naming rule]
- **P2 — the false-PASS grep guard is a standing gate (retro §8 P2).** "Maintain 'grep raw runner output for `SCRIPT ERROR|Parse Error|Compile Error`, never trust the summary PASS line alone' as a gate on every new content/test file." You add many new files AND change six existing repositories' return types — Task 6.3 makes this explicit. [Source: epic-5-retro-2026-06-26.md §8 P2; §5 verification-guards-are-load-bearing]
- **The explanation-only -> felt-effect passive transition + the 20-30 pool are LATER Epic-6 work, NOT 6.1 (retro §7 risk #1 + §10).** Verbatim: "The explanation-only -> felt-effect transition is the core Epic-6 build ... Epic 6 owns turning a `before_attack`/`run_started` explanation into an actual combat-number change (the per-effect operation model + combat hook sites)." That is a LATER Epic-6 story; 6.1 keeps the six baseline passives EXPLANATION-ONLY and authors NO effect engine and NO full pool. Recorded so the dev does not pull the effect model three stories early. [Source: epic-5-retro-2026-06-26.md §7 risk #1 + §10; deferred-work.md "dev of 5-4"/"dev of 5-5"]
- **Stable ordering + conflict handling is a NEW correctness surface — but it is the EFFECT engine's, not 6.1's (retro §7 risk #4).** "Epic 6's FR46 rule-benders + multi-passive stacking demand explicit, tested ordering/conflict/duration semantics." That belongs to the later passive-effect-engine story; 6.1 (content + reward-table + offer fixture) introduces no stacking/conflict surface (the `RulesResolver`'s registration-order resolution is unchanged and untouched here). Recorded so the dev does not build a stacking/conflict model in the content story. [Source: epic-5-retro-2026-06-26.md §7 risk #4]
- **No structural planning drift; Epic-6 dependencies all satisfied and stable — no new infrastructure/library/web research (retro §7 + §10).** The retro confirms Epic 6's plan (FR46-FR53, "use the rules kernel with explicit trigger windows, stable ordering, conflict handling, and explanation output") matches what Epic 5 delivered; "No Epic-6 dependency from Epic 5 is incomplete or unstable." Godot 4.6.3 + typed GDScript is settled; no new engine library and NO web research are required for 6.1. [Source: epic-5-retro-2026-06-26.md §7 + §10]

Epic 1-5 review patterns that still apply to this story:

- Failed/invalid input paths need structured `ActionResult.error()` with stable lower-snake codes + diagnostic metadata — never a silent crash or half-built/coerced state (`WeaponDefinition`/`SupportDefinition`/`LevelRecipeDefinition.validate()` are the model).
- `ActionResult.ok()/error()` deep-copy metadata + normalize codes; reuse, don't reimplement. `duplicate_armor`/`invalid_reward_table_definition` etc. must be lower-snake.
- Fail-closed factories: `create_repository_from_definitions` returns `null` on the first invalid/null/DUPLICATE definition (asserted by the existing repo tests). Every new repo must do the same.
- Value sanitization has been tightened repeatedly across reviews — REJECT (never coerce) every out-of-range/wrong-type/unknown-enum field deterministically, in ONE place per field inside `validate()` (the Story 1.3 board-snapshot "reject, don't coerce" precedent). GIVE EVERY `validate()` branch a dedicated negative test (pre-empting the Story 3.1-style coverage-gap defers — three Low coverage gaps for un-tested `validate()` branches were deferred there; do not repeat that here).
- The duplicate-id retrofit is the highest-regression-risk change in the story — isolate it (Task 2), re-run the FULL suite immediately, and watch the content + run-start suites specifically.

### Git Intelligence

Recent commits before this story:

- `7b220b8 Merge pull request #26 from rthunborg/chore/epic-5-planning-resync`
- `a7c1cd3 docs(epics): Epic 5 retrospective re-sync + schedule carried follow-ups`
- `32364dc Merge pull request #25 from rthunborg/story/5-5-class-start-playable-smoke-slice`
- `4c1d5ef chore(story-5-5): finalize (mark done + GDS status; close epic 5)`
- `29f2ae1 docs(story-5-5): pipeline report`

Actionable patterns:

- The project consistently uses narrow typed `Resource` definitions + fail-closed `RefCounted` `*Repository` factories under `scripts/content/`, with tests under `tests/unit/content/`. Put the new loot/reward definitions + repositories there alongside weapons/enemies/support/recipes/class/passive, with `test_*` files under `tests/unit/content/`.
- The Epic-5 planning re-sync (`a7c1cd3`) folded the carried Epic-5 follow-ups (incl. the duplicate-id hardening + the inert-stream item) into the plan — epics.md Story 6.1 AC6/Task 6.1.5 (duplicate-id, OWNED here) and the T2 inert-stream item (owner Story 6.3). Honor that scheduling: close the duplicate-id item here; leave the live-roll threading to 6.3.
- The headless runner auto-discovers `test_*.gd` under `godot/tests/unit` and `godot/tests/integration`; no registry/`.tscn` edit is needed. Tests under `tests/fixtures` or `tests/headless` are NOT auto-run.
- ENVIRONMENT (every prior epic): the bare `godot` resolves only as `C:\Users\Rasmus\bin\godot.cmd` through PowerShell; the Bash tool's PATH/`where` cannot find it. Run `godot --version` and the headless suite through `powershell.exe -NoProfile -Command "..."`.
- Review findings across the project have repeatedly tightened value sanitization, no-mutation/no-partial-state assertions, stable reason/error ids, structured error metadata, and per-field negative coverage. Treat all five as first-class for every new `validate()`, every new repo error path (incl. `duplicate_*`), and the offer fixture.

### Architecture Compliance

- Runtime-facing definitions are typed Godot `Resource`s queried through a `ContentRepository`/import layer; loot/reward definitions are content. [Source: game-architecture.md#Static Content & Data; epics.md Additional Requirements "Runtime-facing definitions should include ... WeaponDefinition, SupportItemDefinition, ..."; epics.md Epic 6 implementation notes]
- Configuration layering: content/balance lives in the content pipeline (typed Godot Resources, validated — for v0 code-constant baselines mirroring the existing content), NOT in `user://settings.json`. Loot is content. [Source: game-architecture.md#Configuration; project-context.md Static Content rules]
- Gameplay systems depend on repository contracts, not raw files; loot/reward is selected from approved static definitions and never generated at runtime. [Source: project-context.md "Do not access files directly from gameplay systems; use repositories" + Static Content & Asset Rules; AGENTS.md]
- `scripts/content/` is `RefCounted` services + typed `Resource` definitions, independent of scene nodes; headless tests run without rendering/audio/UI scenes. [Source: project-context.md Code Organization rules; AGENTS.md Hard Architecture Rules; NFR14]
- Rules use explicit trigger windows + the rules kernel (Epic 6 implementation note) — but the per-effect OPERATION engine + combat hook sites are a LATER Epic-6 story; 6.1 builds loot/reward CONTENT + reuses the EXISTING passive repo, and authors no effect model. [Source: epics.md Epic 6 implementation notes; project-context.md rules-kernel section]
- Named RNG streams: reward/loot rolls draw through the `rewards`/`loot` streams of an injected `RngStreamSet`; cosmetic-only randomness cannot affect outcomes. 6.1 proves this via fixtures; the live run-level threading is Story 6.3. [Source: project-context.md Determinism & Simulation Rules; game-architecture.md RNG streams]
- Structured results, not exceptions: `validate()` + repository registration return `ActionResult`; lower-snake error/reason codes; deep-copied metadata; `duplicate_*` on a duplicate id. [Source: project-context.md determinism + content rules; `action_result.gd`]
- Do not add cloud services, accounts, multiplayer, telemetry, Godot .NET/C#, new test frameworks, or React/Vite production dependencies. [Source: project-context.md Critical Don't-Miss rules]

### Library And Framework Requirements

- Required engine: Godot 4.6.3 stable standard build. Required language: typed GDScript. No new engine library or external dependency is introduced (confirmed by the Epic-5 retro: Epic 6's dependencies are satisfied and stable; "no new infrastructure/library/web research"). No web research required.
- The new `*Definition`s are typed `Resource`s; the new `*Repository`s + the offer-builder are `RefCounted` (NOT `Node`s, NOT autoloads). The autoload set is unchanged through Epic 5 and this story adds NO autoload.
- Reuse `ActionResult` (`scripts/core/results/action_result.gd`) for `validate()` + repository error paths (incl. `duplicate_*`). Reuse `ContentRepository` (`scripts/content/repositories/content_repository.gd`) as the registration boundary. Reuse `RngStreamSet` (`scripts/core/state/rng_stream_set.gd`) + its `STREAM_REWARDS`/`STREAM_LOOT` + `rand_int`/`rand_float` for the offer-builder. Copy the `_is_lower_snake_id`/`_invalid` helpers verbatim from `WeaponDefinition`/`SupportDefinition` (the local duplication is the established convention).
- Use the existing custom headless harness: tests `extends "res://tests/unit/test_case.gd"`, expose `run() -> Dictionary`, return `result()`. Do NOT add GUT, GdUnit, or another testing dependency.
- No int64/JSON-string encoding is needed this story (loot content carries small bounded ints — level requirements, gold bands, roll ranges, weights — no full-64-bit seed). The offer fixture's `RngStreamSet.to_snapshot()`/`try_restore()` round-trip already handles the int64-safe stream state internally; do NOT reimplement it.

### Latest Technical Information

Official Godot 4.6 sources relevant to typed `Resource` definitions + deterministic RNG (these inform correctness, not gameplay):

- `Resource` + `@export` typed properties are the established definition shape (`WeaponDefinition`/`SupportDefinition`/`ClassDefinition`/`PassiveDefinition` use them). Source: https://docs.godotengine.org/en/4.6/classes/class_resource.html
- `StringName` is the established id type for content ids; lower_snake validation is a per-definition static helper. No new API is needed.
- `RandomNumberGenerator` is deterministic per `seed`/`state` and is what `RngStreamSet` wraps per named stream — the offer-builder must NOT construct its own; it draws through the injected `RngStreamSet` so seed+state reproduce the offer. Source: https://docs.godotengine.org/en/4.6/classes/class_randomnumbergenerator.html
- If a JSON loot-content source is ever added (a `[Decision]` for this story; the safe default is code-constant baselines), `JSON.parse_string` returns `null` on parse failure and must be handled inside the repository/import boundary, never a gameplay/hot path; `res://data/source/...` is the bundled-source location and is read-only in exported builds. Source: https://docs.godotengine.org/en/4.6/classes/class_json.html and https://docs.godotengine.org/en/4.6/tutorials/io/data_paths.html

### Project Structure Notes

- The loot/reward DEFINITIONS + REPOSITORIES belong under the existing `godot/scripts/content/definitions/` and `godot/scripts/content/repositories/` (alongside weapons/enemies/support/recipes/class/passive). The offer-builder belongs under `godot/scripts/content/` (or a fixture-local helper). All tests go under `godot/tests/unit/content/`.
- `test_project_structure.gd` already requires `res://scripts/content`, `res://data/source`, and `res://data/resources`; your additions live inside already-required roots and keep the gate green. Do NOT add a new top-level domain/data root (none is needed).
- If loot content is introduced as JSON (a `[Decision]`; NOT the default), source would go under `godot/data/source/loot/`, typed `.tres` mirrors under `godot/data/resources/loot/`, a schema under `godot/data/schemas/` — all inside already-required roots; route every read through the repository/import boundary, NEVER a gameplay/hot path. The safe default is code-constant baselines (the current weapon/enemy/support/recipe/class/passive convention) and `data/{source,resources}/` stay empty scaffolding.
- Production code stays under `godot/`; no production dependency on `prototype/`.
- Root `project-context.md` is canonical; do not create duplicate project context files under `_bmad-output/`.
- No standalone UX file exists and none is needed for 6.1 — this is a domain/content/contract story with no UI scene artifact (the passive-reward modal UX is Story 6.4; hero-select/HUD scenes are later HUD stories).

### Deferred-Work Ledger Check

A review of `_bmad-output/implementation-artifacts/deferred-work.md` (2026-06-26) found the items that OVERLAP this story's content/repository/RNG area (folded in below as constraints); the many that do NOT overlap are listed last (do NOT reopen them here):

- **OWNED-AND-CLOSED-BY-THIS-STORY — the duplicate-id last-write-wins `[Review][Defer]` (deferred-work.md "code review of 5-1", folded to six repos by 5-4, human-ratified at 5-1 Phase 7).** This is the carried cross-cutting hardening item, "NOW SCHEDULED" as Story 6.1 Task 6.1.5 and OPEN "until that task lands; close it then." THIS story closes it: AC6 + Task 2 (retrofit all six existing repos + the new Epic-6 repos to fail-loud on a duplicate id, with negative tests) + Task 7.3 (mark the ledger entry CLOSED-by-Story-6.1 / archive to `deferred-work-resolved.md`). The gate/check will fail-loud on a duplicate in the new loot/reward repos by construction — that is EXPECTED; extend the duplicate-id negative onto every repo. [Source: deferred-work.md "code review of 5-1" `[Review][Defer]` + "NOW SCHEDULED (2026-06-26)"]
- **HEADS-UP, OWNER STORY 6.3 (NOT 6.1) — the inert run-level `RngStreamSet` `[Review][Defer]` (deferred-work.md "code review of 4-6", owner Epic 6/7/9).** Becomes LIVE the instant a reward/passive ROLLS run-affecting RNG through the run. 6.1 draws through an INJECTED `RngStreamSet` inside FIXTURES only (no orchestrator wiring), so it does NOT trip the trap and does NOT own the fix — but it AUTHORS the offer-builder to accept an injected set so Story 6.3 (the first live reward roll) hands it the orchestrator's run-level `streams` and owns the T2 threading. Recorded so the dev does NOT (a) wire a live roll into the orchestrator this story, or (b) mint a local `RandomNumberGenerator` in the builder. [Source: deferred-work.md "code review of 4-6" inert-RngStreamSet `[Review][Defer]`; epic-5-retro §8 T2]
- **CAUTIONARY PRECEDENT (informational — set the right `validate()` posture) — the Story 1.3 board-snapshot coercion items.** The Epic-1 review of 1.3 deferred that `BoardCell.from_dictionary()` COERCES malformed fields before validation can reject the corrupt snapshot. No action in THIS story (you do not touch `BoardState`/`BoardCell`), but it is the directly relevant cautionary precedent: every new `*Definition.validate()` must REJECT (not coerce) out-of-range/wrong-type/unknown-enum/run-depth-gate fields — exactly what the `WeaponDefinition`/`SupportDefinition` pattern does. The only actionable takeaway is "reject, don't coerce, in `validate()`." [Source: deferred-work.md "code review of 1-3"]
- **CONTENT-BOUNDARY PRECEDENT (informational) — the Epic-3 reward-table-deferred-to-Epic-6 notes (`entity_reward_placer.gd` header + the 3.5 deferred Lows).** The generation layer placed abstract reward MARKERS and explicitly deferred `RewardTableDefinition` + concrete loot to "Epic 6 (Story 3.5.5)," reserving the `rewards`/`loot` streams for runtime resolution. 6.1 builds that reward CONTENT (the table + categories the marker later resolves into) — it does NOT modify `EntityRewardPlacer`, the markers, or the level fingerprints; the marker->content resolution is Story 6.3+. The 3.4/3.5/3.6 generation under-placement/pool-exhaustion Lows are generation-layer and OUT of scope. [Source: deferred-work.md "code review of 3-5"/"3-4"/"3-6"; entity_reward_placer.gd header]
- **NO OVERLAP (do NOT reopen — they belong to their originating domains):** the 3.1-3.7 generation/level coverage + recipe-validation Lows; the `GenerationResult.seed` success-path split (owner = the next story that touches `GenerationResult`); the route-fingerprint `clues`/`reveal_state` extension + the seed-varied-route-depth pacing item (Epic-10); the settings schemaless-`{}` policy (2.9); the `save_open_failed` resume path (2.8); the `RngStreamSet.try_restore` numeric-`state` tightening (2.7); the accessibility/adaptive-layout presentation Lows (2.5/2.6); the 5.x in-story `[Review][Patch]` fold-ins (all closed/in-story). NONE touch loot/reward content, the duplicate-id retrofit, or the `rewards`/`loot` stream fixture.

Stated explicitly so the dev agent does not go hunting the ledger broadly — the actionable takeaways are: (1) CLOSE the duplicate-id item here (AC6/Task 2/Task 7.3); (2) the inert-stream item is Story 6.3's, not 6.1's (author the builder to accept an injected set); (3) "reject, don't coerce, in `validate()`"; (4) reward CONTENT here, reward MARKERS untouched.

### Testing Requirements

Run at minimum (through PowerShell — the bare `godot` is not on the Bash tool PATH; it resolves only as `C:\Users\Rasmus\bin\godot.cmd` via PowerShell):

```powershell
godot --version
godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10
git diff --check
```

If invoking from the Bash tool, wrap the commands, e.g. `powershell.exe -NoProfile -Command "godot --headless --path C:\Sealsworn\godot --scene res://tests/headless/test_runner.tscn --quit-after 10"`.

Expected final result:

- Godot version is `4.6.3.stable.official...` or explicitly compatible with project policy.
- The full headless runner exits with code `0`, final line `Headless tests passed.`.
- ALL existing Epic 1-5 tests stay green — ESPECIALLY the content suite (`test_weapon_repository.gd`, `test_enemy_repository.gd`, `test_support_repository.gd`, `test_support_definition.gd`, `test_level_recipe_definition.gd`, `test_level_recipe_repository.gd`, `test_class_definition.gd`, `test_class_repository.gd`, `test_passive_definition.gd`, `test_passive_repository.gd`) given the duplicate-id retrofit, AND the run-start suite (`test_run_start_command.gd`) since `RunStartCommand`'s three content gates resolve through these repos, AND every RNG/save suite (untouched — `RngStreamSet.required_streams()` is unchanged) and `test_project_structure.gd` (new files inside already-required roots).
- AC1: each MVP category (weapon, armor, jewelry, support, consumable, pickup, passive, gold) is reachable through a typed repository lookup; all new baselines validate; content is resolved ONLY through the repository -> `ContentRepository` (no raw file read).
- AC2: every new `validate()` rejects a missing-id/invalid-range/unsupported-category field with a structured `invalid_*_definition` error; every new repo's `get_<x>(unknown)` returns `null`, `register_<x>(null/<invalid>)` returns a structured error, and `create_repository_from_definitions([<invalid>])` returns `null`; no default fabrication.
- AC3: the reward-offer fixture proves determinism (same seed -> byte-equal offer), divergence (different seed -> different offer), named-stream use (`metadata.stream_name == "rewards"/"loot"`), state-reproduction (the post-draw state reproduces the next draw), and no `randi`/`randf`/`RandomNumberGenerator` in any new file.
- AC4: each equippable definition declares a character-level requirement or explicit `none`; `validate()` rejects a negative requirement AND rejects any run-depth gate (no `min_run_depth`/`min_node_depth`).
- AC5: the roll model uses ranges + affix/enhancement ids + affinity tags (no `item_level`); each un-implemented advanced family carries an MVP-deferral marker and validates inert; every `validate()` branch has a dedicated positive+negative test (pre-empting the Story 3.1-style coverage-gap defers).
- AC6: a second registration under the same id in EVERY content repository (six existing + new) returns a structured `duplicate_<x>` error, `<x>_ids()` keeps the id exactly once, `get_<x>` stays consistent, and a duplicate in a definitions batch returns `null`; the carried `[Review][Defer]` is closed in `deferred-work.md`.
- `git diff --check` reports no whitespace errors.
- Heavy/cross-domain false-PASS guard: no `SCRIPT ERROR`/`Parse Error`/`Compile Error` attributable to any NEW or MODIFIED file (the new definitions/repositories/builder/tests AND the six retrofitted repos) in the run output (grep directly; do not trust the PASS summary alone — Epic-5-retro P2). The pre-existing `ERROR: Parse JSON failed` (save/settings) + the int64-overflow / `invalid_node_type` stderr lines are documented-expected negative-path diagnostics from OTHER suites, NOT failures.

### Project Context Rules

- Read and follow root `project-context.md` before implementation; read `_bmad-output/game-architecture.md` before architecture-sensitive changes (Static Content & Data, Configuration, RNG streams, Naming Conventions).
- Repositories, not raw file access: loot/reward content goes through `ContentRepository` via its repository; gameplay/UI never reads loot files directly. [project-context.md "Do not access files directly from gameplay systems; use repositories"]
- Approved static content: run/reward systems select ONLY from approved static definitions; loot content is never generated at runtime. Code-constant baselines for v0 (the existing content convention); the JSON pipeline is an Epic-6 `[Decision]` — do NOT introduce it implicitly. [project-context.md Static Content rules + Technology Stack "JSON-source -> typed-Resource mirror is a later (Epic 6) decision; do not introduce it early"]
- Named RNG streams: reward/loot rolls draw through the `rewards`/`loot` streams of an injected `RngStreamSet`; NEVER global/un-named RNG or a fresh `RandomNumberGenerator`; cosmetic randomness cannot affect outcomes. 6.1 proves this via fixtures; the run-level threading (do not bypass the run-level `RngStreamSet` when real run RNG arrives) is Story 6.3's. [project-context.md Determinism & Simulation Rules + "do not bypass the run-level RngStreamSet when real run RNG is added in Epic 6/7/9"]
- Duplicate-id guard is a SINGLE cross-cutting hardening across ALL six existing repos + the new ones (parity preserved — do NOT fork ONE repo); reject a duplicate with a structured per-type error + a negative test; until then the convention forbade relying on `*_ids()`/`get_*` agreeing on a duplicate — THIS story removes that caveat by closing the item. [project-context.md duplicate-id section + "Do not fail-loud-fork ONE content repository ... the duplicate-id guard is a single cross-cutting hardening story across ALL SIX repos"]
- Content-accessor naming: NEVER name an accessor after a reserved `Object`/`Resource`/`RefCounted` method (`get_class`/`get_script`/`get_meta`/…) — a hard parse error; use collision-free names (`get_armor`/`get_jewelry`/`get_consumable`/`get_pickup`/`get_gold_reward`/`get_reward_table`). [project-context.md content-accessor-naming rule + "Do not name any new content accessor after a reserved Object method"]
- The `&"none"` sentinel (e.g. an item's "no level requirement") is a REAL resolving value, never "missing" — mirror `SupportDefinition.SUPPORT_NONE`. [project-context.md StartingKit/support `&"none"` rule]
- v0 passives are EXPLANATION-ONLY; the per-effect operation engine + combat hook sites + the full 20-30 pool are LATER Epic-6 work; `scripts/rules/{conditions,operations}` stay EMPTY. 6.1 reuses the existing passive repo (+ duplicate-id retrofit) and authors no effect model. [project-context.md rules-kernel + passive sections; FR46]
- Structured results, not exceptions: `validate()` + repository registration return `ActionResult`; fail closed on bad/unknown/duplicate input; lower-snake error/reason codes; deep-copied metadata. [project-context.md determinism + content rules]
- `scripts/content/` is `RefCounted`/`Resource` services + DTOs, scene-free; headless tests run without rendering/audio/UI. [project-context.md Code Organization + Testing rules; NFR14]
- DIFFICULTY IS A HARD NON-GOAL: loot/reward content shapes build choices, NOT a difficulty tier; no enemy-stat/HP/damage/reward-RATE/RNG/run-length scaling keyed off content (the rarity/value fields are item-identity data, NOT a difficulty knob). [project-context.md "DIFFICULTY IS A HARD NON-GOAL"; epics.md Story 2.9]
- No new top-level `RunSnapshot` key, no change to `RngStreamSet.required_streams()`, no cloud/accounts/multiplayer/telemetry/.NET/new-frameworks/prototype dependencies. [project-context.md Critical Don't-Miss rules + save/snapshot rules]
- Naming: `snake_case` files/folders, `PascalCase` classes (`ArmorDefinition`, `RewardTableDefinition`, `RewardOfferBuilder`), `UPPER_SNAKE_CASE` constants (`DEFINITION_TYPE`, `BASELINE_*_IDS`, `LEVEL_REQUIREMENT_NONE`), `*Definition`/`*Repository` suffixes, `test_*.gd` tests, stable lower-snake content ids + `ActionResult` codes (`invalid_armor_definition`, `duplicate_armor`, `invalid_reward_table_definition`). [project-context.md Naming rules]

### References

- [Source: epics.md#Epic 6: Loot, Passives, and Consume/Destroy / Story 6.1] — user story + AC1-AC6 (verbatim basis), including the carried duplicate-id AC6 + Task 6.1.5.
- [Source: epics.md#Epic 6 Stories 6.2-6.7] — downstream scope boundaries (inventory/equipment, reward-offer flow, passive-reward modal, Consume/Destroy + 70/20/10, build smoke run) that this story must NOT implement.
- [Source: epics.md Epic 6 implementation notes (line 451)] — "Passive effects and reward outcomes use the rules kernel with explicit trigger windows, stable ordering, conflict handling, and explanation output" (the effect engine is a LATER Epic-6 story; 6.1 is content + duplicate-id hardening).
- [Source: epics.md FR46/FR51/FR52/FR53] — MVP passive pool (Epic 6, grows across 6.x — NOT fully built here); small inventory (6.2); loot categories (weapons/armor/jewelry/support/consumables/pickups/passives/gold); consumable scarcity/value-as-data.
- [Source: epics.md "2026-06-04 Readiness Backlog Patch Traceability" (line 395)] — GDD FR75 + FR76 (item character-level requirements + item roll model) -> Story 6.1.
- [Source: gdd.md lines 320-329] — 20-30 passives + 3-5 rule-benders; inventory ~6 backpack items no stacking; consumables semi-rare worth-using; loot categories; character-level (not run) requirements; roll ranges/affixes/affinities/enhancements not fixed item levels; every passive serves a pillar. (gdd.md line 678 "Hundreds of loot affixes" = later/non-MVP.)
- [Source: gdd.md lines 331-338] — passive reward modal fields (icon/name/flavor/exact effects/Consume/Destroy) — that surface is Story 6.4, NOT 6.1.
- [Source: epic-5-retro-2026-06-26.md §7 Next-Epic Preview + §8 Action Items (T1/T2/P2/P3) + §10] — Epic-6 prep: duplicate-id hardening lands here before/with the first data-pipeline content (T1, OWNED); the first run-affecting RNG roll threads the run-level `RngStreamSet` (T2, owner Story 6.3); reserved-name screen for accessors (P3); false-PASS grep guard standing gate (P2); explanation-only -> felt-effect + the 20-30 pool + stacking/conflict are LATER Epic-6 stories; no new infrastructure/library/web research.
- [Source: deferred-work.md "code review of 5-1" duplicate-id `[Review][Defer]` + "NOW SCHEDULED -> Story 6.1 Task 6.1.5"] — the carried cross-cutting item THIS story closes (AC6/Task 2/Task 7.3).
- [Source: deferred-work.md "code review of 4-6" inert-RngStreamSet `[Review][Defer]`] — the inert-stream item (owner Story 6.3); 6.1 proves the stream contract via fixtures + authors the builder to accept an injected set.
- [Source: deferred-work.md "code review of 1-3"] — the "reject, don't coerce" `validate()` cautionary precedent.
- [Source: project-context.md] — repository boundary, approved-static-content rule, named RNG streams, duplicate-id cross-cutting hardening rule, content-accessor naming rule, `&"none"` sentinel rule, rules-kernel/passive sections, structured results, code organization, naming, difficulty non-goal, testing rules, environment (`godot` via PowerShell only), and the JSON-pipeline-is-an-Epic-6-decision note.
- [Source: game-architecture.md#Static Content & Data + #Configuration + RNG streams] — typed `Resource` definitions through a `ContentRepository`/import layer; content/balance in the content pipeline; named RNG streams for gameplay-affecting randomness.
- Reference code to mirror: `godot/scripts/content/repositories/content_repository.gd` (the duplicate-id trap), `godot/scripts/content/definitions/{weapon,support,level_recipe}_definition.gd`, `godot/scripts/content/repositories/{weapon,passive,enemy,level_recipe,support,class}_repository.gd`, `godot/scripts/core/state/rng_stream_set.gd` (the `rewards`/`loot` streams + `rand_int`/`rand_float`), `godot/scripts/core/results/action_result.gd`, `godot/scripts/generation/level/entity_reward_placer.gd` (the reward-content-deferred-to-Epic-6 boundary); reference tests: `godot/tests/unit/content/test_{weapon,passive}_repository.gd`, `test_support_definition.gd`, `test_level_recipe_definition.gd`, `godot/tests/unit/run/test_run_route_position_save.gd` (the stream state-reproduction round-trip), `godot/tests/unit/core/test_project_structure.gd`.

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

### File List
