# Epic 5 — Auto-GDS retro notes

## Story 5-1-class-definition-content-and-repository
- [Phase 3 — create-story] Verified empirically: `scripts/rules/{conditions,operations,resolver,triggers}` are empty scaffolding (0 `.gd` files) and no passive definition/repository exists anywhere. So `class_passive_id`/`equipment_synergy_passive_id` are scoped as lower_snake string-shape forward references resolved later (Epic 6 + Story 5.4) — later stories MUST NOT assume a passive/rules system already wires these.
- [Phase 3 — create-story] Run-start seam kept a by-id class lookup so Story 5.2 EXTENDS `RunStartCommand` rather than forking it. Equipment-id validation is SHAPE-only in 5.1; cross-repository resolution deferred to Story 5.3.
- [Phase 5 — dev-story] `get_class()` is a reserved native `Object` method — defining it is a hard parse error. The class accessor is `ClassRepository.get_class_definition(class_id)`; downstream 5.2/5.3 MUST call that, not the spec-named `get_class(id)`. Any future `get_<thing>` content accessor must avoid reserved `Object` method names.
- [Phase 5 — dev-story] False-PASS guard earned its keep: the `get_class()` collision compile-failed both new test files, yet one still printed `PASS` — caught only by grepping the raw run output for `SCRIPT ERROR|Parse Error`, not the summary line. Keep that grep as a standing gate for every new content/test file.
- [Phase 7 — code review] Cross-cutting hardening deferral logged (human-ratified): duplicate-id last-write-wins is unguarded across ALL FIVE content repos (class/enemy/recipe/weapon/support) — `*_ids()` keeps the first, `get_*` returns the second, silently. Latent fail-quiet trap once content moves to a JSON pipeline. A future hardening story should make all five fail-loud on a duplicate id (see `deferred-work.md`). Per-repo forks were explicitly rejected to preserve convention parity.
- [Phase 7 — code review] D3 ratified: `ClassRepository.get_class_definition(class_id)` is the canonical accessor — Stories 5.2/5.3 story text still references the impossible spec name `get_class` and MUST be updated to call `get_class_definition(id)` when they are created.

## Story 5-2-hero-select-with-playable-and-locked-classes
- [Phase 5 — dev-story] 5.2 took Option A: `RunState.selected_class_id` is a live-run field only and is deliberately NOT in the route-position save / pinned 23-key `RunSnapshot` gate, so a resumed run rehydrates `selected_class_id == &""`. STORY 5.3 MUST thread the class (or its derived kit/HP) into the save to survive a between-node resume — and any new run field MUST nest under `route_state`, never a new top-level `RunSnapshot` key (the 23-key gate fails loud on additions).
- [Phase 5 — dev-story] 5.2 EXTENDED the run-start seam (not forked): `RunStartCommand._init` gained optional trailing `class_id`/`ClassRepository` (fail-closed: new `unknown_class` / `class_not_selectable` ActionResult codes), `RunOrchestrator.start(...)` gained optional trailing `class_id`, all backward-compatible. The `run_started` event payload was left unchanged (class surfaced via command result, not the event).

## Story 5-3-starting-kit-application
- [Phase 3 — create-story] CORRECTNESS TRAP for 5.3 dev: Ranger's `starting_support_id == &"none"` RESOLVES (it is a real `SUPPORT_NONE` baseline id), so kit resolution MUST NOT treat `none` as a missing item — doing so would falsely fail Ranger run-start. Resolve `none` to the real no-support baseline, not a reject.
- [Phase 3 — create-story] No hero/player run-level domain class exists yet; the applied kit has no canonical home, so 5.3 introduces it on the EXISTING `RunState` (5.1's forward-seam intent), recommended as a typed `StartingKit` DTO under `scripts/run/` — additive, lenient, NOT a `validate()`-required field. The 5.2->5.3 save-persistence defer is closed by NESTING the kit/class under `route_state` in `to_run_snapshot_fields()`, never a new top-level RunSnapshot key (23-key gate).
- [Phase 5 — dev-story] 5.2->5.3 class-survives-resume defer CLOSED: `selected_class_id` now persists nested inside the `route_state` payload of `to_run_snapshot_fields()` (lenient read-back; pre-5.3 saves still load), and the `StartingKit` is RE-DERIVED from the class id on restore (not stored), so no top-level `RunSnapshot` key was added (23-key gate stays green) and `RunResumeService`/`RunSnapshot` were untouched. New `StartingKit` (RefCounted, by-id) lives at `RunState.starting_kit`. Kit resolution treats `&"none"` as the real no-support baseline (the Ranger trap was handled). New fail-closed codes: `unknown_starting_weapon`/`unknown_starting_support`. Passive ids remain string-shape (5.4/Epic 6 still owns rules wiring).
- [Phase 7 — code review] Both 5.3 Decision items ratified as-is (no code change). FORWARD NOTE for Story 5.5: a tactical slice that consumes the live starting kit AFTER a route-position resume MUST re-derive the kit from `selected_class_id` via the kit-resolution helper — `restored_run.starting_kit` is null by design (the save persists class-id only). The validate-gate + execute-record double-resolution is intentional and ratified.

## Story 5-4-starting-passive-rule-integration
- [Phase 3 — create-story] HARD SCOPE BOUNDARY: 5-4 is the epic's first real rules-kernel/passive build (from the empty `scripts/rules/*` scaffold). v0 starting passives are EXPLANATION-ONLY — they resolve, seat a pure-read resolver, and are described; the felt-in-combat effect mutation (movement/targeting/damage operations + combat hook sites) is Story 5.5 / Epic 6, NOT 5.4. The AC2 wording ("modifies movement/targeting/damage…") must NOT be over-read into building the whole effect engine. New `PassiveDefinition`/`PassiveRepository` (cloned from Support* through ContentRepository); accessor `get_passive` (no reserved-name collision); passive ids sourced from `RunState.starting_kit` (5.3); fail-closed codes `unknown_class_passive`/`unknown_equipment_synergy_passive`; named-RNG-streams-only if any RNG is ever needed.
- [Phase 7 — code review] Both 5.4 Decision items ratified as-is (no code change). CONSOLIDATED 5.5 OBLIGATION: a tactical slice resuming a route-position save MUST re-derive BOTH (a) the `StartingKit` and (b) the `RulesResolver` from the restored `selected_class_id` — `restored_run.starting_kit` and `restored_run.rules_resolver` are both null-by-design (the save persists class-id only). v0 passives stay explanation-only; per-effect operations + combat hook sites are 5.5/Epic 6.
- [Phase 5 — dev-story] `RunStartCommand` now has THREE sequential fail-closed content gates inside the single selectable-class block: class -> kit -> passive. Each follows the same pattern: resolve in `validate()`, record/seat in `execute()` after the prior gate. A new starting-content type follows the same shape. Built the rules kernel under `scripts/rules/` (`triggers/rule_trigger.gd` = fixed ten-window vocabulary + `is_valid_window`; `resolver/rules_resolver.gd` = register-by-window + resolve + explain, pure read, zero RNG). `conditions/` and `operations/` stay empty Epic-6 scaffolding. `RunState.rules_resolver` is additive, NOT serialized, re-derived on restore (Shape A). v0 passives are explanation-only (no DomainEvent, no effect mutation).
