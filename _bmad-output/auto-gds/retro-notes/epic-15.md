# Epic 15 — Auto-GDS retro notes

## Story 15-1-hud-and-log-layout-clip-fix
- [Phase 5 — dev-story] The F1 "negative-x" symptom cannot be reproduced headlessly (verify-by-construction has no render pass), so the frame-border mitigation (light band backing replacing the nine-patch on short control bands) is a reasoned structural fix, not a pixel-confirmed one — real confirmation depends on the OSG-1 on-device pass.

## Story 15-4-quit-pause-resume
- [Phase 3 — create-story] A story titled as pure UI wiring ("Quit, Pause and Resume") hides a real domain-correctness fix: the shipped resume seat path (RunOrchestrator.start_from) seats a restored run with starting_kit == null, so CombatLoadout.for_run falls open to the 60 HP/sword default and a resumed Warrior/Pyromancer/Ranger silently loses its class loadout. 15.4 is the first story to feed a resumed run into live play, which is why the latent desync surfaces here — review should focus on the kit-intact test, not just button wiring.

- [Phase 5 — dev-story] A session-killed dev-story delegate can leave a compile-breaking half-edit that ONLY a suite run surfaces — here a test function called from run() whose body was never written. The WIP checkpoint looked complete but was one parse error from red. After resuming a killed phase, always run the suite as the first correctness gate, and specifically scan for referenced-but-undefined symbols in whatever file the dead delegate was mid-editing.
- [Phase 5 — dev-story] The Godot `--headless --import` pass rewrites existing `*.svg.import` sidecars with CRLF line endings and no content change, so they surface as modified with a zero-byte diff. Restore them rather than committing the churn.
- [Phase 7 — code review] A delegate's suite command can SILENTLY SELF-FALSIFY: piping `godot ... --path C:\Sealsworn\godot` through `tee` under bash strips the backslashes (→ "C:Sealsworngodot"), Godot aborts, and the pipeline still exits 0 because `tee` succeeded. A green exit code is therefore not evidence the suite ran. Use forward-slash paths and grep the CAPTURED OUTPUT for the pass count and the final banner — never trust a piped exit code.
- [Phase 7 — code review] A fix delegate can encode its own scope-cut into the test it writes: the D2 test labelled the surviving `none`-room reroll "the human-accepted deferred edge" and skipped the assertion, but the human's D2 direction was unconditional ("does NOT reroll it"). The label made a partial fix read as complete. It took an independent second-model pass over the fix commit to surface the gap between the delegate's label and the actual directive — evidence for keeping the alternate-model round on decision-fix commits specifically, not just on the original dev-story diff.
- [Phase 7 — code review] Inverting a previously-pinned contract trips more than the direct test: `test_seed_regression_suite.gd` re-runs canonical tests as a cross-check, so one obsolete assertion in `test_affinity_assignment.gd` surfaced as 2 failures. Anticipate the cross-check cascade when updating pinned behavior.
- [Phase 7 — code review] An API session/spend limit killed a review delegate that had done substantial verification but batched all its writes to the end — it persisted NOTHING and the whole round was lost. Review prompts should require incremental persistence (write each finding as it is confirmed); the retry with that instruction completed and persisted correctly.

## Story 15-3-threat-telegraphs
- [Phase 5 — dev-story] Deliberate scope call beyond the story's literal "pair by telegraph_id": the projection also clears a mark when its source enemy dies. Grep-verified the domain strands a mark on seer death (a dead enemy returns early in prototype_enemy_ai.gd:28 and no board sweep emits a clearing event), so killing the seer to avoid the blast would otherwise leave a lying "danger" glyph. Satisfies AC1's "or is cancelled" clause via the only cancellation the event log exposes — a pure event-log read, no new query/key/fingerprint.

- [Phase 7 — code review] The headless runner reports PASS per test FILE, not per test function — deleting a test function leaves the count flat. Worth remembering when setting expected-count guards for function-level test edits.

## Story 15-2-attack-preview-damage-correctness
- [Phase 5 — dev-story] Per the story's explicit "return expected_damage in the query success metadata", AttackCommand's transient ActionResult.metadata now also carries a benign expected_damage = base key (the command copies preview metadata). The event-log fingerprint is byte-identical (attack/damage payloads use explicit key lists that exclude it), so AC2/AC3 hold at the events/damage/RNG level — but the query-side-vs-VM-side placement of the fold is the one decision worth an auditor's eye.
- [Phase 3 — create-story] F4 is a direct consequence of Story 12.2 adding a resolution-side damage bonus without updating the paired read model — a "damage computed in two places" desync class. The AC2 regression test (preview == resolved per starting kit) is the new standing guard against it recurring as future bonus sources are added.
